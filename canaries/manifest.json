[
  {
    "id": "pinned-units",
    "patch": "canaries/pinned-units.patch",
    "expectedOracle": "no-pinned-or-oscillating-units",
    "playtestArgs": [
      "--max-ticks",
      "3000"
    ],
    "note": "UNIT_SUBGRID_STEP_PER_TICK 2 -> 0: every commanded unit freezes at spawn. NOTE: as of 2026-07-09 the UNPATCHED build already fires this oracle (16 real pinned-unit violations at this seed) so the drill reports canary-invalid - which is the drill working: fix the real violations first, then this canary resumes measuring sensitivity."
  },
  {
    "id": "match-never-completes",
    "patch": "canaries/match-never-completes.patch",
    "expectedOracle": "match-completes",
    "playtestArgs": [
      "--game-length",
      "500",
      "--max-ticks",
      "3000"
    ],
    "note": "Score timer threshold multiplied by 1000: the timer never fires, the inert human keeps its Town Center so conquest never fires, and the run ends at maxTicks instead of stopWhen. --game-length 500 makes the unpatched baseline complete (green) so the canary measures a real green->red flip."
  },
  {
    "id": "tick-failure",
    "patch": "canaries/tick-failure.patch",
    "expectedOracle": "no-tick-failures",
    "playtestArgs": [
      "--max-ticks",
      "3000"
    ],
    "note": "Conquest-outcome system throws once at tick 1500: exactly one recorded tick failure. Baseline is reliably quiet (normal runs record zero tick failures), so this canary stays valid regardless of open gameplay bugs."
  }
]
