# AoE2 coverage roadmap — from vertical slice to the full game

The goal is the complete, publicly-known Age of Empires II (Definitive Edition / HD) experience, not the current early-game slice. `design/spec-final.md` is the TARGET (the rules); this file is the prioritized GAP between the current implementation and that target, and it is the backlog the LLM-playtest loop works through (run → `playtest:findings` → implement → re-run). It is derived from a full simulation-coverage audit (2026-06-13) that grounded every "implemented" claim in running code.

As of 2026-06-15 the north-star also covers VISUAL fidelity and UI polish toward a perfect Age of Empires II HD clone (see M7), so the loop advances three tracks — simulation correctness, graphics realism, UI polish — picking whichever has the highest payoff per iteration. All visual work uses ORIGINAL/procedural art that evokes the AoE2 look (isometric view, textured terrain, wood-and-stone HUD); no copyrighted Age of Empires assets (sprites, textures, UI art, audio) are reproduced.

## Current state (audit headline)

A complete but **shallow** land-only 1v1 vertical slice: Dark → Feudal → Castle → Imperial all work, with a working economy, blacksmith + unit-upgrade tech, abstracted combat, monks, three win conditions, and a competent AI. Breadth coverage vs. the full game: **units 35/104, technologies 37/140, buildings 17/27, civ differentiation ~0/30, naval 0, victory 3/4.** The slice is real and playable — it is depth and breadth that are missing.

## Playtest findings log

- **campaign-7 (2026-06-14, on v0.1.27):** ground-truth replay showed the LLM agent **rushed and wiped out by the AI in the Dark Age** (its villagers fell 5→0, it never reached Feudal, 0 commands rejected) — so the iter-1/2 economy techs (gather-rate, carry) and planned farms are MID-game features the agent never reaches in this matchup. **Reprioritization: early-game survivability before mid-game depth.** First fix shipped: Palisade Walls enabled in the Dark Age (v0.1.28; they were wrongly gated to Feudal+barracks, leaving no Dark-Age defense). Open follow-ups surfaced by this run, to confirm on re-playtest: (a) does the agent actually wall/defend — the affordance places one building per command, with no "build a wall line" helper; (b) is the AI over-efficient in the Dark Age (it kept 6 villagers + 7 buildings while massing 9 military); (c) is Town Center defensive fire strong enough to protect nearby villagers (the agent retreated "under TC arrow cover" and still lost them). Re-playtest after the wall fix to find the next gap.
- **campaign-8 (2026-06-14, on v0.1.28, re-playtest after the wall fix):** the agent still loses villagers to a Dark-Age militia raid and **does not build the now-available palisade walls** (0 wall commands — it retreats villagers "under Town Center fire" instead). Ground-truth code reading found the real game bug behind the deaths: an **empty Town Center fired 0 arrows** (`buildingArrowCount` returned 0 unless garrisoned), violating spec §10.8 ("base attack") and inconsistent with the Castle — so a TC gives zero passive defense during the economy phase. **TC base-fire fix: DONE (v0.1.29).** Blast radius was verified ~25 (all real — a late Claude review's "mostly flaky, verified in isolation" claim was fabricated since that reviewer had no test runner; I measured it: baseline 1255-green → fix → 25 fail → all 25 fail in isolation, HP off by exactly 5). Fixed properly via the "make the change easy" sequence: restaged ~25 combat-isolation fixtures (13 files) so combat is clear of any TC's range (footprint-Manhattan ≤6 of the 4×4 TC), translating each combat group as a unit to preserve asserted distances; behavioral fixtures got bespoke fixes (ai-planner drops the human TCs so the AI's `humanTownCenterId` is null and its military stops churning vs TC fire → ages up + masses 5; ai-rush moves villagers out of TC cover; castle-town-center disables enemy AI + parks its TC far; autoAggression moves the human TC out of range). Then applied the one-liner (`buildingArrowCount('town-center') = 1 + min(garrison,4)`). Full suite 1255 green; Codex review SHIP no-findings (Claude/Gemini throttle-unreachable, re-review queued). Other remaining items: the agent doesn't wall/garrison well (LLM-strategy/affordance — a "build a wall line" helper would help; the agent may currently be too weak to surface mid-game gaps); AI Dark-Age efficiency still unconfirmed.
- **campaign-9 (2026-06-15, on v0.1.29):** **First playtest where the LLM human SURVIVES the Dark-Age rush.** Ground-truth replay (ticks 0/1000/2000/3000) shows the human grow 3→9 villagers + 5 buildings, alive at tick 3000 (both owners alive, stopReason `maxTicks`, $7.72), versus campaigns 7-8 where it was wiped to 0. The v0.1.29 empty-TC base arrow + v0.1.28 Dark-Age walls fixed the early-game death — validated by ground truth. New gaps surfaced: (a) the LLM human plays pure-economy with **0 military the entire game** — it turtles behind TC fire but never builds an army or ages up (an LLM-strategy limit, not a game bug); (b) **neither player reaches Feudal within 3000 ticks** — the AI ends Dark-Age with 6 villagers / 7 military but food-starved to 48 (it masses Dark-Age militia instead of saving for age-up), so a longer run (5000-8000 ticks) is needed to distinguish a too-short budget from a real AI age-up-prioritization gap; (c) the human's wood stays near 0 (it spends on buildings faster than its ~4 wood villagers gather — allocation/spend, not a confirmed gather bug). With early-game survival now fixed, the loop also opens the M7 graphics/UI track (new north-star). Next: a longer campaign to settle the age-up question, and M7 visual items.
- **campaign-9 replay deep-dive (2026-06-15) — finding (c) RESOLVED as not-a-bug; real lever found.** Replayed the actual campaign-9 bundle (engine debug tools first; see `docs/debugging/2026-06-15-campaign9-idle-villagers.md`). Wood gather is HEALTHY — at tick 2500 all 4 wood villagers are active across 2 distinct trees (v0.1.24/25 fan-out working) and wood rises at points; finding (c) "wood ~0" is just LLM allocation + spend, NOT a gather bug. The real economic drag: newly-trained HUMAN villagers default to `desiredResource='food'` but `hasExplicitGatherOrder=false`, and the deliberate `shouldMaintainGatheringOrder` gate (`pureHelpers.ts:316`) auto-assigns only AI villagers and human villagers WITH an explicit order — so untasked new human villagers idle at the TC despite abundant adjacent food (5 full berry-bushes + 2 boars within 6 cells). This is AoE2-faithful (new villagers idle until tasked/rallied), so NOT a sim bug, but it is the missing AoE2 anti-idle mechanism: a rally point set ON a resource that makes new villagers auto-gather it (`productionQueueSystem.ts:110-113` only issues a MOVE to the rally position). This likely drives finding (b) too (the AI is "food-starved" — same idle/under-gather dynamic). **Next fix (M1): rally-point-to-resource auto-gather** (below), then a longer campaign to re-measure age-up.
- **campaign-10 (2026-06-16, on v0.1.39 — first re-grounding since campaign-9, after 7 shipped features) — harness HEALTHY, v0.1.36 validated end-to-end, run too SHORT to reach the mid-game.** Clean run, NO hang (the long-session degradation didn't bite — that worry was overcautious), but the DEFAULT `--cost-budget=$5` capped it at 9 decisions / **2250 ticks** / $5.30 (each claude-opus-4-8 call ≈ $0.65), even shorter than campaign-9's 3000t. Ground-truth replay (`replay:inspect` ticks 0/750/1500/2250): both owners alive, **both still Dark Age at 2250t** — neither aged up, but campaigns 1-2 only reached Feudal ~3100-3500t so 2250t is simply too early (the LLM's food peaked ~280, never near the 500 Feudal needs). LLM-human (owner 1): **0 military the entire run** (campaign-9 gap confirmed — pure-economy turtle), 3→6 villagers, survived with NO enemy raid (enemyMilNearBase=0). AI (owner 2): massed military 2→6 + healthy food (301), also stuck Dark Age (not prioritizing age-up). **26 commands, 0 rejected, 0 stalls** — no missing-feature in what the LLM used. **The recent mid/late-game features (pop-cap-200 v0.1.37, farms v0.1.34/35) were NOT exercised** — a 2250t Dark-Age boom never reaches where they matter (pop ≤12, far under any cap; food still flowed, no farm need). **v0.1.36 annotation bridge VALIDATED end-to-end:** `playtest:findings` injected 3 agent markers @tick 2250 into the real bundle. The 3 probe findings, VERIFIED against code: (1) [high] "no age-up" → NOT a missing feature (age-up exists; campaigns 1-2 reached Feudal) — it's the too-short cost-capped run + weak age-up prioritization; (2) [med] "no research / Loom missing" → REAL: Loom is in `design/stats/technologies.csv:86` (Dark, Town Center, 50 gold, +15 HP / +1 melee / +2 pierce armor) but DATA-ONLY, not wired in `src/` (one of ~103 unimplemented techs) → **next fix**; (3) [med] "TC queue depth 1" → likely NOT a bug (the AI code carries a >1-2-capacity production queue with queue-full handling; the symptom is the LLM queuing one-at-a-time across sparse ~250t decisions, so the TC idles between them — an LLM-affordance issue, not a cap). **Operator note:** a real re-grounding (reach Feudal+ to exercise pop-cap/farms + settle the age-up question) needs `--cost-budget ~$15-18` + `--max-ticks 6000-8000`, best run ATTENDED on a fresh session. **Next loop fix: implement Loom** — the verified data-only Dark-Age tech — as the grounded, playtest-driven content addition (researchable at the TC for 50 gold, applying +15 villager HP + +1/+2 armor via the tech-effect layer). ✅ **DONE (v0.1.40, thread loom-tech):** Loom is now researchable at the Town Center (Dark Age onward, no prereq, 50 gold, 250t) and grants every owned villager (existing + future) +15 max HP (25→40, flat current+max bump) + +1 armor, applied imperatively to the per-villager combat state (mirroring the blacksmith techs; existing villagers bumped on completion, new ones via `combatStateFactory`). NO save-format change. Known divergence: AoE2 Loom is +1 melee / **+2 pierce**; this slice ships +1/+1 (the single `CombatState.armor` scalar that feeds both melee and pierce) — the extra +1 pierce is deferred to the asymmetric-armor-tech slice (M2 below). The dominant +15 HP survivability value ships fully correct.

## Three structural enablers (build these and the most content lights up)

1. **A stat-multiplier subsystem.** Its absence is why ~12 economy techs, all civ bonuses, and many upgrades are data-only. One layer (apply rate/cost/stat deltas from data to owned + future entities) unblocks the widest swath of content. — `[sim]`
2. **Sustained economy: pop cap → 200 and farm reseed.** TC/Castle pop supply (✅ v0.1.23), a buildable, depleting Farm (✅ v0.1.34 slice 1), farm auto-reseed (✅ v0.1.35 slice 2 — a depleted farm refills for 60 wood while the owner can pay), and the 200 population ceiling with correct over-housing/destruction tracking (✅ v0.1.37 — honest raw-supply sum, `cap = min(200, supply)`, over-cap-without-eviction on housing loss) have landed, so a long game no longer hard-starves, a maintained farm gives continuous food, and the cap now scales with housing to the standard 200. The remaining mid/late-game blockers: the farm-capacity upgrade techs + the Mill batch-reseed queue. — `[sim]`/`[sim+data]`
3. **Data-driven combat: melee/pierce armor classes + class-based bonus damage.** Real counter play (skirms↔archers, halbs↔cav, pierce-immune buildings) needs spec §10's class system wired from the CSV columns instead of one armor value + hard-coded type checks. — `[sim]`

## Milestones (ranked; each item validated by tests + a playtest re-run)

Tags: `[sim]` simulation logic here · `[data]` CSV/content wiring · `[engine]` may need civ-engine primitives.

### M1 — Make a sustained match possible
- **Population supply → AoE2-correct.** ✅ done (v0.1.23): Town Center +5, House +5, Castle +20 (were 0); cap fully building-derived (base 0, the starting Town Center supplies the opening 5). `[sim]`
- **Population limit of 200 + correct over-housing/destruction.** ✅ done (v0.1.37). PopulationState gained an honest, unclamped `rawSupply` running sum; the cap is DERIVED as `deriveCap(rawSupply) = min(200, max(0, rawSupply))` at all four cap-mutation sites (entity create/destroy + the construction-flow completion) and on save-load. Over-housing past 200 adds raw supply but not cap; losing housing while raw supply stays ≥ 200 keeps the cap at 200; only a sub-200 supply drops the cap (fixing the Codex population-model iter-1 HIGH — clamping the stored cap would wrongly drop 200→195 on a House loss at supply 245). The old `Math.max(current, …)` don't-evict FLOOR was removed: losing housing can now leave you over cap (current > cap) with NO eviction — existing units survive, training is blocked until current < cap. Save migration is additive (legacy `{current,cap}` loads with `rawSupply = cap`, re-clamped; no schema-version bump). Deferred: a configurable cap / "no pop limit" lobby option (the deriver already takes the hard cap as a parameter), civ-specific cap modifiers, and an AI tweak to stop building Houses once it is capped at 200 (currently the AI keeps proposing useless Houses at the ceiling — bounded and unreachable at current game scale). See `docs/threads/done/pop-cap-200/`. `[sim]`
- **Rally-point-to-resource auto-gather.** ✅ done (v0.1.31). `productionQueueSystem` now gives a villager trained from a building whose rally point sits on a harvestable resource that resource's gather intent (`desiredResource` + `hasExplicitGatherOrder`), and `villagerEconomySystem` routes it to the nearest matching node — so a freshly-trained villager works instead of idling at the rally cell. The spec §9.3 "set rally point" order now carries full AoE2 semantics. Follow-on note (2026-06-15): the once-planned "surface idle-villager count to the agent" affordance is NOT needed — the agent snapshot already exposes `villagerCountByTask` (incl. `idle`) + per-unit `task` (`agentSnapshot.ts`) and the LLM already has a set-rally-point tool (`llmPromptBuilder.ts`). So the campaign-9 idle-villager drag is an LLM-PLAYER-STRATEGY limit (it sees idle villagers but doesn't re-task/rally them), not an affordance gap; rally→gather helps only if the LLM (or a human) sets a resource rally. `[sim]`
- **Farms + reseed.** Slice 1 ✅ done (v0.1.34): a villager-BUILT Farm (Dark Age, 60 wood, 1×1, 480 HP, 15-second build) that becomes a gatherable 175-food resource on construction-complete (a building+resource HYBRID — gains a `resource` component so `villagerEconomySystem` harvests food from it at a berry-bush cadence). Slice 2 ✅ done (v0.1.35): farm auto-reseed-if-affordable — when a farm's food hits 0, if its owner has ≥60 wood the farm refills to 175 in place for 60 wood (same entity, instant, gathering continues); when the owner can't pay it is removed from the map entirely. The renewable-food backbone now sustains itself: a maintained farm gives continuous food as long as you can pay the wood. Deferred: the AoE2 Mill batch-reseed QUEUE + a per-farm manual reseed toggle, the farm-upgrade techs (Horse Collar / Heavy Plow / Crop Rotation → 250/375/550 food, once the multiplier layer covers farm-capacity), a Mill anchor requirement, a dedicated farm sprite, and the AI building farms. See `docs/threads/done/farms-m1-slice1/` and `docs/threads/done/farm-reseed-m1-slice2/`. `[sim+data]`
- **Stat-multiplier subsystem + economy upgrades.** Gather-rate / carry / villager-speed multipliers applied from data; lights up Wheelbarrow, Hand Cart, Double-Bit Axe, Bow Saw, Two-Man Saw, mining techs (~12 techs). `[sim]`

### M2 — Real combat & counters
- **Melee/pierce armor split + data-driven bonus damage.** Slice 1 ✅ done (v0.1.32): melee/pierce split — pierce attackers (archers/skirmishers/siege/tower arrows) reduced by the target's pierce armor (`UNIT_PIERCE_ARMOR` from `units.csv`), melee by melee armor; `combatDamageAfterArmor`/`unitAttackType`/`unitPierceArmor` + the 2 unit-target damage sites. Slice 2a ✅ done (v0.1.33): base MELEE armor from the CSV (`UNIT_MELEE_ARMOR` + `effectiveMeleeArmor`, mirror of the pierce side — cavalry line 2, champion/treb/HCA 1, bombard 2, rest 0); melee damage = base melee + tech bonus. Slice 2b-i ✅ done (refactor, no bump): the hard-coded `attackBonusAgainstUnit`/`attackBonusAgainstBuilding` ladders are now declarative data tables (`UNIT_ATTACK_BONUS_RULES` + `BUILDING_ATTACK_BONUS`, behaviour-preserving, parity-tested) — the data seam for value changes. Slice 2b-ii (the big one, next): adopt the AoE2-accurate CSV bonus VALUES (skirm +3, spear-line vs-cavalry, eagle/siege bonuses where the unit exists) + split which armor techs feed melee vs pierce — a balance change with wide fixture impact. NOTE: the CSV bonus column is free-text prose lacking unit→class memberships, so transcribe roster-relevant values (don't write a runtime parser); see `docs/threads/current/data-driven-combat-2b/PLAN.md`. **Asymmetric-armor-tech follow-up (carried by this slice):** splitting `CombatState.armor` into separate melee/pierce values is the enabler for asymmetric armor techs. Loom (v0.1.40) ships +1/+1 today because of the single scalar; once the split lands, restore Loom to its AoE2 +1 melee / +2 PIERCE (the deferred +1 pierce — see spec §11.8). `[sim]`
- **Projectiles.** Travel time, accuracy rolls, missed shots, and Ballistics/Thumb-Ring; archers/siege/towers fire real projectiles instead of instant HP subtraction. `[sim]`
- **Elevation + blast/splash damage.** 1.25×/0.75× by elevation; mangonel/onager/scorpion area damage via the unused `blast_radius`. `[sim]`

### M3 — Civilization identity
- **Civ-effect application layer.** Read `civilization_bonus` / `team_bonus` and apply cost/rate/stat deltas; per-civ Castle UU gating beyond the Britons special-case; unique techs. 29/30 civs are currently identical. `[sim+data]`

### M4 — Content breadth
- **Unique units beyond Longbowman** (~17 UU + elites; Castle-gated by civ, several with special mechanics). `[data+sim]`
- **University + its techs** (Ballistics, Masonry, Murder Holes, Treadmill Crane, Architecture, Bombard Tower) — needs a building-mutation system (HP/armor/LoS/range). `[sim+data]`
- **Monastery techs** (Redemption, Sanctity, Faith, Block Printing, Herbal Medicine, Theocracy, …) + faith/probability conversion instead of the fixed ramp. `[data+sim]`
- **Tower line / gates / wall upgrades** (Guard Tower, Keep, Bombard Tower; gates; Fortified Wall) so defensive play can develop. `[data+sim]`

### M5 — Naval layer (large)
- **Dock + ships + fishing ships + transport + naval trade** (~18 ships + Dock + dock techs are entirely data-only; water tiles exist but nothing floats). Blocks water maps and transport play. `[sim+data]`

### M6 — Match completeness & control
- **Score-timer victory.** A timeout where highest score wins, so stalemates end (score is currently display-only). `[sim]` (Until this lands, the playtest-corpus CI smoke sets `matchCompleteRequired: false` — deterministic matches stalemate at the tick cap; re-enable the match-completion oracle when score-victory makes matches actually end.)
- **Stances / formations / attack-move / patrol.** UnitCommand is move/build/attack only; auto-aggression is hard-coded. `[sim]`
- **Repair.** Villagers repairing buildings & siege (no handler today). `[sim]`
- **AI depth.** Siege production (so it can break walls/Castles), team play (trade/tribute/joint defense), and difficulty-differentiated tactics (currently only cadence/gather-rate differ). `[sim]`

### M7 — Graphics & UI (AoE2 HD look)
Tags: `[render]` Phaser rendering (`src/phaser/`) · `[ui]` DOM/CSS HUD (`src/ui/`, `src/styles.css`). Baseline (2026-06-15 audit): top-down orthogonal grid, fully PROCEDURAL — units were tinted circles (now per-role silhouettes, v0.1.41), buildings shared one generic rounded-rect body + roof triangle (now per-role silhouettes, v0.1.42), terrain flat-color cell fills (now textured per-cell v0.1.30 + kind-to-kind edge blending v0.1.43), no sprites/textures/animation; the DOM HUD is on a warm gold-on-dark-teal palette (now framed wood/stone chrome + glyph icons, v0.1.38/39). Original/procedural art only — evoke the look, reproduce nothing.
- **Isometric projection.** The single biggest lever: replace the orthogonal (x,y)→(px,py) map with an iso transform across `GameScene`, camera bounds, minimap, and hit-testing. Foundation for everything below. `[render]`
- **Units beyond colored circles.** Slice 1 ✅ done (v0.1.41, thread units-beyond-circles): units render as READABLE PER-ROLE procedural silhouettes oriented to their movement FACING, instead of flat tinted circles. New `gameScene/unitRenderer.ts` (extracted like terrainRenderer/buildingRenderer so the pinned-legacy `GameScene` stays net-zero, 1018→1017): a pure exhaustive `unitRole` maps all 34 unit types to 7 render roles (villager / infantry / archer / cavalry / cavalry-archer / siege / monk — `skirmisher→archer` is render-owned, independent of the sim's archer-line class); `unitFacingRadians` derives the heading purely render-side from the per-tick projected-position delta the scene already tracks for interpolation (so NO sim/bridge/contract change — idle units fall back to a consistent rest orientation, no fabricated heading); `drawUnit` paints 2-4 Phaser primitives per role with the owner-color tint as the body fill, all kept inside the unit's cell-centred bounding circle so the existing health-bar + selection-ring geometry is unchanged. 100% original procedural Phaser graphics (no sprites/assets). Verified per the visual protocol — a test-only `unit-showcase-fixture` (one P1 unit per role) captured before/after/pixel-diff (`tmp/units/`, 0.29% px changed, confined to the seven unit positions; HUD/TC/terrain/health-bars/minimap/fog pixel-identical) — plus the four gates green and a 35-test `unitRenderer.test.ts`. **Deferred to a later slice (still open): a walk/idle ANIMATION cycle, and a distinct silhouette per individual unit TYPE (vs per role).** `[render]`
- **Projectile rendering.** Draw arrows/siege shots in flight (combat is currently instant HP subtraction) — pairs with the M2 projectile sim. `[render]`
- **Building visuals.** Slice 1 ✅ done (v0.1.42, thread building-visuals-slice1): COMPLETED buildings render as distinct per-ROLE procedural silhouettes instead of the one generic body+roof every type shared (buildings were truly generic — the renderer never read `entityType`, only footprint size + owner tint differed). New `gameScene/buildingRole.ts` (a pure exhaustive `satisfies Record<BuildingType, BuildingRole>` map, 18 types → 13 readable roles) + `gameScene/buildingSilhouettes.ts` (the per-role draw fns); `gameScene/buildingRenderer.ts` slimmed to an orchestrator (construction-scaffold + last-seen-ghost paths kept byte-identical — only the completed body+roof varies by role). Roles: town-center hall, fortress/castle (crenellated keep), wonder (dome+spire), house, mill (windmill blades), farm (furrow rows), drop-site/lumber+mining-camp (lean-to+stockpile), military/barracks+stable+archery-range+siege-workshop (banner hall), blacksmith (anvil+chimney), market (striped awning), monastery (cross finial), tower/watch-tower (battlement cap), wall/stone+palisade (low merlons). Body fill = the existing per-type+owner `buildingTint`; every primitive stays inside the footprint rect so the HP-bar / selection-ring / footprint-outline geometry is unchanged; the `BuildingRendererVisualState` flag contract is preserved (completed→body/roof/completion, construction→foundation/scaffold). 100% original procedural Phaser graphics (no sprites/assets), pure per-frame (no random/time). Verified per the visual protocol — a test-only `building-showcase-fixture` (one completed P1 building per role) captured before/after/pixel-diff (`tmp/buildings/`, 10.72% px changed, confined to the building footprints; HUD/terrain/health-bars/selection/minimap pixel-identical) — plus the four gates green and a 27-test `buildingRenderer.test.ts`. **Deferred to a later slice (still open): a construction→complete progress animation/fill (the render data carries only a binary `complete`/`construction` variant today, not a fraction — a fraction would need a bridge/projection change), a distinct silhouette per individual building TYPE (vs per role), rubble/damage states, and per-civilization architecture.** `[render]`
- **Terrain texturing + elevation shading.** Per-cell procedural brightness variation **DONE (v0.1.30)** — `gameScene/terrainRenderer.ts` applies a deterministic ±7% hue-preserving jitter so same-kind cells no longer read as one flat block. Blended kind-to-kind transitions **DONE (v0.1.43, thread terrain-blending):** adjacent cells of different kinds (grass/forest/water/hill) no longer meet at a hard rectangular seam — for each cell edge whose von-Neumann neighbour is a different kind, the renderer stipples a deterministic feather band of small specks on the inner side of the edge tinted with `blendTint` (the two kinds' base-tint average); the neighbour feathers back symmetrically so the two bands interlock into a soft dithered transition. Pure per-frame draw (deterministic from cell coords, no random/time); neighbour kinds read from `entity.entityType` via a WeakMap-memoized per-frame kind-grid (the projection already sets terrain `entityType=kind` — render-only, NO sim/bridge/contract change); v0.1.30 jitter + 1px overdraw preserved; specks stay inside the cell rect; the blend renders under all entity/fog/selection/grid overlays. Verified by TDD (18 tests) + the visual protocol (test-only `terrain-showcase-fixture`, before/after/pixel-diff `tmp/terrain/` 1.89% confined to kind boundaries) + four gates green (1416/2). See `docs/threads/current/terrain-blending/`. **Deferred: light/shadow by ELEVATION** — the render data does not project per-cell elevation onto `ProjectedEntityView` (only the terrain kind is projected), so elevation shading would require a simulation/render-contract change and is left for a later slice; also deferred are diagonal (corner) blends and bespoke per-pair transition textures (e.g. a beach gradient for grass↔water). `[render]`
- **Combat/gather feedback + selection polish.** Impact/death particles, gather sparks, and a selection glow/pulse. `[render]`
- **HUD chrome.** ✅ done (v0.1.38). The major HUD container panels (top resource bar, command/selection panel, minimap frame, footer) now present as framed wood-and-stone surfaces via 100% procedural CSS — a wood-plank gradient grain on the resource bar / command panel / footer, a stone-fleck radial gradient on the minimap frame, a beveled raised edge (light top-left + dark bottom-right inset box-shadows) + an inner recess on all, and a sunken inset on the minimap canvas. New `src/hudChrome.css` (imported after `styles.css`) repaints ONLY the panel chassis: layout, the warm gold-on-dark-teal palette (every colour palette-faithful to the existing gold/teal family — frame accents tokenized in `:root`, base/fleck stops inline), buttons/chips/icons, and `src/phaser/` are untouched. CSS-only (no code/HTML change; the HUD controller selects by `data-hud`, so it is contract-safe). Verified per the visual protocol — before/after/pixel-diff showed 29.34% of pixels changed, all on the panel chassis, with the Phaser canvas and panel text/buttons pixel-identical. See `docs/threads/done/hud-chrome/`. `[ui]`
- **UI icons + minimap/fog polish.** Original SVG/CSS unit/command/build icons (replace text+color dots), a terrain layer on the minimap, and a fog gradient fade. `[ui]`
  - **Resource + build icons.** ✅ done (v0.1.39). The top-bar resource chips and the "Build X" command buttons now carry small ORIGINAL hand-authored inline-SVG glyphs (wheat sheaf / logs / coin / rock for the four resources, plus banner / house / clock for age / pop / time; and a matching glyph per buildable building — house, windmill, axe, pickaxe, swords+shield, tower, anvil, stall, catapult, chapel, keep, dome, wall, palisade stakes, field — plus Town Center / Stable / Archery Range; an exhaustive per-building table covers every buildable type). Icons AUGMENT the text labels (the labels are kept). New `src/ui/hud/icons/glyphs.ts` (`resourceGlyph`/`buildingGlyph`, `stroke=currentColor`, zero `url()`/`<image>`/font/asset ref) + `src/hudIcons.css` (sizing/tone only, palette stays in the existing gold tokens). Contract-safe: the resource chip glyph is a sibling of the value element (the value holds only the number) and the build glyph sits before the preserved "Build X" text inside the `data-command` button. Verified per the visual protocol (before/after/pixel-diff in `tmp/ui-icons/`, change confined to the resource bar + Build-button panel, Phaser canvas pixel-identical) + four gates green (full test 1323/2). See `docs/threads/done/ui-icons/`. `[ui]`
  - **Selection-panel unit + building icons.** ✅ done (v0.1.44, slice 2). The SELECTION-panel badge (previously a bare two-letter text code — "V" Villager, "TC" Town Center, "H" House, "CB" Crossbowman, …) now sits beside an original procedural glyph: UNITS get a per-render-ROLE glyph (villager / infantry / archer / cavalry / cavalry-archer / siege / monk, the same 7 roles as the on-map `unitRenderer`), and BUILDINGS reuse slice-1's per-type `buildingGlyph`. New `src/ui/hud/icons/unitGlyphs.ts` (`unitGlyphRole`/`unitRoleGlyph`/`selectionGlyph` dispatcher; `buildingGlyphWithClass` + `isBuildableBuildingType` added to `glyphs.ts` to reuse the building art under the selection sizing hook) + a `.hud-selection-unit-glyph` rule in `hudIcons.css`. AUGMENT not replace: the glyph is a SIBLING before the badge `<div>` (mirroring slice-1's value-sibling), so the badge keeps its exact two-letter code + every `data-selection-(unit|entity)-icon`/`-count`/`-label` hook — every Playwright `toHaveText('V')`/`'TC'` badge assertion still passes. The unit role map is parity-tested against `unitRenderer.unitRole` (drift guard) while production code stays decoupled from the Phaser layer. Resources/wildlife/relic selection badges stay text-only (out of this slice's scope). DOM/CSS-only — no `src/phaser/`/sim/save/contract touch. Verified per the visual protocol (villager + Town Center + multi captured before/after/pixel-diff in `tmp/ui-icons-2/`, change confined to the selection panel ≈4.8% single / ≈8% multi, Phaser canvas pixel-identical) + four gates green (full test 1429/2). See `docs/threads/current/ui-icons-slice2/`. `[ui]`
  - **Still open:** the non-build command-card icons (train / action / market / research buttons still text-only), the resource / wildlife / relic selection badges (still short text codes in `displayNames/icons.ts` — not player units or buildings), a terrain layer on the minimap, and a fog gradient fade. `[ui]`

## How the loop drives this

Each milestone item is a self-contained change: spec update (the rule), TDD, multi-CLI review, commit, then a playtest re-run whose `playtest:findings` either confirms the gap closed (the `unknown-kind` rejection for that command disappears; the agent uses the new capability) or surfaces the next gap. Cross-cutting note from the audit: nearly every gap is simulation-logic work in this repo, not a civ-engine limitation.

**Loop-strategy note (2026-06-15):** the LLM player reliably STALLS in the Dark Age (turtles with 0 military; leaves villagers idle even though the snapshot surfaces idle counts) — a player-strategy limitation, not a game/affordance gap. So an LLM campaign currently can't reach (and thus can't surface gaps in) Feudal/Castle combat, age-up, or the mid/late game. Two consequences for picking the next item: (1) gap-finding for deeper phases should be DECOUPLED from LLM skill — build known roadmap gaps and validate them with deterministic scenario fixtures (the repo already has combat/siege/castle/age fixtures) + the `playtest:findings` AoE2-rules critique, rather than waiting for the LLM to play there; (2) a fresh LLM campaign is still the right periodic re-grounding, but run it on a FRESH session (the subscription degrades over a long session — campaigns slow/hang) and treat its value as "did early-game survival/economy improve" rather than "did it reach the late game".

M7 (graphics/UI) items validate differently — they are visual, not rule changes — so they follow the AGENTS.md visual-change protocol: capture a before screenshot, apply the change, capture an after screenshot, and use a pixel diff as the verification alongside the normal test/build gates (rendering changes must not break the bridge contract tests). They still get the multi-CLI review and the devlog/spec/changelog discipline where applicable.
