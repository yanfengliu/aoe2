## [2026-04-10 20:29, America/Los_Angeles] - Initialize AoE2 gameplay spec
**Action:** Wrote a clean-room, implementation-oriented gameplay specification for an Age of Empires II-compatible RTS and initialized the repo devlog files required by local project guidance.
**Result:** Added a complete spec covering match structure, world rules, economy, combat, civilizations, maps, UI, AI, determinism, and data schemas. Also created summary and detailed devlog files so future work has a documented trail.
**Files changed:** `design/aoe2-game-spec.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The repository is effectively empty, so the most useful first artifact is a single source-of-truth design doc that defines the target game before engine or content work starts. Initializing the devlog now avoids future agents violating the repo workflow in `CLAUDE.md`.
**Notes:** The spec deliberately targets the classic `Age of Kings + The Conquerors` baseline instead of later Definitive Edition content to keep scope stable. No architecture document was added because this change is documentation-only and does not introduce code structure yet.

## [2026-04-10 20:50, America/Los_Angeles] - Expand AoC spec and document research sources
**Action:** Rewrote `design/spec-codex.md` into a more concrete `The Conquerors 1.0c` design spec, added building-by-building production and technology coverage, expanded the civilization section into a classic 18-civilization catalogue, and converted `design/sources.md` into a real research log.
**Result:** The spec now pins to a specific classic baseline, corrects major version drift from the prior draft, references the local structured CSVs under `design/stats`, and gives future implementers a clearer roster definition for buildings, units, upgrades, and civilization identity. The source file now records both external research and local supporting artifacts.
**Files changed:** `design/spec-codex.md`, `design/sources.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The earlier draft was too high-level for concrete implementation and mixed some non-classic assumptions. The repo now contains structured local data tables, so the right move was to align the prose spec to a strict AoC baseline while using the CSVs as the exact stat and availability companion.
**Notes:** The local CSVs mix classic and later-expansion data, so the prose spec intentionally filters back to the classic roster and mechanics. This was a documentation-only change; no code or tests were run.

## [2026-04-10 20:59, America/Los_Angeles] - Retarget spec to DE and align with local stats
**Action:** Reworked `design/spec-codex.md` from an AoC-focused roster dump into a DE-targeted systems spec that treats `design/stats` as the canonical content source, and rewrote `design/sources.md` to reflect the new source hierarchy and official DE references.
**Result:** The spec now explicitly targets AoE2 DE, removes stale AoC-only assumptions, reduces redundant prose duplication of civ and tech data already present in CSVs, and documents the current gap between the local stats bundle and full live-DE content. The sources file now prioritizes local stats first, official DE sources second, and keeps older AoC references only as historical context.
**Files changed:** `design/spec-codex.md`, `design/sources.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user clarified that the intended target is DE and that `design/stats` is now the source of truth. Given that, the previous hand-maintained AoC-centric prose was both redundant and partially incorrect. The revised spec defines the systems and data-interpretation rules while letting the structured data own the concrete roster.
**Notes:** The local data currently covers 30 unique civ rows with duplicates and a mostly classic unit and tech bundle, so the spec calls out dataset incompleteness instead of guessing missing DE content. This was a documentation-only change; no tests were run.

## [2026-04-10 21:08, America/Los_Angeles] - Split source log into indexed folder
**Action:** Split the single-file research log into a `design/sources/` folder with a top-level index and topic-specific source files, converted `design/sources.md` into a compatibility pointer, and updated the spec to reference the new source-file locations.
**Result:** The source documentation is now organized by policy, local data, official DE references, community and historical references, and findings. The old `design/sources.md` path still resolves for historical references in prior devlog entries, while current documentation can navigate through `design/sources/README.md`.
**Files changed:** `design/sources.md`, `design/sources/README.md`, `design/sources/01-policy.md`, `design/sources/02-local-data.md`, `design/sources/03-official-de.md`, `design/sources/04-community-and-history.md`, `design/sources/05-findings-and-guidance.md`, `design/spec-codex.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The sources file had grown into several distinct concerns. Splitting it reduces merge pressure, makes the official and local source hierarchy easier to scan, and preserves backward compatibility for existing path references instead of leaving historical links dead.
**Notes:** The spec still contains one pre-existing garbled quoted heading string in the DE section; the folder split itself is complete and the source references now resolve through the new index structure. No tests were run because this was a documentation-only change.

## [2026-04-10 21:11, America/Los_Angeles] - Narrow scope to single-player AI skirmish
**Action:** Removed multiplayer and campaign-story assumptions from `design/spec-codex.md` and updated the official DE source notes so they only justify the current single-player skirmish scope.
**Result:** The spec now targets a single-player Random Map experience with one human player fighting AI opponents, allows optional AI allies, and explicitly marks multiplayer, ranked flows, replay requirements, and campaign story mode as out of scope. The simulation section no longer specifies networking or replay behavior.
**Files changed:** `design/spec-codex.md`, `design/sources/03-official-de.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user clarified that the current product scope should be limited to fighting AI in single-player. Keeping multiplayer and campaign requirements in the spec would create false implementation pressure and muddy prioritization.
**Notes:** This was a documentation-only change; no tests were run.

## [2026-04-10, UTC] — Write independent AoE2 DE game spec (spec-claude.md)
**Action:** Created `design/spec-claude.md` — a comprehensive, implementation-ready game specification for Age of Empires II: Definitive Edition. Combined local CSV data (`design/stats/`) with extensive online research into combat mechanics, economy systems, map generation, building mechanics, tech trees, and UI/networking architecture.
**Result:** Complete 17-section spec covering: game overview, match structure (7 game modes, 4 victory conditions), world/map generation (8 map types, terrain system, resource placement), economy (gather rates, trade formulas, market mechanics, tribute, relics), age progression, buildings (economy/military/defensive with all stats), units (all categories with upgrade chains), combat system (damage formula, armor classes, accuracy, elevation, blast damage, garrison arrows, conversion, healing), technology system (all blacksmith/university/monastery/dock/TC/castle techs), civilizations (30 civs across 5 expansions with bonuses), fog of war, pathfinding, AI behavior, UI layout, deterministic simulation model, multiplayer networking, and audio/visual design. Includes key formulas appendix.
**Files changed:** `design/spec-claude.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user requested an independent spec (separate from `spec-codex.md`) based on original research. The spec was built by reading all local CSV data files first, then conducting three parallel research threads (combat mechanics, economy/maps, buildings/tech tree) using web sources to fill in implementation-level detail not captured in the CSVs.
**Notes:** This spec targets the full DE product including multiplayer, unlike `spec-codex.md` which was narrowed to single-player AI skirmish. The spec defers to `design/stats/*.csv` for concrete numeric values and defines the rules/formulas that operate on that data.

## [2026-04-10, UTC] — Narrow spec-claude.md to single-player vs AI
**Action:** Removed multiplayer networking section (§16), lobby system, spectator mode, peer-to-peer lockstep, disconnect handling, and replay system from `design/spec-claude.md`. Reframed all remaining references from multiplayer to single-player with AI opponents/allies. Simplified simulation model section to tick-based simulation without lockstep networking concerns. Added scope declaration to header. Renumbered Audio and Visual from §17 to §16.
**Result:** Spec now has 16 sections (down from 17). All "ally" references now specify "AI ally". Trade, tribute, and team bonus mechanics retained but scoped to AI partners. Game setup replaces lobby system.
**Files changed:** `design/spec-claude.md`, `docs/devlog-detailed.md`
**Reasoning:** User clarified the product scope is single-player only — one human player vs AI opponents. Multiplayer and campaign story modes are out of scope.
**Notes:** Documentation-only change. No tests run.

## [2026-04-10, UTC] — Remove non-standard game modes from spec-claude.md
**Action:** Removed Death Match, Regicide, Empire Wars, King of the Hill, Wonder Race, and Capture the Relic game modes from `design/spec-claude.md`. Kept only Random Map (Standard) mode. Simplified Spies/Treason tech to Spies only (Treason is Regicide-specific).
**Result:** Section 2.1 now describes only the standard Random Map mode. All mode-specific mechanics (King unit, Monument, pre-placed buildings) removed.
**Files changed:** `design/spec-claude.md`, `docs/devlog-detailed.md`
**Reasoning:** User requested only the standard Random Map mode be kept in scope.
**Notes:** Documentation-only change. No tests run.

## [2026-04-10 21:34, America/Los_Angeles] - Clean up local stats integrity issues
**Action:** Corrected obvious integrity problems in `design/stats`: removed duplicate civilization rows, fixed civ and tech spelling/canonical-name drift, fixed the `Archer` hit-point/range field swap in `units.csv`, and normalized one semicolon-delimited civ token with stray whitespace. Updated `design/spec-codex.md` and `design/sources/05-findings-and-guidance.md` so they no longer document already-fixed duplicate-row issues.
**Result:** The checked-in stats bundle is internally cleaner and less likely to break string-based joins during import. `civilizations.csv` now has unique civ names, the Barracks and Stable upgrade chain names line up with `units.csv`, and the most obvious bad unit stat row is corrected. The remaining gap is broader DE roster completeness: several later-expansion civ references still point at units and unique techs that are not yet represented in `units.csv` or `technologies.csv`.
**Files changed:** `design/stats/civilizations.csv`, `design/stats/technologies.csv`, `design/stats/units.csv`, `design/spec-codex.md`, `design/sources/05-findings-and-guidance.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user asked to fix data problems in the local stats bundle. The safest high-confidence pass was to resolve verified duplicates, malformed text, canonical-name mismatches, and clearly wrong local values without inventing missing late-expansion DE content.
**Notes:** No tests were run because this was a data and documentation cleanup pass. A follow-up sourced content pass is still required if the repo wants the later-expansion civ rows to be fully backed by local unit and technology records.

## [2026-04-10 21:37, America/Los_Angeles] - Restrict specs to standard Random Map mode
**Action:** Removed the remaining non-standard mode assumptions from `design/spec-codex.md` and tightened one leftover game-setup line in `design/spec-claude.md` so the specs no longer imply a selectable game-mode roster.
**Result:** `spec-codex.md` now defines a single standard match-setup path, removes Regicide/Wonder-capable preset requirements, and narrows victory support language to the standard Random Map ruleset. `spec-claude.md` no longer tells implementers to expose a generic game-mode selector in setup.
**Files changed:** `design/spec-codex.md`, `design/spec-claude.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user clarified that only standard mode should remain in scope. Leaving special-mode setup hooks in the spec would create false implementation requirements and conflict with the single-mode product target.
**Notes:** Documentation-only change. No tests were run.

## [2026-04-10 21:53, America/Los_Angeles] - Consolidate working specs into official final spec
**Action:** Read `design/spec-codex.md` and `design/spec-claude.md`, merged their compatible content into a new canonical document at `design/spec-final.md`, and updated `design/sources/README.md` so the source index now points at the final spec instead of the older draft.
**Result:** The repo now has a single official spec that keeps the strong source-of-truth and normalization rules from `spec-codex.md` while incorporating the fuller gameplay, economy, combat, UI, AI, and simulation guidance from `spec-claude.md`. The merged doc keeps standard Random Map single-player scope, avoids duplicating CSV-owned roster data, and adds a concise appendix of implementation-critical formulas and constants.
**Files changed:** `design/spec-final.md`, `design/sources/README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user asked for one official spec instead of two parallel documents. Consolidation removes ambiguity about which draft is authoritative and reduces the risk of future design drift between overlapping markdown files.
**Notes:** `design/spec-codex.md` and `design/spec-claude.md` are retained as precursor drafts, but `design/spec-final.md` is now the canonical spec. No tests were run because this was a documentation-only change.

## [2026-04-10 22:06, America/Los_Angeles] - Add implementation plan for building the game
**Action:** Wrote `design/implementation-plan.md` as the execution plan for turning `design/spec-final.md` into a game, chose the default browser stack (`TypeScript`, `Vite`, `Phaser 3`, DOM HUD, `civ-engine` simulation core), defined the repository shape, phased milestones, testing strategy, and risk controls, and updated `README.md` to surface the official spec and implementation plan.
**Result:** The repo now has a concrete greenfield delivery plan instead of only a rules spec. The plan explicitly sequences the work from project bootstrap and content normalization through playable economy, Feudal/Castle/Imperial slices, AI, save/load, and stability. It also frames the project correctly as a proof that `civ-engine` can support an AoE2-style RTS.
**Files changed:** `design/implementation-plan.md`, `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user asked for an implementation plan document, not more design prose. The right output is a plan that names the stack, boundaries, milestones, and validation gates so implementation can start without reopening foundational architecture questions.
**Notes:** This was a documentation-only change. No tests were run.

## [2026-04-10 22:49, America/Los_Angeles] - Bootstrap Phaser and civ-engine prototype
**Action:** Started implementation against the plan by scaffolding a `TypeScript` + `Vite` + `Phaser 3` project that consumes the sibling `civ-engine` package. Added a simulation bridge, Phaser scene, HUD controller, app bootstrap, content build and validation scripts, generated content output, initial content tests, and repo metadata updates. Fixed the first integration issues surfaced by verification: moved Phaser graphics creation into scene lifecycle code, removed an unused civ-engine import, normalized duplicate starter-unit rows in the content pipeline, classified non-trainable unit-state rows, corrected the generated-content output path to stay inside the repo, and documented current engine findings.
**Result:** The repo now builds into a working browser prototype instead of only documentation. `civ-engine` owns a deterministic 10 TPS simulation for a 36x24 prototype world, `Phaser` renders terrain/buildings/units and handles camera movement, the HUD shows live tick metrics, and `design/stats/*.csv` now normalize into `generated/content/content.json` with producer indexes and derived unit lines. Content validation now passes with `0` hard errors; the remaining `41` warnings are the known later-expansion civ gaps already called out in the spec.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `package.json`, `package-lock.json`, `tsconfig.json`, `eslint.config.js`, `index.html`, `vite.config.ts`, `scripts/build-content.mjs`, `scripts/content-lib.mjs`, `scripts/content-lib.d.ts`, `scripts/validate-content.mjs`, `src/main.ts`, `src/styles.css`, `src/app/bootstrap/createApp.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/renderStore.ts`, `src/game/simulation/types.ts`, `src/phaser/scenes/GameScene.ts`, `src/ui/hud/createHudController.ts`, `tests/content/content-lib.test.ts`, `generated/content/content.json`
**Reasoning:** The implementation plan's first milestone is to prove the runtime shell, engine contract, and content pipeline before building deeper gameplay systems. This slice establishes that boundary and leaves the repo in a verified state that Phase 2 and Phase 3 can build on.
**Notes:** Verification run: `npm.cmd run content:validate`, `npm.cmd test`, `npm.cmd run lint`, `npm.cmd run build`. The Vite production build succeeds, but it currently emits a large-chunk warning for the single bundled Phaser runtime (`~1.53 MB` minified JS). A misplaced content artifact created during an earlier buggy build was cleaned up from the parent `github/generated` directory after the output path was corrected.

## [2026-04-10, America/Los_Angeles] — Add AGENTS.md for OpenAI Codex
**Action:** Added repository-root `AGENTS.md` with the same project rules as `CLAUDE.md`, adjusted only in the Subagent section so subagent prompts reference `AGENTS.md` with a fallback to `CLAUDE.md`.
**Result:** OpenAI Codex CLI will auto-discover project instructions via `AGENTS.md` while `CLAUDE.md` remains unchanged for other tooling.
**Files changed:** `AGENTS.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** Codex loads `AGENTS.md` by default; duplicating the canonical rules there avoids manual configuration and keeps behavior aligned with existing agent guidance.
**Notes:** Documentation-only change; no tests run.

## [2026-04-11, America/Los_Angeles] — Stop tracking generated content.json
**Action:** Added `generated/content/content.json` to `.gitignore` (it was only present locally before) and ran `git rm --cached` so Git no longer tracks the build artifact; the file remains on disk for local development.
**Result:** Regenerating content no longer shows as a dirty tracked file; clones get the file from `npm run content:build` / `prebuild` instead of the committed copy.
**Files changed:** `.gitignore`, `generated/content/content.json` (removed from index), `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** Generated JSON churn should not appear in version control; `.gitignore` alone does not untrack files already committed.
**Notes:** Ran `npx vitest run` after the change. Full `tsc` / `vite build` not re-verified in this session due to unrelated local WIP under `src/game/simulation/`.

## [2026-04-10 23:05, America/Los_Angeles] - Add Phase 2 map visibility slice and simulation tests
**Action:** Continued implementation past the bootstrap by refactoring the prototype world into a deterministic scenario generator, then wiring nearby starting resources, per-player visibility, fog of war, and a minimap into the simulation-to-render boundary. Added `src/game/simulation/prototypeScenario.ts` for deterministic terrain and spawn generation, expanded the render frame to carry explored and visible cell sets, added resource entities and vision-source components, updated the Phaser scene to draw fog and resource markers, and updated the HUD to render a minimap from the same frame data. Added simulation tests for deterministic scenario generation, standard opening resource counts, open town-center zones, local visibility culling, explored-territory growth, and HUD visibility metrics.
**Result:** The prototype now feels like an actual RTS map slice instead of a plain debug field. The human player starts with a Town Center, 3 Villagers, a Scout, visible nearby sheep, boar, berries, gold, and stone, explored-state shading, and a minimap that matches the main scene's fog state. The render boundary remains clean: `civ-engine` owns visibility and world state, while Phaser and the DOM HUD consume projected state only. The automated suite now covers content normalization plus deterministic map-generation and visibility behavior.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/renderStore.ts`, `src/game/simulation/types.ts`, `src/phaser/scenes/GameScene.ts`, `src/styles.css`, `src/ui/hud/createHudController.ts`, `tests/simulation/prototypeScenario.test.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** The implementation plan's next milestone is a navigable map with standard nearby resources and player visibility. Doing that through a deterministic scenario layer also makes the rules directly testable, which is necessary if the repo is going to keep broad gameplay coverage as the feature set grows.
**Notes:** Verification run: `npm.cmd run content:validate`, `npm.cmd test`, `npm.cmd run lint`, `npm.cmd run build`. Current automated total: `3` test files, `10` passing tests. The Vite production build still emits a large single-chunk warning for the Phaser runtime, but the build succeeds.

## [2026-04-10 23:11, America/Los_Angeles] - Eliminate content and build warnings
**Action:** Reworked the content validator so it derives civilization support coverage from actual unique-unit and unique-tech availability instead of emitting warning spam for known unsupported civs. Annotated normalized civilization rows with `playable`, `supportStatus`, and missing-content lists; added coverage totals to the generated bundle and CLI output; updated content tests to assert the supported-vs-unsupported split; and made the Vite build chunking policy explicit by splitting `phaser` and `civ-engine` into vendor chunks with a repo-owned chunk-size threshold.
**Result:** The repo's standard verification commands now run warning-free. `content:validate` reports `18` supported civilizations and `12` unsupported civilizations with no issues, while `vite build` no longer emits the prior large-chunk warning. The unsupported DE civ content is still explicit and measurable, but it is no longer treated as an incidental validator warning because it is an intentional coverage boundary rather than an integrity defect.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `scripts/build-content.mjs`, `scripts/content-lib.d.ts`, `scripts/content-lib.mjs`, `scripts/validate-content.mjs`, `tests/content/content-lib.test.ts`, `vite.config.ts`
**Reasoning:** The user asked to fix the warnings. The correct fix was to distinguish actual broken data from intentionally unsupported content, then encode that distinction in the normalization pipeline and build configuration instead of ignoring the warnings or weakening verification.
**Notes:** Verification run: `npm.cmd run content:validate`, `npm.cmd test`, `npm.cmd run lint`, `npm.cmd run build`. Current automated total remains `3` test files and `10` passing tests; the change removes warnings without reducing gate strictness.

## [2026-04-10 23:19, America/Los_Angeles] - Add autonomous Dark Age economy loop with test-first coverage
**Action:** Added the first real economy rules on top of the Phase 2 map slice using test-first changes. Expanded the scenario generator to spawn nearby tree resources in starting forests, extended the simulation model with player stockpiles, population state, villager gatherer state, and resource max amounts, and implemented a deterministic villager loop that assigns nearby resources, gathers food or wood, returns to the Town Center, and deposits into the owning player's stockpile. Updated the HUD to show food, wood, gold, stone, and population from simulation state, and exposed an economy snapshot API for deterministic test inspection.
**Result:** The prototype is now more than a rendered map: villagers actively run a closed-loop economy and the HUD shows the resulting stockpile changes in real time. Starting state now reflects a more recognizably AoE-like Dark Age setup with standard opening resources (`200F / 200W / 100G / 200S`), `4/5` population, nearby herdables/boars/berries/mines/trees, and autonomous gather-dropoff behavior. The simulation test suite now covers this economy slice directly rather than relying on visual confirmation.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/phaser/scenes/GameScene.ts`, `src/ui/hud/createHudController.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** The user asked to keep going toward a complete game and to stop treating tests as second-class citizens. The right next step was to implement one concrete Dark Age subsystem with failing tests first, then make the runtime satisfy those rules and keep all standard verification gates green.
**Notes:** Verification run: `npm.cmd run content:validate`, `npm.cmd test`, `npm.cmd run lint`, `npm.cmd run build`. Current automated total: `3` test files and `11` passing tests. This slice is still scripted economy, not player-commanded villager control or full build/queue gameplay.

## [2026-04-10 23:22, America/Los_Angeles] - Add explicit local run instructions to README
**Action:** Expanded `README.md` with a concrete `Run locally` section covering prerequisites, dependency installation, starting the Vite dev server, the default local URL, and the current camera controls. Also split out a `Verification` section so the standard validation commands are easy to find.
**Result:** A new contributor can now follow the README directly to boot the current prototype instead of inferring the run flow from the raw command list.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user asked for explicit instructions on how to run the game. The README already had command bullets, but it did not give a proper ordered run path or call out the local `civ-engine` dependency.
**Notes:** Documentation-only change. No tests were run.

## [2026-04-11 09:26, America/Los_Angeles] - Add browser gameplay tests and full verification workflow
**Action:** Added a Playwright-based browser test layer for the Phaser prototype. Introduced a narrow browser test API at the app bootstrap so tests can read deterministic HUD, render, economy, and camera state without scraping internal engine objects indirectly. Added `playwright.config.ts`, a `tests/browser/game.spec.ts` suite that exercises boot, minimap/HUD rendering, camera pan/zoom controls, and deterministic fast-forwarded economy/exploration behavior, plus package scripts for browser setup and a full `verify` gate. Added `vitest.config.ts` to keep unit tests scoped to this repo and exclude Playwright/browser specs and linked `civ-engine` tests.
**Result:** The project now has first-class browser automation coverage for in-game controls and gameplay behavior instead of relying only on unit tests. The full verification path now runs content validation, Vitest unit tests, Playwright browser tests, lint, and production build in one command. Browser tests can validate Phaser camera input and HUD/minimap behavior against the live running game while still asserting deterministic simulation state through the exposed test seam.
**Files changed:** `.gitignore`, `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `package-lock.json`, `package.json`, `playwright.config.ts`, `src/app/bootstrap/browserTestApi.ts`, `src/app/bootstrap/createApp.ts`, `src/phaser/scenes/GameScene.ts`, `tests/browser/game.spec.ts`, `vitest.config.ts`
**Reasoning:** The user explicitly asked for browser tests so controls and behaviors can be programmatically verified, and also asked that tests remain comprehensive and relevant as development continues. The correct implementation was a maintained test harness at the app boundary plus a browser runner that checks the live game loop, not more unit-only coverage.
**Notes:** Verification run: `npm.cmd run verify`. Current automated total: `11` Vitest assertions across `3` unit/content test files plus `3` Playwright browser gameplay tests, all passing.

## [2026-04-11 09:47, America/Los_Angeles] - Add first player-commanded Dark Age control slice
**Action:** Extended the simulation bridge with authoritative selection, move, training, and construction commands. Added Town Center villager queues, House placement and construction, population-cap growth from completed Houses, selection state, and unit/building snapshots for deterministic inspection. Wired Phaser pointer input to the simulation bridge for left-click selection, right-click move, and left-click building placement, and added a DOM selection panel with command buttons for training Villagers and building Houses. Expanded both unit tests and browser tests to cover the new gameplay loop. Updated `docs/engine-feedback.md` with the new strengths and friction observed while layering player-commanded systems on top of `civ-engine`.
**Result:** The prototype now has the first genuine player-commanded economy interactions instead of only autonomous behavior. The human player can select a Town Center, queue a Villager, select a Villager, place a House foundation, watch construction complete, and see population cap increase in the HUD. Browser coverage now verifies those controls end to end in the live game, while simulation tests pin the production and construction rules directly.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/app/bootstrap/browserTestApi.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/types.ts`, `src/phaser/scenes/GameScene.ts`, `src/styles.css`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** The implementation plan's remaining Phase 3 gap was player-commanded economy. Production queues, House support, and construction are prerequisites for any broader Dark Age or Feudal gameplay, and they also create a better proof point for `civ-engine` than more scripted prototype behavior would.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, and `npm.cmd run verify` after the slice stabilized. Current automated total: `13` Vitest assertions across `3` unit/content files plus `5` Playwright browser gameplay tests.

## [2026-04-11 10:05, America/Los_Angeles] - Add explicit villager gather context orders
**Action:** Added a simulation-owned `issueContextCommand` path so right click now resolves to gather when a selected human villager targets a visible resource and falls back to movement otherwise. Wired Phaser right-click input to that context command, added a matching simulation test for gold gathering through explicit orders, and added a Playwright browser test that selects a Villager, right-clicks the nearby gold pile, fast-forwards the live game, and asserts gold income. Updated the README controls and kept `docs/engine-feedback.md` aligned with the new command-routing observation.
**Result:** The prototype now supports the first AoE-style context-sensitive command instead of treating all right clicks as movement. Players can explicitly redirect Villagers onto visible gold using the live game controls, and that behavior is now covered both at the deterministic simulation layer and at the browser/gameplay layer.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/phaser/scenes/GameScene.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** The previous control slice still left a major RTS interaction gap: Villagers could only move manually and otherwise relied on autonomous resource assignment. Explicit context orders are a prerequisite for player-directed economy control and a better test of the simulation/input boundary described in the implementation plan.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, `npx.cmd tsc --noEmit`, `npm.cmd run build`, and `npm.cmd run verify`. Current automated total: `14` Vitest assertions across `3` files plus `6` Playwright browser gameplay tests.

## [2026-04-11 10:14, America/Los_Angeles] - Add Dark Age drop-off buildings and routing
**Action:** Extended the Dark Age villager build roster with Mill, Lumber Camp, and Mining Camp using test-first changes. Added new build-type support to the simulation model, generalized the build-placement and cost logic, updated the HUD command panel to render build buttons from the current selection state instead of hardcoding only House, and added resource-to-drop-off routing so villagers choose the nearest completed valid building for food, wood, gold, or stone. Added a deterministic simulation test for Mining Camp-backed gold drop-off and a Playwright browser test that builds a Mining Camp through the live UI and verifies the resulting gold income.
**Result:** The prototype now covers more of the intended Dark Age economy loop. Villagers can build the core eco drop-off structures, the command panel exposes them directly, and explicit gathering orders now use nearby completed drop-off buildings instead of always returning to the Town Center.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** The implementation plan's remaining Phase 3 work called for the Dark Age economy to be playable beyond Houses and the Town Center. Drop-off buildings are a prerequisite for more realistic villager routing, future farms, and the broader Feudal transition.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, `npx.cmd tsc --noEmit`, `npm.cmd run build`, and `npm.cmd run verify`. Current automated total: `15` Vitest assertions across `3` files plus `7` Playwright browser gameplay tests.

## [2026-04-11 10:21, America/Los_Angeles] - Add Barracks and Militia production slice
**Action:** Added the first non-economic production building by extending the simulation model with Barracks and Militia support. Widened shared building and trainable-unit types, generalized the queue system so multiple producers can own queues, added Barracks construction and Militia training rules, updated the HUD command panel to render train buttons dynamically from selection state, and added new simulation and Playwright tests that build a Barracks and train a Militia through the live command flow. Updated README and `docs/engine-feedback.md` to reflect the new producer behavior.
**Result:** The prototype now supports the first military-production loop: a Villager can build a Barracks, the player can select the completed Barracks, queue a Militia, and see the unit spawn into the world under the same authoritative simulation model as the economy systems.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** The implementation plan's next meaningful milestone after the Dark Age economy is the first real military-production path. Barracks and Militia provide that without yet forcing the repo to solve combat, formations, and Feudal tech progression in the same slice.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, `npx.cmd tsc --noEmit`, `npm.cmd run build`, and `npm.cmd run verify`. Current automated total: `16` Vitest assertions across `3` files plus `8` Playwright browser gameplay tests.

## [2026-04-11 10:34, America/Los_Angeles] - Add first melee combat slice with live gameplay coverage
**Action:** Implemented the first deterministic combat loop using test-first changes. Added simulation combat state for units, extended context-sensitive right click so selected human units can target visible hostile units, implemented melee attack execution and death cleanup in the simulation command system, added a forward enemy scout to the prototype scenario as a stable combat fixture, and updated the scenario tests to describe that new opening setup explicitly. Expanded the Playwright suite with a live gameplay test that builds a Barracks, trains a Militia, right-clicks the enemy scout, and verifies the kill through the running game.
**Result:** The prototype now supports the first actual military interaction instead of only production: Militia can be trained, commanded onto a visible enemy unit, path into melee range, attack on a deterministic cooldown, and remove the target from simulation state when it dies. Both the direct simulation API and the browser-level control path now verify that end-to-end behavior.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** The implementation plan's next Phase 4 prerequisite after military production is basic attack behavior. A narrow melee-only slice keeps the change small while proving that command resolution, per-unit combat state, death cleanup, and browser input can all stay simulation-owned on top of `civ-engine`.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, `npx.cmd tsc --noEmit`, and `npm.cmd run build`. Current automated total: `17` Vitest assertions across `3` files plus `9` Playwright browser gameplay tests. The repo still has no `docs/ARCHITECTURE.md`, so there was no architecture file to update for this slice.

## [2026-04-11 10:48, America/Los_Angeles] - Add baseline AI house-and-barracks rush
**Action:** Added a first AI skirmish slice on top of the existing economy and combat systems using tests first. Expanded the simulation bridge with reusable move, attack, training, and construction helpers so the AI and the player use the same authoritative rule paths. Added a minimal AI system that responds to the enemy pop-cap pressure from the forward scout by building a House, places a Barracks, queues Militia, and sends those Militia toward the human economy with villager-priority targeting. Added deterministic simulation and Playwright browser tests that fast-forward the live game until the AI rush has built out and killed at least one human villager.
**Result:** The prototype now has a real enemy pressure loop instead of only passive opposing units. In a fresh match, the AI can execute a small opening, break out of its starting population cap, produce Militia, and inflict economic damage on the player without any scripted browser input. The same outcome is now verified both through the bridge-level simulation tests and through the live browser harness.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** The official scope is single-player against AI, so the next highest-value gap after basic combat was turning the opponent into an actual actor. Reusing the same command helpers for AI actions keeps the rules path unified and makes the browser tests meaningful instead of proving a separate AI-only codepath.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, `npx.cmd tsc --noEmit`, and `npm.cmd run build`. Current automated total: `18` Vitest assertions across `3` files plus `10` Playwright browser gameplay tests. The AI slice is still intentionally narrow: there are no buildings-under-attack behaviors, defeat conditions, or age progression yet.

## [2026-04-11 12:07, America/Los_Angeles] - Adopt civ-engine EntityRef semantics across long-lived gameplay state
**Action:** Read the updated `civ-engine` docs and migrated the simulation bridge away from assuming durable entity IDs for long-lived selection and command targets. Reworked selected-entity state, build targets, and attack targets to store `EntityRef` handles internally, generalized attack commands to cover both units and buildings, added building-health cleanup and destruction handling, and updated the scenario/test fixtures so browser and simulation tests stop assuming numeric IDs remain stable after entity destruction and reuse.
**Result:** The runtime now matches the engine's current entity-lifecycle contract instead of relying on undefined behavior around ID reuse. The player and AI can safely hold attack/build targets across ticks, building-target combat works, and the test suite now verifies semantic world outcomes rather than raw ID continuity in cases where the engine is free to recycle IDs.
**Files changed:** `src/app/bootstrap/browserTestApi.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** The user explicitly asked to re-read the new `civ-engine` docs, update repo code and tests for breaking changes, and keep testing first-class. `EntityRef` was the most important API change for this repo because combat, construction, selection, and browser automation all hold entities across multiple ticks.
**Notes:** Verification during the migration used `npm.cmd test`, `npx.cmd tsc --noEmit`, `npm.cmd run test:browser`, and `npm.cmd run verify` as the slice stabilized. The repo still has no `docs/ARCHITECTURE.md`, so there was no architecture file to update.

## [2026-04-11 12:18, America/Los_Angeles] - Add conquest outcome slice with deterministic fixture seeds
**Action:** Continued implementation after the engine migration by adding conquest-only end-state handling and deterministic test fixtures. Extended `createPrototypeScenario()` with focused `conquest-victory-fixture` and `conquest-defeat-fixture` seeds, added simulation-owned conquest outcome detection plus match freeze behavior, surfaced match outcome through the HUD, made the app boot seedable from the page URL, and added simulation and Playwright coverage for both player victory and player defeat. Also generalized AI fallback target selection so enemy militia can attack the nearest visible enemy building instead of only hardcoding the human Town Center.
**Result:** The prototype can now resolve and display conquest-style match outcomes instead of running forever after the decisive state has already happened. Browser automation can boot directly into focused deterministic skirmish fixtures, verify victory/defeat through the live UI, and assert that the simulation stops advancing after the match is over.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/app/bootstrap/createApp.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** The implementation plan calls for real win/loss resolution, and the new seedable fixtures make that behavior testable without weakening the gameplay path. The AI fallback change was necessary to make the defeat fixture represent real skirmish behavior rather than a special-case test hack.
**Notes:** Verification run: `npm.cmd run verify`. Current automated total: `22` Vitest assertions across `3` files plus `13` Playwright browser gameplay tests, all passing.

## [2026-04-11 17:24, America/Los_Angeles] - Add first Feudal progression slice with live Archer flow
**Action:** Implemented the first age-progression slice using tests first. Added explicit age state, Town Center research queues, `Feudal Age` prerequisite gating, Feudal fixture seeds with scenario-level resource overrides, `Archery Range` construction, `Archer` production, HUD age display, and selection-panel research buttons. Stabilized browser gameplay tests by adding page-local semantic selectors that resolve units/buildings from the live simulation snapshot before issuing UI commands. Updated README and `docs/engine-feedback.md` to reflect the new slice and engine observations.
**Result:** The prototype now supports the first real Dark-to-Feudal transition instead of stopping at Dark Age economy and Barracks play. In deterministic fixtures, the player can research `Feudal Age` from the Town Center, unlock `Archery Range` in the Villager build panel, construct it, and queue `Archer` units through the live HUD. Browser and simulation coverage both verify missing-prerequisite gating and the full Feudal research-to-Archer path.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** The implementation plan's next concrete milestone after the Dark Age and conquest slices is Feudal progression. Age-up plus one meaningful Feudal unlock was the smallest vertical slice that materially advances the game while keeping the rules path unified and fully test-driven.
**Notes:** Verification run: `npm.cmd run verify`. Current automated total: `25` Vitest assertions across `3` files plus `14` Playwright browser gameplay tests, all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 17:34, America/Los_Angeles] - Add Blacksmith and Fletching technology slice
**Action:** Continued Phase 4 with another test-first Feudal slice. Added `blacksmith` building support, `fletching` as a researchable technology, scenario start-age overrides plus a focused `feudal-blacksmith-fixture`, and exposed Archer combat stats through economy snapshots for deterministic assertions. Implemented player-wide researched-technology tracking, Blacksmith research queues, and Archer stat propagation so `Fletching` updates both existing Archers and Archers trained after the tech completes. Expanded browser tests with a live Blacksmith research flow and updated README plus `docs/engine-feedback.md` to match the new capability.
**Result:** The prototype now supports a real Feudal military technology loop instead of stopping at age-up and production unlocks. In the new fixture, the player can select a completed Blacksmith, research `Fletching`, and verify that Archer attack damage and range increase on both existing and newly trained units through the running game. The browser suite now covers one more live gameplay flow, and the sim snapshots are more useful for debugging combat-affecting technologies.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** After the first Feudal age-up slice, the most valuable next step was proving that the research system can drive more than age progression. `Blacksmith` plus `Fletching` is a compact but meaningful tech slice that deepens Feudal play and forces the runtime to support persistent player-wide modifiers cleanly.
**Notes:** Verification run: `npm.cmd run verify`. Current automated total: `26` Vitest assertions across `3` files plus `15` Playwright browser gameplay tests, all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 17:48, America/Los_Angeles] - Add Stable and Scout Cavalry Feudal production slice
**Action:** Continued Phase 4 with another test-first Feudal producer slice. Added `stable` as a Villager build option once Feudal Age and Barracks prerequisites are satisfied, added `scout` to the trainable-unit roster for completed Stables, and introduced a focused `feudal-stable-fixture` with scenario-level age and resource overrides. Expanded deterministic simulation and Playwright browser tests to cover Stable placement, Scout Cavalry queueing, resource spend, and live command-panel usage, then updated README and `docs/engine-feedback.md` to reflect the new Feudal flow.
**Result:** The prototype now supports the second core Feudal military producer instead of routing all early aggression through Barracks and Archery Range only. In the new fixture, the player can place a Stable, complete construction, queue Scout Cavalry through the live HUD, and verify the trained unit enters the world with the expected melee stats through both sim and browser coverage.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** After Archery Range and Blacksmith landed, the next highest-value Feudal gap was cavalry production. `Stable` plus `Scout Cavalry` broadens the basic build-order space, deepens Feudal unit composition, and exercises the existing production/build/selection systems without introducing a new subsystem shape.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, `npx.cmd tsc --noEmit`, `npm.cmd run build`, and `npm.cmd run verify`. Current automated total: `27` Vitest assertions across `3` files plus `16` Playwright browser gameplay tests, all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 17:56, America/Los_Angeles] - Add Feudal Spearman production and anti-scout combat slice
**Action:** Continued Phase 4 with another test-first Barracks extension. Added `spearman` to the unit and trainable-unit roster, introduced a focused `feudal-spearman-fixture`, refactored producer menus behind a shared `getTrainOptions()` helper so Feudal-gated Barracks options stay aligned between queue validation and HUD rendering, and added target-specific combat bonuses so Spearmen deal extra damage to Scouts. Expanded deterministic simulation and Playwright browser tests to cover live Spearman training, resource spend, right-click combat, and the expected fast scout kill window, then updated README and `docs/engine-feedback.md`.
**Result:** The prototype now has the first Feudal counter-unit behavior rather than only generic melee and ranged stats. In the new fixture, the player can train a Spearman from a Feudal Barracks and use it to kill a visible enemy Scout through the normal live command path quickly enough to prove the anti-scout damage rule is active.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** After Stable support landed, the next most valuable combat-depth improvement was adding a real Barracks counter unit instead of only another generic stat line. `Spearman` plus an explicit anti-scout bonus improves Feudal army composition while staying within the current deterministic combat model.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, and pending full-gate rerun before commit. Current automated total before the final verify gate is `28` Vitest assertions across `3` files plus `17` Playwright browser gameplay tests, all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 18:02, America/Los_Angeles] - Add Feudal Skirmisher production and anti-archer combat slice
**Action:** Continued Phase 4 with the matching Archery Range counter-unit slice. Added `skirmisher` to the unit and trainable-unit roster, introduced a focused `feudal-skirmisher-fixture`, widened the shared `getTrainOptions()` helper so Archery Ranges can expose both Archers and Skirmishers in Feudal Age, and added target-specific combat bonuses so Skirmishers deal extra damage to Archers. Expanded deterministic simulation and Playwright browser tests to cover live Skirmisher training, resource spend, right-click combat, and the expected fast archer kill window, then updated README and `docs/engine-feedback.md`.
**Result:** The prototype now has a second Feudal counter-unit and a more recognizable early AoE2 unit triangle instead of only isolated production buttons. In the new fixture, the player can train a Skirmisher from a Feudal Archery Range and use it to kill a visible enemy Archer through the normal live command path quickly enough to prove the anti-archer bonus is active.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** After Spearman landed, the next highest-value Feudal combat-depth improvement was adding the ranged counter line so Archery Range choices start to resemble real AoE2 composition decisions. `Skirmisher` plus an explicit anti-archer bonus deepens ranged play without requiring a new subsystem shape.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, and pending full-gate rerun before commit. Current automated total before the final verify gate is `29` Vitest assertions across `3` files plus `18` Playwright browser gameplay tests, all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 19:38, America/Los_Angeles] - Add Feudal Watch Tower defense slice
**Action:** Continued Phase 4 with the first static-defense building slice using tests first. Added `watch-tower` to the buildable-building roster, introduced a focused `feudal-watch-tower-fixture`, added tower-specific build cost and completion rules, and implemented repo-owned building combat state plus auto-fire targeting for completed Watch Towers. Added default tower sight radius on completion so tower targeting uses the same visibility substrate as other owned vision sources. Expanded deterministic simulation and Playwright browser tests to cover Feudal Villager tower placement, stone spend, and automatic tower kills on a nearby visible enemy Scout. Also raised the timeout on the existing Militia-vs-house test so the growing deterministic suite remains stable under heavier load.
**Result:** The prototype now has the first defensive structure that acts on its own after placement instead of only player-commanded units and passive buildings. In the new fixture, the player can place a Watch Tower once Feudal Age and Blacksmith prerequisites are satisfied, complete construction, and watch it automatically kill a nearby enemy Scout through the normal live game loop.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** After broadening the Feudal unit roster, the next most valuable skirmish improvement was a defensive option that changes map control and punishes nearby pressure. `Watch Tower` adds that defense layer while still fitting the current deterministic ECS model cleanly.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, and pending full-gate rerun before commit. Current automated total before the final verify gate is `30` Vitest assertions across `3` files plus `19` Playwright browser gameplay tests, all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 19:43, America/Los_Angeles] - Add generic production-building rally points
**Action:** Continued Phase 4 with a small usability slice using tests first. Added producer-owned `rallyPoints` memory inside the simulation bridge, extended building selection so right-click on an owned completed building sets its rally point, and applied that rally point as a move command to newly trained units when production completes. Expanded deterministic simulation and Playwright browser tests to cover setting a rally point on an Archery Range and verifying that a newly trained Skirmisher walks to the chosen destination automatically. Updated README and `docs/engine-feedback.md` to capture the new command behavior and engine observation.
**Result:** The prototype now supports a key RTS production usability behavior instead of spawning all newly trained units at the building edge with no follow-up order. In the live game, the player can select a production building, right-click a destination, queue a unit, and watch that unit move to the rally target automatically after it spawns.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** After adding several Feudal producers and counter units, the next smallest high-value improvement was reducing production micromanagement. Rally points materially improve playability while fitting the current deterministic command model without requiring a new subsystem shape.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, and pending full-gate rerun before commit. Current automated total before the final verify gate is `31` Vitest assertions across `3` files plus `20` Playwright browser gameplay tests, all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 19:59, America/Los_Angeles] - Add Feudal Market exchange slice
**Action:** Continued Phase 4 with a test-first Market slice. Added `market` to the buildable-building roster, introduced a focused `feudal-market-fixture`, extended the selection model and HUD command panel with explicit market actions, and implemented match-global market exchange rates for buying and selling food, wood, and stone against gold. Expanded deterministic simulation, scenario, and Playwright browser tests to cover Market placement, live command-panel exchange actions, and dynamic price movement across repeated transactions. Updated README and `docs/engine-feedback.md` to describe the new behavior and engine observation.
**Result:** The prototype now supports the first standard-mode resource exchange loop instead of forcing every midgame economy through only gathering and drop-off. In the Feudal market fixture, the player can place a Market, sell wood for gold, buy food with gold, and see repeated trades move the exchange rate in the expected direction through both the bridge API and the live browser UI.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** `Market` is a remaining Feudal-core requirement in the official spec and implementation plan. It deepens the economy meaningfully while staying inside the existing simulation-owned command model, and it proves that match-global economic state can stay deterministic and well-covered without introducing a new engine-side subsystem.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, `npx.cmd tsc --noEmit`, `npm.cmd run build`, and pending final `npm.cmd run verify` before commit. Current automated total before the final verify gate is `32` Vitest assertions across `3` files plus `21` Playwright browser gameplay tests, all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 20:09, America/Los_Angeles] - Add defensive garrison and ungarrison slice
**Action:** Continued Phase 4 with a test-first defensive-utility slice. Added building action support for `ungarrison`, implemented Villager right-click garrison into owned Town Centers and completed Watch Towers, stored garrisoned units by temporarily removing their `position` and `visionSource`, and restored them onto nearby tiles when the building ungarrisons them. Expanded deterministic simulation tests to cover both Town Center and Watch Tower garrison flows, added a live Playwright Town Center garrison test, and updated README plus `docs/engine-feedback.md`.
**Result:** The prototype now supports a meaningful defensive response to early pressure instead of forcing Villagers to only walk or fight. In the running game, Villagers can enter Town Centers or Watch Towers with a right click, disappear from the world while remaining alive, and be released again through the building command panel.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** After finishing the Feudal Market slice, the next smallest spec-required control improvement was defensive garrisoning. It materially improves the single-player skirmish loop and exercises another RTS-style hidden-entity state without needing the broader pathfinding work that walls still require.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, `npx.cmd tsc --noEmit`, and pending final `npm.cmd run verify` before commit. Current automated total before the final verify gate is `34` Vitest assertions across `3` files plus `22` Playwright browser gameplay tests, all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 20:17, America/Los_Angeles] - Add garrison-driven Town Center defense slice
**Action:** Continued Phase 4 with a focused defensive-fire follow-up. Added a `town-center-defense-fixture`, extended building combat so Town Centers gain defensive arrow fire when they contain garrisoned Villagers, and wired the existing building-combat loop to scale Town Center shots from current garrison state. Expanded deterministic simulation and Playwright browser tests to cover garrisoning a Villager into the focused Town Center defense fixture and verifying that the nearby enemy Scout dies inside a fixed time window. Updated README and `docs/engine-feedback.md` to record the new defensive behavior and the engine observation.
**Result:** The new garrison command now has immediate combat value instead of being only a hiding mechanic. In the focused fixture and the live browser game, the player can garrison a Villager and watch the Town Center automatically kill the nearby Scout through the same simulation-owned defensive-fire system used by towers.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** After the garrison/ungarrison slice landed, the next smallest improvement with direct gameplay payoff was making that garrison actually strengthen defense. Reusing the existing building-combat system kept the change local and testable without yet introducing the broader projectile subsystem the full spec will eventually need.
**Notes:** Verification run: `npm.cmd test`, `npm.cmd run test:browser`, and pending final `npm.cmd run verify` before commit. Current automated total before the final verify gate is `35` Vitest assertions across `3` files plus `23` Playwright browser gameplay tests, all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 20:44, America/Los_Angeles] - Add first Castle Age progression and Knight slice
**Action:** Continued implementation with tests first for the first Castle Age milestone. Added a focused `castle-age-fixture`, widened the simulation type unions for `castle-age` research and `knight` production, implemented Town Center Castle Age prerequisite gating, Castle Age research cost/time, Stable Knight training cost/time, Knight combat stats, and the matching HUD labels. Added deterministic simulation coverage for Castle Age gating plus the full Castle Age to Knight flow, and added a live Playwright command-panel test for researching Castle Age and training a Knight. Updated README and `docs/engine-feedback.md` to reflect the new progression slice and the latest `civ-engine` observations.
**Result:** The prototype now supports the first recognizably midgame progression step instead of stopping at Feudal Age. In the focused fixture, the player can research `Castle Age` from the Town Center after two qualifying Feudal buildings, then select a completed Stable and train a `Knight` through the live HUD. Full repo verification is green after the slice.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** The implementation plan's next major gap after the Feudal support slices was Castle progression. `Castle Age` plus `Knight` was the smallest high-value vertical slice that deepens the standard skirmish loop, exercises age gating beyond Dark-to-Feudal only, and expands the military roster without introducing a broader upgrade system yet.
**Notes:** Verification run: `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npm.cmd run test:browser`, `npm.cmd run build`, and `npm.cmd run verify`. Current automated total is `37` passing Vitest tests plus `24` passing Playwright browser gameplay tests, with content validation, lint, typecheck, and build all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 20:58, America/Los_Angeles] - Add Castle Age extra Town Center slice
**Action:** Continued implementation with tests first for the next Castle-era economy milestone. Added a focused `castle-town-center-fixture`, widened the buildable-building roster so Villagers gain `town-center` as a Castle Age build option, implemented Town Center construction cost and build time from the local structure stats, and reused the existing Town Center production queue so newly placed Town Centers can train Villagers. Added deterministic simulation coverage for Castle-era Town Center placement and follow-up Villager training, plus a live Playwright test that builds a second Town Center, moves the builder off the footprint, and queues a Villager from the new building. Updated the in-game HUD footer, README, and `docs/engine-feedback.md`.
**Result:** Castle Age expansion now includes the first boom-oriented economy behavior instead of only military progression. In the focused fixture, the player can select a Villager, place an additional Town Center, complete it, and use that new Town Center as another Villager producer through the same live command panel used by the original Town Center.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** After Castle Age and Knight landed, the highest-value next slice in the implementation plan was extra Town Centers. It materially deepens the Castle Age match loop while reusing the current construction and production systems, which keeps the change localized and testable.
**Notes:** Targeted verification run: `npx.cmd vitest run tests/simulation/prototypeScenario.test.ts tests/simulation/createSimulationBridge.test.ts` and `npm.cmd run test:browser`, all passing. Full repo verification will be rerun before commit. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 21:13, America/Los_Angeles] - Make production queue visible in the HUD
**Action:** Fixed a HUD usability gap reported during manual play. Added a failing browser assertion that queueing a Town Center villager renders a visible queue item, then updated the selection panel to render a dedicated queue section with per-item labels and progress bars instead of only the small `N queued` summary text. Added matching HUD styles for the new queue block and reran the full verification gate.
**Result:** Queueing a Villager or research item now produces an obvious on-screen queue card in the selection panel, so players can see what is queued instead of only a subtle count line. The browser suite now explicitly verifies that `Training: Villager` appears after queueing from the Town Center.
**Files changed:** `docs/devlog-detailed.md`, `src/styles.css`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`
**Reasoning:** The existing implementation was technically correct but too easy to miss during real play. A dedicated queue list is a small UI change that materially improves readability without changing any gameplay behavior.
**Notes:** Verification run: `npx.cmd tsc --noEmit` and `npm.cmd run verify`, all passing. Current automated total remains `38` passing Vitest tests plus `25` passing Playwright browser gameplay tests, with content validation, lint, typecheck, and build all passing.

## [2026-04-11 21:39, America/Los_Angeles] - Add drag-box multi-selection with marquee feedback
**Action:** Continued the input and HUD slice with tests first for drag-selection. Finished the in-progress selection refactor in the simulation bridge so selection is stored as `EntityRef[]`, added box-select support, updated group move/context fan-out, and added a marquee-aware Phaser input flow that defers left-click selection until pointer release. Added browser-test seam support for inspecting marquee state, updated the HUD to render multi-selection titles, and introduced deterministic `villager-selection-fixture` and `mixed-selection-fixture` scenarios so all-villager and mixed-unit drag-selection are both stable under automation. Expanded simulation, scenario, and Playwright coverage to assert that drag-selection includes all friendly movable units in the box while ignoring buildings.
**Result:** The game now shows a visible drag rectangle at the mouse position, supports left-drag box selection, selects all owned movable units inside the box, preserves all-villager group building commands, suppresses incorrect villager build commands on mixed selections, and fans right-click movement/orders across the full selected group. Browser automation now verifies both a pure villager group selection and a mixed villager/militia/scout selection against the live game.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/app/bootstrap/browserTestApi.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/phaser/scenes/GameScene.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** The prototype had single-click selection only, which made grouped unit control slower and blocked a core RTS interaction. Drag-box selection was the smallest high-value control improvement, and the mixed-unit regression test ensured the HUD and command model stay coherent when a group is not all villagers.
**Notes:** Verification run: `npx.cmd tsc --noEmit` and `npm.cmd run verify`, all passing. Current automated total is `40` passing Vitest tests plus `27` passing Playwright browser gameplay tests, with content validation, lint, typecheck, and build all passing. One targeted `npx.cmd vitest run tests/simulation/prototypeScenario.test.ts` command hit a transient Windows `spawn EPERM` while starting `esbuild`, but the broader `vitest` and `verify` runs succeeded immediately afterward. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 21:47, America/Los_Angeles] - Add building placement preview with valid/invalid feedback
**Action:** Continued the UI/control pass with tests first for building placement preview. Added a failing simulation test that invalid occupied-cell placement is rejected while placement mode stays active, then added a failing browser test for live valid/invalid preview state before construction. Implemented a simulation-owned `getPlacementPreview()` helper so building footprint validity is computed once from the same placement rules used by actual construction, exposed that through the browser test seam, and added a Phaser placement overlay that renders a green footprint ghost for valid placement and a red footprint ghost for blocked placement under the mouse.
**Result:** Building placement now gives immediate on-map feedback. When a Villager is in placement mode, the player sees a green ghost over valid tiles and a red ghost over invalid blocked tiles, and clicking an invalid position no longer fails silently because placement mode remains active with the red preview still visible. Browser automation now verifies both valid and invalid House preview states before construction.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/app/bootstrap/browserTestApi.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/types.ts`, `src/phaser/scenes/GameScene.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** The previous build flow forced the player to infer placement validity from a silent success/failure click, which is poor RTS feedback. Reusing simulation-owned footprint validation for the preview keeps the visual cue aligned with the actual placement rule and avoids scene-side duplication.
**Notes:** Verification run: `npx.cmd tsc --noEmit` and `npm.cmd run verify`, all passing. Current automated total is `41` passing Vitest tests plus `28` passing Playwright browser gameplay tests, with content validation, lint, typecheck, and build all passing. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 22:26, America/Los_Angeles] - Add stacked-tile entity inspection and stabilize browser harness
**Action:** Implemented tests first for tile-level entity inspection. Added failing simulation tests for visible resource selection and repeated-click cycling across stacked selectable entities, plus failing browser tests for resource HUD inspection and same-tile cycling. Extended `SelectionState` so it can represent resources, remaining resource amounts, and tile-stack position, then updated the selection bridge to keep selection data JSON-safe while cycling through all selectable entities on a clicked tile. Added a dedicated `tile-selection-cycle-fixture`, updated the HUD to show tile position, stack index, and resource remaining, and updated README controls. While verifying, Playwright revealed that the Vite dev-server child could drop mid-suite, so the browser harness was switched to serve a built `vite preview` instance via `pretest:browser` instead of the dev server. Consulted `civ-engine` guides for public invariants, rendering, and debugging, then reflected the `WorldDebugger`-probe takeaway in `docs/engine-feedback.md`.
**Result:** Every visible entity type on a tile is now inspectable. Repeated left clicks on the same tile cycle through stacked friendly units, buildings, and resources, and resource selections now show a readable HUD label plus remaining stockpile instead of silently clearing. The browser suite is stable again with the built-preview server, and the new resource/cycling coverage passes end to end.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `package.json`, `playwright.config.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** The previous selection model only represented units and buildings cleanly, so clicking a resource tile or a stacked tile did not let the player identify everything present there. Fixing that in the simulation bridge keeps the renderer thin and matches the `civ-engine` guidance to keep gameplay/debug state as plain data outside Phaser-specific code.
**Notes:** Verification run: `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npm.cmd run test:browser`, and `npx.cmd vite build`, all passing. Current automated total is `43` passing Vitest tests plus `30` passing Playwright browser gameplay tests. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 22:30, America/Los_Angeles] - Lock down military population accounting with tests
**Action:** Investigated the reported population concern with a TDD pass. Added failing simulation and browser tests that assert military production changes the player population counter, then ran the targeted suites to determine whether the bug was in gameplay code or in the new tests. The first pass exposed that the `feudal-spearman-fixture` starts with `0/5` population rather than `1/5`, so the production logic itself was already correct and the tests were adjusted to assert the real military-population transition of `0/5` to `1/5` after a Spearman completes. Reran the targeted simulation and browser suites, then the required direct verification commands.
**Result:** No gameplay rule change was needed: military units already consume population, including the starting Scout in the default opening and newly trained military units such as Spearmen. The repo now has explicit regression coverage for that behavior in both the simulation layer and the live HUD/browser layer.
**Files changed:** `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** The project did not have an explicit test proving military population accounting, so the safest response was to verify the rule with tests before touching gameplay code. That kept behavior stable while making the expectation durable.
**Notes:** Verification run: `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npm.cmd run test:browser`, and `npx.cmd vite build`, all passing. Current automated total is `44` passing Vitest tests plus `31` passing Playwright browser gameplay tests. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 22:47, America/Los_Angeles] - Add true double-click same-type selection
**Action:** Continued the selection-control work with tests first for same-type unit fan-out. Added failing simulation coverage for selecting all owned visible units of one type in a rectangle, added a deterministic `double-click-selection-fixture`, and added a failing Playwright test that double-clicks a Villager in the live canvas and expects all visible Villagers to be selected. Finished wiring `selectOwnedUnitsByTypeInRect()` through the simulation bridge, then updated the Phaser scene to keep repeated single clicks for stacked-tile cycling while handling true native browser `dblclick` input by reusing the simulation-side viewport/type selector. Updated README controls and added a concise `civ-engine` note about keeping gesture handling in Phaser while leaving selection semantics in deterministic sim code.
**Result:** Double-clicking a friendly unit now selects all visible friendly units of the same type on screen, while ordinary repeated clicks on the same tile still cycle through stacked units, buildings, and resources. The new selection behavior is covered at the scenario, simulation, and browser layers and passes the direct verification gates.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/phaser/scenes/GameScene.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`
**Reasoning:** The user needed the RTS-standard double-click affordance without regressing the newer stacked-tile inspection flow. Keeping the gesture detection in Phaser and the selection rule in the simulation bridge preserves the engine boundary and keeps the behavior deterministic under test.
**Notes:** Verification run: `npx.cmd vitest run tests\simulation\prototypeScenario.test.ts tests\simulation\createSimulationBridge.test.ts`, `npm.cmd run test:browser`, `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`, all passing. Current automated total is `45` passing Vitest tests plus `32` passing Playwright browser gameplay tests. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 23:01, America/Los_Angeles] - Add unit icons to selection HUD
**Action:** Added browser-first HUD coverage for unit icons in three cases: a single selected Villager, a same-type Villager group selected by box drag, and a mixed Villager/Militia/Scout group. Implemented unit-icon rendering in the HUD controller by deriving grouped unit counts from `selectionState.selectedEntityIds` plus the live economy snapshot, then added lightweight badge styling for the new icon strip. Updated README controls and recorded a concise `civ-engine` note that these HUD embellishments stay renderer-side and do not require engine metadata.
**Result:** The selection panel now shows simple unit badges for individual unit selections and grouped unit selections. Same-type groups collapse into one chip with a count such as `x3`, while mixed selections show one chip per selected unit type. Browser coverage now verifies all three icon states, and the repo passes the direct verification gates after the HUD change.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/styles.css`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`
**Reasoning:** The user needed clearer, more readable unit identity in the selection HUD without changing gameplay behavior. Deriving icon groups from selected IDs keeps the UI synchronized with the actual sim selection while avoiding duplicate type bookkeeping.
**Notes:** Verification run: `npm.cmd run test:browser`, `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`, all passing. Current automated total is `45` passing Vitest tests plus `33` passing Playwright browser gameplay tests. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 23:00, America/Los_Angeles] - Remove running-state match summary footer clutter
**Action:** Addressed the confusing bottom-right HUD footer with tests first. Added a failing simulation assertion that the initial running `matchState.summary` is empty, plus failing browser expectations that the `match-summary` footer stays hidden during normal play and only becomes visible with concrete text for victory or defeat. Updated the simulation bridge so a running match starts with an empty summary rather than `Battle in progress.`, and updated the HUD controller so the footer is rendered hidden by default and only unhidden when the match has ended with a non-empty summary.
**Result:** The bottom-right `Battle in progress.` panel is gone during ordinary gameplay, freeing that UI space and removing a misleading status message. End-of-match messaging still appears when useful: conquest victory now shows `All enemy forces have been eliminated.` and defeat shows `All of your units and buildings have been destroyed.`. The new behavior is covered in both simulation and browser tests.
**Files changed:** `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `src/game/simulation/createSimulationBridge.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.test.ts`
**Reasoning:** The always-visible footer consumed interface space without adding actionable information, and its `Battle in progress.` text implied a live combat state even when nothing special was happening. Restricting that footer to real match-end summaries keeps the HUD focused on relevant information while preserving clear outcome feedback.
**Notes:** Verification run: `npx.cmd vitest run tests/simulation/createSimulationBridge.test.ts`, `npm.cmd run test:browser`, `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`, all passing. Current automated total is `45` passing Vitest tests plus `33` passing Playwright browser gameplay tests. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 23:04, America/Los_Angeles] - Remove tick units from queue progress UI
**Action:** Addressed the player-facing queue-progress wording with tests first. Added a failing Playwright expectation that the Town Center training queue card must not contain the text `ticks remaining`, then updated the HUD queue-card renderer to replace the raw tick count with a percent-complete label while keeping the existing visual progress bar.
**Result:** Queue progress is no longer presented in engine ticks. When a unit or research item is queued, the card now shows a normal progress label such as `20% complete` plus the progress bar, which is more understandable to a player than internal simulation units.
**Files changed:** `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`
**Reasoning:** Queue progress is player-facing UI, so exposing engine ticks leaks internal simulation units that are not meaningful to players. A percent label preserves progress clarity without binding the UI to simulation-specific vocabulary.
**Notes:** Verification run: `npm.cmd run test:browser`, `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`, all passing. Current automated total remains `45` passing Vitest tests plus `33` passing Playwright browser gameplay tests. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-11 23:11, America/Los_Angeles] - Strengthen building placement indicators
**Action:** Investigated the report that building placement feedback was not visible enough. Added failing browser coverage that requires not just a valid simulation preview state but also a rendered placement-visual state with per-cell footprint outlines and blocked markers for invalid placement. Extended the Phaser scene with a render-state seam for placement-preview visuals, then strengthened the overlay itself: thicker outline, stronger fill, cell-by-cell footprint grid, and explicit X markers on invalid cells. Updated README wording and added a concise `civ-engine` note about the value of keeping legality in the simulation while exposing a minimal scene render state for browser verification.
**Result:** Building placement feedback is now much more obvious. Valid placements show a clear green footprint grid, and invalid placements show a red footprint with blocked X markers instead of only a faint translucent box. The browser suite now verifies that the scene reports those rendered indicators, not just the underlying placement legality.
**Files changed:** `README.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `docs/engine-feedback.md`, `src/app/bootstrap/browserTestApi.ts`, `src/phaser/scenes/GameScene.ts`, `tests/browser/game.spec.ts`
**Reasoning:** The previous preview technically existed but was too subtle to rely on during play. Strengthening the overlay solves the UX issue without changing placement rules, and the new render-state seam keeps visual regression coverage grounded in the scene's actual output path rather than only backend state.
**Notes:** Verification run: `npm.cmd run test:browser`, `npx.cmd vitest run tests/simulation/createSimulationBridge.test.ts`, `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`, all passing. One full `npx.cmd vitest run` attempt hit a transient timeout in an unrelated long combat test, and the immediate rerun passed cleanly. Current automated total remains `45` passing Vitest tests plus `33` passing Playwright browser gameplay tests. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.
## [2026-04-12 00:18 -07:00] — Fix Human Idle Starts And Render-Synced Building Footprints
**Action:** Finished the authoritative-footprint and strict-human-control slice by keeping human villagers/scouts idle until ordered, updating fixture expectations around the larger building footprints, adding render-state coverage for newly placed construction, and resyncing the `RenderAdapter` snapshot whenever out-of-band command handlers mutate world visuals (construction, garrison, ungarrison).
**Result:** Human starting units no longer auto-act, Town Centers and other buildings use the intended footprint sizes across sim/render/browser layers, newly placed houses now render immediately as construction and later as completed buildings, and the previously failing browser construction-visual test is now green.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/phaser/scenes/GameScene.ts`, `src/app/bootstrap/browserTestApi.ts`, `tests/simulation/createSimulationBridge.test.ts`, `tests/simulation/prototypeScenario.test.ts`, `tests/browser/game.spec.ts`, `docs/engine-feedback.md`
**Reasoning:** The user-reported bug was real: command-side world mutations were bypassing the render diff stream, so construction visuals were missing even though the simulation state was correct. Fixing the render boundary and adding coverage at both the render-state and browser layers prevents the same regression from resurfacing.
**Notes:** Verification passed with `npx.cmd vitest run`, `npm.cmd run test:browser`, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`. The `civ-engine` debugging/rendering guides were useful here because they made the snapshot-vs-diff render contract explicit.
## [2026-04-12 00:49, America/Los_Angeles] - Centralize Footprints And Stabilize Vitest Gate
**Action:** Added tests first for two missing guarantees: a scenario-level assertion that the human starting scout must not spawn with autonomous roam state, and a new footprint-table assertion that covers every currently implemented building type instead of only Town Center and House. Implemented a shared `src/game/content/buildingFootprints.ts` module so placement, occupancy, construction, selection, and rendering all use one footprint table. Updated the default scenario so only AI scouts spawn with `velocity` and `wanderBounds`; the human scout now spawns idle with vision only. After the gameplay fix, addressed the failing `npx.cmd vitest run` gate by extracting the oversized `tests/simulation/createSimulationBridge.test.ts` file into focused `core`, `combat`, and `progression` suites plus shared helpers so Vitest no longer trips the Windows worker RPC timeout after all assertions pass.
**Result:** Building sizes are now centralized and consistent across the sim and renderer, the human player no longer has even dormant scout roam state in the default opening, and the full test gate is stable again. `npx.cmd vitest run` now exits cleanly with `49` passing tests instead of hitting `[vitest-worker]: Timeout calling "onTaskUpdate"`, and browser gameplay coverage still passes with `35` tests.
**Files changed:** `src/game/content/buildingFootprints.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `tests/simulation/buildingFootprints.test.ts`, `tests/simulation/prototypeScenario.test.ts`, `tests/simulation/createSimulationBridge.helpers.ts`, `tests/simulation/createSimulationBridge.core.test.ts`, `tests/simulation/createSimulationBridge.combat.test.ts`, `tests/simulation/createSimulationBridge.progression.test.ts`, `tests/simulation/createSimulationBridge.test.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The runtime behavior was mostly already correct, but the implementation still carried prototype-only state on the human scout and the footprint truth lived in an inline bridge constant. Centralizing the data and removing the unused roam state makes the code align with the intended rules, while splitting the long-running test file keeps the required verification command trustworthy instead of flaky.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npm.cmd run test:browser`. The `civ-engine` debugging guide was helpful as a reminder to reason from world state and render snapshots first, but the main blocker here was repo-level test organization rather than an engine limitation. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.
## [2026-04-12 00:56, America/Los_Angeles] - Strengthen Foundation And Finished-Building Visuals
**Action:** Added stricter browser assertions for building visuals so construction must expose a foundation slab plus scaffold state, while completed buildings must expose a proper structure body plus roof accent and must no longer report foundation/scaffold markers. Updated the Phaser building renderer to draw construction as a visible foundation with scaffold posts and completed buildings as a separate body-and-roof silhouette instead of reusing the same generic tinted rectangle with minor accents.
**Result:** Buildings under construction now render as a clear foundation phase, and once complete they visibly change into a real structure shape. The browser suite now guards the semantic difference directly, so regressions back to ambiguous tinted-box visuals will fail tests.
**Files changed:** `src/phaser/scenes/GameScene.ts`, `tests/browser/game.spec.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user was right that the prior visual treatment did not communicate a real foundation-versus-finished-building transition. Tightening the test contract at the scene level keeps the render path honest without changing gameplay rules or simulation state.
**Notes:** Verification passed with `npm.cmd run test:browser`, `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 01:27, America/Los_Angeles] - Add Sub-Grid Unit Movement
**Action:** Read `civ-engine/docs/guides/sub-grid-movement.md`, then applied the guide's recommended boundary in the game code: kept coarse `position` authoritative for occupancy, visibility, and command semantics, and added a repo-owned `unitTransform` component for finer-grained unit motion and rendering. Wrote tests first for the new invariant in both simulation and browser layers, updated the bridge projector and movement systems to use fine unit transforms, and then tightened the combat suite by extracting a semantic `stepBridgeUntil()` helper and simplifying the building-combat test to a purpose-built conquest fixture instead of a long emergent build-and-train flow.
**Result:** Units now move and render on a finer sub-grid while buildings and resources remain coarse-snapped. Human-visible motion is smoother across ticks, the coarse economy/model state remains stable for selection and gameplay rules, and the full verification gate passes cleanly again with `50` Vitest tests and `36` Playwright browser tests.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/types.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.combat.test.ts`, `tests/simulation/createSimulationBridge.core.test.ts`, `tests/simulation/createSimulationBridge.helpers.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The sub-grid guide matched the repo's current architecture well. A separate fine-movement transform lets the game gain smoother unit motion without redefining building footprints, resource occupancy, or other coarse-grid rules that many existing systems already depend on. Tightening the tests around semantic outcomes rather than fixed whole-cell timing keeps the suite relevant under the new movement model.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npm.cmd run test:browser`. The targeted `npx.cmd vitest run tests/simulation/createSimulationBridge.combat.test.ts` check also passes after simplifying the building-destruction case to a deterministic fixture. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 10:47, America/Los_Angeles] - Add Entity Health Bars Above Units And Buildings
**Action:** Added tests first for health-bar behavior at both the simulation/render-contract layer and the live browser layer. Extended `ProjectedEntityView` to carry `currentHp` and `maxHp` for visible units and buildings, exposed a `getEntityHealth` bridge helper so the projector can read repo-owned combat/building health state, and added a new Phaser overlay that draws health bars above visible units and buildings plus a browser-test seam for inspecting those bar layouts. While debugging the damaged-state assertion, re-read `civ-engine/docs/guides/debugging.md` and used its RenderAdapter guidance to diagnose that health changes living in repo-owned side maps needed explicit render resyncs when damage/destruction occurred, then added those sync marks inside combat and destruction paths.
**Result:** Units and buildings now render health bars above themselves, and those bars update as combat damage lands. The full verification gate passes cleanly with `51` Vitest tests and `37` Playwright browser gameplay tests.
**Files changed:** `src/app/bootstrap/browserTestApi.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/types.ts`, `src/phaser/scenes/GameScene.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.core.test.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** Health bars are renderer-facing UI, but they need authoritative simulation health data to stay trustworthy. Extending the projector keeps the scene dumb, and the additional render-resync hooks make sure repo-owned side-map health changes are visible to the renderer without changing gameplay rules.
**Notes:** Verification passed with `npx.cmd vitest run tests/simulation/createSimulationBridge.core.test.ts`, `npx.cmd tsc --noEmit`, `npm.cmd run test:browser`, `npx.cmd vitest run`, and `npx.cmd vite build`. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 11:02, America/Los_Angeles] - Add Clickable Minimap Navigation And Viewport Rectangle
**Action:** Added browser-first coverage for minimap interaction and camera coverage, then finished the HUD-side minimap slice. The new Playwright tests assert that clicking the minimap pans the camera toward the clicked region and that the minimap exposes a viewport rectangle matching the current camera coverage. Implemented minimap viewport computation and drawing in the HUD controller, wired the HUD to scene camera getters/setters, exposed a scene method to center the camera on world coordinates, and then debugged the remaining failure by checking the DOM overlay contract rather than the simulation. The actual bug was that the HUD root disabled pointer events and the minimap panel never opted back in, so the click was falling through instead of reaching the minimap.
**Result:** Clicking the minimap now recenters the camera on the corresponding map area, and the minimap renders a viewport rectangle that tracks what the main camera currently covers. The browser regression is now green, and the required verification gate passes cleanly with `51` Vitest tests and `39` Playwright browser gameplay tests.
**Files changed:** `src/app/bootstrap/createApp.ts`, `src/phaser/scenes/GameScene.ts`, `src/styles.css`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user asked for RTS-standard minimap navigation and a visible camera rectangle. Keeping the rectangle and click math in the DOM HUD layer preserves the existing architecture: the simulation remains authoritative for world state, the scene owns camera behavior, and the HUD adapts camera state into a clickable overview.
**Notes:** Verification passed with `npm.cmd run test:browser`, `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`. Re-reading `civ-engine/docs/guides/debugging.md` was still useful because it reinforced reasoning from the render/camera contract first; the final defect turned out to be outside the engine boundary in the HUD overlay CSS. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 11:41, America/Los_Angeles] - Enforce Blocked Placement And Movement With Deterministic Fixtures
**Action:** Finished the blocker-rules slice with tests first. Added failing simulation coverage that rejects House placement on trees, gold, stone, water, buildings, and occupied unit cells, plus movement coverage for routing around blocked terrain/resources/units and for snapping blocked destinations to the nearest reachable cell. Updated the simulation bridge to route unit movement through blocker-aware civ-engine grid pathfinding and shared cell-passability helpers, then stabilized the suites by replacing brittle random-map assumptions with focused grass fixtures (`orders`, `mining-camp`, `militia-combat`, and `ai-rush`) and by fixing stale fixtures that spawned units inside Town Center footprints. Tightened both simulation and browser tests to wait for semantic outcomes like completed construction, rally arrival, combat kills, and AI villager losses instead of fixed pre-blocker tick counts or exact occupied-cell assumptions.
**Result:** Houses can no longer be placed on top of trees, mines, buildings, water, or units, and units now route around trees, mines, water, buildings, cliffs/forest terrain, and other units instead of walking through them. The runtime and tests now agree on the stricter occupancy model, and the full verification gate is green with `55` passing Vitest tests and `41` passing Playwright browser tests.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.combat.test.ts`, `tests/simulation/createSimulationBridge.core.test.ts`, `tests/simulation/createSimulationBridge.progression.test.ts`, `tests/simulation/prototypeScenario.test.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The requested behavior change was specifically about blocker legality, so the safest way to land it was to make the blocker rules explicit in tests and then route movement/placement through one passability model. Several failures turned out to be invalid fixtures rather than gameplay bugs; moving those tests onto deterministic scenarios keeps the suite focused on game rules instead of incidental terrain noise.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npm.cmd run test:browser`. Re-reading `civ-engine/docs/guides/debugging.md` and the pathfinding guidance was useful here because they made it clear the engine was correctly rejecting blocked starts/goals; the main follow-up is fixture validation, not an engine workaround. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 16:06, America/Los_Angeles] - Claim Nearby Sheep For Player Units
**Action:** Continued the herdable-ownership slice with tests first. Added failing simulation assertions that nearby starting sheep must surface active ownership in the economy snapshot and selection HUD, then added a new deterministic `sheep-ownership-fixture` plus a browser test that moves a scout into range of a neutral sheep and expects ownership to flip live. Finished the implementation by extending `ResourceComponent`/economy snapshots with active `owner`, wiring sheep-claim evaluation into world boot and a dedicated update system, and updating browser resource helpers to prefer active ownership over static `baseOwner`. While tightening the full suite, replaced one noisy default-map AI economy assertion with a focused `ai-economy-fixture` so gather/drop-off coverage no longer depends on unrelated AI build spending.
**Result:** Sheep now become player-controlled once a nearby unit moves into vision range, selected sheep report their active owner in the HUD, and nearby starting sheep spawn already claimed for the appropriate side. The full verification gate is green with `56` passing Vitest tests, `42` passing Playwright browser gameplay tests, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `tests/simulation/createSimulationBridge.core.test.ts`, `tests/browser/game.spec.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** Herdables are a player-control mechanic, so ownership has to be authoritative in simulation data rather than inferred in the UI. Using a dedicated sheep-ownership system keeps the rule deterministic, and the extra focused economy fixture removes accidental coupling between economy assertions and the enemy rush script.
**Notes:** Verification passed with `npx.cmd vitest run tests/simulation/createSimulationBridge.core.test.ts`, `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npx.cmd playwright test`. Re-reading `civ-engine/docs/guides/debugging.md` was useful again because it reinforced reasoning from projected state and render-affecting mutations instead of guessing in Phaser. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 16:42, America/Los_Angeles] - Add Mouse Camera Panning And Stabilize Browser Camera Coverage
**Action:** Added browser-first coverage for RTS-style mouse camera controls, then implemented middle-mouse drag panning and edge-hover panning in the Phaser scene. Reworked the browser gameplay suite so the new camera behavior is covered by explicit DOM-input tests, while unrelated gameplay assertions that became flaky under edge-hover motion now use the existing in-browser test seam (`selectEntityAtCell`, `issueContextCommand`, `confirmBuildingPlacement`) instead of depending on long Playwright pointer paths. Tightened the sub-grid browser assertion so it checks the real contract (fractional render movement while buildings stay coarse-snapped) instead of overfitting one exact coarse-cell transition.
**Result:** The game now supports middle-button drag panning and hover-at-screen-edge panning, the README documents both controls, and the full verification gate is green again with `56` passing Vitest tests, `44` passing Playwright browser gameplay tests, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`.
**Files changed:** `README.md`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`, `src/phaser/scenes/GameScene.ts`, `tests/browser/game.spec.ts`
**Reasoning:** The user asked for standard RTS mouse panning, but the change also had to preserve trustworthy browser automation. Keeping a few explicit DOM-input tests for camera UX while routing broader gameplay smoke tests through the browser seam keeps the suite comprehensive without letting hover-pan side effects make unrelated gameplay checks flaky.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npx.cmd playwright test`. Re-reading `civ-engine/docs/guides/debugging.md` was helpful here because its debugger/render-adapter framing made it clearer that the failures were test-harness/input-contract issues, not simulation-state faults. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 16:52, America/Los_Angeles] - Rewrite Root Agent Instructions
**Action:** Rewrote the root `AGENTS.md` and `CLAUDE.md` into a shorter shared instruction set. Removed duplicated phrasing, fixed encoding-noise wording, grouped rules by workflow, and made the missing `docs/ARCHITECTURE.md` reference explicit so future agents know to call out the gap instead of silently assuming architectural boundaries.
**Result:** Both root instruction files now match, are easier to scan, and preserve the repo's key operating rules around TDD, verification, git usage, devlogs, architecture maintenance, engine feedback, and subagent prompting.
**Files changed:** `AGENTS.md`, `CLAUDE.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The previous files were mostly duplicated, longer than necessary, and contained a broken architecture reference plus visible encoding artifacts. A concise unified version is more likely to be followed correctly by both primary agents and subagents.
**Notes:** No test or build commands were run because this was a documentation-only change and no commit was created. `docs/ARCHITECTURE.md` still does not exist in this repo; the new wording now states that explicitly.

## [2026-04-12 17:35, America/Los_Angeles] - Fix Exact-Entity Right-Click Combat And Stabilize Browser Input
**Action:** Added failing simulation and Phaser hit-test coverage for exact-entity context commands, then implemented a renderer-aware entity hit-test seam so right-clicking a visible enemy unit resolves against the hovered entity rather than only the coarse tile. Added a deterministic `moving-enemy-attack-fixture` to cover sub-grid-target clicks, fixed a simulation bug where wandering scouts were snapping their fine transform back to the coarse cell every tick, and stabilized browser canvas interactions by switching gameplay clicks to absolute `page.mouse` coordinates while routing non-pointer-specific selection assertions through the browser test API.
**Result:** Selecting a friendly unit and right-clicking a visible enemy unit now reliably orders an attack even when the target is moving on the sub-grid or sharing a tile with other entities. The full verification gate is green with `60` passing Vitest tests, `45` passing Playwright browser tests, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/phaser/scenes/GameScene.ts`, `src/phaser/scenes/entityHitTest.ts`, `tests/browser/game.spec.ts`, `tests/phaser/entityHitTest.test.ts`, `tests/simulation/createSimulationBridge.combat.test.ts`, `tests/simulation/createSimulationBridge.core.test.ts`, `tests/simulation/prototypeScenario.test.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** Cell-based context resolution was too coarse once units had sub-grid motion and tiles could stack buildings, units, and resources. Moving the "what entity is actually under the pointer?" rule into a small renderer-aware seam keeps Phaser responsible for hit geometry while the simulation stays authoritative for semantic command resolution.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npx.cmd playwright test`. Re-reading `civ-engine/docs/guides/debugging.md` was useful because it reinforced inspecting the sim-to-render contract first; the hardest defect was not attack logic, but a fine-transform snapback bug in the sim update loop. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 18:09, America/Los_Angeles] - Stabilize Top HUD Chip Layout
**Action:** Added a failing Playwright regression that records the top-bar chip rectangles before and after live resource changes so HUD items cannot shift around as numbers change. Implemented fixed-width HUD chips with tabular numerals plus a single-row horizontally scrollable top bar so status chips keep stable positions without wrapping down into gameplay space. While running the required full browser gate, a real moving-target attack flake reappeared, so I tightened Phaser unit hit-testing with a small extra pointer-tolerance radius and added a focused Phaser unit test for slight pointer drift on sub-grid units.
**Result:** The top status bar now keeps consistent spacing and no longer shuffles chips when values like food or population change. The top bar also stays on one row instead of consuming extra vertical space that can push command buttons out of reach. Moving-unit right-click targeting is more forgiving and the full verification gate passes with `61` passing Vitest tests, `46` passing Playwright browser tests, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`.
**Files changed:** `src/styles.css`, `src/ui/hud/createHudController.ts`, `src/phaser/scenes/entityHitTest.ts`, `tests/browser/game.spec.ts`, `tests/phaser/entityHitTest.test.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user-facing bug was a layout contract problem, not a simulation issue: fixed widths solved number jitter, but wrapping created a second-order regression by stealing vertical space from the command panel. Keeping the top bar as a scrollable single row preserves stable HUD positions and playfield space at the same time. The extra hit-test tolerance follows the same principle for sub-grid units: live UI interactions need a small amount of forgiveness to match what players perceive visually.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npx.cmd playwright test`. Re-reading `civ-engine/docs/guides/debugging.md` was useful as a reminder to inspect the render/input seam directly once the browser failure appeared, rather than assuming the moving-target issue was in combat logic. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 18:43, America/Los_Angeles] - Add Shoreline Fish Resources And Stabilize Moving-Target Browser Control Coverage
**Action:** Added fish support with tests first across the scenario, simulation, and browser layers. Extended the scenario generator to place neutral shoreline `fish` resources only on water cells that have adjacent gatherable land, added a deterministic `fish-fixture`, taught the simulation bridge to treat fish as a food source with gather amounts, HUD naming, tinting, and render sizing, and added browser coverage for selecting and harvesting fish live. While closing the required full browser gate, the moving-enemy right-click attack test exposed a deeper seam issue, so I extracted the GameScene's world-position context-command resolution into a reusable browser-test seam and updated the moving-target browser assertion to stop only when the militia actually enters the `attacking` task instead of accidentally overwriting attack orders with later fallback move commands.
**Result:** Standard maps now include shoreline fish, villagers can gather food from fish, and `fish-fixture` provides deterministic coverage for the shoreline-food loop. The full verification gate is green again with `63` passing Vitest tests, `47` passing Playwright browser tests, `npx.cmd tsc --noEmit`, and `npx.cmd vite build`.
**Files changed:** `src/app/bootstrap/browserTestApi.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/phaser/scenes/GameScene.ts`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.core.test.ts`, `tests/simulation/prototypeScenario.test.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** Fish are part of the baseline AoE2 food economy, so the map generator and gather loop need first-class shoreline food support instead of relying only on herdables, berries, and boar. The browser-control fix was necessary because the repo policy requires the full test gate on every commit, and the failing moving-target test was revealing a real seam mismatch between exact rendered targeting and fallback move orders rather than a fish-specific gameplay bug.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npx.cmd playwright test`. Re-reading `civ-engine/docs/guides/debugging.md` remained useful because it kept the diagnosis focused on the sim-to-render/input contract instead of guessing in browser automation. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 19:01, America/Los_Angeles] - Prevent Trapped Unit Spawns Around Buildings And Starts
**Action:** Added tests first for three spawn-safety regressions: trained Scouts must be able to leave a Stable after spawning, a fully ring-blocked Stable must hold its queue instead of spawning into an invalid tile, and a scenario-marked Scout spawn must relocate if its requested cell would trap it. Implemented shared spawn-safety helpers in the simulation bridge that check passability plus immediate egress, switched production and ungarrison to use footprint-aware perimeter spawn candidates, clamped blocked queue timers at `0`, and introduced an opt-in `requiresSafeSpawn` scenario flag so only intended starts are relocated. Added focused `blocked-stable-spawn-fixture` and `isolated-scout-spawn-fixture` scenarios, and reordered the default opening package so starting villagers and scouts are evaluated for safe spawn positions after nearby resources/forest patches exist.
**Result:** Scouts no longer spawn trapped around producers, blocked Stables keep their queued unit blocked instead of materializing it inside bad cells, and marked starting-unit spawns can relocate to a nearby legal cell without disturbing fixtures that intentionally stack entities. The full verification gate is green with `65` passing Vitest tests, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `47` passing Playwright browser tests.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `tests/simulation/createSimulationBridge.core.test.ts`, `tests/simulation/createSimulationBridge.progression.test.ts`, `tests/simulation/prototypeScenario.test.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The original producer spawn helper only probed a few hardcoded offsets and could fall back to the producer anchor itself, which is not a defensible RTS rule once footprints and blockers are enforced. Keeping scenario relocation opt-in preserves deterministic fixture intent while still fixing the real opening/spawn safety problem for gameplay-owned starts.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npx.cmd playwright test`. Re-reading `civ-engine/docs/guides/debugging.md` was useful because it reinforced diagnosing the issue from world passability and render state rather than from Phaser symptoms. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 19:19, America/Los_Angeles] - Refine Simulation Bridge For civ-engine 0.3.0
**Action:** Added a failing regression that requires sheep ownership to update in the same tick a scout enters claim range, then refined `createSimulationBridge.ts` around `civ-engine 0.3.0`. Introduced a typed owned world/component registry alias for the bridge, switched sheep-claim target collection and ranged enemy search to `queryInRadius`, and declared explicit `before`/`after` ordering for the key update-phase systems so the live rule order is no longer implied by registration order alone. During full-gate verification, the new runtime failed because the linked `civ-engine` package was still serving stale built artifacts even though its docs/source already described 0.3.0 APIs, so I rebuilt the linked package and then normalized helper signatures that had over-inferred the typed world through default parameters.
**Result:** The bridge now uses `civ-engine 0.3.0` features in a stable way: typed world ownership where the bridge owns the instance, explicit system ordering for `prototypeAi` -> `prototypePlayerCommands` -> movement/economy -> herdable ownership -> visibility -> tower combat, and live spatial queries for sheep claiming and in-range enemy targeting. The full verification gate is green with `66` passing Vitest tests, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `47` passing Playwright browser tests.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `tests/simulation/createSimulationBridge.core.test.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** This was the highest-value narrow 0.3.0 refinement: it improves readability and determinism in the simulation bridge without forcing a broad content or UI rewrite. The same-tick sheep regression gives the new ordering constraints a real gameplay contract instead of treating them as a cosmetic refactor.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npx.cmd playwright test`. Re-reading `civ-engine/docs/guides/debugging.md` remained useful because it kept the diagnosis focused on the bridge/runtime contract; the biggest surprise was not a gameplay defect but stale `dist` artifacts in the linked engine package. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 19:27, America/Los_Angeles] - Trim Top HUD To Player-Facing Status Only
**Action:** Added a failing browser regression that requires the top HUD bar to expose only `food`, `wood`, `gold`, `stone`, `age`, `pop`, and `time`, while keeping chip positions stable as values change. Then simplified `createHudController.ts` so the top bar renders only those seven chips, replaced the old debug-oriented `tick`/`entities`/`seed`/`outcome` DOM fields with a player-facing `time` chip derived from tick count and TPS, and updated the browser suite to read boot seed and match outcome from the test snapshot / match summary instead of the player HUD.
**Result:** The top status bar now shows only player-facing information, elapsed match time is formatted as `MM:SS`, and the full verification gate is green with `66` passing Vitest tests, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `47` passing Playwright browser tests.
**Files changed:** `src/ui/hud/createHudController.ts`, `src/styles.css`, `tests/browser/game.spec.ts`, `docs/devlog-detailed.md`
**Reasoning:** The previous top bar still carried several debug-era values that are useful to agents and tests but noisy for a player. Moving those checks to the browser seam/test snapshot preserves coverage while making the visible HUD much closer to an RTS player view.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npx.cmd playwright test`. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 20:12, America/Los_Angeles] - Finalize Player-Facing Selection Details Panel
**Action:** Finished the player-facing selection-panel slice by keeping the HUD focused on useful selection details instead of debug metadata. The selection card now surfaces name, icon, health, attack, armor, faction, civ, and inventory for individually selected units/buildings/resources, while omitting position/cycle/resource-debug rows, empty queue text, and inactive placement text. To close the full browser gate, I updated the browser tests to use a true two-click same-type selection flow and added an exact-building selection helper for the newly built secondary Town Center so the test no longer depends on unit/building overlap on a live footprint.
**Result:** The bottom selection menu now reads like a player HUD instead of a debug panel, and the full verification gate is green with `68` passing Vitest tests, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `47` passing Playwright browser tests.
**Files changed:** `src/game/simulation/types.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/createSimulationBridge.ts`, `src/ui/hud/createHudController.ts`, `src/styles.css`, `src/phaser/scenes/GameScene.ts`, `tests/browser/game.spec.ts`, `tests/simulation/createSimulationBridge.core.test.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The user is playing the game, not debugging it, so the bottom panel must prioritize useful combat/economy identity data over internal coordinates and other diagnostic fields. The browser regressions were interaction-contract issues, not gameplay-system issues, so the safest fix was to keep the runtime behavior player-centric and make the tests deterministic where exact overlap/click sequencing otherwise obscured the real contract.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npx.cmd playwright test`. Re-reading `civ-engine/docs/guides/debugging.md` helped confirm the selection state projection itself was correct; the remaining flake was at the browser interaction seam, which still needs repo-owned helpers rather than more engine-level diagnostics. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.

## [2026-04-12 21:12, America/Los_Angeles] - Add Render-Time Unit Interpolation
**Action:** Added tests first for render interpolation: a new Phaser unit test locks down interpolation math for unit vs non-unit projected entities, and a new Playwright test requires live displayed unit positions to diverge from the raw projected tick positions while a unit is moving. Implemented a narrow render-layer interpolation path by exposing interpolation alpha from the simulation bridge, adding a small Phaser utility that interpolates only unit positions between the previous and current projected tick states, making the scene re-render when interpolation alpha changes, and routing scene drawing, selection rings, health bars, and world-point hit testing through the interpolated entity list. Extended the browser test seam to expose displayed entities directly.
**Result:** Units now move smoothly between simulation ticks in the live game while buildings and resources remain coarse-snapped, and clicks/selection overlays stay aligned with what the player sees on screen. The full verification gate is green with `70` passing Vitest tests, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `48` passing Playwright browser tests.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `src/phaser/scenes/GameScene.ts`, `src/phaser/scenes/entityHitTest.ts`, `src/phaser/scenes/interpolateProjectedEntities.ts`, `src/app/bootstrap/browserTestApi.ts`, `tests/phaser/interpolateProjectedEntities.test.ts`, `tests/browser/game.spec.ts`, `docs/engine-feedback.md`, `docs/devlog-detailed.md`, `docs/devlog-summary.md`
**Reasoning:** The `civ-engine` sub-grid guide recommends keeping simulation occupancy/pathing authoritative on the coarse grid and handling visual smoothness separately. Following that boundary avoided changing gameplay semantics, while still delivering the smoother movement the player asked for. Updating hit testing to prefer displayed entities was necessary so the new interpolation would not desynchronize clicks from visible unit bodies.
**Notes:** Verification passed with `npx.cmd vitest run`, `npx.cmd tsc --noEmit`, `npx.cmd vite build`, and `npx.cmd playwright test`. Re-reading `civ-engine/docs/guides/sub-grid-movement.md` was directly useful here: the right fix was renderer interpolation plus a repo-owned view seam, not a deeper rewrite of `World.position` semantics. `docs/ARCHITECTURE.md` still does not exist in this repo, so there was no architecture file to update for this slice.
