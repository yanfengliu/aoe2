**Findings**
- **Medium — Correctness / Test coverage** — Where: `src/ui/hud/saveLoadPanel.ts:159-165`, `tests/browser/game-combat-and-meta.spec.ts:403-451`. Finding: the Save flow still drops the only guaranteed export path if `localStorage.setItem(...)` throws. The JSON blob is already built, and `triggerBlobDownload(json)` exists, but the handler returns early after `showToast('Save failed (storage unavailable).')`, so private-browsing/quota/security failures lose the save entirely instead of falling back to download. The browser coverage only exercises the happy-path localStorage save/load flow, so this branch is still uncovered. Fix shape: keep the download fallback on storage-write failure and add one browser/unit case that forces `localStorage.setItem` to throw.

- **Medium — Documentation accuracy / process drift** — Where: `docs/architecture/ARCHITECTURE.md:11`, `src/app/bootstrap/browserTestApi.ts:89-120,239`, `README.md:10-44`, `AGENTS.md:105-117`, `docs/devlog/summary.md:3`, `package.json:3`, `docs/changelog.md:5`. Finding: the canonical docs contract no longer matches the repo. `ARCHITECTURE.md` still says `app/bootstrap/` hosts a “dev HTTP API used by browser tests”, but the actual seam is the in-page `window.__AOE2_TEST__` API installed by `installBrowserTestApi(...)`. `AGENTS.md` requires `docs/api-reference.md`, `docs/README.md`, and `docs/guides/<topic>.md`, plus README “Feature Overview” / “Public Surface” coverage for shipped user-visible/public surfaces; those docs are absent, and the current `README.md` only has setup/controls/test-fixtures sections, so newer surfaces like save/load are not covered there. Separately, `docs/devlog/summary.md:3` still says V4-22 versioning + `docs/changelog.md` “needs user input”, but the repo already has `package.json:3` at `0.1.1` and `docs/changelog.md:5` with a `0.1.1` entry. Fix shape: either bring the repo up to the doc structure AGENTS mandates, or relax AGENTS to the smaller doc surface actually maintained here; in either case, update `ARCHITECTURE.md`, `README.md`, and `docs/devlog/summary.md` to match the current code and repo state.

**Verified-not-bugs**
- `createSimulationBridge.ts` is no longer regrowing into a god class; the current file is 284 LOC, and the bridge split described in `docs/devlog/summary.md:2` matches the code layout I spot-checked.
- The bridge-side hotspots you called out look structurally fine in the current tree: `wirePostSeedOps.ts` is 320 LOC, `registerAllSystems.ts` is 334 LOC, and `aiSystem.ts` is 462 LOC.
- The previously deferred multi-base AI producer bug is fixed: `src/game/simulation/bridge/aiDecisionOps.ts:153-176` now chooses the least-loaded producer instead of first match.
- The previously flagged Wonder / FU2 display-name gaps are fixed: `src/ui/hud/displayNames.ts:98,127,133,135,231,260,266,268,514,573`.
- Mutual-annihilation draw behavior is fixed and explicit in `src/game/simulation/bridge/systems/conquestOutcomeSystem.ts:15,74-77`.
- The Unicode seed-hash collision issue is fixed: `src/game/simulation/mapGeneration/sharedTerrainHelpers.ts:28-29` now hashes full code points.
- HUD/browser-test teardown is no longer a live bug: `src/ui/hud/createHudController.ts:89,271,289` exposes `destroy()`, and `src/app/bootstrap/browserTestApi.ts:120-239` uses the dynamic bridge thunk / `__AOE2_TEST__` seam rather than the old stale-reference shape.
