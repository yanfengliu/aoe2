Here is the verification of the Iteration 2 batch-1 fixes on `agent/review-findings-2026-04-26-iter2-batch1`.

- **V5-1** **OK**. The `monksByOwner` map is correctly integrated into `BridgeState` and reliably synchronized in `entityCreateOps`, `entityDestroyOps`, and `monkTaskAppliers`. The hydration correctly reconstructs it from the `world.query('unit')` post-load. The AI loop safely queries the side map in O(1) time. The conversion flip gracefully cleans up the previous owner's Set, appropriately deleting the key entirely if it drops to size 0. All monk creation/destruction passes through `addUnitEntity` and `destroyUnitEntity`, so there are no sync holes.
- **V5-2** **REVERTED-VALID**. Verified the revert in `bridge/aiDecisionOps.ts`; the zero-target skip logic (`if (desired <= 0) continue;`) is intact and the load-bearing Dark Age opening behavior was correctly protected and documented.
- **V5-3** **OK**. `ALL_UNIT_TYPES` and `isUnitType` are cleanly extracted into `unitTypeMap.ts`, maintaining the type-safe `satisfies Record<UnitType, true>` constraint without drift. `GameScene.ts` shrinks below the threshold to 994 LOC.
- **V5-4** **OK**. `saveLoadPanel.ts` maintains its state in a `try/catch` and falls back correctly to `triggerBlobDownload(json)` if `localStorage.setItem` fails. The browser test accurately intercepts the thrown Storage exception and validates the URL download payload.
- **V5-5** **OK**. `ARCHITECTURE.md` (updated test API seam), `AGENTS.md` (relaxed mandates), and `docs/devlog/summary.md` are all accurately updated, completely resolving the multi-site doc drift.
- **V5-6** **OK**. `saveLoad.test.ts` now securely pulls a live, valid villager ID during simulation rather than using a fake ID that would be naturally pruned. The test properly asserts the round-trip throttle retention.
- **V5-7** **REVERTED-VALID**. Verified as correct-by-construction; no test-coverage changes applied.
- **V5-8** **OK**. Precomputing `ownerBuildingsByType` within the AI's tick works perfectly. It freshly pulls `activeWorld.query('building')` making it resilient to runtime structure additions and safely encapsulates the same `!construction.isComplete`, `queueLength >= 2`, and ID-tiebreaker conditions the old `findIdleProducer` used. `findIdleProducer` is safely pruned from all `aiDecisionOps`, `registerAllSystems`, and dependency types.

**Conclusion:** The fix batch is sound. Type safety is maintained, save/load determinism remains pristine with the exact same ID tie-breakers, and the `monksByOwner` invariant handling is clean. There are no newly introduced issues.
