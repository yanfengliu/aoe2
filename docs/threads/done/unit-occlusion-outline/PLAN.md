# Unit occlusion outline — PLAN

Mechanism per DESIGN.md (host-only overlay silhouette). TDD per step; version target v0.2.5.

STATUS 2026-07-15: all steps executed and green; iteration-1 review findings fixed (see 2026-07-15/1/REVIEW.md); iteration-2 confirmation running. Deviations from the plan as written, each deliberate: step 3 concatenates silhouette parts in the adapter's assembly next to the overlay funnel (state retention lives there per the updateHitFeedback precedent) instead of widening createAoeVoxelOverlayParts' signature; step 4 keeps OccludedUnitState in the silhouette module rather than viewTypes (single-owner type, fewer hops); step 5's pause-stability assertion uses the seam's JSON stability across animation frames (the renderer suite already pins sha256 frame identity under pause) and the real-default-view probe was superseded by the default-map capture evidence plus the boot-scene cue analysis; the predicate itself evolved in iteration 1 (mid-body anchor + hairline occluder filter — see DESIGN.md mechanism).

1. **Fixture.** Add `occlusion-showcase-fixture` to `SCENARIO_FACTORIES` (`src/game/simulation/prototypeScenario/dispatch.ts`): one completed player-1 Town Center, one villager parked at the fully-occluded cell diagonally behind it, one villager in open ground, fog on, deterministic. Unit test: factory returns those entities (mirror existing factory tests if present; otherwise covered by the browser spec boot).
2. **Pure predicate + silhouette parts module** `src/rendering/voxel/aoeVoxelOcclusionSilhouettes.ts` (≤500 LOC): `computeOccludedUnits(prepared, nowMs)` → `{ occluded: OccludedUnitState[], parts: VoxelPart[] }`. Failing unit tests FIRST in `tests/rendering/aoeVoxelOcclusionSilhouettes.test.ts` — the 12-case matrix from DESIGN.md acceptance section.
3. **Wire into overlay funnel.** Extend `createAoeVoxelOverlayParts` (`aoeVoxelOverlayParts.ts`) to append silhouette parts; retain last-computed `OccludedUnitState[]` adapter-side following the `updateHitFeedback` precedent (`aoeVoxelAdapter.ts:355-384`). Unit tests: overlay test file asserts `ui:occlusion:<id>:<gen>:<suffix>` instance keys exist for the occluded unit and none for the open one; batch keys unchanged (no 8th batch).
4. **Seam.** `OccludedUnitState` in `src/rendering/viewTypes.ts`; getter on `AoeVoxelWorldRenderer` (state retained from adapter); thin passthrough on `AoeVoxelGameView` (450→must stay ≤500); `getOccludedUnitStates()` on `browserTestApi.ts` (both interface blocks + impl). Unit test at coordinator/renderer level.
5. **Browser spec** `tests/browser/voxel-occlusion-outline.spec.ts`: boot fixture → seam lists exactly the hidden villager; `captureWorldFrame` pixel probe at `worldToScreen(hidden villager)` finds white pixels; open villager NOT listed; `instanceBatches === 7` still; paused double-capture sha256-identical; real-default-view probe on `aoe2-prototype` (move villager behind TC via test API, advanceTicks, probe white > 0).
6. **Evidence.** `scripts/captureMapScreenshot.mjs` before/after on the fixture (and the default map), `scripts/diffMapScreenshots.mjs` counts into the devlog. Screenshot artifacts under `docs/devlog/artifacts/`.
7. **Gates.** Affected tests while iterating; then `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`, plus the explicit headless Chromium suite compared against the known pre-existing red floor on clean main (capture baseline BEFORE my diff per project discipline).
8. **Adversarial review** (in-process Workflow: finder dimensions = fog-leak, determinism/replay, predicate-vs-paint drift, perf, batch/LOC budgets; refuting verifiers on each finding) → fix → re-review to convergence → `docs/threads/current/unit-occlusion-outline/<date>/1/REVIEW.md`.
9. **Docs + ship.** Spec §14.5: rewrite line-1408 bullet as shipped behavior + remove "white behind-building unit x-ray" from the line-1395 not-present list; changelog v0.2.5 entry; devlog detailed + summary; version bump; commit to main; push; move thread folder to `docs/threads/done/`.

Risks tracked: silhouette pose desync vs animated batch (mitigated: same AtTime math; test 11 compares against `inspectPresentedVoxelPartMatrix`); screenLockedDepthOffset assumptions when reused outside the marquee (verify against its implementation before coding); fixture boot determinism (mirror existing fixture factories exactly).
