# Implementation plan — agent-affordances

TDD order; each step = failing tests first, then implementation. Affected-tests-only while iterating; full gates before commit.

1. **researchAvailability.ts** (new, pure): `createResearchAvailability(deps)` → `researchUnavailableReason(owner, buildingType, tech): string` (always a complete sentence; in-flight handling lives in the validator's `in_flight_tech` message + buildingOptionsOps' locked entries instead). Deps as shipped: `getPlayerAge`, `hasTechnology`, `countCompletedAgePrerequisites`, `getResearchOptions`. Tests: new `tests/commands/researchAvailability.test.ts` — age-up reasons (count 0/1, wrong age, already past), already-researched, fallback lists currently-available.
   - Prereq exports: `prototypeBuildingRules.ts` gains `buildingsThatResearch(tech)` + `agePrerequisiteBuildingTypes(forTech)` + `AGE_ADVANCE_REQUIRED_COUNT`; `playerQueries.ts` gains `countCompletedAgePrerequisites(owner, forTech)`.
2. **prototypeEconomyRules.ts**: `describeMissingResources(stockpile, cost): string | null` ("need 500 food (have 320)"; multi-resource joins with ", "). Tests in the existing economy-rules test file.
3. **queueResearchValidator**: split checks; messages from 1+2. Extend `tests/commands/queueResearch.test.ts` with message assertions. `queueTrainValidator`: insufficient-resources detail only.
4. **cellPassability.ts**: `describePlacementBlockers(x,y,w,h): PlacementBlockReport | null` (cause naming via `getCellStatus` claims + tiles terrain kind + component lookups) and `findOpenPlacementAnchors(centerX, centerY, w, h, {max, maxRadius?, isCellVisible})`. Tests: new `tests/simulation/placementHints.test.ts` — cause classification, ring determinism, visibility gating, none-found, maxRadius.
5. **buildingPlaceConfirmValidator**: `placement_blocked` message composed from describer + nearest-anchor suggestion + footprint; insufficient-resources detail. Extend `tests/commands/buildingPlaceConfirm.test.ts`.
6. **buildingOptionsOps.ts** (new): `getAgentBuildingOptions(ownerId)`. Wired in wirePostSeedOps (the 605 pin forced both the validator-deps extraction to `commandValidatorDeps.ts` AND moving this wiring out of wireBridgeOps); exposed via createWorldResult → facade → `SimulationBridge` + `BrowserTestBridge` + `makeReplayBridge` (+ `findOpenPlacementAnchorsNear`). Integration test at bridge level: fresh dark-age scenario → TC shows feudal-age locked with the prereq reason ("you have 0"); villagerCanBuild house entry pinned; anchors near TC non-empty/deterministic.
7. **Snapshot + prompt**: `AgentStateSnapshot.buildingOptions` + `placementHints` (types.ts), optional inputs in agentSnapshot.ts, composition in browserTestAgentApi.ts, rendering + tool-description update in llmPromptBuilder.ts. Extend agentSnapshot + llmPromptBuilder tests.
8. **HUD pass-through**: humanInputOps/placementOps fallback toasts use `result.message`; check/extend any pinned toast-string tests.
9. Full gates → multi-CLI review (Claude + Gemini; retry Codex) → docs (spec §15.7 + rejection contract, ARCHITECTURE + drift-log, changelog 0.1.21, devlog, README check) → commit + push.
10. Campaign-2: `playtest:llm --max-ticks 6500 --cost-budget 20 --observation`; mine results into devlog; expect Feudal reached.
