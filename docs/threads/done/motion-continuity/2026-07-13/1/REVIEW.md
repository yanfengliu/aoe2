# Unit motion continuity and facing — adversarial review iteration 1

## Scope

Three independent in-process Codex reviewers read the live codebase and diff. They checked fine-transform publication, occupancy-slot arrival, blocked autonomous movement, replay timing, role-facing geometry, presented hit/shadow parity, deterministic headless browser evidence, Voxel pin reproducibility, and documentation accuracy. Every review prompt required claims to be verified against live symbols, signatures, and paths.

## Findings and disposition

- **[HIGH, closed] Blocked autonomous movement could publish an over-budget slot reset.** The first repair still called the stationary slot sync before choosing an escape heading; canary scout 2259 moved three fine units on one axis where ordinary movement permits two. Removing the write restored continuity but regressed the existing 2,000-tick no-livelock oracle. The final path validates the candidate, recenters toward the allocated in-cell slot by at most one normal fine step, and uses the slot-relative escape probe. Numeric snapshots over all 2,000 ticks prove a maximum per-axis delta of two while every 600/600/600/200-tick block visits at least 3 cells.
- **[MEDIUM, closed] The arrival test stopped at coarse-cell entry.** It could pass before the transform converged on the destination's allocated slot or the move command cleared. The test now waits for the occupied-destination unit to become idle, pins the allocated endpoint to `(7.5, 10)`, proves a subsequent tick stays stable, reselects the unit, and compares rendering with the canonical transform.
- **[MEDIUM, closed] Independent axis limits admitted a diagonal arrival snap.** A `(0.5, -0.5)` last-step jump satisfied both old per-axis assertions despite moving 0.707 world units. The test now applies the shared Euclidean `UNIT_SUBGRID_STEP_PER_TICK / UNIT_SUBGRID_RESOLUTION` bound to every root step.
- **[LOW, closed] Replay coverage did not pin the named 250 ms policy.** The old `>0 && <=3 ticks` assertion accepted several contradictory caps. The regression now requires a 10-second callback gap to advance exactly two 100 ms ticks with interpolation alpha 0.5, and the shared-policy test pins the exported cap to 250 ms.
- **[LOW, closed] A future-compatible renderer fake used an unsafe metrics assertion.** The fake still returns a structural superset for the newer dirty local Voxel worktree, but no longer casts the object to `ThreeRenderMetrics`; its implemented runtime contract therefore continues to catch newly required fields. The type-only import remains for the runtime-state member.
- **[MEDIUM, closed] Canonical docs overstated earlier evidence and misstated blocked movement.** The original 0.1867-step browser measurement is now explicitly historical performance/facing evidence captured before the publication fix. The debug record, spec, changelog, architecture path, decision, detailed devlog, and summary describe bounded recenter, allocated-slot convergence, the exact replay cap, and the new deterministic browser limit.
- **[HIGH, closed] Fresh transforms began at an identity fallback instead of the allocated spawn slot.** The complete gate exposed perpendicular first movement through fog/selection timing failures: a north command spent its first tick moving west. Spawn occupancy now allocates first and publishes the stationary transform at that slot; the cardinal regression requires zero perpendicular delta and the fog/selection tests wait for their actual visibility prerequisite rather than an incidental three-tick route.
- **[HIGH, closed] Current fine position is not the same as a crowded moving unit's assigned slot.** The first save/load repair rebuilt occupancy from the serialized fine root. A unit at fine X 80 still moving toward assigned X 83 instead became owner of slot X 80 after load, cleared its command, and diverged from uninterrupted play. `UnitTransformComponent` now additively persists the assigned numeric offset, and the 16-id-collision regression compares loaded and uninterrupted next transform plus task.
- **[HIGH, closed] Missing slot fields conflated legacy saves with authoritative overflow.** A lower-id scout saved after entering a full 16-slot cell was promoted into slot `(0,0)` during entity-id reconstruction, displacing a higher-id saved occupant. Current overflow now persists an explicit marker; reconstruction restores saved numeric assignments first, legacy no-field units second, and explicit overflow last. The regression fills all 16 slots with higher-id units and proves both immediate loaded state and the next tick match uninterrupted play.
- **[HIGH, closed] Normal phase-three allocation still promoted explicit overflow when a peer had freed a slot before save.** Exact overflow authority must describe what uninterrupted play owned, not what load could opportunistically allocate. Reconstruction now recreates the overflow claim directly. The same investigation found that live arrival's same-cell-free branch never performed its documented rebind; it now binds the slot but returns the same command target, keeping movement active until the root reaches the new assignment by normal bounded steps. The peer-freed-before-save regression compares loaded and uninterrupted state on every tick and enforces the Euclidean fine-step budget through idle convergence.
- **[MEDIUM, closed] The live `node_modules/voxel` junction targets a newer dirty sibling worktree.** The exact SHA and 0.1.4 lock metadata were verified, then a disposable checkout resolved Voxel from reachable commit `faa00bf2ef83e5134bd9ca62c0b36cd0cfbe394b`; all 32 changed source/test/config/lock files hash-matched the live AoE worktree. Its complete gate passed 1,940 unit tests plus two skips, 105 browser tests plus two skips, lint, typecheck, and build. The separately requested live-link run passed all unit tests before correctly stopping at AoE typecheck on three new uncommitted Voxel metrics fields; no compatibility code was added for that unreproducible future surface.
- **[MEDIUM, closed] Selection regressions initially blamed the production picker but were invalid test geometry.** Stationary slot alignment placed three villagers at centers `5.75`, `7.0`, and `8.25`; the old widest-pair midpoint landed exactly on the middle unit. The shared chooser now excludes any third circular body covering the point, and the actual click/marquee production paths still assert the resulting point and strip are empty. The moving-body proof now requires the displayed root to have crossed its initial cell while the authoritative task remains `moving`, removing both an interpolation-alpha race and an arrival-only false pass. The presented epoch/revision-bound animated silhouette proxy is unchanged.
- **[MEDIUM, closed] The health-bar browser proof raced its own hostile fixture.** Live boot could let the militia damage the enemy house before the test asserted 75/75 HP. Paused boot captures the pristine baseline; the existing explicit 80-tick advance still performs and presents combat damage. The health and moving-body cases passed 20/20 across ten repetitions each, followed by the full browser gate.
- **[LOW, closed] The active spec retained a former-renderer elevation claim.** `ProjectedEntityView` and the live projector already carry terrain elevation; only `compositionGround` deliberately flattens it for the current voxel adapter. The canonical terrain-texture bullet now states that boundary and pairs raised-terrain presentation with matching presented-state picking, while dated Phaser-era and changelog records remain unchanged.

## Verified contracts

- Fine-grid commanded and autonomous movement publishes immutable replacement components through civ-engine dirty tracking.
- Commanded target construction and arrival testing use the current engine-allocated occupancy slot; render roots match canonical fine transforms through command completion and reselection.
- Stationary spawns begin at their allocated slot, and additive assigned-slot/explicit-overflow persistence makes legacy, crowded mid-movement, live-load, and replay reconstruction preserve the serialized root and deterministic continuation.
- Blocked scouts preserve both the ordinary movement bound and the pre-existing deterministic anti-livelock behavior.
- Complete authored actors, contact shadows, and prepared hit silhouettes share the travel-facing transform; semantic-front assertions cover every unit role.
- The deterministic 84-slice headless Chromium corner path requires more than 40 distinct roots, more than 20 interpolated non-quarter-grid samples, root steps at most 0.1 world units, heading steps at most 0.25 radians, and positive facing/velocity alignment for every moving sample.
- Replay and live hosts share one renderer-neutral 250 ms visible-frame policy; explicit scrubs and unrelated identity discontinuities still snap.

## Current disposition

Approved. All implementation, persistence, browser-contract, and documentation findings are closed. The isolated exact-pin `npm run verify` and both requested audits are green; the live-link boundary is recorded honestly and belongs only to the unrelated uncommitted sibling Voxel worktree. Port 4173 is free, no task-owned browser/server process remains, and both disposable checkouts plus their temporary logs were removed without touching the sibling repositories.
