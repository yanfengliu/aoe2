# Narrow-choke queueing plan

1. Reproduce the complaint on `narrow-corridor-fixture` through the authoritative coarse/fine debug snapshot.
2. Add RED bridge tests for single-file admission, centered lane geometry, eventual clearance, repeated-run determinism, and mid-queue save/load parity.
3. Add local traffic arbitration without changing permanent-only A* passability.
4. Run focused movement tests, independent adversarial review, the four repository gates, and both dependency audits.
5. Record measured counts, commit the coherent v0.3.2 unit, and push `main`.
