# AI findings → annotation bridge (v0.1.36) — Review iteration 1

Change under review: bridge the playtest conformance-findings pass into the existing marker system — emit each finding as an `author:'agent'` Marker injected into the run's SessionBundle (`<prefix>.json`), so replaying that bundle surfaces the findings in the existing `MarkerListPanel` list + `TimelinePanel` pins with no new render code. New pure module `src/game/playtest/findingsToMarkers.ts` (`findingsToMarkers`/`injectAgentMarkers`/`deriveAnchorTick`); `scripts/playtest-findings.mjs` glue does the in-place idempotent bundle injection. Design produced + approved in a design-first phase (DESIGN.md), then implemented — both by a fresh-context subagent; reviewed + gated by the main agent.

Reviewers: Codex (gpt-5.5, xhigh, BEGIN/END markers), Claude (opus[1m], --effort max, Read/Glob/Grep), Gemini (gemini-3.1-pro, plan mode). All three read the live codebase. Contamination audit after the batch: `git diff` (unstaged) empty — no reviewer wrote to the tree. Gemini hit a transient 429 (RESOURCE_EXHAUSTED) mid-run but recovered and returned a full review.

## Verdict: APPROVE — no HIGH/MEDIUM correctness bug. Converged at iter-1.

Claude verified all 8 scrutiny points against the live code (not diff-trust): idempotent in-place overlay correct (filters `author==='agent'`, preserves human/engine markers, never duplicates); `--no-llm` marker-strip is consistent-by-design (matches the script's existing `envelope.findings=[]` + `findingsNote`-delete "reflect THIS run" semantics — NOT gating it is correct, else bundle/envelope split-brain); `deriveAnchorTick` edges sound; Marker shape valid (tick required + refs optional confirmed in the engine type; `isAoeMarkerData` tolerates the extra finding-detail keys); severity/category mapping lossless; pure/deterministic (clock injected via ctx); renders for free on both surfaces (verified the replay click path never touches `refs`); docs match. Gemini independently verified all 8 → "ship it." Codex found no correctness bug.

Findings were all LOW + doc-accuracy; the implementation is correct as written. Because iter-1 surfaced no functional defect, this is the nitpick-convergence point; the fixes below (doc corrections + one 2-line atomic-write hardening that node-parses + the unchanged 18-test suite still green) do not warrant an iter-2 re-review.

## Findings and disposition (all addressed this commit)

| # | Severity | Source | Finding | Disposition |
|---|---|---|---|---|
| 1 | LOW | Codex (verified vs validator) | **Inverted engine-contract claim.** The code comment + spec §15.7 + summary + devlog said the live `validateNewMarker` *rejects past-tick markers* (`6.1.tick_future`). The validator does the OPPOSITE: `session-marker-validation.ts` rejects only `tick > world.tick` (future) and its docstring explicitly supports *retroactive* (past-tick) markers. The direct-injection choice is still correct, but the stated *reason* was backwards (the real reason is "no live recorder in the post-hoc pass," not a tick rejection). | FIXED — corrected all four surfaces to state the accurate reason (no live recorder; the validator permits retroactive markers, so the tick is not the blocker). Verified against `session-marker-validation.ts:23-44`. |
| 2 | LOW | Claude | **Non-atomic in-place bundle rewrite.** `writeFileSync(bundlePath, ...)` overwrites a non-regenerable artifact (re-running the LLM playtest is costly + nondeterministic); a crash/disk-full mid-write corrupts it. | FIXED — write to `<bundle>.tmp` then `renameSync` to close the corruption window (added `renameSync` import; `node --check` passes). |
| 3 | LOW | Claude | **`--no-llm` marker-strip undocumented user-side.** Correct/consistent behavior, but a user refreshing metrics via `--no-llm` silently loses prior AI markers. | FIXED — changelog now notes a `--no-llm` re-run clears prior agent markers (keeping the bundle consistent with the regenerated findings report). |
| 4 | LOW | Claude | **`deriveAnchorTick` clamps to `metadata.endTick`, which can exceed the replay-reachable bound for *incomplete/failed* bundles** (`replayableUpperBound` caps below endTick; TimelinePanel caps at `firstFailedTick-1`). | ACCEPTED / deferred. Doubly-rare (empty-trace fallback AND an incomplete bundle) and degrades gracefully (pin disabled, list-row click clamps via `clampTick`, no crash); the normal anchor is a real recorded decision tick ≤ last tick. Precise clamp-to-replayable-bound would add coupling for a gracefully-handled edge — deferred. |
| 5 | MEDIUM | Codex | Docs ahead of tree (changelog "green" + `done/` link; thread still `current/`; devlog `[pending]`). | FIXED in close-out — thread moved `current/`→`done/`, devlog reviewer section filled (this commit). |
| 6 | LOW (trivial) | Claude | `summary.md` is 63 lines (> the 50-line AGENTS.md compaction threshold); this change appended +1. `isAgentMarker` exported but only used internally. | `isAgentMarker`: left exported (a clean public predicate, harmless). summary.md compaction: deferred as a dedicated near-term cleanup (pre-existing multi-session debt; a history-rewrite doesn't belong in a feature commit) — tracked for the next loop iteration. |

## Notes
- The `--no-llm` consistency (#? in the prompt) was independently confirmed correct by all three reviewers — not a footgun.
- Gemini's 429 recovered; its review is complete and concurs with Claude/Gemini on all 8 points. It did not flag finding #1 (the inverted claim); Codex's verified LOW governs (convergence by substantive-finding count, not vote).
