# PLAN — M7 units-beyond-circles slice 1 (v0.1.41)

1. **TDD**: write `tests/phaser/unitRenderer.test.ts` (role map incl. skirmisher→archer + exhaustiveness; facing radians incl. idle→null; drawUnit primitive-set-per-role + bounding-radius containment via a Graphics spy). Red.
2. **Implement `src/phaser/scenes/gameScene/unitRenderer.ts`** — `unitRole`, `unitFacingRadians`, `createUnitRenderer({entityLayer, cellSize}).drawUnit(entity, px, py, facingRadians, fillAlpha)`. 7 role shapes, all within the unit bounding circle, body = `entity.tint`, darkened outline. Green.
3. **Wire GameScene** — construct the renderer in `create()` + `setBridge()` (alongside the other renderers); in `renderState`, replace the 6-line circle branch with: compute `facingRadians` from `previousUnitProjectedPositions.get(entity.id)` vs the unit's current PROJECTED position, then `unitRenderer.drawUnit(...)`. Confirm GameScene LOC ≤ 1018 (net-zero/down).
4. **Add `unit-showcase-fixture`** (test-only): new `src/game/simulation/fixtures/unitShowcase.ts` placing one P1 unit per role (villager, militia, archer, scout, cavalry-archer, mangonel, monk) in a tidy visible row near a P1 TC; export via `fixtures/index.ts`; register in `prototypeScenario/dispatch.ts`. (Fixtures are the sanctioned home for scenario data; this is NOT a sim/gameplay change.)
5. **Visual capture** — write `tmp/units/`-targeted before/after/diff via a small capture script (reuse the Playwright + pngjs pattern of `captureMapScreenshot.mjs`/`diffMapScreenshots.mjs`). Capture BEFORE (git stash or pre-wire build) on `?seed=unit-showcase-fixture`, apply, capture AFTER, diff. Save `tmp/units/{before,after,diff}.png`.
6. **Gates**: `npm test` (full), `npm run typecheck`, `npm run lint`, `npm run build`. All green; baseline 1340 passed / 2 skipped (renderer tests add to the count).
7. **Docs**: `package.json` 0.1.40→0.1.41; `docs/changelog.md` v0.1.41; devlog detailed `2026-06-12_2026-06-16.md` full entry (reviewer comments pending) + `summary.md` line; `design/spec-final.md` §14 unit-render visual; `design/roadmap.md` M7 "Units beyond colored circles" → slice done (silhouettes + facing), animation deferred.
8. Report back (facts) for the team lead's multi-CLI review + commit. Do NOT commit.
