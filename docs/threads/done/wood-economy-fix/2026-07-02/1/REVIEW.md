# REVIEW — v0.1.79 3-part economy fix (wood gather-locality + age-up queue gate + production-queue research-skip)

**Reviewer:** in-process adversarial Workflow (3 dimension finders — determinism/save, queue semantics/human impact, gather behavior/perf — each finding independently verified by a refuting agent against the live code; 9 agents, ~657k tokens). Determinism/save finder returned ZERO findings (sort is deterministic — id tiebreak; `dirty` flag correct in the pop-blocked branch; no save-format change).

**Findings (6 confirmed, 0 high / 2 medium / 4 low) and dispositions:**

1. **[medium] requireReachable probe budget spent drop-off-first** — the recovery probe inherited the new drop-off-locality sort, so a walled pocket of ≥16 unreachable candidates near the drop-off could exhaust `MAX_REACHABILITY_PROBES` and permanently starve a villager standing beside a reachable resource (verifier CONFIRMED with a runnable repro; strictly broader trigger than the pre-diff fully-boxed case). **FIXED:** comparator extracted with a distance-mode flag — the steady-state sort keeps drop-off locality; the `requireReachable` recovery probe re-sorts by the legacy villager-proximity order (cold path only). Regression test added (`requireReachable probes in VILLAGER order…` — the verifier's exact pocket repro).
2. **[medium] research-skip shipped with zero direct test coverage** — semantics verified CORRECT by the reviewer's own code reading (no double-tick — the skip block requires a pop-blocked UNIT head and `continue`s; cost spent once at enqueue; `applyTechnology` idempotency-guarded; `hydrateFromWorldState` rebuilds `inFlightTechByOwner` from all queue entries so save/load mid-skip is consistent) but nothing pinned them. **FIXED:** `populationCap.test.ts` "a pop-blocked unit at the queue head does NOT stall a research behind it" — Loom completes + splices behind a permanently blocked villager; re-queue rejected (applied); head stays blocked.
3. **[low] comment overclaimed "(where it will deposit)"** — the reference drop-off is a ranking anchor; the actual deposit is re-resolved from the villager's live position at `to-dropoff`. **FIXED:** comment reworded (accepted one-lookup approximation documented).
4. **[low] unconditional reference lookup on an empty candidate set** — a maintain-order villager with a fully-depleted kind paid 2 net-new building scans/tick forever. **FIXED:** early-return to idle before the lookup + a counting test.
5. **[low] HUD renders no `isBlocked` indicator** — with research now advancing past a frozen head, a "waiting for housing" badge is worth adding (flag already on the entry). **ACCEPTED as a follow-up** `[ui]` slice (roadmap).
6. **[low] spawn-blocked head still stalls research (pop-block-only fix)** — pre-existing asymmetry, transient in practice (spawn ring frees as units wander), spec §6.10 scopes the rule to pop-block so code matches spec. **ACCEPTED as a recorded decision** (roadmap note; extend to spawn-block only if it is ever observed persisting).

**Post-fix validation:** assignment suite 9/9 (incl. both new regression tests), research-skip test green, full four gates re-run green after fixes.
