# Full Codebase Review — 2026-07-10 (Iteration 2, re-review of the iter-1 fix diff)

**Date:** 2026-07-10. **Scope:** the diff `605979d..49df1b7` — the fixes applied in iteration 1 (H1, H2, H3, H4, H8, M7, M8, M15, L11).
**Reviewers:** Codex `gpt-5.6-sol` / ultra (diff review). Claude `opus[1m]` / max was launched on the same diff but ran anomalously long (>25 min of agentic exploration on a 32-file diff with no output); its findings will fold into iteration 3 if it lands. Codex's pass was substantive and is actioned in full below. Fable 5 remains quota-exhausted.
**Prior:** `docs/threads/current/full/2026-07-10/1/REVIEW.md`.

## Codex findings & disposition (all addressed this iteration)

**[HIGH] H3 requeue was not a single idempotent path — src/game/recording/IndexedDBMirror.ts.** CONFIRMED against live code: an unhandled IDB request error fires `tx.onerror` AND then, as the tx aborts, `tx.onabort` — so the iter-1 code requeued the batch TWICE (unbounded `_pending` growth under a persistent quota failure); and a synchronous throw from `db.transaction()` / `applyPendingWrites()` fired neither handler, silently dropping the already-detached batch (the iter-1 test used an explicit `tx.abort()`, which fires only `onabort`, so it missed both cases). **Fixed:** a `settled` guard + `try/catch` — requeue + reject exactly once, on the first failure of any kind. New `IndexedDBMirror.failurePaths.test.ts`: (a) fire both `onerror`+`onabort`, assert `_requeuePending` called once; (b) make `db.transaction` throw synchronously, assert the batch survives to a later flush.

**[MEDIUM] H4 was incomplete — the Replay button stayed disabled — src/game/recording/IndexedDBMirrorReads.ts:57 / MarkerListPanel.ts:207.** CONFIRMED: iter-1 repaired `reconstructBundle`'s metadata, but `listSessions` still published the FROZEN `metadata.endTick`, and the marker panel's gate `!closedNormally && endTick===startTick` disabled Replay off that value — so the user could never reach the repaired bundle. **Fixed:** `listSessions` now recomputes `endTick` from the persisted ticks for `!closed` sessions (rare crash path pays one extra read; closed sessions untouched). New test: a crash session's descriptor reports the true `endTick` (7), not the frozen 0, so the gate no longer blocks.

**[MEDIUM] H8 grew an over-cap file and had no test — scripts/playtest-llm.mjs.** Addressed by EXTRACTING the parsing + numeric validation into a typed, unit-tested module `src/game/playtest/playtestLlmArgs.ts` (throws `PlaytestLlmArgError`; the script catches → exit 2, preserving the CLI contract). The script shrank 587 → 531 LOC and the validation is now typecheck-covered + tested (`playtestLlmArgs.test.ts`: valid/defaults, non-finite/out-of-range/non-integer, missing value, unknown flag, bad provider). The broader "scripts/ excluded from the size+lint gate" (M14 / Codex iter-1) remains a separate deferred item — `content-lib.mjs` (633) still needs it.

**[MEDIUM] M15 doc still inconsistent — design/spec-final.md:1389, docs/devlog/summary.md.** CONFIRMED. Fixed: spec §1389's military-hall silhouette noted its roof became pitched in v0.1.132 (was "flat roof band", contradicting §1395 + `roleHasPitchedRoof`); the devlog summary's "`LEGACY_VIOLATIONS` now holds only oracles.test.ts" corrected to "is now empty" (the live map is empty).

**[LOW] H1 test didn't independently prove each markDirty + accepted a missing corpse — tests/simulation/wildlifePersistence.test.ts.** Partially addressed. The corpse-existence assertion was tightened (`expect(restoredBoar).toBeDefined()` + `currentHp === 0`, no longer `?? 0`). On independence: both mutation sites fire on the kill tick and mark the SAME slot, so the kill test cannot isolate either — and the damage site is masked in play (a live boar's retaliation re-dirties the slot every tick), so a nonlethal-wound round-trip passes pre-fix too (it can't be a red test). The kill test correctly guards the ORIGINAL bug (both sites missing); the site-1 markDirty is retained as the defensively-correct invariant even though it's masked. This limitation is documented rather than papered over with a test that doesn't actually fail pre-fix.

## Disposition

All five Codex findings are addressed with tests where a real red→green exists. No version bumps: the H3/H4 corrections fold into the original 0.1.136/0.1.137 features (per AGENTS.md "iter-2 fixes fold into the original version"); H8 is harness-only; H1/M15 are test/doc. The iteration-1 DEFERRED items (M3, M5, M9, M10, M11, M12, M13, F16, H5, H6, H7, H9, M6/F8/F9, LOWs L1–L10) are unchanged — none was made worse by this diff. Iteration 3 will pick up Claude's iter-2 findings (if delivered) plus a prioritized subset of the deferred items.
