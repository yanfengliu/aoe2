# Visual conviction plan

1. Capture fixed paused production baselines for the default map plus terrain, building, and unit showcase fixtures.
2. Ship deterministic broadleaf, conifer, and wind-swept trees with multiple canopy palettes as v0.3.3.
3. Add procedural terrain material detail and animated water wave/reflection cues with deterministic-time browser proof.
4. Add shared architectural relief and type-specific detail across all 18 buildings, expanding the showcase where required.
5. Introduce unit visual profiles and compatible weapon rigs so all 34 unit types have convincing equipment and attack motion.
6. Run independent adversarial review and the four repository gates before each code commit; run both audits, the full browser gate, exact-pin verification, and `npm run verify` on the completed objective.
