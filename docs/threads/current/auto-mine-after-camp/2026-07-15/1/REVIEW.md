# auto-mine-after-camp — review iteration 1 (2026-07-15)

In-process adversarial Workflow against the worktree: 3 dimension reviewers (sim-correctness, determinism-persistence, boundary-oracle) + verifiers. 3 verifier agents were killed by a session limit mid-run; their two open findings (duplicate descriptions of one defect) were adjudicated by the lead against live code per the verify-reviewer-claims rule, and one was empirically reproduced by a failing test before any fix.

## Confirmed / adjudicated findings → all fixed

- **HIGH (lead-adjudicated + reproduced): `building.placeConfirm` bypassed both never-preempt layers.** The engine validates at submit time and executes handlers later without re-validation, so the autoGather validator cannot see an explicit command already in the engine queue — and placement was the one human path that never evicted pending intentions (`removePendingUnitCommands` had exactly one call site: humanInputOps move/context). A routine chain-build click in the completion window (finish camp → place house with the same villagers) spent the resources, then all builders abandoned the paid foundation for the mine. Reproduced RED by `never preempts a same-window explicit chain-build placement` (builders' `targetResourceId` was the mine; house stranded). FIX: an accepted placement now evicts pending system intentions for the primary and every additional builder, exactly like the move/context paths; test green, house completes.
- **HIGH (verified): spec/docs absent from the worktree delivery unit.** The slice cited "spec §6.2" from six files while the worktree carried no spec rule, changelog entry, version bump, or devlog. Root cause: coordination — the §6.2 bullet was drafted in the MAIN checkout's uncommitted tree. FIX: the slice merges into main WITH the spec text as one delivery unit; the bullet was additionally extended with the semantics only code/tests stated (idle-from-build validator backstop, replay authority-discard of live-queued intentions, serialized pending-queue persistence for the completion→drain window, placement eviction, no oracle actor keys).
- **LOW (verified): four load-bearing comments falsified by the new command type.** The M3 eviction rationale ("a human unit can only ever have an auto-aggression intention pending … removes exactly the stale attack, nothing else") and three "15 command types" counts. FIX: rationale rewritten to name both evictee classes and both caller sites; counts bumped to 16.

## Post-fix state

Worktree gates: 2,032 unit tests passed / 2 skipped (10 auto-mine tests: 6 behavioral + 4 persistence/replay), tsc, eslint. The validator/module header comments now state the layered never-preempt mechanism precisely (eviction at issue, FIFO ordering for pushes, state backstop at submit).
