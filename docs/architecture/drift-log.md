# Architecture drift log

Append a row whenever `ARCHITECTURE.md` changes. Each entry is a short record of a
structural change, so future agents can trace how the shape of the repo evolved.

| Date | Change | Devlog entry |
| ---- | ------ | ------------ |
| 2026-04-17 | Established `docs/architecture/ARCHITECTURE.md` to document existing runtime layers and directory structure. | `docs/devlog/detailed/` |
| 2026-08-17 | Added `src/rendering/artStyles.ts` and `artStylePreference.ts`: a display-only art-style layer over the sibling `voxel` package's new `StylizedResolvePass`, threaded through `AoeVoxelWorldRenderer` and the in-game menu. | `docs/devlog/detailed/2026-08-17_2026-08-17.md` |
| 2026-04-23 | Added `src/game/simulation/mapGeneration/` for the spawn-list dedupe helper and the procedural default-map generator. | `docs/devlog/detailed/2026-04-23_2026-04-23.md` |
| 2026-04-23 | Split the 9,968-line `prototypeScenario.ts` god-file into `src/game/simulation/fixtures/` (13 category modules + shared common + barrel index) plus new `mapGeneration/` modules (`sharedTerrainHelpers`, `startingOffsets`, `applyStandardPlayerOpening`, `blackForestMap`, `arenaMap`, `constants`). `prototypeScenario.ts` retains public types + dispatcher + re-exports (~790 lines). | `docs/devlog/detailed/2026-04-23_2026-04-23.md` |
| 2026-04-23 | Introduced `src/game/simulation/bridge/` for helper modules factored out of `createSimulationBridge.ts` (pure helpers, render projector + sheep vision, Trebuchet pack/unpack ops factory, fog-memory ops factory). `createSimulationBridge.ts` shrinks from 9,519 → 8,792 lines across four move-only commits. | `docs/devlog/detailed/2026-04-23_2026-04-23.md` |
| 2026-04-23 | Extended `src/game/simulation/bridge/` with four more ops factories (monk task subsystem, technology/upgrade application, match-end + score, AI decision helpers). `createSimulationBridge.ts` shrinks from 8,792 → 7,705 lines across four move-only commits. | `docs/devlog/detailed/2026-04-23_2026-04-23.md` |
| 2026-04-23 | Extended `src/game/simulation/bridge/` with three more ops factories (placement preview + building placement, full-game save serializer, target-finding priority tables + visible-enemy / nearest-drop-off / nearest-wildlife helpers). `createSimulationBridge.ts` shrinks from 7,705 → 7,214 lines across three move-only commits; one planned selection / command-dispatch extraction was deliberately skipped because its dep-bag exceeded 15 fields (~25+), which the brief treats as the wrong boundary. | `docs/devlog/detailed/2026-04-23_2026-04-23.md` |
| 2026-04-23 | Introduced `src/phaser/scenes/gameScene/` for dep-bag renderer factories and a camera controller factored out of `GameScene.ts`: `debugOverlay.ts` (world-space debug overlays), `worldLayers.ts` (health-bar + fog paints), `selectionLayers.ts` (selection ring + placement preview + marquee paints), `cameraController.ts` (camera update / pan / zoom / clamp + HUD camera queries). `GameScene.ts` shrinks from 1,737 → 1,161 lines across four move-only commits. | `docs/devlog/detailed/2026-04-23_2026-04-23.md` |
| 2026-04-23 | Split the 1,868-line `fixtures/ageProgression.ts` into a `fixtures/ageProgression/` subdirectory (`feudal.ts`, `castle.ts`, `imperial.ts`, `blacksmith.ts`); the original file stays as a barrel re-exporting every factory. Split the 1,090-line `fixtures/siege.ts` into a `fixtures/siege/` subdirectory (`mangonel.ts`, `scorpion.ts`, `ram.ts`, `bombardCannon.ts`, `trebuchet.ts`, `workshop.ts`); the original file stays as a barrel. | `docs/devlog/detailed/2026-04-23_2026-04-23.md` |
| 2026-04-23 | Split the six remaining 800+-line files into themed sub-directories with barrel re-exports: `fixtures/castleDefense.ts` (984) → `fixtures/castleDefense/{fletching,fu3Walls,fu3Archers,towerAndCastle}.ts`; `fixtures/combatMatchups.ts` (962) → `fixtures/combatMatchups/{infantryAndCavalry,rangedSkirmish,generalCombat}.ts`; `fixtures/monastery.ts` (858) → `fixtures/monastery/{heal,convert,relic}.ts`; `fixtures/ageProgression/imperial.ts` (817) → `fixtures/ageProgression/imperial/{ageUp,upgrades,castle}.ts`; `tests/browser/game-progression-and-production.spec.ts` (847, 21 tests) → four themed specs (`game-progression-{ageup,upgrades,production}.spec.ts` + trimmed remainder file); `tests/browser/helpers/gameTestHelpers.ts` (820) → `tests/browser/helpers/gameTestHelpers/{types,snapshot,camera,command,selection,hud,placement}.ts`. All original file paths retained as barrel files re-exporting every previous export. | `docs/devlog/detailed/2026-04-23_2026-04-23.md` |
| 2026-04-25 | Doc-only refresh of `ARCHITECTURE.md` `bridge/` topology section (review M-16). Now enumerates every current ops module under `src/game/simulation/bridge/` (`pureHelpers`, `visibility`, `trebuchetState`, `fogMemoryOps`, `monkTaskOps`, `technologyOps`, `matchEndOps`, `aiDecisionOps`, `placementOps`, `saveGameOps`, `targetFindingOps`) and notes top-level `worldOccupancy.ts`, `selectionActivity.ts`, and `renderStore.ts` siblings of `createSimulationBridge.ts`. Catches the doc up to multiple already-shipped phase-1/2/3 extractions. | `docs/devlog/detailed/2026-04-24_2026-04-26.md` |
| 2026-04-26 | Phase 4 of `createSimulationBridge.ts` extraction. Added `src/game/simulation/bridge/systems/` (18 per-system factory files for every `world.registerSystem({...})` block — `prototypeAi`, `prototypePlayerCommands`, `prototypeAutoAggression`, `prototypeMonkBehavior`, `prototypeProductionQueues`, `prototypeScoutMovement`, `prototypeVillagerEconomy`, `prototypeWildlifeCombat`, `prototypeHerdableMovement`, `prototypeHerdableOwnership`, `prototypeFogMemory`, `prototypeRelicGold`, `prototypeVisibility`, `prototypeWonderCountdown`, `prototypeRelicCountdown`, `prototypeWinConditionResolver`, `prototypeConquestOutcome`, `prototypeTowerCombat`) plus 9 new ops modules under `src/game/simulation/bridge/` (`optionsRules`, `selectionStateOps`, `movementPlanOps`, `entityCreateOps`, `entityDestroyOps`, `playerQueries`, `trainingMarketOps`, `combatStateFactory`) and 4 shared-shape modules (`countdownTypes`, `memoryTypes`, `movementTypes`, `systems/systemTypes`). `createSimulationBridge.ts` shrinks 7606 → 4031 lines (-47%) across 9 commits on `agent/split-simulation-bridge`. Same flat dep-bag factory pattern as phase-1/2/3; side-map ownership stays in `createWorld`. | `docs/devlog/detailed/2026-04-24_2026-04-26.md` |
| 2026-04-26 | Phase 5 of `createSimulationBridge.ts` extraction. Adds 8 more ops modules to `src/game/simulation/bridge/`: `humanInputOps` (player command surface), `selectionInputOps` (selection mutation + spatial context queries; folds `selectedEntityRefs`/`selectionFocusCell` into a single shared `selection` holder), `unitCommandOps` (per-unit command issuance + `selectEntity{AtCell,ById}`/`clearSelection`), `cellPassability` (terrain/occupancy queries), `visibilityQueries` (footprint-aware `is*VisibleToHuman`), `transformOps` (unit-transform + occupancy bookkeeping), `debugSnapshotOps`, `economyStateOps`. `createSimulationBridge.ts` shrinks 4031 → 2722 lines (an additional -32%) across 9 commits on `agent/split-simulation-bridge`. Cumulative phase-1..5 shrink: 9519 → 2722 (-71%). | `docs/devlog/detailed/2026-04-24_2026-04-26.md` |
| 2026-04-26 | Phase-5 follow-up extractions. `createWorld` moved to `bridge/createWorld.ts`; side-map ownership hoisted from `createWorld` into a single `bridgeState.ts:createBridgeState()` factory; `wireBridgeOps` + `wirePostSeedOps` + `assembleBridgeApi` extracted; `registerAllSystems` + `registerBridgeSystems` extracted; `scenarioSeedOps` + `hydrateFromSavedGame` split; the AI side of `monkTaskOps` separated from the appliers (`monkAiSearchHelpers` + `monkTaskAppliers`); spatial finders extracted to `selectionFinders`; render-state assembly extracted to `renderStateOps`. `createSimulationBridge.ts` shrinks 2722 → 332 lines (an additional -88%; cumulative phase-1..5 + follow-ups: 9519 → 332, -96%). | `docs/devlog/detailed/2026-04-24_2026-04-26.md` |
| 2026-04-26 | Iter-4 review fixes: extracted `bridge/sharedTypes.ts` (the four bridge-internal types `UnitCommand` / `MonkTask` / `ConstructionState` / `TrebuchetPackState` previously declared in the `createSimulationBridge.ts` facade); `GameScene.ts` extracted `scenes/gameScene/buildingRenderer.ts` (~160 LOC, scene shrinks 1176 → 1034 lines). | `docs/devlog/detailed/2026-04-24_2026-04-26.md` |
| 2026-04-26 | Iter-2 review fixes: added `monksByOwner: Map<owner, Set<monkId>>` to `BridgeState` (V5-1 — replaces the V4-12 countOwnedUnits-based AI monk skip-guard with O(1) lookup; rebuilt on save-load); extracted `scenes/gameScene/unitTypeMap.ts` (V5-3 — moved `ALL_UNIT_TYPES` + `isUnitType` out of `GameScene.ts` to drop the scene under the 1000 LOC strict ceiling, 1034 → 994); dropped `findIdleProducer` from `aiDecisionOps` and `RegisterAllSystemsDeps` (V5-8 — `aiSystem` now precomputes `ownerBuildingsByType` once per decision tick and uses a local `findIdleProducerLocal` over the grouped data, replacing 10–14 full world.query('building') scans with one). | `docs/devlog/detailed/2026-04-24_2026-04-26.md` |
| 2026-05-01 | Phase 1C of commandify rewrite: aiSystem stops calling `enqueueTraining` / `enqueueResearch` / `startConstruction` directly during its `update` phase. Instead it pushes intentions to `state.pendingCommands`; the dispatcher submits each via `world.submitWithResult` between ticks, and the corresponding handlers run at the start of the next tick's `processCommands`. Reorders `pickUnitMix` BEFORE villager training so the FIFO-ordered handler picks military first when food is tight (replaces the pre-1B accidental corner-case timing collision a barracks-rush AI relied on). aiSystem reads `pendingCommands` to fold intentions still in flight into its queue-length / inFlightTechByOwner / ongoingBuilds gates. `RegisterAllSystemsDeps` drops `startConstruction` / `enqueueResearch` / `enqueueTraining` (no system inside `registerAllSystems` calls them anymore — the `*Direct` aliases reach handlers via `wireBridgeOps` → `registerCommandHandlers` instead). | `docs/devlog/detailed/2026-04-26_2026-05-01.md` |
| 2026-05-01 | Phase 2A bridge-state migration scaffolding: `bridgeStateSerialize.ts` (35 Tier-1 SlotCodec pairs + helpers + registry); `bridgeStateAccessor.ts` (per-tick cache layer with lazy world ref + `get`/`mutate`/`markDirty`/`flush`/`reset`); `visibilityCell.ts` (visibility map + dirty-bit wrapper). No existing code migrated yet (Phase 2D's per-slot work follows). | `docs/devlog/detailed/2026-04-26_2026-05-01.md` |
| 2026-05-01 | Phase 2B output-phase tail: new `tier3SyncSystem.ts` (writes `aoe2.visibility` + `aoe2.matchState`), `bridgeSnapshotSystem.ts` (calls `accessor.flush()`), `registerOutputTail.ts` (registration site). `tier3SyncSystem` runs SECOND-TO-LAST in output phase; `bridgeSnapshotSystem` LAST. | `docs/devlog/detailed/2026-04-26_2026-05-01.md` |
| 2026-05-01 | Phase 2C bootstrapFlush + registerOutputTail integration: new `bootstrapFlush.ts` populates `aoe2.bridgeMeta` / `aoe2.matchState` / `aoe2.visibility` once at construction; `wireBridgeOps` constructs accessor + visibility cell after `registerCommandHandlers` and calls `registerOutputTail` then `bootstrapFlush`. | `docs/devlog/detailed/2026-04-26_2026-05-01.md` |
| 2026-05-01 | Phase 2D incremental slot migrations (7/35 Tier-1 slots): `villagerOrdinals`, `gathererDropOffStuckSinceTick`, `monkHealCounters`, `playerAges`, `playerCivilizations`, `wonderCountdownOverrides`, `relicCountdownOverrides`. Each follows the same pattern: drop the field from `BridgeState`, threading `accessor.mutate(codec, ...)` to all readers/writers. `BridgeStateAccessor` construction relocated from `wireBridgeOps` to `createWorld` for early dep availability. | `docs/devlog/detailed/2026-04-26_2026-05-01.md` |
| 2026-05-01 | Phase 2E visibility staleness fingerprint cache: `syncVisibilitySources` gains a closure-scoped `(playerId, x, y, radius)` fingerprint cache shared between bootstrap + per-tick visibilitySystem. Owner-flip branch (monk conversion + sheep-claim transfer mutate `playerId` in place) calls `removeSource(prev.playerId, id)` before re-inserting under the new owner. `aoe2.visibility` is no longer permanently frozen at tick-0 state. | `docs/devlog/detailed/2026-04-26_2026-05-01.md` |
| 2026-05-01 | Full-review iter-1+2 fixes: `pendingCommandQuery.ts` (NEW — pure exhaustive `hasPendingUnitCommand`); `tier3SyncSystem.ts` exports `flushTier3State` for save-time use; `saveGameOps.flushBeforeSerialize` now flushes Tier-3 alongside Tier-1; `bridgeStateAccessor.flush` pre-pass throws on uncached dirty marks (atomic). `hydrateFromSavedGame` adds value-side prune AFTER cross-ref invariant for `garrisonedByBuilding` / `garrisonedUnitToBuilding` / `monkCarriedRelic`. | `docs/devlog/detailed/2026-04-26_2026-05-01.md` |
| 2026-05-04 | Split `unitCommandOps.ts` file-size backlog into `unitSelectionOps.ts` (selection queries/mutators) and `sheepCommandOps.ts` (sheep.move facade/direct helper). `unitCommandOps.ts` keeps the same public factory surface by composing both helpers, drops under 500 LOC, and leaves the architecture budget exemption list. | `docs/devlog/detailed/2026-05-04_2026-05-04.md` |
| 2026-05-04 | AI monk task assignment moved from direct `aiSystem -> assignAiMonkTasks -> setMonkTask` mutation to queued `monk.contextAtEntity` intentions with expected-owner/task-kind guards. The bridge now persists `pendingCommands` across save/load and drains them before the next tick, clearing the AI-side blocker for `monkTasks` migration. | `docs/devlog/detailed/2026-05-04_2026-05-04.md` |
| 2026-05-04 | Migrated `monkTasks` from `BridgeState` to `world.state.aoe2.monkTasks` via `monkTasksCodec` + `BridgeStateAccessor`. Command handlers, deterministic Monk behavior, save/load hydration, entity cleanup, and selection/activity projection now use the accessor-backed task map. | `docs/devlog/detailed/2026-05-04_2026-05-04.md` |
| 2026-05-04 | Migrated `unitCommands` from `BridgeState` to `world.state.aoe2.unitCommands` via `unitCommandsCodec` + `BridgeStateAccessor`. Command handlers, deterministic systems, debug/selection projections, save/load hydration, and save serialization now use the accessor-backed command map, closing the final Tier-1 bridge-owned codec. | `docs/devlog/detailed/2026-05-04_2026-05-04.md` |
| 2026-05-05 | Bumped save format to schema 2 (`seed + worldSnapshot`) with schema-1 compatibility, `hydrateFromWorldState.ts` for world-state runtime hydration, and `aoe2.pendingCommands` for save-critical AI intentions. | `docs/devlog/detailed/2026-05-05_2026-05-05.md` |
| 2026-05-05 | Added the Phase 3A replay-world construction seam: `src/game/simulation/replay/createReplayWorldOnly.ts`, replay context WeakMap storage, replay-mode `registerAllSystems` behavior, replay pending-command boundary preservation/drain, and schema-2 hydration pruning that avoids adding absent empty state slots during replay construction. | `docs/devlog/detailed/2026-05-05_2026-05-05.md` |
| 2026-05-05 | Added the Phase 3B replay controller layer: `src/game/replay/ReplayController.ts` owns live/replay bridge swaps, coalesced scrubbing, and command-driven replay playback; `src/game/simulation/replay/makeReplayBridge.ts` exposes replay worlds through the full `SimulationBridge` read surface without scene-frame advancement. | `docs/devlog/detailed/2026-05-05_2026-05-05.md` |
| 2026-05-05 | Added the Phase 3C replay timeline UI layer: `TimelinePanel.ts` renders replay-only scrub controls and marker/hotspot pins, `ReplayHotkeys.ts` binds replay navigation keys only during replay mode, and `createApp.ts` mounts the panel plus exits replay before host bridge replacement on save-load. | `docs/devlog/detailed/2026-05-05_2026-05-05.md` |
| 2026-05-07 | Phase 3D + 3E (replay-load-and-e2e thread): added `src/ui/replay/replayLoadDialog.ts` (unified ReplayLoadDialog modal under one HUD entry consolidating live-session / prior-session / file-import sources), `src/game/replay/loadCurrentSession.ts` + `loadPriorSession.ts` (status-returning helpers mirroring civ-engine `SessionReplayer.openAt` rejection contract), `src/game/replay/parseSessionBundleFile.ts` (structural validator for imported JSON). `RecordingService.loadPriorSessionBundle(sessionId)` exposes IDB→bundle reconstruction. `ReplayController.enterReplay` restructured: bundle validation/construction happens BEFORE state mutation so any engine-level rejection leaves the controller in its pre-call state (transactional guarantee across all three load sources). `__AOE2_TEST__.replay.{getReplayMode,getReplayCurrentTick,openReplayLoadDialog}` is the new browser-test surface. | `docs/devlog/detailed/2026-05-06_2026-05-06.md` |
| 2026-05-08 | Added playtest runner (Phase 1 of the playtest-loop thread): `src/game/playtest/{runPlaytest.ts,types.ts}` and `scripts/playtest.mjs`. Bridge-driven recording loop, distinct from live-game `RecordingService`. Subsequent phases (oracles, fix-bot, corpus runner, CI workflow) are tracked in the thread. See `docs/threads/current/playtest-loop/`. | `docs/devlog/detailed/2026-05-08_2026-05-08.md` |
| 2026-05-08 | LLM-agent playtest harness (all 5 phases of the llm-agent-playtest thread). Adds `src/game/playtest/{agentSnapshot,llmAgent,llmPromptBuilder,llmProviders,llmRunner,visualOracle,corpusLlmSchema}.ts` + `src/app/bootstrap/{disableAiParam,agentCommandValidator}.ts` + the `__AOE2_TEST__.agent` browser-test sub-API (5 methods). New scripts `scripts/playtest-llm.mjs` (Playwright orchestrator over `vite preview`) and `scripts/playtest-corpus-llm.mjs` (corpus runner with retention pruning + clean ANTHROPIC_API_KEY-absent skip). Separate CI workflow `.github/workflows/playtest-llm.yml` so the deterministic gating loop stays independent of Anthropic availability. `?disableAi=` URL param plumbs through `CreateSimulationBridgeOptions.disableAiForOwners` → `createWorld` → `PlayerStartSpec.disableAi`; never enters world.state. `SimulationBridge.pendingCommands` now publicly exposed (was internal); `setAgentDispatchObserver` lets the agent harness observe per-command dispatch results without competing with the HUD's `consumeCommandRejection` FIFO. `@anthropic-ai/sdk` + `pixelmatch` added as devDeps; production bundle excludes both (verified via `vite build` size + `dist/assets/*.js` grep). See `docs/threads/current/llm-agent-playtest/`. | `docs/devlog/detailed/2026-05-08_2026-05-08.md` |
| 2026-06-15 | AI findings → annotation bridge (thread `ai-findings-annotation-bridge`, v0.1.36): new data flow connecting the playtest conformance-findings pass to the annotation/marker system. Adds the pure module `src/game/playtest/findingsToMarkers.ts` (`findingsToMarkers` maps `ConformanceFinding[]` → `author:'agent'` `Marker[]`; `injectAgentMarkers` overlays them idempotently onto a `SessionBundle.markers[]`; `deriveAnchorTick` picks the coarse anchor tick). `scripts/playtest-findings.mjs` now also rewrites `<prefix>.json` in place to overlay the agent markers, so replaying a playtested bundle surfaces the findings in the existing `MarkerListPanel` list + `TimelinePanel` pins for free (no new render code). First consumer of the `author:'agent'` marker path (annotation-ui thread, 2026-04-29). Not a new subsystem (one pure module + a script edit), so no ARCHITECTURE.md Component Map row. | `docs/devlog/detailed/2026-06-12_2026-06-15.md` |
| 2026-06-11 | Agent affordances (campaign-1 backlog #1-#3, thread `agent-affordances`): three new bridge helper-ops modules — `researchAvailability.ts` (shared "why is this research unavailable" reason engine consumed by both the queue.research validator message and the agent snapshot), `buildingOptionsOps.ts` (`SimulationBridge.getAgentBuildingOptions(ownerId)`: per-building-type research/train options with locked reasons + villager build menu), `commandValidatorDeps.ts` (per-command validator dep-bag construction extracted from `wireBridgeOps` for its 605-line budget). `cellPassability.ts` gains `describePlacementBlockers` (claim-kind cause naming over `worldOccupancy.getCellStatus`) + `findOpenPlacementAnchors` (deterministic fog-gated ring scan). New public `SimulationBridge` methods `getAgentBuildingOptions` + `findOpenPlacementAnchorsNear` thread wirePostSeedOps → createWorldResult → facade → BrowserTestBridge → agent snapshot (`buildingOptions` + `placementHints` fields) → tactical prompt. Reason: campaign-1 evidence — agent burned ~7 decisions reverse-engineering the feudal prerequisite rule and 19/27 placements failed blind. | `docs/devlog/detailed/2026-06-09_2026-06-11.md` |
| 2026-07-01 | Added the passive garrison-heal system (v0.1.63): new update-phase ECS factory `src/game/simulation/bridge/systems/garrisonHealSystem.ts` (19th systems-tier factory), registered in `registerAllSystems.ts` after `relicGoldSystem`. Accessor-only (no world query): it enumerates the `garrisonedByBuilding` side-map — the only listing of position-less garrisoned unit ids — and heals each unit's `combatStates` slot toward maxHp at a fixed deterministic 0.4 HP/tick, dirty-marking the codec only when a heal occurred. Reason: garrisoned units keep combat state but lose `position`, so passive regen needs a side-map-driven system rather than a spatial query. Systems-tier count 18→19. | `docs/devlog/detailed/2026-06-30_2026-07-01.md` |
| 2026-07-05 | Isometric render/camera coordinate switch (v0.1.103, thread `isometric-overhaul` increment 2): the Phaser world-space changed from top-down `cell*CELL_SIZE` pixels to ISO-PIXEL space via the pure `src/phaser/scenes/gameScene/isoProjection.ts` (`worldToIso`/`isoToWorld`, ISO_TILE_WIDTH=64/HEIGHT=32). `terrainRenderer.drawTerrainCell` now fills a 4-corner diamond (`fillPoints`) instead of a square; the v0.1.43 edge-feather was dropped (returns as a diamond feather later). GameScene places every entity at `worldToIso(x+0.5,y+0.5)`, depth-sorts a draw-order copy back-to-front, and converts all pointer hit-testing (`isoToWorld`), placement preview, and the marquee (new `entityHitTest.doesIsoWorldRectIntersectEntity`) to iso. New pure module `gameScene/isoViewHelpers.ts` (computeBaseFocusCell / isoWorldPixelBounds / isoViewportCellBounds / isoDragPixelBounds / isDragSelectableEntity / marqueePreviewEntities) keeps GameScene under its 1018-line cap. `cameraController` lives in iso-pixel space: `getScreenPointForCell` + `centerOnWorldPosition` project through `worldToIso`; world bounds/clamp/min-zoom derive from the iso diamond extent; `CameraState`/`CameraStateSnapshot` gain `viewCell{Min,Max}{X,Y}` (visible cell AABB) consumed by the minimap. `selectionLayers` draws iso diamond footprints + iso-centred rings/marquee/placement. `minimap.ts` maps clicks + the viewport rect through cell space (dropped `MINIMAP_CELL_SIZE`). Reason: user direction — the flat top-down grid looked nothing like AoE2; first structural step of the isometric overhaul. No sim/economy/combat change. | `docs/devlog/detailed/2026-07-03_2026-07-04.md` |
| 2026-07-05 | Isometric building volumes (v0.1.104, thread `isometric-overhaul` increment 6a): new pure `src/phaser/scenes/gameScene/isoBuilding.ts` (`isoBuildingPolys` extrudes a footprint diamond into ground/roof/left-face/right-face polygons; `drawIsoBuilding` paints the lit/shadowed 3/4-view box with an owner-tint roof; also now hosts the shared `darken` colour util) + `buildingRoofAccents.ts` (per-role roof feature dispatch). `buildingRenderer` rewritten: computes the footprint diamond from the entity's cell coords via `worldToIso` and extrudes by a per-role height (`ISO_HEIGHT_CELLS_BY_ROLE`) instead of drawing a flat footprint rect; the render loop's px/py is now unused for buildings. DELETED `buildingSilhouettes.ts` (the flat per-role silhouettes had no remaining caller). No new subsystem/boundary — a renderer-internal reshape, so no Component Map change. Reason: the v0.1.103 coordinate switch left buildings as flat rects floating on the diamond terrain; increment 6a gives them iso depth + grounding. | `docs/devlog/detailed/2026-07-03_2026-07-04.md` |
| 2026-07-06 | Added `src/game/playtest/visualPlaytestAdapter.ts` as the aoe2-side adapter for civ-engine v1.3 visual-playtest contracts. Tactical LLM prompts now prepend engine `buildVisualPlaytestPrompt` context (screenshot metadata, visible-text summary, command-tool controls) while preserving aoe2's JSON snapshot and command tool schemas; conformance finding markers keep the existing `author:'agent'` / `category:'ai'` marker data and embed the engine `visualPlaytestFindingToMarker` payload under `data.visualPlaytest`. Reason: adopt shared engine vocabulary without replacing aoe2's richer runner/provider/cost/dispatch stack. | `docs/devlog/detailed/2026-07-06_2026-07-06.md` |
| 2026-07-08 | Added `src/game/playtest/selfImprovementLoop.ts` and `scripts/playtest-self-improve.mjs` as the recursive self-improvement ledger boundary. The new command reads saved playtest bundles/envelopes/traces, recovers shared `civ-engine` `ImprovementFinding` payloads, records replay self-check evidence, classifies proposal/fix/observe routing, and compares baseline/current metrics through `compareMetricsResults`. Reason: make the existing run -> findings -> verify -> rerun loop inspectable without replacing the LLM runner, findings pass, replay tools, or auto-fix gate. | `docs/devlog/detailed/2026-07-08_2026-07-08.md` |
| 2026-07-08 | Extended the recursive self-improvement ledger to deterministic playtest artifacts via `playtest:self-improve --oracles`: the command now accepts runs without `.llm-trace.jsonl`, runs the existing oracle suite, converts `OracleViolation`s into shared `civ-engine` `ImprovementFinding`s, and compares deterministic baseline/current metrics without NaN placeholders. Reason: give the loop a current-code, strong replay-self-check bootstrap path that does not depend on fresh LLM provider calls. | `docs/devlog/detailed/2026-07-08_2026-07-08.md` |
| 2026-07-08 | Added ledger-driven fix proposal intake: `fixProposalInput.ts` selects classified fix findings from self-improvement ledgers, and `scripts/propose-fix.mjs --ledger ... --dry-run` writes target/prompt artifacts without reparsing the legacy oracle report. Reason: keep recursive-loop proposal routing on the shared `civ-engine` `ImprovementFinding` contract. | `docs/devlog/detailed/2026-07-08_2026-07-08.md` |
| 2026-07-08 | Added standardized finding-delta evidence to self-improvement rerun comparisons: `selfImprovementFindingComparison.ts` reports resolved, persisted, and introduced findings beside the existing metric deltas, using stable oracle/tick/message identity for deterministic oracle findings. Reason: make before/after rerun evidence strong enough to tell whether a proposed fix closed its target or merely changed aggregate finding counts. | `docs/devlog/detailed/2026-07-08_2026-07-08.md` |
| 2026-07-10 | Split the pinned-units oracle out of `oracles.ts` into `pinnedUnitsOracle.ts`: unit-lifetime intervals (id reuse + same-tick kill/reuse boundaries on owner/unitType change), a driven-owner gate with an explicit per-command actor-key whitelist, position-gap segmentation (garrison round-trips), and two confinement boxes — tight (pinned) plus wide (oscillating) — sharing a gathering-suffix exemption; `positionReplay.ts` now also reports garrison gaps and dropped the dead `netManhattanProgress`. Reason: position-only pinning misfired on 14 of 16 canary-baseline findings, and the adversarial review proved the round-1 exemption/oscillation semantics could false-prove real freeze classes (stale gathering sample, amplitude-3+ shuttles). | `docs/devlog/detailed/2026-07-08_2026-07-10.md` |
| 2026-07-10 | Added a render-only unit DEATH FEED data flow: `entityDestroyOps.destroyUnitEntity` records deaths (with an AT-DEATH `witnessedBy` player set, queried from a read-only `visibility` probe before destroy/recompute) into transient `BridgeState.recentUnitDeaths` (never persisted, no codec); `visibility.ts createProjector` surfaces `visibleUnitDeaths()` gated on witness membership on `ProjectedFrameView.recentUnitDeaths`; new render module `src/phaser/scenes/gameScene/deathEffects.ts` animates the collapse/dust. Reason: a unit vanishing from the render projection is ambiguous (death vs fog-exit vs garrison), so the death cue needs an explicit feed rather than present→absent diffing; gating on at-death (not current) visibility is fog-correct both ways (no off-screen-kill leak; own lone-unit deaths still show) per the adversarial review; the feed carries no sim authority so saves/replay determinism are untouched. | `docs/devlog/detailed/2026-07-08_2026-07-10.md` |
| 2026-07-10 | Added `src/phaser/scenes/gameScene/terrainCache.ts` (pure `computeTerrainSignature`) and made `sceneRenderer.ts` cache the terrain layer — it is (re)drawn only when the signature changes (initial, or `resetForBridgeSwap`), not every frame. Reason: the ~2160 iso-diamond terrain fills were the dominant per-frame render cost though terrain is immutable after load; caching cut a forced re-render ~6× (3.64ms→0.60ms). Render-internal; no boundary change to the sim/projector contract. | `docs/devlog/detailed/2026-07-08_2026-07-10.md` |
| 2026-07-10 | Added `src/phaser/scenes/gameScene/occlusionSilhouettes.ts` (pure occlusion detection: shared `depthKey`, `buildingSilhouettePolygon`, `pointInPolygon`, `computeOccludedUnits`) and a new `occlusionOutlineLayer` in `sceneRenderer.ts` (between fog and health bars) that stamps white silhouettes of units hidden behind buildings via a new `silhouette` mode on `unitRenderer.drawUnit`; `getOccludedUnitStates()` surfaced through GameScene + browserTestApi. Reason: a unit painted over by a building in the depth-sorted entity pass vanished; the outline shows where it is. Render-only; the sim/projector contract is unchanged, and the render sort now shares `depthKey` with the predicate so the cue cannot desync from what paints over. | `docs/devlog/detailed/2026-07-08_2026-07-10.md` |
| 2026-07-10 | Full-review iter-1 doc reconciliation (M15) + the previously-unlogged 2026-07-09 file-size splits: `ARCHITECTURE.md` corrected to the live bridge facade (~500 LOC, was 332), four bootstrap Tier-3 slots (was "three"), and 20 ECS system factories incl. `scoreTimerSystem.ts` (was 19); those files (GameScene/aiSystem/IndexedDBMirror/wireBridgeOps → 13 role modules, and the `bridge/systems/*` phase-module split of aiSystem) are refactor-driven Component-Map refreshes with no boundary change. `README.md` LLM-agent player corrected to player 1 (was 2), `spec-final.md` minimap rule reconciled to the iso diamond (§14.3, superseding the §14.5 top-down text) and the shipped v0.1.129 death effect removed from the deferred list. Reason: canonical docs had drifted from the code (Codex-2 + Claude-C findings). | `docs/devlog/detailed/2026-07-08_2026-07-10.md` |
| 2026-07-12 | Added the opt-in composed voxel renderer boundary: `src/rendering/voxel/` owns AoE projection recipes, camera synchronization, Three world canvas, explicit composite capture, bridge epochs, metrics, and teardown while the sibling `voxel` package owns reusable render contracts/meshing/presentation. `GameScene` remains the host; Phaser is the default and retains transparent input/overlay duties in voxel mode. CI pins and builds the sibling source before AoE install. | `docs/devlog/detailed/2026-07-11_2026-07-12.md` |
| 2026-07-12 | Split the 489-line voxel adapter into an orchestration facade plus pure building/unit/resource/terrain recipe modules, deterministic recipe helpers, and a material/resource registry; extracted the exhaustive unit-role table so Phaser and voxel presenters share one AoE-owned classification. The shared package gained only a configurable target-tracked daylight rig. Reason: richer art needed many stable AoE semantics, while keeping those recipes out of the cross-game renderer and every file below the 500-line ceiling. | `docs/devlog/detailed/2026-07-11_2026-07-12.md` |
| 2026-07-12 | Added `aoeVoxelUnitAnimation.ts` as the AoE-owned identity-phased idle/locomotion profile layer; the adapter now owns movement-history reset and resource packing carries optional game-neutral transform arrays. The sibling engine owns only bounded injected-time sampling, conservative motion bounds, uploads, and metrics. Reason: reuse rigid playback across games without sharing AoE roles or gameplay animation semantics. | `docs/devlog/detailed/2026-07-11_2026-07-12.md` |
| 2026-07-12 | Reworked AoE voxel locomotion into a displayed-distance sampler driven by simulation tick plus interpolation time, added eased travel headings and direction-aligned grounded foot transforms, exposed a bounded browser diagnostic seam, and split static from animated surface batches. Reason: make cadence match visible speed, keep pause/replay behavior selection-independent, eliminate corner/axis pops, and prevent large static scenery from inheriting the 16,384-slot active-batch ceiling without adding gait state to the shared engine. | `docs/devlog/detailed/2026-07-11_2026-07-12.md` |
| 2026-07-13 | Replaced the opt-in composed renderer with a standalone voxel-only host. Deleted `src/phaser/`, its painter tests, renderer selection/fallback, composite capture, second canvas, Phaser dependency, and manual build chunk. Added `AoeVoxelGameView`, renderer-neutral camera/pointer/selection controllers, a presentation coordinator, recipe-derived raised-entity hit regions, and voxel terrain/instance feedback for fog, selection, drag marquee, placement, authored-height health, hits, and deaths. Reason: voxel graphics are the sole AoE2 direction; keeping two graphics authorities added hidden work, bundle cost, and false parity signals. The sibling package contract remains game-neutral for City and Townscaper. | `docs/devlog/detailed/2026-07-13_2026-07-14.md` |
| 2026-07-13 | Hardened the standalone host around presented-state parity: prepared recipe-hit data is promoted only at matching epoch/revision, input is fenced during context loss or stale presentation, exact-click history can reorder only current semantic targets at the same projected point, the marquee is screen-locked above geometry, transient sparks clear authored visual tops, and browser API/listener/resize ownership is disposable. Reason: one graphics authority requires input and diagnostics to describe the same pixels the sole canvas actually presents. | `docs/threads/done/voxel-renderer-migration/2026-07-13/7/REVIEW.md` |
| 2026-07-13 | Extended `renderStore.ts` with exact immediately prior forward-tick unit/resource positions, added `renderMetricsCapture.ts` for lightweight always-on HUD metrics, and made `AoeVoxelPresentationCoordinator` accept interpolation history only for adjacent ticks. Full actor rigs now rotate through AoE-owned eased travel headings while moving resources share root interpolation. Reason: multi-tick browser callbacks need simulation-tick history rather than arbitrary rendered-frame history, and full-world debug serialization consumed the live 60 Hz budget despite the overlay being closed. | `docs/devlog/detailed/2026-07-13_2026-07-14.md` |
| 2026-07-13 | Made commanded and autonomous fine-grid movement publish replacement `unitTransform` components through civ-engine dirty tracking, aligned stationary spawn/command/arrival with the occupancy grid's allocated slot, persisted assigned-slot or explicit-overflow authority for slot-first live-load/replay reconstruction, and moved the 250 ms visible-frame bound into `src/game/visibleSimulationTiming.ts` for both hosts. Reason: in-place mutations withheld render projection until a coarse-cell change, first/last movement could recenter, entity-id reconstruction could change a saved crowded destination, and replay could consume an unbounded hidden interval in one callback. | `docs/debugging/2026-07-13-motion-continuity.md` |
| 2026-07-14 | Added the bounded successful-unit-hit presentation feed (`attackAnimationTypes.ts`, `bridge/unitAttackAnimationFeed.ts`, `bridge/unitAttackVisibilitySuppression.ts`, indexed `RenderStore` reconciliation, and recorder-only `aoe2.replayUnitAttacks`) plus AoE-owned voxel impact sampling (including deterministic prior/authored facing for coincident roots), connected rigid-part attack poses, movement-cancellation handoffs, and one monotonic pause-safe Voxel animation clock. Impact witnesses use visibility synchronized on the first hit each tick and after intervening player-command LOS mutations, while unchanged bursts reuse the current snapshot; player-command entity mutations compare actual coarse source fingerprints, building destruction bounds the building plus garrison IDs, and construction marks only when it adds a source; a perspective qualifies only when at least one cell of both actor footprints is visible, and tower targeting holds the pass-start visibility snapshot for the whole pass and refreshes final LOS once after any kills before suppression/output. Render projection retains raw live entities while `renderStateOps` owns the final fog perspective, prior-position confidentiality, and filtered `visibleEntities` surfaces; all-world alive `entityCount` stays diagnostic-only. New recorder checkpoints persist per-perspective cue suppression so fresh replay bridges cannot resurrect a hide-once event, while `RenderStore` keeps a local tombstone as defense in depth; ordinary saves strip the slot. Reason: damage resolves before presentation and stationary or lethal attackers cannot be inferred reliably from ordinary render diffs, fog reveals need retained raw identity without leaking prior positions, and combat roles, fog policy, and clock policy do not belong in reusable Voxel. | `docs/threads/done/unit-attack-animation/DESIGN.md` |
| 2026-07-19 | Added `bridge/movementTrafficOps.ts` as a local commanded/task traffic layer between permanent-only route planning and `transformOps` fine stepping, with its additive authority fields isolated in `movementPersistenceTypes.ts`. Straight and turning one-cell passages use centered movement, owner-scoped same-tick origin reservations, and all-branch directed dependency closure; `UnitTransformComponent` persists direction X/Y, intent key, and attempt tick so current/previous callers can break head-on or longer occupied loops without making transient `movePathCache` save authority. | `docs/devlog/detailed/2026-07-19_2026-07-20.md` |
| 2026-07-19 | Added an AoE-owned `water` voxel surface and its static/animated instance batches while retaining opaque mixed terrain chunks. Deterministic terrain recipes now author grass, forest, hill, shoreline, reflection-cue, and bounded wave-crest detail; fog filters detail before recipe creation, animation overflow degrades to the static water lane, and terrain detail remains outside entity hit state. | `docs/threads/done/visual-conviction/2026-07-19/2/REVIEW.md` |
| 2026-07-19 | Split exhaustive concrete-building facade and working props into `aoeVoxelBuildingDetails.ts`, appended only to completed role recipes. Every transformed detail stays inside the authoritative footprint and enters the same prepared recipe used for rendered instance matrices and presented silhouette picking; construction scaffolds and material/batch topology remain unchanged. | `docs/threads/done/visual-conviction/2026-07-19/3/REVIEW.md` |
| 2026-07-19 | Extracted scaled unit-part authorship into `aoeVoxelUnitRecipeContext.ts`, rigid attack transforms into `aoeVoxelUnitAttackGeometry.ts`, and an exhaustive per-`UnitType` controlled-part/pivot/reach descriptor table into `aoeVoxelUnitAttackRigs.ts`. Eight all-34-type state digests prove the split leaves idle, locomotion, work, attack, and memory recipes byte-identical while opening a data seam for concrete weapons. | `docs/threads/done/visual-conviction/2026-07-19/4/REVIEW.md` |
| 2026-07-19 | Added exhaustive concrete-unit visual profiles plus bounded humanoid, mounted, siege, and monk family builders. Attack descriptors now select one of 17 concrete pose styles so the visible weapon compound and its controlled animation agree, while prepared recipes remain shared by drawing and silhouette picking and no simulation or reusable-Voxel schema changes. | `docs/threads/done/visual-conviction/2026-07-19/5/REVIEW.md` |
| 2026-08-01 | Removed the automated fix arm: `scripts/{propose-fix,playtest-llm-auto-fix,playtest-recursive}.mjs` and `src/game/playtest/{applyAndGate,recursivePass,fixBotPrompt}.ts`, plus their tests and the `propose-fix` / `playtest:llm-auto-fix` / `playtest:recursive` npm scripts. `fixProposalInput.ts` split: its fix-candidate selection went with the arm, and the surviving ledger→`OracleViolation` adapter the canary drill needs is now `ledgerOracleViolation.ts`. Reason: the harness discovers and reports; nothing in this repo writes code or touches git on its behalf. Across the loop's operating life the automated arm proved one fix, while the discovery tooling it wrapped is what earned its keep. | `../fleet/FLEET.md` (canon), owner directive 2026-08-01 |
| 2026-08-18 | Ranged combat gained a projectile layer: `projectileRules` (pure), `projectileTypes`/`projectileOps`/`projectileSystem` (in-flight state + resolution, Tier-1 slot `aoe2.projectiles` #36), `attackDelivery` (the one fly-vs-instant decision, extracted from `playerCommandsSystem`), `projectileProjection` (fog-gated frame views) and `aoeVoxelProjectileParts` (render). Introduced the simulation's first randomness as a counter-based hash rather than a seeded stream (see decisions.md). Split for the 500-LOC budget: `saveBlobReaders` out of `createSimulationBridge`, `renderViewTypes` out of `types`. |
| 2026-08-18 | M5 naval foundation: `unitDomain.ts` (land/water domains + the gatherer predicate) and `shorePlacement.ts` (the Dock's land-and-touching-water rule, applied inside the shared `isPlacementBlocked` so preview and confirm agree). Passability, pathing, trained-unit spawn placement, and scenario spawn validation are now domain-aware. `UNIT_KINDS`/`BUILDING_KINDS` in `scenarioSeedOps` derived from their authoritative tables instead of hand-listed. New `ship` role across the render/glyph/armour unions with `aoeVoxelUnitShipRecipes.ts`. The villager gather loop now serves Fishing Ships unchanged, keyed on `gathersResources`. |
| 2026-08-19 | M4 unique units: `uniqueUnits.ts` is the single civ->unit->building gate that both the Castle and Dock train menus read. `unitTypes.ts` extracted from `types.ts` (the 44-member roster union was duplicated verbatim as `TrainableUnitType`, which is now an alias); `unitClassSets.ts` split the classification sets out of `statTables.ts`; `unitLineUpgrades.ts` turned 23 identical `technologyOps` switch cases into a table; `dockTechOptions.ts` carries the six new Dock technologies. New `unique-unit` armor class, `hand-cannon`/`mounted-gun` weapons, and an `elephant` mount in the render vocabulary. |
| 2026-08-19 | Per-unit base movement speed: `prototypeUnitRules/unitBaseSpeed.ts` (units.csv `movement_rate` as a percent of a villager) multiplied into `movementTechEffects.movementSpeedPercent`, so the tech percents compose rather than replace. The scout wander path applies it directly and now shares one `repickHeading` between both refusal branches. |
| 2026-08-19 | Elite unique-unit tier: 19 elite units + 19 Castle/Dock technologies, each an ordinary row in `bridge/unitLineUpgrades.ts`; the elite chain is an optional field on the `uniqueUnits.ts` entry and the upgrade is offered at the building that TRAINS the unit. Splits for the budget: `prototypeUnitRules/unitArmorTables.ts` out of `statTables`, `buildingProductionTables.ts` out of `prototypeBuildingRules`, `bridge/buildOptions.ts` out of `optionsRules`. |
| 2026-08-19 | Occlusion silhouettes are keyed on ownership (`occlusionSilhouetteTint`) rather than a single white constant. |
| 2026-08-19 | Patrol: `patrolRoute.ts` (pure route + turnaround), `bridge/patrolCommandOps.ts`, `bridge/systems/patrolSystem.ts` (runs after `prototypePlayerCommands`, so an arrival and a finished fight look the same), Tier-1 slot #38 `aoe2.patrolRoutes`. Deliberately NOT a UnitCommand — see spec §12.4.0. Budget splits alongside: `bridge/gatherCommandOps.ts` out of `unitCommandOps`, `app/armedGroundOrder.ts` and `app/voxelGameViewHelpers.ts` out of `AoeVoxelGameView`. |
| 2026-08-19 | Formations: `unitFormation.ts` (pure shapes + front-to-back ordering), `bridge/formationPlanning.ts` (selection + click -> ordered ids and preferred cells), Tier-1 slot #39 `aoe2.unitFormations`, and an optional `preferredCells` argument on `allocateGroupMoveTargets` that every group order now supplies. Budget splits alongside: `bridge/selectionCombatStats.ts` out of `selectionStateOps`. |
| 2026-08-19 | University building-defence technologies: `buildingTechEffects.ts` (pure multipliers), `bridge/buildingHpTechEffect.ts` (the Loom-shaped "raise what is already standing" half; the other half is derived in `entityCreateOps`). `bridge/systems/builderWorkStep.ts` extracted from `playerCommandsSystem` — it returns the construction state it advanced rather than a marker, so the caller can read it without re-narrowing. |
| 2026-08-19 | Unique technologies: `uniqueTechnologies.ts` declares each one's effect (unit stats, building attack/range, train rate, training unlock) and `bridge/uniqueTechEffect.ts` applies it to units already standing; `combatStateFactory` gained `applyUniqueUnitEffect` and one loop in place of sixteen branches. `uniqueTechnologyUnlocks.ts` keeps Anarchy's Barracks Huskarl off `optionsRules`, and `bridge/castleTechOptions.ts` took the whole Castle research branch out of it. |
| 2026-08-19 | `bombard-tower` BuildingType across the ~18 exhaustive building tables. `buildingHitPointMultiplier` generalised to take an optional building type, with a small table of per-building HP technologies (Fortified Wall, Guard Tower, Keep); `buildOptionsFor` gained a tech predicate for the one build option gated on research rather than age. |
| 2026-08-20 | `gatherDomain.ts` gates resource assignment on the gatherer's DOMAIN (a fish is food, and a villager cannot reach one). The AI gained farms (`targetFarmCount`), siege in its unit mix, and Castle production read from the same train options the HUD uses. Budget splits: `aiResourceReserve.ts` out of `ai.ts`, `bridge/systems/dropOffStep.ts` out of `villagerEconomySystem`. |
| 2026-08-20 | `bridge/idleGatherAssignment.ts` out of `villagerEconomySystem` — what a gatherer with no workable target does next (resource-kind fallback, reachability retry cadence). `spawn.ts` egress became a bounded free-region walk rather than a one-neighbour test. `movementTrafficOps` gained a `canTakeATurn` predicate and now returns `proceed` directly from the cycle election instead of falling through to the per-cell rules. `villagerGatherAssignment` bounds auto-assignment to a home range, keeping the unfiltered set for a widen-only-if-nothing-is-reachable recovery probe. |
| 2026-08-20 | `gates.ts` owns the two questions specific to gates (which buildings are gates, which wall each belongs to, and who a gate admits) and leaves everything else to the wall tables; `cellPassability` composes `gateAdmits` as the one exception to a blocked cell. `stone-gate`/`palisade-gate` BuildingTypes across the twelve exhaustive building tables, a `'gate'` render role with its own voxel recipe and detail pass, and a `gate-fixture` that walls the map edge to edge. `scripts/captureMapScreenshot.mjs` gained `FOCUS`. |
| 2026-08-20 | `monasteryTechEffects.monkMayConvert` becomes the single answer to what a monk may convert, read at the conversion site in `monkTaskAppliers` rather than assumed at the target-picking site in `monkTaskOps`. Fervor rides `movementTechEffects` with its own branch, because a monk matches none of the mounted/infantry/villager ones. Two monk-versus-monk fixtures differing only in the converter's researched set. |
| 2026-08-20 | The selection panel becomes a bottom command BAR: `hudCommandPanel.css` owns its width and internal layout (styles.css keeps only the pointer-events gate, where the two stylesheets had been fighting over the same rule), and `selectionPanel.ts` groups heading + portrait + detail chips into one `hud-selection-summary` block so the deck gets the rest of the bar. `captureHudSelection.mjs` gained `VIEWPORT`. |
| 2026-08-20 | `buildingMinimumRange.ts` answers what a defensive building cannot reach because it is too CLOSE; `findPreferredVisibleEnemyUnitInRangeOfBuilding` gained an optional `minimumRange` and the tower system derives it from the owner's researched set. `prototypeScenario/scenarioRegistry.ts` split out of `dispatch.ts`, which had crossed the file budget by holding the seed table alone. |
| 2026-08-20 | `transportShip.ts` (who may board, and how many) plus `bridge/garrisonOps.ts`, which now owns both a building's garrison and a Transport Ship's cargo — the same mechanism and the same side maps, moved out of `trainingMarketOps.ts` when it crossed the line budget. Boarding and unloading route through `contextRouter` on the existing right-click rather than through a new recorded command. |
| 2026-08-29 | `worldOccupancy` gains `notePassabilityChange()`, threaded through `transformOps` → `RegisterAllSystemsDeps` → `PlayerCommandsSystemDeps` → `finalizeBuildingConstruction` (required parameter). A gate finishing changes WHO may pass a cell without claiming or releasing one, so it never moved `structuralRevision` — the counter the v0.3.160 unreachable-plan cache keys on. The bump is load-bearing rather than a perf hint: a stale entry changes what the simulation does. | `docs/devlog/detailed/2026-08-25_2026-08-30.md` |
| 2026-08-30 | `spawnPassabilityMemo.ts` splits out of `worldOccupancy.ts` (which the 500-line gate refused at 519): a per-revision `Uint8Array` cache for `isCellPassableForSpawn`, plus a `computeSpawnPassability` that reads engine claims directly instead of building a merged cell status. Second load-bearing consumer of `structuralRevision`. Tick cost 15.13 → 7.99 ms/tick at 34 units. | `docs/devlog/detailed/2026-08-25_2026-08-30.md` |
| 2026-08-30 | `registerWildlifeSystems.ts` groups the four animal systems (predator combat, deer flight, herdable ownership/movement) out of `registerAllSystems.ts`, which hit its 500-line ceiling; its deps type is the INTERSECTION of the four systems' own dep types rather than a hand-listed `Pick`. `deerFleeSystem.ts` is new. `setUnitAttackCommandDirect` is now threaded into the system layer — the first deterministic-resolution attack call site, which a comment there had asserted did not exist. | `docs/devlog/detailed/2026-08-25_2026-08-30.md` |
| 2026-08-30 | `buildRateMultiplier` gains an optional context carrying WHAT is under construction, so a build bonus scoped to one building (Incas farms) can be expressed at the seam the Spanish builder bonus and Treadmill Crane already shared. `canAdvanceToImperialAge` accepts a lone Castle per spec §7.2. `DARK_AGE_PREREQUISITE_BUILDINGS` gains the Dock. | `docs/devlog/detailed/2026-08-30_2026-08-30.md` |
| 2026-08-30 | `aiResourceReserve` gains `agePrerequisiteWoodReserve` + `QUALIFYING_FEUDAL_BUILDINGS`, and `aiSystemProductionPhase` now subtracts the next qualifying building's wood from what military may spend — the sibling of the age-up research reserve, one step earlier in the chain. Paired with the Feudal villager split going wood 4 → 6 in `aiEconomyPlan`; the two are one rule and were measured as one. | `docs/devlog/detailed/2026-08-30_2026-08-30.md` |
