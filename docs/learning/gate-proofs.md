# Gate proofs

The standing answer to "did the gates actually do their job".

A lesson leaves [lessons.md](lessons.md) only when it becomes a gate, and a gate counts only once it has been made to go RED by reintroducing the defect. This file records the mutation, the failure it produced, and the bound the gate does not reach past. Without it, "this became a gate" is an unverified claim and the deletion it justifies is unearned.

## If a gate here is wrong

A gate and the claim in its header can be wrong together, and when they are they look exactly like a gate that is right. Auditing one means reaching what was believed, measured and abandoned at the time — never the sentence the gate carries about itself. The evidence deleted in a retirement commit is not lost; this repository's evidence file as it stood immediately before the first retirement reads back out of:

    git show 96ed269d:docs/learning/lessons-evidence.md

`git log -- docs/learning/lessons-evidence.md` lists every earlier revision, and `git log -S'<phrase from the gate header>' -- docs/learning/` finds the entry a particular gate retired.

## On SwiftShader the default view costs no more to draw in the Natural style than in Moebius

- **Gate:** `tests/browser/de-ground-frame-cost.spec.ts` :: `draws the default view in the Natural style for no more than Moebius costs, on SwiftShader`, run by `npm run test:browser`, and so by `npm run verify` and by every CI browser run. It retires the `lessons.md` entry of 2026-09-24, "A change to what every frame draws is measured on SwiftShader, on CI's four CPUs, against the default it would replace, before it becomes the default"; its evidence ("The default look that doubled CI's frame") is deleted in the same commit and reads back out of git history.
- **Claim it carries:** on SwiftShader, in one page, the paused opening view of `aoe2-prototype` at 800x600 costs no more in the Natural style than in Moebius: over five rounds that alternate the styles through the game menu's row, the median of Natural's lower-quartile frame over Moebius's is at most 1. A frame is the game's own animation-frame callback, waited on at both ends. Every callback is wrapped: a one-pixel `readPixels` before it makes the page wait until the GPU process has caught up with this context, one after it waits for what it drew, and a callback that drew no world frame is not counted. The first read syncs only the WebGL context's own stream; adding it moved CI's reading from 0.92-0.98 to 0.85-0.87, which is the evidence that it keeps the work the styles share out of the timing. After the verdict it asserts that the Natural ground drew with the single-sample tier, as its textured ground with no terrain chunks, and lit (median luma over 40 at up to 200 visible cell centres), so a detection regression shows its cost first and a ground that stopped drawing cannot pass as cheap.
- **Mutation 1, the regression itself:** SwiftShader dropped from the CPU-rasteriser pattern in `src/rendering/voxel/aoeDeGroundTier.ts`, so SwiftShader gets the blend again. **Red** on the development machine held to four CPUs by an affinity mask: `Natural's default frame costs 2.040 of Moebius's on SwiftShader (... the Natural ground drew with the blend tier)`. On all 32 threads it went red at 1.063, a margin of 0.063. On CI the forced blend read 2.12 (run 36100866192).
- **Mutation 2, the cost check on its own:** the single-sample branch compiled out (`#ifdef DE_GROUND_SINGLE_SAMPLE` to `#ifdef DE_GROUND_NEVER_DEFINED`), so the page reports the single-sample tier while drawing the blend. **Red** at 1.875 on four CPUs and at 1.006 on 32 threads. The tier assertion, which runs after the verdict, would have passed, so this red is the cost check's.
- **Mutation 3, a ground that stopped drawing:** the ground mesh never shown (`this.mesh.visible = false`). The verdict passed at 0.458, a frame with no ground being cheap, and the lit-ground check went **red**: `median luma of visible ground in the Natural style`, 20.4 against 40.
- **Mutation 4, Natural drawn on the voxel ground:** the style's `ground` set to `voxel` in `src/rendering/artStyles.ts`. The verdict passed at 0.591 and the ground check went **red**: `Expected: "textured, 0 terrain chunks"`, `Received: "voxel, 12 terrain chunks"`. The check reads the renderer after the timed frames: its first version read right after the switch, before a frame had presented the switched snapshot, saw Moebius's twelve chunks, and turned the full suite red once with nothing wrong.
- **Green:** each mutation's source was restored byte-identical and rebuilt by the driver, which checks. Unmutated, the gate read 0.85 to 0.87 on CI (four readings in run 36100866192, about 12 s each), 0.81 to 0.83 on four masked CPUs and 0.59 to 0.61 on 32 threads. The local figures were taken on a shared machine whose other sessions' load was not recorded at the time; one of them ran a SwiftShader sweep on about 13 cores during part of the window. The CI figures carry no foreign load.
- **A discarded instrument, recorded because its green would have flaked.** The first version timed a frame from the animation frame's start to a `readPixels` in a callback after the game's, with the HUD's frosted glass left on. That time holds the compositor's work on the frame before, which is the same in both styles, so it pulled the ratio toward 1: 0.80 to 1.01 across runs on four masked CPUs (one run red at 1.010 with nothing wrong) and 0.92 to 0.98 on CI (run 36096561813), where the blend read 1.92 to 1.97. It also took 64 s on CI. The blur is switched off for the run now, and the read before each callback keeps the work the styles share out of the timing.
- **A discarded mutation run.** Twice the masked Mutation 1 went red at boot instead, in `waitForPausedBootWithSeed`'s five-second poll of full snapshots, which a frame as slow as the blend's outlasted on four busy CPUs. A red for the wrong reason proves nothing, so the gate now boots with one light read of the seed, and the mutation was run again.
- **Bound.** SwiftShader only, the one paused view, 800x600. Its teeth are on few CPUs: on 32 threads a blend-sized regression read 0.97 to 1.40 across runs (a probe once read the forced blend at 0.966), so the local suite on a many-core machine can pass one that CI, on four vCPUs, reads at 2. It is a relative check: a change that makes both styles dearer, or Moebius alone, passes it, although the retired rule speaks of any change to what every frame draws. A GPU is not measured, and neither is a moving world or a later game with more ground known. The lesson paired this gate with the change that makes Natural the default again; that flip is the next commit, so for one commit the gate holds a style that is one menu row away rather than the default.
- **Review.** An independent read-only review of the commit found no blocker. It asked for the ground assertion above, the light boot in both new specs, the single-sample tier's rim checked in pixels on every machine (it is now, in `de-ground-full-blend.spec.ts`), and honest wording about what the first read drains; all taken.

## CI draws the Natural ground's full blend, although its rasteriser gets the single-sample tier

- **Gate:** `tests/browser/de-ground-full-blend.spec.ts` :: `draws the full blend on SwiftShader when asked, keeping the fog rule, where the single-sample tier draws its own`, run by `npm run test:browser` and on CI. It ships with the tier it answers for, so no `lessons.md` entry is retired by it.
- **Claim it carries:** on SwiftShader, with the blend forced through `aoe2:de-ground-tier`, the page reports the blend and a SwiftShader renderer, logs no shader or WebGL error, lights visible ground, and keeps unexplored rim cells black at every sampled point with the known side dark by the shared edge. Against the single-sample tier (a fresh context without the override) it draws the same colour within 6 levels at 90% of the centres of visible land cells with no unexplored cell beside them and no building's dirt, and differs by more than 8 levels over more than 1% of the canvas. The single-sample page is held to the same rim rule, so the tier CI draws by default is checked in pixels on every machine. Measured: 69 of 69 centres within 6 levels (largest gap 1), and 2.13% of the canvas differing (2.30% on CI before the single-sample fade became the square of its smoothstep). Nothing is timed.
- **Mutations, each red at the assertion that names it:** the override ignored (`readDeGroundTierOverride` always null): `Expected: "blend"`, `Received: "single-sample"`. The blend's colour divided by 1.3: 28 of 69 centres within 6 levels (0.406 against 0.9). The blend's fade to unexplored made a cut: known edge over centre 0.753 against 0.35. The blend's GLSL broken (`not glsl;` after its colour division): shader errors in the console. The single-sample tier's fade removed: `single-sample: known ground 0.06 of a tile from the unexplored edge, over its centre`, 0.754 against 0.35. Each source was restored byte-identical and rebuilt.
- **Bound.** One map at tick 0 and one camera (zoom 0.7, 1280x720). The blend is compared with the other tier only where the two should agree; whether its soft lines look right is the capture sweep's (`scripts/captureMapScreenshot.mjs` with `STYLE=de GROUND_TIER=blend RASTERISER=swiftshader`), which on 2026-09-25 matched v0.3.232's blend to the pixel in five views on SwiftShader.

## A comment or a test title cannot state a tick rate this build does not run at

- **Gate:** `tests/architecture/tickRateComments.test.ts` :: three cases — `(0) the scan read the tree and found live claims of every kind it checks`, `(1) exactly one file declares the rate, so there is one thing to agree with`, `(2) no comment or string states a wrong rate, tick period, or duration` — run by `npm test`, and so by `npm run verify`. Register-driven (register entry 2026-09-06, "The attack-warning throttle is 40 seconds and its own comment says 20"): found at integration, fixed and gated in one day, so no `lessons.md` entry is retired by it.
- **Claim it carries:** over every `.ts`/`.tsx` file under `src/` and `tests/`, a stated ticks-per-second equals the real `TPS`, a stated milliseconds-per-tick equals `1000 / TPS`, and a text stating one duration AND one rate is read as a claim about a tick count that must equal `round(seconds x TPS)`. It scans STRING LITERALS as well as comments. `ATTACK_WARNING_THROTTLE_TICKS = 400` had carried "~20s at 20 TPS" since the horn shipped as `HORN_THROTTLE_TICKS`; this build runs at 10, so the throttle was forty seconds. The rate is imported from `src/game/simulation/prototypeScenario.ts`, not restated, and case (1) asserts that exactly one file in `src/` declares it — so the gate cannot become one more comment that agrees with itself.
- **Mutation — the exact pre-fix state:** `git checkout HEAD -- src/ui/hud/attackWarning.ts tests/ui/attackWarning.test.ts tests/simulation/bridge-additive-surfaces.test.ts` on the fixed tree, restoring the three live instances verbatim (`9dd31653`).
- **Red:** `1 failed | 2 passed`, exit 1, naming five claims: `src/ui/hud/attackWarning.ts:41 says "20 TPS", but this build runs at 10 ticks per second`; `src/ui/hud/attackWarning.ts:41 says "20s", which is 200 ticks at 10 ticks per second, but the value on line 42 is 400`; `tests/simulation/bridge-additive-surfaces.test.ts:86 says "30 TPS", but this build runs at 10 ticks per second`; and TWO from a test TITLE — `tests/ui/attackWarning.test.ts:135 says "20 TPS" …` and `… says "20 seconds", which is 200 ticks at 10 ticks per second, but the tick count stated in the text is 400`. Those last two are why the scan reads strings: a comments-only gate would have gone green over half this defect, and it is the string scan that found the third instance.
- **Green after revert:** yes — `3 passed`, exit 0, on the three files restored byte-for-byte from the copies taken before the mutation.
- **A discarded mutation, recorded because its red was worthless.** The first attempt hand-edited the title to `it('is AoE2's spacing: …'` without escaping the apostrophe in `AoE2's`, which closes the string and leaves the file unparseable. The gate still went red — on three claims, missing both title claims entirely — because the scanner's quote tracking was reading a file no compiler would accept. A mutation that changes more than the variable produces a red that looks exactly like a real one. `git checkout HEAD --` is the mutation that is guaranteed to be the pre-fix state and nothing else.
- **Bound.** It reads TEXT: it cannot tell a true comment from a stale one that names no number, and it says nothing about whether 200 is the right spacing — only that the words and the number agree. The arithmetic case needs a rate spelled with `TPS` in the SAME text, so a bare duration beside a tick constant, or a duration written in words, is invisible. It skips a text with more than one duration or a slash list of numbers before a unit — three today (`garrisonHealSystem.ts`, two rows of `researchTables.ts`) — and one with no tick count to bind to — two today (`stepReport.ts`, `bridge-additive-surfaces.test.ts`); those are unchecked, not checked-and-passing. Measured on the shipped tree: 44 arithmetic pairs checked, 5 skipped. A ms-per-tick figure written with a SLASH is deliberately out of scope, because in this repo that shape also means measured CPU cost per tick (`spawnPassabilityMemo.ts`, `movementPlanOps.ts`); `scripts/` is out for the same reason (`aiTickRate.mjs` reports wall-clock throughput). `TPS = <n>` with spaces around the equals is not read as a rate, because `at 10 TPS = 4 HP/sec` is an equation. Comment extraction tracks string and template literals but NOT regex literals, so a regex containing `//` could shift what is scanned — which shows up as a spurious failure, never a silent pass. Documentation is not scanned at all: `design/spec-final.md` and the devlogs are held only by review.
- **Guard against a false green:** case (0) is the instrument check — over 900 files, over 50 rate claims, at least one period claim, over 40 arithmetic pairs — so a broken extractor reports red rather than green over nothing. The gate scans its own file like every other, with no exemption, so a shape named in its header must use a placeholder (`<n> TPS`) rather than a wrong literal rate.

## A civ-engine helper this repo is waiting on goes red the day it is promoted

- **Gate:** `tests/civEngineInternalHelpers.test.ts` :: eleven tests in one describe — one instrument check (`loads both barrels, and they are the two distinct surfaces`), four public controls over `snapshotAtTick`, `diffBundles`, `SessionReplayer` and `BundleViewer`, and six absence cases over `applyTickDiff`, `foldTickDiffs`, `emptyTickDiff`, `assertBundleShape`, `validateBundleMarkers` and `cloneTickDiff` — run by `npm test`, and so by `npm run verify`. It ships with its own lesson, so no `lessons.md` entry is retired by it, which is the precedent the LF and `ci:status` entries above already set.
- **Claim it carries:** those six helpers are not runtime exports of `civ-engine` or `civ-engine/browser`, and the day the engine promotes one this repo is told rather than left working around it. Every failure message is written as GOOD NEWS: it names the aoe2 workaround the promotion retires (`positionReplay.ts:41-63`'s hand-folded TickDiff and its two `Record<string, unknown>` casts, `reconstructPositions`, `replayControllerHelpers.ts:101-109`'s hand-rolled bundle-shape guard throwing the engine's own public `BundleIntegrityError`, `findingsToMarkers.ts:117-126` writing a marker set it cannot validate), the engine source line to check, and the instruction to delete the promoted name from `INTERNAL_HELPERS` rather than the test — because deleting the test throws away the notice for the other five.
- **Why it IMPORTS rather than greps.** civ-engine's `package.json` puts `dist` first in `files` and the package has no `.npmignore`, so every emitted declaration ships — `apply-tick-diff.d.ts`, `session-replayer-guards.d.ts`, `session-replayer-markers.d.ts`, `bundle-viewer-internal.d.ts`, `world-internal.d.ts` all sit in a consumer's `node_modules`, findable by a text search and unreachable by an import, because `exports` publishes only `.`, `./browser` and `./package.json`. On 2026-09-06 one worker grepped `node_modules` and reported all six present; another imported and reported all six absent. Both looked; only one measured the thing that matters.
- **Mutation — the control that counts:** both specifiers aliased to a module that loads and exports nothing (a vitest config under gitignored `tmp/probes/`, with the `harness:` line the hook requires).
- **Red:** `5 failed | 6 passed`, exit 1 — the instrument check (`expected 0 to be greater than 100`) and all four public controls (`expected 'undefined' to be 'function'`). **ALL SIX absence checks PASS**, vacuously, against a barrel with nothing in it. That is "did not run reported as passed" demonstrated rather than argued, and it is the whole reason the five non-absence cases exist. Reproduced here on 2026-09-06 independently of the worker that first ran it, with the same counts.
- **A discarded mutation, recorded because its red was worthless:** mocking the module made the file red with SIX cases failing rather than one, because the mocking framework throws on names the mock omits. The failure was the mock's, not the tripwire's, and its red is indistinguishable from a real promotion. A mutation that races the condition it means to disprove proves nothing.
- **Green after revert:** yes — `11 passed`, exit 0, against the real `civ-engine` 2.4.1.
- **Bound.** It is about the RUNTIME export surface of two barrels and nothing else. The TYPE surface is not asserted and cannot be: a static `import { applyTickDiff } from 'civ-engine'` would make `tsc --noEmit` red today, so a file that must compile cannot hold that assertion — `npx tsc --noEmit` refusing such an import is the proof for the type half. It says nothing about whether the behaviour is reachable another way; it is, through the four public controls, which are the supported route. It is pinned to ONE installed engine version — the bound was established against 2.4.1, and the version is reported in every failure message and deliberately NOT asserted, because a bump is not a promotion and must not be reported as one. And it measures a LINKED build: `civ-engine` is a `file:../civ-engine` dependency, so `node_modules/civ-engine` is a symlink to the sibling checkout whose `dist/` is that checkout's own gitignored `tsc` output, while CI resolves the same link from the rolling `engine-dist` tarball — two different sets of bytes making the same claim, and each failure message says which it read. The 2026-09-06 investigation reported the two byte-equivalent; that was not re-measured here.
- **Guard against a false green:** the instrument check reads both namespaces' key counts before anything trusts an absence, and proves the two barrels are two distinct builds — `FileSink` is a function on the node barrel and undefined on the browser one — so a resolver that served one barrel for both specifiers cannot look like full coverage.

## Every checkout of this repository gets LF, on every platform

- **Gate:** `tests/architecture/checkoutLineEndings.test.ts` :: five checks in two describes — `the instrument reads git, and reads more than nothing` (the row parse plus known paths; text-versus-binary really distinguished) and `a checkout of this repository gets LF, whatever platform runs it` (`eol=lf` resolved for every text file; nothing stored with CRLF in the index; the shebang scripts covered) — run by `npm test`, and so by `npm run verify`. Register-driven (register entry 2026-09-06, "A Windows checkout rewrote every tracked file to CRLF"): found by the remote gate, fixed and gated in one session, so no `lessons.md` entry is retired by it.
- **Claim it carries:** `.gitattributes` guarantees that a checkout of this repository writes LF on every platform, so the bytes CI compiles are the bytes that were authored. Git for Windows installs `core.autocrlf=true` in its SYSTEM config and the GitHub-hosted `windows-latest` image is that same install, so without the guarantee `actions/checkout` rewrites every tracked text file to CRLF. A CRLF shebang then breaks Vite: `ssrTransform` finds its import-injection offset with `hashbangRE = /^#!.*\n/` (vite 6.4.3, `dist/node/chunks/dep-Dm0c1Wj2.js:25674`), a JavaScript `.` does not match `\r`, the offset falls back to 0, and `__vite_ssr_import__` calls land in front of `#!/usr/bin/env node` — `SyntaxError: Invalid or unexpected token`. Eighteen tracked scripts carry a shebang.
- **Mutation 1 — the exact pre-fix state:** `.gitattributes` removed from the index and from the working tree. **Red:** `3 failed | 2 passed`. `.gitattributes is tracked but was not in the listing, so the scan is not seeing this repository`; `1767 tracked text file(s) do not resolve \`eol=lf\`, so a checkout under Git for Windows' default \`core.autocrlf=true\` ... writes them with CRLF`; `18 script(s) that begin with \`#!\` are not covered by the LF guarantee`. The binary control stays green, correctly — git sniffs `i/-text` for the 68 PNGs whether or not the file exists, which is why the classifier reads the index column and not the attributes alone.
- **Mutation 2 — the guarantee inverted:** `* text=auto eol=lf` changed to `eol=crlf`. **Red:** `2 failed | 3 passed`, naming all 1,768 text files and all 18 shebang scripts.
- **Mutation 3 — the scripts smuggled out of the guarantee:** `scripts/** binary` appended. **Red:** `1 failed | 4 passed`, on the enumeration control — `No tracked script starts with \`#!\`. There were 18 on 2026-09-06 ... the enumeration is broken and the assertion below is passing over an empty list`. This is the check refusing to report "did not run" as "passed".
- **Mutation 4 — a blob genuinely stored with CRLF:** `git hash-object -w --no-filters` on a CRLF file, added with `git update-index --cacheinfo`, which bypasses the `eol=lf` filter the way a bad merge or an old commit would. **Red:** `1 failed | 4 passed` — `1 tracked file(s) are stored with CRLF in the index. \`eol=lf\` fixes what checkout writes and cannot fix a blob that already carries \`\r\`, so these arrive with CRLF on Linux too.`
- **Green after revert:** yes. `.gitattributes` restored to SHA-256 `d8da2543c21f2554cf94532ca7afa872299139b17e43ca89366b598ba09d09ad`, identical to before the mutations; `npx vitest run tests/architecture/checkoutLineEndings.test.ts tests/scripts` gives `13 passed`, exit 0.
- **The defect itself, proved both ways.** `scripts/ci-status.mjs` converted to CRLF in place: `npx vitest run tests/scripts` gives `6 failed | 2 passed`, `SyntaxError: Invalid or unexpected token` at `tests/scripts/ciStatus.test.ts:131:21` — the Windows runner's failure reproduced exactly, on the machine where it passes. The discriminating control, which is what makes the cause the SHEBANG and not CRLF in general: the same CRLF file with only its `#!` line replaced by an ordinary comment gives `8 passed`.
- **Bound.** The gate speaks about tracked files and about what a fresh CHECKOUT produces. It deliberately asserts nothing about the working tree on disk: 810 of 1,766 tracked files were CRLF in the authoring tree on 2026-09-06, left over from checkouts that predate `.gitattributes`, and such a tree cannot reach CI, which always clones fresh — a check that went red for that would be deleted by the next person it annoyed. It says nothing about Vite's hashbang handling, so a future toolchain that mishandles LF leaves it green. It cannot see a per-clone override outside the repository (`.git/info/attributes`, `core.eol`). Deleting a `.gitattributes` rule and the check together would pass, as always. And it does not prove the runner is fixed — only a Windows CI run does that.

## The command that checks the remote gate answers for a commit, not for whatever ran last

- **Gate:** `tests/scripts/ciStatus.test.ts` :: eight cases in three describes — `ci:status reports on a commit, not on whatever run is newest` (UNGATED, NEVER ASKED, CARRIED), `ci:status separates a gate that could not start from a gate that failed` (the billing annotation, GREEN, RED), and `the fixtures are wired the way the script asks for them` (the two instrument controls) — run by `npm test`, and so by `npm run verify`. Register-driven (register entry 2026-09-05, "The command that checks the remote gate reported an earlier commit's run as the tip's"): found, fixed and gated in one session, so no `lessons.md` entry is retired by it.
- **Claim it carries:** `npm run ci:status` reports on the commit a session is about to build on — the local HEAD, and the pushed tip when they differ — and never on whatever run happens to be newest in a workflow's run list. A commit with no run of its own gets one of three answers rather than silence: NEVER ASKED (the workflow's `on.push.paths` matched nothing that changed, exit 0), CARRIED (a later push gated it at its tip), or UNGATED (a run was due and none exists, exit 1). `const latest = finished[0];` had bound the whole report to the newest entry of a run LIST, and no call in the script mentioned a sha, so a commit that produced no run read as whatever an earlier commit's run had said.
- **Mutation:** restore `scripts/ci-status.mjs` to `b1d0a0ff`, the shipped script before this change, with the gate file and its fixtures left in place.
- **Red:** 6 of the 8 fail. The UNGATED case — a pushed commit that changed `package.json` whose runs API returns `total_count: 0` — fails on `expected +0 to be 1`: the old script exits 0 and prints `CI: GREEN — 20+ consecutive, latest "Merge: motion smoothing survives a slow machine (v0.3.192)" at 2026-09-03T04:04:53Z`, a verdict about a different commit. The two instrument controls are among the two that stay green, which is what they are for.
- **Green after revert:** yes, `8 passed` (`npx vitest run tests/scripts`, exit 0), and the full `npx vitest run tests` exit 0 with 481 files and 3,478 tests on the same tree.
- **Bound. Fixtures are not the API.** Every fixture was captured from the live repo with `gh` on 2026-09-05, but they are the projections the script's own `--jq` asks for, not raw payloads. The suite proves the reporting logic and the exit codes; it proves nothing about whether GitHub still returns these shapes or whether `gh`'s flags still mean what they meant, so a field rename on GitHub's side leaves it green. Running `npm run ci:status` against the live repo is the only check on that. The YAML reader understands `on.push.paths` as the two watched workflows write it and refuses `!`, character classes and brace groups; `origin/main` is the locally cached ref, so an unfetched tip looks like the tip; the path filter is evaluated over the diff since the last gated ancestor, a stand-in for the push GitHub actually saw that is wrong for a force-push or a rewritten history; a `skipped` or `cancelled` run with no jobs still reads RED; and the third workflow, `playtest-corpus-llm`, was not watched at all (it was deleted on 2026-09-25).
- **Guard against a false green:** the fake `gh`/`git` throws on any command it has no fixture for, because a stub that silently returns `""` is exactly how a check reports "did not run" as "passed"; and one case asserts the recorded zero-run and never-started responses still have the shape the other cases depend on, so a fixture edited into uselessness fails by name instead of passing everywhere. The CARRIED case is the over-fix guard: an interior commit of a multi-commit push must not read UNGATED, or the command would cry wolf on every commit but the tip.
- **Amended 2026-09-24.** The two watched workflows no longer filter `on.push.paths`. The three `workflow--*.yml` fixtures stay as the filtered files they were last copied from, at `ecfaa3ff` (2026-09-06), because the script reads the filter a commit CARRIED; the 2026-09-06 case holding them equal to today's files was replaced by two controls that require them to keep that filter, and a new case judges a docs-only commit against today's files and requires UNGATED. See "Every workflow ci:status watches runs on every push to main" below.

## The bottom command bar keeps one height, whatever is selected

- **Gate:** `tests/browser/selection-panel-height.spec.ts` :: four tests, `the command bar keeps one height at <W>x<H>`, one per viewport (800x600, 1024x768, 1280x720, 1920x1080) — run by `npm run test:browser`, and so by `npm run verify`. Register-driven (register entry 2026-09-05, "The bottom command bar was a different height for every selection"): the defect was reported, fixed and gated in one session, so no `lessons.md` entry is retired by it.
- **Claim it carries:** across 17 boot-scenario selections in a fixed order (nothing; a villager on both build pages and placing a House; the scout; the Town Centre empty and with one villager queued; a House foundation; sheep, tree, berry bush, gold mine, stone mine; every villager; a mixed box; nothing again) the panel's height and top edge stay within 0.5px of the empty state's, and each state is held inside the band without a vertical scroll — with one named exception, the queued Town Centre at 800x600, which the spec pins as still overflowing so that a compact queue retires the exception rather than leaving it standing. `.hud-panel--selection` had a `max-height` ceiling and no `height`, and `.hud-bottom` bottom-aligns its row, so the bar was as tall as its conditional content and its top edge followed it on every click.
- **Mutation:** the fixed tree with the `height: var(--hud-selection-bar-height)` line commented out and rebuilt — the bundle carried the token's two definitions and no `height` use, as the session recorded (`docs/debugging/2026-09-05-hud-panel-height.md`); the run is `tmp/hud-height-red-mutation.txt`. The same spec against the tree before the fix is `tmp/hud-height-red.txt`.
- **Red:** `4 failed` of 4 in both runs. At 1280x720: `a villager, Economic page: the bar is 185.5625px tall against 72px with nothing selected; the bar's height must not depend on the selection`, `the Town Centre: the bar is 242.75px tall against 72px with nothing selected`, `a tree: the bar is 114.1875px tall against 72px with nothing selected`, `the Scout Cavalry: the bar's top edge is at 495.4375 against 636 with nothing selected; the bar must not move when the selection changes`; at 800x600: `a villager, Economic page: the bar is 226.5625px tall against 65px with nothing selected`, `every villager: the bar is 232px tall against 65px with nothing selected`. Every failure prints each state's top, height and content-against-box, so the whole profile is in the run.
- **Green after the fix:** yes, `4 passed (29.6s)` (`tmp/hud-height-green.txt`): 236px at 800x600 and 1024x768, 246px at 1280x720 and 1920x1080, in every one of the 17 states, top edge 352 / 520 / 462 / 822.
- **Bound.** `aoe2-prototype` paused at boot only — a Castle's or a Market's card scrolls inside the band under §14.2 rule (b), held by `command-deck-fits.spec.ts`; the queued Town Centre at 800x600 (261px of content in a 234px band, because the wrapped bar puts the queue on a line of its own) is held for height and top edge only, and the spec asserts it still overflows; no queue deeper than one; no width between the four viewports; no window shorter than the band (the ceilings then clamp every selection alike); no replay timeline; no pixel — the twelve capture sets recorded in the debugging doc are the pixel evidence. The mutation removes the `height` line and not the compact-list width, so the box state read 152px / 230px in the mutation run rather than its pre-fix 259.2 / 252: the gate's red does not prove the second defect's fix, and the box state's green needs it.
- **Guard against a false green:** every state asserts that its selection took effect before measuring, so a selection that silently failed cannot pass as "the same height as before"; and the 800x600 exception is asserted as still overflowing, so the exception cannot outlive the reason for it.

## A unit that walks swings its legs at its own walking speed

- **Gate:** `tests/rendering/aoeVoxelUnitWalkLegs.test.ts` :: two `it.each` cases over every UnitType in `UNIT_BASE_SPEED_PERCENT` — `<type> reaches full locomotion weight walking at its base rate` (93 types) and `<type> moves its legs between stride extremes while walking at its base rate` (the 65 humanoid and mounted types) — plus one instrument check; run by `npm test`, and so by `npm run verify`. Register-driven (register entry 2026-09-05, "Soldiers walked with their legs in the rest pose"): the defect was reported, fixed and gated in one session, so no `lessons.md` entry is retired by it.
- **Claim it carries:** every unit walking at its own base rate reaches locomotion weight 0.9 or more, and the humanoid and mounted recipes posed at that weight move a boot, a leg and a horse's front leg between the two stride extremes by at least 0.25, 0.11 and 0.3 of the recipe scale. `resolveUnitAnimationState` had blended the weight toward `speed / 2.5` tiles per second — the uniform clock before §12.4.2 — and since v0.3.160 gave every unit its own rate (villager 0.8, militia 0.9, knight 1.35) no unit reaches 2.5, so every walker's weight sat at 0.2–0.6 and its gait, every amplitude of which multiplies by that weight, played at that fraction of the authored motion.
- **Mutation:** `fullLocomotionSpeedFor(entity)` replaced by the flat `2.5` — the tree exactly as found on 2026-09-05, with the gate file copied in unchanged (`tmp/walk-legs-red.txt`).
- **Red:** `158 failed | 1 passed (159)`, every failure naming its type and its weight: `villager at 0.80 tiles/s reached locomotion weight 0.320, below 0.9: its gait plays at that fraction of the authored motion`; `woad-raider at 1.38 tiles/s reached locomotion weight 0.550, below 0.9` — the fastest humanoid and the highest weight any humanoid reached, at a boot travel of 0.178 against 0.25; `elite-teutonic-knight -boot-left travels 0.084 x scale between stride extremes at locomotion weight 0.260; at least 0.25 is visible`; `elite-war-elephant horse-leg-front-left travels 0.094 x scale between stride extremes at locomotion weight 0.240; at least 0.3 is visible`. The one pass is the instrument check.
- **Green after the fix:** yes, `159 passed (159)` (`tmp/walk-legs-green.txt`); the rest of `tests/rendering` 564 passed, with one assertion in `aoeVoxelUnitAnimation.test.ts` re-timed to read the weight after the motion window empties rather than at 300 ms, where the old normaliser alone had made a 2.1 tiles/s burst read as "stopped" (`docs/debugging/2026-09-05-walk-legs.md`).
- **Bound.** Every UnitType at exactly its base rate, no movement technology, straight-line per-tick steps along +x for 40 ticks at 100 ms, with the rate restated from the sim constants (`UNIT_SUBGRID_STEP_PER_TICK`, `UNIT_SUBGRID_RESOLUTION`, `UNIT_BASE_SPEED_PERCENT`, `TPS`) and NOT from the renderer helper the fix added, so a wrong helper cannot agree with itself; the weight is read once the two-anchor window has filled. The leg check covers the villager, infantry, archer, cavalry and cavalry-archer roles, posed through the real recipes at gait phases pi/2 and 3pi/2 only, against lower bounds under the authored full-weight amplitudes (0.32, 0.14, 0.39). It says nothing about a unit slowed by traffic (the gait is meant to fade with speed), about movement technologies (faster only clamps at 1), about siege wheels or a monk's sway, or about what a pixel does at any zoom — the capture pair in the debugging doc is the pixel evidence (default view at tick 0: 0 px differ; the same view with four walkers: 1,399 px in two blobs on the two in frame; the militia at zoom 2.4: 175 px at t25, 2,023 px at t28), not this file.
- **Guard against a false green:** the instrument case fails if the sim's own constants no longer put a villager at 0.8 tiles/s and a knight at 1.35, so every verdict is about the speed the sim moves units at; and the leg case throws by name when a recipe has no part with the expected key, rather than measuring nothing.

## A thread is closed by the task that closes it, and a pointer follows the thread it names

- **Gate:** `tests/architecture/threadHygiene.test.ts` :: three cases — `every thread path written into a live surface exists on disk, exact case`, `no thread in docs/threads/current carries a closure line`, `every thread in docs/threads/current carries a date within 30 days of HEAD` — run by `npm test`, and so by `npm run verify`.
- **Claim it carries:** `docs/threads/current` holds only objectives being worked on; every thread path written into code, tests, scripts, the spec, the architecture docs, the policies and the learning docs resolves on disk with its exact casing; a thread closed by the ritual (`Closed YYYY-MM-DD:` under its title) has been moved. On 2026-09-05 six closed threads had sat in `current/` for up to seven weeks and four pointers named threads moved months earlier (register entry 2026-09-05).
- **The six, run as a matrix.** Each of the six threads was recreated under `current/` as one probe file holding its HEAD content and file list, first WITHOUT a closure line — the folder exactly as it sat that morning — then WITH one. Without: `animation-feedback-batch` 2026-07-15 (52 days), `auto-mine-after-camp` 2026-07-15 (52), `unit-occlusion-outline` 2026-07-15 (52), `recursive-loop-closure` 2026-07-08 (59) — RED; `moebius-art-style` (19 days) and `render-realism-2026-09-02` (3 days) — GREEN, inside the 30-day tail. With a closure line: all six RED on the closure-line case, each with its file and line named. That is the honest shape of the gate: the ritual is caught at once, the un-ritualised case after 30 days.
- **Mutation, the pointer half:** the stale pointer put back exactly as found — `src/game/playtest/findingsToMarkers.ts:6` naming the `current/` path of `ai-findings-annotation-bridge`. **Red:** `src/game/playtest/findingsToMarkers.ts:6 names ` `…/current/ai-findings-annotation-bridge/DESIGN.md.` ` but ` `…/current/ai-findings-annotation-bridge/DESIGN.md` ` does not exist with that exact casing` — 1 failed | 2 passed. The `docs/threads/` prefix is elided in this record because the literal would fail this file's own scan; the sentence-final dot on the first path and not the second is the normaliser at work.
- **Mutation, the casing half:** `docs/architecture/decisions.md:667` pointed at `…/done/render-realism-2026-09-02/design.md`, lower case. Windows resolves it; Linux CI would not. **Red:** the same message shape, naming the lower-cased path — 1 failed | 2 passed.
- **Mutation, the instrument:** `.ts` dropped from the extension list, which silences exactly the two roots the 2026-09-05 pointers lived in and had left an earlier total-count floor green (critic finding W3). **Red:** `src: 0 real pointers found, at least 3 exist | tests: 0 real pointers found, at least 3 exist` — 1 failed | 2 passed.
- **Green after every revert:** yes, 3 passed, the probe folders gone and the working tree's status identical before and after the seven runs. Driver: a script that applies each mutation, runs the gate and undoes the mutation in a `finally`, with one file per probe folder so no recursive delete was needed.

- **Bound.** (1) sees literal paths in the ten listed roots only: a template with `<objective>` placeholders resolves to the folder, an elided `...` path is checked up to the elision, and `docs/devlog/`, `docs/changelog.md`, `docs/debugging/` and `docs/threads/` are exempt by the rule's own decision because they narrate moves and pending states as they were ("the folder moves from `current/` to `done/` in this commit"); those hold about fifty stale pointers today. (2) catches only a thread closed by the ritual. (3) is a 30-day tail, as the matrix shows: two of the six were green under it alone. It reads `YYYY-MM-DD` dates as text from file contents and path names, so a deadline ("retry after 2026-09-06", live in `concurrency-review-2026-09-02` today) or a version's date counts as maintenance, and a status line that is false but dated today passes; a date followed by a timestamp's `T` counts. The clock is the newer of HEAD's commit date and the devlog's newest date, so an idle repo never goes red on its own, and a run of commits whose devlog was skipped still moves it. Thirty days: a thread closed without its closure line can sit for 29 days of commits without a word.
- **Guard against a false green:** each of `src`, `tests`, `design` and `docs/architecture` must yield at least three real pointers (a template resolving to the bare `current/` or `done/` folder is not counted), so a broken regex, extension list or root entry goes red; the clock throws if neither `git log -1` nor the devlog yields a date.

## An arithmetic claim about hardware is not a measurement, and the gate that measures it must read the buffer the work actually lands in

- **Gate:** `tests/browser/shadow-depth-precision.spec.ts` :: `every buffer the scene draws into resolves the shadow level step` — run by `npm run test:browser`, and so by `npm run verify`.
- **Claim it carries:** `SHADOW_LEVEL_STEP = 0.002` world units separates two casters' shadows in depth. That is derived arithmetically as about four depth quanta from the orthographic camera's 4000-unit range on a 24-BIT buffer, and nothing in the renderer requests 24 bits — the context asks for `{alpha, antialias, powerPreference}` and takes whatever the device gives. At 16 bits one quantum is 0.061 world units, thirty times the separation, and every shadow lands in one bucket: the level scheme degrades silently into the z-fighting stripes it exists to prevent.
- **Mutation:** in the spec's own `depthOf`, `return 16;` for every non-default framebuffer — a device whose offscreen targets are allocated `DEPTH_COMPONENT16`, which is Three's default internal format for a depth-only renderbuffer and therefore the realistic form of this defect rather than a synthetic one.
- **Red:** `the scene draws into a offscreen buffer with 16 depth bits; SHADOW_LEVEL_STEP = 0.002 needs at least 24 to separate two shadows (measured: {"drewAnything":true,"frames":13,"canvasSize":{"width":800,"height":480},"buffers":[{"target":"offscreen","depthBits":16},{"target":"offscreen","depthBits":16},{"target":"default","depthBits":24}]})`.
- **Green after revert:** yes, 1 passed in 2.7s.

**The finding that changed the gate, and the reason this entry is worth reading.** The lesson specified the gate as "a browser spec reading `gl.getParameter(gl.DEPTH_BITS)` off the live world canvas and asserting >= 24". **That gate would have measured the wrong buffer.** The default art style, Moebius, resolves the frame through a stylized pass, so the scene is rendered into offscreen targets first — and a Three render target's depth attachment is allocated independently of the canvas's. Instrumenting `bindFramebuffer` and the four draw entry points for twelve real animation frames shows the scene's draws landing in **three** buffers on this machine: two offscreen and one default. The canvas-only reading would have covered one of the three, and NOT the ones the shadow depth test actually runs in under the shipped default style. It would have been green for the same reason the defect is invisible.

Measured here, all three report 24. The specified gate would also have passed. That is precisely the problem with it: a gate that is right by luck is indistinguishable from a gate that is right, until the day the luck changes.

- **Bound.** ONE machine, ONE art style, ONE scene, ONE moment. It asserts what this device hands back when `aoe2-prototype` is drawn under the default style; a device that allocates 16-bit targets fails it, which is the point, but only if the suite is ever run there. It also only sees framebuffers that receive a draw call in the twelve frames it samples — a lane that draws rarely (a resize path, a picking pass) is outside it. It does not check that 0.002 is ENOUGH at 24 bits; that arithmetic is still arithmetic, and the thing that tests it empirically is the overlapping-shadow gate in `tests/rendering/aoeVoxelShadowLayering.test.ts`.
- **Guard against a false green:** the spec fails if the canvas is 1x1 or nothing drew. That is not hypothetical — measuring this by hand first, through a hidden browser pane, returned a 1x1 canvas, zero animation frames and zero draw calls, and every depth assertion would have passed vacuously because there was no buffer to fault.

## An AI that reaches the Imperial age must still be able to train a unit

- **Gate:** `tests/simulation/aiUnitLineTraining.test.ts` :: `fields an army in the Imperial age with no unit upgrades researched` — run by `npm test`, and so by `npm run verify`.
- **Claim it carries:** the AI trains the tier it HOLDS. `getTrainOptions` offers `latestResearchedInChain`; `pickUnitMix` names the top of each line; matching them by exact name meant an AI that aged up without the upgrades matched nothing at any building and stopped training units for the rest of the match.
- **Mutation:** the pre-fix guard, `if (!getTrainOptions(owner, producer).includes(unitType)) continue;`.
- **Red:** `expected 0 to be greater than or equal to 2` — zero spear-line units in 3,000 ticks, on a fixture holding 4,000 food and 4,000 wood.
- **Green after revert:** yes, 5 passed.

**Bound.** It proves the AI trains SOMETHING from an un-upgraded producer where resources and population are abundant. Not that the army is big enough, arrives, or wins.

**The fixture had to be rebuilt twice, and both versions are the interesting part.** The first handed the AI 4,000 of every resource and PASSED WITH THE DEFECT FULLY PRESENT — on Onagers, Monks and a Throwing Axeman, because the AI built a Castle and a Monastery and researched the gold-free Onager upgrade, none of which reads the unit mix. The second squeezed wood to 600 to seal those escapes and left the gate sitting on its threshold: exactly two Spearmen, with 5 wood to spare, so any later change spending 25 more wood would have turned it red in a way indistinguishable from the defect (found by a critic, not the author). The version that works does neither: resources are generous again, and the ESCAPES ARE CLOSED BY THE ASSERTION rather than by the budget — it names the spear line, and nothing else in the game trains a Spearman. Gold stays 0 because that, alone, is the trap: every unit upgrade costs gold except the Onager's and the Capped Ram's.

## The unit-line table and the tier chains are two sources that can drift apart

- **Gate:** `tests/simulation/aiUnitLineTraining.test.ts` :: `resolves every mix entry against the REAL offers, at both tech extremes`.
- **Claim it carries:** every unit `pickUnitMix` names resolves to something its producer actually offers, at both ends of the tech state.
- **Mutation:** deleted the `'halberdier-upgrade': { from: ['pikeman'], to: 'halberdier' }` row from `UNIT_LINE_UPGRADES` — a tier known to `trainOptions.ts` but not to the line table, which is the drift this gate exists for.
- **Red:** `feudal-age: the mix wants spearman but barracks offers [champion, halberdier] with allResearched=true: expected undefined to be defined`.
- **Green after revert:** yes.

**This gate replaced a TAUTOLOGY, which is why it exists in this form.** The assertion it replaced was `trainableInSameLine(unit, [unitLineOf(unit)])`, which cannot fail for any input — `unitLineOf` is idempotent, so the single offer is always in the line. A critic confirmed it passed for `villager`, `trebuchet`, `petard`, `monk` and the literal string `made-up-unit`. It read like a class check and proved nothing. The binding form builds the REAL `createTrainOptions` so the tier chains come from `trainOptions.ts` rather than from the same table the assertion uses; a check built from the same symbol as the thing it checks proves only that the code agrees with itself.

**Bound.** One civilization with a complete tech tree (`getTrainOptions` filters civ denials, and a civ legitimately lacking a whole line would fail this for the wrong reason). Resolution only — not affordability, population, or whether the unit is worth training.

## The Feudal AI keeps mining stone, measured inside a match that is still being played

- **Gate:** `tests/simulation/aiFeudalStone.test.ts` :: `banks stone from zero, and keeps banking it`.
- **Mutation:** the Feudal villager split's `stone` weight, 1 -> 0.
- **Red:** the crossing assertion reports `stone never reached 125 — peak 60`, and the sustained-mining assertion independently measures **20 against its bar of 30** in the 2,000-5,000 window. Both were checked; an earlier version of this entry recorded only the first, because the crossing throws before the mining assertion is reached, which left the moved window's bar with no red-check at all. A critic caught that.
- **Green after revert:** yes, 2 passed.

**What does NOT redden it, which is the more useful finding.** Setting the CASTLE weight to 0 — stone abandoned on ageing up — changes the mining rate not at all: a steady +10 per 250 ticks straight through, identical to the unmutated run. `villagerRebalance` never treats a zero-target kind as a donor, so villagers already on stone stay there and a mid-game reweight is not a defect that manifests. The window's real blind spot is narrower than it first appears: churn after tick 5,000 from some OTHER cause — villagers dying, or the idle-gatherer fallback rewriting their desired resource when a node stops being assignable.

**Bound, and why the window moved.** It measures ticks 2,000-5,000 only, ending 1,096 ticks BEFORE the AI first holds 125 stone. It was anchored AT that crossing until v0.3.199, when the AI started fielding an army and began winning this fixture at tick 6,561 instead of 11,442 — so the old window spent 2,535 of its 3,000 ticks measuring a FINISHED match and reported 10 mined against a bar of 30, with nothing wrong. The file had carried a liveness assertion for exactly that, placed AFTER the mining assertion where it could never fire; it runs first now, and a second guard reports a match that ends before the crossing in those words instead of as "stone never reached 125".

## A drop-off building has to be able to REACH the woodline of every map that ships

- **Gate:** `tests/simulation/dropOffAnchorReach.test.ts` — run by `npm test`, and so by `npm run verify`.
- **Claim it carries:** the AI's lumber-camp anchor is found from every start of every seed in `PLAYABLE_MAPS`, and IS the nearest tree. `DROP_OFF_ANCHOR_RADIUS` was 12 while `paintWoodlines` keeps every woodline at least `MIN_START_GAP` = 14 EUCLIDEAN cells clear of every start — up to 20 in the manhattan metric the anchor measures in — so on arena, fortress and gold-rush there was no tree within reach of either start, the camp went up beside the Town Centre, and every load was carried fifteen cells.
- **Mutation:** `DROP_OFF_ANCHOR_RADIUS = 12`, the shipped value before this change.
- **Red:** three of the nine maps failed by name — `Arena: no woodline within 12 of the start at (8,8) — the camp would go up beside the Town Centre and every load would be carried the whole way: expected null not to be null`, and the same for Fortress and Gold Rush. The constant case failed with `expected 12 to be greater than or equal to 20`.
- **Green after revert:** yes, 10 passed.

**Bound.** It reads the GENERATED SPAWNS of each map and checks where the AI AIMS the camp. It does not run a simulation, does not check where `findBuildPlacementNear` finally puts the building, and says nothing about a map outside `PLAYABLE_MAPS` or about the SECOND camp — there is none; `pickNextBuildTarget` asks for a lumber camp only in the Dark Age and only when the owner has none, so two of the four measured slots finish a match with zero camps and the carry creeps back from 4 to 9 cells as the near woodline is eaten.

**Why it enumerates a shared roster rather than a list of its own.** The list of maps that ship lived only in `setupScreen.ts`, where a simulation test has no business reading it. It moved to `mapGeneration/playableMaps.ts` so the gate walks the SAME list the player picks from — a hand-typed copy would have let the next map with a distant woodline through silently, which is exactly how three of them got here.

**Why the second case ties two constants together.** The pair was incompatible for as long as both existed, and neither file mentions the other. The gate derives its floor from `MIN_START_GAP`, so it goes red when EITHER constant moves — not only when a shipped map changes.

## An AI whose enemy has only garrisoned units left must still finish the base

- **Gate:** `tests/simulation/aiGarrisonedDefender.test.ts`, two cases on `ai-garrisoned-defender-fixture` and `-few-fixture` — run by `npm test`, and so by `npm run verify`.
- **Claim it carries, half one:** `findOwnedUnitOnMap` queries `('unit', 'position')`. A garrisoned unit is alive and keeps its `unit` component but `garrisonUnit` strips its position, and `setUnitAttackCommandDirect` returns false without a target position — a silent no-op that the validator never sees. The AI's attack phase prefers the target enemy's villager, so one hidden villager pinned an army idle for 30,000 ticks on `gold-rush`.
- **Mutation:** `for (const id of world.query('unit'))`, the shipped query before this change.
- **Red:** both cases. `owner 2 still holds 3 building(s) (town-center, house, barracks); 23 of 23 attackers idle` and `closest approach 21 cells; 4 of 4 attackers idle`.
- **Claim it carries, half two:** the attack-group threshold does not apply to an enemy with NO UNITS ON THE MAP. `shouldPush` gates every fallback in the attack phase, the v0.3.197 last-resort branch included, so an army below the threshold never marched on an opponent that had nothing left to be caught by.
- **Mutation:** `const shouldPush = state.attackGroup.length >= threshold;` — the exception removed.
- **Red:** the `-few` case only: `four attackers against a Castle-age threshold of seven never reached the enemy: closest approach 21 cells; owner 2 still holds 3 building(s); 4 of 4 attackers idle`.
- **Green after revert:** yes, 2 passed; the full arm resolves by about tick 4,000 against an 8,000-tick window, and the few arm's closest approach is 1 against a bound of 4.

**Bound.** One map, one age, two attacker sizes, 8,000 ticks, three enemy buildings. It proves the attacker does not STALL; not that it wins, picks a good building, breaches a wall, or copes with a defender that shoots back with more than a Town Centre.

**Half two was found by a CRITIC, and the first fixture could not have caught it.** With resources the four attackers trained their way back over the threshold within two thousand ticks — measured, four became eleven — so the case never arose. The `-few` arm is therefore broke on purpose: zero of every resource, on a bare grass map with nothing to gather.

**It asks the few arm whether the army MARCHED, not whether it won.** Four Men-at-Arms lose to a Town Centre's arrows and cannot be replaced, so a resolution assertion would demand what the composition cannot do. The measure is the closest any attacker ever gets to the enemy Town Centre: 1 cell with the fix, 21 without — and the attackers spawn at their OWN base rather than the enemy's doorstep, because an army that starts beside its target makes "did it march?" unanswerable.

**The escapes it closes, one of which two earlier versions left open.**
- The assertion is the match OUTCOME, not "a building fell". The first version asked for one building to fall and PASSED WITH THE DEFECT FULLY PRESENT — the attackers engage during the 300 ticks the villagers spend walking to the door, and a house fell before the army went idle for the remaining 3,000 ticks.
- The defenders must be off the map AND STILL THERE, or the attack phase falls through for the right answer for the wrong reason. The second version checked `bridge.world.isAlive(id)`, and **that check was unsound**: `EntityManager.isAlive` takes a bare id with no generation, and the engine recycles ids from a free list — a critic reproduced `alive: false,true` in this very fixture for a villager that had been killed and whose id was reused. It now takes an `EntityRef` at garrison time and asks `world.isCurrent(ref)`, and independently asserts owner 2's POPULATION still counts the two, which is what "garrisoned" means and what no recycled id can fake.

## A test cannot ask whether an entity survived with an id that may have been recycled

- **Gate:** the `no-restricted-syntax` rule for `tests/**/*.ts` in `eslint.config.js` — run by `npm run lint`, and so by `npm run verify`.
- **Claim it carries:** `world.isAlive(id)` takes a bare entity id with no generation, and `../civ-engine/src/entity-manager.ts` recycles ids from a free list. An entity that DIED and whose id was reused reads back as alive, and it carries the same components, so a follow-up `getComponent` check does not catch it either. A test is exactly where that bites, because a test captures an id, steps the simulation, and then asks whether the thing it captured survived. Production code is deliberately NOT restricted: its 27 call sites take the id from a live query and ask in the same tick, where there is no time for a recycle.
- **Mutation:** `expect(villagerIds.every((id) => bridge.world.isAlive(id)), ...)` in `tests/simulation/aiGarrisonedDefender.test.ts` — the exact check a critic proved unsound on 2026-09-04, restored.
- **Red:** `error  world.isAlive(id) cannot tell a recycled id from a surviving entity. Take a ref BEFORE the step — world.getEntityRef(id) — and ask world.isCurrent(ref) after it; the ref carries the generation  no-restricted-syntax`, and lint exits 1.
- **Green after revert:** yes, lint exits 0.

**Bound.** It is a syntactic rule on the NAME `isAlive` in the tests tree. It does not reach production code, where the same call is legitimate; it does not catch the same mistake spelled another way (a raw `entityManager` reach-through, say); and it says nothing about whether a test that DOES use a ref took it early enough to mean anything. What it covers is the whole class of the defect that was actually found: a test asserting survival across a step with an identity check that cannot tell two entities apart.

**Why the boundary is the tests tree and not the call.** The honest rule would be "do not hold an id across a step and then ask", and that is not lintable. The tests tree is where holding an id across a step is the normal shape, so restricting the call there catches the class without a false positive on the 27 production sites — one of which was checked and is correct. The single pre-existing test use, in `rightClickNeverGarrisons.test.ts`, was the same latent defect and was rewritten to take a ref before the step; it had been asserting "the unit is still on the field" in a way a recycled id would have satisfied.

## A gather ranking must measure the distance the units actually walk

- **Gate:** `tests/simulation/gatherWalkRanking.test.ts` :: `never sends a wood villager to a tree far beyond the best one it could walk to` — run by `npm test`, and so by `npm run verify`. Beneath it, `tests/simulation/villagerGatherAssignment.test.ts` ("the comparator ranks by the walk a villager can make", two cases) pins what the comparator does with a haul, and `tests/simulation/dropOffWalkField.test.ts` (eleven cases) pins the field that supplies it.
- **Claim it carries:** the comparator ranked candidates by Manhattan distance to a drop-off while movement is strictly 4-connected, and a forest is a dense block of impassable cells; so wood villagers were sent to trees that read as 5-9 cells away and were a walk of 33-45, past trees at a walk of 0-3 (register entry 2026-09-01). The boot-map gate measures every wood villager's haul with `walkDistanceProbe.ts` — the PROBE is rebuilt from components and shares no symbol with the ranking; the gate itself reads one constant from the code under test, the idle spread cap, which is a parameter of the rules rather than the ranking — and fails when any target is more than 6 cells beyond the best tree the ranking would have preferred by its own rules.
- **Mutation 1 — the defect itself:** the pristine tree at `46a4ba8b` (Manhattan key and Manhattan home range), with this test file copied in unchanged.
- **Red:** `wood villagers walk further than the ranking's own rules allow:` — at tick 4,500 owner 1's villagers 2236 and 2484 on tree 2181 at haul 33 (28 beyond the best available), at tick 6,000 villagers 2207 and 2484 on tree 2182 at haul 34 (29 beyond). Those are the register's numbers, reproduced by the gate.
- **Mutation 2 — the haul key removed:** `if (rankByHaul && left.haul !== right.haul)` made `if (rankByHaul && false)` in a scratch copy of the candidate. Red: `takes the tree with the shorter WALK over the one with the shorter Manhattan distance` — `expected 60 to be 61`, the wall tree chosen on villager distance.
- **Mutation 3 — the home range unbounded:** `candidate.haul <= HOME_GATHER_RANGE` made `true`. Red: `home is a WALK: a sealed tree at Manhattan 8 is not home, and a 30-cell haul is not either` — `expected 63 to be 62`, the villager sent on the 30-cell haul.
- **Mutation 4 — the field answers Manhattan distance:** `haulDistance` in a scratch copy of `dropOffWalkField.ts` made to return the Manhattan distance to the nearest drop-off footprint, everything else intact (a critic asked for this one: it is a different function from the pristine key, so mutation 1 did not cover it). Red: nine misses across the three samples, `t3000 o1 villager 2207 -> tree 2188 haul 33 (28 beyond the best available)` and the same shape at 4,500 and 6,000.
- **Green after revert:** yes — 12 passed (unit), 1 passed in 13.6s (boot map).

**Bound.** ONE map (`aoe2-prototype`), both seats AI, THREE sample ticks (3,000, 4,500, 6,000), WOOD only. The probe treats a finished gate as a wall, which does not matter on this map because it has none; on Arena or Fortress it would over-state a haul that crosses the owner's own gate and could fail a correct ranking. A miss is judged against trees of the same ownership tier or better, so the gate deliberately says nothing about what the spec's own-base-first preference costs in walking — measured at 3 to 6 cells on this map and recorded in the register as the next lever, not a defect. It says nothing about food, gold or stone, which sit in the open where Manhattan and walk agree. And it says nothing about COST: ticks per second is measured by `scripts/aiGatherWalk.mjs` and reported in the register, not gated.

**A near-miss in the unit gate, kept because it is the lesson.** The first "shorter WALK" case put the wall tree at a 30-cell haul, and mutation 2 passed it: the walk-bounded home range had excluded the wall tree before the ranking key was ever consulted, so the case was green for the filter's reason and certified nothing about the key. Both hauls now sit inside the 24-cell range so only the key can decide, and the mutation went red. A test that passes for a reason other than the one in its name is a false green with a true name.

## A resolved match proves an endgame only when one side is gone

- **Gate:** `tests/simulation/villagersAvoidEnemyDefencesSelfPlay.test.ts` :: `loses no more than a handful of villagers inside enemy Town Centre, tower or Castle reach on the boot map` — run by `npm test`, and so by `npm run verify`.
- **Claim it carries:** the deaths-under-defences bar is measured over a window that contains the endgame. The census reaching the 45,000-tick horizon proves that; so does a CONQUEST, read from the survivors (exactly one owner left with any unit or building), which cannot happen without armies at each other's bases. Any other early resolution — Wonder, Relic, score, draw, or a bug — leaves both sides standing and fails.
- **Mutation:** in a copy of the spec, `conquest: false` in place of `outcome !== 'running' && standing.size === 1` — the world where a resolution can never be proved to be a conquest, which is what the old `lastTick >= 40_000` assumed of every resolution.
- **Red:** `AssertionError: the census covered only 39812 ticks (outcome victory, both sides standing) — short of the horizon and not a conquest, so the endgame this bar was measured over may never have happened: re-derive the bar rather than lowering the horizon.: expected false to be true` — 1 failed.
- **Green after revert:** yes; the unmutated spec passed in the same tree (censused to tick 39,812, outcome victory, owner 2: 30 villager deaths, 0 under enemy defences), 139.6 s.
- **Bound.** ONE seed, ONE horizon, the boot map only. `standing` reads `getEconomyState()` units and buildings; an owner alive only through a relic or a wildlife-owned entity is not counted as standing, which matches how the corpus reads a living owner. The mutation proves the conquest branch is load-bearing; it does not prove the bar itself (that red-check is the 2026-09-02 entry, rule OFF -> 43 deaths).

## A coverage floor fails by the name of what self-play stopped reaching

- **Gate:** `tests/simulation/selfPlayContentCoverage.test.ts` :: `exercises every building, unit and technology on the floor, and prints the gap to the content tables` — run by `npm test`, and so by `npm run verify`.
- **Claim it carries:** under the no-attack lab (attacks off, high starting resources, hard AI, 45,000 ticks on the boot map) self-play exercises at least the 15 building types, 18 unit types and 31 technologies named in the floor, and both seats reach the Imperial Age. A type the AI stops reaching fails by its name.
- **Mutation:** in a copy of the spec, `'university'` added to the building floor — a building the AI never builds in this lab (it is in the printed gap).
- **Red:** `AssertionError: Buildings on the coverage floor were never exercised: university. Self-play stopped reaching them. Restore the AI's reach, or edit them out of the floor with the reason in the commit — never by making this a count.: expected [ 'university' ] to deeply equal []` — 1 failed.
- **Green after revert:** yes; the unmutated spec passed in the same tree.
- **Bound.** ONE seed, ONE configuration, ONE horizon, and a deterministic simulation, so the floor is exact on this tree and any AI decision change can move it. The gate proves a loss is SEEN; it forbids nothing — a legitimate loss is edited out of the floor with its reason in the commit. It says nothing about what the AI does under attack (`aiReachesCastleAge.test.ts`), and the printed gap is not asserted.

## A resolved match is scored for the side that won it

- **Gate:** `tests/simulation/aiReachesCastleAge.test.ts` :: `becomes eligible for the Castle Age on the map the game boots` — run by `npm test`, and so by `npm run verify`.
- **Claim it carries:** the loop stops when the boot map resolves and scores the side that WON — owner 1 on `victory`, owner 2 on `defeat` — and the earliest to Castle only while the match is still running; on that side it asserts qualification, the Castle Age, a peak army of at least 10, and a NAMED union floor over both seats (11 building types, 6 unit types) for the shipped configuration.
- **Mutation:** in a copy of the spec, the winner rule inverted — `victory` scores owner 2 and `defeat` scores owner 1 — the world where a resolved match is scored for the side that lost it, which is what "earliest to Castle" alone would have done here only by luck (owner 1 is both).
- **Red:** `AssertionError: the boot map never reached the Castle Age within the horizon: expected null not to be null` — the loser never left Feudal; 1 failed.
- **Green after revert:** yes; the unmutated spec passed in the same tree (victory at 39,812; owner 1 Castle 21,250, peak 15; union 11 and 6), 137.8 s.
- **Bound.** ONE seed, ONE horizon. The mutation proves the winner rule is load-bearing on a resolved match; the union floor's own red-check is the same class as the coverage gate's (`'university'`, below) and was not repeated here.

## A probe names the engine debug tool that answers its question, or it does not run

- **Gate:** `scripts/hooks/probe-harness-check.mjs`, a PreToolUse hook in `.claude/settings.json` on `Write|Edit|Bash` — the harness runs it before the tool, not a test in `npm run verify`.
- **Claim it carries:** every scratch probe under `tmp/probes/` carries a `// harness:` line naming the engine tool that answers its question (`replay:inspect` on a recorded bundle, `diffBundles` / `SessionReplayer.forkAt` for an A/B, the path-queue and occupancy probes for a jam) or `none` with the reason; without it the probe is neither written nor run. The rule is in AGENTS.md ("Debugging"); this is what makes it fire at the moment of the decision rather than at session start, where it was read and skipped twice.
- **Mutation:** none needed — the defect was live. The first command run after the hook was installed mentioned `npx tsx tmp/probes/contentCensus.mjs`, and that probe, written that morning, had no such line.
- **Red:** `PreToolUse:Bash hook error: tmp/probes/contentCensus.mjs: is about to run and has no `// harness:` line` followed by the tool list and `See AGENTS.md, Debugging.` — the command was refused.
- **Green after the fix:** the line was added to the probe and the same command class passes; piped cases: a Write without the line exits 2, a Write with it exits 0, a Bash that only greps a probe exits 0, a Write outside `tmp/probes/` exits 0, unparseable stdin exits 0.
- **Bound.** Paths under `tmp/probes/` with `.mjs`, `.ts`, `.js` or `.py`, and Bash commands whose text runs one through `node`, `tsx`, `npx tsx` or `python`. A probe placed anywhere else, or run through a shell script, is not seen; the run pattern is matched anywhere in the command text, so a command that merely quotes such a run is checked too (it is refused only if the file exists and lacks the line). It checks that the question was answered, not that the answer is right.

## The defect register rolls over, and a live defect never rolls over

- **Gate:** `tests/architecture/defectRegisterRollover.test.ts` :: nine tests over the same six numbered checks the file's header describes — an instrument check, the closed cap, the overflow message's ordering, the overflow message's pinned-entry skip, a real-register property case, the archive's live-defect refusal, the ambiguous-marker refusal, the duplicate check and the pointer check — run by `npm test`, and so by `npm run verify`. The heading language and the rollover plan live in `tests/architecture/helpers/defectRegisterHeadings.ts`; the checks that read the two files stay in the test. Register-driven: the rule it enforces is `docs/policies/local-rules.md`, "The defect register rolls over, and a live defect never rolls over", so no `lessons.md` entry is retired by it. (Six tests until 2026-09-06, when the three message cases were added.)
- **Claim it carries:** `docs/learning/defect-register.md` holds at most 12 CLOSED entries; every OPEN one stays whatever its age; nothing that says the defect is still live sits in `docs/learning/defect-register-past.md`; no entry is in both files or twice in one; and each half's header names the other with a path that resolves on disk with its exact casing. OPEN is read case-insensitively from the heading's LAST balanced parenthesised group, so `Villagers latch on an OPEN cell when buildings move (FIXED)` is closed while `(owner-reported, OPEN)` is open; a marker that says open, unfixed or reopened in a spelling the parser cannot read is AMBIGUOUS and fails, never closed.
- **Claim added 2026-09-06 — the message names the entry that should actually move.** The overflow message walks closed entries OLDEST-FIRST, and because the register is written newest-first, the oldest entry of a shared date is the BOTTOM-most one, so same-date ties break by DESCENDING line number. An entry the archive check would refuse (`mayBeArchived` false, `(PROVEN, fix measured and NOT shipped)` being the standing example) is marked `PINNED, leave it where it is` with the reason and the walk continues to the next archivable entry, so following the message can never turn the archive check — (3) in the file's own numbering — red. When nothing archivable is left the list still comes back non-empty and says the cap is unreachable, because an empty list would pass a check whose cap is still exceeded. What the cap check ENFORCES is unchanged — it counts closed entries and fails at 13 — and so is its exit behaviour; only the naming changed.
- **Mutation:** five, each reintroducing one defect, applied by `tmp/probes/mutate.mjs` through `tmp/probes/runMutation.sh`, which restores all three files afterwards. (a) `archived-open-strict` — an OPEN entry moved verbatim into the archive, which is the 2026-09-05 defect itself. (b) `archived-open` — the same entry archived with its marker rewritten to `(UNFIXED)`, the unsafe spelling. (c) `bypass1` — eight extra closed entries appended to the active file with their headings indented one space, which CommonMark renders as headings and the strict `## ` parser cannot see, so the file really holds 20 closed against a cap of 12. (d) `f4-cross-file` — one entry copied into the archive with a curly apostrophe and a trailing space, the copy a rollover leaves behind. (e) `f8-wrong-case` — the header's pointer lower-cased to `defectregisterrollover.test.ts`, which Windows resolves and Linux CI does not. Plus `f1-floor-only`, nine closed entries moved out so the active file reads 11, for the instrument floor.
- **Red:** every one, gate exit 1 (2026-09-05, on the tree this commit ships). (a) `live defects buried in the archive … docs/learning/defect-register-past.md:1038 — status (OPEN) — OPEN — 2026-09-05 — A green browser-suite run can leave a headless Chrome burning cores for hours (OPEN)`, 1 failed of 6. (b) two failures at once — the ambiguous-marker check on `status (UNFIXED)`, and the instrument check, `pinned as a real heading but no entry in either file has this exact text`, because the pins are verbatim copies and rewriting a heading strands its pin. (c) `the two heading parsers disagree, so every count below this line would be a number about the parser rather than about the register`, naming all eight indented lines with their text — the cap check never runs, which is the point. (d) `the same entry appears more than once … docs/learning/defect-register.md:79` beside `defect-register-past.md:1038`, the two differing only in the apostrophe. (e) `points at \`tests/architecture/defectregisterrollover.test.ts\`, whose real path is \`tests/architecture/defectRegisterRollover.test.ts\``. (f) `docs/learning/defect-register.md: 11 entries parsed, at least 12 exist (the cap check reads THIS file)`.
- **Green after revert:** yes — 6 passed in 19 ms on the restored tree, and the three files' SHA-256 hashes matched their pre-mutation values exactly, so the greens are on the same bytes the reds were taken against.
- **Mutation (2026-09-06, the two the message's own cases stand for):** applied to `rolloverMessage` in `tests/architecture/helpers/defectRegisterHeadings.ts`, not to the register. (g) `tie-break-reversed` — the same-date comparator flipped back to ascending line number, `a.line - b.line`, which is what the message did until this commit: in a newest-first file that names the entry written most recently on that date instead of the one that has to move. (h) `no-archivable-filter` — the `mayBeArchived` branch removed so every entry is named MOVE, which is what let the message point at `(PROVEN, fix measured and NOT shipped)`, an entry the archive check refuses.
- **Red (2026-09-06):** (g) 1 failed of 9 — the ordering case's diff names `SYNTHETIC newer of 09-05` where `SYNTHETIC oldest of them all` was expected. (h) 2 failed of 9 — the pinned-skip case, and the real-register case reporting `the cap check named an entry that check (3) would then refuse in the archive: ## 2026-09-03 — The AI's build order stalls on the first thing it cannot afford, so no seed has ever built a Siege Workshop (PROVEN, fix measured and NOT shipped): expected false to be true`.
- **Green after revert (2026-09-06):** yes — 9 passed on the restored helper.
- **Bound on the two new cases.** The tie-break DIRECTION is proved only by the synthetic case. The real-register case cannot see it: one entry over the cap, both the entry an ascending tie-break would name and the one a descending tie-break names are archivable today, so it stays green under (g) and checks only the property it claims — that a named MOVE is a move the archive would accept. The synthetic cases pin the rule and the real-file case pins the property; neither checks that the register is actually filed newest-first, which the ordering ASSUMES.
- **Bound.** It reads HEADINGS. Nothing here reads an entry's BODY, so an entry moved with its wording mangled, summarised or truncated passes — the only record of what the text was is git history, and the gate deliberately does not shell out to it. Nothing checks the FILE's order, and nothing requires the twelve kept entries to be the twelve NEWEST: keeping any twelve closed passes, so "oldest first" is a convention of the file, not a check on it. Since 2026-09-06 the MESSAGE's order is checked, which is a different claim — it says the reader is told to move the right entry, on the assumption that the file is filed newest-first; a register shuffled out of that order is named in the wrong order and still passes. Deleting a closed entry outright instead of archiving it passes every check here, and the three floors (12 active, 20 archived, 50 total) only ever become more slack because entries are never deleted. The status marker is a text convention, so a marker describing a live defect in words none of the patterns carry still reads as closed. The archive-only check is deliberate: an unfixed-sounding marker in the ACTIVE file is in front of the reader and is allowed, and the gate catches it at the moment it is buried rather than before. The pointer check sees each file's header region only — every line above its first heading — and pointers inside entry bodies are not checked at all.

## A unit or technology price or time that differs from the stats files fails by name, unless an exception names it and pins it to Definitive Edition

- **Gate:** `tests/content/trainAndResearchCostsAndTimes.test.ts`, 8 cases, run by `npm test` and so by `npm run verify`. Register-driven (register entry 2026-09-23, "The game charged the wrong price or time on 38 units and technologies"): found by an audit, fixed and gated in one task, so no `lessons.md` entry is retired by it.
- **Claim it carries:** every `technologies.csv` row and every trainable `units.csv` row is charged `researchCost` / `trainingCost` and takes `researchTimeTicks` / `trainingTimeTicks` exactly as the CSV says (seconds x TPS), or exactly as a named exception says. Each exception records both the CSV's value and the game's, cites the DE dataset pinned at `3bb43b14`, and goes red when the CSV row changes under it (STALE) or comes to say what the game charges (DISSOLVED). Every CSV row and every game entry is accounted for in both directions, and a second parser (two regexes per raw line) must agree with `scripts/content-lib.mjs` on every price and time first. The expected values come from the CSV, never from the tables under test: the content pipelines already prove the charge equals the table, and with the Onager upgrade put back in wood `contentPipelineResearch.test.ts` stays green (exit 0, 51 s).
- **Mutation:** seven, each applied by `tmp/prices/mutations.mjs` to one file and restored byte for byte (SHA-256 checked after each), on the tree this commit ships. (a) `onager-upgrade` back to 800 food + 500 wood. (b) the Skirmisher's 25 food + 35 wood swapped back. (c) `paladin-upgrade` back to 600 ticks. (d) `longbowman` back to 300 ticks, a unit whose time is a named exception. (e) the Champion row of `technologies.csv` edited to DE's 650 food + 350 gold and 70 s, which is what a re-source does. (f) the Iron Casting row edited to 65 s, a value neither side has. (g) a comma instead of a semicolon inside the Onager's cost blob.
- **Red:** every one, gate exit 1. (a) `Onager (onager-upgrade): charges 800 food + 500 wood, technologies.csv says 800 food + 500 gold`. (b) `Skirmisher (skirmisher): charges 35 food + 25 wood, units.csv says 25 food + 35 wood`. (c) `Paladin (paladin-upgrade): 600 ticks (60 s), technologies.csv says 170 s`. (d) `Longbowman (longbowman): 300 ticks (30 s), its named exception says 18 s`, so an exception pins the game to DE and does not excuse it. (e) `champion-upgrade cost: DISSOLVED — the CSV now says what the game charges (650 food + 350 gold); delete the exception` and the same for its time. (f) `iron-casting time: STALE — the exception records the CSV as 70 s, the CSV now says 65 s; re-read the row`. (g) three cases at once: `design/stats/technologies.csv Onager: parsed no cost at all`, `build_time is null`, and `content-lib read {} null s; the raw line reads {"Food":800,"Gold":500} 75 s`.
- **Green after revert:** yes, 8 passed on the restored tree.
- **Bound.** Base prices and times only, before any civilization, technology or team modifier. It sees Definitive Edition only through the named exceptions: a row where the game and the CSV agree and DE differs passes, and when the gate landed there were 97 such rows, 117 fields in all. Six game technologies with no CSV row and the CSV rows nobody trains are compared with nothing. Spies is compared for its per-enemy-villager price only. The second parser reads a cost blob only when no braces nest inside it, which excludes the Gaia and starting-unit rows; those are not compared anyway.
- **Amended 2026-09-23 (v0.3.228), the re-source.** Both stats files' cost and time columns were re-sourced from the same pinned dataset, and the thirteen exceptions did what they were built to do: run on the re-sourced files before anything else moved, the gate went red with all thirteen fields reported as `DISSOLVED — the CSV now says what the game charges`. The exceptions and their mechanism were then deleted, so the claim is now a plain equality: every row is charged and timed exactly as its CSV says. The STALE, DISSOLVED and MALFORMED checks went with the mechanism, and their proofs above describe code that is no longer in the file.
- **Mutation, re-proved on the re-sourced tree (`tmp/prices/mutations-s2.mjs`, each file restored byte for byte):** (h) the Militia back at the Conquerors-era 60 food; (i) Coinage back at 50 s, with its checked `<n> s x <rate> TPS` comment moved to match so that only this gate can see it; (j) the Crossbowman row of `technologies.csv` put back to 125 food + 75 gold while the game keeps DE's price.
- **Red:** every one, gate exit 1. (h) `Militia (militia): charges 60 food + 20 gold, units.csv says 50 food + 20 gold`. (i) `Coinage (coinage): 500 ticks (50 s), technologies.csv says 70 s`. (j) `Crossbowman (crossbowman-upgrade): charges 175 food + 100 gold, technologies.csv says 125 food + 75 gold`, which shows that a stale CSV fails the gate too, not only a stale table.
- **Bound, restated.** The gate compares the game with the CSV and never with live DE: the CSV is DE only as of the pin in its header, so a later DE patch changes nothing here until the files are re-sourced.

## Every workflow ci:status watches runs on every push to main

- **Gate:** `tests/scripts/ciTriggers.test.ts`, 3 cases, and the case `calls a docs-only commit with no run UNGATED under the current, unfiltered workflows` in `tests/scripts/ciStatus.test.ts`, run by `npm test` and so by `npm run verify`. Register-driven (register entry 2026-09-24, "Three parts of the verification plumbing said "checked" or "green" about work they never checked"), so no `lessons.md` entry is retired by it.
- **Claim it carries:** for every workflow name in `WATCHED` in `scripts/ci-status.mjs`, exactly one workflow file has that name, and its `on.push` holds only `branches`, which include main. The file is read with js-yaml, not with the script's `parsePushTrigger`, so the check and the script cannot share a parser bug. The ciStatus case holds the same widening from the reporting side: a docs-only commit with no run, judged against today's workflows, is UNGATED.
- **Mutation:** a block-style `paths:` list holding `- 'package.json'` put back under `on.push` in `ci.yml`, and a block-style `paths-ignore:` list holding `- 'docs/**'` put under it in `playtest.yml`, on the tree this commit ships; both files restored and compared byte for byte afterwards.
- **Red:** 3 failed, 11 passed across the two files: `CI: push to main, with no path or branch filter beyond main`, `playtest-corpus: push to main, …`, and the ciStatus UNGATED case, which read NEVER ASKED.
- **Green after revert:** yes, 14 passed.
- **Added after review, same day:** `parsePushTrigger` now refuses a push trigger with any key but `branches` and `paths`, because it would otherwise read `paths-ignore`, `tags` or `branches-ignore` as "no filter" and `ci:status` would say "runs on every push to main" about a filtered workflow. Three cases in the frozen-fixture describe of `ciStatus.test.ts` hold it; with the refusal line cut, all three went red (`expected { name: 'CI', parsed: true, …(2) } to deeply equal { name: 'CI', parsed: false }`).
- **Bound.** It proves each watched workflow ASKS for a run on every push to main, not that GitHub made one; a `[skip ci]` message or an outage still leaves a commit without a run, which `ci:status` reports as UNGATED. Pull-request and schedule triggers are not checked. The unwatched nightly LLM workflow was not checked either, and was deleted on 2026-09-25.

## A nightly LLM playtest that did not play reads red (gate retired 2026-09-25)

- **Retired 2026-09-25, with what it guarded.** The owner retired the nightly (*"no API key, no nightly playtest"*), and this gate was deleted in the same commit as `playtest-llm.yml` and `scripts/playtest-corpus-llm.mjs`, the two files it held (spec §15.7). The proof below stays as the record of what it caught.
- **Gate:** `tests/scripts/playtestCorpusLlmNotRun.test.ts`, 2 cases, run by `npm test` and so by `npm run verify`. Register-driven (the same 2026-09-24 entry).
- **Claim it carries:** `scripts/playtest-corpus-llm.mjs`, run for real under tsx with `ANTHROPIC_API_KEY` empty and a PATH holding only System32 on Windows or one fresh empty directory elsewhere, exits 2 and prints `NOT RUN` and `ANTHROPIC_API_KEY` on stderr. Node needs no PATH entry, because tsx starts its child through `process.execPath`, and leaving node's directory off keeps a globally installed `claude` shim there out of reach. The test first proves no `claude` file is on that PATH, so it cannot start a paid playtest. Every `actions/upload-artifact` step in `playtest-llm.yml` has `if-no-files-found: error`, and neither a job nor a playtest or upload step has `continue-on-error`, nor a playtest step an `|| true`.
- **Mutation:** applied by `tmp/mutate.cjs`, each file restored byte for byte (SHA-256 checked). (a) the script and the workflow put back as shipped at `5309eeaf`; (b) only `process.exit(NOT_RUN_EXIT)` changed to `process.exit(0)`; (c) `continue-on-error: true` added to the corpus step; (d) `|| true` added to its `run:` line.
- **Red:** (a) 2 failed: `expected '' to contain 'NOT RUN'` (the old script printed its skip on stdout and exited 0) and `a night that uploads nothing played nothing and must fail: expected undefined to be 'error'`. (b) `expected +0 to be 2`. (c) `a playtest or upload step has continue-on-error: expected true to be undefined`. (d) ``a playtest step swallows its exit code with `|| true`: expected 'npm run playtest:corpus-llm || true' not to match``.
- **Green after revert:** yes, 2 passed.
- **Bound.** Only the no-provider path is exercised; a run with a provider, and the workflow as GitHub runs it, are not. The workflow rules are read as YAML, so a swallowed exit code written some other way (a wrapper script, `set +e`) is not seen.

## The voxel build step finds voxel through node_modules, from any working directory

- **Gate:** `tests/scripts/voxelBuildIfStale.test.ts`, 3 cases, run by `npm test` and so by `npm run verify`. Register-driven (the same 2026-09-24 entry).
- **Claim it carries:** `scripts/voxel-build-if-stale.mjs --print-root` prints the real path behind `<script dir>/../node_modules/voxel`, whatever the working directory. A copy of the script runs in a worktree-shaped sandbox (its `node_modules` a junction to another checkout's, whose `voxel` is a junction to a third place) from a directory with a DECOY `../voxel` beside it. With no link it exits 1 and names the path it looked at. From the OS temp directory, the real script prints this checkout's own link target.
- **Mutation:** `const voxelRoot = realpathSync(voxelLink);` changed back to `const voxelRoot = resolve(process.cwd(), '..', 'voxel');`, the shipped line.
- **Red:** 2 failed, 1 passed: the sandbox case received `…\voxel-root-…\elsewhere\voxel` (the decoy) for `…\packages\voxel`, and the real-checkout case received `c:\users\38909\appdata\local\voxel` for `c:\users\38909\documents\github\voxel`. The no-link case stays green under this mutant because the existence check before it still fails first.
- **Green after revert:** yes, 3 passed.
- **Bound.** `--print-root` stops after resolving, so the staleness check and the build lock are not exercised. A checkout whose `node_modules` is a copy rather than a link is not exercised.

## ci:status reads every attempt and every run of a commit, and a red one before a green one reads red

- **Gate:** `tests/scripts/ciStatusReruns.test.ts`, 15 cases in two describes, with the harness in `tests/scripts/helpers/ciStatusHarness.ts`, run by `npm test` and so by `npm run verify`. Register-driven (register entry 2026-09-24, "ci:status called a run green when its first attempt had failed and a re-run passed"), so no `lessons.md` entry is retired by it.
- **Claim it carries:** a commit's own run whose earlier attempt failed and whose newest attempt passed reads GREEN ONLY ON A RE-RUN, names the earliest failing attempt's jobs and failing step, and exits 1. A failed attempt followed by a re-run that could not start reads RED, on the commit's line and on the history's. A later run of the workflow on the same commit that passed after one that failed reads GREEN ONLY ON A LATER RUN and exits 1, and a known red reads RED while a re-run or a later run is still going. An older run whose re-run could not start, or is still going, is read by the attempt before it. A pull-request run on the commit is not read. An earlier attempt or run in which no job got a runner reads as the account, not a flake, and two such runs on one commit stay COULD NOT RUN. An earlier attempt that cannot be read is unknown and red. An earlier attempt whose failed jobs were all cancelled is red and not called a flake. A run that passed on its first attempt stays GREEN, and the history line names the window's runs that passed only on a re-run. The fixtures were captured from the live repo on 2026-09-24 (run 36019109919 on `221329b4`, run 34079856007 on `143ad511`, run 33729682386 on `6c990f1a`); the cases built from captured parts say so. The fake `gh` refuses to hand back a fixture field that is not a projection key of the script's request.
- **Mutation:** sixteen runs, each applied to the tree this commit ships and restored byte for byte after. M-A: the red branch for an earlier failed attempt replaced by `if (false)`. M-B: `attempt: .run_attempt` dropped from the jq. M-C: the no-runner check replaced by `if (false)`, so an account block reads as a failure. M-D: the history's re-run note removed. M-E: an attempt whose jobs cannot be read skipped instead of reported. M-F: the commit line's check for a failed attempt before a re-run that could not start replaced by `if (false)`. M-G: the older runs on the commit not read. M-H: the history's check for a failed attempt before a re-run that could not start replaced by `if (false)`. M-I: the cancelled-only wording never chosen. M-J: `conclusion` dropped from the jobs jq. M-K: a run still going reads not red after a failed attempt. M-L: the older runs not read while the newest is still going. M-M: an older run whose re-run could not start skipped. M-N: pull-request runs read. M-O: an older run being re-run in place skipped. And a control for M-J: the same drop with the harness's field check put back to the word match it had before (taken from `9d7a5dd9`).
- **Red:** M-A through M-O all red. M-A: 3 cases. M-B: 17 of 30 across the two files, because the fake `gh` refuses the old projection. M-C: 2. M-G: 4. M-J: 12. Every other mutant: exactly the case written for it. The control stayed GREEN, which is why the field check was tightened.
- **Green after revert:** yes, 66 passed across `tests/scripts`.
- **Bound. Fixtures are not the API,** as for the ci:status gate above: the suite proves the reading of the projections the script asks for and the exit codes, not what GitHub returns today. Only the two watched workflows, and only attempts and runs GitHub still lists. It says that a commit's gate was flaky, not which spec is flaky across runs.

## A quick double-click on a stacked unit selects every unit of its type, whatever was selected before

- **Gate:** `tests/input/voxelSelectionController.test.ts` :: "two quick clicks on a stacked unit that is already selected" (4 cases), run by `npm test`, and `tests/browser/de-command-surface.spec.ts` :: "double-clicking a stacked villager that is already selected selects every villager on screen", run by `npm run test:browser`; both in `npm run verify`. Register-driven (register entry 2026-09-24, "A quick double-click on a stacked unit that was already selected walked the stack"), so no `lessons.md` entry is retired by it.
- **Claim it carries:** every pointer click leaves as the double-click candidate the type of the player's own unit (or owned sheep) on top at the cursor, whether or not it walked the stack, and a click on anything else leaves none. So a pair selects every unit of that type on screen after a single click on the unit and after a slow walk to what stands behind it, a click on something else between two clicks breaks the pair, and a second click 400 ms after the first still walks. The unit cases hand the controller the fixture's villager in front of its Town Centre; the browser case clicks the boot scenario's stacked villager once at 1280x720, then double-clicks it with `mouse.dblclick`.
- **Mutation:** the controller file swapped for two earlier versions and restored byte for byte after. The original (`origin/main` before the fix): 3 of 10 unit cases red, the three new rules. The first fix, which took the candidate from the unit the click walked from: 1 red, the walk-back case (`expected … to match object { selectedCount: 3, … }`). M-b3, keeping the earlier candidate when the unit on top is not the player's: 1 red, the break case. The browser case, on a build that cleared the candidate on every walk (the original behaviour for this gesture): `Expected: 3`, `Received: 1` at the `selectedCount` assertion, with the type assertion before it passing because the second walk came back round to the villager. Two hand-written mutants that tested "walked" as "the target is not the unit on top" stayed green on the walk-back case; they were unfaithful, because a walk back lands on the unit on top, so the file versions above are the record.
- **Green after revert:** yes, 10 passed in the unit file. The browser case passed 3 of 3 on the GPU and 3 of 3 on SwiftShader with the first fix, beside the fresh-double-click case, and passes in the full suite with this one.
- **Bound.** The controller with a stack handed in, and one stacked villager on one scenario at one viewport on the real page. A double-click on a building is not a gesture here, so neither gate asks for one.

## A hit test draws only a snapshot that is waiting, and a search for a click point reads the stack instead of clicking

- **Gate:** `tests/app/clickHitTestDrawsOnlyWhatWaits.test.ts`, 3 cases, run by `npm test`, and `tests/browser/game-selection-click.spec.ts` :: "only advances an overlapping exact-click stack at the same presented hit point", run by `npm run test:browser`; both in `npm run verify`. Register-driven (register entry 2026-09-24, "A browser spec on the stacked-entity click cycle failed on CI about one run in five"), so no `lessons.md` entry is retired by it.
- **Claim it carries:** a left click, a right click or a read of the click stack draws no frame when the latest snapshot is on screen, draws exactly one and then hit-tests when a snapshot is waiting, and selects and commands nothing when even a drawn frame leaves the hit state not ready. On the real page, reading the click stack at 53 points draws at most one frame, and after each click the selection panel names the bridge's selection by the second rendered frame, with the wall clock bounded at the five seconds the old check had.
- **Mutation:** M-v1: the unconditional draw put back in the view's hit test (`this.renderer.frame(...)` before every one, the shipped line). M-v2: the second readiness check removed, so a hit test reads a state that is still not ready. M-v3: the spec run against a build of M-v1. M-v4: the spec run against a build whose selection panel redraws on every fourth animation frame only.
- **Red:** M-v1: 1 of 3 (`expected "spy" to not be called at all, but actually been called 3 times`). M-v2: 1 of 3 (`expected true to be false`). M-v3: `reading the click stack at 53 points drew 53 frames: a hit test with nothing waiting to be drawn must draw nothing`, `Expected: <= 1`, `Received: 53`. M-v4: `two rendered frames after the sheep was selected, the selection panel did not name it`, `Expected: "Sheep"`, `Received: "House"`; each of its five panel checks has an even chance of landing on a redraw, so the spec misses this mutant about one run in 32, and the old five-second check passed it every time.
- **Green after revert:** yes, 3 of 3 in the unit file; the spec 60 of 60 on SwiftShader and 40 of 40 with the browser's GPU process held to four CPUs, with the moving-villager test beside it each time, and on CI 120 of 120 of each changed test over runs 36069113956 and 36069127456, with the real shard 3/4 at 53 of 53 in each.
- **Bound.** The unit gate mocks the renderer, so it holds the view's rule and nothing about WebGL. The browser half is one fixture, paused, at 800x600. A search over a running world draws one frame per tick it advances, which nothing measures. Whether a burst of frames starves the page on CI's runner is measured by repeated CI runs, not by any gate: no local arm reproduced the starvation.

## The sustained-raid spec spaces the words on the blow each one names, not on the tick the page drew it at

- **Gate:** `tests/browser/attack-warning-sustained.spec.ts` :: "a raid that keeps hitting keeps the warning up, and a pause does not take it down", run by `npm run test:browser`; `tests/ui/raidWarning.test.ts` :: "say ... once per window" and "sounds again for the first blow after the mark went dark, however far a frame stepped", and `tests/ui/alertToast.test.ts`, run by `npm test`; all in `npm run verify`. Register-driven (register entry 2026-09-24, "The sustained-raid warning spec stamped each toast with the tick of the frame that drew it"), so no `lessons.md` entry is retired by it.
- **Claim it carries:** each alert toast names the tick of the blow its horn answered (`data-hud-toast-hit-tick`). That tick is an integer, no later than the tick the page drew the toast at, and inside a frame across which the House lost health. Two named blows are at least 200 ticks apart, with no tolerance. In the unit harness the words carry the blows' own ticks (20, 220, 420, 620, 820), and across a frame that steps ten ticks they carry 20 and 222 while the page's clock reads 25 and 222.
- **Mutation:** MB1, the throttle's `return` removed from `gameAudioController.ts`. MB2, the attribute's line removed from `toast.ts`. MB3, the words naming `hit.tick - 2`. Each was built with vite, run once on SwiftShader, and its file restored byte for byte (checked with `cmp`). Unit: U2, `hit.tick + 1`, and U3, the attribute dropped.
- **Red:** MB1: "the words came faster than the horn is throttled", `Expected: >= 200`, `Received: 9`. MB2: "the words drawn at tick 4 carry no data-hud-toast-hit-tick". MB3: "the words drawn at tick 5 name tick 2, and the House's health went 900 -> 900 between ticks 0 and 3: no blow". U2: 2 of 9 red (`expected [ 21, 221, 421, 621, 821 ] to deeply equal [ 20, 220, 420, 620, 820 ]`, and `[ 21, 223 ]` against `[ 20, 222 ]`). U3: 1 of 1 red (`expected undefined to be '204'`).
- **Green after revert:** yes. The spec passed 30 of 30 on SwiftShader. The words named blows 4 and 204 in every run, and the page drew them at 5 and 204 in 8 of the 30, where the old stamp would have read 199. It passed 6 of 6 with frames starved to one every 300 ms, where the page drew them at 7 and 206 every time. The unchanged spec failed 4 of 20 on the same machine before the fix. On CI it passed 80 of 80 in diagnostic run 36083494109, where the old stamp would have failed 74, and on its first attempt in main's run 36086872447.
- **Bound.** The browser check places a named tick in a frame in which a blow landed, not on the exact tick. The fixture's House gives no sight, so the swings are never drawn for the human and its health is the only record. The unit harness holds the exact tick. One fixture, one speed, one target.

## Every hit point a player loses is recorded as a blow, and an enemy building's blow raises the attack warning

- **Gate:** `tests/simulation/playerHitCensus.test.ts`, 12 cases, run by `npm test`, and `tests/browser/attack-warning-buildings.spec.ts`, run by `npm run test:browser`, both in `npm run verify`. Register-driven (register entry 2026-09-24, "An enemy Town Centre, tower or Castle shooting the player's villagers raised no warning"), so no `lessons.md` entry is retired by it.
- **Claim it carries:** every hit point a player-owned unit or building loses on a tick, and every death but a demolition charge spent by its own blow, has a blow recorded for it on that tick. Every recorded blow on a target that is still standing matches a loss, with the attacker's owner, the target's owner and whether the target is a villager or a building. A blow dealt by a player's unit or building names its owners. This holds over an all-AI real match for 6,000 ticks and over eleven scenarios, each of which must see its own attacker: a Watch Tower, a Town Centre, a Castle, a stone's blast, a bombardment, a stone on a building, a demolition charge, melee on a unit and on a building, an archer, and a wolf. Two of the scenarios must land a blow on a building. On the real page, an enemy Town Centre's blow on a villager reaches at least 100 screen pixels of the mark and the words, and the toast names the blow's tick, with no unit's shot in the air and no enemy unit within reach.
- **Mutation:** census: each recording call removed in turn (landing shot on a unit, shot on a building, blast victim, melee on a unit, melee on a building, wildlife bite), plus the blow stamped `world.tick` instead of `world.tick + 1`, plus attacker and target owners swapped. After the independent review: a shot on a building recorded with no owner (the review's own case), a killing shot left unrecorded, and, after its second round, a melee blow recorded only once its target is destroyed. `tests/simulation/swingFeedKeepsParticipants.test.ts`: the swing feed's `participants` dropped. Browser: the audio mount reading `getRecentUnitAttacks()` again (the shipped source), a landing shot on a unit unrecorded, and a replay step resetting the controller (the v0.3.229 review's defect, against the replay spec). Each file restored byte for byte and checked with `cmp`.
- **Red:** census, as failing cases of 12: 7, 1, 4, 6, 2, 1, 12 and 11, then 1 ("blows naming the wrong owners or the wrong kind of target, or none") and 6 ("1 of 9 losses and deaths with no blow recorded (8 losses, 1 deaths)" for the Watch Tower), then 1 (the all-AI match: "1 of 55 losses and deaths with no blow recorded (48 losses, 7 deaths)"). The swing-feed test: `expected undefined to deeply equal { attackerOwner: 1, …(2) }`. Each names its losses, for example "8 of 8 losses with no blow recorded" for the Watch Tower with the landing shot unrecorded. The owners swap spares only the wolf, whose blows carry no owners. Browser: "the enemy Town Centre hurt villager 2207 (10/25) at tick 678 and the minimap's attack mark covers 0 screen pixels", `Received: 0`, for both the shipped source and the unrecorded shot; the replay spec "37 alert toasts while stepping 40 replay ticks".
- **Green after revert:** yes. Census 12 of 12. The buildings spec passed 5 of 5 on SwiftShader, 142 pixels each time and the toast on tick 678, and passed with the other three attack-warning specs on the GPU.
- **Bound.** The scenarios listed. An entity that dealt a blow on the tick it vanished is taken as a spent charge, not a death. No fixture fights with a Bombard Tower. The browser spec is one Town Centre, one villager and one blow on one map.

## An upgrade keeps every membership its line has

- **Gate:** `tests/simulation/upgradeKeepsLineMemberships.test.ts`, 2 cases, run by `npm test`, in `npm run verify`, with `tests/simulation/projectileLooks.test.ts` and the tier cases in `tests/simulation/uniqueTechnologies.test.ts` beside it. Register-driven (register entry 2026-09-26, "The Siege Onager, the Capped Ram and the Elite Skirmisher were left out of tables that named the rest of their line"), so no `lessons.md` entry is retired by it.
- **Claim it carries:** for every upgrade in `UNIT_LINE_UPGRADES` (from -> to, 49 pairs), 29 exported unit predicates, 19 unique-technology effects, the 32 `applies` a walk of `CIV_BONUSES` finds, and six civilization-and-age bonus functions for 30 civilizations and 4 ages give `to` the same answer as `from`. An instrument case refuses fewer than 49 pairs, a predicate that answers every unit alike, and walks that find fewer rules than on 2026-09-26.
- **Mutation:** the Siege Onager taken out of `PROJECTILE_SPEED`, then out of the stone case of `projectileVisualKind`, then out of Shinkichon's list; the Siege Workshop set put back to its old hand list; the Elite Skirmisher taken out of Yeomen's list; the Capped Ram, and separately the Battering Ram, taken out of `MELEE_UNITS`; the Heavy Scorpion's minimum range removed.
- **Red:** all eight, each naming the upgrade and the predicate, for example `siege-onager-upgrade: onager -> siege-onager: projectileSpeedTilesPerTick was 0.5, is 0.7`. The old workshop list gave six lines (Furor Celtica and Drill on the Siege Onager, the Capped Ram and the Siege Ram). The Battering Ram mutation is the first-tier case an implication-only check would pass: `capped-ram-upgrade: battering-ram -> capped-ram: isMeleeUnit was false, is true`. The same workshop, Yeomen and Shinkichon mutations turn the unique-technology cases red (`the upgraded Siege Onager: expected 70 to be 105`, `the upgraded Elite Skirmisher: expected 5 to be 6`, `siege-onager: expected 1 to be 2`), and the Siege Onager's speed alone turns the look census's in-match case red (`expected 9 to be 12`).
- **Green after revert:** yes, every source restored byte-identical by the mutation driver, which checks a SHA-256.
- **Bound.** Only what a listed predicate asks. Outside it: accuracy and wind-up, which differ between the tiers of some lines by design (the Skirmisher line's have their own case in `projectileRules.test.ts`, red before its fix: `elite-skirmisher: expected 1 to be close to 0.9`); the team bonuses and the Heated Shot target set, read on 2026-09-26 and naming every tier; and the AI's and menus' line lists, one of which, Anarchy's Barracks unlock, named the Huskarl alone until the review found it (now a case in `uniqueTechnologies.test.ts`, red before its fix: `expected [ 'huskarl' ] to deeply equal [ 'elite-huskarl' ]`). Hit points, attack and range are not compared. A unit in no upgrade is not asked.

## Every unit holds its fire inside the minimum range units.csv gives it

- **Gate:** the minimum-range case of `tests/content/baseStats.test.ts`, and `tests/simulation/minimumRangeCensus.test.ts` (109 cases), both run by `npm test`, in `npm run verify`. Register-driven, the same entry as above.
- **Claim it carries:** the differential: every `units.csv` "min-max" row's minimum equals `unitMinAttackRange`, a single-number row means 0, and a unit with a minimum has a row. The census, reading the minimums from `units.csv` and never from the game: each of the eleven units, on its own fixture, draws no shot at the unit one cell inside its minimum while its order stands, draws one at the unit at its minimum, and holds (minimum 2 or more) or fires (minimum 1) at a building one cell away. Left to itself among the units, its first shot is at the unit at its minimum; beside the building one cell away with a second one 6 away, at the far one (minimum 2 or more) or the near one (minimum 1). Owned by an AI, the same in both scenes, and an order at the unit or the building inside its minimum is replaced with one at the unit or building it can fire at; each AI case asserts that the AI decided. Bitten by a wolf beside it, a land unit takes no order at the wolf (minimum 2 or more) or turns on it and hurts it (minimum 1).
- **Mutation:** the seven minimums added on 2026-09-26 removed; the Trebuchet's set to 3; an Archer given 1. On the final census: the attack step's unit hold disabled, then its building hold; the automatic finder's unit filter removed, then its building filter; auto-aggression passing the finders no minimum, then never engaging a building; the AI's kept-order check removed; its building measure always outside, then taken from the building's anchor; the AI finder's unit filter removed, then its building filter; the AI passing no minimum; the wolf's bite ignoring the minimum; the fixture's AI seat not forced.
- **Red:** the differential names each row, for example `trebuchet: minimum range 3 vs csv "4-16" (expect 4)` and `archer: minimum range 1 vs csv "4" (expect 0)`. On the tree before the table fix, 12 of the 33 ordered cases went red. With the new minimums and no targeting fix, all 11 units left among enemies went red ("never fired": each took the unit inside its minimum and held its fire at it for good). Before the AI and wolf fix, 38 of the 42 cases then added for them went red; the other four are the Skirmishers', whose right answer did not change. Then, in order: 21, 17, 11, 9 ("fired first at null; the building 1 away is inside its minimum"), 20, 11, 20 ("kept its order at the unit 2 away, inside its minimum, and never fired"), 9, 4 (the Scorpions and the Cannon Galleons, whose House and Dock anchors sit at their minimum), 22, 18, 40, 7 ("took an order at the wolf 1 away, inside its minimum of 2") and 44 ("the AI seat on owner 1 never decided").
- **Green after revert:** yes, byte-identical as above.
- **Bound.** One attack at a time, no technology, units, buildings and a wolf as targets. Attack-ground is `attackGround.test.ts`'s. Not played: a target that walks inside the minimum after a player's unit took it on its own, which is held, not dropped (spec §10.4); and the AI's march on the enemy's first villager, Town Centre or last building with that target inside the minimum, whose branches use the measure the kept-order cases play for a unit and a building. The differential's "no row" branch guards a future unit: every unit in the roster has a row today.
