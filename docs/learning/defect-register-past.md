# Defect register — past

Closed entries rolled over from [`defect-register.md`](defect-register.md), moved verbatim and in the order they had there.

**Append-only history.** Entries arrive here from the active register and are not edited, summarised, reordered or removed. The active register holds the 12 most recent closed entries plus every open one; when a new entry pushes it past 12 closed, the task that wrote the entry moves the oldest closed ones here in the same commit. `tests/architecture/defectRegisterRollover.test.ts` holds both halves. Order here is the order these entries had in the active register: newest first down through the 2026-09-02 block, then OLDEST first from 2026-08-23 forward. The source file already read that way on 2026-09-05; the inversion is kept rather than corrected, because moving an entry verbatim includes not moving it relative to its neighbours.

**Cross-references that now cross the split.** Every "entry above" or "entry below" was written when all of this shared one file, and the wording is left exactly as it was. Seven references, in six entries, cross the split; a later rollover appends to this list. Every other one still points inside the file it was written in.

- Active, "A unit's shadow does not turn when the unit turns": "see the entry below" means "Every shadow in the game was the same rectangle, whatever threw it", here.
- Active, "The AI hoards the resource it cannot spend and starves on the one that gates every unit": "the unit-line entry below", in its heading and again in its CORRECTION, means "The AI reaches the Imperial age and never trains another unit", here.
- Here, "The AI reaches the Imperial age and never trains another unit": a fourth hypothesis "recorded as its own entry above" means "The AI hoards the resource it cannot spend and starves on the one that gates every unit", now active again.

The 2026-09-05 rollover of "The AI's lumber camp cannot reach the woodline on three of the maps it ships" added three more, in both directions:

- Here, "The AI's lumber camp cannot reach the woodline on three of the maps it ships": "This corrects the entry below" means "The AI's build order stalls on the first thing it cannot afford, so no seed has ever built a Siege Workshop", which is still active.
- Active, "An enemy that garrisons its last villagers pins the attacker's whole army forever": "the four-arm table is in the entry above" means the lumber-camp entry, here.
- Active, "The AI's build order stalls on the first thing it cannot afford": "See the lumber-camp entry above" means the same entry, here.

The last two were already pointing the wrong way before any split, and nothing here repairs them: in the source file the unit-line entry sat ABOVE the hoarding entry, and each of them says the other is on the side it is not. They now cross the split as well, in opposite directions.

**The 2026-09-10 rollover added four entries and no cross-references.** Three new closed entries plus the closure of the map-sealing entry took the active file to 16 closed against a cap of 12, so the four oldest archivable closed entries moved here: the unreachable-placement one, the match-outcome one, the refused-simulation-step one and the off-home-machine browser-suite one. None of the four names an "entry above" or "entry below", and no entry left in the active register names any of them, so the list above is unchanged at seven.

**Three entries came back on 2026-09-05.** The first rollover read "(SUPERSEDED ... still unfixed)", "(PARTLY FIXED: ...)" and "(LATENT, not fixed)" as closures and archived three live defects. They were moved back to the active register with `, OPEN` added inside the trailing group and nothing else changed, and `tests/architecture/defectRegisterRollover.test.ts` now refuses an archived marker that leaves the defect live. The rollover's other 38 entries were not disturbed.

The 2026-09-06 rollover of "The AI waits for a full attack group against an enemy that has no army left" added one more:

- Here, "The AI waits for a full attack group against an enemy that has no army left": "The rename in the entry above did not close the class" means "An enemy that garrisons its last villagers pins the attacker's whole army forever", which is still active.

The next 2026-09-06 rollover, of "An enemy that garrisons its last villagers pins the attacker's whole army forever", brought the other end of two of the references above into this file and added one more:

- The bullet directly above no longer crosses the split: both ends of it are HERE now, so its "which is still active" is out of date and is left as written, the way an entry is.
- Here, "An enemy that garrisons its last villagers pins the attacker's whole army forever": "the four-arm table is in the entry above" means the lumber-camp entry, also here — a reference that crossed the split and now does not.
- Active, "A decided match never ends, because the AI can only aim at a Town Center": "the 2026-09-04 garrison entry above", in its heading, means "An enemy that garrisons its last villagers pins the attacker's whole army forever", here.

The 2026-09-06 rollover of "`../civ-engine/.git` emptied down to `refs/` by removing an aoe2 worktree" added NONE: that entry names no other entry, and no entry in either file names it. Checked rather than assumed.

The 2026-09-06 rollover of "Fourteen loaded carriers freeze head-on on Nomad under the walk ranking" added one more, pointing out of this file:

- Here, "Fourteen loaded carriers freeze head-on on Nomad under the walk ranking": the head-on class it twice defers to, "seed-7 is TWO defects, not one", is still ACTIVE. It names that entry rather than pointing at a position, so nothing in it needs repair; the pointer the other way, in the active "A green browser-suite run can leave a headless Chrome burning cores for hours" ("the field's own timer is what the register's fix entry rests on"), now means this entry, here.

The 2026-09-06 rollover of "Six closed threads sat in `docs/threads/current` for up to seven weeks" added NONE: that entry carries no "entry above" or "entry below", and no entry in either file points at it by position.

The 2026-09-06 rollover of "Soldiers walked with their legs in the rest pose" added NONE: it names no other entry, and no entry in either file names it. Checked rather than assumed.

The 2026-09-06 rollover of "The bottom command bar was a different height for every selection" added NONE: it carries no "entry above" or "entry below", and no entry in either file points at it by position. Checked rather than assumed.

The 2026-09-06 rollover of "A finished playtest threw its own recording away at V8's string ceiling" added NONE, and REMOVED one:

- Here, "A finished playtest threw its own recording away at V8's string ceiling": "The 2026-09-02 register entry above records the previous instance of exactly this" means "The replay bundle outgrew Node's string ceiling, and nearest-first approaches deadlocked the gather economy", which is also here. Both ends are in this file now, so that reference no longer crosses the split — and it points the wrong way, because the entry it names sits BELOW it here rather than above. Left exactly as written, the way an entry is. No entry in either file points at the moved entry by position.

The 2026-09-06 rollover of "A villager right-clicked onto an unfinished building walked up to it and stood there" added NONE: the entry carries no "entry above" or "entry below", and no entry in either file points at it by position. Checked rather than assumed.

The 2026-09-06 rollover of "Right-clicking the empty glass of the command bar sent the selected army to a cell hidden under it" added NONE: the entry carries no "entry above" or "entry below", and no entry in either file points at it by position. Checked rather than assumed.

The 2026-09-06 rollover of "The command that checks the remote gate reported an earlier commit's run as the tip's, so a commit with no run at all read as green" added one, pointing INTO this file:

- Active, "A Windows checkout rewrote every tracked file to CRLF, and a shebang plus CRLF is a syntax error inside Vite": "the gate landed the day before for the entry below this one" means this entry, here.

The 2026-09-06 rollover of "A Windows checkout rewrote every tracked file to CRLF, and a shebang plus CRLF is a syntax error inside Vite" added NONE and REMOVED one: the bullet directly above no longer crosses the split, because both ends are in this file now. It still points the right way — the moved entry is prepended ABOVE the `ci:status` entry it names, exactly as the two sat in the active register — so nothing in it needs repair, and it is left as written, the way an entry is. No entry in either file points at the moved entry by position. Checked rather than assumed.

The 2026-09-06 rollover of "A workflow that runs a contributor's code with the Anthropic key in its environment could be triggered by anyone's pull request" (the v0.3.218 batch) added NONE: the entry carries no "entry above" or "entry below", and no entry in either file points at it by position. Checked rather than assumed. It is prepended ABOVE the CRLF entry, which is where the two sat in the active register.

The 2026-09-06 rollover of "The browser suite had never run on CI, on a config that could never have run there" (the browser-specs commit of the same batch) added ONE, pointing back INTO the active file:

- Here, "The browser suite had never run on CI, on a config that could never have run there": in its `UPDATE 2026-09-06` paragraph, "they are recorded and fixed in the entry above" means "The browser suite's first run off its home machine failed two specs, and both were measuring the host instead of the game", which is still ACTIVE. The same paragraph's "the platform claim above" is self-referential — it means an earlier paragraph of this same entry — and does not cross anything.

The 2026-09-06 rollover of "One thrown frame killed the game for good, and the HUD went on drawing a live match over it" (the v0.3.220 construction-health commit) added NONE: the entry carries no "entry above" or "entry below", and the one entry that refers to it — the active register's "Conquest Victory" entry, which cites "the v0.3.211 halt-notice precedent" — names it by VERSION and not by position, so the pointer still lands. Checked rather than assumed. It is prepended above the greyed-out entry, which is where the two sat in the active register.

The 2026-09-06 rollover of "A greyed-out command gave no reason, so the age-up button quoted a price the player could already afford" (the v0.3.219 walk-order commit) added NONE: its three "above"/"below" words are ordinary prose about its own paragraphs, not pointers at neighbouring entries, and no entry in either file points at it by position. Checked rather than assumed. The active register's new walk-order entry names "the command-availability work" by subject and not by place, so it does not become a cross-file pointer. This entry is prepended above the workflow one, which is where the two sat in the active register.

## 2026-09-06 — A building placed where no villager could walk was accepted, charged for, and stranded forever (found by play, FIXED and gated v0.3.212)

**Symptom.** Placing a building on ground no builder could reach previewed GREEN, the click was accepted, the resources were spent, and the foundation then sat at 0 progress for the rest of the match with no message, no refund and no idle-villager warning — the assigned villager simply went idle in the same tick. Because a foundation is a solid obstacle, a player who keeps doing it walls themselves in. Measured in one real match: it happened **eight times for 440 wood**, and the player's own reachable area fell from 1,353 cells to **106**. The refusals the game did issue were no better: four of twelve named ground nobody could reach, so the game was refusing placements for the wrong reason as well as accepting them for the wrong reason. Ten such cells existed on the starting map at tick 600.

**Attribution.** Found by playing the game. Not owner-reported.

**Investigation.** `isPlacementBlocked` asked one question — are the cells this building would stand on free? — and nothing asked whether a villager could get to a cell beside them. The acceptance path and the movement path then disagreed by construction: `playerCommandsSystem` called `findBuildingApproachPlan`, got null, and cleared the build command in the same tick the placement was accepted.

**Root cause.** Placement was validated against the footprint alone, so "can this building stand here" was being used to answer "can this building be BUILT here".

**Fix.** `src/game/simulation/bridge/builderReachability.ts` asks the question of the actual builders using the mover's own passability, and the preview, the validator, the construction re-check and the AI's placement suggestion all read that one verdict — a second opinion that drifts from the approach search would be worse than the defect. It is computed as a connected-component labelling rather than a path per hovered cell, which is exact for a 4-connected uniform-cost grid and is A*'s own answer by construction. The pending footprint is deliberately not carved out before the search, and the module's header proves that is exact rather than an approximation. The labelling is cached per world and per `worldOccupancy.structuralRevision`. **Cost, measured:** 0.77 microseconds per hovered cell, under 0.2% of a live match.

**How this class is checked from now on.** `tests/simulation/placementReachability.test.ts` (`npm test`, so `npm run verify`) sweeps a real booted scenario and asserts that every cell the game ACCEPTS is one a builder can walk to, and that every anchor the game SUGGESTS satisfies the same test. Proven red 4 of 6 by reintroducing the defect. **Its most instructive case is the one that did not go red:** asking the game whether it accepts its own suggestion passed WITH the defect reintroduced, because both sides were the same code agreeing with itself, so the sweep judges against `staticGridOf` — an independent rebuild of land passability from world components. Its one known divergence (a finished gate is a wall to it and a door to its owner) is guarded by asserting the human owns no complete gate at that tick.

**Bound, named in the test header.** ONE map at ONE tick (`aoe2-prototype`, 60x36, tick 600, three starting villagers), land builders only — the water domain runs the same code and is not exercised — and footprints of 1x1 and 2x2 only, of which only the 1x1 sweep can detect anything, since every unreachable pocket on this fixture is a single cell. The A*/labelling equivalence is exact here but `findGridPath` gives up after 10,000 iterations, which 2,160 cells cannot reach; on the §4 ladder's 116x72 maps it can, and then A* may report unreachable where this says reachable. The map size is asserted rather than assumed for exactly that reason.

## 2026-09-06 — A match the player LOST announced itself as "Conquest Victory", in 13px grey, in a corner (found by the standing loop playing a match, FIXED and gated v0.3.214)

**Symptom.** A read-only play session run by this repo's coordinator played to conquest and LOST. (Attribution corrected 2026-09-06: this heading said `owner-reported` and this sentence said the owner played it. Neither was true — the owner never played this match and never mentioned it. Nothing else in the entry changed; every measurement below is sound.) The entire announcement was a `hud-footer` at (12, 611), 420x97, bottom-left, in 13px `rgba(226,234,243,0.56)` grey, reading "All of your units and buildings have been destroyed. / Conquest Victory / Player 1: 668 / Player 2: 1809". A loss announced as a Victory — and beside that, no winner named, no overlay, no dim, and nothing to act on, so a player can finish a match without noticing it has ended.

**Investigation.** Reproduced at `bcdc88c3` on `conquest-defeat-fixture`: `matchState.outcome` is `defeat`, and the rendered footer says "Conquest Victory". The outcome was CORRECT, sat one property away in the same object, and was rendered by nothing. `postGameSummary`'s own `formatWinConditionLabel` took `winCondition` alone and switched on it (`case 'conquest': return 'Conquest Victory'`), so a win and a loss printed the identical four characters. Nothing else on the HUD said which way the match went.

**Root cause.** Two, stacked, and the second is why the first survived: the words were built from a value that does not carry the answer, and the surface they were drawn on was never sized to be read.

**Fix (v0.3.214).** One module forms all the end-of-match words — `src/ui/hud/matchOutcomeText.ts` — and EVERY function in it takes the outcome, so a label without the outcome is not expressible rather than merely discouraged. Both surfaces build their text there: the footer record (`postGameSummary`) and a new dimmed modal (`src/ui/hud/matchResultOverlay.ts`) that states the result, names how the match was decided, names the winner where that is knowable, and carries one control. The modal follows the v0.3.211 halt-notice precedent deliberately — same dimmed full-screen root, one panel, one action, focused on open — rather than inventing a third modal idiom; a stopped match and a finished one are the same kind of event. The footer stays as the record after the overlay is dismissed.

**Naming the winner is derived honestly, and that is the part worth keeping.** `MatchState` records the outcome from the HUMAN's point of view and stores no winner at all. Score cannot stand in for one: in the conquest fixtures the WINNER HOLDS THE LOWER SCORE — 50 against 10 one way and 10 against 50 the other — so a "top scorer wins" shortcut would have named the loser in the very match this entry is about. So the derivation is refused wherever it would be a guess. A victory is the player's own. A draw has no winner and says so. A `score` defeat names the top scorer, because "highest score wins" is that condition's own published rule (`scoreTimerSystem`) rather than an inference. A conquest, wonder or relic defeat against ONE opponent names them, because only one player can have won. A conquest defeat with several opponents records only THAT an opponent won and names the winning SIDE — "Winner: your opponents" — instead of picking one.

**The one real gap, recorded rather than papered over.** `MatchState` has no `winner` field. Until it does, the multi-opponent conquest case cannot name an individual, and no amount of care in the HUD can invent the datum. This is the only case the announcement is vaguer than DE.

**How this class is checked from now on.** Two gates, and the split between them is the point. `tests/browser/match-result-announcement.spec.ts` (`npm run test:browser`, so `npm run verify`) plays FOUR real matches in the browser to resolution at 800x600 — `conquest-victory-fixture`, `conquest-defeat-fixture` (the human holds the higher score, 50 against 10), `score-timer-draw-fixture` (tied at 180) and `score-timer-defeat-fixture` (three players, P2 the sole top scorer) — and asserts the RENDERED TEXT, never `matchState.outcome`, because the defect is exactly that the field was right and unrendered. Each match must read the right outcome and never a different one, must name a winner, must be legible (visible, at least a quarter of the viewport, dimming what is behind it, headline at 24px or more in a colour at least 80% opaque), and must offer one control that dismisses it and leave the corrected footer behind. `tests/ui/matchResultAnnouncement.test.ts` (`npm test`) is the pure-render half: every `outcome` x `winCondition` pair the union allows, across four owner shapes. Red three ways: every `defeat` and `draw` case reported "Conquest Victory" or "Score Victory" at `bcdc88c3`, `renderMatchResult` did not exist, and the OVER-FIX guard bites too — deleting `.hud-match-result[hidden] { display: none; }` from `hudChrome.css` turns the live-match assertion red. That last one is the author-`display`-beats-UA-`[hidden]` trap `.hud-halt-notice` paid 99.98% of a frame for the day before, and it is now the third surface to carry the compound rule. Live-game capture diffs are 0.00% at both viewports.

**Bounds, named.** The browser spec covers no wonder or relic DEFEAT wording (no fixture ends a match that way; the unit test covers those labels as pure renders), no multi-opponent CONQUEST defeat (no fixture), no viewport but 800x600, no keyboard reachability of the dismiss control, and nothing about what the overlay does across a restart. The unit test proves nothing about mounting, visibility, type size, contrast or reachability — a surface never appended, `hidden` forever, or painting at 1px passes every line of it.

## 2026-09-06 — A simulation step that refused to run said nothing, and two sessions then reasoned from a false model of the engine (found by the standing loop, FIXED and gated)

**Symptom.** `window.__AOE2_TEST__.advanceTicks(3000, 100)` against a game paused from the REAL Esc menu returns after zero ticks, throws nothing, and leaves `matchState.outcome === 'running'` and `engineHalted === null` — the two fields a caller would consult, both saying nothing is wrong. Confirmed live at v0.3.213.

**Investigation.** `bridge.step()` had FOUR paths that decline to advance the world and returned the same nothing for all of them: a manual pause (`setPaused(true)`, which is what the Esc menu applies), `haltState.halted`, a match that already has an outcome, and the replay bridge, which never advances from a frame by design because `ReplayController` owns playback. `advanceTicks` lifts only the pause IT applied (`pausedThroughTestApi`), so a pause set anywhere else survives every step in its loop.

**The expensive part was not the missing ticks.** A play session, reading that silence, published a hazard about the test seam in two forms: that `advanceTicks(n)` throws the engine's re-entrancy `EngineError`, and that it "silently under-advances when the simulation is not paused". Both were repeated in worker assignments as fact. A later session measured them at `d65f84aa` and found BOTH wrong — running and unpaused, 3,000 requested gave 3,000 advanced with no throw; paused through the test API, the same; and `advanceTicks` is a plain loop called from outside any tick, so it cannot reach the engine's re-entrancy guard at all. So two sessions spent effort reasoning from a false model of the engine, and the cause was not a wrong measurement: it was that a function which declined to do its job said nothing about declining, leaving a tick delta of zero as the only evidence and every explanation for it equally available.

**And a caller was already paying for it.** `src/game/playtest/llmRunner.ts` treats `advanced <= 0` as `stopReason = 'engineHalt'` under a comment that asserts the inference is safe — "Reached only when the match is still 'running' ... so no progress here IS a genuine halt". A world paused by anything other than the test API is still `running` and still advances nothing, so that branch reports a PAUSE as an engine crash. It is latent only because the LLM host happens to unpause around each call; nothing in the runner knows that, and nothing would tell it if that stopped being true.

**Root cause.** A function with four distinct refusal conditions and no return value. Every caller had to infer refusal from an unchanged tick counter, and no caller could tell the four apart.

**Fix.** `step()` returns `{ ticks, refusedBecause }` (`src/game/simulation/bridge/stepReport.ts`, typed in `simulationBridgeTypes.ts`), where `refusedBecause` is `'paused' | 'halted' | 'match-over' | 'replay'` or `null`. `ticks` is reported separately from the refusal because zero ticks is legitimate for a `deltaMs` shorter than one tick, so a short step is not a fault. A step that ran some ticks and then hit a failing tick reports BOTH halves. The refusal values are shared FROZEN module constants — one object each for the life of the process, no `Error`, no stack capture, no string building — because a halted, paused or finished match refuses on every frame for the rest of the session, sixty times a second, and that steady state must allocate nothing. `browserTestApi.advanceTicks` now returns the tally (`stepsRequested`, `stepsRun`, `ticksAdvanced`, `refusedBecause`) summed from the steps' own reports rather than read off a tick counter, and warns once per reason for callers that ignore the value.

**Two designs were rejected, and why is the useful part.** THROWING was rejected because the playtest runner polls a finishing match and a resolved match is a normal end there, not an exception. Fixing only the test seam — having `advanceTicks` compare tick counters and report the shortfall — was rejected because it leaves the production silence exactly as it was, and the production silence is the defect: the same `step()` is called by the frame loop and by `runPlaytest`, and it is those callers, not the test harness, that had no way to ask what happened.

**How this class is checked from now on.** `tests/app/stepRefusalIsReported.test.ts` (`npm test`, so `npm run verify`), 13 cases: all four refusal reasons on the REAL objects (`paused`, `halted` and `match-over` on a real `createSimulationBridge`, `replay` on a real `makeReplayBridge`), asserted to be four DISTINCT values — a signal that cannot tell them apart is the same defect in a new costume — plus a control in the other direction (a running unpaused bridge reports no refusal, so the gate cannot pass by refusing everything) and the seam end to end through the real `installBrowserTestApi`. Proven red at `bcdc88c3` with the source reverted: 12 of 13 failed. The 13th passes on both trees ON PURPOSE — it is the backwards-compatibility control for widening `advanceTicks`'s return value — and carries none of the gate's coverage.

**Bounds, named.** Seed `aoe2-prototype`, tick horizons in the tens, and match resolution FORCED with `gameLength` rather than played out, so it proves nothing about how a real match ends. It never runs the live frame loop, which calls `step()` every animation frame and correctly DISCARDS the report; nothing here measures that loop's cost or checks that discarding stays correct. The console warning's dedupe is asserted over two calls, not over a long run.

**Open follow-ups.** (1) `llmRunner.ts` should READ `refusedBecause` instead of inferring a halt from `advanced <= 0`; the signal now exists and the runner still guesses. (2) `src/app/bootstrap/browserTestApi.ts` is now at EXACTLY 500 lines, its cap in `tests/architecture/fileSizeBudget.test.ts`, so the next line added there goes red and the answer is a split by role rather than a trim.

## 2026-09-06 — The browser suite's first run off its home machine failed two specs, and both were measuring the host instead of the game (found by the remote gate, FIXED and gated)

**Symptom.** CI run **34041883943**, the first execution of the new `browser` job and the first run of this suite on any machine but the one developer Windows box, failed **2 of 197** on `ubuntu-latest`. One entry, not two, because they are one finding: the first run of a suite off the machine that had always run it.

**The first: an assertion about how fast the host is.** `tests/browser/play-opening.spec.ts` read the tick, waited for the HUD clock to leave `00:00`, and read the tick again. The clock had ALREADY left `00:00` by the time that wait started, so it resolved on its first poll — 14 ms — and waited for nothing. What was left was an assertion that at least one tick fits in the 25 ms between two round trips. On `ubuntu-latest` (two cores, software rendering) exactly zero ticks fitted, and it read 20 → 20. It now polls for the world to actually advance past the tick it read, with a 30-second budget and a message naming what it was waiting for; nothing else moves the tick, so a world that stops advancing exhausts the poll and the step goes red.

**Its falsifiability proof is the part to keep.** The first attempt to prove the new form red PASSED when it should have failed. The mutation froze the world at tick 11 rather than at zero, so the poll succeeded for the wrong reason — it was racing the very thing it was trying to disprove. A mutation that races what it is trying to disprove proves nothing, and a green result from one is indistinguishable from a green result from a working gate.

**The second: an assertion with a third of a screen pixel of room.** `tests/browser/game-selection-click.spec.ts` searches `tile-selection-cycle-fixture` for a point where a militia, a house and a sheep all answer one click, then cycles the selection there. Of the **625** world offsets its old scan probed, exactly **four** produced the required ordering, in a ribbon one sample wide — and raster order reaches a region at its TIP, so the point it took had the least room of any point in it. Measured margin: **0.30 screen pixels**, at an edge that moves with the idle animation, because the presented hit polygons are evaluated at the renderer's animation clock. A host that reaches a different tick before the scan freezes the world reads the bodies at a different phase, and the click lands outside.

**Root cause of the second, and it is the FIXTURE.** Co-located bodies are drawn at their cell plus one of four slots, `UNIT_CELL_SLOT_OFFSETS[entityId % 4]`, and a sheep carries a unit transform too, so it takes a slot as well. The slots are half a cell apart while the militia's body is 0.25 cells across and the sheep's 0.231, so two different slots leave a **0.019-cell** gap: the two bodies are either exactly coincident or they never touch, and these two never touched. Ids are handed out one per spawn in list order, so the two share a slot exactly when their positions in the list differ by a multiple of four. The fix is a spawn REORDER that puts the militia first and the sheep fifth, so they coincide as the fixture always intended.

**And the scan now measures what it found.** It aims at the middle of the longest run up the militia's body that cycles, rather than at the first offset raster order happens to reach, then grows an eight-point compass around that middle until one direction stops cycling, and fails BY NAME on the fixture if the margin drops below two screen pixels. Margin went **0.30 px to 6.00 px** at default zoom, held across five animation phases. No existing assertion was changed or weakened; one was added.

**Two things the investigation established that outlive this fix.** (1) **Fixture spawns and live play use DIFFERENT lattices** — half-cell slots for fixture spawns, quarter-cell through the occupancy allocator in play — so a fixture pile is spread twice as far as a real one and its bodies never touch. That is the real root and it needs its own task; the reorder above works around it for one fixture. (2) **There is NO player-facing defect here.** Three units genuinely on one cell were ordered correctly in the running game, are separately clickable, and repeated clicks cycle whichever bodies cover the point — which is this spec's own contract and more than the real game does.

**How this class is checked from now on.** Both specs are their own gates, and both now fail for a reason the reader can act on: the first cannot pass on a dead simulation and cannot pass on a fast host by accident, because it polls for the world to advance rather than betting on a wall-clock window; the second measures the fixture's own margin and names the fixture in its failure message, so the next time the two bodies drift apart it says so instead of failing on a click. Bound: the margin is measured at default zoom in iso pixels before the camera zoom (≥ 1), so the real on-screen margin is at least the number asserted; it is held at five animation phases, not continuously.

**Open risk, recorded because the slower runner may hit it.** Two specs in `tests/browser/game-combat-and-meta-meta.spec.ts` went red purely from machine load on 2026-09-05 and passed when re-run alone. The heavier of them drives **2,000 single-tick steps** inside one `page.evaluate` against the suite's 30-second per-test budget, and the file takes **41.7 s** run on its own. A slower CI runner may trip them. If it does, that is this risk and not these changes — and the answer is not a raised timeout.

## 2026-09-06 — The browser suite had never run on CI, on a config that could never have run there (found by the standing loop, FIXED and gated)

**Symptom.** `npm run verify` runs six stages — `content:validate && test && typecheck && test:browser && lint && build`. CI ran five. Nothing in `.github/workflows/` installed a browser, so `test:browser` had no CI leg at all, and all 44 spec files under `tests/browser/` (43 of which execute; `_captureSelectionPanel.spec.ts` is excluded by `testIgnore`) existed only as something one developer machine had run — the same machine whose unrepresentative line endings let a defect ship the day before.

**And it could never have worked anyway, which is the substance here.** `playwright.config.ts` started its preview server with `` command: `npm.cmd run preview -- ...` ``. `npm.cmd` was hardcoded on **2026-04-11**, when only Windows ran the suite (`002b7280`, "Add browser gameplay test harness", introduced it as `npm.cmd run dev`; `cf774cd4` the same day changed `dev` to `preview` — the new in-file comment credits the hardcoding to the second of those, which is the wrong hash for that claim). There is no `npm.cmd` on Linux. Adding an ubuntu job without touching that line would have started nothing, and all 43 specs would have died together on the 120-second `webServer` timeout with **nothing in the output naming npm** — a failure mode that reads as a broken suite rather than a broken command.

**Root cause.** A platform-specific command written when there was only one platform, and a gate stage that was never given a CI leg to expose it.

**Fix.** `playwright.config.ts` picks the binary at run time — `const npmBin = process.platform === 'win32' ? 'npm.cmd' : 'npm';`. `.github/workflows/ci.yml` gains a `browser` job: `ubuntu-latest`, no `needs:` so it runs beside `gates` rather than after it, a 45-minute timeout, the Playwright browser restored from `actions/cache` keyed on the installed Playwright version (full `playwright install --with-deps chromium` on a miss, `install-deps chromium` on a hit, because the apt libraries live outside the cached directory), and `test-results/` uploaded on failure for 7 days.

**Two deliberate omissions, both argued rather than overlooked.** Ubuntu only: a Windows leg costs 30 to 40 extra billable minutes at double rate, and Windows is the one platform the suite is NOT unproven on, since it is the platform that has been running it. And `.nvmrc` was deliberately kept OUT of both push filters: both workflows pin their Node version as a literal (`'20'` in `ci.yml`, `'22'` in `playtest.yml`) and neither reads that file, so a push that triggered on `.nvmrc` would run on a version the file does not name and report green. Worth recording for whoever wires `node-version-file:` later: `.nvmrc` today contains **24**, which matches neither pin, so that wiring silently moves CI from Node 20 to Node 24 and is a change in its own right.

`.gitattributes` WAS added to both push filters, because the commit that fixed the Windows line-ending defect (`5274d7e2`) could not trigger its own proof.

**How this class is checked from now on.** `tests/scripts/ciStatus.test.ts` gained two cases. The three `tests/scripts/fixtures/workflow--*.yml` files are copies of the real workflows and nothing compared them — the copies were taken at `ca24709b`, `52546080` then edited two of the originals, and the fixtures sat a day behind with nothing red. Now each fixture's parsed push trigger must equal the real workflow's, with controls asserted first (the real file parsed at all, and has a readable `name:`) so a match cannot mean "neither parsed"; and a completeness case requires a fixture for every `.yml` in `.github/workflows`. **Bound:** parsed push trigger ONLY. A fixture may differ from its workflow in a comment, a step or a `permissions:` block and this stays green.

**Why this entry is OPEN.** The `browser` job has never run, anywhere. Everything above is a claim about what will happen on the first push, not an observation, and the CI leg is exactly the thing this entry exists because nobody had. It closes when a run of that job is observed and its result recorded here.

**UPDATE 2026-09-06 — the first run happened, and it was RED on a real defect. Still OPEN.** Run **34041883943** started the preview, ran the suite, and failed **2 of 197** on `ubuntu-latest`. That is the outcome this job existed to make possible: both failures were specs measuring the HOST rather than the game, invisible to every run on the one Windows machine that had ever executed them, and they are recorded and fixed in the entry above. So the platform claim above is now an observation rather than a prediction — `npm` resolves, the preview comes up, and the specs execute. The entry stays OPEN because the thing it is waiting for is a GREEN run of that job, and a red run is not one. It closes when a green run is observed and its id recorded here.

**CLOSED 2026-09-06 — the green run happened, and it is recorded here.** Run **34059936180** on `06a83e98` finished with all three jobs green, `ubuntu-latest — browser suite: success` among them, confirmed independently by `npm run ci:status`. That is the observation the two paragraphs above were waiting for, and it is what closes this entry rather than any further code change. What it proves is exactly what it says and no more: on `ubuntu-latest`, `npm` resolves, the preview server comes up, and the job's **199 tests pass in 39.9 minutes** — read out of the run's own log, not carried over from the red run above, whose 197 is a different number for a different tree. It does not prove the suite passes on any other platform — the Windows leg was deliberately left off (see the two omissions above) — and one green run is not a claim about flakiness.

## 2026-09-06 — One thrown frame killed the game for good, and the HUD went on drawing a live match over it (found by play, FIXED and gated)

**Symptom.** The game stops. The clock stops, the units stop, and the HUD keeps showing a live-looking match — resources, age, population, minimap, a running match timer — with nothing anywhere saying anything is wrong. No banner, no error on screen, no recovery short of reloading the page. The observed match froze at tick 29,297. `getHudState().engineHalted` stayed `null` throughout, so every instrument that reads it said the simulation was healthy.

**Attribution.** Found by playing the game; the owner reported it as a freeze.

**Investigation.** The reported trigger — `window.__AOE2_TEST__.advanceTicks(n)` throwing `EngineError: world.step()/stepWithResult() was called re-entrantly from within a tick` — does not reproduce, and neither does the separate report that `advanceTicks` "silently under-advances when the simulation is not paused". Measured live on the dev server at `d65f84aa`: running and unpaused, 3000 requested → **3000 advanced**, no throw; paused through the test API, 3000 → 3000; paused at 100/300/1000, all exact. `advanceTicks` is a plain `for` loop of `bridge.step()` from outside any tick and cannot reach the engine's re-entrancy guard. What DOES under-advance silently, and is the likely source of "3000 requested, returned after 143", is unrelated to re-entrancy: `advanceTicks` lifts only the pause it applied itself (`pausedThroughTestApi`), and `bridge.step()` returns early **with no signal** in three cases — manual pause, `haltState.halted`, and `matchState.outcome !== 'running'`. Driving the real Esc menu and then calling `advanceTicks(3000)` advances **0 ticks and throws nothing**. That early-return-without-a-signal is still OPEN and lives in `src/game/simulation/**`.

The freeze itself needed no re-entrancy at all. `AoeVoxelGameView.frame()` put `window.requestAnimationFrame(this.frame)` as its **last statement with no try/catch** (`d65f84aa:src/app/AoeVoxelGameView.ts:382-386`), so a throw anywhere above it — `bridge.step()`, the camera, the renderer — meant the loop was never rescheduled again. Injecting one throw into the real frame path of the running game reproduces every reported symptom exactly: the tick froze at 15 and was still 15 after another 1.5 s, one uncaught `pageerror`, `engineHalted` **null** (correctly — the world was never halted; the LOOP died), and a screenshot indistinguishable from a healthy match. `engineHalted` was null because it is a different mechanism, and that mechanism was **equally silent**: a grep of the whole tree found the field read by `src/game/playtest/runPlaytest.ts` and by tests, and by **no UI code at all**, so an engine tick failure — which the bridge does catch — also left the HUD drawing a live match over a dead world.

**Root cause.** The animation-frame callback rescheduled itself only on the success path, and neither of the two ways a match can stop had any player-visible surface.

**Fix.** The frame body is wrapped, and the reschedule now happens outside the try, so the loop survives. The first failure stops the simulation for good — an exception raised inside a tick may have left world state inconsistent, and continuing to step it would be worse than stopping — while the camera and renderer keep running so the frozen match stays lookable; a second failure means the draw path is broken too, so the loop then stops deliberately. `src/app/bootstrap/engineHaltSurface.ts` routes BOTH sources into one modal notice (`src/ui/hud/engineHaltNotice.ts`): the view's caught frame throw by push, and `HudState.engineHalted` by a 500 ms poll. It names the cause, adds the tick, phase and code for an engine halt, and offers a Reload button. A paused game, a finished game and a broken one are now three visibly different things.

**The regression the pixel diff caught, which no test would have.** The notice's first CSS gave `.hud-halt-notice` `position: fixed; inset: 0; display: grid`, and an author `display` outranks the UA sheet's `[hidden] { display: none }` on cascade origin — so its dim and 2px blur covered every frame of a perfectly healthy game. The boot capture came back **99.98% changed (479,905 of 480,000 px)**. The same trap is already documented in `styles.css` for `.replay-load-dialog__panel` and `.hud-game-menu`; the compound `.hud-halt-notice[hidden] { display: none; }` re-assertion fixes it, and the re-diff is **0.00%**.

**How this class is checked from now on.** `tests/app/frameLoopHalt.test.ts`, 10 cases under `npm test` and so under `npm run verify`. It drives the real `AoeVoxelGameView` frame callback through a hand-controlled animation-frame queue and asserts that a frame which throws leaves the loop scheduled, that the simulation does not keep stepping afterwards, that a second failure stops the loop deliberately rather than throwing every frame forever, and that a player-visible notice names the cause and offers an action — plus the engine-halt half, that a `HudState.engineHalted` the bridge caught for itself reaches the player. Proven red by reintroducing the defect (frame body un-wrapped, reschedule back as the last statement): 7 of 10 fail, each with the injected error escaping through `frame` at `AoeVoxelGameView.ts:392` — `→ step exploded`, `→ world.step()/stepWithResult() was called re-entrantly from within a tick …`, `→ plain string failure`.

**Bound of that gate, named.** It covers ANY thrown value out of the frame body — a plain `Error`, the reported re-entrancy message pinned as its own case, and a non-`Error` throw — not only the re-entrancy instance. But it is jsdom with the renderer, camera, pointer and presentation coordinator mocked and a hand-driven frame queue, so it proves nothing about real WebGL, real rAF scheduling or throttling, or whether the engine's re-entrancy guard is reachable from the page at all. The rendered half is captured evidence, not this test. Two things it deliberately does not cover: the engine-halt case passes even with the frame-loop defect reintroduced, because that is the second, independent silent stop rather than this one; and the silent under-advance of `bridge.step()`'s three early returns is untouched and OPEN in the simulation lane.

## 2026-09-06 — A greyed-out command gave no reason, so the age-up button quoted a price the player could already afford (found by the standing loop, FIXED and gated v0.3.210)

**Symptom.** On `aoe2-prototype`, the Town Centre's "Research Feudal Age" button sat greyed while its own tooltip said the age costs 500 food and the player held 1,295. Nothing anywhere named what was actually missing. A play session sat in the Dark Age for **38 minutes** while the AI aged up at 12:52. Measured at the moment of the reading: tooltip `"Research Feudal Age. Cost: 500 food; takes 130s."`, `food: 1295`, `disabled=true`, `visibleResearchOptions=["feudal-age","loom"]` against `researchOptions=["loom"]` — the card knew the option existed, knew it was refused, and had no way to say why.

**Attribution.** Found by the standing loop, playing the game; the owner reported none of it.

**The eligibility rule was CORRECT and is unchanged.** Spec §7.2 requires two of {Barracks, Dock, Lumber Camp, Mill, Mining Camp}, and the button enabled the moment a Lumber Camp and a Mill were both complete. Nothing about what may be researched moved, then or now.

**The class, which is what was actually fixed.** A command the card DRAWS but the player cannot use gives no reason. This repository's refusal sentences existed only at the moment of rejection, which leaves two holes: a disabled control can never be clicked, so its reason is unreachable from inside the game at all; and a control that looks usable explains itself only after the click. The age-up button is the worst case of the first hole, because the one number it does show — a price the player has covered twice over — points away from the answer.

**Root cause.** The reason lived in the validators, which run on rejection. Nothing computed it for a control that refuses in advance.

**Fix.** The reason is computed in `bridge/commandAvailability.ts`, beside the option lists, and carried to the HUD in `SelectionState.unavailableCommands` for research, train, build and market — so a tooltip cannot disagree with the validator that would reject the same click, and the HUD adds no wording of its own but routes each reason to the control whose `data-command` matches. Reason order matches the validators' own order, so a structural rule outranks a price and an age the player cannot reach is never reported as merely expensive.

**The error the author caught in their own work, which is the most instructive part of this entry.** The first cut had the build card assemble a LOCAL fallback sentence when the bridge supplied none — a second copy of the wording, free to drift from the bridge's. Worse, it made the "before" arm of the visual comparison answer with a sentence the base build never had, so the pixel diff came back 0.00% and measured nothing at all. The fallback was removed and the bridge is now the only source; the captures below were retaken.

**How this class is checked from now on.** `tests/simulation/commandAvailabilityReasons.test.ts` (6 cases, at the bridge) and `tests/browser/command-refusal-reason.spec.ts` (2 cases, the DOM half), both under `npm run verify`. Between them they cover a building prerequisite, a technology prerequisite and a price across all four command kinds, and they assert that the message NAMES the missing thing — plus, for the age-up, that the reason must NOT contain the word `food`, which is precisely the misdirection this defect consisted of. Proven red twice: with `unavailableCommands: []`, 5 of 6 fail (`expected undefined to be defined`, `expected undefined to be 'Not enough wood.'`); with the age-up reason degraded to a bare `'Not available.'`, 3 of 6 fail (`expected 'Not available.' to contain '2 completed Dark Age buildings'`). The browser spec is red 2 of 2 on a rebuilt tree with the reasons suppressed.

**Visual evidence and cost.** Resting HUD 0.00% across five views — the change adds nothing to a panel nobody is hovering. Hover on the locked age-up: 7.15% (34,309 px), the tooltip box alone. Hover on an unaffordable build card: 0.10% (493 px), the diff image literally spelling out the sentence. `getSelectionState()` costs 0.0215 ms in the worst realistic case measured, a villager with all 17 build options reasoned.

**Bound of that gate, named: six commands still refuse silently.** In the order the author ranked them. (1) A production-queue entry blocked by the POPULATION CAP — observed, not theorised: food charged 500 -> 350, three villagers sitting at 0% forever, and nothing saying the cap is why. `ProductionQueueEntry.isBlocked` is already set by the simulation and ignored by the HUD, so this one is cheap. (2) An unfinished building's card draws its full command set while all three validators reject with `under_construction`. (3) Tribute names neither the resource nor the recipient. (4) A keyboard user cannot reach a disabled button's reason at all, because `disabled` makes it unfocusable. (5) Commands the card does not DRAW are still invisible, where DE greys out a locked item and says "requires X". (6) No gap was found for action, stance and formation on a completed building. None of these is covered by the two gates above.

**A tooling gap this change had to work around, and it is not fixed.** `scripts/captureMapScreenshot.mjs` — the maintained capture script, the one AGENTS.md names as the pair for visual verification — has no `HOVER` variable, and a disabled button cannot be focused with `HOTKEY`. So the only visible surface this change has is unreachable from it, and the hover pair above had to come from a scratch driver that reimplemented the script's two trust checks by hand. A `HOVER="<data-command>"` variable is worth adding to that script and has NOT been added.

## 2026-09-06 — A workflow that runs a contributor's code with the Anthropic key in its environment could be triggered by anyone's pull request (found by the standing loop, FIXED and gated)

**Symptom.** Nothing misbehaved, and that is the entry. The repository went public on 2026-09-05. `.github/workflows/playtest-llm.yml` triggered on `pull_request: types: [opened, labeled, synchronize]`, checked out the pull request's merge ref, and then ran `npm ci` and `npm run playtest:corpus-llm` — the contributor's own install scripts and the contributor's own package script — with `ANTHROPIC_API_KEY` in the step environment. A private repository takes pull requests only from people who can already push. A public one takes them from anyone with a GitHub account, and nothing about that workflow file changed on the day the meaning of its trigger did.

**Attribution.** Found by the standing loop, auditing what the repository exposes now that it is public; the owner reported none of it.

**Investigation, and the finding that matters most.** That path never ran once. Of this repository's 120 workflow runs, 119 are `schedule` and 1 is `push`; `pull_request` appears zero times. The reason is uncomfortable. THREE things were keeping the key safe and NOT ONE of them was visible in the file as a protection: a GitHub platform default that withholds secrets from a fork's pull request, a human applying the `llm-playtest` label, and the job's `if:` requiring `vars.ANTHROPIC_KEY_AVAILABLE == 'true'` — where this repository has NO Actions variables at all, so that half of the condition was false for every event that ever reached it. The third is the one to be afraid of. **A condition that reads a name nobody has defined is not a gate**; it is an accident that looks like one, and setting that variable to `true` — the obvious move for anyone who wants LLM playtests on pull requests — re-arms the whole path in one click from the web UI, with no commit and no review. The label was never a code gate either: `synchronize` sat in the trigger list, so labelling approved the code ONCE and the key-bearing job then re-ran on every commit the contributor pushed afterwards. The generalisation worth carrying out of this: **the shape to hunt for is a protection whose evidence lives outside the file**, because reading the file cannot tell you whether it is there.

**Root cause.** The trigger list. The workflow was written while the repository was private, where `pull_request` means "somebody who can already push", and publishing the repository redefined that trigger without touching a line of it.

**Fix.** The `pull_request` trigger is GONE from `playtest-llm.yml`. Only `schedule` and `workflow_dispatch` can see the secret now, and both run code that is already on a branch of this repository. The `if:` went with it — it guarded only the PR path and evaluated true for every schedule and dispatch run, so those two are unchanged — as did the now-unreachable "Post SUMMARY-LLM.md to PR" step and the `pull-requests: write` permission that existed only for it. `ci.yml`, which does run on `pull_request` and uses no secret, gained `permissions: contents: read`, which it had never declared; the repository's default token permission is already `read`, so that pins the floor rather than closing a live gap. **`pull_request_target` was not introduced and must never be** — it is the trigger that would make this genuinely exploitable, handing contributor-controlled code both the secrets and a write-scoped token against the base repository. It has never appeared anywhere in this repository's history.

**A recommendation that was made, passed on, and is wrong.** "Turn off sending secrets to workflows from fork pull requests" does not exist for a public repository. `gh api repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos` answers 422: it is a private-repository control, and a public repository never sends secrets to a fork's pull request in the first place. The real control, and it is still unset, is `fork-pr-contributor-approval`, currently `first_time_contributors`; the owner should set it to require approval for ALL external contributors. That is a web-UI change no commit here can make. Read and deliberately left alone: `default_workflow_permissions` is `read`, `allowed_actions` is `all`, `sha_pinning_required` is false.

**An open follow-up this does NOT fix: the engine tarball comes from a mutable tag.** CI fetches `civ-engine.tgz` from a rolling release tag whose asset is REPLACED on every engine push, so what a run installs is whatever was uploaded last. Making it immutable needs `civ-engine` to publish a per-commit artifact, and AGENTS.md forbids this repository from modifying that one. A sha256 pin on the mutable URL was considered and rejected: it would turn all three workflows red on every engine publish, and this project has already lost two months to a red streak nobody was watching (the 2026-08-29 entry). It stays OPEN, and it is recorded here rather than as its own entry because it is the same audit and the same day.

**How this class is checked from now on.** `tests/architecture/workflowSecretExposure.test.ts` (added 2026-09-06, runs under `npm test` and so under `npm run verify`) parses every file in `.github/workflows/` with js-yaml and fails if any workflow that references a secret carries a trigger key beginning `pull_request`, or if any workflow at all — secret-bearing or not — carries `pull_request_target`. It reads parsed trigger keys and parsed string nodes, never raw file text, because comments are the false-positive class that defeated the first throwaway checker: `ci.yml`'s comment "uses no secrets." matches a raw `secrets.` scan and `playtest-llm.yml`'s comment "Do NOT add `pull_request_target`" matches a raw scan for that trigger, and a gate that cannot tell a warning from a use gets deleted by the next person it annoys. Six in-file fixtures self-test the classifier in both directions, so gutting the secret detector goes red instead of leaving the real-file checks vacuously green — verified by mutation. Proven red three ways with the tree otherwise untouched: `pull_request` re-added to `playtest-llm.yml`, `pull_request_target` added to secret-free `playtest.yml`, and the detector stubbed out; proven green with a comment naming both banned things added to `ci.yml`. **Bounds.** It sees `.github/workflows/*.yml|*.yaml` and nothing else, so a secret reaching pull-request code through a composite action under `.github/actions/`, through the body of a reusable workflow this repo calls, or through an environment-scoped secret would pass — there is nothing to miss today, since `.github/` holds only `voxel-commit` and the three workflow files and no `.github/actions/` exists. It says nothing about the controls that live in GitHub's web UI rather than in the repository — Actions variables, environment protection rules, the default token permission, fork-PR approval — which is precisely why the in-repository half needed pinning: setting `vars.ANTHROPIC_KEY_AVAILABLE` to `true` re-armed the old path in one click with no commit. And it deliberately does not read `if:` conditions or `permissions:` at all, because the lesson of this incident is that a condition depending on a value defined outside the file is not a protection anyone can verify; a key-bearing job guarded only by an `if:` counts as reachable here. js-yaml is declared in `devDependencies` at `^4.3.1` as of this commit — the version already resolved in `node_modules` and in the lockfile — because until now it was only a transitive dependency of eslint and a lint upgrade could have taken it away; if it disappears anyway the gate throws by name rather than skipping. The test file's own header still describes it as transitive-only, which is now the one stale sentence in it.

## 2026-09-06 — A Windows checkout rewrote every tracked file to CRLF, and a shebang plus CRLF is a syntax error inside Vite (found by the remote gate, FIXED and gated)

**Symptom.** The repository went public on 2026-09-05, which unblocked GitHub Actions for the first time in weeks, and the very first CI run that actually executed found this. On `ca24709b`, run 34017480242: `ubuntu-latest` success on all 15 steps, `playtest-corpus` success, and `windows-latest` FAILURE at the "Vitest unit suite" step. One file was red and nothing else in 482 was — `Test Files 1 failed | 480 passed | 1 skipped`, `Tests 6 failed | 3472 passed | 3 skipped` — and all six failures were `SyntaxError: Invalid or unexpected token` with no further message. The file was `tests/scripts/ciStatus.test.ts`, the gate landed the day before for the entry below this one.

**Attribution.** Found by the remote gate on its first green-lit run; the owner reported none of it. Handed to this task by the coordinator as the priority ahead of planned work, per AGENTS.md.

**Investigation, and the premise that had to be discarded first.** The stack said where, immediately: every one of the six threw at `runScript tests/scripts/ciStatus.test.ts:131:21`, which is `await import(\`${SCRIPT}?case=${caseId}\`)`, with no frame inside the imported module. So nothing in the reporting logic was failing — the module would not PARSE. The two cases that passed are the two instrument controls, which read fixtures and never call `import()`. The obvious reading, "Windows", is wrong and had to be killed before anything else could be believed: this same file passes on the authoring Windows machine and inside this repository's `npm run verify`, which is Windows. So the difference is not the operating system; it is something a long-lived local clone has that a clean runner does not.

**It is the working tree, and the measurement is stark.** Git for Windows installs `core.autocrlf=true` in its SYSTEM config — `C:/Program Files/Git/etc/gitconfig` on the authoring machine — and the GitHub-hosted `windows-latest` image is that same install, so `actions/checkout` writes every tracked text file into the runner's tree with CRLF while the index stays LF. A fresh `git clone` of this repository followed by `checkout ca24709b`, run on the authoring machine to reproduce the runner rather than to argue about it, writes `scripts/ci-status.mjs` with **475 carriage returns**; the long-lived tree beside it has **0**. Git says so itself, unprompted, the moment the file is touched: `warning: in the working copy of 'scripts/ci-status.mjs', LF will be replaced by CRLF the next time Git touches it`. The census of the authoring tree is the whole explanation in one line — index: 1,766 LF, 68 binary, 2 symlinks; **working tree: 932 LF, 810 CRLF, 24 mixed**. A long-lived clone is a patchwork, because a file written by an editor or an agent keeps LF and a file that was genuinely checked out holds CRLF, and git rewrites neither afterwards. `scripts/ci-status.mjs` had been authored the previous day and was one of the 932.

**Reproduced, then narrowed by a control.** Converting `scripts/ci-status.mjs` to CRLF in place and running `npx vitest run tests/scripts` gives `6 failed | 2 passed`, the same `SyntaxError: Invalid or unexpected token` at the same `131:21` — the runner's failure, exactly. The discriminating control is what makes the cause specific rather than "CRLF is bad": with the CRLF kept and only the `#!/usr/bin/env node` line replaced by an ordinary comment, the same suite is `8 passed`. Corroboration from a file nobody touched: `scripts/content-lib.mjs` is CRLF in the authoring tree and is a STATIC import in four content tests that have been passing throughout — it has no shebang.

**Root cause.** `vite@6.4.3`, `dist/node/chunks/dep-Dm0c1Wj2.js:25674`: `const hashbangRE = /^#!.*\n/`. `ssrTransform` uses it to find the offset at which to inject its `__vite_ssr_import__` calls, so that they land AFTER a shebang instead of in front of one. A JavaScript `.` does not match `\r`, so under CRLF the regex misses, the offset falls back to 0, and the injected imports are written in front of `#!/usr/bin/env node` — leaving a bare `#` in the middle of the module, which is what V8 is complaining about. The defect needs both halves: a shebang (18 tracked scripts have one) and a CRLF checkout (every Windows runner). Node's own ESM loader strips a CRLF shebang correctly, which is why `npm run ci:status` itself never failed anywhere.

**Fix.** A `.gitattributes` at the repository root: `* text=auto eol=lf`, plus `*.png binary` so a detection heuristic can never rewrite bytes inside an image. Every checkout on every platform now gets LF, which is what Linux was already getting and what made its leg green. Nothing else changed — not the eight cases and not their assertions, not the `node:child_process` seam that makes the script run whole, and not `scripts/ci-status.mjs`, whose shebang is correct and is shared with 17 other scripts. Deleting the shebang would have made this one test pass and left the class standing. Adding the file causes no renormalization churn: the index was already uniformly LF, so `git status` is unchanged by it.

**How this class is checked from now on.** `tests/architecture/checkoutLineEndings.test.ts` (`npm test`, so `npm run verify`), five checks reading `git ls-files --eol -z` — git's own attribute resolution, not a re-reading of `.gitattributes`. Two are instrument controls: the scan must parse every row, see more than 1,500 files and find three known paths, and it must resolve both a text row and a binary row, so a parser returning nothing or a constant cannot make the rest pass over air. The three substantive checks are that every tracked text file resolves `eol=lf`; that no tracked file is STORED with CRLF, since `eol=lf` governs what checkout writes and does nothing about a blob that already carries `\r`; and that the shebang scripts specifically — the files that actually broke — are text rows covered by both. The gate that replaced a Windows-only failure fails on every platform: with `.gitattributes` absent, 3 of 5 go red on Linux too.

**Proved red four ways** (recorded in [gate-proofs.md](gate-proofs.md)): removing `.gitattributes` entirely, the exact pre-fix state, `3 failed | 2 passed`; flipping `eol=lf` to `eol=crlf`, `2 failed`, naming all 1,768 files and all 18 scripts; adding `scripts/** binary` to sneak the scripts out of the guarantee, `1 failed` on the enumeration control rather than silently passing over an empty list; and writing a genuine CRLF blob into the index with `git hash-object --no-filters`, `1 failed` naming the file. Both files restored to identical SHA-256 afterwards.

**Bounds, named.** The gate speaks about tracked files and about what a fresh CHECKOUT produces; it deliberately says nothing about the working tree on this disk, because 810 files there are CRLF from checkouts that predate this commit and such a tree cannot reach CI, which always checks out fresh — a gate that went red for that would be deleted by the next person it annoyed. It says nothing about Vite's hashbang handling: if a future toolchain mishandles LF instead, it stays green. It cannot see a per-clone override applied outside the repository (`.git/info/attributes`, `core.eol`). And it does not prove the runner is fixed — only the next Windows CI run does that.

## 2026-09-05 — The command that checks the remote gate reported an earlier commit's run as the tip's, so a commit with no run at all read as green (found by the standing loop, FIXED and gated)

**Symptom.** `npm run ci:status` is the first thing AGENTS.md tells a session to run, and it prints one line per watched workflow. Each line describes whichever run is newest in that workflow's run LIST — whatever commit that run belongs to. A commit that produced no run is therefore invisible: the command prints a confident verdict about a different commit and nothing says so. Measured on the recorded fixtures: given a pushed commit whose runs API returns `total_count: 0`, the pre-fix script prints `CI: GREEN — 20+ consecutive, latest "Merge: motion smoothing survives a slow machine (v0.3.192)" at 2026-09-03T04:04:53Z` and **exits 0**. AGENTS.md names the principle in as many words — a gate that cannot tell "passed" from "did not run" reports the second as the first — and the instrument built to stop exactly that (the 2026-08-29 entry: 200 red runs over two months behind green local gates) had the failure inside it.

**Attribution.** Found by the standing loop while pushing; the owner reported none of it.

**Investigation, and a premise that turned out to be wrong.** The trigger was real: `39244310` ("Promote the final acceptance gate") was pushed and `gh api repos/{owner}/{repo}/actions/runs?head_sha=3924431040fab4898add984f16af131de13022c3` returns `{"total_count":0,"workflow_runs":[]}` — no run was ever created — and `ci:status` said nothing about it. The first reading was that GitHub had stopped creating runs. **It had not.** Both watched workflows filter their push trigger: `.github/workflows/ci.yml` runs on a push to main only for `package.json`, `package-lock.json`, `.github/voxel-commit` or `.github/workflows/**`, and `.github/workflows/playtest.yml` adds `playtest-corpus.json`. `39244310` changed one file, `AGENTS.md`. No run was due and GitHub was right not to create one. What was wrong is that `ci:status` could say neither thing — not "this commit was never gated, by design" and not "this commit should have been gated and was not".

A second thing was there to be read and was not being read. Where jobs DO exist and never start, GitHub states the reason on the job's check-run annotations, and today's reason was not the assumed one: the jobs of run 34011049865 carry, at `annotation_level: "failure"`, "The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings". The zero-steps/no-runner inference is sound but generic; "your payment failed" and "you are out of free minutes" call for different actions from the owner.

Sweeping the last 25 commits of main with the fixed classifier found no commit that owed a run and lacked one: 4 have their own run, 12 owed none, and 8 are interior commits of multi-commit pushes whose changes were carried into the run at the tip.

**Root cause.** One line, `const latest = finished[0];` in `scripts/ci-status.mjs` at `b1d0a0ff`. The report was bound to the newest entry of a run LIST instead of to a commit; no call in the whole script mentioned a sha. Nothing distinguished "the newest run" from "the run for the thing you are about to build on".

**Fix.** The report is now per commit — the local HEAD and, when they differ, the pushed tip — via `actions/runs?head_sha=<sha>`. A commit with no run of its own gets one of three answers instead of silence: NEVER ASKED (no run was due; the workflow's `on.push.paths`, read out of the workflow file as of that commit with `git show <sha>:...`, matches nothing that changed since the last gated ancestor — exit 0), CARRIED (a later push gated it at its tip; that run's verdict is reported), or UNGATED (a run was due and none exists — exit 1, in the pre-existing "unknown" bucket; red still means red, and COULD NOT RUN still exits 0). An unparsed workflow trigger or an unreadable file list is reported as UNGATED on purpose, because a filter understood only in part would answer "never asked" for a commit that was in fact never checked. GitHub's own annotation is now printed verbatim beside the never-started verdict.

**How this class is checked from now on.** `tests/scripts/ciStatus.test.ts` (`npm test`, so `npm run verify`), eight cases driven from responses recorded off the live repo with `gh` on 2026-09-05, with `node:child_process` as the only seam so the script runs as a whole rather than through a test-only entry point. Five hold the answers apart — UNGATED (the critical one: a pushed commit that changed `package.json`, runs API empty, must name the sha, say UNGATED, and exit 1), NEVER ASKED, COULD NOT RUN with the billing annotation quoted verbatim, GREEN, and RED with steps and a runner. A sixth is the over-fix guard: an interior commit of a multi-commit push must read CARRIED, not UNGATED, or the command would cry wolf on every commit but the tip. Two are controls on the instrument: the fake `gh`/`git` must throw on any command it has no fixture for (a stub that silently returns "" is how a check reports "did not run" as "passed"), and the recorded zero-run and never-started responses must still have the shape the cases depend on. Proved red by restoring `scripts/ci-status.mjs` to `b1d0a0ff`: 6 of the 8 fail, the UNGATED case on `expected +0 to be 1`.

**Bound of that gate, named. Fixtures are not the API.** Every response was captured from the real repo, but they are the projections the script's own `--jq` asks for, not raw payloads — so the suite proves the reporting logic and the exit codes, and proves nothing about whether GitHub still returns these shapes or whether `gh`'s flags still mean what they meant; a field rename on GitHub's side leaves it green. The only check on that is running `npm run ci:status` against the live repo. Beyond that: the YAML reader understands `on.push.paths` as these two workflows write it and refuses anything with `!`, a character class or a brace group (refusing means UNGATED, which is loud, not silent); `origin/main` is the locally cached ref, so a tip nobody has fetched looks like the tip; the filter is evaluated over the diff since the last gated ancestor, which is a stand-in for the push GitHub actually saw and is wrong for a force-push or a rewritten history; a `skipped` or `cancelled` run with no jobs still reads RED, as it did before; and nothing here watches the third workflow, `playtest-corpus-llm`.

## 2026-09-05 — Right-clicking the empty glass of the command bar sent the selected army to a cell hidden under it (found by the standing loop, FIXED and gated v0.3.209)

**Symptom.** On `aoe2-prototype` at 1280x800: select units, then right-click the empty dark glass of the bottom bar to the right of the training-queue cards — page (700, 700). Every selected unit takes a MOVE order to a cell hidden underneath the bar. Three real right-clicks there gave moves to (31,35), (27,33) and (27,35) on a 60x36 map. Left-clicking the same glass cleared the selection instead, and clicking a training-queue card did nothing but deselect the Town Centre. The bar is painted glass and reads as a control surface; it behaved like a hole in the window.

**Attribution.** Found by the standing loop, playing the game, not reported by the owner. None of this session's play findings were owner-reported.

**Investigation.** No engine tool renders the HUD — `replay:inspect`, `diffBundles`/`forkAt` and the path-queue and occupancy probes all read simulation state back out of a recorded bundle, and the question here is which DOM element the browser hit-tests at a screen point. So the instrument is `document.elementFromPoint`, the browser's own hit test, swept over every painted surface, plus real Playwright clicks recorded by a capture-phase listener on the world canvas. On the unfixed tree at 1280x800 with nothing selected, **1,065 of 3,567** painted points inside `.hud-bar` and `.hud-panel--selection` resolve to `canvas.voxel-world-canvas`; **383 of 1,875** at 800x600; **1,803 of 6,143** and **2,085 of 3,586** with a Town Centre queue, whose cards move the holes about.

**The leak is not the bar's whole box, which is why the fix could not go on the grid row.** `.hud-panel--selection` is `width: auto; justify-self: start` — a 330px slab against a 1,256px `.hud-bottom` row — so the gap beside it is genuine world and must stay clickable. `#hud-tooltip` passes clicks through as well, and correctly: it follows the cursor, so a tooltip that took the pointer would fight the mouse it is chasing.

**Root cause.** `#hud-root` is `pointer-events: none`, so the canvas sees the mouse through the HUD's transparent grid. `.hud-panel--selection` re-stated that gate and then opted eight child selectors back in BY NAME — the command deck, group and list, the build menu, its pages and page tabs, the selection summary, the command buttons. That was sound while the bar was only as big as its content: nearly every pixel it painted belonged to one of the eight. v0.3.207 made it a fixed painted band — 246px above 1120px wide, 236px below — and every pixel no listed child claimed became a hole. **The gate was written on the element that LAYS OUT rather than the one that PAINTS**, so growing the paint silently grew the hole.

**Fix.** `pointer-events: auto` moves onto the painted chassis rule in `hudChrome.css` — `.hud-bar, .hud-panel, .hud-footer`, the rule that sets `background-color` — so a painted surface cannot be added without it. The per-child opt-ins are deleted; the scroll regions keep their events by inheritance instead of by name.

**A test was already aiming through the hole, and it had been RIGHT until the morning of the same day.** `tests/browser/trade-route-reach.spec.ts` centred the camera on the enemy Market's anchor and right-clicked `worldToScreen(market.x + 1, market.y + 1)`, which is (400, 357.6) in the suite's 800x600 viewport. Under today's fixed band that point is 5.6px inside the bar: `elementFromPoint` returns `div.hud-panel.hud-panel--selection` and the canvas receives nothing. **Measured, not inferred**, on a build whose only difference from HEAD is `src/hudCommandPanel.css` restored to its `eb07296b` (v0.3.206) version, with the working tree's pointer-events fix left in place so the hit test reports the painted box honestly: with a Trade Cart selected the v0.3.206 bar was **196px tall with its top edge at y 392**, so the click point sat **34.4px ABOVE it, on open canvas** — `elementFromPoint` returned `canvas.voxel-world-canvas` and a real right-click delivered pointerdown, pointerup and contextmenu to it. The control that proves the swap took effect: under those same v0.3.206 rules the bar is 65px tall with nothing selected against 196px with the cart, where HEAD gives 236px for both, and that 65px matches the v0.3.207 entry's own recorded low end for this viewport. The spec dates from 2026-08-24 (`00c0b4fa`) — twelve days, not two months — and aimed at open canvas for every one of them; **v0.3.207 (`bf5ff47f`, three commits before this one, the same day) grew the bar over its click point**, lifting the top edge from y 392 to y 352, and this defect then hid the consequence, so the spec kept passing. An earlier write-up of this said the spec had been passing for two months on an action no player could perform, and that was wrong in degree and in kind: nothing was passing wrongly for two months, and the spec was not a bad test. A layout change silently invalidated a test's premise, and a second defect concealed it. The spec now centres the camera on the cell it clicks, which puts the click at (400, 319.2) with 32.8px of canvas to spare.

A census of every pointer event the whole browser suite dispatches — 195 tests served from the same `dist/` with a hit-test recorder injected, the instrument proved live against a known-swallowed and a known-good click — found no other spec aiming at a HUD-covered world point; the only rows came from this defect's own gate, which clicks painted HUD glass deliberately.

**How this class is checked from now on.** `tests/browser/hud-opaque-surfaces-swallow-clicks.spec.ts` (`npm run test:browser`, so `npm run verify`). It finds its surfaces at RUN TIME rather than by name: every element under `#hud-root` that is drawn, at least 8x8, with a computed `background-color` at least 50% opaque, and outermost on its branch — five per state. Paused at boot, at 1280x800 and 800x600, in three states (nothing selected; the Town Center with four villagers queued; every villager selected), it holds three things. (1) Every point on an 8px grid inside each rounded border box hit-tests to something inside `#hud-root`. (2) Real right-then-left clicks, at up to eight non-interactive points per surface, reach no listener on the world canvas, leave the selection byte-identical, and leave every selected unit's command unchanged. (3) A right click on open world AND one on the transparent part of the `.hud-bottom` row must both still reach the canvas, before and after each sweep — the over-fix guard, since making the grid row clickable would satisfy (1) and (2) while eating the clicks beside the bar. Red on the unfixed tree in every state at both viewports. The spec refuses a run that sampled too few points, so "no leak" cannot quietly mean "did not run".

**Bound of that gate, named.** Assertion (2)'s world-command half is CORROBORATION, not a reliable detector: it caught villagers retargeted from `move -> 17,18` to `move -> 5,21` in one red run and stayed green in another, because it fires only where the sweep's points resolve to a cell that issues an order, and it is vacuous with a building or nothing selected. Nothing here covers the hidden surfaces (the game menu, the load panel, the match summary, the tech tree, the compendium), `#hud-tooltip` (exempt by design — its `pointer-events: none` is pinned, so the exemption goes red rather than widening), other widths, a deeper production queue, the replay timeline's reserved height, keyboard or wheel input, or whether a swallowed click then does the right thing for the control it landed on. The grid stops one pixel short of each far edge, and a shadow drawn outside the border box is not swept. There is no pixel evidence in the gate: three capture pairs at `TICKS=1` diff at 0.00%, which is the evidence that the behaviour changed and the bar did not.

**Open, and the owner's call rather than a settled thing.** At 800x600 the band now covers the bottom 188px of a 480px canvas — about **39% of the visible world** — and that surface used to leak its clicks through to the map. How much play area the bar should occupy at small windows is a live question, not one this entry answers.

## 2026-09-05 — A villager right-clicked onto an unfinished building walked up to it and stood there (found by the standing loop, FIXED and gated v0.3.208)

**Symptom.** Right-click a villager onto a foundation that already exists — AoE2's way of rescuing an interrupted build and of adding builders to speed one up — and the villager walked to the site and did nothing for the rest of the match. `bridge.issueContextCommand` returned **true** both before and after the fix, so the HUD said the order had taken. A build that stalls looks, from the panel, exactly like a build that is going.

**Attribution, and the honest half.** Found by the standing loop, playing the game; the owner reported none of this session's play findings. **The mouse half of the original observation could NOT be reproduced.** What is proved here is the CELL route, driven through `bridge.issueContextCommand` in simulation. Which of right-click's two routes a given mouse pixel lands in is decided by the entity hit test in `src/input/`, and that was not instrumented — so the original observation is more likely the OTHER defect this session found, where a painted HUD surface swallowed the click entirely (measured and fixed on a branch, not shipped: see the devlog for 2026-09-05). The defect recorded here is real and is fixed; it is simply not certain to be the one that was seen.

**Investigation.** Right-click is routed TWICE. `contextAtEntityRouter` handles a click the entity hit test claims, and it has had a build branch since 0.1.17 (multi-villager construction): it reads the target's construction state, and if the building is incomplete it queues a `build`. That route always worked, and `tests/simulation/multiVillagerConstruction.test.ts` has always covered it. `contextRouter` handles the click on a ground CELL — the route the pointer falls back to whenever the hit test does not claim the click — and it had no build branch at all. The click fell through every branch to a plain move onto a cell the foundation itself blocks, which is why the villager arrived at the site and stopped: it had been told to walk somewhere it cannot stand.

**Root cause.** A rule that belongs to RIGHT-CLICK was implemented on one of right-click's two routes. Nothing in the shape of the code makes the two routes agree, and the entity route's coverage is what kept the gap invisible: the mechanic had a passing test, on the half that worked.

**Fix.** `findOwnedConstructionSiteAtCell` in `selectionFinders.ts` — any cell of the footprint counts, not just the anchor, because that is what the player clicks, and there is no visibility gate, because an owner always knows where its own foundation is (which matches `findOwnedGarrisonBuildingAtCell` beside it). `contextRouter` gains a villager-only branch that must precede the garrison branch, since a foundation cannot be garrisoned. `setUnitBuildCommandDirect` returns false on any precondition miss — non-villager, foreign owner, building missing or already complete — so both routers fall through on a miss instead of swallowing the click.

**How this class is checked from now on.** `tests/simulation/buildExistingFoundation.test.ts` (`npm test`, so `npm run verify`), three cases, every one asserting REAL PROGRESS — `buildProgressTicks` climbing to `totalBuildTicks` — never that a task was assigned, because a task that is assigned and never runs is precisely what this defect looked like. (1) A villager standing adjacent to an orphaned foundation, on three building types (house 2x2, mill 2x2, barracks 3x3), must show progress within 20 ticks: no walk is needed, and the plain move could never satisfy that because it produced no path at all. (2) A villager ordered from the opposite corner walks there and finishes, and is asserted to be standing OUTSIDE the footprint at the end, so it got there under its own steam rather than being teleported onto the site — this is the case a move-only route appears to handle, because the villager does walk, and then never builds. (3) Two runs of one fixture that differ ONLY in whether three more builders are ordered onto the site through the same cell click, with every villager parked against the building in both arms so neither run differs by how far anyone walked: the crew must finish the remainder in under 75% of the solo time, which is the AoE2 crew curve `3*base/(n+2)`.

**Bound of that gate, named.** Only the CELL route is exercised; nothing here says which route a given mouse pixel lands in, and the entity hit test is the browser suite's job. Three building types on the flat all-grass `multi-villager-construction-fixture`, owner 1, AI disabled, Dark Age, no build-rate technology and no Incas or Treadmill multiplier — so it does not cover terrain that blocks the approach, a foundation under fog, an ALLY's foundation, or a non-villager actor. Progress is read from `getEconomyState()`, so it proves the simulation advanced the site, not that the HUD or the renderer showed it. The multi-builder case asserts a RATIO between two runs of the same fixture, not an absolute tick count, so it cannot detect a uniform slowdown.

## 2026-09-05 — A finished playtest threw its own recording away at V8's string ceiling (found by the standing loop, FIXED and gated)

**Symptom.** `npm run playtest` simulates a full match — the default is 30,000 ticks, tens of minutes of wall time — and then dies on the last line with `RangeError: Invalid string length`. The bundle is never written, so the run is gone: nothing to replay, nothing to inspect, nothing to run the oracles over. The same wall stands on the way back IN, which is worse, because a bundle that was written can still be unreadable. `scripts/playtest-corpus.mjs` read one with `JSON.parse(readFileSync(...))`, so a long enough run turned the progression gate into an ERROR row on exactly the runs it exists to check.

**Attribution.** Found by the standing loop, playing the game, not reported by the owner. None of this session's play findings were owner-reported.

**Investigation.** Measured rather than guessed, on 2026-09-03's 20,001-tick corpus bundle: `ticks[]` is **95.9%** of the file at a mean **15.7 KB per tick entry** (of which `diff` 84% and `metrics` 16%), the whole file is **313 MB** at 20,001 ticks, and entries grow as the match fills with units — so the 30,000-tick default lands past V8's cap of **536,870,888** characters. That number is `buffer.constants.MAX_STRING_LENGTH`, and it bounds `JSON.stringify` and `JSON.parse` alike because each operates on ONE string.

**Root cause.** Nothing about the data was wrong: the document was simply larger than a single JavaScript string may be. A comment above the failing line already recorded the same failure being mitigated once, by dropping `null, 2` — which costs about 40% and MOVED the wall rather than removing it. The 2026-09-02 register entry above records the previous instance of exactly this, mitigated the same way.

**Fix.** `src/game/playtest/bundleIo.ts` — `writeBundleFile` and `readBundleFile` — never holds the whole document as one string in either direction. The largest string either side ever builds is ONE array element (a tick entry, about 16 KB) or one non-array top-level value (`initialSnapshot`, about 0.5 MB). **The format is unchanged.** The file is ordinary JSON, byte-identical to what `JSON.stringify(bundle)` produces, so bundles recorded before this still load and anything else that reads bundle JSON still works. The document was not shrunk, sharded or re-encoded; only the serializer changed. Both directions are wired at every call site that touches a bundle: `playtest.mjs` writes through it, and `replay-inspect.mjs`, `run-oracles.mjs`, `playtest-corpus.mjs` and `playtest-findings.mjs` read through it (`playtest-findings.mjs` both ways, since it injects markers and writes the bundle back). **That last sentence was false when it was written — see the correction at the end of this entry.**

**How this class is checked from now on.** `tests/playtest/bundleIo.test.ts` (`npm test`, so `npm run verify`), six cases. The gate is the oversized one: a payload sized from `buffer.constants.MAX_STRING_LENGTH` **at run time** rather than from a baked-in number, so it keeps crossing the cap if a future Node moves it, built from one shared 8 MB string referenced many times so it costs 8 MB of heap and still serializes past the cap. It carries three controls that stop it reporting "did not run" as "passed": the written file's size must exceed the cap, `JSON.stringify` on the same payload must throw `Invalid string length`, and `readFileSync(path, 'utf8')` on the written file must throw too. A payload that quietly shrank fails there instead of passing everywhere. A separate case binds the CALL SITE by reading the text of `scripts/playtest.mjs` — the module can be correct while the CLI still calls `JSON.stringify(result.bundle)`, which is exactly how the run was lost.

**Bound of that gate, named.** The payloads are SYNTHETIC. They prove the serializer survives a document past the cap; they prove nothing about the simulation that produces one. A recorded 30,000-tick run is the only thing that proves the game still records, and that is `npm run playtest` plus `npm run replay:inspect`, not this file. The call-site case is a source check — it sees the text of one script, not a run of it, and it names only `playtest.mjs`; the four readers are wired but not pinned that way. **Superseded 2026-09-06 — see the correction below; the call-site check no longer names a script.**

**CORRECTED 2026-09-06 — "wired at every call site" was not every call site, and the bound above is why nobody noticed.** Two scripts were missed, and between them they held four more whole-bundle strings. All four are gone; the paragraphs above are left as they were written.

`scripts/playtest-llm.mjs` held THREE, and they sat on the expensive path — the LLM playtest spends model calls for tens of minutes and then exports, so every one of them threw only after the run had been paid for. (1) The bundle was stringified whole INSIDE the browser page: `window.__AOE2_PLAYTEST_BUNDLE_TEXT__ = JSON.stringify(bundle)`, which builds the entire document in the page's heap before anything is transferred. (2) The 4 MiB chunks pulled back out of the page were rejoined into one Node string, `JSON.parse(parts.join(''))`. (3) The result was written with `writeFileSync(\`${args.out}.json\`, JSON.stringify(result.bundle))` — the same line, verbatim, that the fix above had just removed from `playtest.mjs`. The chunking read as a fix for this exact problem and was not one: it bounded the JSON-RPC transport, never the document, and (1) had already built the full string before the first chunk was requested.

`scripts/playtest-self-improve.mjs` held the fourth, and it is the one that explains the miss. Its read was `const bundle = readJson(\`${prefix}.json\`)` — a generic three-line helper, `JSON.parse(readFileSync(path, 'utf8'))`, used four times in that file for artifacts that are all small except this one. Nothing at the call site said "bundle" next to a `JSON.parse`, so neither the review nor the first gate could see it: the gate was a source check looking for the shapes it had been shown, and a wrapper is exactly the shape it had not. This one is a READ ceiling — the bundle was written correctly and was then unreadable.

**The gate that replaces the one-script check.** `tests/playtest/bundleIo.test.ts` now scans every `scripts/*.mjs` (34 files today) against four forbidden shapes rather than naming a file: `JSON.stringify(<...>bundle`, `JSON.parse(readFileSync(<...>bundle`, a `const bundle = <anything but readBundleFile>(` — the wrapper shape, added for the fourth ceiling — and `JSON.parse(<...>.join(`, the rejoin. Each carries the sentence a reader needs (`builds the whole bundle as one string; write it with writeBundleFile(path, bundle)`). It was proven red against this repository's own pre-fix `playtest-llm.mjs`, naming all three of its ceilings by line number, and it carries a control case that feeds the four patterns five verbatim historical lines and five correct shapes, so a pattern that stopped matching anything fails there instead of passing everywhere. A positive half asserts `playtest.mjs` and `playtest-llm.mjs` do call `writeBundleFile`.

**Bound of THAT gate.** Still a source check: it reads the text of the scripts, never a run of them, it knows only the four shapes, and it covers `scripts/*.mjs` only — `src/` and `tests/` are outside it. A bundle funnelled through a helper it cannot see (a `readAll()` two modules away, a dynamic `fs[method]`) passes it, which is the fourth ceiling's own trick one level further out. The gate's in-file header says "the three shapes below" while `FORBIDDEN` holds four; the code is right and the sentence undercounts it by exactly the newest entry.

**The size claim in two source comments is wrong and the margin is still large.** `scripts/playtest-llm-host.mjs` and `src/game/playtest/bundleIo.ts` both call 4 MiB "three orders of magnitude under" the 536,870,888-character cap. It is 128x, about 2.1 orders. Recorded rather than fixed here because the numbers themselves — 4,194,304 against 536,870,888 — are correct in both files and only the ratio is overstated.

## 2026-09-05 — The bottom command bar was a different height for every selection (owner-reported, FIXED and gated)

**Symptom.** "The info panel at the bottom should not have different heights for different units. This is visually confusing when switching between units." Click nothing, a villager, the scout, the Town Centre, a sheep: the bar's top edge lands somewhere new each time while the minimap beside it stays put.

**Investigation.** No engine tool renders the HUD; the instrument is the gate itself plus two DOM probes under `tmp/probes/`. Unfixed, at 1280x720: 72px empty, 114.2 tree, 144.4 sheep, 185.6 villager, 212.6 scout, 242.8 Town Centre; at 800x600: 65 to 232 (the queued Town Centre and the box sat on the old 252px ceiling). The first pass's premise that the villager card is the tallest was false — the Town Centre's summary is: a two-line name beside its portrait and four rows of chips (`0 / 15 garrisoned` is 176px wide). Found in passing: the compact multi-select grid could never wrap (`auto-fill` in an auto-width column resolves to one column), so a box selection of several kinds stacked 269px tall in every regime — taller than any lawful band.

**Root cause.** `.hud-panel--selection` had a `max-height` ceiling and no `height`, and the bottom row is bottom-aligned, so the bar was as tall as its conditional content and its top edge followed it.

**How it is checked from now on.** `tests/browser/selection-panel-height.spec.ts` (`npm run test:browser`, so `npm run verify`): `aoe2-prototype` paused at boot, at 800x600, 1024x768, 1280x720 and 1920x1080, 17 states in a fixed order (nothing; a villager on both build pages and placing a House; the scout; the Town Centre empty and with one villager queued; a House foundation; sheep, tree, berry bush, gold mine, stone mine; every villager; a mixed box; nothing again), each asserting its selection took effect, then height and top edge within 0.5px of the first state and `scrollHeight ≤ clientHeight + 1`. Red on the unfixed tree in all four viewports. Bound: the boot fixture only (a Castle's or Market's card scrolls under rule (b), held by `command-deck-fits.spec.ts`); one named exception — the queued Town Centre at 800x600 (261px of content in a 234px band, because the wrapped bar puts the queue on a line of its own) is held for height and top edge only, and the spec asserts it still overflows so a compact queue retires the exception; no queue deeper than one; no width between the four viewports; no window shorter than the band (the ceilings then clamp every selection alike); no replay timeline; no pixel — the capture set recorded in `docs/debugging/2026-09-05-hud-panel-height.md` is the pixel evidence.

## 2026-09-05 — Soldiers walked with their legs in the rest pose (owner-reported, FIXED and gated)

**Symptom.** "Soldiers are not moving their legs during the walking animation." Infantry, archers — and, read the same way, villagers and cavalry — translated across the map with boots level and legs vertical; the walk was a slide.

**Investigation.** The real game first (`unit-showcase-fixture`, five units ordered south, `__AOE2_TEST__.inspectVoxelUnitMotion` per half tick): every unit `moving`, gait phase advancing, `locomotionWeight` 0.19–0.51. The pose path has no role branch and no attack, cache or armour explanation; the number was the whole story. `FULL_LOCOMOTION_SPEED = 2.5` normalised the weight against the pre-§12.4.2 uniform clock. v0.3.160 moved every unit to its own rate (0.8 villager, 0.9 militia, 1.35 knight) and nothing re-normalised the gait, so it played at 0.2–0.6 of the authored amplitude — under a pixel of boot travel at default zoom — for eight days across every walker in the game. Four assertions across three live test files asserted `locomotionWeight > 0`; none asserted it reached the amplitude the pose was authored for.

**Root cause.** A presentation constant carried a simulation rate that the simulation had stopped using; the gait weight was a ratio to a speed no unit could reach.

**How it is checked from now on.** `tests/rendering/aoeVoxelUnitWalkLegs.test.ts` (`npm test`, so `npm run verify`) drives every UnitType at its base rate computed from the sim constants — not from the renderer helper the fix added — and requires weight ≥ 0.9 and, for humanoid and mounted roles posed through the real recipes, a boot/leg/horse-leg travel between stride extremes of at least 0.25/0.11/0.3 × recipe scale. Red on the unfixed tree (158 failed of 159: all 93 UnitTypes on weight, all 65 legged types on travel; the fastest humanoid, a Woad Raider at 172 %, reached weight 0.550 and a boot travel of 0.178 against 0.25). Bound: base rate only, straight line, no technology, no slowed unit, no siege wheel or monk sway, and no pixel — the capture pair recorded in `docs/debugging/2026-09-05-walk-legs.md` is the pixel evidence.

## 2026-09-05 — Six closed threads sat in `docs/threads/current` for up to seven weeks, and four pointers named threads moved months earlier (owner-reported, FIXED and gated)

**Symptom.** The owner asked whether the project really had so many open threads. Eight folders sat in `docs/threads/current` against 94 in `done/`.

**Investigation.** Every thread's DESIGN, PLAN and REVIEW read against the spec's status lines, the changelog, the devlog and git. Six were closed: `animation-feedback-batch` (all four slices shipped 2026-07-15, v0.2.7 to v0.2.9), `auto-mine-after-camp` (v0.2.6; the folder was created by the release commit, whose title said "close"), `unit-occlusion-outline` (v0.2.5; its PLAN's last step, the move to `done/`, never ran), `recursive-loop-closure` (shipped 2026-07-08, then dismantled by the 2026-08-01 decision that removed the fix arm), `moebius-art-style` (v0.3.10; Moebius became the only style in v0.3.196) and `render-realism-2026-09-02` (scoped to one improvement, shipped v0.3.193). Two are open: `concurrency-review-2026-09-02`, whose own status block lists what remains, and `lumber-camp-routing`, which had been moved to `done/` and back on 2026-09-01 and whose record stopped at round 5 while v0.3.191, v0.3.204 and v0.3.205 landed against it. Four pointers were stale before any move: `src/game/playtest/findingsToMarkers.ts` and `tests/browser/replay-scrub.spec.ts` named `current/` paths for threads long in `done/`, and `docs/architecture/drift-log.md` had two more. The 2026-06-09 housekeeping review (`docs/threads/done/thread-housekeeping`) had swept this class once, in `src/` only: `replay-scrub.spec.ts`'s pointer (2026-05-07) sat outside that sweep and survived it, and `findingsToMarkers.ts`'s was written six days AFTER the review by `b3212417`, the very commit that moved its thread to `done/`. The class recurs by construction: a move is a rename, and the commit that renames is the one most likely to write the old path.

**Root cause.** Closing a thread was the LAST step of a plan ("move thread folder to done") rather than part of the shipping commit, and nothing read `current/` at the end of a task, so a thread not moved in its own commit had no later occasion to be moved. The pointer half recurred for the same reason: a move is a rename, and nothing checked that the paths written into code still resolved.

**How it is checked from now on.** `tests/architecture/threadHygiene.test.ts`, in `npm test` and so in `npm run verify`, three checks. (1) Every `docs/threads/(current|done)/...` path referenced from `src/`, `tests/`, `scripts/`, `design/`, `docs/architecture/`, `docs/policies/`, `docs/learning/`, `docs/engine-feedback/`, `AGENTS.md` and `README.md` must exist on disk with its exact casing (Windows accepts a wrong case that Linux CI rejects), and the scan must find at least three real pointers in each of `src`, `tests`, `design` and `docs/architecture`, so dropping an extension or a root goes red instead of passing on less. `docs/devlog/`, `docs/changelog.md`, `docs/debugging/` and `docs/threads/` are exempt by this entry's decision, not by any earlier ruling (the 2026-06-09 review ruled only that source comments are NOT exempt): they narrate moves and pending states as they were, so a literal scan of them cannot be satisfied without falsifying the narrative. They hold 55 stale pointers today; the changelog's seven "see" pointers to long-moved threads were fixed by hand this once, and its one narrative line ("the folder moves from `current/` to `done/`") was left as written. (2) No file under `current/` may carry a closure line, `Closed YYYY-MM-DD:` at the start of a line: the ritual writes that line and moves the folder in one commit, so a closure line still under `current/` is the move forgotten — and without this check the closure line's own date would keep (3) green forever. (3) Every thread in `current/` must carry a `YYYY-MM-DD` date, in a file or a folder name, within 30 days of the reference clock, the newer of HEAD's commit date and the newest date in `docs/devlog/summary.md`, so a thread that a month of commits has not written a dated line into goes red until it gets a status line or a move. All three proved red in `gate-proofs.md`, with the six threads run as a matrix. Bound, stated plainly: (2) catches only a thread closed by the ritual, and (3) is a 30-day tail — a thread closed WITHOUT a closure line and left in `current/` is caught only after 30 days of commits, and two of the six (moebius at 19 days, render-realism at 3) would have been green under (3) alone on the morning this was found. (1) sees literal paths in the listed roots only. (3) reads dates as text: a deadline or a version's date counts as maintenance, and a status line that is false but dated today passes. The rule: `docs/policies/local-rules.md`, "A thread is closed by the task that closes it".

## 2026-09-05 — Fourteen loaded carriers freeze head-on on Nomad under the walk ranking (FIXED v0.3.205, merged on the owner's decision)

**Symptom.** Found by an independent critic of v0.3.204, not by any gate: on Nomad, owner 1's wood force goes 6 to 15 villagers between ticks 21,000 and 22,500 with no deliveries, and from tick 24,000 to 45,000 fourteen of them stand on the two cells (14,14) and (15,14) holding full loads, zero loads delivered, shuffling between the two cells — 47% of the match. `npm run ai:gather-walk` had reported the seat as "walking 85% of the time", because anything that is not chopping or idle counted as walking; the instrument now carries a `frozen` column for this — a walking task that has not left a two-cell square for 750 ticks. Its first version restarted the clock on any cell change and was blind to exactly this jam (the carriers change cell 127 times per 400 ticks between the two cells — a second critic caught it); the box is what makes a shuffle count. Under this commit the column reads Nomad owner 1 at 7.7 frozen villagers per sample averaged over the match — fourteen from tick 24,000 on — with the longest jam at 28,397 ticks, and owner 2 at 1.2 per sample with one carrier jammed for 17,222 ticks.

**Investigation.** With the game's own planner exposed beside the independent walk probe: the two cells are not sealed (probe distance 8 from the Town Centre ring, inside a 1,259-cell component that touches the ring). The carriers at (14,14) hold an approach plan whose next step is (15,14) and the carriers at (15,14) hold one whose next step is (14,14). The full paths: from (14,14) a 27-step loop east, south, west and north around the base to ring cell (10,11); from (15,14) the 10-step route west THROUGH (14,14) to ring cell (11,10). `findMovementPathToCandidates` returns the first ring cell with ANY path in Manhattan order, and for (14,14) the Manhattan-nearest ring cell is reachable only by the loop. Two groups, mirror-image routes, each into the other's cell; the traffic arbiter admits nobody (the head-on class already open under "seed-7 is TWO defects").

**Root cause.** The deposit leg measures a distance the units cannot walk, the same defect as the ranking's, one level down: the Manhattan-nearest drop-off from the carrier's live position, then the first ring cell an A* reaches in Manhattan order. The walk ranking exposes it by sending every wood villager on this wood-starved map to the same two reachable trees, fourteen carriers through one spot.

**The fix, and why it is not merged.** Branch `deposit-walk-field`: the deposit leg descends the walk field's gradient from the carrier's own cell (`descendFrom`) — the true shortest route to the walk-nearest ring cell of the walk-nearest drop-off, identical for every carrier on a cell, no per-carrier search; the Manhattan-plus-A* search stays as the off-field fallback. Gated by four descent cases in `dropOffWalkField.test.ts` and four in `dropOffStepWalkField.test.ts`; measured on all nine maps: Nomad chop 8% to 37%, wood 981 to 1,783 (baseline 1,735); misranked 0-7% on every seat; three of nine matches resolve against two. (The branch's "frozen 0.0" reading was taken with the blind first version of the column and is not evidence; the chop and wood numbers are.)

**What the branch does to the boot map, and the decision it needs.** Owner 1 reaches Castle at 21,250 (31,250 under this commit, never before this work), trains 68 military to a peak of 15, and conquers owner 2 at 39,812. Owner 2 collapses: it never leaves Feudal, peaks at 3 military, keeps 8.4 villagers on average (21.7 under this commit) and loses 30 of them, delivers 1,663 wood against 4,905. The union of what self-play exercised on the map falls from 14 building types and 9 unit types to 11 and 6. Two 45,000-tick gates go red. `villagersAvoidEnemyDefencesSelfPlay.test.ts` demands 40,000 censused ticks; its own window ends at 39,191, the census covered it (0 villagers under enemy defences), and the 40,000 is a proxy for "covered the window" — a horizon artefact of the gate, and asserting the window end instead would not be moving a bar. `aiReachesCastleAge.test.ts` reads the winner's variety as 9 building types and 6 unit types against bars of 13 and 9 that were derived from owner 2 winning an unresolved match; most of the drop is the winner slot flipping (owner 1 shows 10/6 under this commit too, unscored because owner 2 reaches Castle first), but one seat never leaving Feudal is the thing that gate exists to refuse, so its refusal is defensible. Not measured, and the fact the decision turns on: whether owner 2's collapse is owner 1's stronger economy raiding earlier, or a deposit-leg defect on owner 2's side — its `tier` cost reads 13.4 cells under the branch against 4.0 here, which is a lead. The branch waits for that measurement and for the owner: re-derive the contracts for a match that resolves, or say the conquest is the wrong outcome.

**The decision, and the measurement it turned on (2026-09-05).** The owner: merge, re-bound the two gates to the match's resolution, and find another way to cover the content. The unmeasured question — owner 2's collapse from raiding or from a deposit-leg defect on its own side — was measured before merging, with a probe that read both economies every 1,000 ticks and every owner-2 villager death with its task and the nearest enemy building. Owner 2 tracks the old tree to tick 23,000: 22 villagers, 21 buildings, every drop-off kind standing, its lumber camp up until 25,000. Then it loses twenty villagers between ticks 22,896 and 23,600 at its own base, every one of them building, gathering or hauling, with owner 1's army at thirteen after a Castle Age at 21,250 and owner 2's at three. A raid. The `tier 13.4` reading is what a camp-less economy hauling to its Town Centre looks like afterwards, not a cause. No deposit-leg defect on owner 2's side. The boot-map instrument in both arms (`ai:gather-walk`, 45,000 ticks), which a critic found missing from the first draft of this paragraph: owner 1 chop 36% → 34%, wood 4,518 → 4,119 (0.100 → 0.104 per tick over a shorter match), tier 4.2 → 5.2, `frozen` 0.4 → 0.7 per sample with the longest freeze 1,430 → 4,201 ticks. So the map this decision was measured on reads WORSE for owner 1 in the column built for this defect. The Nomad gain (chop 8% → 37%, wood 981 → 1,783) is the branch's own measurement, valid for the merged code because the five deposit-leg files are byte-identical to the branch, and not re-measured here. The owner-1 freeze rise is the head-on class this register already holds open ("seed-7 is TWO defects") and is recorded there as an OPEN observation, not explained. Why owner 1 reaches Castle 10,000 ticks earlier is NOT measured either: its wood is flat-to-down, its Archery Range lands at 19,500 instead of 29,750 and the Castle follows it by 1,750 ticks in both arms — the fix reorders the match, and an outcome is not a mechanism. Cost: the field's 917 rebuilds took 240 ms (0.19% of step); the served path — a building scan and an O(route) descent per loaded carrier per tick, 254,429 calls — is NOT timed by the instrument; ms per thousand unit-ticks rose 62 → 81 with fewer units on the map, unattributed; whole-match ticks/s (339 → 324) is a sanity check and never a cost figure (local rule, 2026-09-05).

**Gates, re-bound.** `aiReachesCastleAge` stops when the match resolves and scores the side that WON it, the earliest to Castle only while it is still running; its variety COUNT bars left, because on a resolved match they measure the match's length (owner 1 read 10/6 under the old tree too, unscored because owner 2 reached Castle first), and a NAMED union floor for the shipped configuration stands in their place — 11 building types and 6 unit types over both seats at resolution — after a critic pointed out that dropping the counts left the configuration players get with no coverage contract at all. `villagersAvoidEnemyDefencesSelfPlay` accepts a conquest — one side wiped off the map, read from the survivors among the two players rather than from the outcome — as proof the endgame it measures over happened; any other early resolution still fails, and a bug that deletes a side reads as a conquest, which is what conquest means. The wider floor is scored in `selfPlayContentCoverage.test.ts` under the no-attack lab (spec §15.8), by name. Seen on the way and NOT fixed: owner 2 loses twenty villagers to a raid in 700 ticks while holding 21 buildings and three military — the AI's defence on this map is the next thing to look at.

## 2026-09-05 — `../civ-engine/.git` emptied down to `refs/` by removing an aoe2 worktree (owner-reported, REPAIRED 2026-09-05)

**Symptom, as the owner reported it.** "How can civ-engine not be a local repo? It is definitely available locally." — after the previous handoff said `../civ-engine` had no git metadata.

**Investigation.** The working tree is complete: every source, dist and docs file is dated 2026-09-02 or older. `.git/` holds only `refs/`; `HEAD`, `config`, `index`, `objects/`, `logs/`, `hooks/` and `packed-refs` are gone. The directory's mtime is 2026-09-03 23:27:25.380. This session's transcript has exactly one command finishing in that second: `git worktree remove --force <scratchpad>/main-control`, issued 23:27:22, failed 23:27:25 with `Filename too long`. Twenty seconds earlier that worktree had been given `node_modules` by `ln -s <aoe2>/node_modules node_modules`. In Git Bash without `MSYS=winsymlinks:*`, `ln -s` on a directory makes a COPY, not a link, and the copy reproduced `node_modules/civ-engine` as a junction to `../civ-engine`, because npm expresses a `file:../` dependency as a junction. The half-removed worktree still on disk proves the path: every entry sorting before `node_modules` is gone and every entry after it survives; inside the copied `node_modules` only `civ-engine` — the junction — survives. So the delete walked in name order, reached the junction, followed it as a directory, and removed `../civ-engine/.git` entry by entry, `COMMIT_EDITMSG` through `packed-refs`, until `refs/codex/turn-diffs/checkpoints/<64-hex>` made the path 267 characters, past Windows' 260, and the error aborted the whole removal. That abort is the only reason `refs/` survived and nothing after `.git` inside `civ-engine` was touched.

**Root cause.** Git's recursive delete on Windows follows directory junctions; npm links every `file:../sibling` dependency as one; a copy of `node_modules` inside a throwaway worktree carried that junction into the sibling repo. `aoe2/node_modules` holds two such junctions, `civ-engine` and `voxel`. `voxel` sorts after `civ-engine`, which is the only reason it was never reached.

**What is lost and what is not.** Nothing on `main`: `refs/heads/main` and `refs/remotes/origin/main` both read `e418a051`, origin still serves that commit, and a fetch restores every object behind it. Also intact: the working tree, any uncommitted edit in it (it shows as a modification once the index is rebuilt), and `refs/tags/engine-dist` = `86ad7918`, which matches origin. Lost: the reflog, the local config (remote URL, hooks, any local setting), and the local-only branch `claude/festive-aryabhata-19b494` at `e0cb614a` (2026-07-10, never pushed under that name), whose commits are now unreachable. The root directory's mtime also moved at 23:27:25, so at least one top-level entry sorting before `.git` was removed first — the repair found it: `.cursor/`.

**Repair (2026-09-05, on the owner's "Ok repair").** In place, never a re-clone: `git init` inside `../civ-engine` recreated `HEAD`, `config` and `objects/` and kept `refs/`; `git symbolic-ref HEAD refs/heads/main`; `git remote add origin`. The first `git fetch` then failed with `did not send all necessary objects`: the surviving loose refs still read `e418a051`, so the client reported `origin/main` as up to date and never asked for it, and the connectivity check tripped on the object it did not have. Turning negotiation off (`fetch.negotiationAlgorithm=noop`) did not help, because the skip happens before negotiation. Removing the four stale loose refs (values recorded above, copies in the scratchpad) made the fetch request everything; `git update-ref refs/heads/main e418a051` and `--set-upstream-to=origin/main` put `main` back; `git reset` (mixed) rebuilt the index without writing a file; `git fsck --no-dangling` exits 0. `git status` then showed exactly two entries. `D .cursor/hooks/state/continual-learning.json` — the tracked entry sorting before `.git` that the delete took first, restored from HEAD with `git checkout --`. `M AGENTS.md` — the 2026-09-04 fleet canon sync (`9c923a21c1ca` → `1f13d9f9e0be`), which had been generated into the tree but could not be committed that day; committed as civ-engine `f123904` and pushed. Anything gitignored under `.claude/` (the pattern `/.claude/*` is ignored there) sorted before `.cursor` and is gone without a record, if it existed.

**How this class is checked from now on.** The class is "a recursive delete crossed into a sibling repo through a link". No test in this repo can see it, because the act is a shell command, not a code path. Two things now stand in its way: the fleet rule staged in `../fleet/canon-candidates.md` — never link or copy `node_modules` into a tree git will remove, and unlink every junction with `rmdir` (never `rm -r`, never `Remove-Item -Recurse`) before any recursive delete — and the local rule "A/B control builds" in `docs/policies/local-rules.md`. The gate that would turn this into a mechanism is a fixed command for the whole A/B dance, `scripts/controlWorktree.mjs`, which makes the junction with `mklink /J` and removes it with `rmdir` before `git worktree remove`. It is not written yet.

## 2026-09-04 — An enemy that garrisons its last villagers pins the attacker's whole army forever (FIXED)

**Symptom.** The user's priority 2, "matches still do not resolve". On `gold-rush` at 60,000 ticks owner 1 holds **151 units, every one of them idle since tick 30,000**, 45 cells from owner 2's 14 remaining buildings. Owner 2 has no unit on the map. The match reports `running`.

**Which arm that reading came from, corrected 2026-09-04 after a critic asked.** It was taken with the camp-anchor radius ALREADY at 20 — the two changes in this session shipped together, and this one was investigated on top of the other. In the true baseline `gold-rush` resolves at 34,704 with an army of 17, so the 151-unit tableau never existed in a shipped build. It is the symptom of an army this AI can now field, and it does not weaken the defect: the mechanism is arm-independent and the mutation proof below is taken on a fixture that has no camp at all.

**Investigation, including two hypotheses that were wrong.** The attack phase was reached and wanted to push: tracing it every decision tick from tick 27,000 gives `group=136 thr=7 push=true targetOwner=2 tcId=2185`, so the army was mustered and the enemy Town Centre was a live target. The first hypothesis was the army being sealed inside its own base by 58 buildings; ordering units by hand through `bridge.pendingCommands` refuted it — a Knight walked **49 cells to distance 2 of that very Town Centre**, and every one of owner 2's 14 buildings was reachable. The second was the `!buildingApproachPlan -> clearUnitCommand` path in `attackCommandStep`; instrumenting it caught **zero** clears. Instrumenting `setUnitCommand` and `clearUnitCommand` then showed the command was never SET in the first place.

**Root cause.** `runAttackPhase` prefers the target enemy's villager, and `findOwnedUnit(targetOwner, 'villager')` returned villager 2448 — alive, and **garrisoned**. `garrisonUnit` strips a unit's position component, and `setUnitAttackCommandDirect` returns false without a target position: a **silent no-op**, exactly as its own comment describes. The validator passes (the entity is alive and has a `unit` component), so nothing is rejected and nothing is logged. Every decision tick, all 136 units were ordered onto a target that cannot be engaged, the order was dropped, and the branch `continue`d before the visible-building and Town-Centre fallbacks that would have worked.

**Fix.** `findOwnedUnit` -> `findOwnedUnitOnMap`, querying `('unit', 'position')`. A unit with no position cannot be walked to, attacked, or engaged in any way, so handing one back as "a unit of this type" is a trap for every caller; it had exactly one production caller and the rename makes the contract impossible to miss.

**Measured, `SEEDS=arena,fortress,gold-rush,coastal TICKS=60000 SAMPLE=500 npm run ai:army-block`.** Matches resolving goes from **one of four to three of four** — the four-arm table is in the entry above, because the two changes shipped together and only the table separates them. This is the change that does the work: on its own it takes `fortress`, `gold-rush` and `coastal`. Army columns are NOT comparable across arms and are not quoted as if they were — a match that ends at 34,509 has 25,000 fewer ticks in which to peak.

**How this class is checked from now on.** `tests/simulation/aiGarrisonedDefender.test.ts` on the new `ai-garrisoned-defender-fixture`: the defender's only two villagers are garrisoned through the real command path, the test asserts they are off the map AND still alive (a dead villager would let the attack phase fall through for the right answer for the wrong reason), and then requires the match to RESOLVE. RED-CHECKED: `owner 2 still holds 2 building(s) (town-center, barracks); 11 of 11 attackers idle`.

**A note on the instrument, because it wasted an hour.** Two rounds of tracing printed nothing and were read as findings. Both times the file had been patched with a broken string escape and `tsx` failed to transform it — and the run was piped through `grep`, so the shell reported exit 0. A trace that prints nothing and a build that never ran look identical. Every later trace was checked by confirming a control line printed first.

## 2026-09-04 — The AI waits for a full attack group against an enemy that has no army left (FIXED, found by a critic)

**Symptom.** None reported — this is a defect an independent critic found in the very change that was supposed to close its class, before anyone played it. It is registered anyway, because the entry above claims to have fixed "the army stands idle beside a base it could raze" and only half of that was true.

**Investigation.** The critic built a control of the garrisoned-defender fixture with the villagers left ON the map and six attackers, and got the same tableau the garrison defect produced: owner 2 with zero units and two buildings standing, owner 1's six-unit army idle **at its own base** for 8,000 ticks, match `running`.

**Root cause.** `shouldPush = state.attackGroup.length >= attackGroupSize(currentAge)` gates EVERY fallback in `runAttackPhase` — the visible-building branch, the Town-Centre branch, and the `lastResortTargetId` branch that v0.3.197 added for exactly this case ("an enemy that still holds a BUILDING is still alive by the conquest rule"). One unit short of seven in the Castle Age and none of them can fire. The threshold is there so a single lost unit does not commit the AI to a base walk; it has nothing to hold back against an opponent with no army to be caught by, whose buildings do not move.

**Fix.** The threshold does not apply when the target enemy has no unit on the map. `ownerHasUnitOnMap` is the predicate, beside `findOwnedUnitOnMap` in `playerQueries`.

**Two more instances of the SAME position-blind query, also found by the critic and also fixed.** The rename in the entry above did not close the class, and the architecture decision record has been corrected to say so.

- `ownedMilitaryUnitIds` prunes the attack group, and `state.attackGroup.length` is what the threshold compares — so garrisoned military read as mustered. It is SPLIT rather than filtered, because its two callers ask different questions: the production phase's growth pause wants "how much military do I OWN" (a garrisoned soldier still costs population), the attack group wants "how much can MARCH". `ownedMilitaryUnitIdsOnMap` is the second. Reachable on `islands`, where the AI garrisons its army into a Transport Ship.
- `findAvailableVillagerForBuild` could hand a foundation to a villager sheltering under the town bell. The `building.placeConfirm` validator asks only that the builder is alive and is a unit, so the site is placed and the concurrent-build slot is spent on a builder that can never walk to it. Not reproduced end to end; the trap is confirmed present.

**How this class is checked from now on.** A second case in `tests/simulation/aiGarrisonedDefender.test.ts` on `ai-garrisoned-defender-few-fixture` — four attackers against a Castle-age threshold of seven. RED-CHECKED with the exception removed: `closest approach 21 cells; 4 of 4 attackers idle`. The full proof, including why that fixture has to be BROKE and why it measures the march rather than the win, is in `gate-proofs.md`.

## 2026-09-04 — The AI's lumber camp cannot reach the woodline on three of the maps it ships (FIXED)

**Symptom.** The user's priority 1, wood income. `arena` owner 1 runs a whole 60,000-tick match at 13-69 wood with roughly 13 villagers assigned to wood, so `pickNextBuild` stalls on a 175-wood Stable for the match and no seed has ever built a Siege Workshop.

**Investigation.** The wood force was never the problem. Sampling every 250 ticks, `arena` owner 1 has **19.6 villagers assigned to wood and 3.01 of them actually chopping**; the other 16.6 are walking (10.18 `to-resource`, 6.10 `to-dropoff`). Reading the live gatherer components, the tree each one picked sits **15 to 16 cells from the nearest wood drop-off for the entire match**, and the AI holds exactly ONE lumber camp. The comparator is not at fault: the tree it picks IS the nearest one to a drop-off, sample after sample. The forest is not depleted either — 24 of 346 trees are felled in 30,000 ticks.

**Root cause.** Two constants, set in different files for different reasons, that cannot both hold. `paintWoodlines` keeps every woodline patch at least `MIN_START_GAP` = 14 EUCLIDEAN cells from every start, deliberately, so a patch cannot punch a hole in Arena's ring or Fortress's square. `DROP_OFF_ANCHOR_RADIUS` was 12, in the manhattan metric, where that same gap is up to 20. So on `arena`, `fortress` and `gold-rush` there is **no tree within 12 of either start** — the anchor returned null at both, the camp fell back to the Town Centre, and every load was carried the whole way.

**Fix.** `DROP_OFF_ANCHOR_RADIUS` 12 -> 20. The enemy is not the constraint at that range: these maps seat their two starts 56 cells apart, so a camp 20 out is still 36 from the enemy's.

**Measured, both arms, `SEEDS=arena,fortress,gold-rush,coastal TICKS=60000 SAMPLE=250`.** On `arena` owner 1: villagers chopping per sample 3.01 -> **5.43**, wood gained 2,964 -> **4,076**, peak wood 182 -> **488**, age reached castle -> **imperial**, and the nearest tree to a drop-off falls from 15 to **4** the moment the camp goes up. Trees felled in 30,000 ticks 24 -> 41.

**On its own it makes RESOLUTION worse, and that is why it ships beside the garrison fix rather than alone.** All four arms, same command, same seeds:

    arm                     arena              fortress           gold-rush          coastal            resolved
    neither fix             running            running            victory 34,704     running            1 of 4
    anchor radius only      running            running            running            running            0 of 4
    garrisoned-unit only    running            victory 57,309     victory 32,478     defeat 52,472      3 of 4
    both (shipped)          victory 34,509     running            victory 34,921     defeat 52,472      3 of 4

Alone the radius change costs `gold-rush` the one resolution the baseline had. Together the two reach `arena`, which neither reaches alone, and lose `fortress`, which the garrison fix alone reaches. WHICH seeds resolve is a knife-edge of a deterministic trajectory and moves under any change; how MANY is the number worth reading, and it goes 1 -> 3.

**One number in the first write-up was not comparable, and a critic caught it.** `fortress` owner 1's wood over a match reads 1,862 -> 8,829, and that is NOT a carry-distance measurement: in the baseline arm that slot ends at **pop 0/40 with zero villagers** — it is wiped out — while in the other it ends at 51/65 with 46. The 4.7x is dominated by the slot surviving. The `arena` pair (2,964 -> 4,076, same owner alive in both arms, villagers on wood 19.6 -> 15.3) is the one that measures what this entry claims.

**RE-MEASURED CLEANLY 2026-09-04, and the earlier table above was contaminated.** That four-arm table was taken by flipping the constant between runs while other work was in flight, so its arms differ by more than the radius, and it never included the boot map. The clean comparison — final code, six seeds, both arms run back to back with the source verified unchanged between them (`SEEDS=aoe2-prototype,arabia,arena,fortress,gold-rush,coastal TICKS=60000 SAMPLE=500 npm run ai:army-block`):

    seed              radius 12          radius 20 (shipped)
    aoe2-prototype    running            running            <- BYTE-IDENTICAL, every column
    arabia            running            running            <- BYTE-IDENTICAL, every column
    arena             running            victory 31,694
    fortress          running            running
    gold-rush         victory 37,837     victory 34,885
    coastal           defeat  43,236     defeat  43,236
    resolved          2 of 6             3 of 6

**This answers the standing objection to any camp-siting change.** v0.3.177 was reverted because it lost at match resolution on the BOOT MAP, and §13.2 made the boot map the bar for a retry. `aoe2-prototype` and `arabia` are byte-identical here on every column of both owners — the change cannot touch them, because their nearest tree is 5 to 7 cells and the anchor already found it at radius 12. That is not luck; it is the same property the `coastal` instrument check turns on, now stated over the map the objection was about.

**Instrument check that makes the attribution safe.** The change can only bite where the anchor was null, so `coastal` — whose nearest tree is 5 to 7 — must be untouched. It is **byte-identical** across both arms on every column, both owners. A change there would have refuted the reasoning rather than confirming it.

**This corrects the entry below.** "The AI's build order stalls on the first thing it cannot afford" closed by pointing at the villager SPLIT — "a split that moved villagers onto wood would let the AI afford the buildings AND the army". The split was already fine. Nineteen villagers were on wood; they were walking thirty cells a round trip. The lever was the CARRY, not the allocation.

**How this class is checked from now on.** `tests/simulation/dropOffAnchorReach.test.ts` walks every seed in `PLAYABLE_MAPS` — the same roster the setup screen offers, so a new map is covered the day it is added — and asserts the lumber-camp anchor is found from every start and IS the nearest tree. A second case ties the two constants together, so the gate goes red when either one moves rather than only when a map changes. RED-CHECKED at radius 12: Arena, Fortress and Gold Rush all failed by name.

**Still open, and corrected 2026-09-04.** The AI builds ONE lumber camp, ever — `pickNextBuildTarget` asks for one only in the DARK age and only when it has none — so a camp that is DESTROYED is never replaced. That is why `fortress` owner 2 and `gold-rush` owner 2 finish a match with none, and the first write-up was wrong to say they never build one: fortress o2 completes a camp at tick 7,800 and loses it at 11,600; gold-rush o2 completes one at 15,300 and loses it at 23,600. For fortress o2 the zero-camp ending is CAUSED BY THIS CHANGE — at radius 12 its camp stands two cells from its own Town Centre and survives the match; at 20 it stands sixteen cells out, in reach of a raid, and dies. Found by a critic running the counterfactual, not by the author. And the carry creeps back: on `arena` the nearest tree to a drop-off goes 4 -> 9 between ticks 7,500 and 30,000 as the local woodline is eaten. AoE2 plants a fresh camp at the new woodline; this AI cannot.

## 2026-09-04 — Neither walled map enclosed a single tree, so its player had to leave the wall to play (FIXED)

**Symptom.** The user's priority 4: `arena` owner 2 sits at 3 population with no army for a whole 60,000-tick match and never leaves the Dark Age.

**Investigation.** Sampled every 1,000 ticks, the story is a death spiral rather than a stall. Owner 2 grows to seven villagers by tick 4,000. At 5,000 its villagers start GARRISONING — the town bell, because owner 1 is raiding. By 7,000 it has **no unit on the map at all** and three villagers inside, and it stays that way to tick 16,000 while its wall is taken apart one segment at a time (34 buildings down to 26). Food 10, no income, no way back.

The raid is not the root cause either. It is that arena's ring encloses **no wood**. Reading each walled start's contents: `arena` holds a Town Centre, 2 sheep, a boar, 6 berries, gold and stone behind its wall, and **zero trees**; `fortress` holds four of each mine, four sheep, two boar, six berries, a Castle — and **zero trees**. Every building that raises the population cap costs wood, and so does every age-up prerequisite, so a walled player has to leave the wall in the first minutes and keep leaving it. On `arena` the nearest tree was 15 cells away, outside the ring, and that walk is where owner 2 lost four of its seven villagers.

**Root cause.** `paintWoodlines` holds every patch at least `MIN_START_GAP` = 14 cells clear of every start, deliberately, so a patch cannot punch a hole in Arena's ring or Fortress's square. Nothing then put wood back INSIDE. The walls were built and the maps shipped with a base you cannot boom in — which is the opposite of what Arena means.

**Fix.** `paintEnclosedWoodline` paints a woodline inside the wall of each walled start, before the wall goes up, so no segment can land on a tree. Trees are IMPASSABLE, so it refuses any cell that would cut the enclosure in two, checked by flooding the enclosure with the patch treated as solid rather than approximated with a margin.

**The ORDER the cells are taken in decides the whole result, and the intuitive one is the worst of the three.** All measured on six seeds at 60,000 ticks:

    ordering                        arena owner 1        arena owner 2                  arena outcome
    (no enclosed woodline)          castle, army 35      DARK AGE, dead, cap 10         victory 31,694
    nearest the Town Centre         DARK AGE, cap 10     DARK AGE, cap 10               running
    farthest, grown from a seed     castle, army 16      DARK AGE, cap 10               running
    row-major band at one edge      castle, army 39      FEUDAL, 786 food, cap 25       victory 50,770

Nearest-first gives the shortest carry and spends the only ground a walled base has to build on: both owners were population-capped in 98% of samples at 10/10 with a peak cap of 10, holding 1,000 food they had nowhere to spend. Farthest-from-the-Town-Centre grows a blob in a corner that fans inward and fragments what is left. What works is a band pushed against ONE EDGE, which leaves the rest of the enclosure as one contiguous piece of building ground — halls in the middle, trees at the edge, which is what a real base looks like.

**Effect.** `arena` owner 2 goes from the Dark Age at 10 population, dead, to the FEUDAL age with 786 food and a 25 cap and 82% of samples unblocked; owner 1's army goes 35 to 39 and its cap 70 to 80. `fortress` owner 2 goes from 0/35 population to 63/65. Every map WITHOUT a wall — `aoe2-prototype`, `arabia`, `gold-rush`, `coastal` — is **byte-identical on every column of both owners**, which is the instrument check: this change can only reach a map that has a wall.

**Still open.** `fortress` does not resolve either way, and its two owners simply swapped which one is wiped out. `arena` resolves 19,000 ticks later now, because owner 2 defends itself.

**How this class is checked from now on.** `tests/simulation/walledMapEconomy.test.ts`: for both seats of both walled maps, the enclosure must hold food, WOOD, gold and stone, and must stay one connected place. RED-CHECKED before the fix — all four seats reported `trees inside: 0`. The connectivity half is not decoration: it caught the first placement cutting two cells off arena seat 2's base.

## 2026-09-04 — A browser gate 0.2 seconds from its own timeout (FIXED)

**Symptom.** `npm run verify` went red on `game-hud-and-camera-hud.spec.ts` :: `keeps top status-bar chip positions stable as live values change`, with `Test timeout of 30000ms exceeded` and no assertion failure. It had passed in the previous full run of the same code.

**Investigation.** Four runs in isolation all passed — and the test reported **29.8s against a 30-second budget** on an idle machine. Per-step timings placed the whole cost in one line: boot 1.2s, chip rects 0.02s, the first `train-villager` click 0.9s, and the click after the population cap is reached **25.5s**.

**Root cause.** The test queued THREE villagers to drive food from 200 down to exactly 50. The default scenario opens at 4/5 population, so a queued villager fills the cap and the card becomes unavailable; Playwright then waits out its actionability check on the next click. Nothing was wrong with the game — the card is correctly unavailable — and nothing was wrong with the assertions. The test simply spent 85% of its budget waiting for a button it did not need.

**Fix.** One villager, and an assertion that the food chip CHANGED rather than that it reads a particular number. The test is about the chips not MOVING while their values change, and it now says that. 29.8s to 2.4s, and it cannot be broken by a future change to what a villager costs.

**How this class is checked from now on.** No new gate; the fix is inside the gate. What this and the flaky trade-route spec have in common is the lesson: **a browser assertion bounded by WALL CLOCK is bounded by the machine's load, and that bound is invisible until a slower machine finds it.** Every runner this repo has is slower than the one it is authored on. A spec whose reported duration is within a few seconds of the timeout is red on somebody's machine already.

## 2026-09-04 — The suite's one flaky spec, and what it was really asking (FIXED)

**Symptom.** `npm run verify` went red on `tests/browser/trade-route-reach.spec.ts`: the Trade Cart's selection panel read `Idle` where the spec wanted `Trading`, after nine polls over five seconds.

**Investigation.** It is not caused by any change in this session — that fixture sets `disableAi: true` on BOTH owners, so neither the AI attack phase nor the drop-off anchor can reach it. Running the spec repeatedly against ONE unchanged build gave 4 passes and 1 failure, then 7 of 8: it is flaky, and it has been. Instrumenting the click showed the screen point and the Market's position identical on every run, passing and failing alike, and the cart's task reading `idle` immediately after the click in runs that later passed.

**Root cause, measured one variable at a time.** Two races, and fixing either alone leaves the other: a step BEFORE the click (pre-click step alone still failed 1 in 6) and a step AFTER it (post-click step alone failed 2 in 8). The click needs a world the simulation has already revealed — an order at an entity the fog has not shown yet is refused, which is the fog contract working rather than the question the spec asks — and the order it submits does not EXECUTE until the next step, which a loaded machine can leave undone past the five-second poll. Both together: 12 of 12, and 18 of 18 across the two earlier arrangements that had both.

**How this class is checked from now on.** No new gate — the fix is inside the gate itself. The lesson is the one the fleet canon already states about bounds: a browser assertion that waits on WALL CLOCK for a simulation step is bounded by the machine's load, and that bound is invisible until a slower machine finds it. This repo's remote runners are all slower than this one.

## 2026-09-03 — The AI reaches the Imperial age and never trains another unit (FIXED v0.3.199)

**Symptom.** The user's standing priority 1, "THE AI NEVER FIELDS AN ARMY". On `fortress` both AI owners reach the Imperial age and finish a 100-minute match with armies of 2 and 0, holding 1,231 wood and 595 food, with five military buildings each.

**Investigation.** Three hypotheses were measured and refuted first — villagers eating the army's population (freeing 39 population left the army at ZERO), the age-up reserve (`ageUpReserveCost` returns `{}` in the Imperial age, where it was being read), and having nothing to train from (`hasProducer` 100%, four producer types, `idleProducer` 100%). A fourth, resource starvation, was measured, recorded as its own entry above, and then refuted as the cause here: 1,231 wood and 595 food buys seventeen Halberdiers.

What settled it was reading the offer instead of reasoning about it — selecting each of owner 1's buildings at tick 60,000 and printing `trainOptions` beside `pickUnitMix('imperial-age')`:

    barracks       offers [long-swordsman, pikeman]                    mix wants halberdier   -> NOTHING
    archery-range  offers [crossbowman, elite-skirmisher, cavalry-archer] mix wants arbalest  -> NOTHING
    stable         offers [light-cavalry, knight]                      mix wants cavalier     -> NOTHING
    siege-workshop offers [mangonel, scorpion, capped-ram]             mix wants siege-ram    -> NOTHING

**Root cause.** `getTrainOptions` returns `latestResearchedInChain` — one unit per line, the best tier the owner has actually RESEARCHED. `pickUnitMix` names the TOP of each line. `aiSystemProductionPhase` gated training on `getTrainOptions(owner, producer).includes(unitType)`, an exact-name match between the two. Every Imperial mix entry is an upgrade-gated top tier, so an AI that reached the Imperial age without those upgrades matched nothing at any building and stopped training units permanently. Reaching an age researches nothing, so this is the DEFAULT path into the Imperial age, not an edge case.

**Fix.** `trainableInSameLine` resolves the wanted unit to the offered unit of the same line, so an AI asked for a Halberdier at a barracks offering Pikemen trains the PIKEMAN — what a player does while an upgrade is unresearched. The lines are derived by union-find over `UNIT_LINE_UPGRADES` rather than restated, because a second copy of the tiers can drift from the one the upgrades use: that table's ram line already gained a middle tier (Battering -> Capped -> Siege) that a hand-written list would have missed.

**Measured with `SEEDS=arena,fortress,gold-rush,coastal TICKS=60000 SAMPLE=500 npm run ai:army-block`, both arms, before -> after.** That script is `scripts/aiArmyBlock.mjs`, promoted out of a scratch probe as part of this change: the numbers below were first taken with an instrument under an ignored path, so the evidence a tracked document cited could not be re-run by anyone. A review caught that, and the rule it teaches is that evidence a tracked doc cites has to be a tracked instrument. `fortress` owner 2 army 0 -> 3 (peak 10 -> 13) and it destroyed owner 1's entire population; `gold-rush` victory at 41,967 -> 34,704 with army 12 -> 17; `coastal` 20 -> 21; `arena` 105 -> 95.

**The arena drop is CAUSED BY THIS CHANGE, and an earlier draft of this entry dismissed it wrongly.** That draft called it "trajectory divergence on a seed whose Castle-age AI already held its upgrades". It does not hold its upgrades: at tick 40,000 arena owner 1's army is `crossbowman:25, man-at-arms:4, scout:1, spearman:24`, and live Spearmen and Men-at-Arms are proof the Pikeman and Champion upgrades were never researched, because researching one mutates the units that exist. Those Spearmen are this change's own product — the Castle mix names `pikeman`, the Barracks offers `[man-at-arms, spearman]`, and before the fix that Barracks trained NOTHING. "Noise" was never available as a defence either: `Math.random` appears nowhere in `src/game/simulation`, and two runs of the same seed are byte-identical. What actually happened is a composition change — the AI now fields anti-cavalry it could not train at all — that costs ten units of total army on this one seed while three other slots gain. That is a trade worth taking and it is recorded here as a trade, not as noise. Found by an independent critic, not by the author.

**Not caused by this change, and separately open:** `arena` owner 2 sits in the Dark Age at 3 population with no army for the entire 60,000 ticks, both before and after.

**How this class is checked from now on.** `tests/simulation/aiUnitLineTraining.test.ts` on the new `ai-imperial-military-fixture`. RED-CHECKED: it reported "expected 0 to be greater than or equal to 2" with the defect present and passes with the fix.

**Bound of that gate, named.** It proves the AI trains SOMETHING from an un-upgraded producer where resources and population are deliberately abundant. It does not prove the army is big enough, arrives in time, or wins. Its fixture is built to close every path that could satisfy it for the wrong reason — no Castle (whose unique-unit branch never reads the unit mix), no Siege Workshop (the Onager upgrade is the one gold-free escape), gold and stone at ZERO (every other unit upgrade costs gold) and wood at 600 (too little to build a Siege Workshop at 200 AND research the Onager at 500). An earlier version of that fixture handed the AI 4,000 of everything and it passed on Onagers, Monks and a Throwing Axeman WITH THE DEFECT FULLY PRESENT; the assertion now names the spear line specifically so those routes cannot come back silently.

**A second gate was found wrong by this fix, and repaired rather than relaxed.** `aiFeudalStone.test.ts` went red at 10 mined against a bar of 30. Nothing had regressed: with an army, owner 2 now conquers owner 1 at tick 6,561 instead of 11,442, and the mining window — anchored to stone crossing 125 at tick 6,096 and running 3,000 ticks — spent 2,535 of those ticks measuring a FINISHED match. That file already carried the warning that "a finished match reads exactly like a deadlocked economy" and a liveness assertion to catch it, placed AFTER the mining assertion where it could never fire. The window moved earlier (ticks 2,000-5,000) and the liveness check now runs FIRST. Re-red-checked by setting the Feudal stone weight back to 0: "stone never reached 125 - peak 60".

**Instrument notes, because two of them nearly published a wrong cause.** `bridge.selectEntityById` populates `trainOptions` only for the LOCAL player's buildings, so the second owner's rows in the probe above are stale copies of the first owner's last selection and were discarded. And a HALTED bridge is indistinguishable from an idle AI — `tryTick` catches the engine's tick failure and every later step returns immediately — which during development turned a missing import into a 73ms run that still failed on "expected 0", reading exactly like the defect. The new gate asserts `getHudState().engineHalted` is falsy for that reason.

## 2026-09-03 — Four of the nine playable maps shipped with no wood, so no game could happen on them

**Symptom.** The AI "never fields an army" and "matches do not resolve" — the two standing acceptance-test failures. On `arena` specifically, both AI players sit in the Dark Age at population 10/10 for the entire 75-minute audit, peak armies of 4 and 2, holding 701 food and 3,184 gold they cannot spend.

**Investigation.** The register's existing entry blamed `villagerCapForAge` (40) against a population cap that reaches only 50, bounding the army at 10. Measuring across three seeds instead of one showed that chain describes the BEST slot on the BEST seed. The real distribution: of six owner-slots, three never reach the Castle Age at all, two never reach Feudal, and none reaches Imperial. A player still in the Dark Age at 75 minutes cannot be bounded by a Castle-Age villager cap.

Tracing `arena` per 4,000 ticks showed the actual shape: **wood 0 at every sample from tick 4,000 onward**, while gold climbed 90 → 3,184 and food sat at 636 unspent. Buildings: one Town Center, one barracks, one house, 31 stone walls — and never a second house. Nine villagers, none idle, population pinned at 10/10 in 80% of samples.

Then the instrument was checked before the conclusion was drawn, which is what turned a plausible story into the right one. A first probe reported `trees alive 0` on arena; run against controls, the same filter reported `tree x246` on `aoe2-prototype` and `tree x1718` on `black-forest`. The filter was sound and the map really had none.

**Root cause.** The pass that gives every forest cell a tree lived INLINE in `defaultMap.ts`. Only maps built from that generator ever ran it. Enumerating the whole roster at tick 0:

| map | trees | | map | trees |
| --- | --- | --- | --- | --- |
| aoe2-prototype | 246 | | arena | **0** |
| arabia | 199 | | coastal | **0** |
| black-forest | 1,718 | | fortress | **0** |
| nomad | 40 | | gold-rush | **0** |
| islands | 40 | | | |

A house costs 25 wood and every age-up prerequisite building costs wood. With 200 starting wood a player buys exactly one barracks and one house — precisely what the arena trace shows — and the economy is then dead forever: the population cap never leaves 10, no age is ever reached, and no conquest can end the match. `createFortressMap` even carried code to fell "a TREE on the wall line", which could never fire because it builds on a bare grass world.

**Fix.** `seedForestTrees` is extracted to its own module and run by every generator. The three grass-world scripts (arena, fortress, gold-rush) also get `paintWoodlines`, which paints forest patches onto plain grass only and stays 14 cells clear of every start — measured from the scripts' own ring radii (Arena 7, Fortress 11), so a woodline can never punch a hole in the wall that is the script's identity. After: arena 346 trees, coastal 198, fortress 348, gold-rush 344.

**Effect, measured on arena at the same ticks.** Owner 1 went from Dark Age / 10 population / 9 villagers / 1 house / wood 0, to **Feudal Age / 28 of 30 population / 22 villagers / 5 houses / 3 farms**, with a lumber camp, mining camp, mill, blacksmith and barracks — and is destroying owner 2's wall ring. A game now happens where none could before.

**How it is checked from now on.** `tests/simulation/mapRoster.test.ts` :: `every playable map supplies wood` asserts, for every named map, total tree wood above ten times the starting stock AND more than eight trees within 30 tiles of each start — the second half because wood only one player can walk to is the same defect wearing a map shape. Red-checked by the defect itself: it fails on exactly arena, coastal, fortress and gold-rush before the fix and passes on arabia and black-forest.

**Bound of that gate, named.** It counts spawns and straight-line distance. It does not prove the wood is REACHABLE — a woodline walled off by terrain would pass it, which is the open gather-comparator defect's territory. It also says nothing about whether the amount is balanced.

**What this predicts, and it is the uncomfortable part.** The roster gate that already existed boots every named map and runs fifty ticks. It was green throughout. Fifty ticks is a horizon that ends long before 200 starting wood does, so the gate measured "does this map crash" and was read as "does this map work". The next defect of this shape is another property nobody asked a map to have — reachable gold, a walkable route between starts, a dock site on a water map — behind a gate whose horizon is shorter than the property's own timescale.

## 2026-09-03 — Every shadow in the game was the same rectangle, whatever threw it (owner-reported, v0.3.194)

**Symptom, as the owner saw it.** "The shadow from units, and buildings, and environment objects such as trees and bushes and ores, they all need to look more realistic. As a start, they need to roughly match the shape of the thing that created the shadow instead of always being a rectangular." Reported one day after v0.3.193 shipped the shadows.

**Investigation.** Reproduced immediately from a 4x crop of two trees and a boar at 2.4 zoom under Painted: each caster put down a hard-edged parallelogram whose only relation to the caster was its bounding box. The cause was not a bug — it was the design, stated as such in `aoeVoxelCastShadows.ts`'s own header: "Each entity casts from ONE box — its mass, not its accents". A single AABB swept along a parallel sun ray is a hexagon, and every hexagon looks like a rectangle stretched away from the sun.

**Root cause, and why no gate could have caught it.** v0.3.193's gates were about correctness of the shadow *it had chosen to draw*: the sweep matched the caster box exactly, the pieces tiled it with no overlap, overlapping casters never shared a height. All of that was true and all of it was measured. Nothing in the suite asked whether the shape drawn RESEMBLED THE CASTER, because a bounding box was the specified caster. The visual verification did not catch it either: the before/after diffs proved shadows had appeared, in a style (Moebius) whose tone bands quantise them, and "a shadow appeared" was the claim being checked.

**A deeper cause, worth separating.** The one-box caster is what a shadow looks like when it is derived from a PROXY for the shape rather than from the shape. The owner's second instruction named the general form: "whenever the shape of the object changes, the shadow also needs to be re-adjusted in the future." A proxy is a second copy of the geometry and drifts from it the first time someone edits one and not the other.

**Fix.** The shadow is now the caster's silhouette, derived from the caster's own parts every frame: `aoeVoxelShadowShape.ts` reads the solid parts as a vertical profile of at most four height bands and tiles the union of their swept footprints with triangles on a dedicated flat triangle geometry. Nothing in the shadow path names a recipe, a part or an entity type.

**What the fix exposed, which is the more interesting half.** The correct silhouette made buildings look WORSE. Almost every building here stands on a plinth wider than its walls and the sun is 52 degrees up, so the whole of a building's shadow lands inside its own plinth — measured on the Town Center, 0.82 tiles reached past a 3.44 tile plinth and the rest fell where nothing was drawing it. The old model escaped the plinth only by pretending the building was as wide as its plinth all the way up, i.e. by being wrong in the direction that happened to look right. A caster's base is now a shadow RECEIVER when it is a flat pad. Two further defects were found by LOOKING rather than by testing: the wedge geometry was first a prism, and two pieces' 0.036-tall side walls stood back to back along every shared edge and drew the tiling as a mesh of dark seam lines over every shadow in the scene; and `resolveShadowLevels` was still measuring overlap with each piece's enclosing parallelogram, which is half again a triangle's area, lifting casters that never shared a pixel (0.084 world units of lift on a 6x6 woodline against 0.038 correct).

**How it is checked from now on, for the CLASS and not the instance.** Three gates, in `tests/rendering/aoeVoxelCastShadows.test.ts` and `aoeVoxelShadowShape.test.ts`:

1. *The shadow is derived, not authored.* A caster is built from a bare box, its height and then its width changed, and the shadow's reach measured against `height * projection`. This fails for any implementation that reads a per-recipe shadow proxy, which is the general form of the defect.
2. *The shadow is not a rectangle.* The drawn shadow is measured ACROSS the sun ray at four distances along it. A single box's swept shadow is a hexagon, whose width across the ray is CONSTANT between its two end caps, so a taper cannot be produced by any one caster box. A tree's must narrow by 20% between 40% and 80% along the ray. Reintroducing the single-box caster makes those two numbers equal and the test red.
3. *The tiling is exact.* Coverage is sampled against a brute-force march of the sun ray through the bands — code that shares nothing with the envelope maths the tiling is built from. This is what caught the inverted sign in the monotone chain, which every band-level assertion passed.

**Bound of those gates, named.** They are on the SHAPE of one caster's shadow, at one sun. They say nothing about how a shadow reads against terrain of varying elevation, and nothing about the tone. The sample grids carry irrational offsets so that a sample cannot land exactly on the diagonal two triangles share, where no strict inside-test can attribute it to either and the rasteriser's own fill rule settles it at draw time — that is a real property of the test, and it means these gates do not check what happens at an internal edge.

**What this predicts.** The next defect of this shape is a visual property nobody asked a question about. Both this one and the plinth finding were invisible to a green suite and obvious in a 4x crop, and in both cases the missing question was the same one: not "is what I drew correct?" but "does what I drew look like the thing it belongs to?"

## 2026-09-02 — The motion smoother stood every unit still on any machine slower than 10 fps (found by a critic reading the code, v0.3.192)

**Symptom.** A fleeing deer drawn STANDING in 92-100% of the frames of its own flight, and a walking villager in 20-38% of its mid-walk frames — the exact defect this smoother's first cut had just been written to cure, reappearing at full strength, on any machine whose frames run slower than 100 ms. Nobody saw it, because nobody had looked at a frame slower than 16.7 ms: the shipped gate drove a 60 Hz grid and nothing else, and at 60 Hz the fix is perfect.

**Investigation.** Driven through the real bridge and the real smoother on a frame grid whose pace was the input (`deer-flight-fixture`, a scout luring a deer; `aoe2-prototype`, a villager's eight-tile open-corridor walk), the still-frame share as a function of the frame pace is a cliff at 100 ms and not a slope: deer 0.0% at 16.7 ms, 0.0% at 100 ms, 14.8% at 105 ms, 78.8% at 125 ms, 92.4% at 143 ms, 93.4% at 167 ms, 100% at 200 ms; villager 0.0% at 100 ms, then 25.0 / 31.7 / 37.8 / 20.0% at 125 / 150 / 200 / 250 ms, with the drawn per-frame step ratio reaching 25.0 at 125 ms — a burst-and-crawl, not merely a stop. 100 ms is the simulation's own tick period, so the cliff is exactly the pace at which one frame starts advancing more than one tick.

**Root cause.** `displayedPositionSmoother.apply` treated a FORWARD TICK GAP as a discontinuity: `tick - lastProcessedTick >= 2` cleared every track. On a machine at 6.7 fps every frame is such a gap, so every frame threw away the whole history the smoother exists to keep, and the drawn root fell back to the sim position — which is precisely the pre-smoother picture. The reasoning behind the rule ("where the steps inside the gap landed was never observed, so nothing is invented for them") does not survive contact with what the smoother already does: it interpolates between two samples FOURTEEN ticks apart for every deer hop. A two-tick gap is bracketed by two OBSERVED sim positions and is less extrapolation than that, not more.

**Fix.** A DISCONTINUITY IS A JUMP IN SPACE, NOT A JUMP IN TIME. The gap rule is deleted; a rewind still resets, and the distance guard — 1.5 tiles PER TICK of the gap, capped at the five ticks a frame can coalesce — carries the whole job. Per tick, not flat, and an independent critic is why: the first cut of this fix kept the flat 1.5 and `wildlifeCombatSystem` moves a CHARGING boar or wolf a whole cell EVERY tick, so a two-tick frame moves one 2.83 tiles and the guard snapped it — measured on `wolf-aggro-fixture`, the drawn root covering 2.0616 tiles in one frame at 150 ms and 3.1623 at 200 ms. The same critic corrected the arithmetic the flat guard had been justified with: the fastest UNIT is the Demolition Ship at 200%, 230% with Dry Dock, which carries 1.25 tiles in five ticks and not the 0.75 first written here. Measured after: 0.0% still at every pace from 16.7 ms to 250 ms, for the deer and for the villager. The accepted cost is ALIASING rather than a stop: a presentation that only looks every 1.5 ticks sees a villager's 3-tick cadence as alternating 1- and 2-tick intervals, so the drawn speed ripples up to 2:1 (measured 1.67 at 150 ms) where 60 Hz ripples 1.33.

**What the independent critic found that this fix had missed, and it was the same mistake one level down.** The first cut removed the tick-gap reset and left the distance guard FLAT at 1.5 tiles. That guard then fired on ordinary movement for the fastest mover in the game: `wildlifeCombatSystem` moves a charging boar or wolf a whole cell EVERY tick, so a two-tick frame moves one 2.83 tiles — measured on `wolf-aggro-fixture` at 150 ms, the drawn root covering 2.0616 tiles in one frame, and 3.1623 at 200 ms. A boar lure is one of the most-watched moments of an AoE2 opening, so that is the reported defect again on a different entity. The guard is now PER TICK of the gap and PER ENTITY, capped at the five ticks a frame can coalesce: 1.5 tiles a tick for a whole-cell mover, 0.75 for everything else. The second half is the critic's too, and it came with the proof this author had said was unavailable — `moveCarryCapHundredths` bounds the bank at `max(100 + perTick, round(perTick x 1.5))`, and sweeping every unit type against every movement technology the worst is the Demolition Ship at 260%, 83 hundredths a tick, granted at most TWO fine steps in one tick. Half a tile, provably, for every unit in the game. That halves the one hole this change opens (a garrison-and-eject completing inside one coalesced frame): a unit's exposure is 1.5 tiles over an ordinary two-tick frame instead of 3.0, and 3.75 over the worst five-tick one instead of 7.5. `displayedPositionSmootherDiscontinuities.test.ts` pins both ceilings and both sides of each, and the cases die when the guard is flattened or made uniform. The critic then caught the list itself being wrong in both directions, which is the third time in this one change that a rule tuned for one mover broke another: a RELIC carried by a monk IS a whole-cell mover, because `monkBehaviorSystem` puts it on the monk's integer CELL rather than its fine transform, so tightening made it snap on every cell boundary while the monk beside it glided; and a SHEEP is NOT one, because it takes a single quarter-tile subgrid step every sixth tick, so it had been given six times the teleport tolerance it can use. The list is now enumerated from every `setPositionAndSyncOccupancy` call site and asserted member by member. THREE more of the critic's findings were about the gate rather than the code and are folded into it below: nothing in the suite bounded the DRAWN root against the SIM root (a 30-tick deer delay draws it 2.879 tiles behind and passed every still-frame and step-ratio bar; now `maxLag` bounds it at 0.5 tiles for a villager and 1.5 for a deer, and the mutant dies), the live half's own justification was false above 100 ms a frame (a still frame cannot be shorter than a frame interval, so a 100 ms stall at 6.7 fps measures 0.0%; it now SKIPS naming the pace instead of passing), and the aliasing ripple is 2.50 across 110-240 ms and 3.00 at 320 ms rather than the "up to 2:1" first written.

**How it is checked from now on.** The gate's FRAME PACE is now one of its inputs, and it drives two: `unit-motion-smoothness.spec.ts` repeats the villager walk and the deer chase at `SLOW_FRAME_MS = 150` (6.7 fps, past the 100 ms line rather than at it), holding the same 2% still bar plus a step-ratio bar of 2.5 and, for the deer, a maximum drawn step of half a tile — because a whole cell crossed in one frame is what "not running" looks like at that pace. Every test in it also bounds how far the drawn root may trail the sim root. Red-checked three ways, each by reintroducing the exact defect: restoring the one-line gap reset fails the two slow-pace tests and the coalesced-sync test (deer 98.1% still with a 1.3637-tile drawn step per frame; villager 31.7% still, ratio 8.00) while all three 60 Hz tests stay green, which is the shipped gate's blindness reproduced on demand, and two `displayedPositionSmoother.test.ts` cases fail with it; flattening the per-tick guard back to a bare 1.5 fails the two smoother cases that pin a whole-cell-per-tick mover; and mutating the deer's display delay from 14 to 30 fails both deer tests, the slow one on `maxLag` 2.879 against its 1.5 bar — the mutant that passed everything before that bound existed.

**A second bound, in the same gate, of the same kind.** The live rAF half allowed `max(1, floor(2% × windowFrames))` still frames over a window of 51-75 frames — an allowance of exactly ONE frame against a measurement of zero, and one duplicate animation-frame timestamp (dt = 0, the drawn root trivially unchanged) would have spent it on a machine slower than this one. The verdict is now taken on TIME (`stillTimeShare`): a zero-length frame weighs nothing in either half of the ratio, while a 100 ms stall weighs 100 ms, 4% of the 2,500 ms window the gate requires, against a 2% bar. Measured after the fix: 0 ms still in a 3,116-3,150 ms window, median frame 33 ms. That is still not enough on its own, and the critic caught the gap: a still frame cannot be SHORTER than a frame interval, so above 100 ms a frame the 100 ms stall leaves no trace and the test would report a clean 0.0% having resolved nothing. The live half now SKIPS, naming the machine's measured median frame, when that median is slower than one simulation tick — which is honest only because the driven 150 ms halves cover that regime deterministically.

**What this predicts.** A guard tuned to one mover is the shape this defect keeps taking: the tick-gap reset was a rule written for a unit and applied to a deer, and its replacement was a distance written for a unit and applied to a charging wolf. Any other constant in the presentation that was derived from a villager — the gait window, the facing turn rate, the attack-cancel blend — has the same exposure on whichever entity moves fastest. And: every gate in this repo that drives frames drives them at ONE pace, and that pace is this machine's. Anything whose correctness depends on how much simulation time a frame covers — the animation clock, the gait window, projectile flight, the camera's own smoothing, the replay scrubber's 250 ms bound — has never been measured on a slow frame, and CI machines are all slower than this one. The next defect of this shape is in one of those.

## 2026-09-02 — The command bar hid half of itself at 800x600, and a clicked card's tooltip would not leave (play-test findings F5 and F6)

**Symptom.** Found by the repeatable opening play test (`tests/browser/play-opening.spec.ts`), which runs at 800x600. F5: with a villager selected, the whole BUILD group rendered at y 647-767 — below the selection panel's 588px bottom edge. Six of the ten build cards were entirely off screen, four of the eight stance and formation buttons were cut in half, and the unit's stat rows were gone. No wheel could reach any of it. F6: clicking a build card left its tooltip — "Place a House foundation (cost: 25 wood)…" — sitting over the bar while the pointer was already out on the map choosing the tile, still showing after a twelve-step sweep across the canvas.

**Investigation.** F5 had three independent causes stacked on one another. The panel's ceiling (`max-height: min(34vh, 224px)` below 1120px) was 204px against 396px of content; the wrapped bar put the summary on one line and the deck on the next, and each labelled command box was wide enough to force the build palette onto a third; and `.hud-panel--selection` is `pointer-events: none` with only `.hud-command-list` and `.hud-command-button` in the allowlist, so neither the panel's own `overflow-y: auto` nor `.hud-build-menu`'s inner scroll region could be reached with a wheel — the content was not merely below the fold, it was unreachable. The same shape was live at 1280x720 and 1920x1080 and had never been reported: the summary's 186px `overflow-y: auto` box put Faction, Civ and Inventory at y 708-769 against a panel ending at 708. Every existing browser test passed over all of it because Playwright's `locator.click()` scrolls hidden overflow into view before clicking, so a card no player could see was a card the suite could press. F6 was the tooltip controller's ownership rule working as written: a click focuses the button it presses, `focusin` claimed the tooltip for that focus, and `pointerout` then handed the tooltip back to the focus instead of hiding it.

**Root cause.** F5: a fixed pixel ceiling on a panel whose content grows with the selection, with the overflow hidden and the scroll unreachable — three ways of saying that nothing in the design forced the bar's content to FIT, and no check measured whether it did. F6: "focus owns a tooltip" with no distinction between the focus a click leaves behind and the focus a keyboard reaches, so a mouse user acquired a sticky tooltip they never asked for.

**How it is checked from now on.** `tests/browser/command-deck-fits.spec.ts` measures every command group, every command button, the build page toggle and every selection stat against the panel's VISIBLE box, and asserts the panel's content exceeds neither its scroll height nor its scroll width — over three viewports (800x600, 1280x720, 1920x1080), three selections the boot scenario can make (a villager with a build palette, a scout with stance and formation, a Town Centre with train and research), and both build pages, with `toBeInViewport({ ratio: 1 })` on each card so a click that only works because the runner scrolled cannot pass. The same file sweeps the pointer across the canvas after clicking a card and requires the tooltip to be gone while the card keeps focus, and requires a Tab to bring one back. Mutation-checked: restoring the old ceiling fails all four 800x600 tests; reverting the tooltip rule fails F6 at all three viewports. The panel's scroll regions are now in the pointer-events allowlist as well, so a future overflow is at least reachable rather than silent.

**What the first fix missed, and the critic found.** The independent critic ran the sweep the gate did not: ten shipped fixtures, every owned building and unit type, 47 selections. Sixteen were still clipped at 800x600 — a Blacksmith's research row ran to x 1724 against a panel ending at 588, and `research-loom` on a Castle-Age Town Centre was off screen — because `.hud-command-group` was `flex: 0 0 auto`, so a group took its max-content width and its buttons never wrapped. That is pre-existing and the first fix made it smaller (Blacksmith scroll width 2102 -> 1711) without making it go away, while the spec written in the same commit asserted it HAD gone away. Groups now shrink with `min-width: 0` and their lists wrap: sideways overflow is zero at every measured selection and viewport, and the three cards still taller than an 800x600 bar are reachable by wheeling the deck (scrollTop 29 / 30 / 152), which the pointer-events allowlist now permits. The critic also found the F6 rule incomplete: it released the pointer's claim on ANY keystroke, so pressing a camera key after clicking a card let the panel's own re-render hand the tooltip back over the world with the cursor 500px away. The claim is now held per control identity and released only when focus reaches a different control. Both are gated.

**What this predicts.** The bar's height is now the scarce resource, and two tests measured that before any reviewer did: the Watch Tower fixture picks its build anchor by "can the mouse reach this cell", so the bar growing 204 -> 227px moved the tower out of line of sight of its target, and the opening play test's Town Centre click landed on the bar. Anything else that picks a world cell by pointer-reachability, or that assumes a screen region is canvas, is the next instance. The second prediction is about the gate's own shape: three of the five fixture cards pass on REACHABILITY, not on fitting, so an 800x600 player still scrolls for a Castle's or a Market's last row — and the next card bigger than those will be found the same way this one was, by sweeping states the boot scenario cannot reach rather than by the gate that names three of them.

## 2026-09-02 — The AI's army is half what it was (RESOLVED 2026-09-03: it was WOOD. The population-cap chain in this entry is refuted below, and the heading it had until then is kept as the record of a second wrong diagnosis)

**CORRECTION TO THE CORRECTION BELOW, same day, from the very next measurement.** The paragraph beneath this one says "the cap was never the constraint; the wood to raise it was." That is TOO ABSOLUTE and the four-seed 60,000-tick run that followed it showed so. The accurate statement is: **the population cap binds wherever it stays low, and what keeps it low differs by map.** Measured at 60,000 ticks:

    arena      o1  cap 145  villagers 40  army 105
    gold-rush  o1  cap 130  villagers 40  army  90
    coastal    o1  cap  50  villagers 27  army  20   (8,545 food banked, unspendable)
    fortress   o1  cap  40  villagers 38  army   2
    fortress   o2  cap  65  villagers 60  army   0   <- this entry's chain, exactly

Fortress owner 2 is in the IMPERIAL age with a cap of 65 against a `villagerCapForAge` of 60: villagers take 60 of the 65 and the army is zero. That is this entry's original chain, working as described. It is real. What was wrong was generalising it — it holds where the cap is low and says nothing where the cap is high, and the cap's height has at least three separate causes: no wood at all (the four maps fixed on 2026-09-03), thin wood (coastal, still capped at 50), and limited buildable space behind the script's own walls (fortress). Only the first has been fixed.

**So the ranking of causes, after both corrections.** Wood was the dominant one and is fixed. The villager-target-versus-cap chain is the next one and is NOT fixed — it is what holds fortress to armies of 0 and 2 in the Imperial age, and the lever the entry already names (`villagerCapForAge` 60 against a cap the AI cannot raise) is untouched. The methodological lesson stands unchanged and is the reason this correction exists at all: every wrong turn in this entry came from generalising one map's regime to all of them, and I did it a third time in the paragraph below within an hour of writing that sentence.

**RESOLVED IN PART 2026-09-03, and the cause was neither of this entry's two diagnoses.** The chain below — `villagerCapForAge` wants 40 against a population cap that only reaches 50, so the army is bounded at 10 whatever else improves — is FALSE. It was measured on a boot map where the AI's wood economy is weak, and it read a symptom of that as a structural ceiling. Once four of the nine playable maps got the wood they had shipped without (see the 2026-09-03 entry above), the same AI on `arena` reached, at 60,000 ticks:

    population   145 / 145   (this entry says the cap "only reaches 50"; peak cap measured 145)
    army         105          (this entry says the army "could not exceed 10")
    villagers     40          at the cap, in 58% of samples — the ONE part of the chain that held

The villager target of 40 is real and does bind. It is simply not a ceiling on the army, because the population cap is not 50: with wood to build houses it climbs to 145, and 145 minus 40 is the 105 that is standing there. **On THAT map the cap was not the constraint; the wood to raise it was.** Every number in the analysis below was taken from an economy that could not buy houses, and none of it generalises past that — which, as the correction above records, is exactly the mistake this sentence then made in the other direction.

**What this costs, recorded rather than smoothed over.** This entry was rewritten once already, from "it is FOOD" to "it is the population cap", each time by measuring harder inside the same frame. Both diagnoses were reached by instrumenting the boot map alone, and both were wrong in the same way: a resource the AI could not obtain looked like a rule the AI was obeying. The correction came from widening the SEED SET, not from measuring the boot map more finely — three of six owner-slots turned out never to reach the Castle Age at all, which no amount of precision on one slot would have shown.

**What is still open.** On maps that always had wood, `aoe2-prototype` owner 1 still peaks at an army of 4 while owner 2 reaches the Castle Age. That is a real remaining question and it is NOT this one; a killer-attribution probe suggests owner 1 is losing a war rather than deadlocking, but 18 of its 27 villager deaths were unattributed and it reported implausible self-kills, so that is weak evidence and is recorded as such.

**The heading of this entry said "it is FOOD, not housing or population" until 2026-09-03, and that was wrong.** Measuring the food side directly — the entry's own stated next step — showed food is NOT scarce: 1,905 banked in the last 9,000 ticks, about 127 a minute against a 60-food militia, with nobody even assigned to gold. The constraint is below. Corrected in place rather than left to be read first and believed, because a heading outlives the body that refutes it.

**Symptom.** Recorded when DE build times landed (v0.3.187): the boot map's self-play winner peaks at 11 military where the pre-change baseline peaked at 20, while building variety went UP (14 types against 9). `aiReachesCastleAge`'s peak-army bar was re-derived from 19 to 10 to match, with the regression logged rather than explained away. This entry is the investigation that followed, and it exists mainly so the next attempt does not re-run the two experiments that failed.

**Two hypotheses, both killed by measurement.** (1) *The AI is population-capped.* Instrumented over 45,000 ticks on `aoe2-prototype`, sampling every 250: population at cap in **11%** of samples, gold below 60 in 20%, **food below 60 in 50%, wood below 60 in 53%**. Not primarily a cap problem. But 11% also UNDERSTATES the cap's role, because the AI fills headroom the instant it appears — the number to read is the cap itself, which reached only 50 at tick 45,000 against a villager target of 40, so an army could not exceed 10 whatever the economy did. (2) *So build houses ahead of demand.* The house rule fired only on `current >= cap`, one at a time, which was survivable at 120-tick houses and is not at DE's 250. Changed to a headroom rule (build below 4 spare, two at once when blocked) and measured: the cap did grow — 44/55 at tick 45,000 with ELEVEN spare population — and **peak military fell from 11 to 8**. Reverted. Houses cost 25 wood each and the AI is wood-short half the match, so the headroom came out of the army it was supposed to enable.

**What the measurement actually says.** At tick 45,000 with the headroom rule: 40 villagers, 4 military, **food 58, wood 27, gold 775, stone 65**. At 36,000: food 13, wood 495, gold 60. The AI is not short of population and not short of gold — it is short of FOOD, continuously, while carrying the maximum villager count its age allows. Each villager costs 50 food, `villagerCapForAge` is 40 in the Castle Age, and the food arrives and leaves as fast as it is gathered. A militia costs 60 food; the AI rarely holds 60 food at once.

**Why DE build times caused it.** Nothing about food income changed. What changed is that a villager now spends far more of its life BUILDING — construction is 2-2.5x longer everywhere — so the same 40 villagers gather materially less. The villager target and the production priorities were tuned when a villager returned to gathering twice as fast.

**The food side, measured (the entry's own next step, taken).** Villager allocation at tick 45,000 is **wood 24, food 13, stone 3, gold 0** of 40 — sixty per cent on wood. Food income is healthy in absolute terms: 1,905 food banked in the last 9,000 ticks, about 127 a minute, against a 60-food militia. So food is not scarce because it is not gathered; the stockpile hovers at 58 because it leaves as fast as it arrives, and the AI ends the match at **49 of 50 population** — 40 villagers plus 9 military. The chain is: `villagerCapForAge` wants 40, the population cap reaches only 50, and 10 is therefore the most army the AI can ever field. Gold is not a constraint at all by then (775 banked, nobody assigned to it).

**A methodological correction that matters more than the numbers.** The housing experiment above compared peak army 11 against 8 on SINGLE runs of one seed. That is exactly the comparison this repo already knows not to make — a chaotic emergent quantity tuned against a single-run validator, where the noise exceeds the signal. Both figures are small integers out of a 45,000-tick match. **The next attempt must use `scripts/selfplay-audit.mjs` across several seeds before and after**, and must not accept a single-match delta as evidence in either direction. That applies to the housing rule too: it was reverted for want of support, not proved harmful.

**How it is checked from now on.** `aiReachesCastleAge.test.ts` holds peak military at 10 with the regression named in its header. There is no gate for the CAUSE yet, and that is the fix's job. **What NOT to repeat:** reading the block rate as the constraint (11% of samples, and misleading because the AI fills headroom instantly), and judging any candidate here on one seed's peak army.

## 2026-09-02 — The replay bundle outgrew Node's string ceiling, and nearest-first approaches deadlocked the gather economy (found by the remote gate, after a green local one)

**Symptom.** The `playtest-corpus` job went red on the first push of v0.3.187, with the full local gate green. Two separate failures in one run. `RangeError: Invalid string length` from `JSON.stringify(result.bundle)` in `scripts/playtest.mjs` — a comment right above that line already recorded the same failure being mitigated once by dropping pretty-printing. Then, once the bundle serialised, the progression gate: "no living owner reached feudal-age (living owner ages: 2:dark-age, 1:dark-age)" at 13,000 ticks on the boot map, where 12 consecutive runs had been green.

**Investigation — the bundle.** Measured rather than guessed: the 13,000-tick boot-map bundle was 537,183,049 characters against V8's ~536,870,888 ceiling — over by 0.06%. Breaking it down by key: `ticks` 537 MB, of which `diff` 498 MB, of which `diff.state` 454 MB, of which one slot, `aoe2.lastSeenStatic`, was **358 MB**. That is 27.5 KB re-serialised into every one of 13,001 ticks. The fog-memory system rewrites a memory entry for every visible building and resource every tick, and the only field that changes for a stationary visible building is `lastSeenTick` — which a grep showed is written in three places and READ in none outside a serialization round-trip test. A codec's slot is diffed whole, so a field with no consumer was two thirds of the replay.

**Investigation — the deadlock.** THREE diagnoses, and the first two died to controls after reading convincingly from the code. (1) DE build times had slowed the opening past the horizon — killed by restoring the old times behind an env flag: owner 2 still never left the Dark Age. (2) The town bell was latching, because an enemy that stops wandering camps inside `RAID_RADIUS` and the all-clear waits for a stale sighting that never comes; a shelter cap was built, measured, and moved the number by nothing (stockpile delta 162 before and after; Feudal at 14,100/13,800 with the cap and 14,100/13,800 with it disabled), so it was reverted. (3) The actual cause, found by tracing positions rather than reasoning: all nine of owner 2's villagers stood stacked on ONE cell (51,28), motionless across a 60-tick trace, every one latched in `to-resource`, its stockpile byte-identical from tick 8,000. Nearest-first ordering had been applied to every approach; on the gather and drop-off paths it converges a whole workforce onto one cell.

**Root cause.** (1) A per-tick write of a field nothing reads, into a slot whose diff granularity is the WHOLE slot. The write was honest — the entry really did change — which is why no dirty-flag audit would have found it; what made it expensive was that the change carried no information. (2) A fix applied wider than the finding that motivated it. Play-test finding F2 is about a BUILDER walking to a construction site; the ordering was put in `findMovementPathToCandidates`, where every caller inherited it. The suite caught the widest version (99 failures from arrival-slot allocation), which produced a narrowing to "approach plans" that still covered resource and drop-off walks — a second, quieter over-reach that only self-play could see.

**How it is checked from now on.** `lastSeenStatic` is written only when an entry's APPEARANCE changes (`sameAppearance` excludes `lastSeenTick`), and the ops split reading from dirtying — `getOrCreateMemoryMap` no longer marks the slot, and a caller that writes calls `noteMemoryChanged()`. Measured after: that slot 358 MB → 2.0 MB, and the 13,000-tick bundle 537 MB → 181 MB. **The headroom figure first written here ("three times under the ceiling") was the 13,000-tick number quoted without its horizon, and the same change raised the corpus horizon to 20,000** — what CI actually writes is 332,020,582 bytes for the boot map and 301,786,923 for `default-seed`, which is **1.62x under** V8's 536,870,888, not 3x. At 16.6 KB/tick that ceiling returns at roughly 32,300 ticks: the next horizon raise reaches it, and so does anything that grows recorded state by half again. Found by a critic re-measuring the shipped artifacts rather than re-reading the note. Nearest-first ordering: **the scope shipped in `ccafefa7` was too narrow and was corrected in v0.3.190.** That commit confined it to a walk to an in-progress construction site, on the stated reasoning that "the gather and drop-off paths" both converged a workforce onto one cell — but only the RESOURCE path was ever measured. A critic restored the ordering on every BUILDING approach and both instruments stayed green; re-measured, it is better than green: the boot map's self-play winner reaches Castle at 26,750 instead of 35,250, with 14 building types against 9 and 10 unit types against 6. Every building approach now sorts (repair, garrison, drop-off, attack, monk included — one geometry problem), `findResourceApproachPlan` does not, and `approachOrdering.ts`'s header carries that split with the evidence for each half. The cache key carries the flag. `aiEconomyKeepsMoving.test.ts` is the corpus gate's cheap twin. **Only its first case gates this defect** — with the deadlock reintroduced the stockpile case fails (moves 19 over 6,000 ticks) and the "every owner leaves the Dark Age" case still PASSES, because the 20,000-tick horizon is generous enough for a frozen owner to crawl out. The second case is a progression check, not a deadlock check, and the description here said otherwise until a critic reintroduced the defect and watched which half went red. The corpus horizon moved from 13,000 to 20,000 ticks, derived from measured transitions rather than guessed — though the figures recorded for it (boot map 14,100/13,800) were already stale for the shipped build when written, and at v0.3.190 the boot map's transitions are 14,250 and 13,750. The margin is real either way; the recorded derivation was not re-measured after the last change to it, which is the same staleness this register keeps catching in gate comments.

**What this predicts.** Any other codec slot written every tick with a value that carries no information is the next bundle blow-up — `aoe2.visibility` is 66 MB of the remaining 181 MB and has the same shape. Any fix applied at a shared choke point will be inherited by callers the finding never mentioned; the two narrowings here were each found by a different instrument (the unit suite, then self-play), and a third caller class would have needed a third. And the ceiling itself is unguarded: nothing fails until a bundle crosses it, so the next 3× of recorded state does this again.

## 2026-09-02 — RETRACTED: `ai-feudal-stone-fixture` does not freeze; the match ENDS

**This entry was filed as an open defect and is wrong.** It claimed owner 2's economy on `ai-feudal-stone-fixture` freezes byte-identical at about tick 15,000 — all four resources static, villager count pinned, 26 villagers stationary across a 60-tick trace with none moving even a sub-cell, 13 stuck in `to-dropoff`. Every one of those observations is real. The conclusion drawn from them was not.

**What it actually is.** The match RESOLVES at tick 15,000: `getMatchState()` returns `outcome: defeat, winCondition: conquest, "All of your units and buildings have been destroyed", scores 1:70 2:1492`. Owner 1 is wiped out and owner 2 wins. A finished match stops, which is the correct behaviour and the reason every unit stands still and every stockpile stops moving.

**Why it is kept.** Because the mistake is the interesting part, and it is one this register has recorded in other forms: a frozen economy has a signature that looks identical whether the cause is a deadlock or a decided game, and one `getMatchState()` call separates them. The same session had just fixed a REAL deadlock with exactly that signature (nine villagers stacked on one cell, boot map), which is what made the second reading feel confirmed. **Check whether the match is still running before calling a still world a deadlock** — the boot-map case was running, this one was not.

**What follows for the gate.** `aiFeudalStone.test.ts`'s measurement window ends at tick 14,000 and must stay there, but for this reason rather than the one first written into it: past 15,000 there is no game left to measure on this fixture. The note in that file says so.

## 2026-09-02 — The Dark Age ran at half DE's pace, a builder walked to the far edge, and a 37-villager crew built a Wonder one villager at a time (found by playing)

**Symptom.** Four of the six findings the 2026-09-02 headless play test recorded while playing the opening through the lobby, the mouse and the HUD. A villager one to two tiles from a House site took ~22 s of GAME time to lay the first hammer blow — measured headless on the same map, villager 2205 at (6,8) ordered to build a House at (7,6) walked (2,8)->(2,5)->(5,4)->(9,5)->(9,6) to build from the EAST edge, and 2206 the same, while only 2207 (already south of the site) started in 1.4 s. The construction itself then took 11.9 s against DE's 25. The BUILD card priced an Outpost at 25 wood + 10 stone against DE's 25 + 5, and `structures.csv` priced a House at 30 wood while the game charged DE's 25.

**Investigation.** The build-time table was hand-maintained: comparing every row against `structures.csv` (which nothing read for this column) found SIXTEEN wrong, not the four the play test could see — Mill and both camps at 18 s against 35, Barracks 24 against 50, Market 20 against 60, Watch Tower 22 against 80, Castle 56 against 200, Town Center 30 against 150, Wonder 120 against 3,503. The same sweep over the cost column found three more: the Watch Tower's 25 wood was missing entirely, and the Wonder charged 1,000 food DE does not ask for. The walk was `findMovementPathToCandidates`, which path-searches candidate cells IN LIST ORDER and returns the first it can reach — so the choice was whatever order `getApproachCellsForFootprint` happened to enumerate, and a villager west of a site took the east edge. That one line also capped every construction crew at one working builder: with the AI's Wonder crew ordered onto the site, 37 villagers held build commands while the site gained exactly 1.0 progress per tick, because they were all queued for the single cell the enumeration named first. And behind THAT: the AI had no crew logic at all — `aiSystemBuildingPhase` assigns one villager per site and never reinforces — which no test could see while a Wonder took 1,200 ticks and a solo builder finished it inside any budget.

**Root cause.** Four hand-maintained tables where a sourced one existed, and one search whose result depended on an input nobody had chosen. `structures.csv` is the spec of record (§1) and its `cost` and `build_time` columns had no reader: `structureStats.test.ts` differentialled hit points and garrison capacity and stopped there, so the two columns that set the game's PACE were unchecked while the two that set its durability were gated. The approach search's list order was an accident of the footprint enumeration that no comment claimed and no test pinned. AoE2's multi-builder formula was written in the spec (§7 and §16, `3 * base_time / (builders + 2)`) and never implemented — builders stacked LINEARLY, so five villagers built five times as fast.

**How it is checked from now on.** `tests/content/structureCostsAndBuildTimes.test.ts` differentials BOTH remaining columns for every structure against the CSV, checks the per-age rows agree with each other first (the table repeats each structure once per age), and refuses to report on an implausible parse — a differential that silently reads zero rows is a green test that checks nothing. `BUILDING_BUILD_TIME_TICKS` is now one-for-one from `build_time x TPS`. `multiBuilderCurve.test.ts` pins the curve as a pure share function summing to (n+2)/3 for any crew and as a measured rate in a real match (five villagers at 2.33x one, not 5x). `buildApproachNearestEdge.test.ts` walks the F2 order through the bridge and fails at the observed 219 ticks. `aiBuildCrew.test.ts` asserts the AI's Wonder gains more than 2.0 progress per tick, which no solo builder can reach. `play-opening.spec.ts` now ASSERTS the walk (<8 s) and the House's build time (23-28 s against DE's 25) instead of recording them.

**What this predicts.** Any other column of `design/stats/` that no test reads is the next instance — `units.csv` and `technologies.csv` have their own unread columns, and the four-CSV differential set is the place to look. Any search that returns "the first candidate that works" from a list nobody ordered will be making a silent choice; the approach planners were one, and `findUnitRangePlan` and the drop-off assignment take the same shape. And a formula written in the spec but not implemented leaves no trace at all: the multi-builder curve sat in §16 unimplemented for the project's whole life, and only a build time long enough to notice exposed it.

## 2026-09-02 — A parked unit jumped the traffic queue, and a converted villager walked into its old side's gate (found by the multi-CLI review, not by play)

**Symptom.** Two defects in code that shipped green with mutation-tested suites (v0.3.175, v0.3.177), found by the adversarial review in `docs/threads/current/concurrency-review-2026-09-02/REVIEW.md`. First: the narrow-passage starvation rule elected a unit that had simply stood still — a builder back from a ~1,500-tick Town Centre, a unit out of a garrison, a parked soldier — over villagers that had been asking to move for hundreds of ticks, on its first ask. On the boot map in self-play at 30,000 ticks the rule granted 245 relief admissions across 18 villagers, and five of them went to units whose last attempt to move was 793-2,354 ticks earlier. Second: a villager converted by an enemy monk while standing beside its own side's gate replayed the step it had remembered INTO that gate and stood in the enemy's wall — walked through the real bridge, it reached the gate cell and sat there for 400 ticks still in `to-resource`.

**Investigation.** The starvation clock was stamped on cell change alone, so it counted every tick a unit spent not moving for ANY reason; the 750-tick threshold had been tuned against walking villagers (healthy maps never held one still past 500), so the constant was validated against a different quantity than the code counted. The census that called the boot map "preserved field-for-field" ran to 20,000 ticks and the rule first engages there at 15,284 in self-play — the same horizon error as the camp candidate one commit earlier. The cache defect: `flipConvertedUnit` writes `unit.owner` in place; gate admittance reads the asking unit's owner; the approach-plan cache keys on (unit, start cell, target, structural revision) and replays a stored `nextStep` without re-checking passability; nothing bumped the revision, and a stationary unit's entry lives until the next unrelated structural change — measured in play at a median of 97-746 ticks between bumps and up to ~2,000. The `worldOccupancy` comment had named exactly this boundary and accepted it when the only cache stored null verdicts; the positive cache extended the same unpinned input to concrete paths without revisiting it.

**Root cause.** (1) A clock that measured "ticks since last cell change" standing in for "ticks waiting in a contested passage" — two quantities that coincide only for a unit that never stops trying to move. (2) An input the search reads — the asking unit's owner — that the cache key does not carry, changed in place by a mechanic whose author did not know the cache existed. Behind both: the tests pinned the container, not the derivation; the review showed a one-line revert of the election fix and a one-line deletion from the cache key both passed every gate in the repo.

**How it is checked from now on.** The clock restarts when a unit resumes asking after a gap of more than one tick (`trafficProgressClock`, one rule for the stamp and the snapshot), and the election reads it from the tick-start snapshot. Gates: `movementTrafficStarvationClock.test.ts` (a unit idle 2,000 ticks is not elected over one that has asked for 800; both consult orders agree), `movementTrafficReplenishedJam.test.ts` (the real arbiter with walking modelled at 0.32 fine units per admitted tick against a replenished stream — fails when the clock is stamped on every consult and when the threshold is 1, the two mutants the old suite could not see), fairness boundary cases at 749/750/751, and `movementTrafficReliefBootMap.test.ts` (the boot map, self-play, 30,000 ticks: zero phantom relief, and the rule still engages — after the fix 229 admissions across 10 units, first at 22,195). The conversion flip calls `notePassabilityChange()` (required dep, both unit and building flips); `convertedUnitGateReplan.test.ts` walks the defect through the real bridge on the new `converted-unit-gate-fixture` and fails both ways with the bump removed; `movementPlanOpsCacheKey.test.ts` pins the key's range leg (the review's surviving mutant) and the owner-bump seam. The audit for OTHER in-place writes of a passability input found one more — `upgradeOwnedUnits` rewrites `unit.unitType`, and the unit's domain is read by the same predicate — and `unitDomain.test.ts` now asserts that no line in `UNIT_LINE_UPGRADES` crosses a domain, which is what keeps that sweep bump-free; a sheep changing owner (`visibility.ts`) is not an instance, because resource ownership is not a passability input, and teams are written only at seed time.

**What this predicts.** Any other input `isCellPassableForUnit` reads that is changed in place — a future capture mechanic, a team change mid-match, an upgrade that turned a unit into a ship — is the next instance; `worldOccupancy.structuralRevision`'s comment now lists the boundary honestly, including recycled unit ids (C2, unfixed, low). And any constant tuned against one measured quantity while the code counts another will pass every census that stops before the code's quantity diverges from the measured one.

## 2026-09-02 — Villagers gather inside an enemy Town Centre's range and die there (fixed and gated the same day, v0.3.191)

**Symptom.** Found by an independent critic reviewing v0.3.177, not by play: at 45,000 ticks on `aoe2-prototype` the candidate's owner 2 lost thirty villagers between ticks 35,750 and 45,000 — every death inside owner 1's Town Centre range (enemy TC within 9 cells; owner 1 had at most one soldier), 29 of 30 in `to-resource`, fourteen of them walking to the same boar beside the enemy TC at (12,7-9). On `seed-2` the same: of 27 late deaths, 24 were en route to or inside owner 1's base (gold at (13,9) and (8,15), trees along y=14). Nobody raided; the villagers were sent.

**Why it is a baseline defect, not a candidate one.** Neither the gather comparator (`villagerGatherAssignment.ts`) nor the hunt phase (`aiSystemHuntPhase.ts`) knows where enemy static defences are. The v0.3.177-era baseline lost zero villagers by 45k only because its smaller economy had not exhausted the resources near home; the candidate's larger economy (40 villagers, 26 buildings by 30k) thinned the home woodline sooner and the assignment then reached for the nearest remaining nodes, which were the enemy's. Any economy that grows enough hits this — and by v0.3.190 the ORDINARY baseline had grown enough: on the boot map at 45,000 ticks it loses 43 villagers under the enemy Town Centre with no candidate involved. Predicted here on 2026-09-02 and true three versions later.

**Root cause.** `assignNearestResource` ranks every gatherable node of the wanted kind by distance to the villager's reference drop-off and takes the first; `findNearestDropOffBuilding` takes the nearest drop-off; `runHuntPhase` takes the nearest live boar within twenty cells of the Town Centre. None of the three read the enemy's buildings, so an enemy Town Centre's reach — six cells from its footprint, nine with the arrow technologies — was just more map. The home-range bound (v0.3.30) hid it: on the boot map every node within 24 cells of owner 2's drop-offs is its own until its economy grows enough to strip them, after which the nearest remaining wood, gold and boar are owner 1's, five to nine cells from a building that kills a villager in six seconds.

**How it is checked from now on.** The direct gate landed with the fix (v0.3.191, `design/spec-final.md` §6.4): automatic assignment excludes any node, drop-off or hunt target inside an enemy static defence's reach plus one cell, and falls back through the other resource kinds before it accepts a dangerous node as the last thing left. Four layers. `tests/simulation/enemyDefenceRange.test.ts` pins the geometry (range + 1 is inside, range + 2 is not; footprint not anchor; allies and foundations never exclude), the reach ARITHMETIC bonus by bonus (`effectiveStaticDefenceRange`: arrow technologies on every building that shoots, Keep on Watch Towers only, Crenellations on Castles only, Koreans' towers by age, and the widest stack), the box-to-box footprint variant the drop-off choice uses, and the assignment's own filter including the `dangerous-only` answer the kind fallback depends on. `tests/simulation/enemyDefenceRetarget.test.ts` covers the one mechanism the class gate cannot reach: the boot map never raises a tower on the other side's woodline, so deleting the re-target would leave that gate green. `tests/simulation/villagersAvoidEnemyDefences.test.ts` reproduces the defect through the bridge — a villager whose nearer bush stands under the enemy Town Centre takes the far one, chops a safe tree before it accepts the dangerous bush, and takes the dangerous bush only when nothing else of any kind exists, and the AI hunt party takes the far boar. The CLASS gate is `tests/simulation/villagersAvoidEnemyDefencesSelfPlay.test.ts`: 45,000 ticks of self-play on `aoe2-prototype`, reading the raw death feed, asserting no owner loses more than three villagers inside an enemy Town Centre's, tower's or Castle's fully-upgraded reach. RED-CHECKED by taking the rule out (`collectStaticDefences` returning nothing) and re-running the same gate: 43 against a bar of 3. `scripts/selfplay-audit.mjs` carries the same census as two columns (`vill lost`, `under def`) so a future audit reports it rather than needing a critic to count.

**What the GATES could not see about themselves.** Three separate defects in the checking, each found by an independent critic and none by a test run. (1) The census asked whether the NEAREST enemy building covered the death cell, not whether ANY did — so a villager eleven cells from a Town Centre (reach 10) and twelve from a Castle (reach 12) scored as safe, which is the configuration a late-game base creates. (2) The first attempt at an anti-truncation assertion, `lastTick >= 40_000 || outcome !== 'running'`, is true by construction on a loop that exits only those two ways: a tautology, banked as a fix in three documents before anyone read it against the loop. (3) The comment justifying the census's deliberately narrow net claimed the exact reach was "gated separately by `enemyDefenceRange.test.ts`" — nothing in the repo referenced `effectiveStaticDefenceRange` or `collectStaticDefences`, and every case in that file handed the predicate a hand-written range, proving it reads the field and nothing about what fills it. A gate's own comment is the least-checked prose in a repo, and all three of these lived there.

**What number moved.** Measured on the boot map at 45,000 ticks, the same tree twice, the only difference being that `collectStaticDefences` returns nothing. Without the rule, owner 2 loses 50 villagers, 43 of them inside owner 1's reach; with it, 7 and none. `seed-2` is byte-identical either way (0 under enemy defences), because neither economy there ever reaches the other's base — which is why the earlier session, measuring only seeds like that one, recorded "BEFORE == AFTER to the tick" and concluded the fix could not be shown on a current baseline. It can: the baseline had grown (v0.3.190's DE build times and nearest-first approaches move owner 2's Castle from 35,250 to 26,750), and a bigger economy is exactly the precondition. Ages, building variety, unit variety and peak army are identical with and without the rule on both seeds — which says the rule costs nothing THOSE columns can see, and no more than that: they are sampled every 250 ticks and two builds whose villager losses differed by nine produced byte-identical columns, so they are the wrong instrument for arguing the 43 villagers were free. The claim the evidence does support is narrower: the rule saves 43 villagers and no coarse progression measure moves against it.

**What the first fix missed, and re-measuring found.** The rule as first written still lost NINE villagers under owner 1's Town Centre at ticks 38,793-39,191 — every one `desired=gold`, every one automatically assigned, every one walking to a mine three cells from that Town Centre. The cause was a SECOND idle-assign site: `villagerEconomySystem` assigns work once at the top of the villager loop (through `assignIdleGatherer`, which walks the kinds safe-first) and again at the bottom, for the villager that has just delivered a load and is idle in the same tick — and that second call went straight to `assignResource`, so it skipped the kind walk and took the exception the moment safe gold ran out. Every delivery handed the miner the same enemy mine back. `assignNearestResource` has SIX callers on this tree, and the enumeration took two independent critics to finish: the two idle passes, the unreachable-target reroute and the give-up reroute (all four now take the cross-kind walk); the over-subscription redistribution, which must NOT change the villager's resource kind and so refuses a dangerous node outright and leaves the villager on the node it already has; and the re-target this change itself added. The un-fixed over-subscription site was a smaller hole than first written here: without `requireSafe` a dangerous node is chosen only when NO node of the kind is safe, which includes the villager's current one, so it could only ever have moved a villager between dangerous nodes of one kind, never off a safe node. Generalisation, and the reason this entry stays after it is gated: a rule installed in a shared ASSIGNMENT function is only as good as the enumeration of the places that call it. The author enumerated three, the measurement found the fourth, and the critic found the fifth.

**What this predicts.** The rule bounds the NODE, not the ROUTE: a villager sent to a safe node on the far side of an enemy base still walks through the arrows to get there, and nothing measures that yet. The measured runs show none of it — all 43 deaths were at dangerous nodes — but the boot map's bases are 40 cells apart, and a map where the shortest path home runs past an enemy tower is the next instance. The second prediction is the shape of the class: any other rule added to a shared assignment or targeting function will be inherited only by the callers someone enumerated, and this one needed a 45,000-tick self-play plus a per-death trace to find the fourth caller and an independent critic to find the fifth. Three more instances of the class are already visible and unfixed. (1) The AI still SITES buildings without asking where the enemy's arrows fall, so it can pay for a mining camp inside an enemy Town Centre's reach that the gather rule then refuses as a node and the drop-off rule refuses as a drop-off. (2) The AI's hunting party issues its orders through the same right-click channel a player uses, and if the boar dies in the one tick between the intention and its resolution the order lands on the carcass as an EXPLICIT gather order — which permanently exempts that villager from the re-target, on a carcass that may be under enemy arrows. (3) The reachability probe is capped at sixteen candidates per list, so a kind with seventeen or more safe nodes whose nearest sixteen are unreachable reports "no safe node reachable" and buys the exception; §6.4 now states that bound rather than promising more than the code does. (4) For the HUMAN slot the re-target can never fire at all: a non-AI villager is only auto-assigned when `hasExplicitGatherOrder` is set, and the re-target refuses to touch exactly that flag, so the two predicates are complements — a human player's villager is never moved off a node a new tower has just covered. Separating "the player right-clicked this node" from "the game assigned it and set the flag so it would keep working" needs a second field on the gatherer. (5) The COST of this gate is not paid for twice on purpose: it plays the same 45,000-tick boot-map match as `aiReachesCastleAge.test.ts` — same seed, same options, same horizon — and the two are the longest tests in the suite (measured together: 440 s and 468 s under the full suite on a 32-core machine, 219 s and 159 s alone). Both critics say they should share one run, and they are right; folding the census into the Castle gate is the named next step and was not taken here.

## 2026-08-23 — Two buttons that did nothing: the Transport Ship, and the Goths' Huskarl at the Barracks

**Symptom.** Clicking `Train Transport Ship` at a Dock did nothing at all: no unit, no queue entry, no resources spent, no error, no toast. The button rendered as enabled. In a browser probe on `naval-imperial-fixture`, wood stayed at 2000 across the click while the same click on `Train Fishing Ship` charged 75. The Transport Ship is the only way a land army crosses water, so the effect was that a documented, menu-visible mechanic could not be used at all in a real game. The same defect was live for the Goths' Huskarl: their Anarchy unique technology is supposed to move the Huskarl into the Barracks, and the button appeared there and did nothing.

**Investigation.** The train MENU and the train VALIDATOR read two different tables. `trainOptions.ts` builds the card — and it offers `transport-ship` at the Dock from the Feudal Age, and the Anarchy unlock through `unlockedTrainingFor`. `TRAINABLE_UNITS_BY_BUILDING` in `buildingProductionTables.ts` is what `canTrainAt` consults when the command is validated, and it listed neither. So the UI accepted the click, the validator refused the command, and nothing reported the refusal — the worst possible failure shape, because there is no error to notice.

**Root cause.** Two tables that must agree, with nothing making them agree. Every unit added to a menu had to be remembered in a second place, and twice it was not. The Transport Ship shipped this way in the naval slice; the only transports that ever existed were spawned by fixtures, which is exactly why every naval test passed.

**How it is checked from now on.** `tests/simulation/menuMatchesValidator.test.ts` enumerates what the train menu can offer across every building, all four ages, all 30 civilization names and both extremes of the researched set, and asserts the validator accepts every one of them. It caught both instances on its first run. A companion assertion checks the mirror direction for technologies — every technology with a cost is hosted by some building — and the Transport Ship is pinned by name as well, so a regression names the unit rather than only the class.

**What this predicts.** Any pair of tables that must agree and are not derived from each other is the same defect waiting to happen. The menu/validator pair is now gated; the remaining pairs of this shape (armor-class tables vs stat tables, render recipes vs the roster) are where to look next.

## 2026-08-23 — The research half of the same pair: two elite naval upgrades

**Symptom.** A Viking Dock showed `Research Elite Longboat` and a Korean Dock showed `Research Elite Turtle Ship`. Both clicks were silently refused — no charge, no queue entry, no error. Each civilization's unique ship could be trained and could never be upgraded, so half of two civilizations' naval identity was unreachable.

**Investigation.** Found by WIDENING the gate written for the Transport Ship defect above, not by a report. That gate checked the train pair tightly (per building) and the research pair loosely (a technology must be hosted by SOME building). Tightening the research side to per-building — every technology a building's card offers must satisfy `canResearchAt(thatBuilding, tech)` — failed immediately with both names.

**Root cause.** The same missing derivation as the entry above: `optionsRules` computes the Dock's card, `RESEARCHES_BY_BUILDING` decides what the Dock accepts, and nothing tied them together. The loose check passed because the CASTLE's research list contains every elite upgrade, including the two naval ones, so "hosted somewhere" was satisfied by the wrong building.

**How it is checked from now on.** The per-building assertion in `tests/simulation/menuMatchesValidator.test.ts`, over every building x 4 ages x 30 civilizations x both tech extremes, for the researchable list AND the visible (locked-but-drawn) list. The two upgrades are additionally proved end to end in `dockTechs.test.ts`: a Viking and a Korean Dock complete the research in a real match.

**What this predicts.** An aggregate assertion ("hosted somewhere", "exists in some table", "appears in the union") is not coverage of a MAPPING. Where two tables are keyed by the same thing, the check has to be keyed by that thing too.

## 2026-08-23 — The mirror direction: an upgrade the tables hosted and no card offered

**Symptom.** The Skirmisher line never improved in a real game, six versions after the tier was added to fix exactly that. An Archery Range never offered `Research Elite Skirmisher`, so no player could start it; the unit existed, cost what units.csv says, and was unreachable.

**Investigation.** Found by running the menu/validator gate in the direction it did not cover. That gate asserted menu ⊆ table: every button a card draws must be one the validator accepts. The mirror, table ⊆ menu — every technology a building HOSTS must be offerable by some card, for some age, civilization and researched set — reported one entry: `archery-range: elite-skirmisher-upgrade`. `optionsRules`' archery-range block offered the Crossbowman, Arbalest, Heavy Cavalry Archer and Thumb Ring, and had never gained a line for the Skirmisher line's only upgrade.

**Root cause.** Same missing derivation as the two entries above, in the other direction. v0.3.45 added the unit to `UnitType`, the stats to every table, the upgrade to `UNIT_LINE_UPGRADES`, the cost to `RESEARCH_COSTS` and the entry to `RESEARCHES_BY_BUILDING` — every table the typechecker enumerates — and the option list is not a table the typechecker enumerates.

**Why the tests said otherwise.** The browser test asserting the Elite Skirmisher was reachable booted `new-unit-reach-fixture`, whose owner starts with `elite-skirmisher-upgrade` ALREADY RESEARCHED so the train menu would resolve to the new tier. It proved the unit could be trained once you had the upgrade, and said nothing about getting the upgrade. **A fixture that grants the thing under test cannot test how the thing is obtained.**

**How it is checked from now on.** The mirror assertion in `tests/simulation/menuMatchesValidator.test.ts` (table ⊆ menu, over every building x 4 ages x 30 civilizations x two tech states per candidate, including the all-but-this-one state that satisfies prerequisites), plus an end-to-end test in `unitLineTiers.test.ts` that researches the upgrade through the menu in a real match and asserts the train entry resolves from `skirmisher` to `elite-skirmisher`.

**What this predicts.** Look at what each fixture PRE-GRANTS before trusting what a test proves. A `startingResearchedTechnologies` list is a list of things that test cannot check.

## 2026-08-24 — Two AIs that never fought each other

**Symptom.** A deterministic 12000-tick all-AI match on the default map ended with `enemyMilNearBase = 0` for both owners at every sample. Twenty minutes of game time, one owner holding 15 military units in Castle Age, and not one engagement. In a skirmish with more than one AI player, the AIs would build side by side until the clock ran out.

**Investigation.** Found by PLAYTESTING, not by a test — the whole simulation suite was green, and stayed green after the fix. `aiSystemAttackPhase` chose its target with `findOwnedUnit(humanPlayerId, 'villager')` and `ctx.humanTownCenterId`: the human player, by id, and nobody else. An AI in the human slot therefore had itself as its target and never marched. A discriminator fixture (two AI armies 32 cells apart, vision 4, so neither can see the other and auto-aggression cannot fire) measured it exactly: owner 1's closest approach to the enemy base was 31 cells — it never left home — while owner 2 marched to within 4.

**Root cause.** A rule written for the only case anyone tested. Human-versus-AI is the shipping configuration, the human IS the AI's nearest enemy there, and every AI test in the suite is that configuration — so the identifier and the intent agreed everywhere the tests looked.

**How it is checked from now on.** `tests/simulation/aiAttacksAnyEnemy.test.ts`: the two-AI march discriminator, plus direct tests of the target choice (two players picks the other; several picks the nearest; ties break on the lower owner id; a base-less AI still picks somebody; no enemy Town Centers means no target). The march test fails without the fix, at 900 ticks.

**What this predicts.** Any rule keyed on `humanPlayerId` is a rule that only works in the configuration the tests use. The remaining ones are worth reading with this in mind — the villager auto-gather gate was already found to be one of these (2026-07-02), which makes this the second.

**Audited 2026-08-24, same session.** Every remaining `humanPlayerId` reference in `src/game/simulation` was read. They fall into three legitimate groups and none is a third instance: INPUT (a player may only command their own units — `unitCommandOps` gating a context-click on `unit.owner !== humanPlayerId`), VIEW (selection, fog memory, render state, minimap — the screen belongs to one player), and REPORT (`winConditionResolverSystem` and `matchEndOps` label the outcome `victory` or `defeat` from the human's seat, and break a wonder-tick tie in their favour). The simulation RULES that were keyed this way are the two already fixed: the villager auto-gather gate (2026-07-02) and the attack target (this entry). Visibility itself is per-owner (`isVisibleToOwner(owner, …)`, `findPreferredVisibleEnemyUnit(owner, …)`), so an AI does not see through fog.

## 2026-08-24 — A stray arrow ended the match

**Symptom.** A 40000-tick AI-versus-AI run crashed at tick 24173: `EngineRangeError: visibility_out_of_bounds`, `Grid coordinate (33, -1) is out of bounds`, thrown out of the render projection. The match was healthy up to that point — both players in Castle Age, one being raided down from 20 villagers to 2 — and then the process exited.

**Investigation.** The stack is `refreshRenderProjection` → `projectFrame` → `visibleProjectiles` → `visibility.isVisible(playerId, x, y)`. The coordinate has y = -1, so a projectile was above the top edge of the map. `projectileMissAimPoint` offsets the aim from the target by a hash-driven direction and distance with no knowledge of the board, so a shot at a target standing on an edge can be aimed off it — and the interpolated flight position then leaves the map. The engine's `VisibilityMap.isVisible` asserts its argument is in range and throws rather than answering false.

**Root cause.** Two independent gaps that only matter together: the simulation could produce an off-map projectile, and the presentation asked a question about it that the engine treats as an error. It stayed hidden because it needs a target on the very edge of the map AND a miss rolled outward AND a fight near an edge — which is why it only appeared once AIs started actually fighting each other (v0.3.54, the previous entry).

**How it is checked from now on.** `tests/simulation/projectilesStayOnTheMap.test.ts`: 240 shots at targets on every edge and corner assert the aim stays inside the map, a scatter check keeps the clamp from degenerating into a bullseye, and the projection is handed an `isCellVisible` that THROWS out of range — the engine's real contract — and must omit the shot instead. Two of the four fail without the fix, with the production error message.

**What this predicts.** Engine calls that ASSERT rather than answer are a defect class of their own: every coordinate this repo passes into an engine query has to be range-checked on this side. Visibility was one. The others worth reading with this in mind are pathing and occupancy queries fed by anything interpolated or scattered.

**Audited 2026-08-24, same session.** Every `visibility.isVisible` / `isExplored` call site in `src/game/simulation` was read and classified by where its coordinate comes from. Entity positions (unit deaths, AI target checks, fog memory, target finding, selection) are always on the map, because occupancy keeps units there. The two that walk a FOOTPRINT from an anchor — `isFootprintVisible` and `isFootprintExplored` in `pureHelpers` — range-check nothing, and are correct only because a building's cells are all on the map. That guarantee was not pinned anywhere, so it is now: `placementSearch.test.ts` confirms a 4x4 building anchored at each edge and corner is REFUSED rather than placed or thrown on. Empirically the placement path already refused all four (`confirmed=false`), so no second instance existed — but the invariant those helpers depend on is now a test rather than an assumption.

## 2026-08-29 — Every gate a player built stayed shut to them

**Symptom.** Reported out of an independent review of v0.3.160, not from play: a Palisade or Stone Gate raised during a match never admits its owner's units. Walling your own base is a way to lock yourself out of it — the one thing a gate exists to prevent. Every gate test in the repo passed.

**Investigation.** `cellPassability.admitsThroughGate` computed completion as `!constructionStatesCodec.has(entity)` — "there is no construction record, so it must be finished". That is true of a scenario-SEEDED building, which never gets a record at all, and never true of a BUILT one: `finalizeBuildingConstruction` sets `construction.isComplete = true` and deliberately keeps the record (build progress and population supply are read from it afterwards). Every gate in every fixture is seeded, so the whole gate suite — owner passes, enemy blocked, ally passes, unfinished blocks — exercised only the branch where the shape happens to agree with the field.

**Root cause.** A state predicate inferred from a data structure's SHAPE (entry present or absent) instead of the FIELD that records it. Every other completion reader in the simulation uses `construction.isComplete`, including the economy snapshot two files away; this one call site inferred it, and drifted the moment the finalize path stopped deleting entries.

**Second defect underneath it.** A gate finishing changes who may pass a cell without claiming or releasing anything, so it does not move `worldOccupancy.structuralRevision` — the counter v0.3.160's unreachable-plan cache keys on. Fixing the gate alone would therefore arm a stale cache: a unit that gave up while the gate was a foundation keeps refusing the route after it opens. Measured, not theorised — with the fix in place and only the new `notePassabilityChange()` call disabled, the villager never uses the route its own gate opened.

**How it is checked from now on.** `tests/simulation/gateBuiltInMatch.test.ts` walks a gate that is BUILT rather than seeded: the owner crosses it, the enemy does not, a foundation shuts everyone out, and a villager whose route was sealed under it mid-walk resumes once the gate opens. The first and last both fail without their respective fixes. The fixture (`gate-built-in-match-fixture`) is the seeded gate fixture with the gate row left open for the player to close, so the built path can never again be tested only through seeded buildings.

**A second instance of the class, found by the review of this fix — FIXED in v0.3.164, see its own entry below.**

**What this predicts.** Two classes worth reading with this in mind. First, shape-inferred state: every other `constructionStatesCodec` reader was audited this session and uses `isComplete` — the class is closed for construction, but any predicate written as "the map has no entry, therefore X" is one refactor away from this. Second, and larger: a fixture family that only ever exercises the seeded path. Gates were tested exclusively through scenario-placed buildings for the whole life of the feature. Anything a player can BUILD deserves at least one test that builds it, because seeded and built entities differ in exactly the state that completion, population, and progress are read from.

## 2026-08-29 — main's CI had been red for two months, and nobody looked

**Symptom.** Found by watching the remote gate after a push, which the fleet canon requires and this session had not been doing. `gh run list --branch main --workflow CI --limit 200` returns **200 runs and zero successes**, back to 2026-07-07. Every push in that window went out behind a green local gate. Five of them were mine, this session.

**Investigation.** Two independent causes, one per platform, both in the "Fetch pinned voxel source" step shared by `ci.yml`, `playtest.yml`, and `playtest-llm.yml`.

*ubuntu-latest* built `voxel@0.1.4` while the working tree builds against `voxel@1.2.0`: `.github/voxel-commit` still pinned `faa00bf2`, so typecheck failed on APIs that exist only in the newer voxel (`MOEBIUS_RESOLVE_PRESET`, `StylizedResolveOptions`, `setStylizedResolve`, the `stylizedResolve` runtime option). The local gate cannot see this: it builds `../voxel` at whatever the sibling checkout happens to be, which is the pushed HEAD.

*windows-latest* never got that far — it died in the fetch with `tar (child): Cannot connect to D: resolve failed`. `$RUNNER_TEMP` on a Windows runner is `D:\_temp`, and GNU tar reads a leading `D:` in an archive path as `host:path`. The sibling civ-engine step was written with a relative archive path and so never hit it.

**Root cause.** A pinned dependency with no mechanism tying the pin to what the code actually requires, plus a platform-specific path bug in the step that fetches it — and, above both, nobody reading the remote gate. The canon already carried this exact lesson from the Voxel repo's five-week outage ("the local gate and the remote gate run on different machines, and only the remote one is what a collaborator sees"); aoe2 then ran the same failure for twice as long.

**A third cause, found while fixing the first two — and the reason they lasted.** CI ran only on pushes touching `package.json`, `package-lock.json`, or `.github/voxel-commit`. A workflow file was not in that list, so **a fix to CI could never trigger the CI that would prove it**: the Windows tar fix went out invisibly, and the only way to learn whether it worked was to push something unrelated and remember to look. `playtest.yml` had the identical gap for `playtest-corpus.json`, the config its own gate reads. Both now include `.github/workflows/**` (and the corpus its config) in the filter, with `workflow_dispatch` alongside.

**Resolved 2026-08-29.** CI green on ubuntu AND windows — the first fully green run since 2026-07-07 — and playtest-corpus green behind it. The Windows fix took two attempts because the first made the symptom MOVE rather than vanish: with the archive path fixed the job died one line later on `-C`, since Git-bash mangles a drive-lettered path wherever it appears, not only after `-f`.

**How it is checked from now on.** The pin is advanced to voxel's pushed HEAD (`08398b57`, 1.2.0) and every path in the fetch step is relative in all three workflows. `npm run ci:status` prints both gates' conclusion AND the length of the current streak, exits non-zero on red, and the session-start ritual in AGENTS.md names it, so "read the remote gate first" is a command rather than a good intention.

**What this predicts.** A gate that cannot be triggered by a change to itself is untestable by construction, and every path filter deserves reading with that question. Any pin whose correctness is only checked on a machine that does not use it will drift: the voxel commit here, and by the same shape the civ-engine `engine-dist` tarball, which the local gate also never fetches. And a red gate nobody reads is indistinguishable from no gate at all — the count is the tell. A run list that is uniformly one colour, in either direction, deserves suspicion before it deserves trust.

## 2026-08-29 — On the map the game boots, one AI never banks food (RESOLVED v0.3.163)

**Symptom.** `aoe2-prototype` is `DEFAULT_SEED` — the map a player lands on. Run it all-AI and owner 1 reaches Feudal around tick 16,000 while **owner 2 is still in the Dark Age at tick 24,000** (40 minutes of game time). Reproduced on `corpus-seed-b`. The measured anomaly is income, not headcount: across 10,000 ticks owner 2's food **never exceeds ~50 and repeatedly reads 0**, while 7 to 10 of its villagers are alive and working. Its Feudal age-up needs 500.

**Why no gate caught it.** `progressionCheck` passes when ANY living owner reaches the required age (`reached.some(...)`), so a permanently frozen second player is structurally invisible to the corpus gate. The gate is green on a match where one side never plays.

**Ruled out, each by measurement.** Not enemy raids on the base: the Town Centers are 43 tiles apart, the nearest owner-1 unit sits 15-50 tiles from owner 2's Town Center at every sample, never inside the AI's `RAID_RADIUS` of 8, and both players' building counts grow. Not wildlife: the wildlife damage path was instrumented to log every kill it makes, and it logged NONE while the counts moved. Not gather assignment walking villagers onto a live boar: read at component level off `gatherer.targetResourceId`, zero villagers ever target one. Not villager auto-aggression: wildlife are resources, not units, so the stance system's `unit` query cannot see them. Not sheltering: villagers garrison only twice in 10,000 ticks (2 and 4 of them) and are back out by the next sample.

**A correction, because the first version of this entry got it wrong.** It claimed "ten villagers are lost in clusters", read from `getEconomyState().units`. That snapshot is built from `world.query('position', 'unit')`, and garrisoning REMOVES a unit's `position` component — so sheltering villagers silently drop out of it and reappear later. Measured against the authoritative world entities, the snapshot showed 3 villagers while 7 were alive. The oscillation was mostly visibility, not death; the real attrition is mild (10 to 7 over 10,000 ticks). Any future probe counting units must count world entities, or state explicitly that it is counting the POSITIONED ones.

**Measured: the villagers walk instead of gathering.** Sampling owner 2's villager task every 50 ticks for 10,000 ticks, against the same measurement for a seed where the AI DOES age up:

| | `aoe2-prototype` (stuck) | `default-seed` (ages up) |
|---|---|---|
| gathering | **32%** | 62% |
| walking to a resource | 32% | 22% |
| hauling back | 21% | 13% |
| idle | 9% | 0% |

Half the gathering share, and more time in transit than at the resource. With ~8 villagers that is roughly one villager-equivalent actually on food, ~0.33 food/s gross, which villager training at 50 food each consumes entirely — hence a stockpile that never passes 50.

**Four more causes eliminated.** Drop-off placement is not it, and is in fact BETTER than the winning player's: at tick 10,000 owner 2 holds mill at distance 2 and lumber-camp at 4, with berries 3 tiles out and a 1-tile haul, while owner 1 ages up with its nearest sheep **41 tiles** away. Reassignment churn is not it: role flips number 2-4 per player across 10,000 ticks, so the v0.3.159 no-inversion guard is holding. Target switching is only modestly higher for owner 2 (150 vs 105) and is ordinary depletion. And the Dark-Age gold villager (12% of owner 2's villager-time) is DELIBERATE — `aiDecisionOps` documents it as the canonical AoE2 fourth-villager opening and warns that removing it breaks age progression.

**One cause found and FIXED (v0.3.162), and it was not the whole story.** Tracing a single owner-2 food villager tick by tick — the instrument this entry asked for — caught it standing IDLE for 211 ticks holding a full carry of 10 food. `assignNearestResource` set `task = 'idle'` at both of its no-target branches, and the idle-to-assign path re-ran assignment, found nothing again, and left the load in the field. Fixed: a villager that cannot find work now hauls what it is carrying (`parkOrDeliver`), pinned by `tests/simulation/loadedVillagerDeposits.test.ts`. Measured effect on this map: owner 1's Feudal moved from ~16,000 to **12,500 ticks**, a 3,500-tick gain — it had been leaking carries the same way, just not enough to freeze. **Owner 2 is still Dark-Age at 20,000**, so the freeze has at least one more cause.

**Narrowed, then partly RETRACTED (2026-08-29, after the v0.3.162 carry fix).** Owner 2's phase budget is 48% walking to a resource, 36% idle, 8% hauling, **8% gathering** — against owner 1, which ages up normally on the same map.

I first reported that "all six villagers sit idle with `targetResourceId=null` amid abundant resources". That was a SINGLE-TICK sample and it does not hold up. Measured across 1,000 ticks: **owner 2 holds a target 77% of the time, owner 1 100%**. The gap is real and worth explaining, but it is a 23% orphan rate, not a total failure — and the dramatic reading was exactly the one-tick trap this entry warns about two paragraphs down. I made it anyway, one iteration after writing the warning.

**What is solidly established.** Assignment is not failing: instrumented over 200 ticks it runs for owner 2, its candidates pass the harvestability and kind filters, and it never takes a no-target branch. Nothing is abnormally clearing targets either: every `targetResourceId = null` site was tagged and only THREE fired in 200 ticks, all from `dropOffStep` after a normal deposit. So targets are set and mostly kept — yet gathering is 8%. The time is going into transit against targets the villager is far from (the not-moving histogram put most of it at 9+ tiles), while near nodes sit unused: 6 berry bushes, 2 sheep, 4 gold mines, 4 stone mines and 30+ trees within 8 tiles, all `baseOwner=2` or unowned, with `MAX_GATHERERS_PER_RESOURCE = 2` giving ample capacity for six villagers.

**ROOT CAUSE, found by that exact measurement.** Instrumenting the distance at the moment of assignment: owner 2's food targets sit a **median 7 tiles** away against owner 1's **3**, and owner 2 reassigns **59 times in 1,000 ticks against owner 1's 16**. A 7-tile walk at §12.4.2 speed takes ~87 ticks — and `GATHER_APPROACH_TIMEOUT_TICKS` was a flat **80**. The villager abandoned its target JUST before arriving, was reassigned to another far target, and repeated forever.

That constant is a v0.3.160 casualty. It was written when a unit moved 0.5 tiles/tick, so 80 ticks bought ~40 tiles of walking and only a genuinely queued villager reached it; the honest movement clock walks 0.08 tiles/tick, turning the same 80 ticks into 6.4 tiles. Owner 1's 3-tile food (~37 ticks) never crossed it, which is exactly why the freeze looked map-specific.

**Fix (v0.3.163).** `gatherApproachBudgetTicks(distance)` makes the fan-out budget a TRAVEL allowance — the walk the villager was actually handed, plus margin, with a floor — so it tracks any future change to the movement clock, which a bare tick count cannot. Pinned by `tests/simulation/gatherApproachBudget.test.ts`: the budget must always outlast its own walk, must stay bounded so fan-out still fires, and must grow with distance.

**And a gate that can SEE it (v0.3.164).** The reason this hid for a whole session is structural: `checkAgeProgression` passes when ANY living owner reaches the age, so a match where one side never plays reads green. `checkEveryOwnerAgeProgression` is the strict companion — every LIVING owner must have reached it, with eliminated players still exempt because losing is not a stall — opt-in per corpus row via `requireAgeForEveryOwner`. It is enabled on a new `boot-map-both-owners` row running `aoe2-prototype`, the map this defect lived on, where both owners now age up at 9,300 and 11,900, inside the 13,000 budget.

One hard constraint bounds this, and it is worth recording precisely: **the replay bundle has a length ceiling between 13,000 and 14,000 ticks.** `JSON.stringify(result.bundle)` in `scripts/playtest.mjs` throws `RangeError: Invalid string length` at 14,000, 16,000 and 18,000; 13,000 succeeds. So no corpus row can run longer than ~13,000 ticks at all, which caps every long-horizon gate this repo can have until the bundle is streamed or trimmed. That leaves the boot-map gate ~700 ticks of margin over its slower owner (12,300) — enough to catch a FROZEN player, which is dark-age at any budget, but thin against a merely slow one. (The v0.3.163 default-seed regression that first blocked this is gone in v0.3.165, so that row now reaches Feudal on both owners inside 13,000 as well.) And the gate was RED-CHECKED end to end rather than trusted for printing "ok": temporarily requiring `castle-age` made the corpus exit 1 with "a living owner never reached castle-age (behind: 2:feudal-age, 1:feudal-age)". A gate that has only ever passed is not known to work.

**Measured across three seeds — and CORRECTED after review (v0.3.165).** The first fix (v0.3.163) made the budget a function of the villager's CURRENT distance, recomputed every tick. Review showed that inverts the rule: the budget shrinks as the villager closes while its timer keeps running, so it fires at ~56% of any walk and, below 11.5 tiles, gives LESS reach than the flat 80 it replaced — 3.9 tiles into a 7-tile walk against the old 6.4. It also measured the walk with Euclidean `hypot` while movement steps ONE AXIS per tick (so travel is MANHATTAN, and the budget was short for anything more than ~18° off-axis), collapsed to its floor for a villager standing on the destination cell, and could exceed the 600-tick unreachable backstop past 38 tiles, inverting that documented ordering. Its test asserted the Euclidean model and so certified a property the game did not have.

The budget is now **flat, expressed in tiles** (32 tiles → 400 ticks at §12.4.2), which has none of those failure modes and still tracks the movement clock. Measured on all three seeds, it is strictly better than both predecessors:

| seed | flat 80 (pre-fix) | distance-based (v0.3.163) | flat 400 (shipped) |
|---|---|---|---|
| `aoe2-prototype` | 12,500 / **never** | 11,900 / 9,200 | 12,300 / **11,000** |
| `default-seed` | 12,200 / 10,100 | **15,800** / 10,300 | **12,100** / 10,400 |
| `corpus-seed-b` | 9,300 / 20,300 | 9,600 / 15,500 | 8,500 / **17,200** |

No frozen player on any seed, and the `default-seed` regression the first fix caused is gone. Two corrections to what this entry previously claimed: `corpus-seed-b` owner 2 was **20,300 with the old constant, not "never"** — one freeze plus one slowdown, not two freezes. And the honest description is that a constant was retuned and re-measured across three seeds, not that a single root cause explains every outcome: disabling the fan-out branch entirely also cures the `aoe2-prototype` freeze, and the metric is non-monotone in the budget on `corpus-seed-b`, so this is a tuned constant standing on measurement, not a proof.

**Measurement caution for whoever takes this.** Two traps already caught here. Comparing integer CELL positions to detect movement reports ~93% "not moving" for a perfectly healthy walker, because at §12.4.2 speed a villager changes cell only every ~12.5 ticks — compare fine positions or measure over a window. And sampling one tick proves nothing about a staggered system; the first version of this probe saw zero assignment calls and would have concluded assignment never runs. The untested candidates are traffic — villagers queueing or yielding at a contested patch, which `movementTrafficOps` arbitrates — and assignment sending villagers past the near resource to a farther one. Instrument the per-villager trip: for one food villager, log its target, its distance to that target, and its tick-by-tick position for a few complete cycles. A single traced round trip will show whether the time goes to distance, to blocking, or to re-targeting.

**What this predicts.** A gate written as "did anyone progress" cannot see the player who did not, and every such `some()` deserves re-reading as a blind spot. And a snapshot assembled from a component query silently omits whatever loses that component — `position` for garrisoned units here; the same shape will recur wherever a view is built from `query(...)` rather than from ownership.

## 2026-08-29 — Ships sailed through Fish Traps (RESOLVED v0.3.164)

**Symptom.** A Fish Trap is a BUILDING that stands on water — `shorePlacement`'s only water building — and every ship sailed straight through one. Found by the independent review of the v0.3.161 gate fix, not from play, and it is exactly the same defect class: a passability predicate whose justifying comment quietly stopped being true when a later feature shipped.

**Root cause.** `cellPassability.isCellPassableForUnit` short-circuited every water-domain unit to `return true` on any terrain-passable water cell, justified by "Water cells hold no buildings or land resources, so open water is clear". True when written; false since the Fish Trap landed. The predicate never consulted building occupancy for ships at all.

**A SECOND instance in the same file, found by auditing the class.** `isCellPassableForSpawnInDomain` carried the same claim in its own words — "Water carries no buildings or land resources, so a passable water cell is spawnable" — and returned `true` for water unconditionally, so a ship could also be placed on a Fish Trap's cell. The audit that found it is mechanical and worth repeating: grep the simulation for comments asserting a category is EMPTY (`hold no`, `carries no`, `cannot hold`, `contains no`) and check each against what has shipped since. Those two were the only live instances.

**Fix.** Both sites now ask `worldOccupancy.isCellBlockedByBuilding(x, y)`: open water stays clear — the shared spawn check would otherwise reject it merely for being water — but a building on water blocks a ship exactly as a wall blocks a knight. Deliberately buildings only, not resources: fish ARE water resources and a fishing ship must still reach them.

**How it is checked from now on.** `tests/simulation/fishTrapBlocks.test.ts` on a new `fish-trap-blocks-fixture`: a one-cell-tall channel with a trap mid-way, so a ship ordered from one end to the other must pass THROUGH the trap cell or not arrive. The test watches for occupation of the trap cell directly rather than inferring it from arrival. Its control — a second ship in an identical open lane that must cross freely — is what stops a fix that simply makes ships refuse water; the control passed before the fix and the blocked case failed, so the pair discriminates on exactly this defect.

**What this predicts.** Docks sit on the shore and their footprints may include water cells, so this fix could have blocked ships from spawning at their own dock; it does not (the naval and shore-fishing suites pass), but any future water building — a harbour, a sea gate — inherits that question. And the wider class stands: a comment that asserts what a category CANNOT contain is a dated claim, not an invariant, and every one of them should be re-read whenever the game gains a new kind of thing.

## 2026-08-30 — The AI built its drop-offs next to its Town Center, not next to the resources (RESOLVED v0.3.166)

**Symptom.** Villagers spent 45% of their time walking, against an ideal nearer 24% for the geometry involved, and the AI reached Feudal in 18-20 minutes of game time against DE's 11-13. Found by measuring the villager time budget after the earlier freezes were fixed: with idling gone (36% to 1%), what remained was carry distance.

**Root cause.** `aiSystemBuildingPhase` anchored EVERY building at the Town Center (`findBuildPlacementNear(ownerTownCenterPosition, nextBuild)`), except the bootstrap Lumber Camp, which anchored at the builder's own feet. A Lumber Camp, Mining Camp and Mill exist precisely to shorten a carry, and AoE2 puts them ON the woodline and the ore. Measured across three seeds, camps landed a median 5 tiles from what they serve, with a Mining Camp at 16 and a Mill 52 tiles from the nearest berry.

**Fix.** `dropOffAnchorFor` returns the nearest harvestable resource of the kind a drop-off serves, bounded to the owner's neighbourhood; non-drop-offs and out-of-range resources return null so callers keep their old anchor. Camp-to-resource distance dropped to a median 2 (lumber) and 3 (mining).

**Two things measurement corrected mid-fix.** Anchoring a Mill on sheep and boar as well as berries is wrong — both are brought TO the Town Center in AoE2, and a wandering sheep won the nearest-resource contest. And the "mill is 8 tiles from the nearest berry" metric is confounded by DEPLETION: a mill built beside berries that are later eaten reads as badly placed. Haul distance and gather share are the honest measures.

**Outcome, including the cost.** On the worst-owner-across-seeds ranking this repo adopted after an earlier review, **17,200 → 14,100 ticks**. `aoe2-prototype` owner 2: 11,000 → 9,200. `corpus-seed-b` owner 2: 17,200 → 12,100. Owner 2's gather share on the boot map: 51% → 59%. The cost, recorded not buried: `default-seed` owner 1 slows 12,100 → 14,100. Three slots improve, one regresses, two are unchanged.

**What this predicts.** The anchor radius (12) is not currently load-bearing — every camp on every tested map already falls inside it, and 22 gave identical results. It becomes real on a map whose resources sit far from the start position, and that is the case to measure before trusting it.

## 2026-08-30 — A wolf could kill anything and take no damage doing it

**Symptom.** On `wolf-aggro-fixture`, one villager and one wolf, 1,200 ticks: the villager was gone and the wolf was at 25/25 — untouched. The villager never fought back, never moved, and nothing reported anything. Any unit a wolf met died for free, and the only counterplay was a human noticing and issuing an attack command by hand.

**Investigation.** Auto-aggression finds targets through `world.query('position', 'unit')`. Wildlife are `resource` entities — a wolf has a `resource` component and a wildlife state, and no `unit` component at all — so it is structurally invisible to every auto-target path in the game. This is the same shape as the retracted claim in the 2026-08-29 boar investigation, where "wildlife not in the `unit` query" explained a different empty result: the query was answering a question nobody had asked.

The attack path itself was fine. `attackCommandStep` has handled `targetEntityKind === 'resource'` since the boar hunt shipped, and `setUnitAttackCommandDirect` accepts a resource target. Nothing was missing except an issuer.

**Root cause.** No call site. `registerAllSystemsTypes.ts` said so in a comment — "No deterministic-resolution system calls attack today, so no direct helper threaded through this layer" — which was true when written and had quietly become a description of the defect. Wildlife combat landed bites and never told the victim anything had happened.

**How it is checked from now on.** `tests/simulation/wildlifeRetaliation.test.ts` asserts the MECHANISM — the wolf takes damage — rather than who wins, because a 25 HP villager against a 25 HP wolf is close enough that an outcome assertion would pin an arithmetic coincidence. It carries a control — a wolf on `wolf-idle-fixture` with nothing inside its aggro range, stepped for the same 1,200 ticks — so the assertion cannot be satisfied by ambient damage. The first version of that control never stepped the simulation at all: it asserted a wolf was at full health at tick 0, which no implementation could fail. Review caught it, and this entry had claimed it was doing work it was not.

**A second defect was shipped inside the first fix and caught in review.** The retaliation originally fired for any unit that was not already attacking. `setUnitAttackCommandDirect` REPLACES the victim's whole command — shift-queued waypoints and build refs, trade-route refs and carried gold — and calls `clearGathererOrder`, which also zeroes gather progress and the explicit assignment. Measured: a house foundation frozen at 0/120 that otherwise reached 43/120, a move order replaced at tick 3, a garrison-escape order destroyed by the very thing it was escaping. Worst of all it made the boar LURE unplayable — the retreat order that defines that spec-mandated mechanic was overwritten by the boar's next bite — so one commit had put two spec rules in direct conflict. Retaliation now fires only for a unit with NO order, and `wolf-at-shoulder-fixture` (wolf adjacent, so the bite lands on tick one) pins it: with the wide guard the villager never reaches the `building` state at all. `wildlifeCombatSystem` now issues the retaliation itself, and only when the victim is not already attacking — re-issuing on every bite would reset the approach each tick and two wolves sharing a victim would leave it oscillating between them.

**What this predicts.** Every remaining behaviour that reaches for units through `query(..., 'unit')` is blind to wildlife in the same way, and each is worth checking on its own terms rather than assumed: fleeing, stance response, formation, and the AI's threat assessment. More generally, a comment asserting that no caller exists is an absence claim with a shelf life — the same class as the 2026-08-26 roster entry, and it expired the same way.

## 2026-08-30 — A scout that reached the edge of its patrol box stopped scouting

**Symptom.** An AI scout parked at a corner of its own wander box visited three distinct cells in 1,200 ticks and stayed there for the rest of the match. Its owner is blind from that moment: the scout is the only thing exploring. Found while a wolf chase happened to leave a scout at that corner, but the wolf is incidental — parking a scout there by hand, on a map with no wildlife at all, reproduces it exactly.

**Investigation.** The wander box is declared in GRID cells (`wanderBounds`) and enforced in FINE units (four per cell). The far edge converted as `maxY * UNIT_SUBGRID_RESOLUTION`. That is the FIRST fine slot of the last legal cell, not the last: cell 32 spans fine 128..131, and the ceiling was 128. So on that edge every outward step was flipped by the bounds reflection and then snapped back by the clamp to the single value 128 — the scout could not even move within its own last cell. Traced directly: fineY held at 65–67 and 128–129 while the velocity flipped every few ticks.

**Root cause.** An off-by-one that only exists on one side. `min * RESOLUTION` genuinely is the first fine slot of the first legal cell, so half of every bound was correct — which is why this survived from the introduction of the fine grid, and why the near corner behaves perfectly while the far one does not.

**How it is checked from now on.** `tests/simulation/scoutWanderCorner.test.ts` parks the scout at the FAR corner and requires 40 distinct cells in 1,200 ticks — 19 on the unfixed conversion, 57 on the fixed one, so the bar sits between them and the test red-checks by reverting the two ceiling expressions.

**The first version of this gate did not gate, and this entry said it did.** It asserted a flat `>= 8` across three corners and claimed a red-check. Review measured it: all three cleared 8 against the unfixed code (19, 13, 29), because the bar had been calibrated from a "three cells" figure that no gated corner actually produces. Two of the three corners are unmoved by the fix entirely — they are kept as regression floors and are now labelled as floors rather than as detection. The lesson is the general one: a bar copied from the symptom you happened to observe is not the same as a bar measured against the two builds.

**Still OPEN: the (maxX, minY) corner.** That corner still pins — two cells in 1,200 ticks, on a map with no wildlife — after this fix, so it is a second and independent defect in the same system rather than a case the fix missed. It is deliberately not in the test, with a comment saying why, so that nobody lowers the bar for the three corners that now work in order to accommodate it. Repro: `createSimulationBridge('aoe2-canary')`, take the owner-2 scout with the largest `maxX`, set its position and `unitTransform` to `(bounds.maxX, bounds.minY)`, step 1,200 ticks, count distinct cells.

**What this predicts.** Every other place a cell-space bound is enforced in fine space has the same trap, and the asymmetry is what makes it invisible: the min side is always right, so a symmetric-looking expression is half wrong. `worldOccupancy`'s spawn guard was audited for this in the same session and is correct because it compares in cell space throughout.

## 2026-08-30 — A red Windows CI on a suite that was green everywhere else

**Symptom.** `main`'s remote gate failed on the push of commit e6c73556. Ubuntu passed; Windows failed with one test timing out — `selectionActivity.unit.test.ts > villager returning with wood reports Dropping off wood`, `Test timed out in 30000ms`. Every local gate had been green, including the full suite, the browser suite and the corpus.

**Investigation.** The test takes 8.0 seconds on the author's machine against a 30-second budget. The Windows job took 34 minutes against the previous run's 24. Two candidates were measured rather than argued:

- *Did the change make the simulation slower?* An A/B on the scout wander fix — the only behaviour change that makes units do MORE work — over the two heaviest AI suites: 70.46s with it, 69.11s without. About 2%. Not the cause.
- *Did the new tests cost that much?* They total roughly 21 seconds of the suite's 967 seconds of test time, about 2%. Not on their own either.

What is left is contention. The suite runs in parallel workers on a CI runner with few cores, and a test with an 8-second cost and a 30-second budget has no headroom once anything else is competing for the machine. The new tests were enough to tip it; on a faster runner they would not have been. Windows runner variance covers the rest of the 24-to-34-minute gap.

**Root cause.** A budget calibrated on the author's machine with nothing else running. It is the same shape as the 2026-08-29 two-month CI outage entry: the local gate and the remote gate run on different machines, and every machine that runs this suite in CI is slower than the author's.

**How it is checked from now on.** The marginal test's budget is now 90 seconds with the measurement written beside it, so a slow runner has room. The new tests were re-measured and shortened wherever the signal allowed — the scout corner floors dropped from 1,200 ticks to 400, the retaliation horizon from 1,200 to 300 (the wolf is at 1/25 by tick 200) — which took the suite from 110.7s to 104.7s locally. The one test that kept its full horizon kept it for a measured reason: its signal does not exist below 1,200 ticks (17 cells against the broken build's 19 at 600).

**What this predicts.** Every remaining test whose runtime is a large fraction of its timeout is the same defect waiting for a busy runner. `gatherDomain` at 107s, `aiPlayer` at 44s and `aiFeudalStone` at 37s are the ones to look at first, and the general rule is that a per-test budget should be a multiple of the measured cost rather than a round number that happened to pass once.

## 2026-08-30 — The AI cannot leave the Feudal Age: wood-starved while banking everything else (RESOLVED v0.3.169)

**Symptom.** In a 40-minute all-AI match neither player ever reaches the Castle Age, on any of three seeds. The whole match exercises four unit types (villager, scout, militia, spearman) and eight or nine building types. Against a content audit that puts 137 of 138 technologies live with real effects and 93 units trainable, AI self-play exercises a small fraction of the game — which matters because self-play is the standing acceptance test for feature completeness.

**Investigation.** Measured on `default-seed`, the winning AI at tick 24,000:

    food=1571  gold=1140  stone=750  wood=38   feudal buildings: NONE

It is sitting on nearly twice the food and five times the gold the Castle advance costs (800 food, 200 gold) and cannot take it, because the advance also needs two qualifying Feudal buildings and those cost 150–175 WOOD each. Wood never rises above about 50 at any sample on any seed, on either side, for the whole match.

Two hypotheses were tested and BOTH failed, which is the useful part:

- *More wood villagers.* Feudal weights `{food:7, wood:4, gold:1, stone:1}` → `{food:7, wood:6, …}`, which moves the split from ~7 wood villagers to ~9. Castle still never reached on any of the three seeds; building and unit variety unchanged.
- *The farm gate.* `pickNextBuildTarget` returns `farm` on essentially every decision tick, so the theory was that farms block the Feudal buildings. Holding farms to the floor while the age-up prerequisites are missing made it slightly WORSE on `aoe2-prototype` (eight building types down to seven) and changed nothing elsewhere — because a farm is CONSUMED, so the count falls back below even a floor of two and the branch re-fires anyway.

So wood income is not the constraint and the farm ordering is not the constraint: wood SPEND is. Every point of wood goes to houses for a growing population and to replacing farms as they are eaten, and the AI never accumulates the 150 for its first Blacksmith.

**Why the reserve makes this self-sustaining.** `ageUpReserveCost` returns an empty reserve unless the owner ALREADY qualifies for the next age. Not qualifying, the AI has nothing protecting a stockpile it also has nothing to spend on — so food and gold pile up, unspendable, while the one resource that would unblock it stays at zero. The state is stable, which is why 40 minutes changes nothing.

**A fourth hypothesis, also tested and reverted: reserve the wood.** `ageUpReserveCost` protects the age-up RESEARCH but only once the owner qualifies, so nothing protects the cost of BECOMING qualified — the AI defends a purchase it cannot yet make and spends the money for the one it can. Making the farm wait unless the wood also covered the next qualifying building was implemented, unit-tested with six cases (including a red-check), and wired through the caller. Measured over the three seeds: `aoe2-prototype` gained a Blacksmith (8 building types to 9), `default-seed` LOST one, and `corpus-seed-b` lost its farms entirely. Reserving for all still-needed prerequisites rather than the next one moved `default-seed` to 10 types but cost `aoe2-prototype` and `corpus-seed-b` their farms. No seed reached Castle under either variant. Starving the food economy to fund a hall is not a trade worth making, so both were reverted.

**An instrument flaw caught before it became a conclusion.** The exercise metric counts DISTINCT building types across the whole match, which unions BOTH owners. A run showing a Blacksmith and an Archery Range therefore does not show that any single owner had two qualifying buildings, and the tempting reading — "it qualifies and still will not advance, so the blocker is elsewhere" — is not supported by it. Anything measuring qualification has to be per-owner. The union metric is still correct for what it claims (what the match exercises) and, if anything, understates the problem, since per owner the variety is lower still.

**ROOT CAUSE, found by instrumenting the decision rather than guessing at it.** Tracing the AI's building phase directly on `aoe2-prototype`, owner 2, over 20,000 ticks:

    next=farm  ongoing=0  max=21  wonder=false  vills=22  builder=2162     x164
    WHY farm   builder=2162  anchor=SOME  afford=FALSE  wood=0..24  cost={wood:60}

The AI decides to build, has a builder, has capacity and a valid anchor, and simply cannot pay. Tallying every spend it did manage over those ticks:

    buildings   4 house(25) + 4 farm(60) + mining-camp(100) + mill(100)
                + lumber-camp(100) + barracks(175)            =  715 wood
    military    21 spearmen at 25 wood each                   =  525 wood
                                                                -----------
    total                                                       1,240 wood

1,240 wood in 2,000 seconds is 0.62/s against a nominal 2.73/s for seven wood gatherers — **23% efficiency**, the rest of their time spent walking under the §12.4.2 clock. And **military takes 42% of what does arrive**.

That is the whole defect, and it was designed in by a comment that stopped being true: `aiSystemProductionPhase` reads *"Military is the only pre-age-up food/gold spend (builds spend wood/stone)"*, and the age-up reserve is built on it. A **spearman costs 25 wood**. So military and construction compete for the one scarce resource, nothing arbitrates, and the 150 wood for a first Blacksmith is never accumulated — the AI cannot qualify, so `ageUpReserveCost` stays empty, so it also banks food and gold it has no way to spend. 980 to 1,922 food at the end of a match, and a stockpile that never clears 24 wood.

**The intervention that works, measured.** Adding the next qualifying building's wood to the production-phase reserve — the exact pattern the file already uses for the age-up — produced `default-seed` owner 2 **qualifying at tick 20,000 and reaching the CASTLE AGE at 21,500**, the only time any player has reached it in this investigation, with unit variety up from four types to five. It is NOT adopted: it costs a unit type on `aoe2-prototype` and `corpus-seed-b` (four down to three), which is a regression on the boot map and fails this repo's own adoption rule. Gating it behind an established economy (15+ villagers) restores the military everywhere and loses the Castle everywhere — the reserve has to bite early to work at all. What remains is a tuning problem with a known mechanism, rather than an unknown.

**A seventh experiment, testing the obvious pairing, and it is worse.** If the reserve costs military because the spearman is the AI's only Feudal unit and it costs wood, then give it a wood-free alternative: militia, at 60 food and 20 gold, both of which this AI has banked in the hundreds. Measured: peak military HALVED (8 to 4 on two seeds) and the Castle was lost. Militia's 60 food buys fewer units than the spearman's 35, so trading the wood cost for a food cost bought a smaller army and spent the food the age-up needs. The pairing that looks obvious from the cost table is wrong once the food side is measured.

**RESOLVED, and the two halves only work as a pair.** The sweep the previous entry called for — reserve SIZE against the Feudal wood weight, across three seeds — found that neither lever works alone and together they clear it. Wood 4/5/6/7/8 against reserve sizes 0/60/75/150/175 and two stop conditions:

    reserve 150, wood 4   boot map: no castle          default-seed: castle 21,500, peak military halved
    reserve 0,   wood 6   boot map: no castle          (income alone: the spearman ate each 25 as it landed)
    reserve 150, wood 6   BOOT MAP: eligible at 23,500   default-seed: castle 24,000
    military floor 3      bldg types 8 -> 9              bldg types 7 -> 12
                          unit types 4 -> 5              peak military unchanged
                          peak military unchanged (8)    totals: 1 castle where baseline
                                                         had 0, 50 bldg types vs 44,
                                                         26 unit types vs 24, and peak
                                                         military 37 vs 37 — no army cost

Both halves red-check independently against the end-to-end test: reverting either one alone returns "no player reached the Castle Age". 150 and 175 are identical because the reserve clamps to the next building's cost and the Blacksmith is 150; wood 6 gives the earliest boot-map Castle of the family.

**The reserve needed a floor, and review found that the hard way.** The first version had none. Across eleven seeds it cost 21% of all peak military, and on every seed whose peak wood never reached the reserve it blocked 100% of military training for the whole age while still never affording the building — pure loss, with the army pinned at 3 units against a baseline of 6-7. The obvious floor, `attackGroupSize` (5), makes the reserve a no-op: `militaryGrowthPausedForAgeUp` already stops GROWTH at 5, so the drain this reserve exists to stop happens entirely below it, in replacing losses. Swept 0/2/3/4/5 across three seeds; **3** costs nothing at all — peak military identical to baseline's 37, six more building types, two more unit types.

**The cost, stated rather than buried.** `corpus-seed-b` still regresses, though far less than the floorless version: each owner loses one building type and one unit of peak military, and neither reaches Castle. The boot map does NOT regress on anything, which is what the older no-regression rule asks for. Recorded so it can be reversed on evidence rather than re-litigated from memory.

**How it is checked from now on.** `tests/simulation/aiReachesCastleAge.test.ts` plays 24,000 ticks of all-AI self-play on the boot map and requires Castle-Age ELIGIBILITY (baseline never qualified there at all), peak military of at least 8, five unit types and nine building types. It red-checks against each half of the pair separately. A second seed is deliberately not gated — each match is ~2 minutes and heavy tests in this suite tipped an unrelated test into a CI timeout the same day — so `default-seed`'s numbers are recorded in the test header as evidence instead.

**Where this leaves the tuning.** Reserve-alone remains the only configuration that has produced a Castle Age. Its scorecard against baseline, per owner, over three seeds: `default-seed` clearly better (owner 2 qualifies at 20,000, reaches Castle at 21,500, building types 7 to 9, unit types 4 to 5); `aoe2-prototype` mixed (owner 1 gains two building types and loses a unit type, owner 2 loses one building type); `corpus-seed-b` slightly worse (a unit type and a building type). One large win, two small losses, and a loss on the boot map is exactly what this repo's adoption rule refuses. Not adopted, and the remaining work is a proper sweep of the reserve's size and duration rather than another single-shot experiment.

**Also measured: the match never resolves (OPEN, and a fix was attempted and REVERTED).** At 60,000 ticks — 100 minutes of game time — every number was byte-identical to 24,000. `default-seed` owner 1 ended with zero units, five buildings including a Town Center, and 43 food against a villager's 50, so it could never act again; `getMatchState().outcome` was still `running`.

Conquest is not the gap — its rule is AoE2's own, every unit AND building destroyed, and a Town Center was standing. The missing piece is resignation.

**The attempt, and why it was reverted.** A resignation arm was built: an owner with no units that cannot afford the cheapest unit any completed production building it owns can train is treated as eliminated, behind a grace period. It passed a forcing fixture, its control, and all six gates. Review then broke it three ways:

- *It declares a living player dead, reproduced.* `enqueueTraining` DEBITS the stockpile when a unit is queued and creates the entity only when the counter reaches zero, 250 ticks later for a villager. So a player who has just correctly spent its food on eight villagers has zero food and zero units, and the rule resigns it — with eight paid-for villagers in the queue. The reviewer produced a match ending "All enemy forces have been eliminated" against a standing Town Center. The same shape hits a human as a FALSE DEFEAT one tick before a villager pops, and the pop-blocked branch makes the window unbounded.
- *Three unit-free income routes make "can never act again" false.* A Market converts wood to gold to food with no unit anywhere; relic gold pays a per-tick trickle into a Monastery; and ally tribute needs nothing of the recipient but a resource bank. Each is a live player judged dead.
- *The grace period was chosen on a false premise — mine.* I wrote that the longest fixture horizon was 8,000 ticks and set the grace to 12,000. It is not: `aiReachesCastleAge` runs 24,000 (a test I wrote the same day, whose own comment says "half a real 24,000-tick match"), `aiFeudalStone` and `gatherDomain` 14,000, a farm test 21,600, the CI corpus 13,100, and `npm run playtest` defaults to 30,000. Because `step()` no-ops once an outcome is set, a future break is SILENT: the world freezes and assertions read frozen state instead of failing. Two dev scripts — including `ai:tickrate`, the tool used for this session's tick-cost work — loop on `world.tick` with no outcome check and hang forever, which the reviewer reproduced.

**Why it is not simply repaired.** Each hole is individually fixable, but the shape is the problem: proving a position TERMINAL means enumerating every route by which resources can still arrive, and that enumeration silently rots as the game gains features. The rule was correct on the day it was written and would have been wrong the next time someone added a trickle.

**And the symptom no longer arises.** Re-measured after v0.3.169, at 30,000 ticks: `default-seed` owner 1 holds 24 units and 17 buildings where it used to end with zero, and `aoe2-prototype` owner 1 holds 26 and 11. The economy fix stopped the loser being wiped out at all, so this was a downstream symptom of the economy collapse rather than an independent defect. That is the reason it stays open rather than being pursued: the cost of a false elimination is a stolen game, and the benefit is currently theoretical.

**`corpus-seed-b`, the seed where neither owner qualifies, measured (2026-08-30).** Owner 1 finishes with **food 1,895, gold 790, stone 440 and wood 20** — the same shape as the original defect, one seed further on. Cumulative income over 20,000 ticks: food 3,189, wood **1,300**, gold 760, stone 380. So 1,300 wood arrives across the match and every point of it is spent; the balance never approaches the 150 a Blacksmith needs. Of seven villagers assigned to wood, only **two are gathering at any moment** — three walking out, two walking back. That is ~25% of nominal gather rate, and it is the whole constraint.

**Two build-order hypotheses tested and both negative.** The AI builds **exactly one lumber camp and one mining camp for the entire game**: `missing(type)` means "owns none", and the camp branch sits inside the Dark-Age arm of `pickNextBuildTarget`, so past that age it is never reached at all. In AoE2 a player builds a camp per woodline as the near trees deplete, so this is a real divergence — and closing it does not help. Allowing a second camp changed nothing (the AI cannot afford the 100 wood); allowing camps past the Dark Age as well moved one seed by a single building type and no seed reached the Castle Age. Both reverted.

**Where the 25% actually goes, measured (459 samples over 20,000 ticks on `corpus-seed-b`).** The two halves of a gather trip, in tiles:

    villager -> target    median 6    p90 20    max 61
    target   -> dropoff   median 6    p90 9     max 50

The HAUL is healthy. Carry capacity is 10 and wood gathers at 0.385/s, so a full load is ~26 seconds of chopping against a median 6-tile haul — about 7.5 seconds each way at the §12.4.2 clock of 0.8 tiles/s. That alone predicts roughly 68% efficiency, not 25%.

What destroys it is the APPROACH, and specifically its tail: at the 90th percentile a villager walks **20 tiles to reach its tree** — 25 seconds, as long as chopping the load it came for — and the worst case is 61 tiles, 76 seconds. The median approach is fine; one trip in ten costs more in walking than it returns in wood.

**And this is why the one-camp limitation matters.** A villager is assigned the nearest AVAILABLE tree, so once the near woodline is worked out the nearest one is far — and the AI cannot answer the way a DE player does, by planting a camp at the new woodline, because it owns one lumber camp for the whole game and cannot afford a second. The two findings are one finding: the approach tail is the cost, and the camp is the instrument for cutting it. The failed camp experiments above did not disprove the fix; they showed it is gated behind affording 100 wood, which is the same wall.

**The tenth attempt LOOKED like the answer and was an artifact of its own bugs (2026-08-30).** Diagnosis said: cut the approach tail by planting a camp at the receding woodline. Allowing a second Lumber Camp, with `dropOffAnchorFor` skipping resources an existing camp already serves, took three seeds from 1 Castle-Age owner-slot to 3, with building types 50 to 55 and unit types 26 to 31. It reproduced exactly, passed all six gates, and did not regress the boot map.

Review found two defects in it, both real:

- *A null anchor fell back to the Town Center.* Null used to mean only "nothing in range"; with the filter it also means "everything is already served". The caller planted the camp on the Town Center — where `canDropOffAt` already accepts wood, so it shortens NO carry. 100 wood of the scarcest resource for nothing.
- *The second Lumber Camp displaced the Mining Camp.* Both cost 100 wood and lumber is asked first inside the Dark-Age arm, which is the only place either is reachable. Over 12 seeds, mining camps fell 17 to 15 and owner-slots ending with no gold/stone drop-off at all rose 7 to 9 — permanently, since a slot that misses it in the Dark Age never gets one.

**Fixing both destroyed the benefit, which is the finding.** With the null case refusing to build and the opening order putting the FIRST of each drop-off ahead of any second, a ten-seed sweep over 20 owner-slots says:

    camps   Castle   building types   unit types   peak army
    1           4         156              89          110
    2           4         160              85           99
    3           2         159              83           98

No Castle-Age benefit at all, and it costs unit variety and 10% of peak army. So the three-seed win was not the mechanism working; it was the two defects. Reverted in full.

**And the accident points somewhere real.** Displacing the Mining Camp helped because it spends 100 wood on wood rather than on gold and stone — and this AI banks gold and stone unspent all game (measured repeatedly: 790 gold and 440 stone against 20 wood at the end of a match). The next hypothesis is therefore not another camp: it is that the Mining Camp is itself a poor buy on these maps, and the AI should not build one while the resources it serves are accumulating unused. That is testable, cheap, and was found only because fixing a bug made a result worse.

**The twelfth attempt was the never-build variant in disguise, and its win was a horizon artifact (2026-08-30).** The eleventh accident said the Mining Camp is a poor buy while gold and stone bank unspent, so the twelfth moved it out of the Dark Age into the Feudal arm, placed AFTER the age-up qualifying halls. Ten seeds gave Castle-Age slots 4 to 6 and the boot map reached the age at tick 22,250 where HEAD reached it never. Six gates green, reproducible.

Review found three things, each fatal alone.

- The insertion sits after a loop demanding blacksmith AND archery-range AND stable AND market, and the AI qualifies with two and rarely completes four. Camps built: 25 of 34 owner-slots at baseline, 4 of 34 under the change, all four in the last 17% of the match. A separately-built never-build arm is byte-identical on 10 of 12 slots. The rule that shipped is the one its own comment rejected.
- The win is an artifact of the 24,000-tick horizon and REVERSES at 34,000: Castle slots 7 to 9 at the cut-off, 18 to 16 at the longer one, with five slots losing an age they otherwise reach.
- The insertion is not age-gated, so in the Castle Age a missing camp outranks Siege Workshop, Monastery and Castle, and an unplaceable one blocks the whole order. That stall class did not exist at HEAD.

Two of my own claims were wrong. The 790-gold-440-stone figure quoted in two tracked files is ONE cherry-picked slot; across 34 the gold range is 11 to 1,670, and the change RAISES the median hoard it was written to stop paying for (gold 575 to 614). And my prose and my test file stated building types moving in opposite directions.

Review names the position nobody tried: keep the camp in the Dark Age and gate it on a gold or stone mine actually being within DROP_OFF_ANCHOR_RADIUS.

**The wall is HALF a measurement artifact (2026-08-30, and this supersedes the framing above).** Every figure in this entry was taken at a 24,000-tick horizon. Measured at 34,000 on the same ten seeds, with no code change at all:

    horizon    owner-slots reaching Castle Age    building types   unit types   peak army
    24,000              4 of 20                        156             89          110
    34,000             10 of 20                        176            115          168

Half of all owner-slots reach the Castle Age. They arrive after minute 40, which is slow against DE, but "the AI cannot leave the Feudal Age" was never true as stated — it was true of the window being looked through. The honest claim is that the AI reaches the Castle Age late, and that self-play run to forty minutes cannot see it.

**The thirteenth attempt was a no-op, and it falsified the hypothesis that suggested it (2026-08-30).** Review of the twelfth proposed the position nobody had tried: keep the Mining Camp in the Dark Age but only want one when a gold or stone mine is actually within `DROP_OFF_ANCHOR_RADIUS`, so the camp is never bought when it shortens nothing. Implemented, and measured in both arms at both horizons in a single job — after nearly corrupting the comparison by stashing the working tree while the first arm was still reading it.

    arm        24,000                          34,000
    gated      4 / 156 / 89 / 110              10 / 176 / 115 / 168
    baseline   4 / 156 / 89 / 110              10 / 176 / 115 / 168

Byte-identical. The gate never fires: on all ten seeds a mine is always within range when the camp is wanted, so the camp is never planted away from ore in the first place. The Town-Center-fallback hazard that motivated the idea is LATENT on these maps, not live — which is worth knowing, because it was carried forward from the second-lumber-camp revert as though it were an active cost.

**WHERE THE TIME ACTUALLY GOES, measured at the corrected horizon (2026-08-30).** With the wall reframed, the arrival times can finally be decomposed. Ten seeds, 34,000 ticks, for the ten owner-slots that reach the Castle Age:

    Feudal -> qualified (two buildings)      mean 10,225 ticks   17.0 min   median 10,500
    qualified -> Castle (800 food, 200 gold) mean  3,800 ticks    6.3 min   median  2,250

**73% of the post-Feudal delay is spent QUALIFYING, not accumulating.** Banking the age-up's own price takes about six minutes; putting up the two buildings that make the AI eligible takes seventeen. Of the other ten slots, two qualified and could not afford the advance inside the horizon, and eight never qualified at all.

The two cheapest qualifying buildings are a Blacksmith at 150 wood and any of Archery Range / Stable / Market at 175 — 325 wood. Seventeen minutes for 325 wood is a NET rate near 0.32 wood per second, against a nominal 2.73 for the seven villagers assigned to it. That is the same wood starvation the first instrument found, now attached to the outcome it causes and priced.

So the target is wood INCOME in the Feudal Age, and specifically the gathering efficiency rather than the allocation: the weights were already moved from 4 to 6 in v0.3.169, and the approach walk is where the rate goes. It is NOT the build order, which is what twelve of the thirteen attempts changed.

**The Feudal economy is not MISALLOCATED, it is short — which retires a whole class of attempts (2026-08-30).** Measuring gross wood separately from spend during the Feudal Age, over three seeds:

    gross income   ~0.77 wood/s   (~1,100 wood across the age)
    spend          ~0.90 wood/s   (drawing down the opening stock)

The two qualifying buildings cost 325 of that 1,100, yet take seventeen minutes to appear — so they are losing the competition for wood. The obvious inference is to protect them, and v0.3.169 already does that against MILITARY. Extending the same reserve to FARMS was measured at both horizons:

    arm             24,000                        34,000
    farm reserve    0 Castle / 140 / 79 / 111     5 Castle / 163 / 98 / 146
    baseline        4 Castle / 156 / 89 / 110    10 Castle / 176 / 115 / 168

Catastrophically worse, and both horizons agree on the direction. The reason is the correction: starving farms starves FOOD, and the Castle advance costs 800 food against the 325 wood the reserve was protecting. The qualifying buildings lose the competition to something that is ALSO required for the same goal, so the premise — that this is a misallocation to be corrected — is false. The allocation is roughly right and the economy is simply short.

**What this retires.** Every attempt on this wall that moved wood from one claimant to another is now ruled out as a class: military-to-buildings was the only one that helped, farms-to-buildings actively harms, and the villager weights were already swept. What is left is raising the wood a villager-second produces — the approach walk — which is the one thing measured repeatedly here and never attacked.

**The approach walk is not pathological, which closes the last cheap hypothesis (2026-08-30).** With reallocation retired as a class, the remaining lever was the wood a villager-second produces. Two measurements on `corpus-seed-b`, owner 2, ticks 12,000-20,000:

- *Task shares for wood villagers:* to-resource 42.3%, to-dropoff 37.7%, **gathering 20.0%**. Four fifths of a wood villager's life is walking.
- *Effective movement while walking:* **0.259 fine units per tick against a nominal 0.320 — 81% of the movement clock.** Congestion and re-pathing cost about a fifth, and no more.

So the walking is real but the walk itself is close to as fast as the clock allows. There is no 5x defect hiding in movement; the shape is simply that this map's trees are far enough from a drop-off that a round trip costs four times the gathering it enables. That is a map-and-clock property, not a bug, and it is what §12.4.2's honest movement speeds bought — the same trade that made the AI look broken in the first place.

**One instrument error, and it is the recorded one again.** The first speed probe reported zero walking ticks. `EconomyState.villagers[]` carries owner, task and resource but NO `id`, so `getComponent(v.id, ...)` was asking about `undefined` for every villager — a query structurally incapable of finding anything, reported as a finding. The fix was to read `units[]`, which does carry ids, and cross-reference the `gatherer` component. Printing the shape of the object being filtered is what caught it, which is exactly what the lesson prescribes.

**THE AI BUILDS A LUMBER CAMP AND THEN DOES NOT USE IT (2026-08-30, OPEN — and this is the wall's structural cause).** Measured at tick 16,000 across five seeds, ten owner-slots, counting which drop-off is nearest to each wood villager's actual target:

    seed/owner        camps  woodVils  servedByCamp  byTownCentre  medianHaul
    aoe2-prototype 1    1       5            0            5            8
    aoe2-prototype 2    1      12           12            0            7
    default-seed   1    1       1            1            0            4
    default-seed   2    1      10            0           10           10
    corpus-seed-b  1    1       7            0            7           11
    corpus-seed-b  2    1       4            0            4           10
    seed-1         1    1       7            0            7            9
    seed-2         1    1       7            0            7            8
    seed-2         2    1       8            8            0            7

**Six of the nine slots that own a Lumber Camp have ZERO villagers served by it.** On `corpus-seed-b` owner 2 the camp sits at (57,25) with **6 live trees within 3 tiles and 17 within 6**, and all four wood villagers are working trees a median of 10 tiles from the Town Center instead. Where the camp IS used the haul falls to 4-7.

**Why.** `assignNearestResource` ranks candidates by proximity to a reference drop-off, and that reference is *the drop-off nearest the VILLAGER* — its own comment calls this "an accepted one-lookup approximation". A villager standing near the Town Center therefore ranks trees by Town-Centre proximity and picks one there, so it never migrates to the camp; the camp only ever serves villagers that happen to already be beside it. The deposit building is re-resolved correctly at haul time, which is why this never showed up as a wrong-destination bug — the villagers walk to the right drop-off, they were just sent to the wrong TREE.

**Why it matters more than everything above it.** This is the structural cause the fifteen tuning attempts were working around. It explains the 20% gathering share, the 0.77 wood/s gross, the seventeen minutes to afford 325 wood of qualifying buildings — and it explains why a SECOND Lumber Camp changed nothing (attempt ten): a second camp cannot help when the first serves nobody.

**The shape of the fix.** Rank candidates by the round trip they actually imply — distance from the candidate to the candidate's OWN nearest drop-off — rather than to whichever drop-off the villager is standing near. Trees beside the camp then rank best, because their nearest drop-off is one to three tiles away.

**The camp fix improves the aggregate and regresses the boot map, so it does not ship (2026-08-30).** Ranking gathering candidates by the haul each one actually implies — its distance to the drop-off nearest IT, rather than to whichever drop-off the villager happens to stand near — was measured in both arms at both horizons:

    arm         24,000                          34,000
    haul-rank   6 Castle / 162 / 88 / 105       11 Castle / 182 / 107 / 182
    baseline    4 Castle / 156 / 89 / 110       10 Castle / 176 / 115 / 168

The first candidate in sixteen attempts whose primary metric improves at BOTH horizons. But per seed, `aoe2-prototype` owner 2 takes the Castle Age at 28,250 against a baseline 25,250 — three thousand ticks LATER — with unit types 8 down to 5. The aggregate gain is real and the boot map pays for it, which is the exact shape of the Dark-Age tuning this repo already adopted and withdrew. Reverted.

**And the mechanism is still not understood, which matters more than the result.** Camp usage barely moved: 21 of 67 wood villagers served by a Lumber Camp before, 20 of 73 after. Two separate theories of the gate — the ranking comparator, then the home-range filter — were each implemented and each changed nothing, so whatever routes villagers away from a camp that sits on live trees is still unidentified. The Castle-Age gain therefore came from somewhere other than the mechanism it was aimed at, which is reason enough not to ship it even without the boot-map regression.

**THE ROUTING GATE, identified by tracing the decision rather than theorising about it (2026-08-30).** Instrumenting `assignNearestResource` on `corpus-seed-b` owner 2 at tick 16,000:

    TRACE vil=2481 at=50,23 refDropOff=2209@48,24 candidatesAfterHomeRange=101 of 232
          nearest4= 2398(53,23)d6  2230(47,30)d7  2384(51,20)d7  2388(52,21)d7
    CAMP  2211@57,25          TC 2209@48,24

The villagers stand at (50,23). `findNearestDropOffBuilding` is asked from the VILLAGER's position, so it returns the Town Center at (48,24) — 3 tiles — and not the Lumber Camp at (57,25), 9 tiles. Every candidate is then ranked by distance to that Town Center, and the best available is 6 tiles away, while the camp sits on trees within 3 of itself.

So the gate is: **the reference drop-off is chosen by proximity to the VILLAGER, and villagers cluster around the Town Center, so the Town Center is permanently the reference and no tree near a camp is ever preferred.** A camp can only ever serve a villager that already happens to stand beside it — which is why building a second one changed nothing.

**And a flaw in my own measurement of it.** The "servedByCamp" probe attributed each target to whichever drop-off was nearest, breaking ties by array order. A tree at (53,23) is 6 from the Town Center and 6 from the camp — a tie — so the probe's verdict on such trees was arbitrary, and "camp usage barely moved" is not trustworthy as stated. The boot-map regression that actually caused the revert is an outcome metric and is unaffected by this.

**Why the obvious gate was NOT adopted, measured rather than assumed.** `@typescript-eslint/no-use-before-define` with `functions: false` was configured and run: **24 violations repo-wide, 11 in `src/game/simulation`** — and every one inspected is the SAFE idiom, a function body referencing a `const` declared lower in the file, which runs after module initialisation. `PERSIAN_WORK_RATE_BY_AGE`, `EMBEDDED_TABLE`, `findNearestOwnedMonasteryToDeposit`, the UI controllers' `api` and `rafHandle` — all safe by construction.

The rule cannot separate those from the case that actually crashed, where the helper was called in code executing immediately in the same synchronous flow. So adopting it means 24 refactors of correct code for a signal that is mostly noise, and it would NOT have specifically gated the hazard. Recorded as a rejected gate with its cost, so a future session does not re-derive the same answer: the config is one line, the price is 24 sites, and the discrimination is poor.

**A latent crash class found en route, and it is a gate gap.** Moving the haul-cost helper introduced a use-before-define: `haulCostOf` is a `const` arrow, and the filter that now called it ran above its own definition, so every gather assignment threw a TDZ `ReferenceError` and the AI stopped gathering entirely. `npm run typecheck` and `npm run lint` were both GREEN through it — `no-use-before-define` is not configured in `eslint.config.js` — and the only reason it surfaced is that a probe reported zero gathering villagers. A reviewer had flagged exactly this hazard earlier the same day on a different file.

**Weighting the round trip does not recover the boot map, it just loses the gain (2026-08-31).** With the routing gate identified, the direct fix is to rank candidates by the haul each implies rather than by distance to whichever drop-off the villager stands near. Pure haul does that and improves the aggregate at both horizons, but costs `aoe2-prototype` three thousand ticks to the Castle Age — it sends villagers on migration walks without weighing the one-time approach. The obvious refinement is to price both legs, `approach + w * haul`, since the approach is paid once and the haul on every trip. Swept at both horizons over ten seeds:

    arm                24,000                        34,000
    baseline           4 Castle / 156 / 89 / 110     10 Castle / 176 / 115 / 168
    w = 1              4 Castle / 162 / 86 /  96     10 Castle / 182 / 110 / 158
    w = 2              3 Castle / 162 / 83 /  93      9 Castle / 179 / 101 / 123
    pure haul          6 Castle / 162 / 88 / 105     11 Castle / 182 / 107 / 182

There is no interior optimum. Weight 1 returns Castle slots exactly to baseline and weight 2 falls below it, while peak army drops in every weighted arm — so the approach term does not buy back the boot map, it only cancels the benefit. Only the unweighted extreme moves the primary metric, and that is the arm with the boot-map regression.

**What that says about the fix.** The gate is real and correctly identified, but ranking is the wrong lever for it: any ranking that makes camp-adjacent trees attractive enough to use also makes distant trees attractive enough to walk to, and the two effects are not separable by a scalar weight. A fix has to distinguish the villager MIGRATING to a camp's neighbourhood from the villager choosing a node within its current one — which is a home-range or assignment-time question, not a comparator question. Two of the seventeen attempts already guessed at a home-range filter and changed nothing, so that needs the same tracing treatment that found the gate rather than a third guess.

That is eight measured negatives on this wall now. Every lever tried moves wood between things that all need it; none creates any. The remaining candidates are on the GATHER side rather than the spend side — walk distance, camp siting, and how many gatherers share one drop-off — and the measurement to beat is the 25% figure above, not the age timings.

**What a future attempt needs.** Not a resource test at all — a POSITIVE signal that the player is doing something, rather than a proof that it can do nothing. The queue, the market, the trickle and tribute are all things this rule had to know about only because it was arguing from absence.

**Superseded hypothesis (kept for the record).** The villager allocation is a fixed per-age split that does not respond to which resource is actually binding. A player who has banked 1571 food and 1140 gold and cannot build a 150-wood building moves people onto wood; this AI cannot, because its split is a constant.

**How it is checked from now on.** Not yet, and that is the honest state — this is registered rather than fixed. What a fix needs is stated here so the next attempt does not repeat these two: the acceptance metric is what a match EXERCISES (ages reached, distinct building and unit types), measured across at least the three seeds above, and not Feudal timing, which both failed hypotheses left untouched while changing nothing that matters. The repo has already withdrawn one economy tuning that won on one seed and lost on the boot map; rank on the worst owner across seeds.

**What this predicts.** Any other fixed proportional allocation in the AI has the same failure mode available to it — the split is right for an assumed spend pattern and silently wrong when the actual bottleneck moves. The Castle and Imperial splits are untested against real play for exactly the reason this entry exists: no match has ever got there.

## 2026-08-30 — `fog-memory-fixture`'s first assertion fails in isolation, passes in the full suite (RESOLVED)

**Symptom.** `tests/browser/game-simulation-and-exploration-resources.spec.ts > remembers an enemy house with reduced-opacity memory rendering after the scout walks away` fails on its FIRST assertion: three ticks after boot the enemy house already reports `isMemory === true`, where the test expects `false` (it is meant to start in live vision and only become a memory after the scout leaves).

**Investigation, and what it rules out.** It is not the change that was in flight when it surfaced. With all four AI source files reverted to HEAD and `dist/` rebuilt, it still fails — three runs out of three. It is not the stale `vite preview` server that had been squatting on port 4173 since early in the session (`reuseExistingServer: true` means every run had been reusing it): killed, re-run, still fails. It is not the wildlife system registration ORDER, which the v0.3.167 extraction changed by inserting the deer-flee system second — moving it back to last and rebuilding does not help.

**ROOT CAUSE.** The fixture's scout carries no wander bounds, but it does not stand still: it auto-engages the enemy house four tiles away and steps off (10,10), taking that house out of its own radius-4 vision. The test warmed up three ticks and then asserted the house was in LIVE vision — a moment it had already left.

Measured in the browser: `isMemory` is false at boot and already true after one extra tick, with the scout at (10,9). Measured in the node simulation: false at ticks 0-3, true at 4-6, false again at 12 as the scout drifts back. The two disagree because boot advances further in the browser, which is exactly why a fixed warm-up count could not be right in both — and why the same assertion passed inside a warm suite and failed in a cold single-spec run.

**Fix, after the first attempt failed in the other direction.** Removing the warm-up made it pass in isolation and fail in the SUITE — the exact reverse of the original symptom, which is the proof that no fixed tick count can be right: boot advances a variable number of ticks under load. So the premise moved out of the browser entirely. `tests/simulation/fogMemoryFixturePremise.test.ts` pins "the house starts in live vision" where the tick count IS deterministic, and the browser spec keeps only what a browser can answer — that a house which has left vision renders as a reduced-opacity memory. That file also pins the scout's departure, so if it ever stops wandering off the browser spec can pin the premise directly again.

**What it was NOT, each ruled out by measurement rather than argument.** Not the AI change in flight when it surfaced — reverting all four source files to HEAD and rebuilding still failed 3/3. Not the stale `vite preview` server that had squatted on port 4173 since early in the session, which `reuseExistingServer: true` meant every run had been reusing — killed, re-run, still failed. Not the v0.3.167 wildlife registration order, nor the v0.3.167 scout wander-ceiling fix: reverting each and rebuilding leaves the scout stepping off at exactly the same tick.

**How it is checked from now on.** The test itself, with the warm-up removed and the measurement written beside it so the next person does not re-add one.

**What this predicts.** Any assertion pinned to a small tick count after boot is measuring warm-up rather than behaviour, and the browser and the node simulation do not agree on how far boot has already advanced. Every remaining `advanceTicks(n)`-then-assert in the browser suite is the same defect waiting for a different scheduler; the robust shape is to poll for the state, which this very test already does further down for the scout's arrival.

**The prediction came true the same day, and named the mechanism.** `game-combat-and-meta-combat > can right-click just beyond the rendered body of a moving enemy unit` failed twice inside the full suite and passed 4/4 in isolation. It aims at a MOVING target, and the simulation was not paused — so the view's own frame loop advances the world in real time, and extra ticks pass between reading the scout's position and clicking beside it, however many the machine's load allows. The test's 4-attempt retry loop was compensating for a wall-clock dependency rather than removing it. `setPaused(true)` makes `bridge.step` a no-op so only `advanceTicks` moves the world, and the scout is then exactly where the snapshot said.

So the sharper form of the rule is: a browser assertion is load-dependent whenever the thing it measures can move without the test asking. Measured population: **only 4 of 34 browser specs pause**. That is not thirty defects — most assert nothing that moves — but it is where the next one comes from, and it is now a counted population rather than a surprise.

## 2026-08-31 — A TDZ crash that both gates passed clean (RESOLVED)

**Symptom.** During the sixteenth attempt at the AI economy, moving a haul-cost helper put a `const` arrow below the filter that called it. Every gather assignment threw a `ReferenceError` in the temporal dead zone, and the AI stopped gathering entirely. `npm run typecheck` and `npm run lint` were BOTH green through it. The only reason it surfaced is that a probe reported zero gathering villagers; a reviewer had flagged the identical hazard on a different file hours earlier the same day.

**Root cause.** `@typescript-eslint/no-use-before-define` was not configured in `eslint.config.js`. TypeScript does not reject a use-before-define on a `const` — it is legal to write and fails only at runtime — so nothing in the gate set could see it.

**Fix, and what it found.** The rule is on, with `functions: false` so the hoisting the codebase deliberately relies on for mutually-recursive helpers still works. Enabling it immediately reported **24 existing violations across 13 files**, so the class is live rather than theoretical. Seven were genuinely restructured — module constants hoisted above their uses in `teamProductionBonuses`, `civTechTree`, `voxelSelectionController`, `worldOccupancy`, and the two closure `let`s in `createHudController` plus the formation planner in `humanInputOps`. The remaining seventeen are marked at the site with an explicit disable naming this entry: all are deferred-execution uses that cannot fire today, and none has been individually audited, which is precisely why they are marked rather than quietly excluded.

Three of the touched files crossed the 500-line cap once the markers went in, and that is worth recording as a design consequence: a per-site suppression costs a line, so a file at the budget cannot absorb one. Two were brought back by doing the real restructure instead of suppressing, which is the outcome the constraint should produce.

**How it is checked from now on.** The rule itself, verified to bite: a fresh two-line use-before-define is rejected by `npm run lint`. New code cannot reintroduce the class.

**What this predicts.** Any rule that TypeScript accepts and ESLint is not configured for is invisible to this repo's whole gate set, and the 24 hits say the assumption that "typecheck plus lint covers it" was already false. The seventeen marked sites are a standing list of where this class still lives.

## 2026-09-01 — The behind-building cue's ENEMY colour was the half nobody looked at (RESOLVED)

**Symptom.** The cue that shows a unit hidden behind a building draws it in one of two ownership colours. The enemy one was a warm coral, and it very nearly vanishes against the building it is drawn on — measured mean RGB distance from a bright roof of 82 against the friendly blue's 197.

**Why it survived three iterations of the feature.** `occlusion-showcase-fixture` spawned only owner-1 villagers. Every capture ever taken of this cue — through the white-to-coloured change, the transparency attempt, and its revert — was the FRIENDLY blue. The colour carrying the answer to the question the cue exists for ("are those mine?") had never been rendered in a screenshot anyone looked at.

**Root cause.** Coral shares a hue family with every roof in the palette. Scored as the SMALLEST RGB distance to any building material — which is what a cue's legibility is bounded by, since it is only as readable as its worst background — coral manages 76 against thatch, where the friendly blue manages 105 against steel. Magenta reaches 137: more legible than either, while staying 192 from the blue so the two can never be confused, and far from the yellow selection outline and cyan drag rectangle the spec requires it to differ from.

**How it is checked from now on.** Two things, because either alone would have missed it. The fixture now seats an ENEMY-owned occluded unit beside the friendly one behind the same Town Center, so both colours appear against the same roof in a single capture. And `tests/rendering/occlusionSilhouetteContrast.test.ts` pins the PROPERTY rather than the hex — worst-case distance to any building material, plus a minimum separation between the two ownership colours — and red-checks: restoring the coral fails it at 76 against a bar of 90.

**An instrument correction inside the fix.** My first scoring probe used a hand-picked subset of the palette and reported the friendly blue at 97. Against the real `VOXEL_COLORS` the blue sits 67 from `waterGlint` — but a silhouette is only ever painted over a BUILDING, never water or foliage, so the correct population is building materials and the true figure is 105. The test names that subset explicitly rather than inferring it, because getting this scoping wrong in the other direction is exactly what let coral through.

**A third instance of the load-dependent browser class, found while shipping this.** `game-progression-production > can train a Monk ... and earn gold income` measured 28 ticks elapsed against a 20-25 tolerance band. Its own comment named the cause — "the game view's own update loop running between evaluate() calls" — so the band existed to absorb real-time drift rather than to assert anything. Pausing makes the count EXACTLY the 20 requested, which is both stronger and load-independent, and the band is gone. That is now three specs fixed by the same move (fog memory, the moving-enemy click, this one), against the register's own prediction that only 4 of 34 browser specs pause.

**What this predicts.** Any two-state visual cue whose fixture exercises one state is untested in the state that matters, and the untested one will be the one carrying the information. The pixel diff also showed a deterministic ±1-5 per channel shift across the minimap from the tint change alone — sub-perceptual, and worth knowing that a colour change in one lane moves rounding in another.

## 2026-09-01 — Three recorded conclusions were all the same horizon artifact (CORRECTION)

The AI self-play verification ran to 34,000 ticks and reported: no owner-slot reaches the Imperial Age, and decided matches sit unresolved. Both went into the record. Both are wrong, and so was a third conclusion built on the second.

Run to 60,000 on five of the same seeds:

    default-seed  o2   feudal 9,750   castle 24,000   IMPERIAL 40,250
    seed-3        o2   feudal 9,500   castle 21,500   IMPERIAL 41,250
    delta              match ended early: VICTORY
    eta                match ended early: DEFEAT
    seed-1        o1   qualified 53,250, castle 54,750

**The Imperial Age is reached**, at ticks 40,250 and 41,250 — 6,000 past where the audit stopped looking. Castle arrives between 20,500 and 30,000, so a 34,000 horizon left as little as 4,000 ticks to bank 1,000 food and 800 gold AND put up two Castle-Age buildings. The game reaches all four ages.

**Matches DO resolve by conquest**, given time: one victory and one defeat in five seeds. The 2026-08-30 register entry describing a decided match running forever, and the resignation feature built and reverted for it — which review proved would have declared living players dead — were both responses to a 60,000-tick behaviour observed through a 34,000-tick window.

**And late arrivals are real, not noise.** `seed-1` owner 1 qualifies at 53,250 and takes the Castle Age at 54,750. Any horizon under that scores it as walled.

**What this costs and what it buys.** The costs are recorded above: a feature designed, implemented, reviewed and reverted for a symptom that partly was not there. What it buys is a sharper rule than "use a longer horizon" — a horizon is an instrument that must be re-justified every time the measured thing changes. 34,000 was chosen to see the Castle Age and is the wrong instrument for the Imperial one. The self-play audit's headline numbers are now qualified by the window they were taken through, wherever they appear.

## 2026-09-01 — Two technology cost rows claimed a source that was never consulted (RESOLVED)

**Symptom.** `researchTables.ts` carried `sultans: { gold: 400 }, // ... (wiki: 400 gold)` and `carrack: { wood: 200, gold: 200 }, // ... (wiki: 200w 200g)`, and the commits that added them said the values were wiki-verified. No wiki was consulted for either. The numbers came from model knowledge of AoE2 DE.

**Why it matters more than the numbers themselves.** The four technologies before them — Shatagni, Recurve Bow, Farimba, Kasbah — WERE checked against the wiki, and two changed as a result: Shatagni is +2 range rather than the +1 memory offered, and the Incas' Couriers turned out to grant armour where memory had speed. That is the entire reason this repo has a standing lesson that a remembered game rule is a hypothesis. Writing "(wiki: ...)" beside an unchecked value does not just risk a wrong number; it disarms the check for whoever reads it next, because the row looks already settled.

**Fix.** Both rows now read `UNVERIFIED`, with a sourcing note above `RESEARCH_COSTS` that states plainly what was and was not checked and why the repo cannot settle it internally: `design/stats/technologies.csv` has no row for ANY expansion-civilization unique technology, which is the data gap recorded separately today.

**How it is checked from now on.** Not by a test — a value's provenance is not something the suite can see, which is the point. What changes is the standard: a sourcing annotation is a claim about work performed, and if the work was not performed the annotation says UNVERIFIED. The effects of both technologies are right in kind and their tests pass on the mechanism, so nothing is broken; what was wrong was the confidence attached to two numbers.

**What this predicts.** Any comment of the form "(source: ...)" written in the same motion as the code it annotates is worth doubting, because the natural time to write it is when the value feels obvious — which is exactly when it was not looked up.

## 2026-09-01 — The Feudal wood ledger was wrong by an order of magnitude, and farms are the sink (CORRECTION)

**What was published.** Repeatedly, in the register and the devlog: "715 wood reached buildings and 525 went to 21 spearmen out of 1,240 total" over 20,000 ticks. That figure has been the basis for calling military-versus-construction the contention that walls the AI in Feudal.

**What is actually true.** Measured observationally — sum of stockpile DECREASES as ground truth, attributed by entity-id diffs and farm-refill jumps — over the Feudal window only, nine seeds and eighteen owner-slots:

    7,500  41.4%  farm construction        1,650   9.1%  blacksmith
    4,020  22.2%  farm reseeding           1,400   7.7%  archery range
    1,775   9.8%  spearmen                 1,175   6.5%  houses
                                             525   2.9%  stable
                                             175   1.0%  market

**Farms take 63.6% of all Feudal wood — three times what the qualifying halls get (20.7%).** The old figure did not count reseeds at all, and reseeds alone are 22%. Military is under 10%, not the co-equal claimant it was described as.

**And the AI farms while free food stands next to it.** `default-seed` owner 1 held 2,125 food of gatherable natural food within gather range of its own Town Center, FLAT from tick 18,000 to the horizon, while its wood never passed 58 and it built up to seven farms. Every seed carries roughly 1,820 of sheep, boar and berries inside that radius. A farm gathers 0.333 food/s — level with sheep, better than berries at 0.3125, WORSE than boar and deer at 0.408 — so the soil is not even faster.

**Why capping farm COUNT does not fix it, proved rather than assumed.** A branch built exactly that cap: it improved the headline at both horizons (Castle-Age slots 4→7 and 10→13) and was correctly rejected, because the wood ledger moved only 5.6%. Soil costs 60 wood per 175 food whether it is built or reseeded, so cutting plots does not cut wood — the same labour simply cycles fewer plots faster, and new-farm wood down 23% came straight back as reseeds up 25%. The headline gain was a build-order SEQUENCING effect (the picker stops returning `farm` and reaches `blacksmith`), not a demand reduction, and it failed on held-out seeds at 24,000 (Castle 1→0).

**The two levers this points at, neither yet tested.** The reseed is ungated: it fires whenever affordable, even with 1,600 food banked, and is 22% of Feudal wood. And the assignment comparator prefers a 60-wood farm to free sheep ten tiles away, because farms are tier-1 and Town-Center-adjacent. The second is what would make the first pay.

**Verified 2026-09-03, so the next attempt starts from facts rather than re-deriving them.** Three things were checked directly against the code and the running sim:

1. *The seeds this entry measured are not contaminated by the wood-less maps.* `default-seed`, `corpus-seed-b` and `seed-1` carry 197, 244 and 271 trees at tick 0. The 2026-09-03 finding that four of the nine PLAYABLE maps shipped with zero wood does not touch this analysis — it was measured on standard-generator seeds that have plenty. The 63.6% figure stands.
2. *A farm is NOT in the comparator's top owner tier, and a plausible-sounding theory that it is, is wrong.* `entityCreateOps.ts` gives a farm `owner: null, baseOwner: owner`, which lands it in `compareCandidates`'s tier ONE (`owner === null && baseOwner === owner`), the same tier as the player's own home resource patches — not tier zero (`resource.owner === owner`). So farms do NOT structurally outrank natural food regardless of distance. This was checked because the opposite reading is easy to reach from the entry's own phrasing and would send an attempt at the tiering, which is not where the defect is.
3. *What actually decides it is drop-off proximity WITHIN that tier.* Once two candidates share an owner tier, the first ranking key is Manhattan distance to the reference drop-off. A farm is built Town-Center-adjacent, so it wins that key against a berry patch or a boar ten tiles out, every time.

**Which reshapes the fix.** It is not a tiering bug and not a distance bug: the comparator's round-trip cost is CORRECT as a measure of walking, and it is the only thing being measured. What it does not price is that a farm's food cost 60 wood and a boar's cost nothing. Any fix therefore has to put a price on the node, not a thumb on the distance — and it has to do so without making a distant free node beat a near one, which is the failure mode the seventeen attempts above already found by ranking alone.

## 2026-09-01 — The gather comparator measures a distance the units cannot walk (FIXED 2026-09-05, v0.3.204; it relocated the root cause)

**What was believed.** Commit `5c31d4e8` identified the routing gate as the ANCHOR: `findNearestDropOffBuilding` is resolved from the VILLAGER's position, so villagers clustered near the Town Center always make the TC their reference, and a tree beside a Lumber Camp is never preferred. That is true, and it is the smaller half.

**What is actually true.** The comparator ranks candidate resources by MANHATTAN distance, and movement is strictly 4-connected — `findGridPath` is called from `movementPlanOps.ts:236` with no `allowDiagonal`, and `civ-engine/src/path-service.ts:202` defaults it to `false`. Both verified directly.

On open ground those agree exactly, so the metric is correct there. Around obstacles it is arbitrarily wrong — and a forest is a dense block of impassable resource cells, which is why the error concentrates almost entirely in wood.

Measured on the AI's live targets: wood villagers work trees at Manhattan 3.6-10.1 whose true walk is **2.6 to 45.1 tiles**. On `aoe2-prototype` at tick 24,000, three villagers were on trees at walk 37 and 35 while trees at walk 0 and 3, holding 100 wood each, sat unused — every one reads as Manhattan 8-9 to the comparator, and two more at the same distance are UNREACHABLE.

**The size of the prize, measured rather than assumed.** A near-optimal assignment is worth **1.49x** the AI's measured total gather throughput over 16 owner-snapshots on four seeds, three of them held out. By resource: wood **2.61x**, stone 1.22x, food 1.05x, gold **1.00x**. Gold sits in the open and has nothing to gain, which is the cleanest confirmation available that obstacles are the mechanism. A replica of the current policy scores 1.01x measured, which is what makes the model credible.

**Which half to fix, ablated.** Ranking from the resource's own nearest drop-off (the anchor fix) recovers 1.16-7.63x on wood; ranking by TRUE WALK recovers 1.62-7.63x; both together equal the metric fix alone. **The metric dominates and is never worse.** This is why seventeen attempts at re-weighting the comparator all failed — every one re-weighted a broken measurement.

**Corroborated by two shipped games, verified verbatim rather than summarised.** AoE2's own AI scripting language measures resource-to-dropsite and never villager-to-dropsite, and `sn-wood-dropsite-distance` targets a 3-tile wood haul against our 2.6-45.1. 0 A.D.'s Petra builds a new dropsite when the fraction of gatherers walking rather than gathering exceeds 0.15; our wood figure is 0.66-0.67, over four times that, against 0.10 for food and 0.00 for gold.

**What this retires.** Camp PLACEMENT is the small family — an extra optimally-placed drop-off on top of good assignment is worth only 8-20%. And this search's own done-condition "camp usage rises" is WRONG: under a true-path optimum the camp's haul share FALLS on several bases, because the best trees sit beside whichever drop-off is nearest in walking terms, often the Town Center. The DESIGN file records that revision.

**What it does not say.** 1.49x is a resource RATE. Nobody has shown that rate is what gates the Castle Age, and the disqualifiers about aggregate-versus-boot-map and improving-by-doing-less apply to any candidate unchanged. There is also a second, unmodelled loss: one base has a 2.6-tile walk, 17% gathering and 3.28 retargets per load, so churn costs something the distance model treats as neutral.

**A near-miss worth recording.** The branch's first pass priced hauls at Manhattan and concluded the family was capped at 1.03-1.27x and should be abandoned. It caught itself because the modelled gathering fraction (70%) did not match the measured one (19%). The same wrong assumption that causes the defect nearly buried the evidence for it.

**FIXED 2026-09-05 (v0.3.204).** The ranking key is now the HAUL: the true 4-connected walk from the cell beside a node to the cell beside the owner's nearest drop-off of the kind, read from one multi-source breadth-first field per owner, resource kind and movement domain (`bridge/dropOffWalkField.ts`), cached on the structural revision PLUS the set of drop-offs — a camp completing bumps no revision — and rebuilt only when either moves. The home range is the same haul, so a node no drop-off can reach is never home whatever its straight-line distance. (The drop-off id an assignment records is still the Manhattan-nearest: the deposit leg re-resolves it from the carrier's live position the tick the load fills, before anything reads it, so a walk-nearest value there would be a dead write — a critic's finding.) The comparator was not re-weighted; the measurement under it was replaced, which is what the ablation above said would work and what seventeen re-weightings could not do. The recovery probe (`requireReachable`, sixteen candidates in villager order) is untouched.

**The deposit leg has the same defect one level down, and its fix is NOT in this commit.** A critic found it by its symptom: `dropOffStep` chooses the Manhattan-nearest drop-off from the carrier's live position and then the FIRST ring cell of it an A* can reach, in Manhattan order. On Nomad that hands a loaded villager at (14,14) a 27-step loop around its base to the Manhattan-nearest ring cell of the Town Centre while its neighbour at (15,14) gets the 10-step route to the next ring cell THROUGH (14,14) — and fourteen carriers, all sent to the last two reachable trees by the now-correct ranking, shuffle into each other from tick 24,000 to 45,000 with zero deliveries. That is the open entry "Fourteen loaded carriers freeze head-on on Nomad" at the top of this file. The fix — the deposit leg descends the walk field's gradient from the carrier's own cell, no per-carrier search — is built, gated and measured on branch `deposit-walk-field`, and it is not merged because it also makes the boot map resolve by conquest at tick 39,812, which trips two 45,000-tick gates whose contracts were derived from an unresolved match; re-deriving those is the owner's decision, recorded in that entry.

**Instrument.** `npm run ai:gather-walk` (`scripts/aiGatherWalk.mjs`) measures every wood villager's haul with `src/game/playtest/walkDistanceProbe.ts` — rebuilt from components alone, sharing no symbol with the ranking — and judges each target against the best unsaturated tree the ranking would have preferred by its own rules (`gap`; same ownership tier or better) and against the best tree of any tier (`tier`). It counts every delivered wood load, splits the wood force's time into chopping, walking and idle, counts FROZEN villagers (a walking task that has not left a two-cell square for 750 ticks — the column the Nomad freeze taught it), and reads the field's rebuild count and wall time from the bridge's debug snapshot. The symptom above reproduces on the boot map at tick 6,000 (`SEEDS=aoe2-prototype TICKS=6000 SAMPLE=1500 npm run ai:gather-walk`, pristine tree against this one): owner 1's three wood villagers on a tree at a walk of 34 (Manhattan 5), trees at a walk of 2 and 4 untouched, chop share 24% against 61%, 110 wood delivered against 340.

**Measured on every playable map, both arms, 45,000 ticks, `SEEDS=<all nine> TICKS=45000 SAMPLE=1500 npm run ai:gather-walk`, baseline = pristine 46a4ba8b plus the instrument files, candidate = this commit's ranking, both arms in one job with source hashes checked before and after (candidate 0a699d3621c7, baseline 801db7dd0d66, unchanged).** `chop` is the wood force's chopping share of its time, `haul` the mean walk from the target to the nearest drop-off with the mean Manhattan distance in brackets, `gap` the mean cells beyond the best tree the ranking's own rules allowed, `misranked` the share of targets more than six cells beyond it, and a star marks a match that ended before 45,000 ticks.

    seat                 chop        wood delivered      haul mean (manhattan)     gap mean     misranked    villagers on wood
    aoe2-prototype o1    33% -> 36%   2,868 ->  4,518    17.5 (6.0)  ->  9.7 (9.0)   12.3 -> 0.7   39% ->  0%    5.5 ->  7.9
    aoe2-prototype o2    36% -> 37%   5,022 ->  4,905     7.3 (5.6)  ->  6.4 (5.6)    5.2 -> 0.4   17% ->  1%    8.9 ->  9.3
    arabia o1            28% -> 32%   2,603 ->  3,319     7.8 (7.3)  ->  9.2 (7.4)    0.7 -> 1.0    2% ->  4%    5.5 ->  6.2
    arabia o2            35% -> 39%   5,882 ->  4,959    19.1 (8.1)  ->  6.9 (8.1)   11.4 -> 0.5   50% ->  1%   11.3 ->  8.6
    arena o1             26% -> 39%   4,676 ->  3,923*   10.2 (9.7)  ->  9.9 (11.1)   1.3 -> 0.2    4% ->  0%   10.8 ->  7.7
    arena o2             23% -> 47%   3,171 ->  2,428*   14.8 (12.1) ->  7.6 (9.0)    4.5 -> 0.5   20% ->  2%    8.4 ->  4.3
    black-forest o1      63% -> 64%   8,644 ->  8,537     4.7 (6.3)  ->  4.1 (5.8)    2.4 -> 0.1    4% ->  0%    8.7 ->  8.4
    black-forest o2      74% -> 56%   6,210 ->  8,669     2.7 (4.5)  ->  4.9 (5.6)    1.2 -> 1.5    1% -> 15%    5.3 ->  9.9
    coastal o1           40% -> 37%   2,342* -> 5,191    11.2 (7.4)  ->  8.8 (9.4)    9.4 -> 0.5   72% ->  2%    4.1 ->  9.2
    coastal o2           48% -> 36%   4,726* -> 2,392     7.8 (9.0)  ->  4.8 (6.2)    5.9 -> 0.7   63% ->  0%    6.5 ->  4.5
    fortress o1          22% -> 21%   5,561 ->  6,797    27.6 (15.7) -> 23.2 (19.3)   7.4 -> 1.1   59% ->  2%   16.5 -> 20.3
    fortress o2          28% -> 33%   6,125 ->  3,549    22.5 (12.2) -> 14.5 (14.8)   9.0 -> 0.9   63% ->  5%   14.7 ->  7.4
    gold-rush o1         53% -> 50%   4,044* -> 9,282     7.6 (9.4)  ->  9.5 (11.4)   2.1 -> 0.3   10% ->  0%    6.5 -> 11.4
    gold-rush o2         55% -> 56%   2,031* -> 2,484     6.9 (8.7)  ->  5.8 (7.6)    0.5 -> 0.3    0% ->  0%    3.3 ->  2.9
    islands o1           74% -> 75%   1,600 ->  1,600     3.3 (4.8)  ->  3.2 (4.7)    3.0 -> 0.0    0% ->  0%    1.5 ->  1.5
    islands o2           66% -> 64%   1,796 ->  1,759     2.4 (4.3)  ->  2.4 (4.2)    1.1 -> 0.0    0% ->  0%    1.8 ->  1.8
    nomad o1             34% ->  8%   1,735 ->    981    17.5 (3.8)  -> 24.7 (6.1)    3.8 -> 0.0   14% ->  0%    3.4 ->  9.7
    nomad o2             56% -> 20%   1,889 ->  1,678     3.7 (4.8)  -> 70.6 (5.2)    1.3 -> 0.3    0% ->  0%    2.1 ->  5.8

    resolved: 2 of 9 (gold-rush victory 34,885, coastal defeat 43,236) -> 1 of 9 (arena victory 34,231)

**Reading it.** The defect is gone on every map that had it: the misranked share falls from 39-72% on the six seats that suffered most (17-72% on the eight worst) to 0-5% on every seat but one (black-forest o2, 15%, item (6) below), and the mean gap from 5-12 cells to at most 1.1 on every seat but that one (1.5). Haul falls wherever it was inflated (aoe2-prototype o1 17.5 -> 9.7, arabia o2 19.1 -> 6.9, arena o2 14.8 -> 7.6, fortress o2 22.5 -> 14.5). Islands, where the wood sits beside the drop-off, is unchanged to the load, which is the instrument check the open-ground gate makes at the unit level. Nomad gets WORSE — item (5) below, the open entry at the top of this file — and wood delivered per seat is otherwise mixed; the seats that fall are explained below rather than claimed away.

**Cost, measured rather than believed.** The field's own timer, on every map: 332-1,260 rebuilds in 45,000 ticks — once per 36-136 ticks, because a felled tree, a placed building and a wandering animal each bump the revision — costing 67-568 ms in total, **0.05-0.25% of step time**, while answering 2,000-4,700 asks. On the boot map over 15,000 ticks a rebuild took 0.25-0.42 ms. A CPU profile of a 30,000-tick run puts the walk field at 0.4% inclusive and the whole gather assignment at 0.6%, against 37% in the engine's JSON diffing and fingerprinting, 13% in occupancy queries and 10% in pathfinding. Ticks per second, sampled interleaved (baseline, candidate, baseline, candidate, 20,000 ticks each, same minute, machine quiet): boot map 490 and 488 against **555 and 581** with 51 against 46 units on the map at the end; arena 445 and 448 against **583 and 585** with 48 against 29 units. Read the boot map as "not slower at a comparable world" and arena as nothing at all: at 20,000 ticks the arms' worlds there differ in size, so interleaving removes the machine's noise and nothing else — a critic's point, and the reason an earlier draft's "faster because villagers walk less" is withdrawn. Milliseconds per thousand unit-ticks over whole matches move both ways by seed (from -36% on nomad to +25% on the boot map) under a load that changed mid-run, and are not read here. Whole-match ticks per second in the table above is NOT a cost measurement either: it swings by tens of percent by seed, it tracks the size of the world each arm produced (black-forest 43 to 64 units on the map), and identical baseline work measured 366 and 268 ticks per second in two runs forty minutes apart while an orphaned headless Chrome burned twelve CPU-minutes per minute. The instrument prints milliseconds per thousand unit-ticks and the field's own share so the next reader does not have to argue from ticks per second.

**What the measurement also shows, and this fix does not claim.** (1) Wood delivered per seat over 45,000 ticks is NOT uniformly up: the two seats interact and the AI's villager split responds — fortress owner 2 keeps 7.4 villagers on wood against 14.7 before and delivers 3,549 against 6,125; arena's two seats play 34,231 ticks instead of 45,000 because the match now resolves. What is uniformly better is the ranking itself: the share of targets misranked by the comparator's own rules falls from 0-72% per seat to 0-5% on every seat but black-forest o2 (15%, item (6)), and the mean gap to the best available tree from 0.5-12.3 cells to 0.0-1.5. (2) Coastal owner 2 chops LESS (48% to 36%) at a SHORTER haul (7.8 to 4.8). Inspected at tick 20,000: its near woodline is eaten down to trees holding 3, 50 and 60 wood, because it took the nearest trees first, while the baseline was still leaving untouched trees at hauls of 1, 2 and 5 beside its Town Centre. A correct ranking eats the near wood sooner, so the open item above — "the AI builds ONE lumber camp, ever" — costs more with this fix than without it. That is the next lever. (3) The spec's own-base-first tier still costs walking: at tick 6,000 on the boot map owner 1 walks 6 for a tier-1 tree while tier-2 trees at hauls 0 and 2 stand unused; measured as `tier` 3-7 cells on the two maps that tag trees with a base owner. That is the preference working as written, recorded as the lever after the camp. (4) Resolution stays the knife-edge it was: which seeds resolve moves under any change; how many is in the table. `aiReachesCastleAge.test.ts` at 45,000 ticks is the boot-map bar and passes on this commit WITH NO MARGIN on one bar: owner 2 reaches Castle at 27,750 with 14 building types and 9 unit types against a bar of 9, peak army 10 against 10; owner 1 reaches Castle at 31,250 where it never left Feudal before. One fewer unit type on the boot map and this commit's own gate is red. (5) NOMAD GETS WORSE UNDER THIS COMMIT, and the first reading of it was wrong. The measurement read "owner 1 walks 85% of its time" and blamed the AI's split. A critic tracked it tick by tick: fourteen loaded carriers shuffle between two cells from tick 24,000 to 45,000 with zero deliveries — not walking, jammed, by the deposit-leg defect described above. Wood delivered falls 1,735 to 981 (-43%) and 1,889 to 1,678 (-11%). The baseline is broken there too, in the ranking's own way — at tick 30,000 its four wood villagers all work one tree at a walk of 29 (Manhattan 3) while a tree at a walk of 1 holding 70 wood stands untouched — but it delivers more, because a slow walk delivers and a jam does not. Under this commit the correct ranking sends every wood villager on this wood-starved map to the same two reachable trees (seven and six per tree), and the deposit leg then hands the two cells' occupants mirror-image routes. Owner 2's seat is its own case: every tree on the map is unreachable from its own drop-off rings at tick 30,000 (its base is sealed by its own buildings — the placement class, unrecorded before), its haul reads 70.6 because its few reachable targets are across the map, and one carrier stands still for 12,032 ticks. The fix for the deposit leg is on branch `deposit-walk-field` (Nomad there: chop 37%, wood 1,783). (6) BLACK FOREST OWNER 2 keeps 15% of its targets more than six cells beyond the best available tree (42 of 280 samples, mean gap 1.5). Inspected at ticks 39,400 and 40,000 with the live field exposed beside the probe: the game's haul and the probe's haul agree on every tree, the two passabilities on every cell of the map, no enemy defence stands within 14 cells of the untouched haul-5 trees, and the six villagers concerned stand west of the Town Centre with full loads holding trees at hauls of 12-14 — carriers, stranded by the deposit leg's routes, the same defect as (5). On branch `deposit-walk-field` the seat reads 0% misranked and a mean gap of 0.2. (The first reading here blamed the recovery reroutes' villager-order probing; that hypothesis was not needed, and that path is untouched.)

**How this class is checked from now on.** `tests/simulation/gatherWalkRanking.test.ts` runs the boot map to 6,000 ticks and fails when any wood villager's target is more than six cells beyond the best tree the ranking's own rules allow, measured by the independent probe — RED on the pristine tree at exactly this entry's numbers (haul 34, 29 beyond), green after. `tests/simulation/dropOffWalkField.test.ts` pins the metric on a hand-built wall, the Infinity answer, which drop-off a walk ends at, every cache key, and the open-ground equivalence: on open ground the walk is Manhattan minus the two end cells, so the ranking is unchanged everywhere it was already right. `tests/simulation/villagerGatherAssignment.test.ts` pins what the comparator does with a haul, and both unit mutations went red (gate-proofs.md, which also records the unit case that first passed for the home filter's reason rather than the key's).

## 2026-09-01 — Villagers latch on an OPEN cell when buildings move (FIXED)

**The fix.** The narrow-passage election keeps stable lowest-id as its ordinary rule, and yields to a unit that has not CHANGED CELL for `TRAFFIC_STARVATION_TICKS` (750). Gated by `tests/simulation/movementTrafficFairness.test.ts`.

Measured across six seeds at 20,000 ticks, villagers frozen >= 1,000 ticks while in a walking task:

```
seed              before            after
aoe2-prototype     500 / 0     ->    500 / 0     preserved field-for-field
seed-2            2750 / 6     ->    500 / 0
default-seed      1750 / 3     ->    500 / 0     (o1 buildings 17 -> 19)
corpus-seed-b     1250 / 5     ->   1000 / 2
seed-7            5250 / 12    ->   3500 / 5     improved, NOT cured
seed-11            500 / 0     ->    500 / 0
  stuck villagers     26                7
```

`seed-7` is the honest remainder: five villagers still freeze there and its owner 1 is still walled in the Dark Age, so that seed holds a second cause this fix does not reach.

**Two rejected attempts, which is where most of the information is.**

*Attempt 1 — replace the election with a fair epoch-rotated rank.* Cured four seeds (26 stuck villagers down to 5) and regressed the boot map from ZERO stuck villagers to five, with owner 1 losing four villagers. Rejected on the adoption rule, the same bar that rejected branch A and round 2. A fix that perturbs a map with no pathology is trading a real regression for an aggregate.

*Attempt 2 — yield only to a unit that has not been ADMITTED for 750 ticks.* Preserved the boot map and changed nothing else at all: `seed-2` stayed at 2750/6. The null result diagnosed the error, though the arithmetic first written here was wrong in every number and was corrected on 2026-09-02 after the multi-CLI review (finding E7). `resolveMovementTraffic` is consulted about ONCE per unit per tick — the "~44 per unit" was the whole population's consults per tick at ~44 moving units, mislabelled as per-unit (review instrument I1: 121,814 unit-ticks on `seed-2`, maximum 1 consult per unit per tick) — and a villager frozen for 2,750 ticks still collected sporadic admissions, 12 in 40 ticks, measured, every one of which reset the clock. **Permission is not progress:** an admitted tick credits 0.32 FINE units (0.08 tiles) against a 4-fine-unit cell, so crossing one cell needs ~12.5 CONSECUTIVE admissions and 12 scattered ones move under one cell; an occasional yes buys no movement, and an admission clock that resets on each of them never reaches 750 at any consult rate. Attempt 3 gates on the unit actually changing cell, and that is the only difference between them.

**Why lowest-id was there.** The file's header: decisions must be derived so that "ECS iteration order cannot decide who gets to enter first." Any replacement must stay a pure function of tick, ids and per-unit state, and every member of one closure must elect the SAME winner or two units drive into one cell. The shipped fix keeps both properties.

**Residual risks, named rather than discovered later.** 750 is a tuned constant standing on measurement — above every healthy figure (500) and below every pathological one (1,250+) across these six seeds; a map whose healthy waits exceed it would be perturbed, and no such map is proven absent. And two movers whose closures differ could still elect different winners; this degrades no worse than lowest-id did, which is an argument, not a proof. AGENTS.md routes concurrency-class work to multi-cli-review; that skill runs through subagents, which this session's instructions prohibit, so this was self-reviewed and the risks are recorded here instead.

---

## 2026-09-01 — Villagers latch on an OPEN cell when buildings move (root-cause record)

**The cause is measured, and it is neither of the two things this entry previously proposed.** It is not containment and it is not an unreachable-target latch. The narrow-passage traffic arbiter elects `Math.min(...cycle)` — the single LOWEST unit id in the jam — and admits only that one. In a jam that is continuously replenished, high-id units are never admitted. Admission rate over 40 ticks in the frozen window on `seed-2`, every unit in the choke:

```
id     proceed   wait    admitted
2432     2032     264      88%
2434     2232    1091      67%
2436     2255    1565      59%
2443     1121    1658      40%
2448      827    1668      33%
2451      769    2126      27%
2452      647    2335      22%
2455      349    2673      12%
2456      243    2543       9%
2458       68    2080       3%
2460       12    2092     0.6%
```

Monotonic in id, with no exceptions. The newest villager wins 12 of 2,104 attempts. Every unit moves sometimes, so this is STARVATION, not deadlock — which matters because a deadlock-breaker is the wrong fix.

**The geometry.** A one-tile choke at (48,30): trees at (48,29) above, the 2x2 lumber camp at (48,31)-(49,32) below. Eleven villagers meet there head-on — five westbound to trees (2434 2443 2451 2456 2460) against five eastbound to drop-off 2186 (2432 2436 2448 2455 2458) — three deep in each of (45,30), (46,30), (47,30). All carry a CURRENT learned direction, so the wait-for closure is valid and the election runs normally; the election itself is the defect.

**The target churn is a symptom, not the cause.** A frozen villager's `targetResourceId` cycles (2247 -> 2251 -> 2245 -> 2252 -> 2253) because the unreachable branch in `toResourceStep` keeps reassigning it. That branch is working as designed; it re-picks a DESTINATION, and this unit's problem is that it cannot take a STEP. Destination-side recovery can never fix an origin-side block.

**A carrier is included.** 2458 holds a full 10 wood it cannot deliver, so a BUILDING is unreachable too, not only trees — which is why "every direction is blocked" earlier read as containment.

**Reproduces on unmodified HEAD**, no camp patch: five villagers frozen simultaneously on `seed-2`, longest 2,750 ticks, ids 2460/2451/2458/2452/2455.

**My own instrument error, caught before it was published.** The first map I drew used `b.width ?? 1`; the field is `footprintWidth`, so every building rendered 1x1 and a 4x4 Town Centre appeared as one cell. I had a complete chain ready to write from that map — not sealed, therefore the pathfinder is wrong, therefore the bug is in A* — and every step of it followed from a grid built on a guessed field name. Verified afterwards by drawing both anchor conventions and counting units standing inside buildings: TOP-LEFT gives 0, CENTER gives 3, so TOP-LEFT is correct. A probe that reconstructs game state from guessed field names is a hypothesis about the schema, not a measurement of the game.

**Two prior characterisations in this entry were wrong and are corrected above:** "(51,28) is not a sealed pocket... this is a movement or assignment latch" was RIGHT about it not being containment and imprecise about the rest; the blocker is other UNITS, which never appear in a passability check, so open neighbours are entirely consistent with the freeze. The `isFree` placement guard has no hole — it was never the mechanism.

**What the fix must preserve.** The current rule is stable-lowest-id specifically so that "ECS iteration order cannot decide who gets to enter first" (the file's own header). Any replacement must stay a pure function of tick and unit id, and must keep every member of one closure electing the SAME winner, or two units enter one cell. A fair rotation keyed on the tick satisfies both. This file already carries one scar from this family — "two wood carriers and two villagers sat one step from their lumber camp for ten thousand ticks" — whose fix added this very election, so the class needs a liveness property, not another special case.

---

## 2026-09-01 — Villagers latch on an OPEN cell when buildings move (superseded framing, kept for provenance)

**Symptom, at its worst.** During branch A of the lumber-camp search, a candidate that relocated the Lumber Camp pushed the Mining Camp onto the Town Center's shoulder at (52,24). On `aoe2-prototype`, owner 2's economy then stopped dead at tick ~7,500 and never restarted: all ten villagers stood on a single cell, (51,28), every one in `to-resource`, none moving, from tick 9,000 to tick 20,000 — with the berry bushes REGROWING because nobody was eating them. Food 320, wood 157, gold 300, stone 200, byte-identical across twelve thousand ticks. No engine halt and no tick failure. Owner 2 never left the Dark Age.

**(51,28) is not a sealed pocket.** (50,28) and (52,28) are open grass in both arms, and there is a gold mine three tiles away the villagers never reach. The `isFree` guard in `findPlacementAnchorNear` exists to stop the AI walling itself in and does not prevent this. So this is not a containment bug; it is a movement or assignment latch that a building rearrangement can trigger.

**Isolated rather than theorised.** Flipping the Dark-Age build order so the Mining Camp is requested first returns it to (46,31) and the deadlock disappears entirely — owner 2 has eleven villagers spread across farms and trees, all gathering. The trigger is the DISPLACEMENT, not the camp's own position.

**How common is it on unmodified HEAD? Smaller than first reported, and the first measurement was mine and wrong.** Counting villagers whose CELL does not change over 250-tick samples gave 1-3 stuck villagers on every seed and freezes of 20,750 ticks. That probe was invalid: a villager with `task: 'gathering'` stands still BY DESIGN — it is chopping — so the query counted normal work as a freeze.

Restricted to `to-resource` and `to-dropoff`, the states where a villager should be moving, over 34,000 ticks:

    seed-2           4.2% of walking samples frozen >= 2000 ticks   longest 5,750
    default-seed     0.3%                                          longest 2,500
    aoe2-prototype   0.2%                                          longest 2,250
    seed-1           0.0%                                          longest 1,500
    corpus-seed-b    0.0%                                          longest 1,500

So on HEAD this is largely one seed's problem and marginal elsewhere — NOT the widespread standing defect the first probe implied. What branch A found is a far more severe manifestation that a building rearrangement can provoke.

**Why it is registered rather than fixed.** It is the gate in front of the largest prize measured on this wall: branch A's candidate moves camp usage from 39.7% to 51.9% and Castle-Age owner-slots from 4 to 7 and 10 to 14, and this latch is the only thing disqualifying it. A fix needs the latch understood first — a villager in `to-resource`, on open ground, with a reachable target, that never moves again.

**What this predicts.** Any AI change that rearranges building placement can trip it, which makes it a hazard for a whole class of future work rather than for one candidate. It also means the placement guard's contract — "do not wall yourself in" — is weaker than it reads: a unit can be immobilised on ground that is not enclosed at all.

## 2026-09-02 — Entering placement mode collapsed the build palette, and its tooltip sat on the minimap (RESOLVED v0.3.179)

**Symptom.** Found by the owner playing at `localhost:5173`. With a villager selected, clicking a card in the BUILD group to place a building made the BUILD group collapse to a sliver a few pixels wide at the right edge of the bottom bar, and the card's tooltip — "Place a House foundation (cost: 25 wood). The selected villager walks to the site and builds it." — rendered on top of the minimap in the bottom-right. The owner's steering that day was "the UI needs to be elegant"; local-rules already records "a modern looking UI" for the HUD.

**Investigation.** Reproduced headlessly at three viewports on the v0.3.177 build with a measurement script, before any fix. The bar is a flex ROW: a fixed 264px summary, then the command deck, and only the deck may shrink (`flex: 1 1 auto`); inside it, stance and formation are fixed 2x2 blocks and the build group takes what is left down to `min-width: 250px`. In placement mode the status ("Placing: House" / "Choose a clear map tile") was rendered as a THIRD sibling of the bar between the summary and the deck, so its ~240px came out of the deck, the deck's fixed groups shifted right by that much, and the build group — still 250px wide in LAYOUT — was pushed under the bar's clipped edge (`overflow-x: auto`). Measured: at 1280x720 the build group's visible width went 261px → 36px (its layout box moved from x 736 to x 976 under a panel clipped at x 1012); at 1440x900, 421px → 196px; at 800x600 the wrapped regime below 1120px was unaffected (546 → 546) — which is exactly why the suite's default 800x600 viewport had never seen it. The tooltip half turned out to be a CLASS rather than a consequence: the shared tooltip is centred above its control, the palette's right-hand cards sit beside the minimap, and hovering them put the tooltip over the minimap frame at EVERY viewport before placement mode was ever entered (1280: Mill card, tooltip x 804..1064 against the frame at x 1022..1268; 1440: Lumber Camp, x 958..1218 vs 1182..; 800: Mining Camp, x 376..636 vs 598..). The first fix's after-screenshot then showed a third thing: with the palette restored, the clicked card's tooltip sat over the group heading and hid the very "Placing: House" status the click had just revealed.

**Root cause.** A mode announcement rendered as a LAYOUT SIBLING of the thing it annotates, in a bar where exactly one sibling absorbs every width change; and a tooltip positioner that knew about the viewport and the modal menu but not about the one HUD surface it must never cover.

**How it is checked from now on.** `tests/browser/placement-mode-hud-layout.spec.ts` boots at 1280x720, 1440x900 and 800x600 (both bar regimes), hovers EVERY build card before and during placement and asserts the tooltip's rect never intersects the minimap frame, clicks a card with the real mouse, and asserts the build group's VISIBLE width — its rect clipped by the scrolling panel and the viewport, because the layout box keeps its min-width and a plain rect check passed on the broken code (250 vs 261 = 96%) — stays at 90% or more of its pre-placement width, that the placement status is inside the bar, and that the deck's set of groups is unchanged. `tests/ui/tooltipAvoidsMinimap.test.ts` pins the positioner's arithmetic (above the bar; slide left of the frame; above the frame when nothing fits beside it; a tooltip anchored inside the frame stays put), and `tests/ui/selectionPanelCommands.test.ts` pins the status to the Build group's heading rather than the bar. Fix: the status is a 17px pill inside the Build heading between title and count — the count badge's own height, so the row is unchanged whether the badge is shown or, below 380px of group width, steps aside with the title for the pill (a 20px pill grew the group 180 → 183px; a 16px one lost 1px once the badge was hidden; the threshold was 340 until the critic pointed out that "Placing: Siege Workshop" plus the hint needs about 350, so a 1366px window would have cut the hint mid-word) — and command-bar tooltips float above the BAR and never intersect the minimap frame. The gate also holds the group's height, the heading's fit inside its group, the hint whole-or-hidden, and the tooltip above the bar's top edge.

**What this predicts.** Any status, badge or banner inserted as a sibling into a flex row with a single shrinking member will take its width from that member — the summary column, the queue list and the deck are the other siblings of this bar, and any future "mode" UI should go INSIDE the group it describes. And a positioner that clamps to the viewport is not a positioner that clamps to the HUD: any other floating element (toasts, the idle-villager badge, the countdown chip) is one layout change from the same overlap — the idle-villager badge already sits over the SELECTION label at the bar's bottom-left corner, seen in this task's whole-frame sweep and not yet touched.

## 2026-09-02 — Units walked with a stutter: move, stop, move (RESOLVED v0.3.192)

**CORRECTED 2026-09-02, same day, and read this first.** Two things below are no longer true. (1) The VERSION: this work never shipped as v0.3.187 — that number on `main` is the DE build-times change — and it ships as v0.3.192. (2) The snap-on-a-skipped-tick rule this entry describes as the fix was itself a defect and is GONE: it stood every unit still on any machine drawing slower than 10 fps. Wherever a paragraph below says a coalesced sync or a skipped tick SNAPS, the current rule is that it GLIDES and only a move longer than 1.5 tiles per tick of the gap snaps. See the 2026-09-02 entry at the top of this file, "The motion smoother stood every unit still on any machine slower than 10 fps". The sentences are left as written because the register is history; this note is the correction.

**Symptom, as the owner saw it (2026-09-01, watching live).** "Units seem to move and briefly stop mid-movement between cells." Every walking unit, every walk, on every map — a villager crossing open ground visibly lurched: a short slide, a pause, a short slide.

**Investigation.** Measured rather than eyeballed, on the real boot map (`aoe2-prototype`, fog on): one villager ordered eight tiles down the open-grass corridor at row 11 (cells 12–20), the DRAWN root (`getDisplayedEntities`, the presentation coordinator's output) sampled after every 60 Hz frame driven through the same bridge/presentation path the live loop uses. Of the 552 mid-walk frames (first and last 300 ms excluded), **372 — 67.4% — drew the villager exactly where the previous frame had**. Every frame that did move it moved it 0.0417 tiles, one sixth of a fine step, at 2.50 tiles per second — three times the villager's 0.8 — and then nothing for the next twelve to eighteen frames. Live requestAnimationFrame sampling of the same walk, unpaused, read 43–53% still frames at the ~20 fps the headless renderer manages. A 12-frame strip at 60 ms spacing showed the drawn root's screen x as 400.0, 396.2, 384.6, 380.8, 380.8, 380.8, 377.0, 365.4, 361.6, 361.6, 361.6, 357.8 — three identical frames out of every five.

**Root cause — render-side, and pure arithmetic.** Spec §12.4.2 (v0.3.160): the sim advances a unit through a per-unit carry bank. `movementEntitlement` adds `round(0.32 × speedPercent)` hundredths of a fine step each tick — 32 for a villager — and grants a whole fine step (a quarter tile, `UNIT_SUBGRID_RESOLUTION` 4) only when the bank crosses 100: ticks 4, 7, 10, 13, 16, 19, 22, 25, 29, … — every third tick, every eighth interval a fourth. Between those ticks the sim position does not change at all. The presentation (`interpolateProjectedEntities`) blended only between the PREVIOUS tick's position and the current one, so on a stepping tick the drawn root slid a quarter tile across 100 ms and on the two or three ticks after it stood still, because both endpoints were equal. 100 ms moving, ~200 ms standing, repeating: a 3.3 Hz pulse. The locomotion animation had already been given a two-anchor window for exactly this reason (v0.3.160, "18% idle frames, 250 ms idle holds on a continuously walking villager"), which fixed the GAIT and left the ROOT pulsing — the walk cycle turned steadily while the body hopped.

**Fix.** `src/rendering/displayedPositionSmoother.ts` (new): each live unit's drawn root replays that unit's own recent sim samples — recorded only when the sim position changes — interpolated between the two that bracket `now − delay`, with the delay equal to the unit's longest unobstructed fine-step interval at its base rate (`src/rendering/unitStepCadence.ts`: `ceil(100 / round(0.32 × basePercent))` — villager 4 ticks, scout 3, knight 2, Battering Ram 5; a sheep its 6-tick herd throttle; a fleeing deer its 14-tick hop clock, so a whole-cell hop is drawn as a 1.4 s glide at 0.71 tiles/s; charging wildlife, which the sim moves a whole cell every tick, one tick). The render time therefore always sits inside a segment whose far end is known, and the drawn root advances every frame at that interval's average speed; the 3-tick/4-tick alternation leaves a 4:3 speed ripple where there was a full stop. A unit leaving rest has its rest sample re-timed so motion is drawn the tick the first step lands, at cadence speed; the delay is paid at arrival, where the drawn root settles four villager-ticks after the sim. Jumps over 1.5 tiles (a deer's diagonal hop is 1.41 and glides — both sides of the line are pinned), ticks going backwards, a presentation that skipped a tick (the live loop coalesces up to 2.5 ticks a frame, 5 at double speed; the steps inside the gap were never observed, so every track snaps to its sim position and resumes from there), bridge swaps, recycled ids, a unit whose type changed under the same id, and entities absent from the previously presented tick all snap, as the spec's continuity contract requires. The sim, its subgrid, occupancy and determinism are untouched. `interpolateProjectedEntities.ts` and the `RenderStore.previousPositionFrame` it read are deleted with their per-tick producers (a sorted position array and a Set of string keys built for every visible unit, every tick, in `renderStore.ts` and `bridge/renderStateOps.ts`); the sheep and deer cadence constants moved into `bridge/wildlifeCadence.ts` so the renderer reads the numbers the throttles run on (`pureHelpers.ts` sits exactly at the 500-line cap — the first attempt to host them there was caught by `fileSizeBudget.test.ts` at 511).

**After, same instrument, same corridor.** Driven 60 Hz: 570 mid-walk frames, **0 still (0.0%)**, per-frame step 0.0104–0.0139 tiles (ratio 1.33 — exactly the sim's own 4-tick/3-tick alternation), drawn speed 0.62–0.83 tiles/s against the sim's 8.00 tiles covered in 100 ticks. Live rAF: 107 frames, 75 in the window, 0 still. The strip's screen x: 396.2, 392.3, 388.5, 384.6, 380.8, 377.0, 373.1, 369.3, 365.4, 361.6, 357.8, 353.9 — 3.8 px every frame. Strips under `tmp/motion/before` and `tmp/motion/after` (gitignored evidence; `frames.json` beside each carries the drawn and sim positions per frame). **The deer, measured separately (2026-09-02, `deer-flight-fixture`, a scout luring a deer for ten seconds at 60 Hz, six hops, 406 frames inside the fourteen ticks after a hop): at a one-tick delay — the adjacent-tick lerp the fix had first left the deer on — 370 of those 406 frames, 91.1%, drew the deer where the previous frame had, and every moving frame crossed 0.2357 tiles (a sixth of a diagonal hop, 14.1 tiles/s); at its fourteen-tick cadence, 0 still frames, every frame 0.0168 tiles, 1.01 tiles/s.** The whole-series still share of that run is 28% after the fix, and that is the sim: a scout on the four-connected grid gains 1.2 tiles of Manhattan distance a second while a diagonal hop gains 2 every 1.4 s, so the deer periodically escapes the five-tile flee trigger and stands until the scout closes again. Villager cross-check on the same instrument: with every delay forced to one tick (the exact arithmetic of the old adjacent-tick lerp) the driven walk reads 67.4% still again, so the before and after numbers come from one instrument and one build path.

**How it is checked from now on.** `tests/browser/unit-motion-smoothness.spec.ts` is the gate for the CLASS — any rendering that draws a moving unit standing still — not for this instance. On the real boot map it stages a villager on the nearest straight open-grass corridor, walks it eight tiles, samples the drawn root after every driven 60 Hz frame and asserts at most 2% still frames mid-walk and a largest-to-smallest per-frame step ratio under 2 (the ratio bar is for a villager's 4/3; a unit earning 50–99 hundredths a tick would sit at exactly 2.0, so the walker must stay a villager); repeats the walk unpaused under real animation frames and asserts the 2% still bound as a floored frame COUNT (the headless renderer manages ~20 fps, so a share would let one frame decide a 50-frame window; the step ratio is not asserted live, where frame times vary — measured 9.4 on a clean run); asserts that a sync coalescing five ticks snaps the drawn root onto the sim root and that tick-by-tick syncs then glide again; and on `deer-flight-fixture` lures a deer with a scout for ten seconds and asserts, over the fourteen ticks after each of at least four hops, at most 2% still frames and a step ratio under 2 (`analyseHops` in `tests/browser/helpers/motionAnalysis.ts` — the whole-series share is logged, not asserted, because the deer really stands between hops). Its header records the red numbers. `tests/rendering/displayedPositionSmoother.test.ts` pins the mechanism: no zero-displacement frame while the target advances, ratio under 1.5 on the honest cadence, no overshoot, snap past 1.5 tiles and glide at 1.41, snap on a skipped tick, settle-and-hold, idempotence, fog re-entry, recycled ids, a type change, a same-tick correction, the cancellation-tick seed, the sheep and deer cadences; `tests/rendering/AoeVoxelPresentationCoordinator.test.ts` pins the coordinator's snap on a coalesced sync and on a non-adjacent first sighting, the two contracts the deleted adjacent-tick tests held.

**Instrument corrections made on the way, all worth knowing.** (1) The first green run FAILED the ratio bar at 2.50 with zero still frames: the spec's staging walk had advanced ten ticks per presentation sync (`advanceTicks(10, 100)`), so the smoother's history held sync times rather than step times and its tail crept into the measured walk at 0.33 tiles/s. The staging now syncs every tick, as the loop does — but the CODE was wrong too, not only the instrument: a coalesced sync had been glided as a straight segment where the spec says gaps snap, and the independent critic caught it; the smoother now snaps every track on a skipped tick, and the browser gate drives a five-tick sync to prove it. (2) The deer measurement the first version cited (93%) was arithmetic, never measured: on `deer-flee-fixture` the villager has four tiles of sight and a deer's first diagonal hop leaves it within a tick, so the render state held the deer for five ticks in fourteen and the sample series ended after six frames. `deer-flight-fixture` (a scout with twelve tiles of sight) was added for the gate, and the measured number is 91.1%. (3) Port 4173 was held by another worktree's preview server while the gate was first measured, and `reuseExistingServer` would have pointed the spec at THAT dist without a word — `playwright.config.ts` now honours `PREVIEW_PORT` so a worktree can run the browser suite on its own preview.

**What this predicts.** Any display that keys off the sim's tick-quantised position — projectiles, the minimap dot, a selection ring, a health bar — pulses the same way unless it reads the DISPLAYED root; the coordinator derives health bars and building states from `displayedEntities`, so those follow the smoothed root, and anything that later reads `getRenderState()` positions for drawing will bring the stutter back. The minimap (`src/ui/hud/minimap.ts`) does read sim positions today: at its scale a quarter-tile lag is under a pixel, so it is left as is, but a zoomed minimap would show the pulse. And any new mover on a tick-modulo clock (a future wildlife behaviour, a garrison ejection walk) needs its cadence in `displayDelayTicksFor`, or it will be drawn with the villager's four-tick delay and pulse at its own clock — the deer was exactly that case.
---

## 2026-09-02 — The idle-villager bell sat on the command bar's SELECTION label (RESOLVED v0.3.186)

**Symptom.** In every screenshot taken during the same day's placement-mode HUD fix (register entry "Entering placement mode collapsed the build palette"), the idle-villager badge — the bottom-left button showing the idle count — sat ON the command bar whenever a villager was selected: its bottom edge covered the "SELECTION" label at the top-left of the bar, at 800x600, 1280x720 and 1440x900 alike, leaving the label a smear under the badge.

**Investigation.** Reproduced headlessly first: `tests/browser/hud-floating-controls.spec.ts` boots `aoe2-prototype` paused at each viewport, selects a villager, and measures the badge's and the label's `getBoundingClientRect`. Red on v0.3.177 at all three, and red again on the merged v0.3.179 layout (main 781a7f7a's badge and toggle over the bar the placement-mode fix reworked) with the same numbers — badge [12,366 → 63,410] over label [27,399 → 255,412] at 800x600; [12,486 → 63,530] over [27,515 → 287,528] at 1280x720; [12,666 → 63,710] over [27,695 → 287,708] at 1440x900: 11–15px into the label's text and 26–30px inside the bar's top edge (384 / 500 / 680). The label overlap is a selected-state defect only; at 1280x720 and 1440x900 the badge also stood 29px inside the bottom ROW's top edge (501 / 681) with nothing selected — beside the minimap panel, which is what makes the row that tall there — over nothing, but on the row. With a villager selected the bar stands 204–208px tall; the badge was 44px tall at a fixed `bottom:190px` from the viewport, so it collided with any bar taller than 178px, which the empty bar is not and the selected bar always is.

**Root cause.** The badge (v0.3.103) and the speaker toggle above it (v0.3.109) were anchored to the VIEWPORT — `bottom:190px` and `bottom:244px` — while the bar they were meant to stand above has a height that depends on the selection and on the viewport. Nothing tied the two together: the numbers were right when chosen, and the bar has been re-laid out several times since (the v0.3.33 column-to-bar change, the 2026-08 wrap rules), each one silently moving the collision point. The one browser test of the bell asserted its count and its click, never its place.

**How it is checked from now on.** `tests/browser/hud-floating-controls.spec.ts`, at 800x600, 1280x720 and 1440x900, empty and with a villager selected: the badge does not intersect the SELECTION label and ends above the bar's top edge; and, for the CLASS, no floating `[data-hud]` element (absolute or fixed, visible) intersects the bar's heading or any other floating control, and none leaves the screen. Every assertion is soft, so one red run names every overlapping rect (the spec's header carries the full list from the merged pre-fix layout) rather than the first check that happens to fail. The fix is the smallest layout change that satisfies it: the badge measures its `bottom` from the bar's top edge every frame in the rAF loop it already ran (`src/ui/hud/idleVillagerBell.ts`, 8px gap), and the speaker toggle stacks above the badge the same way (`src/audio/mountGameAudio.ts`, 10px gap) — DOM, styling and z-order unchanged. After: badge [12,332 → 63,376] under bar top 384 at 800x600, [12,448 → 63,492] under 500 at 1280x720, [12,628 → 63,672] under 680 at 1440x900. Persisted as the LAYOUT RULE (v0.3.186) in the idle-villager-bell paragraph of `design/spec-final.md` §9.3. The full browser suite then caught the fix's own side effect: `game-hud-and-camera-camera.spec.ts` probed its wheel zoom at a fixed point 48px in from the canvas's left edge at mid-height, which at 800x600 with the Town Center selected had dodged the old toggle by 12px and lies under the relocated one — the probe now walks inward until it finds bare canvas and, failing that, names what covered each candidate, so the next control that moves fails with the culprit rather than a bare `false`.

**What the instrument taught on the way.** The red check could not honestly be run through `npm run test:browser` as it stood: port 4173 was held by a sibling worktree's `vite preview`, and `reuseExistingServer: true` would have measured THAT bundle and called it this one. `PREVIEW_PORT` now selects the port for the suite and for `scripts/captureMapScreenshot.mjs`, and both refuse a server whose served asset hashes — the JS bundle AND the extracted CSS, which the critic noted carry independent hashes, so a styles-only build must be told apart too — differ from this checkout's `dist/index.html` (`tests/browser/helpers/verifyPreviewServesThisBuild.ts` as Playwright `globalSetup`; the same check inline in the capture script). An orphaned preview that outlived its parent and kept serving whatever dist its checkout held is exactly what that check exists for — one appeared within the hour (Vite's preview reads `dist/` on every request, so an orphan serves its disk, not a cached listing — a preview started from this checkout serves each rebuild without a restart). `SELECT=villager` lets the maintained capture pair show the bar's selected state, which no boot capture could reach. The pair has a second hole the init-script pause does not close: two captures of the SAME build are byte-identical (0.00% at 1280x720 and 1440x900), but boot captures of two DIFFERENT builds freeze the minimap canvas on different frames of its first reveal — 6,132 px at 1280x720 and 5,940 at 1440x900 differing by no more than 4/255 per channel, invisible in a 3x crop, none at 800x600 — so a cross-build pair read 1.24% / 0.87% where the change accounts for 0.58% / 0.41%. Captured with `TICKS=1` (one deterministic tick after the pause, so both frames are drawn after it) the pair differs by exactly the two buttons' old and new boxes: 5,311 px at both widths, 0 in the minimap panel, 0 elsewhere; 4,888 px at 800x600, all in the button column.

**What this predicts.** Any element positioned against the viewport that is meant to sit "above" or "beside" a panel whose size is content-dependent is the same defect waiting to happen. The remaining pairs of that shape are the toast stack (fixed, top-right) against the resource bar and the debug overlay (fixed, bottom-left) against the bar; the class assertion covers only `[data-hud]`-tagged elements inside `#hud-root` that are visible at boot — the replay timeline panel is mounted there without a tag and is not covered. Known cost of anchoring to the bar (second critic, 2026-09-02): at window widths of 1120px and below the bar's height depends on the selection, so the badge follows it — 37px up when a villager is selected at 800x600 (empty [12,369 → 63,413], selected [12,332 → 63,376]) — and a second click on the badge for the round-robin must follow it; above 1120px it moves 1px. A fixed offset above the regime's tallest bar was rejected because it would float both buttons 45px above an empty bar; the cure is a bar whose height does not depend on the selection, which the "HUD holds its shape" rule already asks for and this fix does not attempt.
