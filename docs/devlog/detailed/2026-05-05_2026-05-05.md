# Devlog: 2026-05-05

## 2026-05-05T18:05:00-07:00 - Phase 2F schema-2 save format

### Action

Finished Phase 2F of the agentic automation roadmap. `SaveBlob` is now a `SaveBlobV1 | SaveBlobV2` union, current saves emit schema 2 (`seed + worldSnapshot`), schema-1 saves remain loadable, and `aoe2.pendingCommands` carries save-critical AI intentions through bootstrap, output-tail, and save-time snapshots.

### Code reviewer comments

- Codex iter-1: MEDIUM - schema-1 hydration overlaid many `sideMaps.*` slots onto stale duplicate `worldSnapshot.state` values without clearing first. Fixed by clearing migrated map slots before applying legacy side maps and adding a `productionQueues` authority regression.
- Codex iter-1: MEDIUM - `aoe2.pendingCommands` was only save-time flushed, so drained commands could remain stale in recorder-visible world snapshots. Fixed by syncing a cloned pending-command snapshot in the output tail and adding a post-load+step `world.serialize()` regression.
- Claude iter-1: unreachable - quota limit (`resets May 5, 7pm America/Los_Angeles`).
- Codex iter-2: HIGH - bootstrap did not write schema-1 `sideMaps.pendingCommands` into immediate post-load `world.serialize()` snapshots. Fixed by adding pending-command flushing to `bootstrapFlush` and a schema-1 conflicting-pending regression.
- Codex iter-3: MEDIUM - schema-2 load silently accepted missing `aoe2.matchState`. Fixed by throwing on missing `aoe2.matchState` and adding a missing-slot regression.
- Codex iter-3: MEDIUM - drift log referenced this detailed devlog before it existed. Fixed by adding this entry.
- Codex iter-4: no substantive findings after the iteration-3 fixes. It verified the schema-2 save/load paths against the live code but could not run typecheck from its read-only sandbox.
- Claude iter-4: still unreachable - quota limit (`You've hit your limit · resets May 5, 7pm (America/Los_Angeles)`).

### Result

Focused save/load, pending-command, output-tail, typecheck, lint, and file-size gates passed after the reviewer fixes. Final full gates passed before commit: `npm.cmd test` (99 files, 743 passed, 1 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`.

### Reasoning

The final Tier-1 bridge-owned slot was removed in the previous unit, so duplicating side maps in new saves was no longer necessary. Schema 2 keeps the save format aligned with replay snapshots by making `worldSnapshot.state.aoe2.*` authoritative, while the legacy path keeps existing user saves valid and explicitly resolves stale duplicate state in favor of schema-1 side maps.

### Notes

`pendingCommands` remains bridge runtime state rather than a Tier-1 codec because it drains between ticks, but it is snapshot-critical. It is therefore written as a dedicated `aoe2.pendingCommands` state slot at bootstrap, output-tail, and save-time boundaries.

## 2026-05-05T18:34:00-07:00 - Phase 2G snapshot equivalence and perf gate

### Action

Finished the Phase 2G migration invariant. `tests/replay/snapshotEquivalence.test.ts` now iterates the authoritative `TIER_1_CODECS` registry instead of a hand-maintained incremental codec list, covers every Tier-3 slot including `aoe2.pendingCommands`, and keeps the active Monk-task/unit-command snapshot regressions. Added `tests/replay/bridgeSnapshotPerf.test.ts` with bounded budgets for all-slot Tier-1 flush and schema-2 save serialization.

### Code reviewer comments

- Codex iter-1: MEDIUM - registry-driven equivalence was self-referential and could miss a removed Tier-1 slot if another codec kept the count at 35. Fixed by pinning the exact Phase 2G slot inventory before iterating `TIER_1_CODECS`.
- Codex iter-1: LOW/process - devlog/summary placeholders must not land as final docs. The final review and gate results are folded in before commit.
- Claude iter-1: unreachable - quota limit (`You've hit your limit · resets May 5, 7pm (America/Los_Angeles)`).
- Codex iter-2: no substantive findings after the exact-inventory fix.
- Claude iter-2: still unreachable - same quota limit.

### Result

Targeted Phase 2G gates passed: `npm.cmd test -- tests/replay/snapshotEquivalence.test.ts tests/replay/bridgeSnapshotPerf.test.ts`, `npm.cmd test -- tests/architecture/fileSizeBudget.test.ts tests/replay/snapshotEquivalence.test.ts tests/replay/bridgeSnapshotPerf.test.ts`, `npm.cmd run typecheck`, and `npm.cmd run lint`. Final full gates passed before commit: `npm.cmd test` (100 files, 746 passed, 1 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`.

### Reasoning

Phase 2F made schema-2 saves depend on `worldSnapshot.state`; Phase 2G makes that dependence testable at the registry level. Using `TIER_1_CODECS` directly removes the old risk that a migrated slot could be forgotten in a parallel test list, while the perf gate catches accidental all-slot flush or schema-2 serialization blowups before replay UI work leans on snapshots.

### Notes

The first Tier-3 assertion assumed pending commands were empty after one `feudal-age-fixture` tick. The red run showed the fixture can already queue an AI `queue.train` intention, so the test now asserts the slot is an array and round-trips exactly rather than assuming emptiness.

## 2026-05-05T19:36:00-07:00 - Phase 3A replay world construction and command round-trip foundation

### Action

Finished the Phase 3A/A.5 replay foundation for the agentic automation roadmap. Added `createReplayWorldOnly(snapshot)` and replay context WeakMap storage, threaded replay mode through `createWorld` / `wireBridgeOps` / `registerAllSystems`, registered command handlers for replay worlds, and added command round-trip tests that cover initial-snapshot replay, AI pending-command boundary ticks, closest-snapshot replay from a pending snapshot, and validator/result parity for the initial command stream.

### Code reviewer comments

- Codex iter-32: MAJOR - replay worlds did not handle snapshots that persisted non-empty `aoe2.pendingCommands`; advancing from such a snapshot could leave stale hydrated intentions. Fixed with `aoe2ReplayPendingCommandDrain` before replay AI systems and a pending-snapshot regression.
- Claude iter-32: unreachable - quota limit (`You've hit your limit - resets 7pm America/Los_Angeles`).
- Codex iter-33: HIGH - the drain fixed stale snapshots but replay still dropped live `aoe2.pendingCommands` when `openAt(t)` targeted an AI-decision boundary tick. Fixed by running replay-safe `prototypeAi` and `prototypeAutoAggression` with real intention emitters into the replay pending queue while recorded command payloads remain the only execution source.
- Codex iter-33: MEDIUM - validator replay docs overstated closest-snapshot byte-for-byte coverage. Fixed by scoping exact result parity to the initial-snapshot command stream and using structural equality for closest-snapshot paths.
- Codex iter-33: MEDIUM - docs still described `drainPendingCommands` as an after-step/createApp hook. Fixed PLAN/DESIGN/architecture docs to describe the actual pre-step `createSimulationBridge` boundary.
- Claude iter-33: still unreachable - same quota limit.
- Codex iter-34: unreachable - usage limit after the previous substantive Codex reviews (`try again at 8:37 PM`).
- Claude iter-34: still unreachable - same quota limit.
- Gemini iter-34 fallback: LOW - duplicated live/replay AI registration blocks in `registerAllSystems.ts`. Fixed by making only the replay drain conditional and sharing the AI/auto-aggression registrations.
- Gemini iter-34 fallback: LOW - copied replay-branch comment showed encoding noise. Fixed by removing the duplicate branch.
- Gemini iter-34 fallback: LOW - one hydration pruning loop mutated a Map while iterating it directly. Fixed by iterating over a spread like nearby loops.
- Gemini iter-35 fallback: no substantive findings. It verified the prior Codex findings as resolved, with the caveat that Gemini is a structural fallback and not equivalent to Codex/Claude live-code review.
- Gemini iter-36 fallback: no substantive findings on the final current diff after the non-semantic file-size shrink.

### Result

Focused Phase 3A/A.5 gates passed: `npm.cmd test -- tests/replay/roundTripViaCommands.test.ts tests/replay/validatorReplayConsistency.test.ts`, `npm.cmd run typecheck`, and `npm.cmd run lint`. The first full gate found a file-size-budget regression in `wireBridgeOps.ts`; fixed by shrinking that file to 599 lines without raising the legacy cap. Final full gates passed before commit: `npm.cmd test` (102 files, 750 passed, 1 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Required Codex/Claude final re-review was attempted but quota-blocked; the final fallback Gemini review found no open issues on the current diff after cleanup.

### Reasoning

Replay UI needs `SessionReplayer.openAt` to rebuild aoe2 worlds from recorded command payloads plus schema-2 snapshots. The tricky part is `aoe2.pendingCommands`: it is serialized boundary state, but recorded commands at tick `t` should only execute when replay advances to `t + 1`. Replay mode therefore clears stale hydrated pending queues before AI decisions run, lets replay-safe AI systems repopulate the queue for target-tick structural equality, and leaves actual command execution to the recorded payload stream.

### Notes

No changelog/version bump is needed for this unit because it is replay infrastructure and regression coverage rather than a user-visible UI feature. Phase 3B can now build `ReplayController` on top of a replay world that structurally matches live state at command and pending-command boundaries.

## 2026-05-05T21:52:00-07:00 - Phase 3B ReplayController foundation

### Action

Finished the Phase 3B controller layer for the replay scrubber roadmap. `src/game/replay/ReplayController.ts` now owns live/replay mode, bridge-cell swaps, coalesced scrub display state, command-driven playback, failed-tick stops, no-payload replay guards, destructure-safe methods, and prior live pause restoration. `src/game/simulation/replay/makeReplayBridge.ts` keeps scene-frame `step(delta)` non-advancing so only the controller moves replay time.

### Code reviewer comments

- Gemini impl-37 fallback: replay playback advanced by display frames instead of simulation tick duration; selection state was not preserved across scrub; interpolation alpha stayed zero; method forwarding could lose context; replay bridge seed was hardcoded. Fixed with tick-duration pacing, selection replay, alpha flushes, bound/closure forwarding, and replay seed propagation.
- Gemini impl-38 fallback: no substantive issue after impl-37 fixes; noted the `setTimeout` scheduler fallback should pass a real timestamp instead of zero. Fixed with `performance.now() ?? Date.now()`.
- Codex impl-39: MAJOR - replay entry rollback could leave live play paused if `bridgeCell.replace` threw. Fixed by restoring the captured live pause state on replacement failure.
- Codex impl-39: MAJOR - playback could advance beyond `metadata.failedTicks`. Fixed by treating failed ticks as playback upper-bound stops so the controller does not step into recorded failure boundaries.
- Codex impl-39: process/docs - detailed devlog was missing and the Phase 3B KAD was nested under the previous decision. Fixed in this docs pass and in `docs/architecture/decisions.md`.
- Codex impl-40: MAJOR - `play()` bypassed the no-command-payload guard from `SessionReplayer.openAt`. Fixed by rejecting playback for bundles without command payloads.
- Codex impl-40: MAJOR - exported controller methods depended on dynamic `this` and could break when destructured. Fixed by delegating through closure-local functions.
- Codex impl-41: MAJOR - `exitReplay()` always resumed live play with `setPaused(false)`, even when the live game had been manually paused before replay entry. Fixed by adding `ReplayControllerConfig.isLivePaused`, capturing the prior live pause state, and restoring it on exit.
- Codex impl-42: no blocking correctness findings; noted a rollback coverage gap for "pre-paused live bridge + replacement throws." Fixed with a focused regression.
- Codex impl-43: MAJOR - `exitReplay()` and committed `scrubTo()` updated controller state before `bridgeCell.replace(...)` succeeded, so replacement failure could desynchronize mode/tick from the rendered bridge. Fixed by committing state only after successful replacement and adding exit/scrub rollback regressions.
- Codex impl-43: MEDIUM - DESIGN still described the old per-frame playback loop and "exit reverses pause" semantics. Fixed by documenting accumulator playback, failed-tick/no-payload boundaries, and prior live pause restoration.
- Codex impl-43: MEDIUM - the cached playback performance contract lacked a regression. Fixed by spying on the controller's replayer and asserting playback does not call `openAt` per frame.
- Codex impl-44: MEDIUM - coalesced scrub commit rollback still left `displayedTick` on an uncommitted pending target when bridge replacement failed. Fixed by rolling pending/displayed scrub state back to the committed replay context and adding a coalesced rollback regression.
- Codex impl-44: MEDIUM - prior-pause restoration docs overstated an optional API default. Fixed by making `ReplayControllerConfig.isLivePaused` required and documenting the host callback requirement.
- Codex impl-44: MEDIUM - DESIGN promised replay-error emission even though the controller exposes only mode/tick listeners. Fixed by documenting failed ticks as upper-bound stops and step exceptions as scheduler-propagated errors.
- Codex impl-44: LOW - summary said full gates passed while post-fix full gates were still pending. Fixed the summary to reflect focused gates and pending final gates until the post-review suite runs.
- Codex impl-45: MEDIUM - replay-entry replacement failure lacked direct coverage for an originally unpaused live bridge. Fixed with an unpaused rollback regression.
- Codex impl-45: LOW - DESIGN still referenced the removed `_currentReplayContext.world` JSDoc as the post-play source of truth. Fixed the note to describe closure-local `replayContext.world`.
- Codex impl-46: MEDIUM - ADR 10 and PLAN still described the retired `_playState` / one-tick-per-animation-frame playback model. Fixed canonical Phase 3B docs to describe closure-local `replayContext`, `displayedTick`, and TPS-paced accumulation; historical iteration notes remain historical context only.
- Codex impl-47: MEDIUM - KAD-0013 still described replay entry as pausing live before replay world construction. Fixed the architecture decision to document the implemented transaction: build replay world/bridge first, pause live immediately before replacement, and restore prior pause state if replacement fails.
- Codex impl-48: MEDIUM - this devlog retained stale replay-error wording for failed ticks. Fixed it to describe failed ticks as playback upper-bound stops.
- Codex impl-49: no substantive issues. It checked the live controller, replay bridge, pause-control semantics, rollback tests, and canonical replay docs in the diff.
- Claude impl-37 through impl-49: unreachable due to the account limit (`You've hit your limit - resets 7pm America/Los_Angeles`).

### Result

Focused controller gates passed after the impl-45 fixes: `npm.cmd test -- tests/replay/ReplayController.test.ts tests/replay/ReplayController.rollback.test.ts tests/architecture/fileSizeBudget.test.ts` (3 files, 19 tests) and `npm.cmd run typecheck`. Final full gates passed after impl-49 review before commit: `npm.cmd test` (104 files, 767 passed, 1 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`.

### Reasoning

The UI-facing scrubber work needs a small, explicit controller boundary instead of letting Phaser, HUD components, or timeline widgets manipulate `SessionReplayer` directly. The controller keeps live and replay worlds separate, makes scrub and play deterministic over recorded commands, and preserves the user's live session state when entering, exiting, or failing to enter replay mode.

### Notes

No changelog/version bump is needed for this unit because it is replay infrastructure and regression coverage rather than a shipped user-visible timeline UI. Phase 3C can now target the controller API for `TimelinePanel` controls, marker/hotspot pins, and keyboard playback.

## 2026-05-05T23:25:00-07:00 - Phase 3C TimelinePanel UI

### Action

Finished the Phase 3C replay timeline layer. `src/game/replay/TimelinePanel.ts` renders the replay-only bottom strip with marker pins from `bundle.markers`, hotspot pins from `bundleHotspots(bundle, { includeMarkers: false })`, a coalesced range scrubber, play/pause, +/-1 step, exit, source label, and current/total tick. `src/game/replay/ReplayHotkeys.ts` binds Space, ArrowLeft/ArrowRight, Home/End, Escape, and Alt+T only while replay mode is active. `src/app/bootstrap/createApp.ts` mounts the panel in the HUD root, registers replay hotkeys, creates the app-level `ReplayController`, and exits replay before save-load replaces the live bridge. CSS reserves bottom viewport space while the panel is visible so the playfield and bottom HUD do not overlap it.

### Code reviewer comments

- Codex impl-50: HIGH - host `loadGame` could replace the bridge while replay mode still held the old live bridge, letting a later replay exit restore stale pre-load state. Fixed by adding `exitReplayBeforeLiveBridgeReplacement(replayController)` before save-load bridge replacement and covering it in `tests/replay/ReplayController.test.ts`.
- Codex impl-50: MEDIUM - replay hotkeys and focused timeline controls could double-handle Space/range input or suppress range-hosted replay keys incorrectly. Fixed by letting range inputs participate in hotkeys, calling `preventDefault()` for handled shortcuts, and binding replay hotkeys only while replay mode is active.
- Codex impl-50: MEDIUM - the fixed bottom panel overlapped the playfield/bottom HUD and had only jsdom coverage. Fixed with bottom-viewport reservation CSS and desktop/mobile Playwright geometry checks plus screenshots/diff artifacts under `output/playwright/`.
- Codex impl-50: MEDIUM - docs/version surfaces were stale for Phase 3C. Fixed changelog, architecture, drift log, decisions, PLAN, and devlogs in this pass.
- Claude impl-50: unreachable due to account limit (`You've hit your limit - resets 7pm America/Los_Angeles`).
- Codex impl-51: MEDIUM - mobile game-root reserve still used the desktop `78px` value while the mobile panel wraps taller. Fixed with `--timeline-panel-reserve` and a `112px` mobile reserve; desktop, mobile portrait, and mobile landscape geometry now report no overlap.
- Codex impl-51: MEDIUM - `TimelinePanel.render()` rebuilt marker/hotspot pins and rescanned `bundleHotspots(...)` every tick. Fixed by skipping render work while hidden and caching pins by `(bundle, startTick, endTick)`; added a DOM-node stability regression.
- Codex impl-51: MEDIUM - stale-bridge save-load ordering was not covered at an app/bootstrap seam. Fixed by extracting `replaceLiveBridgeAfterReplayExit(...)`, using it from `createApp.handleLoadGame`, and adding ordering tests.
- Claude impl-51: still unreachable - same quota limit.
- Codex impl-52: MEDIUM - play/step controls and hotkeys could throw on no-command-payload bundles. Fixed by disabling panel controls and pins for unreachable ticks, making replay hotkeys no-op for unreachable forward/end targets, and adding no-payload UI/hotkey regressions.
- Codex impl-52: MEDIUM - changelog validation still had the older suite counts while final gates were pending. Final validation evidence will be filled in after the post-review full suite.
- Claude impl-52: still unreachable - same quota limit.
- Codex impl-53: MEDIUM - Space pause and terminal playback could leave the panel play button stale because playback state changes did not refresh the panel after `isPlaying()` changed. Fixed by refreshing the panel after Space play/pause and emitting terminal tick changes after playback clears `playing`.
- Codex impl-53: MEDIUM - marker/hotspot pins after the capped replay range stayed enabled and scrubbed to the cap. Fixed by disabling pins outside `[startTick, replayTimelineUpperBound(metadata)]` with coverage.
- Codex impl-53: MEDIUM - narrow mobile widths below the previous visual check could exceed the `112px` reserve. Fixed by raising the mobile reserve to `156px` and adding a 320px visual check.
- Codex impl-53: MEDIUM - Phase 3C is user-visible and needed its own package/changelog version. Fixed by bumping `package.json` and `package-lock.json` to `0.1.7` and moving the Phase 3C changelog entry to `0.1.7`.
- Claude impl-53: still unreachable - same quota limit.
- Codex impl-54: MEDIUM - `HotkeyRegistry` let checkbox/radio inputs receive global hotkeys after the range-input exception. Fixed by scoping the exception to `input[type=range]` and adding checkbox/radio suppression coverage.
- Codex impl-54: MEDIUM - save-load bridge replacement exited replay before replacement bridge creation succeeded. Fixed by creating the replacement bridge first, exiting replay immediately before install, and adding failed-load transactional coverage.
- Claude impl-54: still unreachable - same quota limit.
- Codex impl-55: MEDIUM - changelog validation still reported the pre-impl-54 full-suite count while the detailed devlog said post-fix gates were pending. Fixed by rerunning post-fix full gates and updating the changelog/devlog counts consistently.
- Codex impl-55: no remaining substantive code issues in the impl-54 fixes.
- Claude impl-55: still unreachable - same quota limit.
- Codex impl-56: MEDIUM - thread-level PLAN/DESIGN still had authoritative-sounding full-`v0.1.6` scope, including Phase 3E scrubber e2e and `ReplayLoadDialog`, despite Phase 3C shipping as `0.1.7` with source loading/e2e still future work. Fixed by making the version split explicit and marking older full-`v0.1.6` sections as historical context.
- Codex impl-56: no remaining substantive code issues.
- Claude impl-56: still unreachable - same quota limit.
- Codex impl-57: MEDIUM - PLAN still pointed at `DESIGN.md` v19 while DESIGN had moved to v20 for the authoritative version-split correction. Fixed the spec reference.
- Claude impl-57: still unreachable - same quota limit.
- Codex impl-58: MEDIUM - the detailed devlog still said the impl-56 docs fix was pending final re-review after later review iterations had moved the closure point. Fixed by removing the stale pending-review sentence.
- Claude impl-58: still unreachable - same quota limit.
- Codex impl-59: MEDIUM - summary.md still said Phase 3C fixes were awaiting final re-review. Fixed the summary to state the converged review/gate result.
- Claude impl-59: still unreachable - same quota limit.
- Codex impl-60: MEDIUM - the converged Phase 3C review thread was still under `docs/threads/current/replay-scrubber/`. Fixed by moving the objective folder to `docs/threads/done/replay-scrubber/` and updating live lesson pointers to the new path.
- Claude impl-60: still unreachable - same quota limit.
- Codex impl-61: MEDIUM - the moved PLAN still described itself as active, referenced the old `docs/threads/current/replay-scrubber/DESIGN.md` path, and instructed future reviews to write under the old current thread. Fixed by marking the plan closed, pointing it at `docs/threads/done/replay-scrubber/DESIGN.md`, and telling future Phase 3D/3E work to open a fresh current objective.
- Claude impl-61: still unreachable - same quota limit.
- Codex impl-62: MEDIUM - DESIGN still said `Status: Draft v20` after the thread moved to done and PLAN was closed. Fixed by marking DESIGN closed v21 and aligning its future-work guidance with PLAN.
- Claude impl-62: still unreachable - same quota limit.
- Codex impl-63: no findings. It verified the closed thread path, DESIGN closed v21, PLAN done-thread spec reference, and absence of `docs/threads/current/replay-scrubber`.
- Claude impl-63: still unreachable - same quota limit.

### Result

Focused Phase 3C gates passed first: `npm.cmd test -- tests/replay/TimelinePanel.test.ts tests/replay/ReplayHotkeys.test.ts` (2 files, 10 tests), the app/bootstrap/controller/hotkey focused suites, `npm.cmd run typecheck`, and `npm.cmd run lint`. Full gates passed before the impl-54 review: `npm.cmd test` (107 files, 784 passed, 1 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. The impl-54 fixes passed `npm.cmd test -- tests/control/HotkeyRegistry.test.ts tests/app/replaceBridgeForLoad.test.ts` (2 files, 16 tests). Post-fix full gates passed after impl-55: `npm.cmd test` (107 files, 786 passed, 1 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Visual checks captured `output/playwright/phase3c-before.png`, `output/playwright/phase3c-after.png`, `output/playwright/phase3c-diff.png`, `output/playwright/phase3c-mobile-after.png`, `output/playwright/phase3c-mobile-landscape-after.png`, and `output/playwright/phase3c-mobile-narrow-after.png`; desktop, mobile portrait, mobile landscape, and 320px narrow mobile geometry all report no overlap with the playfield or bottom HUD. The first full-suite attempts exposed two under-sized per-test timeouts under full-suite contention; isolated assertions passed, explicit caps were raised for those existing long deterministic tests, and the follow-up full suite passed.

### Reasoning

Phase 3C should expose replay navigation through a small DOM surface without teaching the HUD or Phaser about replay internals. Keeping all controls on `ReplayController` preserves the Phase 3B boundary, while the hotkey lifecycle avoids claiming live-mode browser defaults just because replay bindings exist at app boot. The save-load guard keeps replay mode transactional with the host bridge cell: any live bridge replacement first exits replay, so the controller can no longer restore a stale captured bridge later.

### Notes

The panel is wired and visible whenever replay mode is active, but the normal user-facing replay-load dialog and full browser scrubber workflow are still Phase 3D/3E work.
