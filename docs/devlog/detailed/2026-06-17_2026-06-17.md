# Devlog 2026-06-17 → 2026-06-17

## 2026-06-17 — campaign-11 finding (a) "Houses don't count toward Feudal" — VERIFIED NOT A BUG (attempted fix reverted, no code change) [thread feudal-prereq-buildings]

- **Timestamp:** 2026-06-17
- **Action (investigation; an attempted fix was caught + reverted):** campaign-11 conformance-probe finding (a) claimed that building Houses didn't advance the Feudal 2-building prerequisite. I verified against the code (`DARK_AGE_PREREQUISITE_BUILDINGS` = {mill, lumber-camp, mining-camp, barracks} — it omits house/palisade-wall/farm) and, ASSUMING the probe's AoE2 claim ("any non-Town-Center Dark-Age building counts; two Houses alone suffice"), built a fix adding house/palisade-wall/farm, with TDD + docs + a 0.1.48 bump. **The mandatory 3-CLI review caught it as a gameplay REGRESSION before commit.**
- **What the review found:** **Codex (gpt-5.5 xhigh)** — no HIGH; it ACCEPTED the core rule and flagged only doc nits (a stale Dark-Age menu list in spec §7.3; and that my §7.2 generalization over-broadened — the Feudal/Castle prerequisite sets exclude watch-tower/stone-wall). **Claude (opus[1m] max) — HIGH [H1]: the core rule is factually WRONG per AoE2.** With fetched Liquipedia + Fandom quotes: advancing Dark→Feudal needs 2 of {Barracks, Dock, Lumber Camp, Mill, Mining Camp}; **Houses, Farms, and Walls do NOT count.** The original {mill, lumber-camp, mining-camp, barracks} was already AoE2-correct (the qualifying five minus Dock, which this land-only slice lacks). The campaign-11 finding (a) was a probe MISDIAGNOSIS. **Gemini** — 429-throttled, emitted no review (the authoritative wiki was decisive regardless).
- **Verification (the deciding step — did NOT just trust the reviewer):** per the just-written "verify reviewer claims" lesson, I fetched the authoritative AoE2 sources MYSELF. Fandom (`ageofempires.fandom.com/wiki/Feudal_Age_(Age_of_Empires_II)`): "Two of the following buildings: Barracks, Dock, Lumber Camp, Mill, Mining Camp" and "Houses, Farms, and Walls … do not count." A cross-check web search agreed ("any two Dark Age buildings except Houses, Farms, walls, and Outposts"). So Claude's H1 is CORRECT; the probe + my AoE2 assumption were WRONG.
- **Disposition:** REVERTED the attempted fix entirely (`git checkout HEAD --` on `prototypeBuildingRules.ts` + the 4 test files + `spec-final.md` §7.2/§15 + changelog 0.1.48 + `package.json` + summary). NO code change; the rule stays the AoE2-correct {mill, lumber-camp, mining-camp, barracks}. This commit is DOCS-ONLY: the roadmap reclassifies finding (a) as not-a-bug, this entry, the thread (`done/feudal-prereq-buildings/`, incl. the REVIEW.md), and a `lessons.md` entry. No version bump, no changelog.
- **Result:** working tree is back at the v0.1.47 rule; full suite unchanged at **1478/2** (the reverted files match HEAD exactly). The 3-CLI review process did its job — a conformance-probe-driven would-be gameplay regression was caught + verified-out before it shipped.
- **Reasoning / lesson:** an LLM conformance probe's GAME-RULE claim ("Houses count") is a CLAIM about AoE2, not ground truth. Code-reading confirmed the probe's claim about the Set's CONTENTS but NOT the rule's CORRECTNESS — the decisive check was the authoritative wiki. Lesson recorded (`docs/learning/lessons.md`): verify external game-rule facts against the wiki, not the probe or memory; and the mandatory 3-CLI review (a reviewer instructed to verify AoE2 fidelity with sources) is what surfaced it.
- **Notes:** remaining campaign-11 findings: (b) accepted-vs-executed UX signal, (c) AI age-up prioritization. When naval is added, `dock` joins the Feudal prerequisite set — the only AoE2-qualifying Dark-Age building this land-only slice currently lacks.

## 2026-06-17 — CI runs only on version-bump pushes (infra; commit aeea894)

- **Action:** GitHub Actions has been billing-blocked (runs die in 3-13s with "the job was not started because recent account payments have failed or your spending limit needs to be increased" — USER ACTION required: GitHub Settings → Billing & plans). To cut Actions usage, added a `paths: [package.json]` filter to the push trigger of `.github/workflows/ci.yml` + `playtest.yml`, so they run only on version-bump (or dependency) pushes; refactor/doc/CI-only pushes skip CI. Pull requests still run the full gates; the nightly `playtest-corpus-llm` (schedule, not push) is untouched. No version bump (infra config, no observable game behavior). Committed separately (aeea894). After billing is restored, re-check the engine-dist tarball fetch (the other historical CI breaker).
