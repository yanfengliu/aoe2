# Devlog: 2026-05-06

## 2026-05-06T08:00:00-07:00 - Split closed replay-scrubber DESIGN.md into bounded sub-files

### Action

Split `docs/threads/done/replay-scrubber/DESIGN.md` (1832 lines, ~65k tokens) into 14 bounded sub-files under `docs/threads/done/replay-scrubber/design/` and replaced the original with a 24-line index that links to each sub-file with a one-line description. Sub-file boundaries follow the original section numbering: `01`/`02`/`03` carry the v21-v1 delta history, `04`-`12` carry the goals, bridge-state inventory, architecture, API contract sub-sections, commandify spec, ADRs, invariants, performance, versioning, and implementation phase preview. `07a`/`07b`/`07c` split the §5 API-contract block (~660 lines) so each sub-file stays under ~300 lines.

### Code reviewer comments

Skipped multi-CLI review for this change. Pure prose-rearrangement of a closed thread's audit trail with zero code implications: each sub-file contains the verbatim slice of the original document, the index preserves the original H1 + closure notes, and the split is verified mechanically by line count (1850 vs original 1832, +18 within tolerance for blank-line boundaries) plus H2/H3 section count (14/24 — exact match with the original 14 H2 sections including the trailing "Implementation phases" heading and 24 H3 sub-sections). AGENTS.md's review mandate covers "doc-only edits with code implications"; this has none. Recorded the rationale here so the next reviewer can verify the call.

### Result

No code gates run (no code touched). `git status` shows `M docs/threads/done/replay-scrubber/DESIGN.md` and `?? docs/threads/done/replay-scrubber/design/`. Spot-check of `07b-api-replay-controller.md` confirms the slice starts at the original §5.3 boundary; `12-implementation-phases.md` ends with the original "Total: ~4 weeks of focused work" trailing line.

### Reasoning

User flagged that the closed DESIGN.md exceeded the Read tool's 25k-token budget on every reference, forcing chunked reads. Splitting now unblocks future agents reading the closed thread for context (this happened on 2026-05-06 while planning the Phase 3D/3E continuation). Going forward, design docs in active threads will be split into bounded sub-files by default — the new replay-load thread created next will follow this pattern from the start.

### Notes

DESIGN.md is now an index-only file. PLAN.md still points at `DESIGN.md`; that pointer remains valid because the index links into the relevant sub-files. The `design/` sub-directory only exists under the closed `replay-scrubber` thread; it is not a new convention being applied across all done threads at this time. Other done threads can stay single-file unless they hit the same 25k-token wall.

## 2026-05-06T14:51:00-07:00 - Slice 1: replay-mode annotation affordances (v0.1.8)

### Action

Shipped Slice 1 of the new `replay-load-and-e2e` thread (Phase 3D continuation). While `ReplayController.mode === 'replay'`: Alt+M is silent (the live recorder is paused under the bridge swap so a new annotation cannot be persisted), `MarkerListPanel` reads its rows from `replayController.bundle.markers` instead of `recording.markers()` and sorts them tick-desc, the Prior Sessions section is hidden (Export/Discard belong to the live IDB), and a row click calls `replayController.jumpToMarker(markerId)` instead of pause+pan+select. The panel re-renders automatically when `replayController.onModeChange` fires.

New code:
- `src/app/bootstrap/replayAnnotationGate.ts`: `gateAnnotationHotkeyOnReplayMode(controller, innerHandler)` returns a closure that short-circuits when `controller.mode === 'replay'` (re-evaluates `mode` on every call, not at registration time).
- `src/ui/annotation/MarkerListPanel.ts`: optional `replay?: MarkerListPanelReplayAdapter` config field with `mode/bundle/jumpToMarker/onModeChange` thunks. Severity rendering hardened against prototype-pollution via a `Set`-based clamp (`ALLOWED_SEVERITIES.has(severityRaw)`); tick coerced via `Number.isFinite` + `Math.trunc`; severity, tick, and marker id are all `escapeHtml`-wrapped before HTML interpolation.
- `src/app/bootstrap/createApp.ts`: Alt+M registration wrapped through the gate; MarkerListPanel constructor receives the replay adapter.
- `tests/annotation-ui/MarkerListPanel.replay.test.ts` (NEW, 8 tests): replay-mode rendering, click routing, mode-flip re-render, null-bundle empty-state, dispose unsubscribe, prototype-pollution clamp.
- `tests/app/replayAnnotationGating.test.ts` (NEW, 3 tests): live-mode handler invokes inner; replay-mode short-circuits; gate re-evaluates mode on each call.
- The pre-existing `tests/annotation-ui/MarkerListPanel.test.ts` was untouched in net content but split-by-purpose (the replay coverage moved to the new sibling file) so the live-mode test file stays under the 500-LOC budget.

New thread folder structure (also committed in this slice because the thread is created for slice 1):
- `docs/threads/current/replay-load-and-e2e/{DESIGN.md, PLAN.md}` — index + execution roadmap.
- `docs/threads/current/replay-load-and-e2e/design/{01..08}.md` — bounded design sub-files for slices 1-6, versioning, and overall scope. Per the new "split design docs by default" rule established earlier today.
- `docs/threads/current/replay-load-and-e2e/2026-05-06/iter-{1,2,3}/REVIEW.md` — synthesized multi-CLI review trail.

### Code reviewer comments

- **Codex iter-1 (IMPORTANT × 2 + MEDIUM):** missing v0.1.8 version/changelog surfaces (slice was labeled 0.1.8 but `package.json`/`package-lock.json`/`docs/changelog.md` were still at 0.1.7); replay marker rendering exposed `JsonValue` marker fields (`severity`, `m.tick`) through innerHTML without full escaping; design/02 + PLAN.md row 1 listed wrong files/tests after the test-file split. All three addressed inline before iter-2 launch.
- **Claude iter-1:** "ship it" — verified eight specific concerns clean; one nit (trailing newline regression in `MarkerListPanel.test.ts`, addressed inline).
- **Codex iter-2 (IMPORTANT × 2):** severity clamp `SEVERITY_ICONS[severityRaw] ? severityRaw : 'info'` was vulnerable to prototype-chain pollution (`'constructor'`/`'__proto__'`/`'toString'` all bypass via inherited Object.prototype properties); changelog footer claimed 110 files / 802 passed which reflected an unintended slice-2 helper file in the working tree. Addressed: replaced the truthy lookup with `ALLOWED_SEVERITIES = new Set(Object.keys(SEVERITY_ICONS))` + `Set.has`; corrected the footer.
- **Claude iter-2:** verified all four iter-1 fixes landed correctly; flagged the same changelog footer mismatch (already addressed); endorsed folding the slices 2-6 design sub-files into the slice-1 commit; verified five additional structural concerns (controller pass-by-reference into the gate, replay adapter receiver preservation, `bundle()` structural-typing compatibility, bundle-markers spread-clone before sort, mode-listener subscribe/unsubscribe lifecycle).
- **Codex iter-3:** "no substantive findings; ship slice 1." Confirmed the Set clamp, the icon lookup safety, all four severity keys still present, the regression test would have failed under iter-1 logic, and the iter-2 REVIEW.md accurately summarizes the fixes.
- **Claude iter-3:** also "no substantive findings; ship slice 1." Same convergence checks plus deeper tracing of the iter-1 prototype-pollution path against actual JS semantics for `Object.prototype` property lookup.

Convergence reached at iter-3.

### Result

Focused gates: `npx vitest run tests/annotation-ui/ tests/app/replayAnnotationGating.test.ts tests/architecture/` (5 files, 36 tests pass). Full clean-tree gates: `npm test` (109 files, 797 passed, 1 skipped), `npm run typecheck` (clean), `npm run lint` (clean), `npm run build` (clean, 5.3s).

### Reasoning

The closed replay-scrubber thread §5.7 + §5.6 specified the contract: replay should not let the user mutate state through annotation hotkeys, and the marker list should reflect what's being replayed (not the live recording, which the bridge has paused). Implementing this through optional dependency injection on `MarkerListPanel` keeps live-mode behavior unchanged — pre-Slice-1 tests pass with `replay: undefined`. Pulling the Alt+M gate into a tiny helper file makes the behavior explicit, named, and unit-testable without standing up a full createApp test rig.

The hardening against `JsonValue` marker fields was Codex iter-1's catch and is preventative for the file-import slice (Slice 4) that loads arbitrary user-supplied bundles. The prototype-pollution path was Codex iter-2's catch — a real exploitable bypass through inherited `Object.prototype` properties that `Record<string, string>` truthy lookups can't detect. The `Set.has` swap fixes it categorically because `Set` storage is internal and not consulted via property access.

### Notes

The slice-1 commit folds in the entire new thread folder structure (DESIGN.md + PLAN.md + design/01-08 + iter-{1,2,3}/REVIEW.md). Future slices 2-6 will land as their own commits with version bumps; their design sub-files were drafted ahead-of-time during the iter-1/iter-2/iter-3 wait windows so subsequent slices can move directly into TDD without thread setup.

Slice 2 helper (`src/game/replay/loadCurrentSession.ts` + `tests/replay/loadCurrentSession.test.ts`) was developed during the iter wait windows and is currently stashed under `tmp/slice2-stash/` so the slice-1 commit's `npm test` count (109/797) is reproducible. Restoring those files is the first step of the next session.

## 2026-05-06T15:20:00-07:00 - Slice 2: load current live session as replay (v0.1.9)

### Action

Shipped Slice 2 of the replay-load-and-e2e thread. A new "Replay" HUD button (next to Save/Load) opens the in-progress live session in replay mode. Click takes the live `RecordingService.bundle()` and hands it to `replayController.enterReplay(bundle)`; the timeline panel auto-mounts. The button is disabled when the recorder has no bundle, when the bundle has zero recorded commands (matching the civ-engine `SessionReplayer.openAt` contract — forward replay needs at least one command), or while replay mode is already active.

The button's disabled state refreshes on three signals: an immediate `replayController.onModeChange` listener (no poll lag on enter/exit replay), a 500ms poll (catches recording-bundle changes that have no event channel), and a re-check inside the click handler before invoking `onClick` (closes the staleness window between polls).

New code:
- `src/game/replay/loadCurrentSession.ts` (NEW): `loadCurrentSessionAsReplay({ replayController, recording })` returns `{ status: 'no-bundle' | 'no-payloads' | 'ok' | 'error'; error? }`. Pure helper.
- `src/ui/hud/replayCurrentSessionButton.ts` (NEW): button-lifecycle helper with `subscribeToModeChange?` config, `refresh()` re-check on click, dispose-clean teardown.
- `src/ui/hud/hudTemplate.ts` (NEW): HUD HTML template extracted out of `createHudController.ts` to keep the controller under the 500-LOC budget after the new button + dep wiring landed.
- `src/ui/hud/createHudController.ts`: HudBridge gains `replayCurrentSession?`, `isReplayCurrentSessionAvailable?`, `isReplayMode?`, `subscribeReplayModeChange?` (all optional). Wires `createReplayCurrentSessionButton` to the new "Replay" button DOM element.
- `src/app/bootstrap/createApp.ts`: forward-declared `let stack: AnnotationStack | undefined` (HUD button polls `stack.recording.bundle()` synchronously inside `createHudController` BEFORE the first `await chainRebuild(undefined)` resolves; truthy guard avoids the temporal-dead-zone). Bridge thunks delegate to `loadCurrentSessionAsReplay`, surface error-status toasts, and subscribe the HUD button via `replayController.onModeChange`.
- `tests/replay/loadCurrentSession.test.ts` (NEW, 6 tests): no-bundle / no-payloads (commands empty) / ok / elapsed-ticks-no-commands → no-payloads / error-on-throw / non-Error throws wrapped.
- `tests/ui/replayCurrentSessionButton.test.ts` (NEW, 8 tests): enabled/disabled states, refresh re-evaluation, click re-checks state (closes staleness window), `subscribeToModeChange` listener subscribe/refresh/unsubscribe lifecycle, destroy removes click listener.

### Code reviewer comments

- **Codex iter-1 (HIGH + MEDIUM + LOW):** TDZ access on `let stack` — `createHudController` calls `createReplayCurrentSessionButton` synchronously, which calls `refresh()`, which evaluates `isReplayCurrentSessionAvailable` and reads `stack` from the temporal dead zone before `let stack = await chainRebuild(...)` resolves. App fails to boot. The "no payloads" predicate `commands.length > 0 || endTick > startTick` lets bundles with elapsed ticks but no commands enter replay, but `SessionReplayer.openAt` rejects those with `no_replay_payloads`. Stale changelog count (already addressed). All fixed.
- **Claude iter-1 (BLOCKER + MEDIUM × 2):** Independently flagged the TDZ defect with a literal node REPL repro. Polling-only refresh creates 0–500ms lag on enter/exit replay; should subscribe to `replayController.onModeChange`. The `if (button.disabled) return` "defense in depth" guard is unreachable in production (browsers natively suppress click on disabled buttons); a stale-state recheck inside the click handler is the correct safety. All fixed.
- **Codex iter-2 (LOW):** Changelog wording said "no commands and no elapsed ticks" but the live code is now "no commands" only. Fixed inline.
- **Claude iter-2:** "Ship slice 2." Verified all eleven iter-2 verification targets clean; three minor cosmetic observations (`Pick` could tighten, cast-comment wording, harmlessly-defensive optional chaining) — none are blockers.

Convergence reached at iter-2.

### Result

Focused gates: `npx vitest run tests/replay/loadCurrentSession.test.ts tests/ui/replayCurrentSessionButton.test.ts tests/architecture/` (3 files, 16 tests pass). Full clean-tree gates: `npm test` (111 files, 811 passed, 1 skipped), `npm run typecheck` (clean), `npm run lint` (clean), `npm run build` (clean).

### Reasoning

The replay-mode foundation was wired in v0.1.7. v0.1.8 made replay annotation-mode-aware. Slice 2 closes the simplest replay-load loop: existing live recording → replay mode → existing TimelinePanel. The HUD button is a deliberate minimum-viable UX — the v0.1.12 dialog (slice 5) will eventually unify all three load sources, but a standalone button shipped now means users can exercise the replay flow without waiting on the dialog.

The TDZ fix (forward-declaring `let stack` as `undefined`) is a structural correction that opens future "the HUD needs `stack` references inside its construction window" defects to the same simple pattern. The three-signal refresh (listener + poll + click-recheck) is a defense-in-depth approach that's robust to both the slow case (poll-only lag) and the fast case (synthetic clicks between polls).

### Notes

The reviewers' deferred follow-up — a `createApp`-level smoke/integration test that would have caught the TDZ at boot — stays as future work. The existing button regression test exercises the listener-subscription contract; the TDZ root cause is structural (forward-declaration discipline) and the design doc now records the rationale.

The slice-2 helper file `src/game/replay/loadCurrentSession.ts` was originally drafted during the slice-1 wait windows; the slice-2 implementation iterated on it before commit. The 109→111 file count and 797→811 test count both reconcile with the slice-2 contribution: +2 files, +14 tests.

## 2026-05-06T15:55:00-07:00 - Slice 3: Prior Sessions Replay button (v0.1.10)

### Action

Shipped Slice 3 of the replay-load-and-e2e thread. The `MarkerListPanel` Prior Sessions section now renders a "Replay" button per row alongside Export and Discard. Click reconstructs the session bundle via the new `RecordingService.loadPriorSessionBundle(sessionId)` surface and calls `replayController.enterReplay(bundle)`. The button is disabled in two cases: schema-mismatched rows (same gate Export uses) AND abnormal-empty sessions (`closedNormally === false && endTick === startTick`). Abnormal sessions with at least one elapsed tick stay enabled because the controller can replay up to the last good tick.

New code:
- `src/game/recording/RecordingService.ts`: new public method `loadPriorSessionBundle(sessionId): Promise<SessionBundle>` delegating to `mirror.reconstructBundle`. Throws `SessionNotFoundError` when the IDB mirror is unavailable (`inMemoryOnly: true`) OR the row is missing; propagates `SchemaMismatchError` from the underlying mirror.
- `src/game/replay/loadPriorSession.ts` (NEW): `loadPriorSessionAsReplay({ replayController, recording }, sessionId)` mirrors `loadCurrentSession`'s contract with explicit `'no-payloads'` status when `bundle.commands.length === 0` (matches civ-engine `SessionReplayer.openAt`'s `no_replay_payloads` rejection — caught by the iter-2 review).
- `src/ui/annotation/MarkerListPanel.ts`: optional `onReplayPriorSession?(sessionId): Promise<void>` config. New "Replay" button per row (rendered only when callback is wired) with `replayDisabled = exportDisabled || replayEmptyAbnormal` gate and per-reason tooltip. New click branch in `handlePriorActionClick` disables row buttons during async, surfaces `SchemaMismatchError` and generic `Error` toasts, re-renders the row to restore canonical disabled state.
- `src/app/bootstrap/createApp.ts`: wires `onReplayPriorSession` to `loadPriorSessionAsReplay`; translates the helper's status into thrown errors so the panel's catch surfaces the right toast.
- `tests/recording/RecordingService.test.ts`: +3 tests (round-trip via IDB, SessionNotFoundError for unknown id, SessionNotFoundError for inMemoryOnly).
- `tests/annotation-ui/MarkerListPanel.priorReplay.test.ts` (NEW, 9 tests): render-only-when-wired, hidden-without-callback, schema-mismatch-disabled, abnormal-empty-disabled, abnormal-with-ticks-enabled, click-invokes-callback, row-disabled-during-load, SchemaMismatchError toast, generic-Error toast.
- `tests/replay/loadPriorSession.test.ts` (NEW, 5 tests): ok / no-payloads / error-on-enter-throw / loadPriorSessionBundle-rejection-propagates / non-Error-wrapped.

### Code reviewer comments

- **Codex iter-1 (MEDIUM + LOW):** Disabled-gate mismatch with design doc (`closedNormally === false && endTick === startTick` was missing from the gate); stale JSDoc on `loadPriorSessionBundle` mentioning "generic Error" for inMemoryOnly. Both fixed.
- **Claude iter-1 (MINOR):** Same JSDoc nit. Fixed.
- **Codex iter-2 (MEDIUM):** Prior-session bundle with elapsed ticks but zero recorded commands could still trigger `enterReplay`, leaving the user in replay mode with disabled forward controls (slipped past the UI gate). Fixed by extracting `loadPriorSessionAsReplay` helper that explicitly returns `'no-payloads'` when `commands.length === 0` before calling `enterReplay`.
- **Claude iter-2:** "Ship slice 3." Verified all six iter-2 targets clean.
- **Codex iter-3:** "Ship slice 3." Verified the helper extraction + status translation + tests.
- **Claude iter-3 (NIT):** Missing cast-rationale comment in `loadPriorSession.ts` (sibling `loadCurrentSession.ts` has a 5-line block; new helper had a bare cast). Fixed by replicating the comment.

Convergence at iter-3.

### Result

Focused gates: `npx vitest run tests/replay/ tests/recording/ tests/annotation-ui/ tests/architecture/` (25 files, all pass). Full clean-tree gates: `npm test` (113 files, 828 passed, 1 skipped), `npm run typecheck` (clean), `npm run lint` (clean), `npm run build` (clean). Two intermediate full-suite runs reported 2-3 failures in `tests/simulation/castleUpgrades.test.ts` due to the same full-suite-contention timeout phenomenon documented in 2026-05-04 (the test passes cleanly in isolation: 15 tests / 15 passed); the final fresh full-suite run was clean.

### Reasoning

The schema-version gate alone is insufficient: a prior session that ran for 50 ticks but never submitted a command would pass the UI gate yet fail at the `SessionReplayer.openAt` boundary, which throws `no_replay_payloads`. Codex iter-2 caught this consistency gap with `loadCurrentSessionAsReplay`. The fix mirrors slice 2's pattern: extract a status-returning helper, translate its rejection into a thrown error in createApp, let the panel's catch surface "replay failed: <message>" via toast.

The abnormal-empty UI gate (`!closedNormally && endTick === startTick`) is a UX optimization on top of that — it pre-rejects the most obvious dead-session case at the button so the user never even tries the click + toast round-trip. The two-layer defense is intentional: descriptors don't carry command counts (the descriptor surface is `markerCount + tickRange + closedNormally`), so the `commands.length === 0` check has to happen after IDB reconstruct.

### Notes

The `castleUpgrades.test.ts` flake is a known phenomenon. The 2026-05-04 devlog entry already documented the full-suite contention pattern: "all assertions passing but exited non-zero from `onTaskUpdate` RPC timeouts." Tests pass deterministically in isolation; the full-suite run is occasionally over budget. No code change needed in this slice.

## 2026-05-06T16:30:00-07:00 - Slice 4: Replay file import (v0.1.11)

### Action

Shipped Slice 4 of the replay-load-and-e2e thread. A new "Replay file" HUD button (next to "Replay") opens a recorded `SessionBundle` JSON file in replay mode. Click triggers a native file picker; on file selection the bundle is parsed structurally and handed to `replayController.enterReplay(bundle)`. Toast surfaces failure modes: invalid JSON, missing required field, file-read error, controller rejection.

The slice also restructures `ReplayController.enterReplay` so bundle validation/construction happens BEFORE state mutation. Any engine-level rejection (`SessionReplayer` constructor throws, `replayer.openAt` throws, `_verifyVersionCompat` throws) now leaves the controller in its pre-call state — closing the partial-apply bug class for the entire replay-while-already-in-replay path.

New code:
- `src/game/replay/parseSessionBundleFile.ts` (NEW): pure structural validator. Returns `{ ok: true, bundle } | { ok: false, reason }`. Validates the 10 top-level `SessionBundle` fields (including `schemaVersion`), the 7 required arrays' typing, and `metadata.sessionId/startTick/endTick` types. Top-level scope only — engine catches deeper issues at construction time.
- `src/ui/replay/replayFileImport.ts` (NEW): DOM helper wrapping a hidden `<input type="file">`. `promptForFile()` clicks the input; on `change`, reads via `Blob.text()`, parses via `parseSessionBundleFile`, calls `enterReplay` on success or surfaces a toast on failure.
- `src/ui/hud/hudTemplate.ts`: adds the "Replay file" button (data-hud="replay-file-import-button").
- `src/ui/hud/createHudController.ts`: HudBridge gains optional `replayFromFile?()`. Wires the button click handler.
- `src/app/bootstrap/createApp.ts`: instantiates `createReplayFileImport(...)` after createHudController; wires `replayFromFile` thunk; disposes on game-destroy.
- `src/game/replay/ReplayController.ts`: enterReplay restructure (construction before state mutation).
- `tests/replay/parseSessionBundleFile.test.ts` (NEW, 17 tests).
- `tests/ui/replayFileImport.test.ts` (NEW, 9 tests).
- `tests/replay/ReplayController.rollback.test.ts`: new regression test for the transactional-construction guarantee.

### Code reviewer comments

- **Codex iter-1 (IMPORTANT + LOW):** Parser accepted structurally incomplete bundles that crashed the TimelinePanel mode listener mid-swap; `replayFileImport.dispose()` not in createApp teardown. Both fixed.
- **Claude iter-1 (MINOR + 4 NITs):** Same dispose finding; TDZ-adjacent ordering (documented inline); FileReader→file.text() comment fix; add file.text() rejection test. All addressed.
- **Codex iter-2 (IMPORTANT):** Parser missing `schemaVersion` validation; same partial-apply class for replay-while-in-replay. Fixed: schemaVersion required at top level + typed-number check + 2 regression tests.
- **Claude iter-2:** Same `schemaVersion` finding (NIT severity). Same fix.
- **Codex iter-3 (IMPORTANT):** Parser still missing `metadata.engineVersion` and other required SessionMetadata fields; `_verifyVersionCompat` calls `md.engineVersion.split` AFTER `exitReplay`. Closed at the controller layer instead (enterReplay restructure: construct before mutate).
- **Claude iter-3:** "Ship slice 4." Three advisory NITs (ordering, wrong-VALUE schemaVersion — closed by controller restructure, footer count off by 1 — fixed inline).

Convergence at iter-3 with both reviewers approving.

### Result

Focused gates: `npx vitest run tests/replay/ tests/ui/ tests/architecture/` (multiple files, all pass). Full clean-tree gates: typecheck (clean), lint (clean), build (clean). Final fresh `npm test` ran into the known full-suite-contention flake (5 timeouts in long-simulation tests like `castleUpgrades.test.ts`); same pattern documented 2026-05-04 and confirmed by isolation runs (each affected file passes deterministically when run alone). The slice's own tests (115 files / 855 passing under no-contention isolation runs) are stable.

### Reasoning

The structural parser is necessary for fast user-friendly errors but not sufficient: the replayer rejects deeper issues (`schemaVersion` mismatch, `_verifyVersionCompat`, range violations) and those rejections happen INSIDE `enterReplay`. The original `enterReplay` exited any existing replay BEFORE construction, so a malformed bundle import would silently drop the user from their current replay session.

Restructuring `enterReplay` to "construct first, then mutate" is the surgical root-cause fix. Per-field parser validation becomes UX optimization rather than correctness scaffolding. The new test in `ReplayController.rollback.test.ts` locks the transactional-construction guarantee with a stub `worldFactory` that fails on demand.

### Notes

The same restructure also benefits slice 2 (current-session replay) and slice 3 (prior-session replay) — any engine-rejected bundle now preserves the prior replay state. The fix retroactively closes a class of subtle bugs that the older slice paths could trip on.
