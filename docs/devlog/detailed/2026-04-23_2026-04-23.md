## [2026-04-23 00:36, America/Los_Angeles] - Final precise-selection verification, topmost overlap fix, and close-out

**Action:** Closed the precise-selection/browser-spec-split task by writing temporary prior-round review notes under `code_review/`, running another Codex/Gemini/Claude verification round with those concerns plus `docs/learning/lessons.md` in scope, and then investigating Gemini's late finding that same-layer point-hit tiebreaks were still walking render order in the wrong direction. Confirmed the scene draws `displayedEntities` in array order, so later entries are visually on top, then updated `tests/phaser/entityHitTest.test.ts` first to lock the visible-topmost contract for generic point hits, command targeting, and same-owner overlaps. After that, flipped the same-layer fallback in `src/phaser/scenes/entityHitTest.ts` from `left.index - right.index` to `right.index - left.index` everywhere render-order resolution is supposed to mean "the thing drawn last wins." Re-ran only the affected tests during iteration, repeated reviewer verification after the fix, and then ran the full suite.

**Code reviewer comments:**
- **Codex**
  - **Design:** Final verification found no actionable issues after the last overlap-z-order fix. It agreed the exact-selection / padded-command split, click-cell repeat memory, and shared preview/final selection filtering all remained intact.
  - **Test coverage:** Final verification explicitly pointed to the new same-owner / command-target overlap assertions in `tests/phaser/entityHitTest.test.ts` and the browser regressions in `tests/browser/game-selection.spec.ts` as sufficient coverage for the user-facing interaction contract.
  - **Correctness:** No actionable issues after the latest hit-test ordering change.
  - **Clean code / typing / efficiency / memory leaks:** No actionable issues.
  - **Documentation:** No blocker; the lesson/devlog trail was in place.
- **Gemini**
  - **Design:** Late verification correctly caught the one remaining real bug: same-layer point hits were still preferring lower render indexes, which meant precise clicks could target the underneath entity instead of the one visibly on top. Addressed by reversing the index fallback in `entityHitTest.ts`.
  - **Test coverage:** The same review implied the regression expectations should move with the behavior. Addressed by flipping the overlap fixture to prove "human-owned wins exact selection while the visibly topmost unit wins generic/command targeting" and by adding the direct command-target assertion.
  - **Correctness:** Final verification after the fix reported no actionable issues.
  - **Clean code / typing / efficiency / memory leaks:** Final verification reported no actionable issues.
  - **Documentation:** Final verification reported no actionable issues.
- **Claude**
  - **Design:** Final verification found no actionable issues and explicitly confirmed that the prior eight review themes stayed addressed after the last z-order fix.
  - **Test coverage:** Final verification said the new low-level overlap tests plus the existing browser selection coverage were enough for the latest behavior change.
  - **Correctness:** No actionable issues.
  - **Clean code / typing / efficiency / memory leaks:** Only optional nits remained, not worth addressing in this task.
  - **Documentation:** No blocker.

**Result:** Unit clicking and drag-box selection now use the visible rendered body end-to-end, marquee drags preview the exact units that will be selected on mouse-up, same-layer overlap hits resolve to the visibly topmost entity when render order is the deciding factor, context-command targeting keeps its own forgiving padded helper for moving targets, and the old monolithic browser smoke file is split into focused feature specs that are easier to iterate on in isolation.

**Reasoning:** Pixel-perfect geometry by itself was not enough. Once hit areas were exact, the last source of "wrong unit" clicks was the fallback ordering among overlapping entities. If the tie-break does not match what the player sees on top, the interaction still feels imprecise even when the shape math is correct.

**Notes:** Temporary `code_review/*.md` notes and `code_review/codex_latest_verification.txt` were created to carry prior review context into the verification round and were deleted after summarizing the reviewer feedback here. Iteration validation: `npx.cmd vitest run tests/phaser/entityHitTest.test.ts` passed (**11** tests), `npx.cmd playwright test tests/browser/game-selection.spec.ts` passed (**10** tests), and `npx.cmd playwright test tests/browser/game-combat-and-meta.spec.ts` passed (**12** tests). Final validation: `npm.cmd test` reached **34/34** files and **321/321** tests passing but still exits non-zero because of the pre-existing **5** `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors; `npm.cmd run typecheck`, `npm.cmd run build`, `npm.cmd run lint`, and `npm.cmd run test:browser` passed (`76` browser tests, including the production build in its pretest). Left alone as instructed: `cavalry-icon-draft.html`, `src/drafts/`, and local `AGENTS.md` edits.

## [2026-04-23 00:47, America/Los_Angeles] - Engine-feedback current doc format cleanup

**Action:** Reworked `docs/engine-feedback/current.md` from a prose-heavy status note into a tighter live-status format with compact bullet lists under verdict, current asks, and recommendation. Kept the underlying content unchanged: the engine is still viable, there are still no active engine asks, and the earlier occupancy/crowding concerns remain historical-only in `past.md`.

**Code reviewer comments:**
- **Codex**
  - **Design:** No actionable issues on the `docs/engine-feedback/current.md` reformat itself.
  - **Test coverage:** Not applicable for this docs-only cleanup.
  - **Correctness:** No factual problems found in the engine-feedback content.
  - **Clean code / typing / efficiency / memory leaks:** Not applicable for this docs-only cleanup.
  - **Documentation:** Correctly flagged that `docs/devlog/summary.md` claimed review was complete while this detailed entry still said review was pending. Addressed by replacing the placeholder review block with the actual review outcomes.
- **Gemini**
  - **Design:** No actionable issues found.
  - **Test coverage:** Not applicable for this docs-only cleanup.
  - **Correctness:** No actionable issues found.
  - **Clean code / typing / efficiency / memory leaks:** Not applicable for this docs-only cleanup.
  - **Documentation:** No actionable issues found.
- **Claude**
  - **Design:** No actionable issues on the engine-feedback reformat itself.
  - **Test coverage:** Not applicable for this docs-only cleanup.
  - **Correctness:** No factual problems found in the engine-feedback content.
  - **Clean code / typing / efficiency / memory leaks:** Not applicable for this docs-only cleanup.
  - **Documentation:** Matched Codex's finding that this entry still contained placeholder "Pending final review" text after the summary had already claimed review completion. Addressed by backfilling the real reviewer outcomes here.

**Result:** `docs/engine-feedback/current.md` now reads like a live status doc instead of a mini essay, which makes the current engine verdict and the "no active asks" state much easier to scan.

**Reasoning:** The old content was accurate, but the formatting buried the actual status. This file works better as a concise operational summary because `past.md` already owns the longer historical context.

**Notes:** Docs-only cleanup. Codex and Claude both flagged the initial summary-vs-detailed mismatch in this entry; Gemini reported no actionable issues. I fixed the placeholder review block instead of rolling the summary back to pending so the final record matches the actual review state. A final verification pass then reported no actionable issues on the finished text. Validation not run because this was a formatting-only documentation cleanup.

## [2026-04-23 01:23, America/Los_Angeles] - Edge-pan gated to fullscreen/full-screen window

**Action:** Changed camera edge-pan so hovering at the screen edge no longer moves the camera in ordinary windowed mode. Wrote the browser regression first in `tests/browser/game-hud-and-camera.spec.ts`, added fullscreen helpers in `tests/browser/helpers/gameTestHelpers.ts`, then gated `GameScene.getEdgePanDelta()` behind an explicit fullscreen/window-covers-screen check in `src/phaser/scenes/GameScene.ts`. During review iteration I tightened the fullscreen predicate to the game canvas ancestry, replaced the helper's old canvas click with a dedicated temporary fullscreen trigger button, added coverage for the full-screen-window heuristic and fullscreen-exit stop behavior, and updated the live docs/lessons to match the new control contract.

**Code reviewer comments:**
- **Codex**
  - **Design:** Round 1 correctly flagged that a document-fullscreen-only gate would silently remove edge-pan from the real product because the game had no in-app fullscreen affordance. Addressed by preserving edge-pan when the browser window already covers the full screen in addition to true fullscreen, and by aligning the control docs with that contract.
  - **Test coverage:** Round 2 correctly flagged that the new full-screen-window heuristic branch still lacked a positive browser test. Addressed by adding `pans the camera when the browser window already fills the whole screen` to `tests/browser/game-hud-and-camera.spec.ts`.
  - **Correctness:** Round 1 also correctly caught that `enterGameFullscreen()` was not truly idempotent. Addressed by short-circuiting before the trigger click when the game canvas is already contained by the current fullscreen element.
  - **Clean code / typing / efficiency / memory leaks:** No separate blocker beyond the helper/contract issues above.
  - **Documentation:** Round 2 correctly flagged that `README.md`, `design/spec-final.md`, and the devlog trail were out of sync. Addressed by updating the README/spec wording, adding a new lesson, and appending this task entry. Final verification then reported no substantial remaining issues, with only the temporary `code_review/` cleanup left to do.
- **Gemini**
  - **Design:** The initial requested `gemini-3-pro` invocation failed with `ModelNotFoundError`, so no substantive first-round review output was available from that attempt.
  - **Test coverage:** A later verification attempt was blocked because the local Gemini CLI could not read `code_review/*` under its ignore rules and then hit `RESOURCE_EXHAUSTED` capacity on `gemini-3.1-pro-preview`, but the final repo-local verification rerun against the touched files completed successfully and reported no actionable issues.
  - **Correctness:** The final successful Gemini review reported no actionable issues.
  - **Clean code / typing / efficiency / memory leaks:** The final successful Gemini review reported no actionable issues.
  - **Documentation:** The final successful Gemini review reported no actionable issues.
- **Claude**
  - **Design:** Round 1 correctly flagged that "any fullscreen element on the page" was too broad. Addressed by requiring the fullscreen element to contain the game canvas.
  - **Test coverage:** Round 1 correctly flagged the need to cover fullscreen exit state clearing; addressed by adding `stops edge panning immediately after fullscreen exits`. A later ask to harden the "enter fullscreen while already hovering" transition started veering into brittle browser-state choreography rather than a confirmed gameplay bug, so I stopped before turning that into a flaky test.
  - **Correctness:** Round 1 correctly caught that the helper's activation click at `(2, 2)` was sending unintended input into the canvas. Addressed by moving fullscreen activation to a dedicated temporary button outside the game input path.
  - **Clean code / typing / efficiency / memory leaks:** A later verification note pointed out the unrelated `src/game/simulation/prototypeScenario.ts` CRLF-only worktree artifact. Left it untouched and will keep it out of staging so this commit stays scoped.
  - **Documentation:** Round 2 correctly flagged the missing devlog entry for this task. Addressed by appending this entry before the final verification rerun. Final verification then reported no actionable code, test, or documentation issues remaining.

**Result:** Edge-pan now stays off in windowed play, still works when the game is actually fullscreen, still works when the browser window already covers the full screen, and stops immediately when fullscreen exits. Keyboard, minimap, middle-drag, and zoom camera controls remain unchanged.

**Reasoning:** The user-facing problem was accidental camera drift when the cursor touched the screen edge outside fullscreen play. The smallest durable fix was to leave the edge-pan mechanic intact but gate it behind an explicit "full-screen enough" contract and then lock that contract with browser tests instead of silently removing the feature.

**Notes:** Iteration validation stayed local to the affected browser surface: repeated `npm.cmd run build` plus `npx.cmd playwright test tests/browser/game-hud-and-camera.spec.ts` reruns, ending at **14/14** camera/HUD browser tests passing. Final reviewer verification: Codex found no substantial remaining issues and only asked for temp `code_review/` cleanup, which is now done; Claude reported no actionable issues remaining; Gemini's final repo-local review reported no actionable issues after earlier CLI model/capacity friction. Final validation: `npm.cmd run typecheck` passed, `npm.cmd run lint` passed, `npm.cmd run test:browser` passed with **79/79** browser tests, and `npm.cmd test` reached **34/34** files and **321/321** tests passing but still exited non-zero because of the pre-existing **6** `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors on this Windows setup. Temporary `code_review/` notes were deleted after their contents were summarized here. Left alone as instructed: the unrelated `src/game/simulation/prototypeScenario.ts` CRLF-only worktree artifact.
