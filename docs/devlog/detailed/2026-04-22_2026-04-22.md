## [2026-04-22 11:30, America/Los_Angeles] - Move-order path caching for selected-unit movement lag

**Action:** Investigated the report that selected units sometimes move in a laggy, sub-60-FPS-feeling way. Read the current devlog + architecture first, then traced the movement path through `src/game/simulation/createSimulationBridge.ts`. The hot path turned out to be the move-command branch re-running `findGridPath(...)` every simulation tick for the same destination via `findMovePlan(...)`, even when the unit was already following a stable route. Added a transient `movePathCache` side map keyed by unit id, new `clearUnitCommand` / `setUnitCommand` helpers so command replacement always invalidates cached routes, and a `resolveMovePlanFromCache(...)` path that reuses the cached path while the next cell is still passable and falls back to a fresh solve only when needed. Added `tests/simulation/movementPathCaching.test.ts` first to lock three gameplay-facing contracts: steady move orders do not re-solve every tick, retargeting drops the old route immediately, and save/load forces a fresh re-plan instead of persisting transient route state. During final review, Codex identified a real regression in the first cache implementation: if the player clicked a blocked cell that later became free, the unit could stop forever at the stale fallback tile. Added a new `move-target-unblocks-fixture` plus a fourth movement-path test, then adjusted the cache logic so reaching a cached fallback destination forces one more re-plan toward the original clicked cell before the command is considered finished.

**Code reviewer comments:**
- **Codex**
  - **Design:** Initial review approved the transient-side-map direction and explicitly called out keeping the cache outside serialized `unitCommands` as the right boundary. A later review caught the dynamic-blocker regression where units could stay committed to an old fallback route after the clicked cell opened up; addressed by re-planning as soon as the original target becomes passable and by adding the extra redirect-before-fallback regression.
  - **Test coverage:** Initial review asked for a save/load regression to prove the route cache does not cross persistence boundaries. Addressed by adding the third test in `tests/simulation/movementPathCaching.test.ts`. Final verification said the added retarget/save-load/dynamic-blocker coverage covers the main introduced behaviors.
  - **Correctness:** Final verification reported no actionable correctness regressions.
  - **Clean code / typing / efficiency / memory leaks:** Final verification reported no actionable findings after the cache invalidation and dynamic-blocker fixes landed.
  - **Documentation:** Initial review noted the missing detailed devlog entry. Addressed by this file; final verification did not raise doc issues.
- **Gemini**
  - **Design:** Earlier attempts were blocked by local model / network issues (`gemini-3-pro` missing; later TLS failure). Final successful review found no design issues.
  - **Test coverage:** Final successful review found the added coverage sufficient for the change.
  - **Correctness:** Final successful review found no issues.
  - **Clean code / typing / efficiency / memory leaks:** Final successful review found no issues.
  - **Documentation:** Final successful review found the docs updates appropriate.
- **Claude**
  - **Design:** First review correctly flagged that putting cached route state on `UnitCommand` would pollute save/load, and that non-move planners should not pay extra clone / cache-entry overhead. Both were fixed: the cache now lives in `movePathCache`, and non-move planners still use the old lightweight `findMovementPlan(...)` path.
  - **Test coverage:** First review asked for broader regression coverage around stale-route invalidation and save/load. Addressed by the retarget and save/load tests in `tests/simulation/movementPathCaching.test.ts`, then extended with the two dynamic-blocker tests on `move-target-unblocks-fixture`.
  - **Correctness:** Verification review passed the stale-route invalidation and transient-cache boundary after the fixes. A later targeted re-review found no actionable correctness issues in the final cache logic.
  - **Clean code / typing / efficiency / memory leaks:** Follow-up review noted duplicated helper shapes and fixture drift. Addressed by aligning `createMoveTargetUnblocksFixture`'s `starts` / `spawns` Town Center coordinates, hoisting shared candidate/path solving into `findMovementPathToCandidates`, making `CachedMovePath` extend `ResolvedMovementPath`, removing the extra cache-entry wrapper, and making the move-only passability choice explicit at the re-plan call. Final targeted re-review reduced the remainder to an optional non-blocking future test around next-step obstruction.
  - **Documentation:** Verification review said the remaining required doc gap was the detailed devlog entry, addressed here. No later doc findings.

**Result:** Selected-unit move orders now reuse a cached route instead of resolving a fresh grid path every tick while chasing the same destination, and they still stay anchored to the player's original click target if a temporary blocker disappears mid-walk. If the clicked cell opens up while the unit is still following the old fallback route, the cache now drops immediately and re-solves toward the real target on that same tick. That removes avoidable A* churn from the simulation loop without changing mechanics. Validation after the implementation:
- `npm.cmd run typecheck`: passed.
- `npm.cmd test`: reported **32/32** files and **301/301** tests passing, but still exits non-zero because the pre-existing `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors remain on this Windows setup.
- `npm.cmd run build`: passed (and was re-run via `npm.cmd run test:browser`).
- `npm.cmd run test:browser`: passed (**71** browser tests).
- `npm.cmd run lint`: still fails in unrelated pre-existing files, including existing warnings/errors in `src/game/simulation/createSimulationBridge.ts`, `src/ui/hud/minimap.ts`, and several older test files that this task did not touch.

**Reasoning:** The user-facing symptom looks like "not 60 FPS," but the repo's simulation intentionally runs at `TPS = 10`, so any expensive work in the tick loop is more noticeable than in a 60 Hz sim. The renderer can still draw at 60 FPS, but if the simulation hitches on repeated per-tick pathfinding, selected units appear to advance unevenly. Caching the route for steady move orders is the smallest behavior-preserving fix because move-command passability already ignores other units; that means we can safely reuse the route until the next terrain/building/resource passability check says the immediate next cell is no longer valid.

**Notes:** Left alone as instructed: `cavalry-icon-draft.html`, `src/drafts/`, and local `AGENTS.md` edits. No `civ-engine` repo changes were made. The path cache intentionally remains transient and unsaved; `unitCommands` continues to represent durable player intent only. The new fixture uses a second villager to clear a one-hit tree while the selected villager is already walking toward that blocked cell, so the regression test models the exact dynamic-blocker case from review rather than a synthetic direct state mutation. If movement still feels choppy after this, the next likely place to inspect is broader tick-budget pressure in the simulation step rather than pathfinding repetition specifically.

## [2026-04-22 15:17, America/Los_Angeles] - Owned sheep now share player vision and peel fog while moving

**Action:** Implemented the follow-up gameplay request that claimed sheep should reveal terrain for their owner as they explore. Traced the fog-of-war path in `src/game/simulation/createSimulationBridge.ts` and confirmed visibility only updates from ECS entities that carry both `position` and `visionSource`, while sheep were still plain `resource` entities with no visibility component. Added a dedicated `sheep-vision-fixture` in `src/game/simulation/prototypeScenario.ts` and a gameplay-facing regression in `tests/simulation/sheepVision.test.ts` first. The fixture now includes one human-claimable sheep, one neutral sheep near the hidden enemy house, and one enemy-claimed sheep near the same house so the initial hidden-house assertion proves both "neutral sheep do not leak human vision" and "enemy sheep do not leak human vision." Then updated `updateSheepOwnership(...)` so owned sheep gain a `visionSource` tied to `resource.owner`, neutral sheep drop that component, and the existing visibility pipeline can reveal and then re-fog the explored area without any new fog-specific code paths.

**Code reviewer comments:**
- **Codex**
  - **Design:** First-round review agreed with the minimal "owned sheep need a real `visionSource`" direction and did not flag a design regression in the bridge change itself.
  - **Test coverage:** First-round review said the standalone sheep-vision test was too narrow because it only pinned the reveal happy path. Addressed by broadening the fixture so nearby neutral and enemy-owned sheep would expose an ownership leak immediately, and by making the test assert both reveal and re-fog behavior around the same hidden house.
  - **Correctness:** First-round review did not find an actionable gameplay correctness bug in the implementation.
  - **Clean code / typing / efficiency / memory leaks:** First-round review did not identify an actionable code-quality or perf regression beyond the test narrowness.
  - **Documentation:** First-round review correctly flagged the missing devlog/summary trail. Addressed by this detailed entry and the summary update.
- **Gemini**
  - **Design:** Review did not flag a design problem with the bridge/fixture shape.
  - **Test coverage:** Review asked for stronger negative coverage and a less brittle happy-path test. Addressed by extending the fixture with nearby non-human sheep that must NOT reveal the enemy house, increasing the claim wait budget slightly, and explicitly asserting both the live reveal and the later memory state in `tests/simulation/sheepVision.test.ts`.
  - **Correctness:** Review did not report an actionable correctness regression in the shipping behavior.
  - **Clean code / typing / efficiency / memory leaks:** Review did not report a code-quality or efficiency issue in the final bridge path.
  - **Documentation:** Review did not object to the doc plan once the missing devlog trail was added.
- **Claude**
  - **Design:** First-round review agreed the change stays inside the existing visibility architecture (resources gain a normal `visionSource`; fog systems remain unchanged).
  - **Test coverage:** First-round review asked for broader negative coverage than the original one-way reveal test. Addressed by the extra neutral/enemy sheep in `sheep-vision-fixture` plus the reveal-then-memory assertions in `tests/simulation/sheepVision.test.ts`.
  - **Correctness:** Review did not identify an actionable gameplay regression in the owned-sheep reveal path.
  - **Clean code / typing / efficiency / memory leaks:** Review noted the new per-tick sheep-vision check is small and localized; no blocker was raised for the current herd sizes in the prototype.
  - **Documentation:** First-round review correctly flagged the missing detailed devlog / summary update. Addressed here.

**Result:** Owned sheep now reveal fog for their owner using the same visibility system as units and buildings. A claimed sheep walking into hidden territory brings an enemy house into the live render frame, and when that same sheep walks back out, the house drops back to fog memory instead of staying fully visible. The extra fixture sheep keep the contract honest by proving that nearby neutral or enemy-owned sheep do not accidentally reveal the same area for the human player. Validation from the final code state:
- `npm.cmd test`: reported **33/33** files and **302/302** tests passing, but still exits non-zero because the pre-existing `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors remain on this Windows setup.
- `npm.cmd run typecheck`: passed.
- `npm.cmd run build`: passed.

**Reasoning:** The smallest behavior-preserving fix was to keep sheep inside the existing visibility model rather than teaching fog-of-war special cases about herdables. Once a sheep is owned, it should behave like any other scouting entity from the fog system's perspective: it carries a `visionSource`, reveals tiles for that owner, and naturally stops revealing them when it is neutral again or when the sheep leaves the area.

**Notes:** Left alone as instructed: `cavalry-icon-draft.html`, `src/drafts/`, and the concurrent `tests/simulation/sheepMovement.test.ts` edits. The sheep-vision lesson entry in `docs/learning/lessons.md` was added by this change set, not pre-existing repo state. No `civ-engine` repo changes were needed for this follow-up.

## [2026-04-22 15:28, America/Los_Angeles] - Sheep vision review cleanup and final verification

**Action:** Addressed the final review pass on the sheep-vision follow-up. Dropped an intermediate depletion-only draft branch before finalizing the `updateSheepOwnership(...)` diff so the shipped change stays narrowly about ownership-driven vision instead of inventing a new depleted-sheep state transition. Added an inline comment explaining why `SHEEP_VISION_RADIUS = 4` is intentional, tightened the new lesson entry in `docs/learning/lessons.md` so it no longer promises a non-existent "loses ownership" path, and restored the file's punctuation/encoding after an accidental mojibake rewrite. Re-ran the required repo validation and the external reviewer pass after those fixes landed.

**Code reviewer comments:**
- **Codex**
  - **Design:** First review only found the `lessons.md` encoding regression. Addressed by restoring the file content and re-saving it in proper UTF-8.
  - **Test coverage:** Verification review did not request additional coverage beyond the existing `sheep-vision-fixture` + `tests/simulation/sheepVision.test.ts` contract.
  - **Correctness:** Verification review reported no discrete correctness issues in the final simulation change.
  - **Clean code / typing / efficiency / memory leaks:** Verification review reported no actionable findings after the docs fix. Its own internal Vite/Vitest startup hit a local `spawn EPERM`, but repo validations were run directly in this session.
  - **Documentation:** Verification review reported no remaining documentation findings once the encoding issue was fixed.
- **Gemini**
  - **Design:** A first attempt with an explicit `gemini-3-pro` model failed locally with `ModelNotFoundError`; reran with the default configured model. Successful review reported no design findings.
  - **Test coverage:** Successful review reported no missing-test findings.
  - **Correctness:** Successful review reported no correctness findings.
  - **Clean code / typing / efficiency / memory leaks:** Successful review reported no issues.
  - **Documentation:** Successful review reported no documentation findings and reviewed `docs/learning/lessons.md`.
- **Claude**
  - **Design:** First review correctly pushed back on the temporary depletion draft branch and on the unexplained `SHEEP_VISION_RADIUS` magic number. Addressed by dropping that draft branch before finalizing the diff and documenting the radius choice inline.
  - **Test coverage:** First review tied its depletion concern to missing branch coverage. Because that draft branch never made it into the final diff, the remaining gameplay test coverage stayed focused on the intended claim -> reveal -> memory contract in `tests/simulation/sheepVision.test.ts`.
  - **Correctness:** First review's only concrete correctness concern was the ghost-ownership risk from that depletion draft branch. Dropping the draft branch eliminated the issue; a follow-up verification pass was requested after docs landed.
  - **Clean code / typing / efficiency / memory leaks:** First review also noted that the first `lessons.md` rewrite broadened the change scope and that the lesson wording overstated the implemented behavior. Addressed by restoring the file content and tightening the wording to the real ownership-to-vision invariant.
  - **Documentation:** First review correctly flagged the missing final devlog trail. Addressed by the sheep-vision entry above plus this verification follow-up, after which Claude was asked to verify again.

**Result:** The final sheep-vision change remains small and behavior-focused: owned sheep reveal nearby fog through the normal visibility system, and the supporting docs now accurately describe that invariant without extra depletion logic or encoding damage. Validation after the cleanup:
- `npm.cmd test`: reported **33/33** files and **302/302** tests passing, but still exits non-zero because the pre-existing `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors remain on this Windows setup.
- `npm.cmd run test:browser`: passed (**71** browser tests) and re-ran `npm.cmd run build` / `tsc --noEmit` as part of its pretest step.
- `npm.cmd run lint`: still fails in unrelated pre-existing files (`src/game/simulation/createSimulationBridge.ts`, `src/ui/hud/minimap.ts`, and several older tests) that this task did not touch.

**Reasoning:** Claude's depletion-branch feedback was the only review finding that affected live simulation semantics. The cleanest response was to remove that extra state path altogether, because the requested feature never needed it: sheep vision should track ownership, and existing depletion/destroy flows can keep handling dead resources the same way they already do.

**Notes:** Left alone as instructed: `cavalry-icon-draft.html`, `src/drafts/`, and local `AGENTS.md` edits. No `civ-engine` repo changes were needed. The earlier sheep-vision entry in this file captures the core implementation; this follow-up records the final review cleanup and verification pass.

## [2026-04-22 16:02, America/Los_Angeles] - Sheep vision save/load regression and final verification

**Action:** Followed up on the last sheep-vision review request by adding a dedicated save/load regression to `tests/simulation/sheepVision.test.ts`. The new test saves after a claimed sheep has already revealed the hidden enemy house, then loads through `createSimulationBridge('aoe2-prototype', { savedGame })` so the assertion does not depend on replaying the original fixture seed. Re-ran the required validation after that test landed and asked the external reviewers to verify the final diff again.

**Code reviewer comments:**
- **Codex**
  - **Design:** Verification review reported no remaining actionable findings in the final sheep-vision diff.
  - **Test coverage:** Verification review reported no remaining actionable findings after the save/load regression was added.
  - **Correctness:** Verification review reported no remaining actionable findings.
  - **Clean code / typing / efficiency / memory leaks:** Verification review reported no remaining actionable findings.
  - **Documentation:** Verification review accepted the current devlog/summary trail and only noted that it did not personally rerun the local commands.
- **Gemini**
  - **Design:** The final follow-up still pushed on test robustness, specifically that the save/load regression should not depend on the original fixture seed. Addressed by loading the saved blob through `createSimulationBridge('aoe2-prototype', { savedGame })` instead of reusing `'sheep-vision-fixture'`.
  - **Test coverage:** The save/load request above was Gemini's only remaining actionable coverage note. Addressed by the second test in `tests/simulation/sheepVision.test.ts`.
  - **Correctness:** No new runtime correctness issue was identified once the load path stopped reusing the original fixture seed.
  - **Clean code / typing / efficiency / memory leaks:** Gemini's only remaining note after the save/load fix was optional cleanup around shared vision-radius constants; not a blocker for this task.
  - **Documentation:** No blocker after the docs were present.
- **Claude**
  - **Design:** Final verification called the change ready to ship and reduced the remainder to low-severity optional observations.
  - **Test coverage:** Final verification marked the shipped coverage as sufficient for this task; the only remaining note was an optional future positive assertion for enemy-owned sheep revealing for the enemy player.
  - **Correctness:** Final verification reported no correctness blocker.
  - **Clean code / typing / efficiency / memory leaks:** Final verification reduced the remainder to low-severity optional notes about making the herdable-movement/visibility ordering edge explicit and about the currently defensive null-owner removal branch.
  - **Documentation:** Final verification found the detailed devlog and summary trail present and consistent.

**Result:** The sheep-vision change now has two dedicated regressions in `tests/simulation/sheepVision.test.ts`: one for reveal/re-fog behavior while moving, and one for save/load continuity once a claimed sheep is already revealing the hidden house. Final validation from the exact final code state:
- `npm.cmd test`: reported **33/33** files and **303/303** tests passing, but still exits non-zero because the pre-existing `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors remain on this Windows setup.
- `npm.cmd run typecheck`: passed.
- `npm.cmd run build`: passed.

**Reasoning:** The save/load regression kept the reviewer-requested persistence check inside the same scoped sheep-vision test file without widening the task into the broader save/load suite. Passing a different seed string on load is the smallest way to prove the `SaveBlob` is authoritative and self-sufficient.

**Notes:** Claude's last pass left only low-severity optional ideas: make the `prototypeVisibility` dependency on `prototypeHerdableMovement` explicit, add a symmetric enemy-vision positive assertion someday, and possibly delete or comment the defensive null-owner removal branch if sheep ownership rules never broaden beyond neutral-to-owned.

## [2026-04-22 16:14, America/Los_Angeles] - Sheep vision contract hardened against fixture-seed leakage

**Action:** Finalized the dedicated sheep-vision regression after checking the load path in `createSimulationBridge(...)`. Because the bridge already ignores the caller's seed when `savedGame` is present and instead reloads `savedGame.seed`, the stronger test is to load the saved blob through `createSimulationBridge('aoe2-prototype', { savedGame })` and prove the owned sheep still reveals the hidden enemy house. Kept the added ownership assertions that the nearby neutral sheep stays `owner === null` and the nearby enemy sheep stays `owner === 2`, so the test now locks both "no leaked human vision" and "save blobs are authoritative" in one focused file. Re-ran the full repo validation on that exact final diff.

**Code reviewer comments:**
- **Codex**
  - **Design:** The final verification state remained design-safe because the stronger save/load regression still exercises the same ownership-driven visibility path instead of adding a new fog-specific mechanism.
  - **Test coverage:** No further coverage blocker remained once the test proved both the negative ownership guards and the load-from-blob path.
  - **Correctness:** No new correctness issue was introduced by swapping the load call to a different seed because the bridge restores from `savedGame.seed`.
  - **Clean code / typing / efficiency / memory leaks:** No additional clean-code or efficiency finding was introduced by the final test tightening.
  - **Documentation:** The existing devlog trail already covered the sheep-vision change; this appended note records the exact final contract and validation state.
- **Gemini**
  - **Design:** Gemini's last actionable concern was exactly this save/load robustness point; the final test now proves the seed argument is irrelevant once the `SaveBlob` exists.
  - **Test coverage:** The added ownership assertions and cross-seed load path resolve the remaining coverage concern.
  - **Correctness:** No new correctness blocker remained after the stronger save/load assertion.
  - **Clean code / typing / efficiency / memory leaks:** No new finding.
  - **Documentation:** No new blocker once the final verification state was written down.
- **Claude**
  - **Design:** Claude had already reduced the remaining notes to low-severity optional ideas; this final test hardening stays within that approved design.
  - **Test coverage:** The dedicated sheep-vision test now explicitly guards the neutral/enemy non-leak case before and after movement, alongside the persistence round-trip.
  - **Correctness:** No new correctness blocker remained after the final test tightening.
  - **Clean code / typing / efficiency / memory leaks:** Only optional future cleanup ideas remained.
  - **Documentation:** No blocker; the appended entry keeps the log aligned with the shipped test.

**Result:** The final sheep-vision change now ships with a focused regression file that proves three gameplay-facing behaviors: claimed sheep reveal fog while moving, nearby neutral/enemy sheep do not leak that vision to the human player, and the revealed state survives save/load from the blob rather than from replaying the original fixture seed. Final validation from the exact committed code state:
- `npm.cmd test`: reported **33/33** files and **303/303** tests passing, but still exits non-zero because the pre-existing `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors remain on this Windows setup.
- `npm.cmd run typecheck`: passed.
- `npm.cmd run build`: passed.
- `npm.cmd run test:browser`: passed (**71** browser tests).
- `npm.cmd run lint`: still fails with **15** unrelated pre-existing errors in `src/game/simulation/createSimulationBridge.ts`, `src/ui/hud/minimap.ts`, and several older test files.

**Reasoning:** Loading with a different seed is only worth keeping if the save system truly owns the restored state. The bridge code does that already, so the stronger regression is the right contract: it catches any future bug where load accidentally starts depending on fixture bootstrap details again.

**Notes:** Left alone as instructed: `cavalry-icon-draft.html`, `src/drafts/`, and local `AGENTS.md` edits. No `civ-engine` repo changes were needed. The final sheep-vision regression still lives in one dedicated file instead of spilling into unrelated save/load coverage.

## [2026-04-22 19:41, America/Los_Angeles] - Extract prototype rule tables out of the simulation bridge

**Action:** Extracted the inline economy, building, and unit rule tables plus their pure helpers out of `src/game/simulation/createSimulationBridge.ts` into `prototypeEconomyRules.ts`, `prototypeBuildingRules.ts`, and `prototypeUnitRules.ts`; added `tests/simulation/prototypeRules.test.ts` to lock gameplay-facing rule contracts against the extracted modules; removed a handful of unused locals/imports in touched HUD/test files; and fixed an accidental UTF-8 BOM/mojibake rewrite plus trailing-blank-line whitespace in `createSimulationBridge.ts` after the bulk edit. During review I briefly explored widening a few rule tables to match canonical AoE2 expectations, then rolled those mechanics changes back so the final diff stayed behavior-preserving and cleanup-only.

**Code reviewer comments:**
- **Codex**
  - **Design:** Initial review said the extraction looked mechanically consistent. A later review correctly flagged that the temporary skirmisher / bonus-table edits would change live mechanics during a cleanup-only task; addressed by rolling those edits back. Another pass also surfaced an unrelated untracked draft HTML page and, on the staged diff itself, an EOF blank-line issue in `createSimulationBridge.ts`; the draft page stayed out of scope and uncommitted, while the whitespace issue was fixed with `git diff --check`.
  - **Test coverage:** No staged coverage blocker remained once `tests/simulation/prototypeRules.test.ts` covered representative extracted rules. Codex could not rerun Vitest/build in its sandbox because esbuild/Vite spawn failed with `EPERM`.
  - **Correctness:** Final staged-only review reported no actionable defects once the out-of-scope mechanics edits were removed.
  - **Clean code / typing / efficiency / memory leaks:** Final staged-only review accepted the modular extraction as behavior-preserving.
  - **Documentation:** No blocker in the staged cleanup.
- **Gemini**
  - **Design:** An early broad review first reported no actionable issues. A later pass, reasoning about canonical AoE2 data rather than the cleanup scope, suggested widening spear bonuses, the skirmisher archer classification, and trebuchet minimum range. Because this task was refactor-only, those edits were not kept. The final staged-diff-scoped review reported no actionable issues remaining.
  - **Test coverage:** `prototypeRules.test.ts` was sufficient for the extraction once it stayed behavior-preserving.
  - **Correctness:** Final staged-diff-scoped review reported no actionable regression in the cleanup.
  - **Clean code / typing / efficiency / memory leaks:** No blocker remained; Gemini emitted repeated `429` capacity retries after the useful answer.
  - **Documentation:** No blocker.
- **Claude**
  - **Design:** First review's concrete findings were mechanical, not architectural: the bridge file had picked up BOM/mojibake during the bulk edit, and the new tests used owner-id magic numbers instead of `HUMAN_PLAYER_ID`. Addressed directly.
  - **Test coverage:** Claude suggested adding an explicit non-harvestable-resource error-path regression while the pure rule tests were open; addressed in `tests/simulation/prototypeRules.test.ts`.
  - **Correctness:** First review also caught the duplicated unharvestable-resource error block in `prototypeEconomyRules.ts`; addressed by centralizing it in `throwUnharvestableResourceError`. A targeted verification pass then reported no remaining actionable issues.
  - **Clean code / typing / efficiency / memory leaks:** No remaining blocker after the cleanup. A final post-rollback verification attempt timed out, so the last successful Claude verification is the earlier targeted pass plus direct local validation.
  - **Documentation:** No blocker once the lessons/devlog trail existed.

**Result:** The final diff turns the bridge's biggest pure-rule clusters into three smaller simulation modules, adds one direct regression file for those rules, and leaves gameplay behavior unchanged. Final validation from the exact final code state:
- `npm.cmd run typecheck`: passed.
- `npm.cmd run lint`: passed.
- `npm.cmd test`: reported **34/34** files and **314/314** tests passing, but still exits non-zero because the pre-existing `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors remain on this Windows setup.
- `npm.cmd run build`: passed.
- `npm.cmd run test:browser`: passed (**71** browser tests).

**Reasoning:** The cleanup followed the usual code-quality guidance for this repo and the broader refactoring literature: smaller modules, one source of truth for data tables, and explicit tests around user-visible rules. The important correction from review was scope, not implementation style: a cleanup pass should first preserve current mechanics, and any balance or rules correction should land as its own gameplay task.

**Notes:** Left alone as instructed and out of this commit: `cavalry-icon-draft.html`, `src/drafts/`, and local `AGENTS.md` edits. No `civ-engine` repo changes were needed.

## [2026-04-22 22:45, America/Los_Angeles] - Precise selection geometry, live marquee preview, and browser spec split

**Action:** Tightened unit selection to use exact rendered-body hit geometry for left-click and marquee selection, added live marquee preview highlights for the units that will actually be selected on mouse-up, kept right-click context targeting on its own forgiving padded unit-target helper, and changed same-cell exact-click cycling to key off a repeated click in the same click cell instead of any prior selection state. At the same time, broke the oversized browser smoke test file into focused specs (`game-selection`, `game-combat-and-meta`, `game-hud-and-camera`, `game-progression-and-production`, `game-rendering-and-world`, `game-simulation-and-exploration`) with shared helpers under `tests/browser/helpers/`.

**Code reviewer comments:**
- **Codex**
  - **Design:** First review caught that selection-specific owner priority had leaked into generic point targeting. Addressed by splitting selection ordering (`findEntitiesAtWorldPointInEntities`) from generic/command targeting and then keeping command targeting on a dedicated padded helper. Verification later also pushed for same-cell repeat-click cycling to depend on the actual click cell, not just the current selection.
  - **Test coverage:** First review wanted coverage around overlapping-unit priority; the follow-up review wanted an exact-click cycle regression and a forgiving command-target regression. Addressed with the expanded `tests/phaser/entityHitTest.test.ts` coverage and the browser exact-click cycle regression in `tests/browser/game-selection.spec.ts`.
  - **Correctness:** Verification review correctly flagged two user-facing edge cases: near-edge right-clicks on moving units had become too strict, and the first exact click on an already-selected stacked target could jump to the next entity. Addressed by `findCommandTargetEntityAtWorldPointInEntities(...)` plus the new `recentExactSelectionClick` gate in `GameScene`.
  - **Clean code / typing / efficiency / memory leaks:** No blocking clean-code issue remained after the helper split.
  - **Documentation:** No blocker after the lesson/devlog trail was added.
- **Gemini**
  - **Design:** Initial review reported no actionable issues. A later pass correctly flagged that the native `dblclick` DOM handler was now redundant with Phaser's pointer-up path and could race with it.
  - **Test coverage:** No blocker beyond ensuring the browser split kept the original gameplay coverage intact.
  - **Correctness:** The actionable correctness note was the redundant `dblclick` path above; addressed by reducing `handleNativeDoubleClick` to `event.preventDefault()` so it only suppresses browser-native text selection.
  - **Clean code / typing / efficiency / memory leaks:** No blocker after the simplification.
  - **Documentation:** No blocker once the updated lesson and devlog entry existed.
- **Claude**
  - **Design:** First review surfaced the preview/selection asymmetry for sheep and the reuse of selection-specific ordering in the command path. Addressed by adding `bridge.filterSelectableUnitIds(...)` for preview parity and by separating exact selection ordering from padded command targeting. A later verification review also flagged that same-cell double-click promotion could stomp exact-click stack cycling; addressed by skipping same-type promotion whenever an exact repeat-click actually cycles to a different target.
  - **Test coverage:** Claude asked for targeted regressions instead of broader rewrites. Addressed by expanding `tests/phaser/entityHitTest.test.ts` and `tests/browser/game-selection.spec.ts`, while keeping the browser split behavior-preserving.
  - **Correctness:** First review also caught that memory ghosts could still participate in point-hit resolution and then fail bridge selection. Addressed by filtering `isMemory` entities out of shared point-hit candidates.
  - **Clean code / typing / efficiency / memory leaks:** Verification review pushed on avoidable marquee-preview work in the hot path. Addressed by moving the selection-box cache key onto raw drag bounds (no preview recompute there), computing the rendered marquee preview once per render pass, and avoiding the extra UI-side re-filter before `selectUnitsByIds(...)`.
  - **Documentation:** First review correctly flagged the missing devlog/summary updates for both the selection work and the browser spec split. Addressed by this entry, the summary update, and the lesson additions.

**Result:** The current diff now gives exact rendered-body precision for unit clicking and drag selection, visibly previews the units that a marquee drag will capture, preserves slightly forgiving context-command targeting for moving units, and keeps exact-click overlap cycling stable after drag-boxes or prior selections. The browser smoke suite is now split into smaller feature files that are easier to run in isolation while iterating.

**Reasoning:** Selection felt wrong for two opposite reasons at once: precise clicks were being rounded back out into broad hit areas, while the drag-preview path and the final bridge selection path were disagreeing about what was selectable. The durable fix was to share one exact selection geometry, keep command targeting on its own padded helper, and let repeat-click stack cycling depend on the player's repeated click cell rather than on stale selection state.

**Notes:** Targeted validation from this state: `npx.cmd vitest run tests/phaser/entityHitTest.test.ts` passed (**9** tests) and `npx.cmd playwright test tests/browser/game-selection.spec.ts` passed (**10** tests). Full-suite validation and final reviewer verification still remain before commit. Left alone as instructed: `cavalry-icon-draft.html`, `src/drafts/`, and local `AGENTS.md` edits.
