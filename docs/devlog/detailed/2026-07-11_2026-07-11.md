# 2026-07-11

## Full-review iteration 4 — independent multi-CLI verification of the deferred-backlog sweep

**Timestamp.** 2026-07-11 (continuing the /full-review flow after the deferred backlog was worked through; iterations 1–3 converged and moved to `docs/threads/done/full/2026-07-10/`).

**Action.** Ran iteration 4 of the multi-CLI adversarial review over the accumulated backlog diff (`git 92f486f..HEAD`, 70 files / +1917-258) — the fixes shipped AFTER the 3 converged iterations. Codex `gpt-5.6-sol`/ultra + Claude `opus[1m]`/max (Fable 5 quota still exhausted — `You've reached your Fable 5 limit`, exit 1; noted, retry next session). Prompt carried the reviewers-must-read directive, the doc-accuracy addendum, per-item intent, and a 6-point anti-regression checklist. Each finding was re-verified against live code (an Explore verifier independently confirmed the three Codex-only loop-honesty findings) before disposition. Fixed six code/security findings + one doc; acknowledged four with reasons. Synthesis: `docs/threads/done/full/2026-07-11/4/REVIEW.md`.

**Code reviewer comments.** Codex (10 findings: 3 HIGH, 6 MEDIUM, 1 LOW) + Claude (2: 1 MEDIUM, 1 LOW; verified the rest correct with live git-apply repros). By theme —

- *Security (both CLIs converged; Claude gave a live repro).* The H9 `applyAndGate` sensitive-path guard was COMPLETELY bypassable two ways: (a) CRLF headers — a trailing `\r` left every `$`-anchored pattern unmatched so `patchTouchesSensitivePaths` returned `[]` for every path and a malicious `package.json` `postinstall` applied unseen; (b) case-insensitive FS — the case-sensitive regexes missed `Vite.config.ts`/`Package.json` that `git apply` writes to the real lowercase files on the loop's Windows/macOS checkout.
- *Loop-honesty (Codex HIGH/MEDIUM; verifier CONFIRMED all three).* M13/M6 added completion-honesty gates to the rerun/prove side but left the initial-run selection path and the LLM runner's tick/termination accounting ungated: (A) a degenerate initial run — costBudget/providerError/engineHalt with only the excluded `match-completes` — exited 0 as a clean `no-fix-candidate`, hiding a broken run; (B) `selectLedgerFixCandidate` ignored `verificationStatus`, so an `unverified` finding from a failed-replay run could be patched and, being spurious, vanish on a healthy rerun → `fixed-proven`; (C) `ticksRun += ticksToAdvance` credited the REQUESTED not observed delta, and with no match-state check a COMPLETED match (bridge `step` no-ops once `outcome !== 'running'`) was mislabeled `engineHalt` (winner dropped, prove-reruns rejected).
- *Finding-loss (Codex MEDIUM + Claude LOW).* H6's within-run union reused the deliberately-coarse cross-run `findingIdentityKey`, collapsing two distinct same-`[category, area]` conformance findings (a later HIGH could vanish behind an earlier LOW).
- *Self-inflicted regression the review caught (Codex MEDIUM-8).* M14a had pushed `saveLoad.test.ts` to 519 LOC (> 500), unexempted — the committed file-size gate was RED on `main`. (The M14 commit's suite actually printed `1 failed`; a stale concurrent task's green summary had been misread — the headline value of the independent pass.)
- *Doc (Codex LOW).* `ARCHITECTURE.md` still advertised the removed `monkTaskOps` legacy direct-assignment helper (L4 removed it).
- *Acknowledged (bounded / pre-existing).* Codex MEDIUM-4/5 (fog memory keys storage/render by raw id while cleanup is generation-aware — Claude independently assessed both as ≤ old behavior, migration-only, not M5 regressions) and MEDIUM-9 (`corpusSchema` accepts unknown threshold keys — pre-existing, LOW; the type-level removal already covers TS-authored corpora).

**Result.** Six TDD'd (red→green) fixes landed — H9-bypass (case-insensitive patterns + CR/timestamp normalization; 3 new bypass tests), H6-collision (`withinRunUnionKey` keys conformance on per-finding id), B (require `verificationStatus === 'verified'`), A (`qualifyInitialRun` verify+horizon gate → `initial-run-unqualified`), C (host `getMatchOutcome()`; observed-delta + terminal-match `stopWhen` before the halt check), and the fileSizeBudget remediation (extracted `captureSnapshot` → `saveBlobTestUtils` [519→476] and the self-improvement-loop fixtures → `selfImprovementLoopTestKit` [504→387]). Plus the ARCHITECTURE.md doc fix. Four gates green: typecheck, lint, build, and `npm test` (2024 passed | 2 skipped; the 4 subprocess-spawning playtest-SCRIPT files flaked once under an overlapping-run contention — the earlier foreground `npm test` timed out at the 2-min bash cap but its vitest child kept running alongside the background rerun — and were re-verified green in isolation, 12/12). No version bump (harness/test-infra/docs; no game-behavior change).

**Reasoning.** The /full-review contract is iterate-until-reviewers-nitpick; iteration 4 verified the backlog fixes independently and surfaced real defects same-model subagents had missed — most importantly a security guard that was defeated by a trailing `\r`, and a file-size gate I had committed RED after misreading a stale task's summary. A/C were fanned out to parallel subagents (disjoint files) with precise specs + the verifier's evidence, then each diff was verified against live code before integration (C's `getMatchOutcome` deviation to `getHudState().matchState.outcome` reads the identical fresh value and stays within the `BrowserTestBridge` surface).

**Notes.** The loop-honesty root cause is now closed on BOTH sides of the pass (initial-run + rerun/prove) and the runner's tick/termination accounting matches the sibling `runPlaytest.ts`. A `false-verify` misread lesson is recorded in `docs/learning/lessons.md`. Deferred backlog (MEDIUM-4/5/9 acknowledged, H5/M4 civ-engine-blocked) is documented in the REVIEW.md.

## Full-review iteration 5 — re-review of the iter-4 fix batch; H9 quoted-header bypass found + hardened; CONVERGED

**Timestamp.** 2026-07-11 (immediately after iteration 4).

**Action.** Re-reviewed the iter-4 fix batch (`git d692ce5..HEAD`) to confirm the fixes are correct + complete. Codex `gpt-5.6-sol`/ultra was BLOCKED by its cybersecurity-risk filter (the prompt's "construct a patch that git apply writes to a sensitive file… give exact header bytes" tripped it — treated as unreachable; compensated with the driver's own adversarial git testing + Claude `opus[1m]`). Synthesis: `docs/threads/done/full/2026-07-11/5/REVIEW.md`.

**Code reviewer comments.** BOTH the driver's adversarial git testing AND Claude independently proved a THIRD H9 parser-drift bypass the iter-4 fix (CRLF + case) had not closed: `git apply` calls `unquote_c_style` on any header starting with `"`, so a MANUALLY c-quoted `--- "a/package.json"` is unquoted + written by git while the three iter-4 regexes (requiring an unquoted `a/`) return `[]`. Confirmed end-to-end in three variants (modify `package.json`; quoted `---/+++` with no `diff --git`; ADD a new `.npmrc`/CI workflow via a quoted `+++`). The iter-4 comment's "git only c-quotes special-char paths" dismissal reasoned about what `git diff` EMITS, not what `git apply` ACCEPTS from a hand-authored patch. Claude confirmed EVERYTHING ELSE correct + re-ran green (A 15/15, B 7/7, C 17/17, H6 11/11; all file splits < 500, behavior-preserving) and judged the acknowledged deferrals (MEDIUM-4/5/9) reasonable. Claude also caught the driver's uncommitted mid-refactor as the exact "false-verify"/concurrent-look-alike trap the batch had just documented in `lessons.md` — it checked `git show HEAD:` instead of the live file and avoided reporting a phantom fileSizeBudget regression.

**Result.** H9 hardened robustly (commit 77eda1f): the sensitive-path set is now derived from git's OWN parser via `git apply --numstat -z -` (reports exactly what a real apply writes, without touching the tree — unquoting c-quotes, normalizing CRLF, resolving case), UNION the regex classifier as defense-in-depth. New exported `parseNumstatPaths` (NUL-record, rename-aware) + a test proving the quoted header is rejected while the regex alone still misses it. Verified all three variants reject; the regex-only fallback (numstat exit ≠ 0) is ZERO-risk because `--numstat` and `apply` share one parser (a malformed patch fails BOTH with exit 128). H9 tests split to `applyAndGate.sensitivePaths.test.ts` + `applyAndGateTestKit.ts`. Gates: typecheck + lint green; applyAndGate suites 24 passed; fileSizeBudget green. No bump.

**Reasoning.** The quoted-header vector proves Claude's iter-4 point that hand-rolled diff parsing keeps drifting from git — after fixing CRLF then case, a third vector remained. The robust fix is to stop re-implementing git's parser and ask git directly. Finding this via the driver's own empirical `git apply` testing (before the reviewers) is the debugging-tools-first discipline applied to security.

**Notes.** Iterations 4–5 CONVERGED: the one substantive finding is fixed + dual-confirmed + empirically validated; a further iteration would only re-confirm the committed fix. Thread moved `docs/threads/done/full/2026-07-11/` → `done/`. Codex's cyber-filter block is a prompt-phrasing note for next session (ask for bypass CLASSES, not exact exploit bytes).