## [2026-04-17 10:45, America/Los_Angeles] - Reorganize docs to match AGENTS.md layout
**Action:** Restructured the `docs/` tree so every doc path `AGENTS.md` references now exists. Moved `docs/devlog-summary.md` to `docs/devlog/summary.md`, split the single 521-line `docs/devlog-detailed.md` at the 2026-04-11 → 2026-04-12 natural boundary into `docs/devlog/detailed/2026-04-10_2026-04-11.md` (349 lines) and `docs/devlog/detailed/2026-04-12_2026-04-13.md` (172 lines), then created skeleton files for the previously missing architecture, debugging, and learning surfaces. `docs/engine-feedback.md` stays where it is per AGENTS.md.
**Result:** `docs/devlog/summary.md`, `docs/devlog/detailed/*`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/drift-log.md`, `docs/architecture/decisions.md`, `docs/debugging/template.md`, and `docs/learning/lessons.md` are all in place. Future agents can follow AGENTS.md's session-start reads and append-only conventions without hitting missing paths. Existing `Files changed:` references in the split archives keep their original `docs/devlog-*.md` paths as historical record.
**Files changed:** `docs/devlog-detailed.md` (removed), `docs/devlog-summary.md` (moved), `docs/devlog/summary.md` (new location), `docs/devlog/detailed/2026-04-10_2026-04-11.md`, `docs/devlog/detailed/2026-04-12_2026-04-13.md`, `docs/devlog/detailed/2026-04-17_2026-04-17.md`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/drift-log.md`, `docs/architecture/decisions.md`, `docs/debugging/template.md`, `docs/learning/lessons.md`
**Reasoning:** AGENTS.md was recently rewritten to point at a richer doc tree than the repo actually had. Bringing the filesystem in line with the instructions is strictly a housekeeping move: it does not change game mechanics, build steps, or tests, and it prevents future agents from silently skipping the architecture/debugging/learning workflows just because the target paths did not exist. Splitting the detailed devlog at the 2026-04-11/2026-04-12 boundary keeps each active file under the 500-line rotation threshold.
**Notes:** No code, content, or test changes. Architecture scaffolding was filled from the visible code layout, `docs/engine-feedback.md`, and existing devlog entries rather than invented; key decisions captured as KAD-0001 through KAD-0004 reflect choices already in force. This is a doc-only change so the `npx vitest run`, `npx tsc --noEmit`, `npx vite build` gate was not re-run.

## [2026-04-17 15:55, America/Los_Angeles] - Player-commandable sheep movement with sticky ownership and group selection
**Action:** Added sheep movement on top of the existing herdable-ownership system. In `src/game/simulation/createSimulationBridge.ts`: added `SHEEP_SUBGRID_STEP_PER_TICK = 1` (half villager step), parameterized `stepUnitTransformToward` and `moveUnitOneSubgridStep` with an optional `stepUnits` argument so unit movement stays byte-identical, gave sheep a `unitTransform` component on creation, made `updateSheepOwnership` early-return when the sheep already has an owner so once-claimed ownership becomes sticky, registered a new `prototypeHerdableMovement` system that steps sheep one subgrid unit per tick toward `sheepMoveOrders` targets via the existing `findMovementPlan` and `isCellPassableForWildlife` helpers, added `issueSheepMoveCommand` plus a `getSelectedOwnedSheepIds` selector, dropped the unit-component conjunction in the render projector so any entity with a `unitTransform` renders smoothly (codex review caught that sheep were still snapping cell-to-cell), and rewired `issueMoveCommand`, `issueContextCommand`, and `issueContextCommandAtEntityInternal` to dispatch to all selected human units AND owned sheep so a single right-click moves a mixed group together. Extended `selectUnitsInBox` (drag-box) to also pick up owned sheep via a new `getHumanOwnedSheepIdsInRect` helper. Cleared sheep move orders inside `destroyResourceEntity` as a leak guard. Added a `sheep-movement-fixture` scenario in `prototypeScenario.ts` (human villager + claimed sheep cluster, enemy scout for the stickiness steal scenario, pre-claimed enemy sheep, lone unclaimed sheep, large human TC vision so off-screen sheep are selectable from tests). Wrote `tests/simulation/sheepMovement.test.ts` (8 vitest cases: sticky ownership against an enemy steal, single-sheep move on command, order clears at arrival, unclaimed and enemy sheep reject move commands, half-villager-speed invariant, drag-box catches the herd, single right-click moves the whole herd) and a Playwright case in `tests/browser/game.spec.ts` that selects an owned sheep and right-clicks a destination, asserting rendered manhattan distance to target shrinks. Wrote design and plan docs at `docs/superpowers/specs/2026-04-17-sheep-movement-design.md` and `docs/superpowers/plans/2026-04-17-sheep-movement.md`.
**Result:** Full gate green sequentially: `npx vitest run` → `84` tests, `npx tsc --noEmit` clean, `npx vite build` clean (`46` modules transformed, `1.48 MB` Phaser bundle), `npm run test:browser` → `58` Playwright tests pass. The player can left-click an owned sheep to select it, right-click ground or an entity to walk it there at half villager speed, drag-box across a herd to grab them all and move them with one right-click, and ownership of any claimed sheep is permanent until the sheep is depleted/eaten/killed.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `tests/simulation/sheepMovement.test.ts`, `tests/browser/game.spec.ts`, `docs/superpowers/specs/2026-04-17-sheep-movement-design.md`, `docs/superpowers/plans/2026-04-17-sheep-movement.md`, `docs/devlog/detailed/2026-04-17_2026-04-17.md`, `docs/devlog/summary.md`, `.gitignore`
**Reasoning:** User reported "the sheep cannot move" and chose option B (player commands the sheep manually) with sticky-once-claimed ownership and half villager speed. Treating sheep as movable resources (instead of giving them a `unit` component) keeps the resource/unit boundary intact: combat, AI targeting, population, and economy code never see sheep as units, so the change is fully additive. The new movement system reuses the unit pathing and subgrid transforms; the only widening was making `stepUnitTransformToward` accept a step size and dropping the projector's `unit && unitTransform` conjunction. The plan's original `before: ['prototypePlayerCommands']` ordering cycled with existing dependencies (`prototypePlayerCommands → … → prototypeHerdableOwnership`); since gather and wildlife combat both target sheep by entity ref rather than by cell, dropping that constraint costs at most one tick of position lag for those systems with no observable gameplay impact. Group selection followed naturally from extending the rect-search helper and the multi-dispatch commands.
**Notes:** Codex review (after the user fixed `~/.codex/rules/default.rules` to remove `decision="deny"`) ran twice. Round 1 caught the smooth-rendering projector regression (shipped) and the ordering vs `prototypePlayerCommands` (documented above as a one-tick lag with no observable impact since gather and wildlife combat target by entity ref). Round 2 caught a High-severity bug — sheep move orders never cleared when the requested target was blocked because `isUnitTransformAtTarget` was checked against the requested cell rather than the planner's chosen destination — and a Medium-severity regression where mixed villager+sheep drag-box selections hid villager build options (the eligibility check required all selected entities to be villagers). Both shipped in 408ceeb with regression tests. Gemini review surfaced one real Medium follow-up (when right-clicking a moving entity with a mixed group, sheep go to the entity's static cell at click-time while villagers track the moving entity — acceptable for v1 since sheep have no follow semantics in real AoE2 either) plus two findings that did not hold under verification: sheep matches the existing unit pattern of running `findMovementPlan` per tick and deleting orders on path-plan failure, neither is unique to sheep. Known follow-ups: (a) gather-state thrash when a villager is harvesting a moving sheep — economy loop drops the villager out of `gathering` whenever the sheep's approach cell changes; (b) same-type-on-screen double-click (the `selectOwnedUnitsByTypeInRect` path used by the GameScene's double-click handler) was deliberately not extended to sheep in this slice — it requires a small GameScene change to record sheep selection as a double-click candidate; (c) sheep do not have a `unit` component, so they are not picked up by `getSelectedHumanUnitIds`-style queries and require the parallel `getSelectedOwnedSheepIds` accessor. No `civ-engine` features were missing.

## [2026-04-17 13:35, America/Los_Angeles] - Preserve game aspect ratio and make minimap clicks center the camera exactly
**Action:** Letterboxed `#game-root` to a fixed 5:3 aspect ratio in `src/styles.css` so the Phaser canvas never stretches when the viewport shape differs from the 60×36 world. In `src/phaser/scenes/GameScene.ts` and `src/ui/hud/createHudController.ts`, scaled pointer coordinates by `canvas.width / bounds.width` so clicks line up with world cells when CSS size differs from internal canvas size. Switched `centerCameraOnWorldPosition` to `camera.centerOn` (which already applies Phaser's `useBounds` clamp) and rewrote `getCameraScrollBounds` to match Phaser's own `clampX/Y` formula — the old clamp used `[0, world - view]` and bounced the camera away from world edges when zoomed in. Computed the snapshot's `viewX/Y/Width/Height` directly from `scrollX/Y`, `width/height`, and `zoom` rather than reading `camera.worldView` (which is integer-rounded every frame by the `pixelArt: true` path). Added a `pointer.moveTime === 0` guard to `getEdgePanDelta` so edge-pan doesn't activate before the real cursor has ever entered the canvas — Phaser's `activePointer` defaults to `(0, 0)`, which is inside the top-left edge zone. Added a new browser test that zooms in and then clicks the minimap center, asserting the camera lands exactly on the world center; tightened the existing drag/click minimap tests so they assert the exact expected center (with max-scroll clamping) instead of only checking a direction. Rewrote the "viewport shrinks" test to assert letterboxed dimensions.
**Result:** `npx.cmd vitest run` passes with `76` tests, `npx.cmd tsc --noEmit` clean, `npx.cmd vite build` clean, and `npx.cmd playwright test` passes all `57` browser tests (including the new exact-minimap-click centering test). The game canvas preserves pixel-art aspect under any viewport shape, minimap clicks land exactly on the clicked world position, and edge-pan no longer drifts the camera before the user has moved their cursor.
**Files changed:** `src/phaser/scenes/GameScene.ts`, `src/styles.css`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-17_2026-04-17.md`, `docs/devlog/summary.md`
**Reasoning:** The old `getCameraState` read `camera.worldView`, which Phaser rounds each frame — so exact-position assertions on the minimap were off by up to a pixel. The old `clampCameraToWorld` used a scroll range of `[0, world - view]` that corresponds to the camera TL origin but Phaser's `useBounds` uses a different formula based on `displayWidth`; when zoomed the two disagreed, so the camera couldn't reach the right/bottom world edge via `centerOn`. The CSS fix removes the need for DPR or aspect-ratio workarounds in coordinate math because the canvas now always has the same aspect as the world. The edge-pan guard is a root-cause fix for a pre-existing bug exposed by the sharper test assertions: without a real mouse event, the default `(0, 0)` pointer is indistinguishable from "user hovering top-left", so `pointer.moveTime === 0` is the right "has any real event landed" signal.
**Notes:** The `pixelArt: true` flag in `src/app/bootstrap/createApp.ts` is the reason Phaser rounds `scrollX/Y` and `worldView` — that's still the right setting for this game, we just stopped relying on the rounded output for precise world math. The minimap coordinate scaling (`canvas.width / bounds.width`) is defensive for HiDPI/future CSS sizing even though the current CSS keeps CSS size equal to canvas attribute size at viewport 800×600. Two `createSimulationBridge` tests (Mining Camp drop-off, AI Barracks rush) timed out on one earlier vitest run but passed on the full final gate; they are long-running simulation tests and the timeouts reproduce on unrelated baselines, not caused by these changes.

## [2026-04-17 17:20, America/Los_Angeles] - Slice 1 Task A: reorder sheep movement before player commands
**Action:** In `src/game/simulation/createSimulationBridge.ts` the `prototypeHerdableMovement` system registration dropped `after: ['prototypeHerdableOwnership']` and gained `before: ['prototypePlayerCommands']`. This resolves the one-tick lag where gather and wildlife combat saw the sheep's old position; ownership is sticky after the first claim so ordering relative to ownership no longer matters.
**Result:** Full `npx vitest run` green: 86 tests across 13 files. Sheep movement file still 10/10. No observable behavior delta beyond the removed lag.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `docs/devlog/detailed/2026-04-17_2026-04-17.md`
**Reasoning:** Spec Task A. The original `after` constraint was from the sheep-movement slice's first registration; the now-closed ownership cycle is gone, so `before: prototypePlayerCommands` makes the per-tick pipeline see the sheep's new position on the same tick a right-click targets it. Stickiness guarantees ownership does not flip back to null between movement and downstream systems.
**Notes:** No test changes — ordering-only change with no observable new behavior, per spec. Gate-level full suite runs only at end of slice (Task E); ran it here because Task A is the whole change.
