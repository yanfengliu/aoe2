# 2026-07-07

## v0.1.119 — Terrain surface detail

**Timestamp.** 2026-07-07 01:47:18 -07:00

**Action.** Continued the screenshot-backed graphics loop on the real default `aoe2-prototype` view and shipped the next AoE2-style polish slice: terrain diamonds now draw deterministic kind-specific interior marks. Grass, forest, and hill tiles get faint speckles; water tiles get short translucent wave strokes. The change is pure presentation in `terrainRenderer.ts`; simulation, pathing, fog, selection, camera, saves, and replays are unchanged. Version bumped to 0.1.119 and the changelog/spec/current graphics plan were updated.

**Code reviewer comments.** In-process adversarial review: `drawIsoBuilding` already had a ground-contact shadow, so the originally considered building-shadow slice would have duplicated existing behavior. The visual weak point shifted to flat terrain diamonds. The review kept the implementation deterministic and bounded: two tiny marks per tile, derived from cell coordinates with no `Math.random` or time input, drawn inside the diamond and under entities/fog/selection. The existing edge-feather test was tightened so it checks blend-colour feather styles rather than assuming every `fillCircle` is a boundary speck. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `drawTerrainCell` now calls a surface-detail helper after the base diamond fill and before edge feathering. Non-water terrain uses translucent dots; water uses short line strokes. Focused tests in `tests/phaser/terrainTexture.test.ts` cover the new red/green contract and keep the old deterministic, diamond, tint, and edge-feather contracts green. Visual evidence lives under `tmp/vision/foot-units-after`, `tmp/vision/terrain-detail-after`, and `tmp/vision/terrain-detail-diff`; the canvas diff was 5,878 / 983,040 changed pixels (0.598%), localized to terrain.

**Reasoning.** Real Age of Empires II terrain is richly textured even when individual units are tiny. The procedural renderer should not import sprite assets, but small deterministic surface marks give grass, hills, forests, and water a more natural read than flat diamond fills while preserving the current simulation and projection contracts.

**Validation.** TDD red: `npx.cmd vitest run tests/phaser/terrainTexture.test.ts` failed because same-kind grass terrain produced 0 interior detail marks. Green: the same focused command passed with 17 tests after implementation. Visual validation captured the real default view before/after and a pixel diff. Full gates passed: `npm.cmd test` (216 files passed, 1 skipped; 1797 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** Remaining isometric polish: per-civilization/architecture-set facade variation, attack/death feedback, richer weapon animation, per-unit/per-civilization visual variety, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## v0.1.118 — Foot-unit limb detail

**Timestamp.** 2026-07-07 01:20:14 -07:00

**Action.** Continued the screenshot-backed vision loop on the real default `aoe2-prototype` view and shipped the next AoE2-style graphics polish slice: villager, infantry, and archer silhouettes now include planted leg strokes plus small visible foot marks below the torso. The change is pure presentation in `unitRenderer.ts`; movement, hit testing, selection rings, health bars, simulation state, saves, and replays are unchanged. Version bumped to 0.1.118 and the changelog/spec/current isometric plan were updated.

**Code reviewer comments.** In-process adversarial review: the first implementation satisfied the primitive-count test but the default-view pixel diff moved only 7 / 983,040 pixels because the torso drew over the foot marks. The review therefore converted the visual failure into a draw-order contract: foot marks must be drawn after the torso for foot humanoids so they survive default-zoom overdraw. A documentation review updated the current plan so "fuller unit figures" no longer refers to the now-shipped planted-foot detail. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `drawFootLegs` now draws the lower leg strokes behind the body, while `drawFootMarks` draws two small owner-outline foot marks after the torso for villager, infantry, and archer roles. Focused tests in `tests/phaser/unitRenderer.test.ts` cover the red/green contract: the old renderer had no planted legs, then had hidden feet, and the final renderer passes all 48 unit-renderer tests. Visual evidence lives under `tmp/vision/wildlife-species-after`, `tmp/vision/foot-units-after`, and `tmp/vision/foot-units-diff`; the final canvas diff was 28 / 983,040 changed pixels (0.003%), localized to the two on-screen foot-unit silhouettes.

**Reasoning.** Real Age of Empires II foot units read as small upright people because the lower-body silhouette remains visible even at default zoom. The procedural renderer should keep that silhouette hierarchy without changing mechanics or importing copyrighted sprites. Drawing leg strokes behind the body but the small feet after the torso gives the desired read while keeping every primitive within the existing unit bounding circle.

**Validation.** TDD red: `npx.cmd vitest run tests/phaser/unitRenderer.test.ts` first failed because villager/infantry/archer had no planted legs; after the first implementation it passed but the visual diff exposed overdraw. A second red test then failed because foot marks were absent after the torso (`expected 0 to be greater than or equal to 2`), and the final focused run passed with 48 tests. Visual validation captured the real default view before/after and a pixel diff. Full gates passed: `npm.cmd test` (216 files passed, 1 skipped; 1796 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** Remaining isometric polish: per-civilization/architecture-set facade variation, attack/death feedback, richer weapon animation, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## v0.1.117 — Wildlife species silhouettes

**Timestamp.** 2026-07-07 00:54:23 -07:00

**Action.** Continued the screenshot-backed vision loop on the real default `aoe2-prototype` view and a bounded `playtest:llm` opening, then shipped the next AoE2-style graphics polish slice: sheep, boar, and wolves no longer share a single generic body+head animal silhouette. Sheep draw wool clumps and short legs, boar draw tusks, and wolves draw pointed ears plus a tail. The change is pure presentation in `resourceRenderer.ts`; resource ownership/tint semantics, hit testing, selection, gathering, combat, fog memory, simulation state, saves, and replays are unchanged. Version bumped to 0.1.117 and the changelog/spec/current isometric plan were updated.

**Code reviewer comments.** In-process adversarial review: the real first-screen capture plus the 250-tick LLM checkpoint showed the Town Center and terrain were readable after v0.1.116, while sheep/boar/wolf still read as tiny generic marks. The review scope therefore stayed render-only and species-local. A contract check verified the implementation does not read or mutate simulation state, does not change `ProjectedEntityView`, preserves the existing tint path used for sheep ownership, and leaves other resources on their existing draw paths. Documentation review updated the current plan so wildlife species differentiation is no longer listed as future work. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `drawResourceEntity` now branches inside the existing wildlife path: sheep get rounded wool detail, boar get tusks and a heavier profile, and wolves get leg lines, ears, and a tail. Focused tests in `tests/phaser/resourceRenderer.test.ts` cover the red/green contract: the old generic wildlife path failed on missing wool/legs/tusks/ears/tail and identical primitive signatures; the new renderer passes. Visual evidence lives under `tmp/vision/default-view-v0116-current`, `tmp/vision/llm-v0116-current-screenshots`, `tmp/vision/wildlife-species-after`, and `tmp/vision/wildlife-species-diff`; the canvas diff was 589 / 983,040 changed pixels (0.060%), localized to wildlife positions.

**Reasoning.** Real Age of Empires II relies on tiny but distinct animal silhouettes: sheep are round and woolly, boar read heavier and dangerous, and wolves are angular/predatory. This project still uses original procedural primitives, but it should borrow that silhouette hierarchy so the map is readable at default zoom and the vision harness sees more meaningful objects.

**Validation.** TDD red: `npx.cmd vitest run tests/phaser/resourceRenderer.test.ts` failed with 4 expected failures because sheep had only 1 circle, boar/wolf had 0 triangles, and all wildlife shared 1 primitive signature. Green: the same focused command passed with 11 tests. Visual validation captured the real default view before/after and a pixel diff. Full gates passed: `npm.cmd test` (216 files passed, 1 skipped; 1793 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** The bounded vision-harness opening run accepted all 7 commands (house placement, two gather orders, scout move, TC rally, and two villager queues), ran 250 ticks, cost $0.7786, and ended in-progress with owners 1 and 2 alive. Remaining isometric polish after v0.1.118: per-civ/architecture-set facade variation, attack/death feedback, richer weapon animation, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## v0.1.116 — Isometric building material breakup

**Timestamp.** 2026-07-07 00:36:55 -07:00

**Action.** Used the new screenshot-backed vision loop on the real default `aoe2-prototype` view and shipped the next AoE2-style graphics polish slice: completed tall isometric buildings now draw subtle base-parallel masonry course lines across both visible wall faces and sparse diagonal roof tile seams inside the roof diamond. The change is pure presentation and localized to the Phaser building renderer; footprints, hit testing, health bars, selection, construction, fog memory, simulation state, saves, and replays are unchanged. Version bumped to 0.1.116 and the changelog/spec/current isometric plan were updated.

**Code reviewer comments.** In-process adversarial review: visual default-view comparison confirmed the original issue was the first-viewport Town Center reading as one large flat tan block, so the implementation should add small material cues without changing game mechanics or the art style. A self-review finding caught that the shared `drawIsoBuilding` path would also add completed-building masonry/roof seams to tall construction stubs (Castle/Wonder), which would make incomplete buildings read as finished; fixed by adding a `materialDetail` style flag and passing `false` from the construction-stub path, with a regression test in `tests/phaser/buildingRenderer.test.ts`. Documentation review grep found a stale current-plan reference to wall banding as future work after the feature shipped; updated the summary and isometric design thread so only per-civ/architecture-set texture variation remains in that slot. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `isoBuilding.ts` now exposes pure geometry helpers for wall-course and roof-tile segments and draws the material detail only for completed tall buildings. `buildingRenderer.ts` keeps construction stubs visually plain. Focused tests cover the new geometry contracts and the construction-stub exclusion. The visual protocol captured before/after/default-view evidence under `tmp/vision/default-view-before`, `tmp/vision/default-view-after`, and `tmp/vision/default-view-diff`; the canvas diff was 2,719 / 983,040 changed pixels (0.277%), confined to the building area.

**Reasoning.** Real Age of Empires II buildings carry a lot of small wall and roof texture that helps them read as built structures at default zoom. The current procedural art should not chase exact copyrighted assets, but it can borrow the composition lesson: large flat planes need restrained, repeatable material cues. Lines were chosen over new filled shapes so windows, doors, owner tint, roof accents, HP bars, fog, selection, and construction states keep their existing visual priority.

**Validation.** `npx.cmd vitest run tests/phaser/isoBuilding.test.ts` failed before implementation on missing helpers and zero material-detail lines, then passed after implementation. `npx.cmd vitest run tests/phaser/isoBuilding.test.ts tests/phaser/buildingRenderer.test.ts` failed before the construction-stub fix because a castle construction stub produced 11 completed-building line segments, then passed after `materialDetail: false`. Full gates passed: `npm.cmd test` (216 files passed, 1 skipped; 1789 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** The LLM playtest harness completed a bounded opening pass before the visual fix (`playtest:llm`, max 250 ticks, screenshot every 250): it accepted 5 commands and left the game in progress, with no game-rule defects identified for this slice. Remaining isometric polish: per-civ/architecture-set facade variation, fuller unit and wildlife figures with attack/death feedback, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.
