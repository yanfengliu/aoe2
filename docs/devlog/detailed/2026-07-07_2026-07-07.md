# 2026-07-07

## v0.1.125 - Town Center roof post detail

**Timestamp.** 2026-07-07 09:10:00 -07:00

**Action.** Wrapped the existing dirty Town Center roof-accent polish unit into a coherent release slice. The renderer adds four short flanking post strokes around the completed Town Center's central roof block, and the focused spy test now counts `lineBetween` calls to pin the added silhouette detail. Version bumped to 0.1.125 and package lock, changelog, spec, and summary devlog were updated.

**Code reviewer comments.** Self-review kept the change inside `buildingRoofAccents.ts` and scoped to the `town-center` accent path. The added strokes are decorative roof accents only; they do not touch footprints, hit testing, selection, construction state, health bars, fog memory, simulation state, saves, or replays. Multi-CLI review was not run because this is a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `drawTurret` now derives four small post anchors from the existing roof center and accent width, then draws short dark vertical strokes after the top face is painted. Other building roles keep their prior accent geometry.

**Reasoning.** The recent roof bevel/eave work made large roof caps less flat, but the starting Town Center still benefits from a few structural vertical cues around the central roof block. The posts give the roof a supported 3/4-view hall read while staying original/procedural and bounded to the existing roof footprint.

**Validation.** Focused test passed: `npm.cmd test -- tests/phaser/buildingRoofAccents.test.ts` (4 tests). Full gates passed: `npm.cmd test` (217 files passed, 1 skipped; 1808 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** This entry wraps a renderer/test diff that was already present at session start; no unrelated local work was staged with it.

## v0.1.124 - Water shoreline strokes

**Timestamp.** 2026-07-07 08:50:20 -07:00

**Action.** Continued the local screenshot-backed graphics loop and shipped a terrain polish slice: water cells that border land now draw short, low-alpha inset shoreline strokes over the existing water ripples and terrain feathering. The change is pure presentation in `terrainRenderer.ts`; terrain kind data, movement/pathing, fog state, camera math, simulation state, saves, and replays are unchanged. Version bumped to 0.1.124 and the changelog/spec/current graphics plan were updated.

**Code reviewer comments.** Local adversarial review caught that the first full-length shoreline strokes were too bright and too grid-like under fog. The implementation was softened before documentation/gates: each stroke is shortened to the middle of the edge, inset toward the water cell centre, and drawn with lower alpha. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `drawTerrainCell` now adds a shoreline line only when the current cell is water and an orthogonal neighbour exists with a non-water terrain kind. Open water keeps only the existing interior wave strokes; grass/forest/hill cells do not draw shoreline lines themselves. Visual evidence lives under `tmp/vision/water-shoreline-before`, `tmp/vision/water-shoreline-after`, and `tmp/vision/water-shoreline-diff`; the final canvas diff changed 4,921 / 983,040 pixels (0.501%), localized to water/land boundaries.

**Reasoning.** Real Age of Empires II shorelines are not plain blue tile edges; they have a subtle contact cue where water meets land. The existing terrain feather softened kind transitions, but water still read blocky. Short inset strokes provide a shoreline read while staying deterministic and original/procedural.

**Validation.** TDD red: `npx.cmd vitest run tests/phaser/terrainTexture.test.ts` first failed because shoreline and open-water cases both drew only two line strokes. Green: the same focused suite passed after implementation and after the visual softening pass (18 tests). Visual validation captured the v0.1.123 baseline/after/diff from the real default canvas. Full gates passed: `npm.cmd test` (217 files passed, 1 skipped; 1807 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** Remaining polish: per-civilization/architecture-set facade variation, attack/death feedback, richer weapon animation, per-unit/per-civilization visual variety, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## v0.1.123 - Roof bevel depth cues

**Timestamp.** 2026-07-07 08:37:25 -07:00

**Action.** Continued the local screenshot-backed graphics loop after the external LLM-backed harness was blocked by the external-data approval boundary. The current v0.1.122 canvas showed the Town Center roof still reading as a broad flat tan slab, so this slice adds inset roof highlights plus darker lower eave shadows to tall completed isometric buildings. The change is pure presentation in `isoBuilding.ts`; footprints, hit testing, health bars, selection, construction state, fog memory, simulation state, saves, and replays are unchanged. Version bumped to 0.1.123 and the changelog/spec/current graphics plan were updated.

**Code reviewer comments.** Local adversarial review kept the change inside the existing `materialDetail` branch so construction stubs and low flat buildings do not receive completed-building roof detail. The new geometry helper returns two highlight and two shadow segments inset inside the roof diamond, avoiding outline overpaint and preserving the footprint-bounds contract. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `roofBevelSegments` now computes roof-depth highlight and shadow/eave segments from the same roof polygon used by the existing tile seams. `drawIsoBuilding` paints the highlight and shadow passes only for tall material-detailed buildings. Visual evidence lives under `tmp/vision/roof-bevel-before`, `tmp/vision/roof-bevel-after`, and `tmp/vision/roof-bevel-diff`; the canvas diff changed 2,008 / 983,040 pixels (0.204%), localized to the visible Town Center roof.

**Reasoning.** Real Age of Empires II buildings rely on small roof-edge lighting and eave shadows to sell depth at default zoom. The project already has original/procedural isometric volumes; adding restrained roof-edge cues improves the 3/4-view read without importing assets or changing mechanics.

**Validation.** TDD red: `npx.cmd vitest run tests/phaser/isoBuilding.test.ts` first failed because tall buildings still drew 11 roof/wall lines instead of 15 and `roofBevelSegments` did not exist. Green: the same focused suite passed after implementation (15 tests). Visual validation captured the v0.1.122 baseline/after/diff from the real default canvas. Full gates passed: `npm.cmd test` (217 files passed, 1 skipped; 1806 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** Remaining polish: per-civilization/architecture-set facade variation, attack/death feedback, richer weapon animation, per-unit/per-civilization visual variety, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## v0.1.122 - Soft fog edge fade

**Timestamp.** 2026-07-07 08:23:02 -07:00

**Action.** Continued the screenshot-backed graphics loop and shipped a fog-of-war polish slice: visible cells that touch fog now draw a low-alpha isometric shroud pass over the visible cell, softening the hard diamond sawtooth around the explored frontier. The change is pure presentation in `worldLayers.ts`; visibility, exploration, fog memory, pathing, targeting, selection, saves, and replays are unchanged. Version bumped to 0.1.122 and the changelog/spec/current graphics plan were updated.

**Code reviewer comments.** Local adversarial review challenged two failure modes before completion: changing fog semantics instead of render-only presentation, and muddying fully visible interiors. The implementation only iterates the already-rendered `visible` set, checks four in-bounds neighbours against that same set, and draws an additional fog diamond without mutating frame data. Focused tests cover both the border-cell fade and the fully visible no-fade case. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `renderFog` now shares the iso diamond drawing helper between hard fog masks and the new soft inner rim. A visible cell gets the rim only when at least one orthogonal neighbour is not visible; fully visible regions stay clear. Visual evidence lives under `tmp/vision/fog-edge-before`, `tmp/vision/fog-edge-after`, and `tmp/vision/fog-edge-diff`; the canvas diff changed 76,407 / 983,040 pixels (7.773%), localized to the visible fog frontier.

**Reasoning.** Real Age of Empires II-style fog reads as a softened frontier rather than a jagged exact tile cutout, especially around the visible/explored boundary. Adding a subtle render-only inner shroud gives that read while keeping the hard gameplay visibility contract untouched.

**Validation.** TDD red: `npx.cmd vitest run tests/phaser/worldLayers.test.ts` first failed because the visible edge cell produced no extra fog diamond. Green: the same focused suite passed after implementation (7 tests). Visual validation captured the real default canvas before/after and a pixel diff. Full gates passed: `npm.cmd test` (217 files passed, 1 skipped; 1805 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** Remaining polish: per-civilization/architecture-set facade variation, attack/death feedback, richer weapon animation, per-unit/per-civilization visual variety, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## Vision harness conformance guard

**Timestamp.** 2026-07-07 03:36:11 -07:00

**Action.** Hardened the post-hoc vision/conformance findings pass after the saved v0.1.121 run produced false high-severity "missing age-progression", "missing technology", and "missing combat" findings from a short 750-tick Dark Age opening. The codebase already implements age-up, research, and combat surfaces, so the problem was the evaluator over-reading command absence as capability absence. No package version bump or changelog entry: this is internal harness/reporting behavior, not gameplay behavior.

**Code reviewer comments.** In-process adversarial review verified the claims against the live code before acting: `queue.research`, age progression, and `unit.attack` are present in the simulation/command surface, while the trace only proved that the agent did not use those commands in the first 1:15. The fix therefore stayed in `conformanceProbe.ts` instead of touching gameplay. The prompt now explicitly forbids treating short early-game absence as proof of a missing feature, and the digest label now says command types were "used in this run" rather than "could use".

**Result.** `RunMetrics` now carries `maxTicks`, the digest emits a short-opening evidence caution when a max-tick run is tiny, the command-type metric wording is usage-scoped, and the system prompt requires `record_findings` exactly once even when the correct output is `findings: []`. `tests/playtest/conformanceProbe.test.ts` covers both prompt contracts and the short-run digest warning.

**Reasoning.** The vision loop is only useful if its critique is trustworthy. A short AoE2 opening normally focuses on food, houses, scouting, and villager production; it should not be interpreted as proof that later age-up, technology, or combat systems do not exist. Keeping that distinction in the deterministic digest reduces expensive false-positive loops and makes the next visual/gameplay iteration cleaner.

**Validation.** TDD red: `npx.cmd vitest run tests/playtest/conformanceProbe.test.ts` first failed on the "could use" wording/short-run warning, then failed on the missing no-findings tool-call prompt. Green: the focused suite now passes 16 tests. Live harness check: rerunning `npm.cmd run playtest:findings -- tmp/vision/llm-v0121-probe` on the same saved trace now returns 0 findings with no probe note. Full gates passed: `npm.cmd test` (217 files passed, 1 skipped; 1803 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** Continue using the harness, but verify every substantial finding against the live code before implementing. Short max-tick openings are now explicitly scoped as incomplete evidence in the LLM-facing digest.

## v0.1.121 - Faceted mine resources

**Timestamp.** 2026-07-07 02:50:42 -07:00

**Action.** Continued the screenshot-backed graphics loop and shipped another AoE2-style resource-readability slice: gold and stone mines now keep the existing mound/shadow silhouette but add small light/dark facet planes and edge strokes. The change is pure presentation in `resourceRenderer.ts`; resource amounts, gathering, hit testing, selection, fog memory, simulation state, saves, and replays are unchanged. Version bumped to 0.1.121 and the changelog/spec/current graphics plan were updated.

**Code reviewer comments.** In-process adversarial review: the existing resource path already differentiated trees, berries, wildlife, and mines, so this slice stayed localized to the gold/stone branch instead of inventing a new renderer abstraction. The test contract intentionally requires both light and dark non-shadow fills plus edge strokes so the deposits become faceted mineral piles rather than just brighter blobs. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** The mine branch now derives a shaded color and a glint color from the resource tint, paints three small triangular facet planes over the existing rock lumps, and draws three dark edge strokes across the mound. `tests/phaser/resourceRenderer.test.ts` now covers the facet planes and edge strokes for both gold and stone. Visual evidence lives under `tmp/vision/mine-facets-before`, `tmp/vision/mine-facets-after`, and `tmp/vision/mine-facets-diff`; the canvas diff changed 825 / 983,040 pixels (0.084%), localized to visible mineral deposits.

**Reasoning.** Real Age of Empires II resource deposits are highly readable at a glance: gold and stone are not just colored circles, they have rock faces and bright/dark facets. This procedural version should evoke that readability while preserving original art, owner/resource tint semantics, and all existing gameplay contracts.

**Validation.** TDD red: `npx.cmd vitest run tests/phaser/resourceRenderer.test.ts` failed because gold/stone mines drew 0 facet triangles. Green: the same focused command passed with 12 tests after implementation. Visual validation captured the real default canvas before/after and a pixel diff. Full gates passed: `npm.cmd test` (217 files passed, 1 skipped; 1800 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** Remaining polish: per-civilization/architecture-set facade variation, attack/death feedback, richer weapon animation, per-unit/per-civilization visual variety, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## v0.1.120 - Minimap marker readability

**Timestamp.** 2026-07-07 02:23:26 -07:00

**Action.** Ran the screenshot-enabled vision harness against the current v0.1.119 build, then shipped the next AoE2-style polish slice: the minimap now renders as explicit layers instead of trusting projected entity ordering. Terrain draws first, live entity markers draw second with a dark backing, and fog plus the camera viewport draw last. The change is pure UI presentation in `minimap.ts`; camera click/drag mapping, simulation, fog state, saves, and replays are unchanged. Version bumped to 0.1.120 and the changelog/spec/current graphics plan were updated.

**Code reviewer comments.** In-process adversarial review: a first marker-backing-only patch passed the primitive-style contract but produced a 0-pixel minimap diff on the real default view because real render-state ordering could let later terrain cells overpaint entity markers. The review converted that visual failure into a stronger contract: minimap markers must draw after all terrain cells regardless of render-state ordering. Multi-CLI review was not run because this was a low-risk visual/UI renderer slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `drawMinimap` now performs a terrain pass, a non-terrain marker pass with `rgba(4, 8, 9, 0.72)` backing rectangles, then the existing visibility and viewport passes. New focused coverage in `tests/ui/minimap.test.ts` locks both the dark-backed marker styling and the ordering guarantee. Visual evidence lives under `tmp/vision/minimap-backing-before`, `tmp/vision/minimap-backing-after`, and `tmp/vision/minimap-backing-diff`; the minimap-only diff changed 875 / 35,200 pixels (2.486%), localized to the explored minimap blob.

**Reasoning.** Real Age of Empires II depends on a readable tactical minimap: terrain gives context, but unit/building/resource dots must still separate from that terrain and from fog. The project already had a top-down minimap and viewport rectangle, so the highest-value small step was not an isometric minimap rewrite; it was restoring the tactical layering hierarchy so markers remain visible at a glance.

**Validation.** Vision harness: `npm.cmd run playtest:llm -- --max-ticks 500 --decision-interval 250 --screenshot-every 250 --cost-budget 0.8 --out tmp/vision/llm-v0120-probe` ran 2 decisions / 500 ticks, accepted the opening train/build/gather/scout/rally commands, captured two checkpoints, and stopped on `cost-budget-exceeded` with both owners alive. TDD red: `npx.cmd vitest run tests/ui/minimap.test.ts` first failed on missing marker backing, then failed on marker/terrain ordering (`expected 3 to be greater than 4`), and passed after the two-pass renderer. Visual validation captured minimap-only before/after/diff images from a strict local Vite port. Full gates passed: `npm.cmd test` (217 files passed, 1 skipped; 1799 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** Remaining polish: per-civilization/architecture-set facade variation, attack/death feedback, richer weapon animation, per-unit/per-civilization visual variety, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## v0.1.119 — Terrain surface detail

**Timestamp.** 2026-07-07 01:47:18 -07:00

**Action.** Continued the screenshot-backed graphics loop on the real default `aoe2-prototype` view and shipped the next AoE2-style polish slice: terrain diamonds now draw deterministic kind-specific interior marks. Grass, forest, and hill tiles get faint speckles; water tiles get short translucent wave strokes. The change is pure presentation in `terrainRenderer.ts`; simulation, pathing, fog, selection, camera, saves, and replays are unchanged. Version bumped to 0.1.119 and the changelog/spec/current graphics plan were updated.

**Code reviewer comments.** In-process adversarial review: `drawIsoBuilding` already had a ground-contact shadow, so the originally considered building-shadow slice would have duplicated existing behavior. The visual weak point shifted to flat terrain diamonds. The review kept the implementation deterministic and bounded: two tiny marks per tile, derived from cell coordinates with no `Math.random` or time input, drawn inside the diamond and under entities/fog/selection. The existing edge-feather test was tightened so it checks blend-colour feather styles rather than assuming every `fillCircle` is a boundary speck. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `drawTerrainCell` now calls a surface-detail helper after the base diamond fill and before edge feathering. Non-water terrain uses translucent dots; water uses short line strokes. Focused tests in `tests/phaser/terrainTexture.test.ts` cover the new red/green contract and keep the old deterministic, diamond, tint, and edge-feather contracts green. Visual evidence lives under `tmp/vision/foot-units-after`, `tmp/vision/terrain-detail-after`, and `tmp/vision/terrain-detail-diff`; the canvas diff was 5,878 / 983,040 changed pixels (0.598%), localized to terrain.

**Reasoning.** Real Age of Empires II terrain is richly textured even when individual units are tiny. The procedural renderer should not import sprite assets, but small deterministic surface marks give grass, hills, forests, and water a more natural read than flat diamond fills while preserving the current simulation and projection contracts.

**Validation.** TDD red: `npx.cmd vitest run tests/phaser/terrainTexture.test.ts` failed because same-kind grass terrain produced 0 interior detail marks. Green: the same focused command passed with 17 tests after implementation. Visual validation captured the real default view before/after and a pixel diff. Full gates passed: `npm.cmd test` (216 files passed, 1 skipped; 1797 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** Remaining isometric polish: per-civilization/architecture-set facade variation, attack/death feedback, richer weapon animation, per-unit/per-civilization visual variety, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## v0.1.118 — Foot-unit limb detail

**Timestamp.** 2026-07-07 01:20:14 -07:00

**Action.** Continued the screenshot-backed vision loop on the real default `aoe2-prototype` view and shipped the next AoE2-style graphics polish slice: villager, infantry, and archer silhouettes now include planted leg strokes plus small visible foot marks below the torso. The change is pure presentation in `unitRenderer.ts`; movement, hit testing, selection rings, health bars, simulation state, saves, and replays are unchanged. Version bumped to 0.1.118 and the changelog/spec/current isometric plan were updated.

**Code reviewer comments.** In-process adversarial review: the first implementation satisfied the primitive-count test but the default-view pixel diff moved only 7 / 983,040 pixels because the torso drew over the foot marks. The review therefore converted the visual failure into a draw-order contract: foot marks must be drawn after the torso for foot humanoids so they survive default-zoom overdraw. A documentation review updated the current plan so "fuller unit figures" no longer refers to the now-shipped planted-foot detail. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `drawFootLegs` now draws the lower leg strokes behind the body, while `drawFootMarks` draws two small owner-outline foot marks after the torso for villager, infantry, and archer roles. Focused tests in `tests/phaser/unitRenderer.test.ts` cover the red/green contract: the old renderer had no planted legs, then had hidden feet, and the final renderer passes all 48 unit-renderer tests. Visual evidence lives under `tmp/vision/wildlife-species-after`, `tmp/vision/foot-units-after`, and `tmp/vision/foot-units-diff`; the final canvas diff was 28 / 983,040 changed pixels (0.003%), localized to the two on-screen foot-unit silhouettes.

**Reasoning.** Real Age of Empires II foot units read as small upright people because the lower-body silhouette remains visible even at default zoom. The procedural renderer should keep that silhouette hierarchy without changing mechanics or importing copyrighted sprites. Drawing leg strokes behind the body but the small feet after the torso gives the desired read while keeping every primitive within the existing unit bounding circle.

**Validation.** TDD red: `npx.cmd vitest run tests/phaser/unitRenderer.test.ts` first failed because villager/infantry/archer had no planted legs; after the first implementation it passed but the visual diff exposed overdraw. A second red test then failed because foot marks were absent after the torso (`expected 0 to be greater than or equal to 2`), and the final focused run passed with 48 tests. Visual validation captured the real default view before/after and a pixel diff. Full gates passed: `npm.cmd test` (216 files passed, 1 skipped; 1796 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** Remaining isometric polish: per-civilization/architecture-set facade variation, attack/death feedback, richer weapon animation, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## v0.1.117 — Wildlife species silhouettes

**Timestamp.** 2026-07-07 00:54:23 -07:00

**Action.** Continued the screenshot-backed vision loop on the real default `aoe2-prototype` view and a bounded `playtest:llm` opening, then shipped the next AoE2-style graphics polish slice: sheep, boar, and wolves no longer share a single generic body+head animal silhouette. Sheep draw wool clumps and short legs, boar draw tusks, and wolves draw pointed ears plus a tail. The change is pure presentation in `resourceRenderer.ts`; resource ownership/tint semantics, hit testing, selection, gathering, combat, fog memory, simulation state, saves, and replays are unchanged. Version bumped to 0.1.117 and the changelog/spec/current isometric plan were updated.

**Code reviewer comments.** In-process adversarial review: the real first-screen capture plus the 250-tick LLM checkpoint showed the Town Center and terrain were readable after v0.1.116, while sheep/boar/wolf still read as tiny generic marks. The review scope therefore stayed render-only and species-local. A contract check verified the implementation does not read or mutate simulation state, does not change `ProjectedEntityView`, preserves the existing tint path used for sheep ownership, and leaves other resources on their existing draw paths. Documentation review updated the current plan so wildlife species differentiation is no longer listed as future work. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `drawResourceEntity` now branches inside the existing wildlife path: sheep get rounded wool detail, boar get tusks and a heavier profile, and wolves get leg lines, ears, and a tail. Focused tests in `tests/phaser/resourceRenderer.test.ts` cover the red/green contract: the old generic wildlife path failed on missing wool/legs/tusks/ears/tail and identical primitive signatures; the new renderer passes. Visual evidence lives under `tmp/vision/default-view-v0116-current`, `tmp/vision/llm-v0116-current-screenshots`, `tmp/vision/wildlife-species-after`, and `tmp/vision/wildlife-species-diff`; the canvas diff was 589 / 983,040 changed pixels (0.060%), localized to wildlife positions.

**Reasoning.** Real Age of Empires II relies on tiny but distinct animal silhouettes: sheep are round and woolly, boar read heavier and dangerous, and wolves are angular/predatory. This project still uses original procedural primitives, but it should borrow that silhouette hierarchy so the map is readable at default zoom and the vision harness sees more meaningful objects.

**Validation.** TDD red: `npx.cmd vitest run tests/phaser/resourceRenderer.test.ts` failed with 4 expected failures because sheep had only 1 circle, boar/wolf had 0 triangles, and all wildlife shared 1 primitive signature. Green: the same focused command passed with 11 tests. Visual validation captured the real default view before/after and a pixel diff. Full gates passed: `npm.cmd test` (216 files passed, 1 skipped; 1793 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** The bounded vision-harness opening run accepted all 7 commands (house placement, two gather orders, scout move, TC rally, and two villager queues), ran 250 ticks, cost $0.7786, and ended in-progress with owners 1 and 2 alive. Remaining isometric polish after v0.1.118: per-civ/architecture-set facade variation, attack/death feedback, richer weapon animation, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.

## v0.1.116 — Isometric building material breakup

**Timestamp.** 2026-07-07 00:36:55 -07:00

**Action.** Used the new screenshot-backed vision loop on the real default `aoe2-prototype` view and shipped the next AoE2-style graphics polish slice: completed tall isometric buildings now draw subtle base-parallel masonry course lines across both visible wall faces and sparse diagonal roof tile seams inside the roof diamond. The change is pure presentation and localized to the Phaser building renderer; footprints, hit testing, health bars, selection, construction, fog memory, simulation state, saves, and replays are unchanged. Version bumped to 0.1.116 and the changelog/spec/current isometric plan were updated.

**Code reviewer comments.** In-process adversarial review: visual default-view comparison confirmed the original issue was the first-viewport Town Center reading as one large flat tan block, so the implementation should add small material cues without changing game mechanics or the art style. A self-review finding caught that the shared `drawIsoBuilding` path would also add completed-building masonry/roof seams to tall construction stubs (Castle/Wonder), which would make incomplete buildings read as finished; fixed by adding a `materialDetail` style flag and passing `false` from the construction-stub path, with a regression test in `tests/phaser/buildingRenderer.test.ts`. Documentation review grep found a stale current-plan reference to wall banding as future work after the feature shipped; updated the summary and isometric design thread so only per-civ/architecture-set texture variation remains in that slot. Multi-CLI review was not run because this was a low-risk render-only slice with no persistence, security, money, dependency, concurrency, or data-loss blast radius.

**Result.** `isoBuilding.ts` now exposes pure geometry helpers for wall-course and roof-tile segments and draws the material detail only for completed tall buildings. `buildingRenderer.ts` keeps construction stubs visually plain. Focused tests cover the new geometry contracts and the construction-stub exclusion. The visual protocol captured before/after/default-view evidence under `tmp/vision/default-view-before`, `tmp/vision/default-view-after`, and `tmp/vision/default-view-diff`; the canvas diff was 2,719 / 983,040 changed pixels (0.277%), confined to the building area.

**Reasoning.** Real Age of Empires II buildings carry a lot of small wall and roof texture that helps them read as built structures at default zoom. The current procedural art should not chase exact copyrighted assets, but it can borrow the composition lesson: large flat planes need restrained, repeatable material cues. Lines were chosen over new filled shapes so windows, doors, owner tint, roof accents, HP bars, fog, selection, and construction states keep their existing visual priority.

**Validation.** `npx.cmd vitest run tests/phaser/isoBuilding.test.ts` failed before implementation on missing helpers and zero material-detail lines, then passed after implementation. `npx.cmd vitest run tests/phaser/isoBuilding.test.ts tests/phaser/buildingRenderer.test.ts` failed before the construction-stub fix because a castle construction stub produced 11 completed-building line segments, then passed after `materialDetail: false`. Full gates passed: `npm.cmd test` (216 files passed, 1 skipped; 1789 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Dependency audit was not required because no dependency surface changed.

**Notes.** The LLM playtest harness completed a bounded opening pass before the visual fix (`playtest:llm`, max 250 ticks, screenshot every 250): it accepted 5 commands and left the game in progress, with no game-rule defects identified for this slice. Remaining isometric polish: per-civ/architecture-set facade variation, fuller unit and wildlife figures with attack/death feedback, and elevation/cliffs once the engine exposes the per-cell-elevation render contract.
