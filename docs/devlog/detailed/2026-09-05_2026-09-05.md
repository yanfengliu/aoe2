# Devlog — detailed, from 2026-09-05

The previous file (`2026-09-02_2026-09-05.md`) stood at 492 lines and the entry below would have carried it past the 500-line budget, so it stays as the archive with its end date already correct and this file starts today. Rename this file's end date as it grows.

## 2026-09-05 — Two test files left the 500-line cliff

**Action.** `tests/rendering/aoeVoxelUnitAnimation.test.ts` and `tests/simulation/aiPlayer.test.ts` both sat at EXACTLY 500 lines, the hard cap `tests/architecture/fileSizeBudget.test.ts` enforces (`HARD_LIMIT = 500`, offenders are `lines > cap`), so each was one added line from a red gate — and red before the suite ran, on a file nobody had touched. Both were split by role, and only the documentation and the one stale pointer were written here; the five test files were produced by two workers, inspected and accepted by the coordinator, and ship unmodified.

**The line counts, and which half kept the original name.** Rendering: 500 → 289 `aoeVoxelUnitAnimation.test.ts` (locomotion sampling — gait phase, body direction, speed, the locomotion weight, the identity phase that seeds them) + 181 `aoeVoxelUnitAnimationChannels.test.ts` (what layers ON TOP of baked geometry: the ambient mechanisms and the attack/strike blend) + 91 `tests/rendering/helpers/voxelUnitAnimationFixtures.ts` (the six builders both halves need). Simulation: 500 → 251 `aiPlayer.test.ts` (the Slice 10 planner core) + 281 `aiPlayerMonksAndWonder.test.ts` (FU4 Monks and the Wonder victory push), with no helper module because the imports divided cleanly between the two halves.

**Which half keeps the original filename is decided by the live pointers, not by which half is bigger.** Rendering's locomotion half keeps `aoeVoxelUnitAnimation.test.ts` because `docs/learning/gate-proofs.md:31` cites it for the re-timed "stopped weight has started to decay at 300 ms" assertion and `docs/debugging/2026-09-05-walk-legs.md:36` and `:40` cite it twice more — all three about locomotion, all three still resolving. Simulation's planner half keeps `aiPlayer.test.ts` because `src/game/simulation/aiEconomyPlan.ts:18` names its `ai-planner-fixture` runs and `src/game/simulation/bridge/systems/aiSystemTypes.ts:163` names its "ages up through the ages within a generous tick budget" case — both in `src/`, both still resolving. That left exactly one pointer stale: `docs/architecture/decisions.md:159` (KAD-0008) said the monk one-tick delay is covered by `aiPlayer.test.ts` with the `ai-monk-relic-fixture`; that case is `defers AI Monk task assignment through the command queue`, which moved, so the path is now `tests/simulation/aiPlayerMonksAndWonder.test.ts`. Confirmed before editing: `ai-monk-relic-fixture` appears six times in the new file and zero times in the old one.

**The verification was block-level, not title-level, and it was redone independently here.** A title diff proves only that the titles match, which is exactly what a split that silently drops an assertion would also show. The coordinator's acceptance compared the `it` bodies block by block against the base revision: 17 rendering and 24 simulation, none added, none dropped, describes intact. Redone here from the base blob with a parser-free line-multiset difference (`git show HEAD:<file>` against the union of the split files, blank lines ignored, CRLF normalised): the simulation pair has **zero** non-blank lines present in the base and missing from the split — every line of the original 500 survives — and its 29 added lines are all new header comments plus two import lines. The rendering pair differs on exactly ten lines, and all ten are the move into the helpers module: three relocated import lines, one relocated type-import specifier, and the six builders (`unit`, `forwardRadians`, `signedAngleDelta`, `movingState`, `part`, `maxMatrixDelta`, `ORIGIN_HISTORY_FIELDS`) gaining `export`, with `movingState`'s signature re-wrapped across three lines. No test body line changed in either pair.

**A brace-balancing extractor was the wrong instrument, and said so loudly enough to catch.** The first attempt pulled each `it(...)` block by paren balance and reported 22 of 24 simulation blocks matching, with two "differing" — but the printed diff showed one block swallowing the whole `describe('FU4 AI Monks', …)` that followed it, which is a parser that ran off the end (an apostrophe in a comment reads as a string delimiter), not a content change. It also reported every simulation body differing until `\r` was normalised, because `git show` hands back the LF blob while the working tree is CRLF. Both were instrument faults; the line-multiset check has neither failure mode. Worth remembering: the parser's first answer, "22 of 24 match", is a plausible-looking number that a reader would not have questioned.

**MEASURED AND NOT FIXED: the cap is being met by trimming to fit rather than by splitting.** Measured with the gate's own instrument — `fileSizeBudget.test.ts`'s `ROOTS` (`src`, `tests`, `scripts`), `SKIP_DIRS`, `FILE_EXTS` (`.ts`, `.tsx`, `.mjs`) and its `lineCount` (`wc -l` semantics), over 1,205 files. After this split, **six** files sit at exactly 500: `src/game/simulation/bridge/humanInputOps.ts`, `pureHelpers.ts`, `registerAllSystems.ts`, `selectionInputOps.ts`, `wireBridgeOps.ts`, and `src/game/simulation/uniqueTechnologies.ts`. **Nineteen** sit within ten lines of the cap reading the band as 490-500 inclusive (eighteen if read as 491-500), and **ten** of those nineteen are under `src/game/simulation/bridge/` — that one directory holds half the files on the cliff. All six at exactly 500 are behaviour-bearing `src/`; none is a test. The only files over 500 are the two legacy-listed scripts (`content-lib.mjs` 633, `playtest-llm.mjs` 531).

**The dictated summary line's figures were pre-split, and were corrected before landing.** The line handed to this session read "seven files sit at exactly 500 and 21 within ten lines of it, nine of those in `src/game/simulation/bridge/`", and its own closing clause then said "the six still on it" — internally inconsistent. Measured at HEAD (`6523d8a0`, the committed tree before this split) the figures are **eight** at exactly 500 (the six above plus the two test files), 21 within 490-500, and ten under `bridge/`; measured on the tree being committed they are six, 19 and ten. So "21 within ten" was the pre-split number, "seven" matched neither tree, and "nine" was wrong on both. The summary line ships with six / 19 / ten and is otherwise the dictated prose. A devlog is history a later session reads as fact, and this repo's canon is explicit that the instrument is verified before the measurement is trusted; shipping a number measured to be wrong would have been the cheaper and worse option.

**The six were deliberately not attempted.** They are all behaviour-bearing `src/` — five of them the simulation bridge, which a play session was exercising while this task ran — so splitting them is a change to code under active use by another session, not a test-file reorganisation, and it belongs in its own task against a quiet tree. Nothing here shrinks them and nothing here is blocked by them; the measurement is recorded so the next session does not have to rediscover the shape of the problem.

**Gate: red first, and the wrapper reported it as green.** `npm run verify` (`content:validate && test && typecheck && test:browser && lint && build`, chained with `&&`, so its exit code is the answer) was run in the background as `npm run verify > log 2>&1; echo "VERIFY_EXIT_CODE=$?" >> log; echo done`. The harness reported that command as exit 0 — because the wrapper ends in `echo done`, whose status is the shell's — while the recorded `VERIFY_EXIT_CODE` was **1**. The unit half was green (478 test files passed / 1 skipped, 3,458 tests passed / 3 skipped) and `test:browser` was red: 2 failed, 189 passed. `eight-player-skirmish.spec.ts:15` timed out at 5,000 ms waiting for `hudState.tick` to exceed 0, and `play-opening.spec.ts:98` ("the HUD clock leaves 00:00 within seconds of real time") read `tickAfter` 14 against `tickBefore` 14 — both liveness assertions failing because the simulation had not advanced, on a machine simultaneously running this 20-minute gate and a play session. Neither can be reached by this change: all five moved files are vitest suites under `tests/rendering/` and `tests/simulation/`, which no Playwright spec imports. Re-run alone against the same build, the two passed in 1.2 minutes (exit 0).

**Gate: green on the re-run, which is the run this commit stands on.** Nothing was weakened and no budget was moved to get there — the second run is the same command on the same tree. `VERIFY_EXIT_CODE=0`: content validation clean; 478 test files passed / 1 skipped (479), 3,458 tests passed / 3 skipped (3,461) in 507.46 s; typecheck clean; `test:browser` **191 passed** in 17.6 min with none failed, including the two that had been red; lint clean; build 808 modules in 4.65 s. Worth keeping: the first run's red is the repo's known loaded-machine browser class, and the wrapper's exit code claimed the opposite of the truth — the log's recorded status is what caught it.

**The gate verified a superset of what this commit contains.** The working tree also holds separate in-flight register work that is under review and stays uncommitted: `docs/learning/defect-register.md` (modified), `docs/learning/defect-register-past.md` (new) and `tests/architecture/defectRegisterRollover.test.ts` (new). That last one is a test file under `tests/`, so `npm run verify` collected and ran it. The gate therefore proves the split is green in the presence of that work; it does not separately prove the committed eight paths green in isolation, and nothing was staged from the register work — the staged list was read back with `git diff --cached --name-only` and matched the eight intended paths exactly.

**Notes.** No engine debug tool applies: `replay:inspect`, `diffBundles`/`forkAt` and the path-queue/occupancy probes all read simulation ground truth from a recorded run, and the question here is whether two files' text was rearranged without changing what it asserts, which is answered by reading the base blob and the working tree. No probe was written under `tmp/probes/`; the three comparison scripts ran from the session scratchpad and are task-run evidence, not repository inputs. No version bump and no changelog line: a pure structural split ships nothing user-visible, so `package.json` stays at 0.3.207, and `ARCHITECTURE.md` and the drift log are untouched because moving test cases between test files is not a structural change. Threads checked at the end of the task as the convention requires: `docs/threads/current` holds `concurrency-review-2026-09-02` and `lumber-camp-routing`, this task advanced neither objective, so neither took a status line and neither moved. The tree was shared with another session throughout — HEAD moved from `bf5ff47f` to `6523d8a0` (a peer compacting `docs/devlog/summary.md`) during the work — so `summary.md` was re-read immediately before editing and every stage was by explicit pathspec, never `git add -A`, `-a` or `.`.

**Seen, not fixed.** `docs/debugging/2026-09-05-walk-legs.md:36` still ends "That file now sits at exactly 500 lines, the hard cap; the next addition to it has to split it." Its path pointer is still correct and still resolves, but the sentence's fact is now superseded — the file is 289 lines and the split it predicted has happened. Left as written rather than edited, because it is a dated session record of what was true that morning and the convention's "update the old doc" applies to an invalidated CONCLUSION, not to a fact the next entry supersedes; flagged here so a reader who lands on it knows. **Superseded the same day** — the next entry corrects that sentence in place on the coordinator's direction, keeping the paragraph and naming the commit that split the file.

## 2026-09-05 — The defect register rolls over, and a live defect never rolls over

**Action.** `docs/learning/defect-register.md` had reached 1,437 lines and 58 entries and nothing had ever split it, while every other long-lived document here already rolls over: the devlog archives its detailed file past 500 lines, engine-feedback splits `current`/`past`, threads split `current`/`done`. The active file now holds the 12 most recent CLOSED entries plus every OPEN one whatever its age; the rest is in `docs/learning/defect-register-past.md`, moved verbatim, and the rollover is done by the task whose new entry overflows the file, in that task's own commit. The register split, its archive and `tests/architecture/defectRegisterRollover.test.ts` were produced by a worker, inspected and accepted by the coordinator, and ship unmodified but for one two-character correction named below; the rule, the gate proof, the debugging-log correction and this entry were written here.

**An OPEN entry never rolls over, and the first attempt got exactly that wrong.** The rollover that produced the archive read `(SUPERSEDED … What is recorded here is still TRUE and still unfixed …)`, `(PARTLY FIXED: 1 of 4 seeds resolved then …)` and `(LATENT, not fixed)` as closures and buried three LIVE defects. They were moved back to the active register with `, OPEN` added inside the trailing group and nothing else changed, and the archive's own header records that it happened. The gate now refuses an archived marker that leaves the defect live, and refuses — as AMBIGUOUS rather than closed — any heading that says open, unfixed or reopened in a spelling the marker parser cannot read. That direction is the load-bearing one: reading such a heading as closed both passes the archive check and makes the entry eligible to roll over.

**The rule met its first real use in this same session, and the gate caught the rollover being got wrong three times.** Each of the two entries that follow this one adds a closed entry to a file already holding exactly 12 closed, so each had to move one out as part of its own commit. That is the rule landing on its own first use rather than being written and filed.

**The oldest closed entry is not always the one to move, which the rule now says out loud.** The cap check's failure message sorts the closed entries by date and names the oldest as the overflow, but the ASSERTION is a count — the gate's own bounds say so — and the file's genuinely oldest closed entry, `2026-09-03 — The AI's build order stalls … (PROVEN, fix measured and NOT shipped)`, cannot be archived at all: `NOT shipped` is one of the three spellings the archive check refuses. It is deliberately allowed where it stands, in front of the reader, and goes red the moment it is buried. So the entry to move is the oldest closed one the gate will ACCEPT, and this session moved two of the 2026-09-04 block from the bottom of that date's run upward.

**The gate was made to go red five ways, each with its own message, and the files restored byte-for-byte.** Driven by the worker's own `tmp/probes/mutate.mjs` through `runMutation.sh`, which applies one defect, runs the gate and restores all three files. The full text of each failure is in `docs/learning/gate-proofs.md`; the one worth repeating here is `bypass1`, because it is the failure mode a reader would never guess: eight extra closed entries appended with their headings indented ONE SPACE. CommonMark accepts up to three leading spaces, so they render as real entries, and the strict `## ` parser cannot see any of them — the file holds 20 closed against a cap of 12 and the cap check counts 12. The gate's answer is not a smarter regex but a second, deliberately looser probe (`/^\s{0,3}#{2}\s/`) cross-checked against the strict one, and a refusal to return any parse the two disagree on: "every count below this line would be a number about the parser rather than about the register". After the five reds, `sha256sum` on the three files matched their pre-mutation values exactly and the gate was green in 19 ms, so the greens are on the same bytes the reds were taken against.

**The one two-character edit, and what it cost.** One heading read `… but it is not what was stopping the army., OPEN)` — a comma after a full stop — and was corrected to `… the army. OPEN)`. That is the only change made to any file a worker produced here, and it could not be made alone: the gate PINS twelve real headings verbatim, asserting each still exists in one of the two files, precisely so that a paraphrase or a curly apostrophe cannot masquerade as coverage. Changing the register heading stranded its pin, and the gate said so — `pinned as a real heading but no entry in either file has this exact text` — so the pin string was updated to match, which is the maintenance the test's own comment prescribes ("Copy the heading out of the register, or mark the case synthetic"). Nothing was weakened: the pin still asserts `verdict: 'open'` and `archivable: false`. The mutation `archived-open` had already demonstrated this coupling independently, failing on the ambiguous marker AND on the stranded pin at once.

**A correction to the previous entry, not a new finding.** `docs/debugging/2026-09-05-walk-legs.md:36` was left alone by the entry above as a dated record. The coordinator's direction here is that its closing sentence states a fact that is now false — "That file now sits at exactly 500 lines, the hard cap; the next addition to it has to split it" — and that a reader who lands on it is misled about a file that is 289 lines. The sentence now says the file sat at 500 when the log was written and names `90915ad4` as the commit that split it that evening, keeping the paragraph and the rest of its reasoning intact.

**Gate: ONE run over the combined tree, and it went red for a real reason that belonged to a fourth change.** The working tree held four finished, independently accepted changes: this register split, the playtest bundle streamer and the foundation build route (the two entries below), and a HUD change that makes every painted HUD surface swallow the click instead of letting it through to the map. `npm run build` first, because `dist/` was stale and mixed, then one `npm run verify` — `content:validate && test && typecheck && test:browser && lint && build`, chained with `&&`, so the exit code is the answer, and run unwrapped so no trailing `echo` could report its own status instead. **Exit 1.** Content validation clean; 480 test files passed / 1 skipped (481) and 3,470 tests passed / 3 skipped (3,473) in 334 s; typecheck clean; `test:browser` 192 passed, **1 failed**, 2 skipped in 16.7 min — and because the chain stops at the first failure, `lint` and `build` never ran in that invocation (the standalone `npm run build` before it had already passed, 808 modules).

**The failure was NOT the known loaded-machine class, and re-running it alone is what proved that.** `trade-route-reach.spec.ts` failed on `expect(locator).toContainText` — `Trading` wanted, `Idle` read, nine polls over five seconds — which is character-for-character the symptom of this repo's one historically flaky spec (register, 2026-09-04). It failed AGAIN in isolation on an idle machine. So it was diagnosed rather than re-run a third time: a probe under `tmp/probes/` put the spec's own click point through `document.elementFromPoint` on the live page and it resolved to `div.hud-panel.hud-panel--selection` — at the suite's 800x600 viewport the panel spans y 352-588, the canvas spans y 60-540, and the spec centres the camera on the enemy Market and right-clicks one tile past it at (400, 357.6), five pixels inside the bar. Then a single-variable A/B in one run, same build, same page: arm A (shipped tree) hit `div.hud-panel.hud-panel--selection`, activity `Idle`, cart command `none`; arm B, with `.hud-panel--selection { pointer-events: none }` injected — the exact rule the HUD change deleted — hit `canvas.voxel-world-canvas`, activity `Trading with Market`, cart command `trade -> 44,24`. The HUD change causes it, and it is the HUD fix working as designed: the click really is on an opaque panel, and what it exposes is that at 800x600 the region immediately below the camera centre is HUD, which is the standing "the bar must never reach the centre of the canvas" rule one tile further out than the rule measures. The HUD change is therefore NOT committed and stays in the working tree for its owner; nothing in these three commits depends on it.

**So the gate was run a second time, over exactly the tree being committed.** The two CSS files were restored to their HEAD bytes (`git diff` on them empty) and the HUD spec renamed to `_hud-…spec.ts`, which `playwright.config.ts` ignores by `testIgnore`, so the control tree is HEAD plus these three changes and nothing else; the worker's four files were saved with their SHA-256 first and restored afterwards. That second `npm run verify` is the run these commits stand on and its result is recorded in the last entry of this file. Nothing was weakened, no assertion relaxed and no budget moved in either run.

**No version bump and no changelog line for this commit:** a register reorganisation and a rule ship nothing user-visible. `ARCHITECTURE.md` and the drift log are untouched — splitting a markdown document and adding an architecture TEST changes no module boundary.

## 2026-09-05 — A finished playtest no longer throws its own recording away

**Action.** `npm run playtest` simulated a whole match — the default is 30,000 ticks, tens of minutes of wall time — and then died on its last line with `RangeError: Invalid string length`, so the bundle was never written and the run was gone: nothing to replay, nothing to inspect, nothing to run the oracles over. `src/game/playtest/bundleIo.ts` and its test were produced by a worker, inspected and accepted by the coordinator, and ship unmodified; the register entry, the devlog and the correction below were written here.

**The ceiling is V8's, and the fix streams rather than shrinks.** `buffer.constants.MAX_STRING_LENGTH` is 536,870,888 characters on Node 24, and it bounds `JSON.stringify` and `JSON.parse` alike because each works on ONE string. Measured on 2026-09-03's 20,001-tick corpus bundle: `ticks[]` is 95.9% of the file at a mean 15.7 KB per tick entry (its `diff` 84%, `metrics` 16%), the whole file is 313 MB, and entries grow as the match fills with units — so the 30,000-tick default lands past the cap. `writeBundleFile` and `readBundleFile` never hold the whole document as one string in either direction; the largest string either side builds is ONE array element (a tick entry, ~16 KB) or one non-array top-level value (`initialSnapshot`, ~0.5 MB). **The format is untouched.** The file is ordinary JSON, byte-identical to what `JSON.stringify(bundle)` produces, which is why bundles recorded before this still load and every other reader of bundle JSON still works. Nothing was sharded, re-encoded or thinned — that distinction is the whole point, because the previous mitigation was thinning: dropping `null, 2` in v0.2.x cost about 40% and MOVED the wall.

**The read side is the half that would have been missed.** A bundle that was written can still be unreadable, and four scripts read one with `JSON.parse(readFileSync(path, 'utf8'))`: `replay-inspect.mjs`, `run-oracles.mjs`, `playtest-corpus.mjs` and `playtest-findings.mjs`. The corpus one is the sharpest — it read the bundle to check age progression, so a long enough run turned the progression gate into an ERROR row on exactly the runs that gate exists to check. All five call sites now go through the pair; `playtest-findings.mjs` goes through it BOTH ways, since it injects agent markers and writes the bundle back through a temp file and a rename.

**The gate refuses to report "did not run" as "passed", which is the part worth copying.** `tests/playtest/bundleIo.test.ts` sizes its oversized payload from `buffer.constants.MAX_STRING_LENGTH` at RUN TIME rather than from a baked-in number, so it keeps crossing the cap if a future Node moves it, and builds it from one shared 8 MB string referenced many times — 8 MB of heap, still past the cap when serialized. Three controls then run before the claim: the written file's size must exceed the cap, `JSON.stringify` on the same payload must throw `Invalid string length`, and `readFileSync(path, 'utf8')` on the written file must throw. A payload that quietly shrank under the cap fails there instead of passing everywhere. A sixth case binds the CALL SITE by reading the text of `scripts/playtest.mjs` and requiring `writeBundleFile(...)` and the absence of `JSON.stringify(result.bundle)`, because the module can be correct while the CLI still calls the old thing — which is exactly how the run was lost.

**Bound, and it is a real one.** The payloads are SYNTHETIC. They prove the serializer survives a document past the cap; they prove nothing about the simulation that produces one. A recorded 30,000-tick run is the only thing that proves the game still records, and that is `npm run playtest` plus `npm run replay:inspect`, not this file. The call-site case is a source check that names one script; the four readers are wired but not pinned that way.

**Attribution corrected before landing.** The drafted register entry called this owner-reported. It was not: the owner reported none of this session's play findings, and the standing loop found them by playing the game. The entry says so.

**The register rolled over, and the gate proved itself on the real defect rather than on a mutation.** The active file already held exactly 12 closed entries, the cap. Writing this entry first, before moving anything, turned the gate red on the genuine overflow: `docs/learning/defect-register.md holds 13 closed entries and the cap is 12 (8 open entries do not count)`. Its message then named as the overflow candidate `2026-09-03 — The AI's build order stalls … (PROVEN, fix measured and NOT shipped)` — the oldest closed entry by date, and the one entry that CANNOT be archived, since `NOT shipped` is one of the three spellings the archive check refuses. So `2026-09-04 — The suite's one flaky spec, and what it was really asking (FIXED)` moved instead, verbatim, to the top of the archive's entry list where its position relative to its neighbours is preserved. That entry is about `trade-route-reach.spec.ts`, which went red in this session's first gate run — for a different and separately proven cause, the HUD change, not the wall-clock race that entry closed.

**Gate.** The one `npm run verify` over the combined tree, and the control run over exactly what is being committed, are both recorded in the entry above. No version bump and no changelog line: this is internal tooling and the changelog is for player-visible change. `ARCHITECTURE.md` and the drift log are untouched — a serializer swap behind two functions moves no boundary; every call site keeps the same shape and the file format is byte-identical.
