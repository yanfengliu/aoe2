# 2026-07-08

## Recursive loop closure (Track A): engine-1.6 alignment, episodic memory, playtest:recursive

**Timestamp.** 2026-07-08 (session continuing 2026-07-07 engine work).

**Action.** Closed the recursive self-improvement loop the prior thread deferred. (1) Engine 1.6.0 alignment: `classifyFinding` routes the widened `nextAction` vocabulary (`improveHarness`/`fileEngineFeedback`/`addRegression`/`updateDesign` -> proposal, never auto-fix); oracle/conformance emission stamps `minimalImprovementFindingSchemaVersion` so v1-vocabulary payloads keep schema version 1 and the existing marker pins stay green; oracle-sourced ledger findings now carry `verificationStatus: 'verified'` + `verificationMethod: 'metric'` ONLY under strong replay self-check (validated with the engine's strict `requireVerificationEvidence` mode; weak evidence downgrades to `unverified`). (2) Episodic memory: `selectKnownIssues` renders a prior ledger's open findings into a "Known open issues" section in both agent prompts via `LlmAgentConfig.knownIssues`; `playtest:llm --known-findings <ledger.json>` (validated before server/browser startup). (3) `playtest:recursive` (scripts/playtest-recursive.mjs + src/game/playtest/recursivePass.ts): one bounded pass chaining run -> ledger (--oracles) -> `selectLedgerFixCandidate` -> propose-fix -> opt-in `--apply` applyAndGate on a `recursive/<seed>-<stamp>` branch -> rerun -> prove-fixed -> engine `createImprovementRunManifest` pass manifest with artifacts/gates/cost/outcome. Proposal-only is the default; a proven fix leaves the branch push-ready (never merged); an unproven fix reverts. (4) Legacy `playtest:llm-auto-fix` header marks it the engineHalt crash fast-path. Shared `bundleEndTick.ts` repair helper extracted; `propose-fix` proposal dirs slugified; `critical` severity now maps to `high` in fix-candidate selection.

**Code reviewer comments.** Codex CLI (gpt-5.5 xhigh) + one in-process adversarial subagent with live probes (multi-CLI escalation: agent-loop + apply-to-worktree machinery). Two independent HIGHs converged on prove-fixed soundness: oracle identity keys embed run-specific ticks/messages so a drifting rerun could fake resolution under a fresh identity, and ledger source priority (markers > oracles) could shadow persisting violations out of the rerun ledger entirely. Fixed with oracle-granularity proving over the union of rerun-ledger findings and a fresh endTick-repaired oracle sweep (`proveFixOutcome`, unit-tested incl. fresh-identity and marker-shadow cases). Codex MEDIUMs: pass could spend ~2x its budget (now split run/rerun) and undercounted cost (manifest documents envelope-only metering); invalid `--known-findings` leaked Vite/Chromium (validated pre-startup). In-process MEDIUMs: prove logic moved out of untested script code; unsanitized oracle path segments slugified in both scripts; DESIGN aligned with implementation. LOWs: manifest attribution, silent cost probe, `return finish(...)`, whitespace-collapsed known-issue lines, seed/branch sanitization, pid-stamped pass dirs. Synthesis: `docs/threads/current/recursive-loop-closure/2026-07-07/1/REVIEW.md`.

**Result.** The loop is closed and bounded: `npm run playtest:recursive` executes run -> record -> find -> verify -> classify -> propose (-> apply -> gate -> rerun -> prove) -> manifest in one command, with false-proven structurally unreachable for same-oracle recurrences and every pass leaving a machine-readable engine run manifest the next pass (or agent) can read. Episodic memory stops agents rediscovering ledgered findings.

**Reasoning.** This is the reference vertical slice for civ-engine's cross-game recursive-improvement contract (its thread's Phase 1/Track A); aoe2 was one integration from closure. Oracle-granularity proving exists because identity-level matching inverts the failure direction under rerun nondeterminism — the loop's core guarantee is that "proven" is hard to fake.

**Validation.** TDD red-first for the alignment (3 failing tests), knownIssues/prompt sections (2 failing + module load), and recursivePass (module load, then oracle-granularity rewrite after review). Focused suites 312 passed + 1 skipped (35 files); `tsc --noEmit` clean. Full gates green before commit: `npm test` (1846 passed + 2 skipped), `npm run typecheck`, `npm run lint`, `npm run build`. Harness/docs only — no gameplay, package version, or changelog change (loop-work precedent). npm audit n/a (no dependency change; engine symlink version moved 1.4.0 -> 1.6.0 upstream).

**Notes.** Auto-chaining the previous pass ledger into `--known-findings` and multi-pass operation are follow-ups; the engine-feedback file records the clean v1.5/v1.6 adoption.

## Recursive self-improvement ledger

**Timestamp.** 2026-07-08 01:20:00 -07:00.

**Action.** Added the recursive self-improvement ledger boundary: `src/game/playtest/selfImprovementLoop.ts`, `scripts/playtest-self-improve.mjs`, the `playtest:self-improve` npm script, focused Vitest coverage, and thread/design/architecture/spec docs. The command consumes saved LLM playtest bundles/envelopes/traces and writes JSON/Markdown ledger output that recovers shared `civ-engine` `ImprovementFinding` payloads, records replay self-check evidence, classifies proposal/fix/observe routing, preserves dispositions, and compares baseline/current metrics with `compareMetricsResults`.

**Code reviewer comments.** In-process adversarial review (Curie, codebase-grounded) found one real MEDIUM issue: the first version copied `SessionReplayer.selfCheck().ok` directly, so a no-op or failure-skipped self-check could be formatted as successful replay evidence and could make `autoFix` findings eligible. Fixed by normalizing self-check results at the aoe2 ledger boundary: strong evidence now requires at least one checked segment and zero skipped segments; partial/vacuous results preserve `engineOk` but set ledger `ok: false`, record an explanatory error, and block `autoFixEligible`. Added a regression test for a skipped/no-checked result. No other substantive review findings.

**Result.** The repo now has a canonical evidence ledger for the run -> findings -> replay/self-check -> classify -> rerun-compare loop without replacing the existing LLM runner, findings pass, replay tools, or auto-fix gate.

**Reasoning.** The previous loop pieces were useful but scattered. A small pure contract plus script glue keeps the runner and replay boundaries authoritative while making recursive-loop evidence inspectable and reusable.

**Validation.** TDD red passes covered the missing module and missing npm script before implementation. Focused tests now pass for marker extraction, envelope fallback, classification, metric comparison, Markdown formatting, CLI error-evidence capture, and partial self-check normalization (`npx.cmd vitest run tests/playtest/selfImprovementLoop.test.ts tests/playtest/playtestSelfImproveScript.test.ts`: 2 files / 6 tests). Dogfood command `npm.cmd run playtest:self-improve -- --baseline output/playtests-llm/campaign-10 --current output/playtests-llm/campaign-11 --out output/self-improvement/2026-07-08-campaign-11-ledger.json` wrote a ledger with 3 standardized findings, proposal classifications, a baseline/current comparison, and replay self-check failure evidence. Full gates passed: `npm.cmd test` (220 files passed, 1 skipped; 1816 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`.

**Notes.** Internal harness/docs only; no gameplay, public API, package version, or changelog change.

## Deterministic oracle ledger bootstrap

**Timestamp.** 2026-07-08 01:45:00 -07:00.

**Action.** Extended the self-improvement ledger so deterministic playtest artifacts can participate in the same shared loop contract as LLM conformance artifacts. `playtest:self-improve --oracles` now accepts runs without `.llm-trace.jsonl`, runs the existing deterministic oracle suite, converts `OracleViolation`s into `civ-engine` `ImprovementFinding`s, records deterministic no-trace metrics without NaN values, and keeps before/after comparison on the shared `compareMetricsResults` path.

**Code reviewer comments.** In-process adversarial self-review (iteration 2, codebase-grounded) found no substantive correctness issues after checking missing-trace behavior, deterministic no-NaN metrics, oracle-to-`ImprovementFinding` mapping, and the `run-oracles` legacy/report boundary. Full-suite verification caught one LOW process issue: `selfImprovementLoop.ts` had grown to 522 LOC, above the hard 500 LOC budget. Fixed by splitting oracle conversion into `src/game/playtest/oracleImprovementFindings.ts`; the focused budget test now passes. Residual risk: the oracle suite can surface known short-run failures, but the ledger correctly preserves them as candidate findings rather than deciding the fix automatically.

**Result.** The loop now has a current-code bootstrap path that does not require a fresh LLM provider call: `npm run playtest` can create replayable bundles, and `npm run playtest:self-improve -- --oracles` turns those bundles into standardized, classified improvement findings with replay self-check evidence.

**Reasoning.** The first ledger slice proved the command on historical LLM artifacts but replay self-check failed because those bundles predated current recorder/replayer contracts. Fresh deterministic bundles self-check strongly, and the existing oracle suite already identifies concrete regressions. Standardizing oracle violations through `ImprovementFinding` makes that evidence part of the recursive loop instead of leaving it stranded in the legacy `run-oracles` report format.

**Validation.** TDD red: focused tests failed because `extractImprovementFindingsFromRun` returned `source: "none"` for oracle violations, deterministic no-trace metrics produced `NaN`, and the CLI rejected `--oracles`. Green: `npx.cmd vitest run tests/playtest/selfImprovementLoop.test.ts tests/playtest/playtestSelfImproveScript.test.ts` passed (2 files / 9 tests) and `npm.cmd run typecheck` passed. Fresh dogfood: `npm.cmd run playtest -- --seed self-improve-smoke --max-ticks 500 --out output/self-improvement/fresh-smoke-baseline --all-ai`, then `npm.cmd run playtest -- --seed self-improve-smoke --max-ticks 700 --out output/self-improvement/fresh-smoke-current --all-ai`, then `npm.cmd run playtest:self-improve -- --baseline output/self-improvement/fresh-smoke-baseline --current output/self-improvement/fresh-smoke-current --out output/self-improvement/2026-07-08-fresh-smoke-current-ledger.json --oracles` wrote a ledger with strong replay self-check evidence (`ok`, 1 checked / 0 skipped for baseline and current), 22 standardized oracle findings, fix/observe classifications, and a baseline/current comparison. `npm.cmd run run-oracles -- --in output/self-improvement/fresh-smoke-current` exited nonzero as expected for one high oracle violation and wrote the legacy report, confirming the ledger is preserving real oracle failures rather than inventing findings. First full `npm.cmd test` run failed on the file-size budget only; after splitting `oracleImprovementFindings.ts`, `npx.cmd vitest run tests/architecture/fileSizeBudget.test.ts tests/playtest/selfImprovementLoop.test.ts tests/playtest/playtestSelfImproveScript.test.ts` passed (3 files / 11 tests). Full gates passed after the split: `npm.cmd test` (220 files passed, 1 skipped; 1819 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`.

**Notes.** `scripts/run-oracles.mjs` remains the pass/fail report path; recursive-loop evidence should prefer `playtest:self-improve --oracles` so oracle violations are persisted as shared improvement findings. Internal harness/docs only; no gameplay, package version, or changelog change.

## Ledger-driven fix proposal intake

**Timestamp.** 2026-07-08 02:05:00 -07:00.

**Action.** Added a ledger-first proposal intake path for recursive-loop findings. `src/game/playtest/fixProposalInput.ts` selects eligible `classification.kind: "fix"` entries from a self-improvement ledger, converts their shared `ImprovementFinding` payloads into the existing fix-prompt violation shape, and keeps the current bundle prefix attached. `scripts/propose-fix.mjs --ledger <ledger.json> [--finding-id <id>] [--dry-run]` now uses that path and only falls back to legacy `REPORT.md` parsing when no ledger is supplied.

**Code reviewer comments.** Iteration-3 review found two LOW issues and both were fixed: shared `ImprovementFinding.severity` can include `info`, while proposal input must normalize to `low|medium|high`; and the `propose-fix` rewrite needed an explicit legacy `REPORT.md` fallback regression test. Codex CLI external review was attempted after upgrading to 0.143.0, but escalation for external model network access was rejected because it would export private repo diff/context without explicit authorization; Claude CLI was unavailable on PATH. Residual risk: this slice only dry-runs proposal intake and does not apply a patch or prove a finding fixed by rerun.

**Result.** Fix proposal intake no longer needs to demote standardized findings back into the legacy oracle report table. The recursive loop can now move from ledger finding -> selected fix candidate -> prompt target while preserving finding id, ledger path, oracle details, and bundle prefix.

**Reasoning.** The previous slice standardized oracle violations into `civ-engine` `ImprovementFinding`s, but `propose-fix` still reparsed `REPORT.md`. Reading the ledger directly keeps proposal routing aligned with the classified shared contract and makes the legacy report path a compatibility fallback.

**Validation.** TDD red: `npx.cmd vitest run tests/playtest/fixProposalInput.test.ts tests/playtest/proposeFixScript.test.ts` failed because `fixProposalInput.ts` did not exist and `propose-fix` rejected `--ledger`. Green: the same focused command passed (2 files / 6 tests) after adding the severity-normalization and legacy-fallback regression cases. Fresh dogfood: `npm.cmd run propose-fix -- --ledger output/self-improvement/2026-07-08-fresh-smoke-current-ledger.json --dry-run --proposal-root output/self-improvement/fix-proposals` selected `aoe2-oracle-match-completes-run-0`, wrote `TARGET.json` with prefix `output/self-improvement/fresh-smoke-current`, and wrote `PROMPT.md` for the high-severity `match-completes` fix candidate without invoking an external reviewer. Full gates passed: `npm.cmd test` (222 files passed, 1 skipped; 1825 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`.

**Notes.** Internal harness/docs only; no gameplay, package version, or changelog change.

## Rerun finding delta evidence

**Timestamp.** 2026-07-08 02:20:00 -07:00.

**Action.** Added standardized finding-delta evidence to self-improvement rerun comparisons. `src/game/playtest/selfImprovementFindingComparison.ts` compares baseline/current `ImprovementFinding` identities and reports `resolved`, `persisted`, and `introduced` lists. Generic findings use their ids as identities; oracle findings use stable oracle/tick/message evidence identities so generated id suffix churn does not overstate resolved or introduced work. `SelfImprovementComparison` now carries those deltas beside the existing `civ-engine.compareMetricsResults` metrics comparison, and Markdown summaries include a concise finding-delta line.

**Code reviewer comments.** Iteration-4 in-process review found one real issue: raw id comparison was too weak for deterministic oracle findings because `oracleImprovementFindings.ts` includes a positional suffix in generated ids, so the same oracle/tick/message could be misclassified as resolved plus introduced when ordering changed. Fixed by comparing oracle findings through stable oracle/tick/message identity while still reporting the actual finding ids in each bucket. External Codex/Claude review was not rerun for this low-risk internal slice; the prior Codex escalation was rejected for private diff export and Claude remains unavailable on PATH.

**Result.** The loop can now say what happened to specific findings across a rerun instead of only comparing aggregate `findingsCount`.

**Reasoning.** Aggregate count deltas are useful but weak evidence for fix closure: one finding can disappear while another appears. Tracking finding identities makes before/after behavior explicit and lets later fix loops prove whether a target was actually resolved, persisted, or replaced by new work.

**Validation.** TDD red: `npx.cmd vitest run tests/playtest/selfImprovementLoop.test.ts` failed because `ledger.comparison.findings` was undefined and Markdown lacked a finding-delta line. Review red: `npx.cmd vitest run tests/playtest/selfImprovementFindingComparison.test.ts` failed because shifted oracle ids misclassified the same oracle/tick/message as resolved plus introduced. Green: `npx.cmd vitest run tests/playtest/selfImprovementFindingComparison.test.ts` passed (1 file / 1 test), `npx.cmd vitest run tests/playtest/selfImprovementLoop.test.ts` passed (1 file / 8 tests), and the combined focused run passed (2 files / 9 tests). Fresh dogfood: `npm.cmd run playtest:self-improve -- --baseline output/self-improvement/fresh-smoke-baseline --current output/self-improvement/fresh-smoke-current --out output/self-improvement/2026-07-08-fresh-smoke-current-ledger.json --oracles` wrote an updated ledger with finding deltas: 14 introduced, 8 persisted, 8 resolved. Full gates passed: `npm.cmd test` (223 files passed, 1 skipped; 1827 tests passed, 2 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`.

**Notes.** Internal harness/docs only; no gameplay, package version, or changelog change. After the final rerun-delta review, the thread artifact moved from `docs/threads/current/recursive-self-improvement-loop/` to `docs/threads/done/recursive-self-improvement-loop/`.

## 2026-07-08 — full-loop default for playtest:recursive (v0.1.126)

**Action:** Per the owner's directive that the proper loop be the mandatory behavior fleet-wide: `playtest:recursive` now runs the FULL loop by default (propose -> apply+gate on a branch -> rerun -> prove; never auto-merges). `apply` became tri-state `'auto' | true | false`: `'auto'` (default) degrades to proposal-only with a warning on a dirty/off-main worktree; explicit `--apply` hard-fails there; `--propose-only` opts out. Episodic memory defaults on: `defaultKnownFindings(outRoot)` picks the newest prior `ledger.json` (explicit `--known-findings` overrides). `parseArgs`/`defaultKnownFindings`/`planRerunBudget` exported and `main()` guarded behind `import.meta.url === pathToFileURL(process.argv[1]).href` so tests import without launching a pass; `scripts/playtest-recursive.d.mts` declares the exports. TDD: 4 new test pins written first (tri-state defaults, ledger auto-discovery x2, budget planning), existing 3 subprocess pins prove the guard still runs main under tsx.

**Review:** Codex MEDIUM — the rerun's `Math.max(0.5, remaining)` floor could exceed the pass budget AND an underfunded rerun would false-prove any candidate (near-empty bundle, no findings); fixed with `planRerunBudget` (unspent remainder, $0.50 viability floor, refusal -> `fix-unproven`). Claude MEDIUM — summary.md/DESIGN.md still described the proposal-only default; fixed. Claude LOW — `.d.mts` untracked; committed. Both reviewers verified the tri-state at every read site and the main guard in both directions. Synthesis: docs/threads/current/recursive-loop-closure/2026-07-08/1/REVIEW.md.

**Result:** Full suite 1849 passed / 2 skipped (clean post-edit run); 7/7 script pins; typecheck/lint/build green. Engine 2.0.0 alignment validated (aoe2 already passed the redaction/strict options explicitly).

**Reasoning:** aoe2 is the only repo with an automated fix arm, so "the proper loop is mandatory" here means apply-and-prove by default, bounded by branch isolation, repo gates, and no auto-merge. The degrade-vs-hard-fail split keeps the default usable in dirty working states without weakening explicit `--apply` CI semantics.

**Notes:** First unattended full-loop pass on a clean main worktree will be the live validation of the degrade/apply paths end to end; the unit pins cover the decision logic.

## 2026-07-09 — Oracle canary drill (v0.1.127, loop-engineering slice 2B)

**Action:** `npm run playtest:canary` + `canaries/` (two one-line seeded-bug patches with declared expected oracles + manifest.json). Per canary: LLM-free unpatched baseline (expected oracle must be quiet), patch on a throwaway branch, rerun, assert the oracle fires, revert. Decision matrix + manifest validation + ledger-oracle matcher in src/game/playtest/canary.ts (pure, reuses ledgerFindingToOracleViolation; own buildCanaryManifest so the canary vocabulary never widens the pass outcome union). Recursive pass now also appends its manifest to output/self-improvement/recursive/passes.jsonl (fleet convention; loop-ops shift runner reads it). Recon verified both canary targets by reading source: UNIT_SUBGRID_STEP_PER_TICK 2->0 (pinned oracle) and score-timer threshold x1000 with --game-length 500 (match-completes green->red).

**Review:** in-process; the clean-worktree precondition proved itself live (the drill refused to run over these uncommitted changes). Live drill runs post-commit; results recorded in the next entry.

**Result:** canary tests 10/10 + script pins 7/7; full gates green (suite exit 0, typecheck/lint/build clean).

**Reasoning:** prove-fixed made the loop's claims honest; canaries make its perception honest. An oracle that silently stops firing is currently invisible forever; after this it is caught within a drill for zero provider quota (all runs deterministic/LLM-free).

## 2026-07-09 — Canary drill live validation (fold into v0.1.127)

**Action/Result:** First live drill exposed a decision-matrix ordering bug (saturated baseline + legitimately-skipped patched run mislabeled run-failed; fixed test-first, canary-invalid now decided before the patched-null check) and a saturated canary: the UNPATCHED build fires no-pinned-or-oscillating-units 16 times at seed aoe2-canary/3000 ticks, so pinned-units correctly reports canary-invalid until the real pinned-unit violations are fixed (they are open candidates for the recursive pass — episodic memory already carries them). Added a third canary (tick-failure: conquest system throws once at tick 1500 -> no-tick-failures; reliably quiet baseline). Final live drill: pinned-units=canary-invalid (honest), match-never-completes=canary-ok, tick-failure=canary-ok — both sensitivity checks proven green->red live; exit 1 by design while any canary is non-ok.

## 2026-07-09 — AGENTS.md restructure + multi-CLI review runbook extraction

**Action:** Audited AGENTS.md against agent-instruction best practices on user request, then restructured on approval. Extracted all multi-CLI review mechanics (~46 lines: model pins, Codex/Claude commands, sandbox flags, the `--ignore-user-config` warning, upgrade pre-flight, background/poller pattern, the codex prompt-echo extraction recipe, failure modes) into `.claude/skills/multi-cli-review/SKILL.md` — repo-committed, and now the single bump site for review-command pins. AGENTS.md keeps policy only (reviewers-must-read directive, aspects, convergence-by-finding-count, model-currency rule) and additionally: gained a top-of-file "Session start" section (reads hoisted out of Documentation), scoped plan-writing to non-trivial changes, replaced the file-size wording with the enforced gate (hard 500 LOC + `tests/architecture/fileSizeBudget.test.ts` `LEGACY_VIOLATIONS` shrink-only ratchet), and added a Game-specific bullet routing the playtest model policy to `design/spec-final.md` §15.7 (Fable-5-ban aware). `docs/learning/lessons.md`'s codex-extraction pointer updated to follow the moved snippet. Net: 248 → 224 lines despite three new bullets.

**Review:** in-process adversarial subagent with a refutation brief (dropped rules, dangling refs, contradictions, doc-formatting, gitignore). 1 HIGH — the first LOC rewording ("target ≤ 500, hard cap 1000", taken from user-memory phrasing) contradicted the live fileSizeBudget gate, which hard-fails any NEW file over 500; fixed to state the gate + ratchet. 2 MEDIUM — lessons.md:224 still pointed at AGENTS.md for the extraction snippet (fixed); the "single source for pins" claim was false because `scripts/propose-fix.mjs` hard-codes its own reviewer pins, and its Codex pin (gpt-5.5/xhigh) has already drifted past today's 4cfbe50 gpt-5.6-sol/ultra bump (claim re-scoped to name the secondary pin site; script sync spun off as a task chip). 2 NIT fixed (reviewer independence made explicit; "runbook skill" nickname replaced with the path at first use). A token-preservation grep independently confirmed every mechanic moved to the skill rather than vanished.

**Result:** AGENTS.md 224 lines + SKILL.md 85 lines; the subagent verified all cross-references resolve in both directions, including spec §15.7's back-reference to the AGENTS.md model-currency rule. Gates: `npm test` exit 1 — all 15 failures belong to a CONCURRENT session's in-flight pinned-units-oracle work (13 red TDD tests in `tests/playtest/pinnedUnitsOracle.test.ts`, plus fileSizeBudget tripping on that session's `oracles.test.ts` 505→252 split), with those files modified 19:00–19:13 while this task's gate run started 19:02; typecheck/lint/build were short-circuited by the `&&` chain and not rerun. This task's diff is markdown-only with no gate surface; committed with explicitly-staged paths so the concurrent WIP stays untouched.

**Reasoning:** the always-loaded instruction file should carry rules that shape every session; occasionally-needed runbooks belong in on-demand files — 4 of the last 15 AGENTS.md commits were pin/flag churn that now becomes a one-line edit in one place. Doc wording must match enforced gates, not memory paraphrases: the reviewer catching my own fresh "hard cap 1000" wording is direct evidence the adversarial pass earns its cost even on doc-only changes.

**Notes:** docs/process only — no version bump, no changelog. Follow-up chips spawned: split the `LEGACY_VIOLATIONS` files (GameScene.ts 1012 first), sync `propose-fix.mjs`'s Codex pin. Fleet note: sibling repos' AGENTS.md still inline the runbook — worth a `/sync-instructions` pass when convenient.

## 2026-07-09 — propose-fix.mjs Codex reviewer pin sync (gpt-5.6-sol / ultra)

**Action:** Synced the known secondary review-pin site: `scripts/propose-fix.mjs`'s hard-coded Codex reviewer pin bumped from `gpt-5.5` / `model_reasoning_effort=xhigh` to `gpt-5.6-sol` / `model_reasoning_effort=ultra`, matching the 4cfbe50 pin bump now tabled in `.claude/skills/multi-cli-review/SKILL.md`. The script's Claude pin (`claude-opus-4-8[1m]`, `--effort max`) is deliberately untouched — governed by `design/spec-final.md` §15.7 and the Fable-5 ban. Closes the follow-up chip spawned by the AGENTS.md-restructure task.

**Review:** trivial mechanical two-token diff, self-reviewed per the trivial-change clause; no multi-CLI pass. Model-currency smoke test before commit: `echo "ok" | codex exec --model gpt-5.6-sol -c model_reasoning_effort=ultra -c approval_policy=never --sandbox read-only --ephemeral` returned a real completion on codex-cli 0.144.1 with session header `model: gpt-5.6-sol` / `reasoning effort: ultra`, exit 0 (18,795 tokens; a post-response rmcp session-DELETE 400 log line is cleanup noise, not a model rejection). Pin sweep: `xhigh` outside docs/ survives only in AGENTS.md's "earlier models capped at xhigh" note; `gpt-5.5` outside docs/ survives only as an arbitrary agentId fixture in `tests/annotations/markerSchema.test.ts` (schema-validation sample, not a reviewer pin — left alone); `tests/playtest/proposeFixScript.test.ts` exercises `--dry-run` only and pins no model strings, so no test updates were needed. Historical REVIEW.md/lessons/changelog mentions of gpt-5.5 stay as intentional audit trail.

**Result:** four gates green on the fast-forwarded tree — `npm test` 228 files passed / 1 skipped, 1860 tests passed / 2 skipped (the `proposeFixScript` dry-run pins included), typecheck, lint, and build all clean.

**Reasoning:** `propose-fix` invokes its reviewer CLI directly, so a stale pin silently routes automated fix proposals to an older model — exactly the drift the SKILL.md single-bump-site table exists to catch; syncing the named secondary site keeps that table's claim true.

**Notes:** internal loop tooling only — no gameplay change, no version bump, no changelog. Session ran in an isolated worktree fast-forwarded onto 7fa4069 pre-commit (the primary worktree carries a concurrent session's in-flight pinned-units WIP, untouched by this task).
## 2026-07-09 — File-size budget: four LEGACY_VIOLATIONS splits (pure refactor)

- **Timestamp:** 2026-07-09 19:20 → 2026-07-10 00:30 PT
- **Action:** Split the four remaining `src/` LEGACY_VIOLATIONS files under the 500-LOC cap as pure refactors and deleted their entries from `tests/architecture/fileSizeBudget.test.ts`: GameScene.ts 1012→254 (+ gameScene/ sceneViewTypes 144, selectionController 235, pointerInputController 275, sceneRenderer 388) [794c93d]; aiSystem.ts 788→166 (+ aiSystemGating 177, aiSystemBuildingPhase 143, aiSystemProductionPhase 271, aiSystemAttackPhase 109, aiSystemTypes 135) [9fb9266]; IndexedDBMirror.ts 698→363 (+ Schema 108, Queries 50, Reads 172, Writes 150) [c451538]; wireBridgeOps.ts 604→412 (+ wirePreSeedOps 262, aiIntentionPushers 99) [e11ddd8]. Follow-up [e142330]: fix-bot `SOURCE_FILES_BY_ORACLE` lists all six post-split aiSystem modules. The map now holds only `tests/playtest/oracles.test.ts` (505) — owned by a concurrent session's in-flight split, deliberately untouched.
- **Orchestration:** four parallel implementer subagents in isolated worktrees; three were killed mid-verification by an account session limit — the aiSystem and wireBridgeOps worktrees were implementation-complete and salvage-committed then cherry-picked, and the GameScene agent was resumed from its transcript after the reset and completed normally. Process note: `cmd | tail` masks exit codes — the branch browser run reported exit 0 with 12 real failures; redirect to a file and read `$?` directly.
- **Code reviewer comments** (in-process adversarial subagents — Opus per-task ×3, Fable on the GameScene task and the whole branch; multi-CLI deliberately skipped: verbatim-move refactor with mechanical multiset proofs and full gates rather than a semantic persistence/security change, escalation reserved for surfaced semantic drift):
  - Correctness/verbatim: all four splits APPROVED; verbatim residue mechanically closed on every split (whitespace-normalized sorted line-multiset diffs); GameScene's six declared deltas (dead-guard drops, pre-create fallbacks, setBridge regrouping, feedbackRenderer timing, make*Renderer dedup, local renames) each adjudicated SAFE against the 7fa4069 original.
  - Persistence (IndexedDBMirror): store names/keyPaths/versions, transaction scopes and modes, upgrade wiring order, the U+FFFF range sentinel all byte-identical; no await moved across a transaction boundary; `_executionSequence` stays per-instance.
  - Execution/wiring order: aiSystem per-tick phase order (gating once/tick → per-owner TC/plan/villagerRebalance/stockpile/sighting/pop → building → production → attack) and wireBridgeOps pre-seed factory order + seedFreshScenario position proven unchanged; registerBridgeSystems deps-bag key-order shift proven inert (consumers destructure by name; zero reflection over the bags).
  - Loop machinery (the one substantive finding, MEDIUM): `fixBotPrompt.ts`'s oracle→source-file map still pointed three oracles at aiSystem.ts alone, blinding the recursive loop's fix-bot to the moved decision logic → fixed in e142330 with the contract pinned by a new test, re-verified by the finding reviewer.
  - Cross-split (whole-branch): zero cross-imports between the aiSystem* and wirePreSeedOps/aiIntentionPushers modules; the push*→submit* seam through registerAllSystems byte-identical; no module-level mutable state in any of the 13 new files; all export surfaces preserved; LEGACY_VIOLATIONS ends with exactly the one foreign entry.
- **Result:** gates — typecheck ✓, lint ✓, build ✓, full vitest 228 files / 1861 tests ✓, size-budget test green at every commit. Browser suite on the branch: 82 pass / 12 fail / 2 skip vs clean-7fa4069 baseline 84 pass / 10 fail / 2 skip — the 10 baseline failures are identical specs on both trees (incl. 2 of 3 replay-scrub with the same enter-replay precondition timeout), and the branch's two extras (game-combat-and-meta save/restore, replay-scrub timeline commit) pass 4/4 isolated on the branch AND 4/4 isolated on baseline: load-dependent flakes, zero regressions attributable to the diff. Preview smoke: clean boot, HUD + test API mounted, zero console errors; the scene itself cannot start in the hidden preview pane (document.hidden, RAF never fires) — stash-verified pre-existing environment limitation, identical on baseline.
- **Reasoning:** split by lifecycle/role with composition per AGENTS.md review aspect 4; each original keeps its path and full export surface so importers are untouched; each map deletion rides the same commit as its split so every commit on the branch is gate-green in isolation.
- **Notes:** ARCHITECTURE.md component-map lines refreshed for accuracy only (gameScene/ enumeration + sceneRenderer/pointerInputController/selectionController/sceneViewTypes; wireBridgeOps described as orchestrator delegating to wirePreSeedOps/aiIntentionPushers; aiSystem phase-module sentence) — no boundary change, so no drift-log row. lessons.md evidence pointers retargeted to the modules that now hold the referenced logic (pickUnitMix → aiSystemProductionPhase; selection/exact-click pointers → selectionController/pointerInputController; getEdgePanDelta → cameraController, a pointer that was already stale pre-task). No changelog entry and no version bump — pure refactor, not user-visible.

## 2026-07-09 — Pinned-units candidate closed: oracle rebuilt + both AI scouts unpinned, fixed-proven 16 → 0 (v0.1.128)

**Action:** Completed the recursive-loop pass on loop-ops DIRECTIVES priority 1 (the 16 `no-pinned-or-oscillating-units` violations saturating the canary drill's baseline at seed `aoe2-canary`). Diagnosis followed the engine-tools-first rule: replayed the recorded canary baseline bundle (`SessionReplayer` + `createReplayWorldOnly`) and read per-entity ground truth instead of trusting finding text. 14/16 were oracle misfires in three sub-classes (entity-id reuse conflating resource and unit lifetimes; the inert human's correctly-idle units; legitimate stationary gathering including a zero-walk gold miner), 2/16 were real: the AI base scout wedged into its own TC footprint (transform reset without heading change) and the forward enemy scout spawned with no wander components at all. Rebuilt the oracle as `src/game/playtest/pinnedUnitsOracle.ts` (per unit-lifetime intervals, undriven-owner gate, churn-qualified oscillation, gathering-exempt hard-pin with `pinnedStuckTicks`), fixed both scout defects (wander state on every scout spawn incl. forward scout; obstacle deflection), all test-first.

**Round 2 (the part a code diff would never prove):** the mandatory prove rerun dropped 16 → 1, exposing a residual the first fix could not see — scout 2257 roamed ~500 ticks then froze 2502 ticks at (38,18), its wander-box west edge beside its own forward house. Env-guarded live trace caught a two-state livelock: blocked heading (1,1) rotates to (-1,1), but the box-edge reflection flips dx straight back to (1,1) — the failed heading — every tick, so the actual escape (a phase-dependent staircase through (38,17)→(39,17) that a naive no-corner-cut rule would reject) was never tried. Fix: `pickEscapeHeading` emulates each candidate heading's stepped path (reflection + clamp identical to the live loop, `UNIT_SUBGRID_RESOLUTION` ticks) and assigns the first that reaches a different passable cell; blind rotation survives only as the genuinely-boxed-in fallback; the no-transform path got the grid-level analogue. Pure tests pin the recorded trap geometry exactly (reflection-cancelled heading rejected, staircase accepted, deterministic pick `(-1,-1)`, boxed→null); an integration test bounds max stationary streak < 600 ticks over 2000 ticks for all AI scouts.

**Result:** second prove rerun at the same seed/ticks: `no-pinned-or-oscillating-units` **absent** (16 → 0) under a strong replay self-check (3 segments, 0 skipped, 0 divergences); `match-completes` (bounded-run shape) and the 9 low perf-outlier findings are unchanged classes. Four gates green (231 files / 1887 tests, typecheck, lint, build). Debugging session recorded in `docs/debugging/2026-07-09-pinned-units-oracle.md` (round 2 appended).

**Reasoning:** the pass contract (prove at bug-class granularity by rerunning, never from the diff) is what caught round 2 at all — the round-1 fix was green on gates, green on the early-window wander test, and still shipped a scout that froze for 83% of the match. The livelock also shows why "rotate on block" needs its interaction with "reflect at bounds" verified: two individually-correct local rules composed into a permanent freeze.

**Notes:** user-visible gameplay fix → v0.1.128 + changelog entry; spec §13.3 wander paragraph updated to the emulation contract. Canary drill rerun (expect pinned-units `canary-invalid` → `canary-ok`) happens post-commit on the clean tree; drill appends its own rows to `passes.jsonl` and this pass appends a `fixed-proven` manifest row.

## 2026-07-10 — Adversarial-review round on the pinned-units diff: 17 confirmed findings fixed pre-commit (folds into v0.1.128)

**Action:** Ran the in-process adversarial Workflow on the full uncommitted v0.1.128 diff — 4 finder dimensions (oracle correctness, scout sim, test quality, docs/spec accuracy), each finding independently verified by a refuter agent instructed to REPRODUCE the claim against live code (23 agents total). 19 findings, 17 confirmed, 2 refuted. All confirmed findings fixed in the same pre-commit round, test-first (8 new red probes → green).

**Review, by theme:** (1) HIGH oracle-honesty — the gathering exemption used ANY-sample semantics plus always-consulted bounding snapshots, so one stale 'gathering' sample (or a pre-span bounding one) exempted an arbitrarily long later freeze; both blind spots were empirically reproduced by the verifier with probe bundles (P1/P2), and both are the false-prove direction the loop guards against. Fixed with the gathering-SUFFIX rule (latest interior sample re-anchors the stuck clock; bounding only when zero interior samples); P1/P2 are now regression tests. (2) MEDIUM oracle blind spots — amplitude-≥3 shuttle oscillation was invisible (every leg re-anchored the tight box; the deleted sliding-window oracle had caught it) → second wide confinement box `pinnedOscillationBoxCells` (6) with shared exemptions; same-tick kill+id-reuse merged lifetimes (engine nets remove+set into a pure set) → owner/unitType change on an open interval is a lifetime boundary; garrison round-trips counted as confinement (1400-tick garrison stay read as a 1650-tick pin) → positionReplay now records gaps and the scan splits segments at them. (3) MEDIUM sim — wander-resume clamp teleported a chase-stranded scout to its box edge in one tick (newly reachable at scale via the forward scout) → effective box widens to the current cell so the edge reflection ratchets it home stepwise (walled-off scouts patrol in place; the oracle reports them honestly — bounded patrol is not navigation); the naive grid fallback was unified onto the fine-emulation escape path (its livelock was refuted as unreachable in production, but one escape semantics beats two). (4) MEDIUM test quality — the 25-tick-sampled streak regression aliased away period-2 cell alternation → per-tick reads, ≥3 distinct cells per 600-tick block. (5) LOW/docs — drivenness suffix heuristic replaced with an explicit per-command actor-key whitelist typed exhaustively over the 15-command surface (unit.gather.resourceId and market.action.playerId mis-resolved); dead `netManhattanProgress` deleted; spec §13.3 corrected (TC-centered base-scout box, candidate order, units-don't-block passability, walk-home + walled-off honesty); ARCHITECTURE component row + drift-log row; changelog wording aligned to shipped semantics; lessons anchors corrected to the real test files. Refuted (not acted on): the 200-tick test timeout risk (measured 5.9× headroom) and the grid-fallback livelock (dead path — every wander unit gets unitTransform; unified anyway).

**Result:** oracle suites 49/49 green including the 8 new review probes; walk-home test red (8-cell teleport reproduced) → green; full four gates green on the final tree. The reviewer count that matters: the verifiers CONFIRMED findings by writing and running probe bundles against the live oracle — two of those probes (P1/P2) directly contradicted the round-1 design intent as documented, which is exactly the kind of substantive finding AGENTS.md's convergence rule weighs over vote counts.

**Reasoning:** the pass's honesty invariants (findings verified mechanically, prove-by-rerun) apply to the SENSOR too: an oracle that can be talked out of firing by one stale sample false-proves any fix it is asked to judge. The review round hardened the sensor before the prove rerun leaned on it.

**Notes:** folds into v0.1.128 (same coherent change, no extra bump). Prove rerun + canary drill re-run against the final tree follow before commit.

## 2026-07-10 — Round 4: pocket-orbit livelock caught by the hardened oracle itself; patrol kick ships; fixed-proven 16 → 0 (v0.1.128 final)

**Action/Result:** The post-review prove rerun was clean of every original class but the NEW wide-box oscillation verdict fired once — forward scout 2259 circling within 5 cells of (46,17) for 2963 ticks (1185 moves) with the south exit open the whole time. Replay showed a deterministic limit cycle: diagonal bounce + a fixed escape order is a billiard, and blocker pockets (water/forest/berries) form attractors; the class was invisible before round 3's wide box existed. Fix: `applyWanderKick` — a deterministic heading rotation every 400 ticks (staggered per unit id, angle cycling 90°/180°/270°), so no closed orbit survives; replays/saves stay byte-identical. TDD: 3 pure kick tests (cadence, angle cycle, determinism). Prove rerun 4: `no-pinned-or-oscillating-units` ABSENT under the hardened oracle, replay self-check strong. Full gates green (231 files / 1904 tests). This closes the loop with a nice property: the sensor hardening the review forced (round 3) immediately caught a real bug the original sensor could not see (round 4) — the recursive loop improving its own senses, then cashing them in, in one pass.

**Notes:** spec §13.3 patrol-kick sentence, changelog + debugging doc updated. Same v0.1.128. Canary drill + pass manifest + push follow post-commit. Rebase onto the concurrent file-size-budget splits (6 upstream commits) resolved three append conflicts (both devlogs; `LEGACY_VIOLATIONS` merged to the empty map — upstream split the four src violators, this pass split `oracles.test.ts`) and extended the upstream fix-bot source-map fix to this pass's oracle: `no-pinned-or-oscillating-units` now lists `scoutMovementSystem.ts` (where all three real pinning bugs lived), contract-pinned in `fixBotPrompt.test.ts`.

## 2026-07-10 — Multi-CLI review runbook consolidated to fleet-canonical (loop-ops)

**Action:** On the user's direction ("why can't they be just one shared skill? … Do it"), the per-repo multi-CLI review runbook copies created by yesterday's fleet sync were consolidated: `loop-ops/docs/skills/multi-cli-review.md` is now the fleet-canonical runbook (following the recursive-playtest single-source precedent, including user-level pointer skills in `~/.claude/skills/` and `~/.codex/skills/`), and each repo's `.claude/skills/multi-cli-review/SKILL.md` shrinks to a thin stub holding only repo deltas. The canonical content is civ-engine's superset version: `codex exec -o <file>` output extraction promoted to primary (validated 2026-06-13: 280 KB polluted stdout → ~2 KB clean review file; kills the prompt-echo extraction bug class from the v0.1.6 thread), last-marker-pair extraction demoted to fallback, both Windows full-review gotchas (no-pipe stdin hang → `< /dev/null`; concurrent codex read-only sandboxes fail `STATUS_DLL_INIT_FAILED` → serialize codex, parallel Claude fine), distinct-lens full-codebase guidance, and the extra-instances-when-a-CLI-is-down rule. aoe2's stub names the local pin sites (`scripts/propose-fix.mjs`) and the §15.7 playtest-pin boundary; AGENTS.md's three runbook references now point at the fleet runbook, and the lessons.md codex-extraction pointer follows.

**Review:** consolidation executed as loop-ops canonical first (its npm test green; README/AGENTS pointer-surface rules updated same-commit per its currency rule), then per-repo stub agents with content-preservation self-review, then a cross-repo verification sweep from this session (stub presence, canonical reference, no pins left outside the canonical, no content loss).

**Result:** one bump site for review pins fleet-wide; the `-o` recipe and Windows gotchas — previously in 1-2 of 17 copies — now apply everywhere; future flagship bumps are a single loop-ops edit plus any repo-local script sites named by stubs.

**Reasoning:** 17 copies diverged the day they were created (the best extraction recipe existed only in civ-engine); per-repo copies were an accident of extracting AGENTS.md sections in place, not a chosen design. The runbook's audience is the orchestrating agent on this machine, so per-repo self-containedness bought nothing; the loop-ops pattern was already proven for the loop contract.

**Notes:** markdown-only diff; full gates run before commit regardless. No version bump, no changelog (internal process docs).

## 2026-07-10 — Fable everywhere: the §15.7 playtest ban is lifted, all Claude pins fable-first

**Action:** Owner directive ("Everything should use the most advanced model if possible. There is no reason we should be stuck on opus") lifted the 2026-06-12 Fable ban. Executed spec §15.7's wholesale-swap procedure: smoke-verified both `claude-fable-5` and `claude-fable-5[1m]` (echo-ok probes, exit 0 — the 2026-06-12 unavailability is gone), then swapped every call site claude-opus-4-8 → claude-fable-5: `scripts/playtest-llm.mjs` (strategyModel + tacticalModel + the cost-note comment block and printed string), `scripts/playtest-findings.mjs` default, `scripts/playtest-llm-auto-fix.mjs` proposeFix call, `scripts/propose-fix.mjs` Claude reviewer (`claude-fable-5[1m]`). `src/game/playtest/types.ts` cost-table comment and spec §15.7's model-policy bullet now encode the standing rule: most advanced model always, latest Opus (`claude-opus-4-8`) strictly as the unavailability fallback, swap back when Fable returns. The aoe2 review-skill stub's pin-site bullets updated to match. Earlier the same morning the fleet review pin went fable-first in the canonical runbook (loop-ops 4d422de) with the mini-metro PowerShell echo re-synced (5cb229d); an agent is applying the same directive to investing's stale analysis-backend defaults (claude-opus-4-7/gpt-5.5) in that repo.

**Review:** cost analysis confirmed the swap is cost-neutral on the claude-code provider — per-call cost is prelude-dominated (~$0.61/decision Fable vs ~$0.67 Opus, campaign-3 observed), so the $10/$50-vs-$5/$25 rate gap does not bind. No test pins the default model strings (grep-verified); the playtest suite ran 355 passed / 3 failed, and all 3 failures are `tests/playtest/runPlaytest.test.ts` cases exercising the live bridge — attributable to a CONCURRENT session's in-flight death-effects work (uncommitted edits to bridge/visibility/replay files + untracked deathEffects/unitDeathFeed tests; none of those files are in this diff). Typecheck's only diagnostics are that session's `recentUnitDeaths` WIP in its own test files; zero diagnostics on anything touched here.

**Result:** every Claude call in the fleet now prefers the most advanced model: playtest harness (bare `claude-fable-5`), propose-fix reviewer (`claude-fable-5[1m]`), multi-CLI review pin (`claude-fable-5[1m]`, canonical), loop-ops nightly ladder (already fable-first), knowledge's registry (already fable-first). Opus survives only as the explicit unavailability fallback everywhere.

**Reasoning:** the ban's sole premise was the 2026-06-12 unavailability; with Fable reachable again and the per-call cost prelude-dominated, opus-as-primary had no remaining justification anywhere.

**Notes:** internal tooling — no version bump, no changelog (matches the 2026-06-12 swap and 2026-07-09 pin-sync precedent). Spec §15.7 updated in the same change per the spec-persistence rule. Committed with explicitly staged paths; the concurrent death-effects session's WIP left untouched.

## 2026-07-10 — Unit death feedback: fog-filtered death feed + collapse/dust render effect (v0.1.129)

**Action:** Shipped the highest felt-quality graphics slice off the isometric-overhaul plan: a dying unit now plays a ~0.7s collapse-and-fade tell with expanding dust rings where it stood, instead of blinking out. Because a unit vanishing from the render projection is ambiguous (death vs fog-exit vs garrison), the cue is driven by an explicit DEATH FEED, not present→absent diffing. Sim seam: `entityDestroyOps.destroyUnitEntity` records each death (id, tick, sub-cell x/y, owner, unitType, tint, size) into a transient `BridgeState.recentUnitDeaths` — NEVER persisted (no codec), pruned to the last 10 ticks + a 64 hard cap. The projector (`visibility.ts createProjector`) surfaces `visibleUnitDeaths()` on `ProjectedFrameView.recentUnitDeaths` — a pure fog+age filter (fresh AND cell currently visible to the viewing player), wired through createWorldResult/assembleBridgeApi/wireBridgeOpsTypes and both projector call sites (live + replay). Render: `deathEffects.ts` (`createDeathEffectsRenderer`, mirroring the v0.1.45 `feedbackEffects` seam) ingests the frame feed (dedupe by id:tick), animates `deathBodyGeometry` (collapse + fade, tip direction by id parity) + `deathDustRings` over the scene clock — NO Math.random/Date.now, replay-identical. `sceneRenderer` ingests per tick-change, folds `isAnimating` into the render-cache-bust, draws deaths first (ground decals under living units), resets on bridge swap.

**Verification:** TDD red→green — 11 pure/renderer tests (`tests/phaser/deathEffects.test.ts`: collapse/fade monotonicity, dust expand+fade, id-parity determinism, dedupe by id:tick, id-reuse-different-tick = new effect, duration+grace, draw-while-alive/nothing-when-expired, alpha fade, cap, reset, null frame) + 3 sim tests (`tests/simulation/unitDeathFeed.test.ts`: pure fog/age filter drops fogged + aged deaths; live heresy-kill fixture surfaces the death on the frame, un-fog-filtered position within a cell, then ages out). Visual protocol (throwaway Playwright capture on the built dist, deleted after): a magenta/oversized diagnostic proved the effect renders exactly at the death cell (wiring), then the real effect captured on the `feedback-showcase-fixture` melee in REAL time (RAF-driven, avoiding the frozen-scene-clock artifact of `advanceTicks`) showed a clean collapsed corpse + two dust rings on the ground, distinct from living units, fully cleared 3 frames later while a fresh death began — cropped/upscaled evidence. Default `aoe2-prototype` view (with fog) unchanged. Four gates green.

**Reasoning:** present→absent diffing was the tempting shortcut and is wrong — it fires the death cue on fog exits and garrisons. An explicit, fog-filtered, transient feed is the honest channel: it renders only real, visible deaths and carries zero sim authority (absent from saves and every deterministic hash), so save/load and replay determinism are untouched by construction. Reused the feedbackEffects render-only pattern so the animation clock never re-enters the sim.

**Notes:** user-visible visual feature → v0.1.129 + changelog; spec §14.5 updated (death tell added, "death/destruction particles" removed from the deferred list).

**Adversarial review (13 agents, 4 finder dimensions + refuters; 7 confirmed / 2 refuted):** caught a real HIGH — a **fog-of-war leak**. The death feed was filtered by CURRENT visibility at projection time, so a player uncovering a cell within DEATH_FEED_TICKS (~1s) of an enemy dying there saw the death animation — off-screen-combat intel AoE2 fog would never grant. A paired MEDIUM was the mirror image: an isolated unit's OWN death was DROPPED for its owner, because losing the unit (its sole vision source) re-fogged the cell before projectFrame ran. Both share one root: filtering on current visibility instead of AT-DEATH visibility. Fixed by recording, per death, the set of players who could see the cell at the moment of death (`ProjectedUnitDeathView.witnessedBy`, computed in `recordUnitDeath` over the real players in `populationCodec` plus the dead unit's own owner, queried pre-recompute) and gating `visibleUnitDeaths` on membership in that set — no current-visibility term at all. This is the AoE2 model: you see deaths you witnessed, and only those. Threaded a read-only `visibility` probe into `entityDestroyOps`. Also addressed: MEDIUM seen-set re-admit — the at-death gate makes a death's frame membership a CONTIGUOUS window (fixed witness set + monotonic age, no flicker), resolving the flicker root cause; kept the original dedupe-prune (rebuilding from `active` would have RE-admitted an in-window death whose ~0.7s effect ends before its ~1s feed window) and corrected the comment to state the invariant. New tests: pure filter asserts witness-gating both directions (a non-witness never sees it, even at the cell) + age-out; the live heresy kill asserts the human is in `witnessedBy`; a garrison-no-misfire test (garrisoning removes a unit from the view but records NO death). Two findings refuted correctly: drift-log/decisions WERE updated (stale-diff read), and the prune compaction is sound. Four gates green.
