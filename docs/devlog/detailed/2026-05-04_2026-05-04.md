# 2026-05-04 — devlog (active file)

## 2026-05-04 13:17 — v0.1.6 roadmap continuation: `unitCommandOps` file-size split

**Action.** Continued the active replay-scrubber / agentic automation roadmap from the current red gate. `npm test` was failing the architecture budget because `src/game/simulation/bridge/unitCommandOps.ts` had grown to 594 lines, above its 588-line legacy cap. Split two cohesive helper groups out of `unitCommandOps.ts`: `unitSelectionOps.ts` owns selected-sheep/unit queries plus select-by-cell/id/clear-selection mutators; `sheepCommandOps.ts` owns the `sheep.move` facade and direct helper. `createUnitCommandOps` still returns the same public surface by composing both helpers, so call sites do not change. Removed `unitCommandOps.ts` from the legacy file-size exemption list.

**Code-reviewer feedback.**

- **Codex** (`gpt-5.5`, xhigh) found no issues. It verified the moved selection and sheep command bodies against the live wiring: `createUnitCommandOps` still composes the same public surface, `wireBridgeOps` / `registerBridgeSystems` / `humanInputOps` still receive the expected functions, and the file-size budget exemption removal matches the current split. Codex could not run npm in its read-only review shell, so main-session verification remains the source of gate evidence.
- **Claude** (`claude-opus-4-7[1m]`, max) was unreachable due quota: "resets May 5, 7pm (America/Los_Angeles)". Per AGENTS.md, this is recorded as a reviewer blocker and the work proceeds with the available reviewer plus direct validation.

**Result.** Targeted red-green pass: `npm.cmd test -- tests/architecture/fileSizeBudget.test.ts` passes; unit command coverage (`unitMove`, `unitAttack`, `unitGather`, `unitContext`, `unitContextAtEntity`, `sheepMove`) passes; `npm.cmd run typecheck` and `npm.cmd run lint` pass. Final full gates pass after the Vitest runner stabilization entry below.

**Reasoning.** This is a behavior-preserving structural split to clear the known architecture gate before continuing into Phase 2F/2G and replay UI work. Keeping the public `UnitCommandOps` surface stable avoids rippling a file-size fix through bridge wiring.

**Notes.** `docs/changelog.md` and `package.json` already contained an uncommitted `0.1.6-rc1` update when this pass began; left in place while continuing the roadmap.

## 2026-05-04 13:47 — Vitest worker timeout gate stabilization

**Action.** Investigated the full-suite `npm.cmd test` failure that remained after the file-size split. The suite reported all 99 files / 727 tests passing, then exited non-zero with seven `[vitest-worker]: Timeout calling "onTaskUpdate"` errors. Added debugging notes in `docs/debugging/2026-05-04-vitest-worker-ontaskupdate.md`. Tested `--maxWorkers=4` and `--reporter=dot`; both reproduced the same post-pass RPC errors. Tested `--pool=threads`; it avoided the RPC errors but exposed slower thread-pool execution against two explicit 15s production tests. Moved the stable runner defaults into `vitest.config.ts` (`pool: 'threads'`, `testTimeout: 30_000`), then raised `createSimulationBridge.production.test.ts`'s shared production-progression timeout to 30s because explicit `it(..., timeout)` values override the runner timeout. During the AI monk intention split, the larger 734-test suite exposed two more explicit caps that were too tight only under full-suite contention: the watch-tower utility case and the first blacksmith Forging progression case. Raised only those proven caps after isolated reruns passed.

**Code-reviewer feedback.** Codex iter-2 flagged that the first stabilization was too narrow because it only changed the `npm test` script while `vitest.config.ts` still documented and configured the flaky forks pool. It also flagged that the pre-existing `0.1.6-rc1` `package.json` version bump had not been reflected in `package-lock.json`. Codex iter-3 then flagged the broader release-metadata issue: the pre-existing `docs/changelog.md` entry says there is no user-visible behavior change even though changelog/package versioning is reserved for user-visible behavior changes. Final disposition for the runner contract: move the contract into `vitest.config.ts`, leave `npm test` as plain `vitest run`, exclude the pre-existing package/changelog version-marker edits from this committed unit, and revert the local lockfile metadata churn. Later timeout-headroom review found two bad edits before closure: the utility timeout was raised on the wrong test twice, and the docs overclaimed the final 734-test gate before rerun evidence existed. Final timeout-headroom disposition: only the Watch Tower utility cap and first Forging progression cap were raised, the debug/devlog evidence was corrected to the verified 733-passed/1-skipped run, and Codex iter-4 found no remaining issues. Claude remained unreachable in all follow-up iterations due quota.

**Result.** Targeted production progression passes under the new runner. Follow-up targeted reruns for the watch-tower utility case and Forging progression case pass in isolation after proving the full-suite failures were timeout headroom, not assertion failures. The full gate is clean: `npm.cmd test` passes with 99 files, 733 tests passed, 1 skipped, and no worker RPC timeout errors. `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build` pass.

**Reasoning.** The root cause is the fork-pool transport failing at the runner level after assertions pass, not a gameplay/test assertion failure. The threads pool keeps the same test contract while avoiding the unreliable fork worker RPC path; the shared 30s production-test cap preserves a bound and covers the known long production cases under full-suite contention.

**Notes.** This is test infrastructure only; no user-visible changelog entry beyond the already-present `0.1.6-rc1` roadmap entry.

## 2026-05-04 14:50 - AI monk task assignment command-intention split

**Action.** Continued the v0.1.6 commandify / bridge-state migration roadmap at the `monkTasks` blocker. Added a regression proving AI monk task assignment now crosses the command boundary: on `ai-monk-relic-fixture`, the first decision step leaves the relic on the map and no carried-relic side-map entry exists; the next step processes `monk.contextAtEntity` and the monk picks the relic up. Implemented `pushAiMonkTaskIntentions(owner, pushMonkContextAtEntityIntention)` in `monkTaskOps.ts`, sharing the existing deposit > pickup > heal target selection with direct `assignAiMonkTasks`. Wired `wireBridgeOps -> registerBridgeSystems -> registerAllSystems -> aiSystem` so AI monk decisions push `monk.contextAtEntity` intentions to `state.pendingCommands` instead of mutating `monkTasks` directly. Follow-up review hardening added AI-only `expectedOwner` / `intendedTaskKind` fields, persisted bridge-owned `pendingCommands` across save/load, and cloned pending-command payloads when saving/hydrating so caller mutation of a save blob cannot alter the still-live queue.

**Code-reviewer feedback.** Codex iter-1 flagged two real issues: stale AI monk intentions could be reinterpreted as the wrong task or wrong owner because the queued command only carried monk id + target id, and save/load in the one-tick command-boundary window could drop the queued assignment because the bridge drained into the engine command queue immediately after the decision tick. Both were fixed before iter-2 review: `monk.contextAtEntity` now carries optional `expectedOwner` / `intendedTaskKind`, stale owner/task mismatches no-op, `pendingCommands` drains before the next tick instead of after the previous one, and `SaveBlob.sideMaps.pendingCommands` persists queued bridge intentions. Codex iter-2 then flagged one real snapshot-isolation issue: `saveGame()` and hydration reused pending command objects, so mutating `blob.sideMaps.pendingCommands[0].data` could alter the live queue before it drained. Fixed with `clonePendingCommand` and a red-green regression. Codex iter-3 found no remaining issues. Claude remained unreachable due quota: "resets May 5, 7pm (America/Los_Angeles)".

**Result.** Targeted red-green pass: `npm.cmd test -- tests/simulation/aiPlayer.test.ts -t "defers AI Monk task assignment"` failed before the implementation and passes after it; `npm.cmd test -- tests/simulation/aiPlayer.test.ts -t "keeps saved pending AI Monk intentions isolated from the live queue"` failed before cloning and passes after it. `npm.cmd test -- tests/simulation/aiPlayer.test.ts -t "FU4 AI Monks"` passes, now including save/load preservation, save-blob mutation isolation, and stale intended-task no-op coverage. `npm.cmd test -- tests/commands/monkContextAtEntity.test.ts`, `npm.cmd test -- tests/commands/dispatcher.test.ts tests/commands/unitMove.test.ts`, and `npm.cmd test -- tests/architecture/fileSizeBudget.test.ts` pass. `npm.cmd test` passes with 99 files, 733 tests passed, 1 skipped; `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build` pass.

**Reasoning.** KAD-0007 blocked `monkTasks` migration because `aiSystem.execute -> assignAiMonkTasks -> setMonkTask` wrote task state directly during the update phase. The existing `monk.contextAtEntity` command already covers the four necessary task outcomes through `routeMonkContextAtEntityCommandDirect`, so a new four-command monk surface is unnecessary for AI assignment. Reusing the existing command keeps human and AI monk routing on one revalidated handler path and makes the next-tick delay explicit.

**Notes.** `docs/changelog.md` and `package.json` still contain pre-existing uncommitted `0.1.6-rc1` release-marker edits and remain outside this coherent unit.

## 2026-05-04 16:00 - `monkTasks` accessor migration

**Action.** Continued the bridge-state migration now that the AI-side direct-mutation blocker was gone. Added a red replay snapshot regression proving an active Monk pickup task did not appear in `world.state.aoe2.monkTasks` before the migration. Moved `monkTasks` out of `BridgeState` and onto the existing `monkTasksCodec` through `BridgeStateAccessor`: `monkTaskOps.setMonkTask/clearMonkTask`, `prototypeMonkBehavior`, unit move cleanup, entity destruction cleanup, save-game serialization, save-load hydration, and selection/activity projection now read or mutate the accessor-backed map. Added `monkTasksCodec` to the incremental migrated-codec snapshot equivalence list.

**Code-reviewer feedback.** Codex iter-1 found two real issues: canonical docs overclaimed all Tier-1 slots were migrated even though `unitCommands` remains bridge-owned, and schema-1 hydration merged `sideMaps.monkTasks` into stale `worldSnapshot.state.aoe2.monkTasks` instead of replacing it. Fixed by correcting the roadmap/architecture/changelog wording and adding a red-green save-load divergence test where empty `sideMaps.monkTasks` wins over stale snapshot state. Codex iter-2 found two more real issues: ordinary `unit.move` dirtied `aoe2.monkTasks` on no-op deletes, and active replay-scrubber docs still described raw `monkTasks.delete`. Fixed with guarded dirty marking plus current design/plan doc updates. Codex iter-3 found the idle Monk context-move fallback still dirtied `aoe2.monkTasks`; fixed the shared `clearMonkTask` path and guarded entity-destroy cleanup too. Codex iter-4 found the active `0.1.6-rc1` changelog still said `monkTasks` was blocked; fixed it to include `monkTasks` and name `unitCommands` as the remaining Tier-1 bridge-owned codec. Codex iter-5 found no code-level correctness, security, or performance issues; it noted one stale source comment, which was corrected. Claude remained unreachable in every iteration due quota: "resets May 5, 7pm (America/Los_Angeles)".

**Result.** Red-green pass: `npm.cmd test -- tests/replay/snapshotEquivalence.test.ts -t "active Monk tasks flush into world.state"` failed before the migration with `undefined` `aoe2.monkTasks` and passes after it; `npm.cmd test -- tests/simulation/saveLoad.test.ts -t "schema-1 sideMaps.monkTasks"` failed before the hydrate replacement fix with stale task resurrection and passes after it; `npm.cmd test -- tests/simulation/createSimulationBridge.unitMovement.test.ts -t "monkTasks diffs"` failed before guarded dirty marking with `aoe2.monkTasks: []` and passes after it. Targeted migration gates passed, then the full required gates passed after lockfile metadata sync: `npm.cmd test` (99 files, 737 passed, 1 skipped), `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`. Audit evidence: `npm.cmd audit --audit-level=high --omit=dev` found 0 vulnerabilities; `npm.cmd audit --audit-level=high` found no high/critical vulnerabilities and reported one pre-existing moderate `postcss` dev-tree advisory.

**Reasoning.** `monkTasks` was the KAD-0007/KAD-0008 blocker because it previously mixed AI decision writes with deterministic resolution. With AI now queuing context intentions, the task map can follow the same accessor contract as `monkCarriedRelic` and `monkHealCounters`: command handlers create tasks, deterministic Monk behavior consumes/clears them, and snapshots see the state through `world.state`. `unitCommands` remains the final bridge-owned Tier-1 codec before Phase 2F/schema-2 can remove redundant side-map projections.

**Notes.** This is internal replay/save infrastructure; the existing active `0.1.6-rc1` changelog metadata was updated only to keep the roadmap/release marker accurate.
