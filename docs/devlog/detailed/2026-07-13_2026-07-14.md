# 2026-07-13 — continuous 60 Hz voxel motion (v0.2.1)

## Outcome and boundary

AoE now presents live units and moving wildlife continuously from the exact immediately preceding simulation tick. The simulation remains authoritative and still advances on fixed ticks; the renderer owns only disposable prior-position history, interpolation, eased facing, and procedural part poses. Teleports, rewinds, bridge swaps, generation changes, and non-adjacent tick pairs fail closed to the current position rather than blending across unrelated state.

The cross-game boundary remains unchanged. AoE owns unit roles, gait, heading history, body-forward conventions, and projected resource meaning. The sibling `voxel` package gains only a pure testing reporter for reference-scene frame samples; it does not schedule frames or receive game movement state.

## Implementation

- `RenderStore` snapshots exact prior unit and moving-resource positions before every forward simulation tick. A same-tick projection cannot overwrite that history, and a rewind clears it.
- `AoeVoxelPresentationCoordinator` uses the prior projection only when its tick is exactly `current.tick - 1`; missing or stale history snaps safely. Moving fine-grid resources now use the same display interpolation as units.
- `aoeVoxelUnitAnimation.ts` rotates the complete rigid actor through a bounded shortest-arc heading while preserving authored forward axes for humanoids, cavalry, monks, and siege silhouettes. Hit proxies and contact shadows use the same presented root/body transform.
- `renderMetricsCapture.ts` supplies the normal render/HUD path with alive-entity count and existing ECS metrics. AoE's game-specific `getDebugSnapshot()` remains on demand; full `WorldDebugger.capture()` serialization is no longer constructed by the bridge or routine render path.

## Browser evidence

The named reference was Windows, Headless Chrome 150, ANGLE D3D11 on an RTX 4090, 1280x720 viewport, DPR 1, foreground tab, live `aoe2-prototype` scene. After 60 warmup frames, 658 measured frames over 11.0 measured seconds (12.0 seconds including warmup) sustained 59.82 FPS. Browser callback intervals were p50 16.7 ms, p95 16.8 ms, p99 16.8 ms, max 33.3 ms. Full main-thread callback work was p50 3.9 ms, p95 8.2 ms, p99 11.9 ms, max 19.5 ms. The presented scene held 13 draw calls, 10,396 triangles, 371 instances, 12 terrain chunks, and seven instance batches.

A live selected scout was ordered east and then north through a path corner. Across 150 sampled frames, 69 were moving, 70 positions were distinct, and 78 samples had fractional world coordinates. The root's maximum frame step was 0.1867 world units (p95 0.168). Its maximum heading change was 0.230 radians / 13.2 degrees per frame (p95 0.169), and the facing-to-velocity dot product had median 0.914 and p95 0.996. The locomotion weight and gait phase advanced with displayed speed. `output/playwright/60hz-motion/aoe2-voxel-60hz.png` was inspected at original resolution. This was the original interpolation measurement, captured before the follow-up ECS-publication fix below; it is performance/facing evidence, not the final continuity bound.

These measurements establish the named reference scene and environment. They do not guarantee an exact callback cadence on arbitrary hardware, background tabs, high-DPI viewports, or contended operating systems.

## Verification status

Focused contracts cover exact previous-tick capture, same-tick/rewind behavior, multi-tick coordinator fail-closed behavior, moving-resource interpolation, generation preservation, complete-actor facing, authored forward axes, shortest-arc turns, grounded feet, shadow/hit-proxy parity, and the lightweight live/replay metrics path. The complete `npm.cmd run verify` gate is green: content validation, 1,926 unit tests plus two skips across 254 files, 104 Chromium tests plus two intentional skips, zero-error lint, typecheck, and the 443-module production build.

The first complete run exposed two integration gaps that focused role tests did not: a newly idle militia was initially rotated away from its authored recipe axis, changing an exact overlapping click stack, and malformed test projections could reach a missing role-to-forward lookup. Initialization now starts every known role on its authored forward axis and uses a neutral finite fallback for unknown projections. The exact browser selection regression, all 35 focused animation/adapter tests, and the subsequent complete gate pass. Durable measurement data is recorded in `docs/devlog/artifacts/2026-07-13-60hz-motion.json`.

# 2026-07-13 — standalone voxel-only world renderer (v0.2.0)

## Outcome and authority

User direction made voxel graphics the only AoE2 graphics path going forward. The application now creates one interactive Three.js canvas through the sibling `voxel` package. There is no renderer selector, compatibility fallback, second world/input canvas, Phaser source tree, Phaser package, or Phaser production chunk. The DOM HUD, minimap, dialogs, annotation UI, and replay timeline remain UI and do not paint the playable world.

Simulation state remains authoritative. The renderer consumes copied projected data and owns disposable camera, presentation, capture, GPU, listener, resize, and animation-frame state only. Terrain remains elevation-zero. Raised entity clicks use a deterministic AoE-owned proxy generated from the same voxel part recipes; raised terrain and a generic engine query remain future work rather than behavior delegated to a hidden renderer.

## Implementation

- Added `AoeVoxelGameView` as the browser host for bridge stepping, one-canvas lifecycle, direct capture, bridge replacement, resize/fullscreen synchronization, and deterministic frame ordering.
- Moved isometric projection, interpolation, view state, entity hit testing, unit/building roles, camera control, pointer normalization, drag selection, and click/context selection into renderer-neutral `src/rendering/` and `src/input/` modules.
- Added `AoeVoxelPresentationCoordinator` to interpolate projected units, inject simulation display time, center the initial base, and publish stable building/health/placement/display diagnostics.
- Added AoE-owned voxel feedback parts for selection rings, conditional health bars, placement markers, death debris, and hit sparks. Fog now shades terrain snapshot tints instead of allocating transparent per-cell overlay instances.
- Added visible four-segment voxel marquee geometry even for empty drag selections, and invalidated presentation immediately for selection changes while the simulation is paused.
- Derived health-bar height from transformed authored parts and added recipe-projected click/context hit regions for raised walls, units, resources, and roofs. A real canvas click on the Town Center tower roof now selects the Town Center even though the ground-plane inverse misses its footprint.
- Made bootstrap transactional: top-level failures render an accessible fatal panel, constructor/setup failures unwind WebGL/canvas/controllers/listeners/HUD/recording resources, and annotation capture failures keep the marker while reporting the missing attachment.
- Visible 15 FPS elapsed time is no longer truncated to 50 ms. The host preserves elapsed time up to a named 250 ms catch-up bound and resets its clock/input state on document visibility changes.
- Deleted `src/phaser/`, renderer-mode parsing, composite Canvas2D capture, painter-only tests/diagnostics, and the `phaser` dependency/manual Vite chunk. Annotation capture reads the direct Three capture.
- Preserved save/load, replay epochs, browser automation, minimap camera state, input semantics, smooth root interpolation, speed-matched gait, context-loss fencing, and idempotent teardown contracts.

## Measured migration evidence

The production build transforms 442 modules and emits one application JavaScript chunk at 1,275.74 kB (322.26 kB gzip). The former build also emitted a separate approximately 1,481 kB Phaser chunk. The fixed 1280x720/DPR1 evidence is `docs/devlog/artifacts/2026-07-13-voxel-only-{before,after,diff}.png`; 106,615 of 921,600 pixels differ (11.57%). The fully presented after state reports revision 80/80, 12 chunks, six material resources, one geometry, seven batches, two animated batches/26 animated instances, 365 total instances, 12 draw calls, 10,336 triangles, seven renderer geometries, one texture, and no context loss.

## Verification status

`npm.cmd run verify` is green: content validation, 1,905 unit tests plus two skips, 104 Chromium tests plus two intentional visual-baseline skips, zero-error lint, typecheck, and the 442-module production build. Focused coverage additionally proves the no-Phaser source/dependency/build contract, camera math, presented recipe picking, current-target exact-click cycling, moving-unit silhouette clicks, visible empty marquee, paused selection presentation, context-loss input fencing, renderer lifecycle, and one-canvas ownership. The complete gate exposed two deterministic multi-thousand-tick scenarios whose existing 30-second caps were too tight under full-suite contention; isolated runs completed in 22.6 and 24.5 seconds, so only those documented caps received measured 60-second headroom.

The generated source and bundle scans report no legacy graphics matches. `npm ls` resolves one `three@0.185.1` shared by AoE and `voxel`, with no Phaser or EventEmitter renderer dependency. Both `npm audit --omit=dev` and the full `npm audit` report zero vulnerabilities. The unchanged sibling engine passes 80/80 tests, typecheck, lint, and build. Three independent live-code reviewers re-read the final implementation after lifecycle, bounding-box, presented-hit, exact-click-cache, and moving-unit evidence repairs and found no remaining substantive issue. The durable review record is `docs/threads/done/voxel-renderer-migration/2026-07-13/7/REVIEW.md`.

# 2026-07-13 — Voxel 0.1.4 reproducible integration and migration closeout (no version bump)

**Timestamp:** 2026-07-13 18:42 PDT

## Action

Released the sibling Voxel 0.1.4 work as reachable Git history, including the follow-up verification hardening that brings the packaging script under lint and documents headless Chromium provisioning. Voxel `origin/main` now resolves to `faa00bf2ef83e5134bd9ca62c0b36cd0cfbe394b`; AoE's `.github/voxel-commit` pins that exact SHA, and `npm install` re-resolved `package-lock.json` so the linked package records version 0.1.4 and its current development metadata.

Added `resolve.dedupe: ['three']` to the standalone `vitest.config.ts`, matching production Vite instead of assuming the two configurations inherit from one another. A focused constructor-identity assertion failed before the configuration change and passed after it. The existing production browser identity assertion was not weakened or replaced.

Adopted Voxel's new `raycastDensePaletteChunks` only as a compatibility contract test for AoE's uniform 16×1×16 terrain snapshot chunks, including a missing-chunk seam. Runtime flat-ground input continues to use the exact analytic inverse, while raised and moving entities continue to use epoch/revision-gated recipe silhouettes that match presented geometry. The portable dense-occupancy helper is not presented-revision-bound and does not compose geometry or instance hits.

Reconciled the active graphics roadmap and migration documentation with the voxel-only runtime. Phaser-era delivery details remain explicitly bounded historical evidence, stale projection and completion claims are corrected, and the completed `isometric-overhaul` and `voxel-renderer-migration` threads are archived under `docs/threads/done/` with their dated review records preserved.

## Code reviewer comments

**Codex — Voxel release audit.** The reviewer found that the new package-verification script was initially outside the lint surface and that clean-machine browser provisioning was undocumented. The follow-up Voxel commit added the scripts lint scope, a non-type-aware ESLint override, a typed error cause, `test:browser:install`, and README guidance. Voxel's final full verification and both audits passed before publication.

**Codex — AoE code/config review.** The reviewer verified the remote-reachable SHA, 0.1.4 lock metadata, one resolved Three version, focused tests, unchanged production identity assertion, and preserved presented-picking path. It found one LOW stale comment in `vite.config.ts` claiming Vitest inherited Vite aliases; the comment now states that the standalone config mirrors shared resolve settings explicitly, and re-review closed the finding.

**Codex — migration documentation review.** The reviewer verified the thread moves, byte-preserved dated review records, historical bounding, live links, and ray-query limitation. It found one MEDIUM stale roadmap statement that said elevation was missing from `ProjectedEntityView`; the field and live projection already exist. The roadmap now identifies the missing simulation-side attacker/target elevation damage rule, and re-review closed the finding. The final synthesis is `docs/threads/done/voxel-014-integration/2026-07-13/1/REVIEW.md`.

## Result

Voxel verification passed 12 Vitest files and 108 tests, typecheck, lint, build, the core-only packaged import/typecheck gate, and one headless Chromium lifecycle test. Voxel's `npm audit --omit=dev` and full `npm audit` both reported zero vulnerabilities.

AoE's final `npm run verify` passed content validation for 18 supported and 12 unsupported civilizations; 255 Vitest files passed and one was skipped, with 1,928 tests passed and two skipped out of 1,930; Playwright ran 106 serial headless Chromium tests with 104 passed and two skipped; lint and typecheck passed; and the production build transformed 444 modules. AoE's `npm audit --omit=dev` and full `npm audit` both reported zero vulnerabilities. Port 4173 had no listener after the browser gate.

The first integrated fixture used a stale `complete` property and was rejected by typecheck after the unit portion of the gate; replacing it with the current `visualVariant` contract made the focused tests and typecheck green before the successful complete rerun. A later restricted-sandbox attempt could not rebuild the linked Voxel `dist/`; the canonical command was rerun with the required sibling-write access and completed successfully in 356.3 seconds.

## Reasoning

The exact pushed SHA and lock metadata make CI reproducible; the local `file:../voxel` link alone does not. Mirroring Three deduplication in Vitest tests the same constructor-identity invariant as production without removing the browser backstop. Keeping the portable ray helper at the terrain-data boundary avoids falsely treating occupancy data as the pixels currently presented for animated or raised entities.

## Notes

This integration changes dependency reproducibility, test resolution, compatibility coverage, and documentation state only. It introduces no user-visible behavior, public API, save-format, or architecture-flow change, so `package.json`, `docs/changelog.md`, README, and the architecture topology remain unchanged.

# 2026-07-14 — motion continuity closure (v0.2.1 follow-up)

**Timestamp:** 2026-07-14 04:24 PDT

## Action

Closed the continuous-motion follow-up by publishing every fine transform immutably, aligning stationary spawns and commanded arrivals with one allocated occupancy slot, persisting numeric slot authority and explicit overflow across save/load and replay, and bounding blocked-scout recentering plus live/replay visible-time catch-up. Complete voxel actors retain speed-matched gait and turn through the shortest arc toward displayed travel, with authored-front assertions spanning every representative unit role.

Added deterministic browser coverage for a selected scout moving through a path corner, moving-body click selection after a presented cell crossing, true empty gaps between adjacent unit bodies, and a hostile health-bar fixture whose pristine baseline now boots paused. The presented epoch/revision-bound animated silhouette picker and the production Three identity check remain unchanged. The dense terrain ray helper remains compatibility-only and is not used for raised or moving entity picking.

## Code reviewer comments

**Codex — simulation continuity and persistence.** Independent reviewers found and closed an over-budget blocked-scout recenter, a coarse-only arrival assertion, a diagonal step-bound hole, a legacy-save first-step redirect, crowded mid-movement slot divergence, entity-id promotion of explicit overflow, and opportunistic promotion after a peer freed its slot. The final persistence matrix compares loaded and uninterrupted worlds tick by tick, preserves the serialized fine root, and converges to a newly available same-cell slot within the ordinary Euclidean step budget.

**Codex — browser picking and timing.** Reviewers reproduced three apparent selection regressions and proved they were test assumptions, not production picker failures. The old widest-pair midpoint landed exactly on a third villager after stationary slot alignment, and the moving-body test relied on a timing-dependent displayed/authority mismatch. The shared gap chooser now rejects third-body coverage, while the movement proof requires a presented cell crossing during `task === 'moving'`. A further review caught an arrival-only false-pass possibility and required the task assertion. The two timing-sensitive cases passed ten repetitions each, 20/20 total.

**Codex — complete-gate adversary.** The full browser gate exposed a live-start race in the conquest health-bar fixture: the militia could damage the house before the test asserted full health. Paused boot captures the baseline while the existing explicit 80-tick advance still proves damage. Re-review verified `advanceTicks` unpauses and restores pause in `finally`, and found no remaining substantive issue. The synthesis is `docs/threads/done/motion-continuity/2026-07-13/1/REVIEW.md`.

**Codex — final persistence and documentation adversaries.** A high-risk refuter independently verified numeric-slot-first reconstruction, legacy fallback ordering, explicit-overflow restoration, bounded freed-slot convergence, exact-slot command completion, immutable publication, and the Euclidean fine-step tests, with no substantive finding. The documentation audit found and closed three links to the pre-rollover detailed-devlog filename plus historical-plan wording that still called the deleted Phaser bootstrap recommended guidance; re-review was clean. The first repository-mandated external Codex/Claude launch was rejected before execution by the environment's private-data export policy, so no diff left the workspace at that time; that bounded outcome remains recorded in `docs/threads/done/motion-continuity/2026-07-14/2/REVIEW.md`. After the user explicitly authorized disclosure of the exact committed range `81dbb5f..13ce014`, iteration 3 completed: OpenAI Codex found no substantive bug, security issue, or performance regression, and Anthropic's `opus[1m]` fallback approved after live-code inspection and 34 focused tests. Claude's two non-blocking observations were verified as pre-existing/deferred rather than regressions, so no code change was required. The successful external synthesis is `docs/threads/done/motion-continuity/2026-07-14/3/REVIEW.md`.

## Result

The final disposable exact-pin checkout resolved reachable Voxel commit `faa00bf2ef83e5134bd9ca62c0b36cd0cfbe394b`; all 32 changed AoE source/test/config/lock files hash-matched the live worktree. `npm run verify` passed content validation for 18 supported and 12 unsupported civilizations; 256 Vitest files passed and one skipped, with 1,940 tests passed and two skipped out of 1,942; Playwright ran 107 serial headless Chromium tests with 105 passed and two skipped; lint and typecheck passed; and the production build transformed 444 modules. `npm audit --omit=dev` and full `npm audit` both reported zero vulnerabilities.

The same requested command was run from the live AoE worktree. Its Voxel prebuild and all 1,940 unit tests passed, then the browser prebuild stopped at AoE typecheck because the unrelated dirty sibling Voxel worktree adds three uncommitted `ThreeRenderMetrics` fields (`instancePresentationMatrixWrites`, `instancePresentationColorWrites`, and `instancePresentationUpdateRanges`). AoE remains pinned to exact reachable Voxel 0.1.4, whose isolated gate is green; adapting AoE to that uncommitted future surface would make the committed pin unreproducible, so no such change was made.

## Reasoning

Smooth interpolation cannot hide stale ECS publication or arrival-time slot replacement; continuity requires authoritative fixed-step endpoints to be observable and their destination identity to survive reconstruction. Likewise, test evidence must select genuinely empty presented space and prove interaction while a body is still moving, not infer those facts from pair width or authority lag. Exact-pin isolation separates AoE correctness from concurrent sibling development without discarding either worktree.

## Notes

This follow-up remains part of v0.2.1 rather than receiving a second version bump. The first v0.2.1 commit and this repair are one coherent user-visible movement change. The debug trace is `docs/debugging/2026-07-13-motion-continuity.md`; the assigned-slot persistence lesson is recorded in `docs/learning/lessons.md`; screenshots remain under ignored `output/playwright/60hz-motion/` and were not staged.

# 2026-07-14 — concurrent villager boar hunting (v0.2.2)

**Timestamp:** 2026-07-14 10:04 PDT

## Action

Confirmed with the live `boar-hunt-fixture` that one six-villager selection and one entity context order already create six independent attack commands: all six retain the same boar target, two adjacent villagers strike in the first tick, and boar HP falls from 75 to 69. Converted the wildlife persistence regression from six separate selections/orders to that same one-order group path.

The remaining failure was input-side. Presented voxel command hits correctly rank a front unit silhouette above a resource, so once a friendly villager visually overlapped the boar, the controller selected that villager as the context target and changed the selected hunters to move commands. Added a controller regression that failed with zero retained attacks before the fix. Villager context arbitration now skips human-owned unit hits only when a resource, building, or non-owned unit exists farther down the same presented hit list; it falls back to the original first hit otherwise. Raw silhouette geometry/order, selection cycling, presentation revision fencing, and every non-villager command path remain unchanged.

Recorded the concurrent-attacker/non-exclusive-retaliation rule and input boundary in `design/spec-final.md`, updated the active roadmap baseline, published the user-facing v0.2.2 changelog/version metadata, and added the thread design, plan, and adversarial review record.

## Code reviewer comments

**Codex — simulation and presentation trace.** Independent tracers verified that `humanInputOps` emits one command per selected villager, `unitCommandsCodec` is keyed by attacker rather than target, each cooldown/damage path is independent, and the boar's `targetEntityRef` controls retaliation only. A real Voxel-recipe probe found reachable villager/boar polygon overlap at valid melee fine slots, proving the controller failure was not only synthetic.

**Codex — correctness adversary.** The reviewer found no substantive code issue. It verified that arbitration is command-only, villager-only, preserves raw selection ordering and Monk healing, retains friendly building/enemy/resource ordering, and falls back to the original target when no deeper actionable hit exists.

**Codex — tests and documentation adversary.** The reviewer ran 13 focused tests and found no simulation, save/replay, determinism, API, or architecture defect. It flagged the missing mandatory devlogs as MEDIUM and the stale active roadmap v0.2.1 label as LOW; both were corrected before final re-review. A final LOW summary-length finding was closed by compacting the oldest process-only bullet to keep the file at exactly 50 lines. The converged synthesis is `docs/threads/done/concurrent-boar-hunt/2026-07-14/2/REVIEW.md`.

## Result

The focused regression set passed four files and 13 tests: raw presented-hit ordering, controller arbitration, one-order concurrent wildlife combat, and group-kill save/load persistence. The disposable checkout used committed Civ Engine `a3da2eedb18115bb9cd4dc22db3b4f8ce4746f5c` and exact reachable Voxel 0.1.4 `faa00bf2ef83e5134bd9ca62c0b36cd0cfbe394b`. `npm run verify` passed content validation for 18 supported and 12 unsupported civilizations; 256 Vitest files passed and one was skipped, with 1,942 tests passed and two skipped out of 1,944; Playwright ran 107 serial headless Chromium tests with 105 passed and two skipped; lint and typecheck passed; and the production build transformed 444 modules. `npm audit --omit=dev` and full `npm audit` both reported zero vulnerabilities.

The first disposable gate attempt stopped during unit tests because the clean Civ Engine clone had not yet generated its untracked `dist/` package output. Building that exact committed engine fixed the verification environment; the unchanged gate then passed. Port 4173 was free before the browser phase and again after its owned server PID exited. The user-requested localhost Vite server on port 5173 remained running and was not touched.

## Reasoning

The simulation did not need an attacker reservation or group-combat rewrite; adding either would have expanded save/replay and movement semantics without addressing the observed input failure. Choosing the first non-friendly-unit target only for villager context orders fixes hunters crowding a boar while preserving the presented silhouette proxy as the geometry authority and retaining Monk-friendly-unit semantics.

## Notes

This is a non-breaking user-visible bug fix, so it receives the single patch bump from v0.2.1 to v0.2.2 and a changelog entry. It adds no public API, save-format field, dependency, or structural subsystem, so README and the architecture topology, drift log, and decisions log remain unchanged.
