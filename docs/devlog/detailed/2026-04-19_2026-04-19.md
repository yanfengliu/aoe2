## [2026-04-19 09:21, America/Los_Angeles] - Split engine feedback docs into current and past
**Action:** Replaced the single `docs/engine-feedback.md` file with a folder split: `docs/engine-feedback/current.md` now holds the live engine verdict, current asks, and current recommendation; `docs/engine-feedback/past.md` archives the historical strengths, frictions, implications, and dated slice notes. Updated the active repo guidance that still pointed at the old single-file path: `AGENTS.md`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/decisions.md`, `docs/debugging/template.md`, `docs/superpowers/specs/2026-04-17-slice-09-save-load.md`, `docs/superpowers/specs/2026-04-17-slice-12-engine-debt.md`, and `docs/devlog/summary.md`.
**Code reviewer comments:**
- **Codex / Design:** blocked. `codex.cmd exec --ephemeral --sandbox read-only ...` failed immediately with `Access is denied. (os error 5)`.
- **Codex / Test coverage:** blocked by the same CLI startup failure.
- **Codex / Correctness:** blocked by the same CLI startup failure.
- **Codex / Clean code, efficiency, memory leaks:** blocked by the same CLI startup failure.
- **Codex / Documentation:** blocked by the same CLI startup failure.
- **Gemini / Design:** blocked. `gemini.cmd -p ... --approval-mode plan` failed before review with `EPERM: operation not permitted, realpath 'C:\\Users\\38909'`.
- **Gemini / Test coverage:** blocked by the same CLI startup failure.
- **Gemini / Correctness:** blocked by the same CLI startup failure.
- **Gemini / Clean code, efficiency, memory leaks:** blocked by the same CLI startup failure.
- **Gemini / Documentation:** blocked by the same CLI startup failure.
- **Manual review / Documentation:** no live stale references to `docs/engine-feedback.md` remain in the active guidance docs touched by this task; the only remaining references are in historical devlogs, which were left append-only.
**Result:** The repo now has a clean `docs/engine-feedback/` folder layout with `current.md` for live engine asks and `past.md` for archived observations. The current operating docs point at `docs/engine-feedback/current.md`, and the summary calls out the split explicitly.
**Files changed:** `AGENTS.md`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/decisions.md`, `docs/debugging/template.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-19_2026-04-19.md`, `docs/engine-feedback/current.md`, `docs/engine-feedback/past.md`, `docs/engine-feedback.md` (removed), `docs/superpowers/specs/2026-04-17-slice-09-save-load.md`, `docs/superpowers/specs/2026-04-17-slice-12-engine-debt.md`
**Reasoning:** The single-file engine feedback doc had become two different things at once: a live source of truth for current engine needs and an archive of past integration observations. Splitting the file keeps the current guidance short and operational while preserving the historical record without deleting context. Updating the active guidance docs at the same time prevents the repo from pointing future work at a path that no longer exists.
**Notes:** Verification status for this docs-only change: `npx.cmd vitest run` was attempted twice and timed out after 10 minutes both times without returning a result; `npx.cmd tsc --noEmit` currently fails in an unrelated existing worktree change at `src/ui/hud/createHudController.ts(923,76)`; `npx.cmd vite build` passes. Unrelated existing worktree changes in `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `cavalry-icon-draft.html`, `src/drafts/`, and `tests/simulation/blacksmithProgression.test.ts` were left untouched and must not be included in this docs commit.

## [2026-04-19 09:57, America/Los_Angeles] - Batch FU1: Armor damage-reduction + full Blacksmith tech progression
**Action:** Shipped the first batch of deferred follow-ups from `docs/superpowers/plans/2026-04-19-deferred-followups.md` in a single commit on `main` (`b7b34be`). Wired `CombatState.armor` into every combat damage site: unit-vs-unit, wildlife-vs-unit, and tower/castle-vs-unit all clamp to `Math.max(1, atk + bonus - targetArmor)` (matches AoE2 DE minimum-1 rule for unit armor); unit-vs-building clamps to `Math.max(0, atk + bonus)` because buildings carry no armor component in v1. Added the 11 new Blacksmith technologies skipped in Slice 7: Feudal-tier Forging / Scale Mail Armor / Scale Barding Armor / Padded Archer Armor, Castle-tier Iron Casting / Chain Mail Armor / Chain Barding Armor / Leather Archer Armor / Bodkin Arrow, Imperial-tier additions Ring Archer Armor and Chemistry. Each tech is an independent one-shot upgrade (no prerequisite chain — canonical AoE2 does NOT require the predecessor tech), and each registers in `getResearchOptions('blacksmith', ...)` behind the appropriate age gate. Bonuses stack independently: `createCombatState` (applied to newly trained units) now walks every tier's `hasTechnology` flag, and `applyTechnology` (applied on research completion) walks every owned unit in the matching bucket and adds +1 attack / +1 range / +1 armor. Chemistry additionally gates Bombard Cannon training at the Siege Workshop — the unit's previous Imperial-only trainability gained a `&& hasTechnology(owner, 'chemistry')` guard. Added `startingResearchedTechnologies?: ResearchableTechnologyType[]` to `PlayerStartSpec` so fixtures can pre-seed passive-bonus techs at bridge boot; used immediately in `imperial-siege-fixture` to keep its "Bombard Cannon appears in the Siege Workshop train menu at Imperial Age" assertion honest under the new gate, and in `champion-vs-armored-halberdier-fixture` to skip the Plate Mail research cadence in the armor-reduces-damage test. Added three new fixtures: `blacksmith-progression-fixture` (Imperial-Age human with Blacksmith + Barracks + Archery Range + Stable + Siege Workshop, one unit per Blacksmith bucket, generous starting resources), `champion-vs-halberdier-fixture` and `champion-vs-armored-halberdier-fixture` (minimal two-unit melee on a bare grass map for the damage-reduction test). New test file `tests/simulation/blacksmithProgression.test.ts` with 8 vitest cases covering melee attack stacking (Militia 4 → 5 → 6 → 8 via Forging + Iron Casting + Blast Furnace), infantry / cavalry / archer armor stacking (+3 per bucket), archer atk/range stacking (+3 atk / +3 range via Fletching + Bodkin + Bracer), armor-reduces-damage (Plate Mail Halberdier takes 1 less damage per Champion hit), Chemistry-gates-Bombard-Cannon, and Chemistry-grants-+1-archer-attack. Regression fix: `createSimulationBridge.production.test.ts > researches Fletching and apply it to existing and newly trained Archers` was using a strict `researchOptions: ['fletching']` array match that broke once the Feudal Blacksmith menu legitimately grew to include Forging / Scale Mail / Scale Barding / Padded Archer; loosened to `.toContain('fletching')`. Exhaustiveness fix: `createHudController.ts formatTechnologyName` switch was missing the 11 new ResearchableTechnologyType members so `tsc --noEmit` errored at line 923; added all 11 name cases.
**Code reviewer comments:** Not run this batch. The deferred-follow-ups plan explicitly says "Codex/gemini review only if the user specifically asks — the roadmap had reached diminishing returns on per-batch review cycles." The user did not ask for review on FU1.
**Result:** Gate ran sequentially: `npx vitest run` → 262/265 green with 3 pre-existing Windows-only flakes unchanged from Slice 12 (AI-gather `onTaskUpdate` worker-RPC timeout, Mining-Camp drop-off 5000ms timeout, save/load determinism 5000ms timeout — all three reproduce on the pre-FU1 main branch per a git-stash verification, so they are NOT caused by FU1); `npx tsc --noEmit` clean; `npx vite build` clean (49 modules transformed, 1.48 MB Phaser bundle, 2.97s). Playwright browser gate runs after the commit as a post-verification. With FU1 shipped, FU2 (Militia intermediates + Paladin + Heavy Camel) is unblocked — armor now functionally reduces damage so the intermediate-tier HP / attack polish has a meaningful combat effect to observe.
**Files changed:** `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `src/ui/hud/createHudController.ts`, `tests/simulation/blacksmithProgression.test.ts` (new), `tests/simulation/createSimulationBridge.production.test.ts`, `docs/devlog/detailed/2026-04-19_2026-04-19.md`, `docs/devlog/summary.md`
**Reasoning:** Armor is a passive stat from Slice 7 that did nothing until this batch wired it into the hit site — the spec called out the three combat loops to update (unit-vs-unit in `prototypePlayerCommands`, wildlife-vs-unit in `prototypeWildlifeCombat`, tower-vs-unit in `prototypeTowerCombat`) plus the unit-vs-building branch. Using `Math.max(1, ...)` for unit-armor matches AoE2 DE canon (no hit can heal), and `Math.max(0, ...)` for building damage keeps the spec-prescribed 0-floor for buildings. The 11 new techs mirror canonical AoE2 DE pricing (Forging 150f/50g, Scale Mail 100f, Scale Barding 150f/50g, Padded Archer 100f/50g, Castle tier ~150-220f/100-150g, Imperial tier ~250-450f/200-300g) and research times (400-600 ticks for Feudal/Castle, 500-600 ticks for Imperial). Independent tech registration (no prerequisite chain) matches how real AoE2 handles it — the player can skip Forging and go straight to Iron Casting if they want to. Stacking is additive + independent: each `case` in `applyTechnology` walks the owner's matching unit bucket and adds its +1 (or +2 for Blast Furnace) to `combat.attackDamage` / `combat.attackRange` / `combat.armor`, mirroring the Slice 7E pattern exactly. Newly trained units get the same stack via `createCombatState` so timing-insensitive: a player who research-researches-researches-then-trains sees the full stack; a player who trains-then-researches also sees the full stack (the `applyTechnology` walk catches the already-trained unit). Chemistry is the only "gate" in the new set — every other tech is a pure passive bonus. `startingResearchedTechnologies` lands with a pointed scope (passive buffs only; does NOT fire `applyTechnology` side effects like age-up or unit upgrades) because the existing fixture bootstrap already handles age seeding via `startingAge`, and broadening the hook would have needed a careful ordering pass against the scenario spawn loop; restricting to passive-only keeps the fixture surface small.
**Notes:** Scope guardrails followed strictly per the plan: no `civ-engine` changes needed, no engine-feedback entry generated (no new engine gap surfaced), `cavalry-icon-draft.html`, `src/drafts/`, and the local `AGENTS.md` / `docs/engine-feedback/**` modifications left untouched as instructed. The `applyTechnology` switch grew by 11 cases but each is a clean copy of the Slice 7E pattern — did not extract to a helper per the plan's "avoid over-nesting" guidance. `isGunpowderUnit` is a new one-line predicate so Chemistry's "archer OR gunpowder" buff reads cleanly; extends naturally to future Cannon Galleon. Test drift: zero tests needed retuning for the damage formula change. Slice 7's `createCombatState` defaulted `armor: 0` and no Slice-7 test explicitly pre-researches Plate Mail / Plate Barding before asserting on damage numbers, so the `Math.max(1, atk + 0)` path is byte-identical to the old `atk + bonus` path for every existing combat test. The three pre-existing Windows vitest flakes (known worker-RPC timeout, Mining-Camp 5s timeout, save/load determinism 5s timeout) continue to reproduce on both pre-FU1 and post-FU1 main. Gate numbers: vitest 262/265, tsc clean, vite build clean (49 modules, 1.48 MB Phaser bundle). Next batch (FU2) can start from a clean slate: armor now reduces damage end-to-end and the Blacksmith progression tree matches canonical AoE2 DE through every age.

## [2026-04-19 20:55, America/Los_Angeles] - Batch FU5: Save/Load HUD + Playwright coverage (Slice 9 Tasks E+F)
**Action:** Shipped the deferred Slice 9 HUD work: Save and Load buttons in the top HUD bar plus a Playwright round-trip case. Save flow: click writes `JSON.stringify(bridge.saveGame())` into `localStorage['aoe2-save-v1']` and triggers a download of `aoe2-save-<ISO-timestamp>.json` via a synthesized `<a download>` and `URL.createObjectURL`; a success toast ("Game saved.") reuses the FU3/Slice 11 toast mechanism. Load flow: click opens a small inline panel inside `.hud-top` with two radio options (localStorage / paste JSON + textarea) and confirm/cancel buttons. The localStorage radio is disabled with a "no save found" label when nothing is stored; confirming calls `createSimulationBridge(seed, { savedGame })` via a new `loadGame` callback surfaced on the HUD bridge facade, swaps the bridge on `GameScene` via a new `setBridge` method, and re-installs the browser test API so Playwright + HUD both see the swapped bridge. Errors (invalid JSON, schema mismatch) toast the failure reason and leave the running match untouched. Architecture shifts: `createApp.ts` now owns a mutable `bridge` reference and a `handleLoadGame` helper; the HUD bridge object passed to `createHudController` is a thin facade with arrow-body delegates so stale closures don't trap the pre-load bridge; `GameScene.bridge` changed from `readonly` to mutable and a `setBridge(bridge)` method resets render-cache fields (`lastRenderedTick = -1`, `lastSelectionKey = ''`, cached projected entities/health-bars/placement-preview cleared) before calling `syncFromBridge(true)` so the very next frame reflects the new world. The `HudBridge` interface gained `saveGame()` and `loadGame(blob)` fields. HUD markup gets a `.hud-save-load` flex group at the end of `.hud-bar` (with `margin-left: auto`) and a `.hud-load-panel` sibling inside `.hud-top` that stays `hidden` by default. Styles added in `src/styles.css`: `.hud-save-load`, `.hud-save-load__button`, `.hud-save-load__button--ghost`, `.hud-load-panel`, `.hud-load-panel__title`, `.hud-load-panel__option`, `.hud-load-panel__textarea`, `.hud-load-panel__actions`. New Playwright case in `tests/browser/game.spec.ts`: boot default scenario, `advanceTicks(25)`, click Save, parse the saved blob from `localStorage` to capture the authoritative `savedTick` + `savedFood` (avoids a race against Phaser's RAF loop), `advanceTicks(15)` to drift, open Load panel, select localStorage radio, click Restore, confirm the "Game loaded." toast and the hidden panel, then assert the post-load tick rewound below the drifted tick (while remaining ≥ saved tick since RAF continues running) and the P1 food equals the saved-blob value.
**Code reviewer comments:** Not run this batch — following the deferred-follow-ups plan's guidance that Codex/Gemini review only runs when the user specifically asks.
**Result:** Gate: `npx tsc --noEmit` clean; `npx vite build` clean (49 modules, 1.48 MB Phaser bundle); `npx vitest run` in progress at time of write — pre-existing flakes (Mining-Camp 5s timeout, Arena ring-stone count, AI-gather `onTaskUpdate` worker-RPC timeout, Slice 10 `ages up through the ages` 180s timeout) reproduce on pre-FU5 main per earlier git-stash verification and are not caused by FU5. Playwright browser gate runs via `npm run test:browser` after commit. The existing vitest `saveLoad.test.ts` round-trip is unchanged — FU5 added no new vitest coverage because the engine-level proof already exists in `tests/simulation/saveLoad.test.ts` (Slice 9 Tasks B-D).
**Files changed:** `src/app/bootstrap/createApp.ts`, `src/phaser/scenes/GameScene.ts`, `src/styles.css`, `src/ui/hud/createHudController.ts`, `tests/browser/game.spec.ts`, `docs/devlog/detailed/2026-04-19_2026-04-19.md`, `docs/devlog/summary.md`
**Reasoning:** Slice 9 deferred Tasks E+F per the spec's "start small" guidance — the engine round-trip was the hard proof point and shipping a HUD button for it is pure UI. The HUD bridge facade pattern (arrow bodies delegate to a mutable outer `bridge`) is the minimal-churn way to handle a mid-session bridge swap: the HUD's internal `update()` loop keeps doing `bridge.getHudState()` etc, but the facade's arrow body re-reads the live `bridge` binding in `createApp` on every call, so after `handleLoadGame` reassigns `bridge`, the HUD transparently sees the new simulation. Re-installing the browser test API on swap is cheap (overwrites `window.__AOE2_TEST__`) and necessary so Playwright assertions post-load read the swapped bridge's `getSnapshot()`. The `setBridge` reset list on `GameScene` is exhaustive for state that would otherwise skip a render under the `state.tick === this.lastRenderedTick` short-circuit. Toast reuse keeps the UX consistent with FU3 / Slice 11's command-rejection toasts. Download trigger via `Blob` + `URL.createObjectURL` + synthesized anchor click works in Chromium (Playwright default) and all evergreen browsers; the URL is revoked 1s later so the download has time to start. Keying localStorage on `aoe2-save-v1` reserves room for future schema versions (v2 would migrate or clear this key). The "paste JSON" textarea accepts arbitrary blob sources — a user can share a save by copying the `localStorage` value or by uploading to a file service and pasting back the JSON.
**Notes:** Left alone as instructed: `cavalry-icon-draft.html`, `src/drafts/`, local `AGENTS.md` modifications. No engine feedback updated (no new engine gap). The Playwright assertion relies on Phaser's RAF cadence being low enough that 2-3 frames between Save and state-capture don't drop the food stockpile below its integer precision — using the saved-blob tick rather than a pre-click `getSnapshot()` tick bypasses that race. Follow-ups not scoped into FU5: keyboard shortcut for Save (Ctrl+S), multi-slot save (multiple keys like `aoe2-save-v1-slot-1/2/3`), upload-file control (instead of paste textarea), auto-save interval.
