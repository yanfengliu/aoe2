## [2026-04-19 09:21, America/Los_Angeles] - Split engine feedback docs into current and past
**Action:** Replaced the single `docs/engine-feedback.md` file with a folder split: `docs/engine-feedback/current.md` now holds the live engine verdict, current asks, and current recommendation; `docs/engine-feedback/past.md` archives the historical strengths, frictions, implications, and dated slice notes. Updated the active repo guidance that still pointed at the old single-file path: `AGENTS.md`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/decisions.md`, `docs/debugging/template.md`, `docs/superpowers/specs/2026-04-17-slice-09-save-load.md`, `docs/superpowers/specs/2026-04-17-slice-12-engine-debt.md`, and `docs/devlog/summary.md`.
**Code reviewer comments:**
- **Codex / Design:** blocked. `codex.cmd exec --ephemeral --sandbox read-only ...` failed immediately with `Access is denied. (os error 5)`.
- **Codex / Test coverage:** blocked by the same CLI startup failure.
- **Codex / Correctness:** blocked by the same CLI startup failure.
- **Codex / Clean code, efficiency, memory leaks:** blocked by the same CLI startup failure.
- **Codex / Documentation:** blocked by the same CLI startup failure.
- **Gemini / Design:** blocked. `gemini.cmd -p ... --approval-mode plan` failed before review with `EPERM: operation not permitted, realpath 'C:\\Users\\38909'`.
- **Gemini / Test coverage:** blocked by the same CLI startup failure.
- **Gemini / Correctness:** blocked by the same CLI startup failure.
- **Gemini / Clean code, efficiency, memory leaks:** blocked by the same CLI startup failure.
- **Gemini / Documentation:** blocked by the same CLI startup failure.
- **Manual review / Documentation:** no live stale references to `docs/engine-feedback.md` remain in the active guidance docs touched by this task; the only remaining references are in historical devlogs, which were left append-only.
**Result:** The repo now has a clean `docs/engine-feedback/` folder layout with `current.md` for live engine asks and `past.md` for archived observations. The current operating docs point at `docs/engine-feedback/current.md`, and the summary calls out the split explicitly.
**Files changed:** `AGENTS.md`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/decisions.md`, `docs/debugging/template.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-19_2026-04-19.md`, `docs/engine-feedback/current.md`, `docs/engine-feedback/past.md`, `docs/engine-feedback.md` (removed), `docs/superpowers/specs/2026-04-17-slice-09-save-load.md`, `docs/superpowers/specs/2026-04-17-slice-12-engine-debt.md`
**Reasoning:** The single-file engine feedback doc had become two different things at once: a live source of truth for current engine needs and an archive of past integration observations. Splitting the file keeps the current guidance short and operational while preserving the historical record without deleting context. Updating the active guidance docs at the same time prevents the repo from pointing future work at a path that no longer exists.
**Notes:** Verification status for this docs-only change: `npx.cmd vitest run` was attempted twice and timed out after 10 minutes both times without returning a result; `npx.cmd tsc --noEmit` currently fails in an unrelated existing worktree change at `src/ui/hud/createHudController.ts(923,76)`; `npx.cmd vite build` passes. Unrelated existing worktree changes in `src/game/simulation/createSimulationBridge.ts`, `src/game/simulation/prototypeScenario.ts`, `src/game/simulation/types.ts`, `cavalry-icon-draft.html`, `src/drafts/`, and `tests/simulation/blacksmithProgression.test.ts` were left untouched and must not be included in this docs commit.
