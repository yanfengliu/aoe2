# 2026-07-19 — narrow-choke queueing (v0.3.2)

- **Timestamp and request:** 2026-07-19; units entering a narrow space should wait behind one another instead of wrapping around the leader, with temporary unit traffic distinguished from permanent roadblocks.
- **Research:** AoE II update history and Ensemble's coordinated-movement material support a two-layer model: durable terrain/structures drive route search, while moving units are handled by local coordination. The source record and applied boundary are in `docs/threads/done/narrow-choke-queueing/RESEARCH.md`.
- **RED reproduction:** the real `narrow-corridor-fixture` placed three villagers abreast in one coarse corridor cell; the first contract failed with two entrants on tick 2. Successive adversarial passes produced REDs for task movers, bends, head-on flow, same-tick activation, misleading targets, longer cycles, co-located origins, stale intent, multi-occupancy, and owner isolation.
- **Action:** inserted `movementTrafficOps.ts` between permanent-only A* planning and fine stepping for commanded, monk, and villager-task movement. Straight and turning one-cell legs use an owner-scoped origin reservation and centered lane; acyclic followers wait with their route intact, while a fully occupied directed dependency loop releases only its lowest eligible id when every branch closes through a current or previous-tick caller.
- **Persistence:** `UnitTransformComponent` additively carries direction X/Y, intent key, and attempt tick; legacy state learns the fields on first use. Arbitration never reads the transient route cache. `movementPersistenceTypes.ts` holds the additive movement authority separately so the core `types.ts` catalog remains under the 500-LOC gate.
- **Review:** five adversarial passes closed four rounds of substantive findings; the final pass reported no code blocker. The complete synthesis is `docs/threads/done/narrow-choke-queueing/2026-07-19/1/REVIEW.md`.
- **Result:** 2,165 unit tests passed and 2 skipped across 287 passed and 1 skipped files; typecheck, lint, and the 565-module production build passed. `npm audit --omit=dev` and `npm audit` both reported zero vulnerabilities.
- **Boundary:** open-terrain subcell packing, autonomous scout/herdable movement, formation regrouping, persistent opposing-flow fairness, and idle-blocker timeouts are unchanged.

# 2026-07-19 — convincing tree forms (v0.3.3)

- **Request:** trees should have variety in color and form as part of a broader visual-conviction pass.
- **RED:** the prior recipe gave every tree the same four-block broadleaf topology and nearly the same greens. New cluster classification tests initially reported only broadleaf forms and one canopy palette: 18 passed, 2 failed.
- **Action:** the position-seeded recipe now chooses broadleaf, tall tiered conifer, or asymmetrical wind-swept geometry and one of three natural canopy palettes, then layers bounded within-family jitter. All forms retain the exact trunk/crown part-key set used by recipe silhouettes.
- **Visual evidence:** the fixed paused default-map capture changed 16,854 of 480,000 pixels (3.5113%), confined to the forest cluster and the translucent HUD pixels directly above it; the after frame visibly mixes tiered evergreens, low broad crowns, offset crowns, and dark/olive/blue-green foliage.
- **Focused result:** 74 rendering tests passed; the final variation plus file-size boundary was 23/23. Full gates passed 2,168 unit tests with 2 skipped across 287 passed and 1 skipped files, typecheck, zero-warning lint, and the 565-module production build.
- **Review:** independent adversarial review passed the recipe, hit-proxy, and file-size probes 60/60. It found one substantive documentation overclaim: independent position hashes cannot guarantee every small forest contains all three forms. The spec now states map-wide diversity honestly, and changelog wording now distinguishes synchronized recipe-driven picking from byte-identical hit polygons. Synthesis: `docs/threads/done/visual-conviction/2026-07-19/1/REVIEW.md`.

# 2026-07-19 — terrain materials and moving reflective water cues (v0.3.4)

- **Request:** improve terrain texture and give water convincing waves and reflection while keeping the voxel renderer's reproducible presented-state boundary.
- **Pinned boundary:** exact Voxel `faa00bf2ef83e5134bd9ca62c0b36cd0cfbe394b` supports standard-material roughness/metalness and bounded harmonic instance transforms, but no bitmap/normal/environment maps or planar reflection. Mixed terrain chunks therefore stay opaque; AoE layers low-profile details without changing the dependency pin.
- **RED:** the new focused terrain-detail contract began with 4 expected failures and 1 passing flat-terrain/picking invariant because the old recipe had only sparse matte glints, rocks, tufts, and logs. The implementation adds deterministic grass flecks, forest litter, hill strata, water ripple/reflection grammar, a dedicated low-roughness water surface, and spatially phased crests.
- **Review-driven hardening:** full-cycle bounds sample 65 poses per animated part, an 8,193-crest test proves the 8,192 active-animation budget sends the excess crest to the static water lane without loss, explored fog emits no terrain-detail keys, and visible land adjacency adds cell-contained shoreline foam. Browser resource expectations now account for seven materials and nine batches.
- **Visual evidence:** a clean parent/current paused comparison changed 20,591/480,000 pixels (4.2898%), confined to the world band. A separate live-water canvas comparison changed 1,631/384,000 pixels (0.4247%), confined to water streaks while simulation time advanced and then froze identically.
- **Result:** the focused source suite passed 65/65 after shoreline-budget alignment. Full gates passed 2,175 unit tests with 2 skipped across 288 passed and 1 skipped files, typecheck, zero-warning lint, and the 565-module production build. Independent adversarial review approved the reachable pin, fog/picking boundaries, complete-period bounds, overflow behavior, browser timing proof, and visual evidence in `docs/threads/done/visual-conviction/2026-07-19/2/REVIEW.md`.

# 2026-07-19 — concrete building detail (v0.3.5)

- **Request:** make buildings convincing through added detail while preserving the existing renderer, picking, fog, persistence, and construction boundaries.
- **RED and action:** an exhaustive `satisfies Record<BuildingType, ...>` signature contract first exposed that shared role recipes could not distinguish all 18 concrete types. Completed recipes now append two to four type-specific architectural or working props from `aoeVoxelBuildingDetails.ts`; construction recipes return before that layer.
- **Geometry boundary:** exact transformed corners for every part remain inside each authoritative footprint, total recipes stay at or below 48 parts, no new material or batch lane is introduced, and the adapter's prepared hit state contains the same detail parts and instance matrices presented to Voxel.
- **Review-driven evidence fix:** the first showcase still ended in conquest and let the victory/minimap overlays obscure its lower row. A RED liveness regression now steps through the conquest evaluation interval, and an inert fog-hidden second owner keeps the capture at tick 0 with outcome `running` while owner 1 still has exactly one of all 18 types.
- **Visual evidence:** the matched no-detail/detail pair uses that final nonterminal fixture and a 1,200×720 raw world capture that frames all 18 types. It changed 4,148/864,000 pixels (0.4801%), confined to building geometry; instances rose 903→959 and triangles 19,028→19,700 while seven materials, nine batches, and 14 draws stayed fixed.
- **Result:** full gates passed 2,180 tests with 2 skipped across 289 passed and 1 skipped files, typecheck, zero-warning lint, and the 566-module production build. Independent adversarial review and a nested contract audit approved the final source and matched evidence with no unresolved finding; synthesis: `docs/threads/done/visual-conviction/2026-07-19/3/REVIEW.md`.

# 2026-07-19 — unit recipe and attack-rig extraction (no bump)

- **Reason:** concrete unit weapons cannot safely be layered onto role-wide suffix regexes: locomotion, ambient suppression, attack pivots, and reach correction must agree on the same compound parts before spear, crossbow, javelin, mounted, and siege variants are authored.
- **Action:** extracted the scaled unit recipe context, pure rigid attack geometry, and an exhaustive per-`UnitType` attack descriptor table. The initial descriptors deliberately reproduce the seven prior role rigs, preserving every part key, order, dimension, surface, tint, transform, and animation payload.
- **Characterization:** all 34 types are hashed together across idle, locomotion, builder work, attack coil, impact, snap, recovery, and fog-memory states. All eight SHA-256 digests were captured on unchanged `d5e652a` and remain exact after the extraction.
- **Result:** full gates passed 2,181 tests with 2 skipped across 290 passed and 1 skipped files, typecheck, zero-warning lint, and the 569-module production build. Independent adversarial review is recorded in `docs/threads/done/visual-conviction/2026-07-19/4/REVIEW.md`.

# 2026-07-19 — concrete unit identity and matching attack equipment (v0.3.6)

- **Request:** make units detailed and convincing while preserving smooth travel-facing motion, independent simultaneous attacks, planted roots, deterministic presentation, and presented silhouette picking.
- **RED:** the initial all-34 contract failed 14 of 23 expectations because role recipes reused generic equipment. Review then produced four additional failures: malformed active-attack projections threw on a missing rig; armor and rider overlays lagged a leaning torso; the trebuchet sling left its stone suspended; and forced spear reach moved the left arm 0.4032 world units while its shield moved only 0.1728.
- **Action:** exhaustive profiles select armor, headgear, shield, mount, weapon, tier, and signature for every `UnitType`; focused humanoid, mounted, siege, and monk builders keep recipes at or below 32 parts. Seventeen concrete pose styles drive the visible tool, sword, greatsword, polearm, bow, crossbow, javelin, mounted weapon, or siege mechanism. Shared body groups keep armor, heads, rider equipment, ambient suppression, and bounded melee reach coherent.
- **Boundaries:** prepared unit parts still supply both rendered instance matrices and AoE's presented silhouette proxy. Malformed legacy projections remain shadow-only; memory parts have no animation; feet, mounts, wheels, roots, and contact shadows stay planted. No simulation, combat timing, damage, pathing, command, save, replay, or sibling-Voxel schema changed.
- **Visual evidence:** the nonterminal showcase contains every one of the 34 P1 unit types exactly once. Its fixed paused 1,200×720 comparison changed 12,962/864,000 pixels (1.5002%), confined to x=228–968/y=184–536 unit geometry; instances rose 916→1,017 and triangles 19,388→20,600 while seven materials, nine batches, and 16 draws stayed fixed.
- **Review:** independent adversarial review closed all four substantive findings and reported no remaining actionable issue. Root-focused proof passed 81 tests across seven files; the independent pass completed 95 focused tests plus scoped lint, file-size inspection, and diff checks. Synthesis: `docs/threads/done/visual-conviction/2026-07-19/5/REVIEW.md`.
- **Result:** the required code gates passed 2,208 tests with 2 skipped across 292 passed and 1 skipped files, typecheck, zero-warning lint, and the 575-module production build. Final audits and the complete headless `npm run verify` are recorded after delivery validation.
