# 2026-05-02 — devlog (active file)

## 2026-05-02 — Phase 2D slot migrations 8–20 (no version bump per slot; consolidated catch-up)

**Action.** Continued the Phase 2D bridge-state migration that began on 2026-05-01 with slots 1–7 (`villagerOrdinals`, `gathererDropOffStuckSinceTick`, `monkHealCounters`, `playerAges`, `playerCivilizations`, `wonderCountdownOverrides`, `relicCountdownOverrides`). Today's session migrated slots 8–20 in sequence:

8. `marketExchangeRates` — flatMap. Validator deps now thread `accessor` instead of the raw rates record.
9. `trackedVisibilitySources` — used by `visibilitySystem` + bootstrapFlush; closure-scoped fingerprints cache shared with bootstrap.
10. `playerScoreCounters` — read in `matchEndOps.computePlayerScore`; ensure-helper writes via `accessor.mutate`.
11. `rallyPoints` — `building.setRallyPoint` handler swap to `accessor`; tests rewritten to construct real `BridgeStateAccessor`.
12. `wonderCountdowns` — entityCreateOps writes via mutate; system uses hot-loop pattern with dirty bit.
13. `relicCountdowns` — same hot-loop pattern; size-check on clear path so no-op clear doesn't markDirty.
14. `townCenterRefs` — aiSystem deps swap from raw map to accessor; entityCreateOps wraps TC creation in mutate.
15. `relicsInMonastery` — `scenarioSeedOps` writes via mutate on initial relics.
16. `sheepMoveOrders` — herdableMovementSystem hot-loop; unitCommandOps writes via mutate.
17. `conversionState` — applyMonkConvert reads + markDirty after in-place HP/progress mutation.
18. `monkCarriedRelic` — applyMonkPickup/Deposit + monkBehaviorSystem follow-loop. Reviewed in `impl-27/REVIEW.md` — APPROVED by Codex/Claude/Gemini.
19. `trebuchetPackStates` — `trebuchetState.ts` factory closes over accessor; pack/unpack validators take accessor; tests rewritten via `freshAccessorWithPackStates` helper. Reviewed in `impl-28/REVIEW.md`.
20. `lastSeenStatic` — mapOfMap codec; `fogMemoryOps.getOrCreateMemoryMap` now wraps get-or-create in `accessor.mutate(lastSeenStaticCodec, ...)` so the dirty bit fires per tick fog memory updates.

**Pattern.** Each slot follows the established Phase 2D template:
- Drop the field from `BridgeState` struct + `createBridgeState` initializer + `registerAllSystems` destructure.
- Reads route through `accessor.get(codec)`.
- One-shot writes use `accessor.mutate(codec, m => ...)`.
- Hot-loop writes use the `accessor.get + dirty bit + markDirty` pattern (one read, one markDirty per tick when any mutation happens).
- Save sites read via `accessor.get(codec).entries()`.
- Hydrate wraps initial set + orphan-prune (key-side and value-side) in `accessor.mutate` so the dirty bit fires for fresh-load.
- Validators (handlers) that consumed the raw map now take `accessor: BridgeStateAccessor` in deps; their tests construct a real accessor over a fake World seeded via `accessor.mutate`.

**Code-reviewer feedback.**

Slot 18 (`monkCarriedRelic`, impl-27):
- **Codex** APPROVE on code; flagged stale `summary.md` (still said 7/35) and pre-existing 500-LOC violation on `unitCommandOps.ts` (591 LOC).
- **Claude** APPROVE — verified all 7 checklist items; noted pre-existing redundant delete in `applyMonkDeposit` (left as-is, defensive).
- **Gemini** APPROVE — verified all migration patterns; noted missing devlog/changelog entry.

Slot 19 (`trebuchetPackStates`, impl-28):
- **Codex** APPROVE on code; flagged MEDIUM that `tests/replay/snapshotEquivalence.test.ts` was missing the new codec + 12 prior codecs, so a silent dirty-bit regression in `trebuchetState.ts` would slip through. Also flagged MEDIUM missing detailed devlog entry. Pre-existing LOW: `wireBridgeOps.ts` is 561 LOC.
- **Claude** APPROVE on code — explicitly verified the markDirty calls at trebuchetState.ts:51/61/70 for the three mutating ops and confirmed the two read-only ops do NOT call markDirty. Same MEDIUM doc finding (missing detailed entry) and MINOR housekeeping (devlog file END_DATE drift).
- **Gemini** APPROVE — verified all 8 conventions including the doc-discipline catch-up to 19/35.

**Disposition.**
- All slot 18, 19, 20 commits land as-is on `main`.
- Codex's MEDIUM finding on `snapshotEquivalence.test.ts` addressed in the slot 21 commit: extended the codec list from 7 to 20 (a single `MIGRATED_CODECS` const drives both the round-trip and the Map-instance test, so future slots only need to add one entry).
- Doc-discipline catch-up: this entry covers slots 8–20 in one consolidated record (rather than per-slot fragments) since the migration pattern is identical and the per-slot review summaries already live under `docs/threads/current/replay-scrubber/2026-05-02/impl-{27,28}/REVIEW.md`. Future slots will get individual entries when they introduce a new pattern wrinkle.
- File-size violators (`unitCommandOps.ts`, `wireBridgeOps.ts`, `aiSystem.ts`, `IndexedDBMirror.ts`, `GameScene.ts`) tracked on the file-split queue; addressed after slot migrations complete.

**Result.** 20/35 slots migrated. Build/lint/typecheck/targeted tests all green per commit. Multi-CLI review converged to APPROVE on every reviewed slot.

**Reasoning.** The /yolo session directive ("do not stop until you run out of things to do") drives a continuous pace. Each slot ships as a self-contained commit with its own gates so any single migration can be reverted without knock-on. Reviews run in background with the `until [ -s file.txt ]` poller pattern from AGENTS.md so review latency doesn't bottleneck forward progress.

**Notes.**
- 15 slots remain in Phase 2D (`garrisonedByBuilding`, `garrisonedUnitToBuilding`, `garrisonedUnitVisionSources`, `productionQueues`, `constructionStates`, `combatStates`, `buildingHealthStates`, `buildingCombatStates`, `wildlifeStates`, `aiStates`, `playerResources`, `population`, `researchedTechnologies`, `inFlightTechByOwner`); `monkTasks` is BLOCKED per KAD-0007.
- After Phase 2D completion, the remaining file-size splits land, then Phase 2F (schema-2 + version bump) → Phase 2G (round-trip equivalence) → Phase 3A–E (replay scrubber UI).
