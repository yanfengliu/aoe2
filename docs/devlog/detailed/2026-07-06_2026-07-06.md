# Devlog - 2026-07-06

## Internal - civ-engine v1.3.0 visual-playtest contract absorb

**Timestamp:** 2026-07-06 18:52 PT

**Action.** Added an AoE2-local visual playtest adapter that imports civ-engine v1.3.0 visual-playtest contracts, prepends the shared player-surface prompt context to the existing tactical LLM prompt, and embeds shared visual finding payloads inside the existing agent marker data. Preserved the current AoE2 LLM runner, command-tool schemas, screenshot attachment path, provider wiring, cost tracking, dispatch feedback, deterministic marker ids, severity mapping, anchor tick handling, and `isAoeMarkerData` conformance. Refreshed `package-lock.json` so the local `file:../civ-engine` dependency records civ-engine `1.3.0`.

**Code reviewer comments.** Codex CLI and Claude CLI review attempts were blocked by tenant policy because the required diff review would upload private workspace code to external model services; the concise artifact in `docs/threads/done/civ-engine-v1-3-visual-playtest-contracts/2026-07-06/1/REVIEW.md` records the blocked external review and the local adversarial review. Local review finding Local-1 flagged misleading screenshot metadata for present screenshots; fixed by reporting the attached screenshot block rather than a fake unavailable path. Local review finding Local-2 flagged that the architecture drift row was inserted ahead of older chronological rows; fixed by appending the 2026-07-06 row at the end.

**Result.** Red tests were observed before implementation for the missing shared visual-playtest prompt context and missing `data.visualPlaytest` marker payload. Targeted tests passed after implementation: `npx.cmd vitest run tests/playtest/llmPromptBuilder.test.ts tests/playtest/findingsToMarkers.test.ts` passed 44 tests across 2 files. Full gates passed: `npm.cmd test` passed 216 files with 1784 tests passing and 1 file / 2 tests skipped; `npm.cmd run typecheck` passed; `npm.cmd run lint` passed; `npm.cmd run build` passed. Dependency audit after the lock refresh found runtime dependencies clean with `npm.cmd audit --audit-level=high --omit=dev` reporting 0 vulnerabilities; the full tree audit still reports pre-existing dev-tool advisories in Vite / esbuild / undici and a moderate `js-yaml` advisory, with no clean lock-only `npm audit fix --package-lock-only --dry-run` remediation shown.

**Reasoning.** The civ-engine contract is adopted as vocabulary at the edge of the AoE2 harness instead of replacing the mature AoE2 command-tool runner. That keeps the current playtest capabilities intact while giving downstream tools a shared prompt/header shape and a portable visual finding payload nested inside existing AoE2 marker data.

**Notes.** The architecture docs now treat `src/game/playtest/visualPlaytestAdapter.ts` as the boundary between shared civ-engine visual-playtest contracts and AoE2-specific tactical state / marker UI. No package version bump or changelog entry was made because this is an internal harness compatibility change rather than a user-visible gameplay or public API change.
