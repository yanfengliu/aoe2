# Devlog Summary
- 2026-09-06 A BUILDING'S HEALTH BAR READ FULL WHILE IT WAS STILL A FOUNDATION (v0.3.220). Found by the standing loop playing: a Mill at `currentHp: 1000, maxHp: 1000` — exactly the Mill's `structures.csv` hit points — still drawn as a foundation, still refusing to work, still counting 0 toward Feudal. Health and progress were two INDEPENDENT accumulators in the same block of `builderWorkStep`: progress on the crew curve (`builderProgressShare` = 1 for the first builder credited that tick, 1/3 for each of the rest, telescoping to `(n+2)/3`), health on a flat `(maxHp − startHp)/totalBuildTicks` added once PER BUILDER, so `n` per tick. Ratio `3n/(n+2)` — 1.8x at three builders, 2.7x at eighteen — so health saturated at `(n+2)/3n` of the build: 56% at three, 37% at eighteen, 100% only for the single builder that never showed it, and `Math.min` then clamped the bar into silence. IT STALLED NOTHING — completion is decided by progress alone — what it destroyed was the player's only reading, making a site under construction, a site everyone had walked away from, and a finished building three states with one appearance, while `countCompletedAgePrerequisites` counts `isComplete` and told the player they owned none of the buildings they were looking at. `constructionHealth.ts` makes health a READING of progress, derived twice on purpose: the earned value is the CEILING (a pure function of progress, so the two cannot disagree) and the tick's `progressDelta` in hit points is the STEP, which exists only for the chipped foundation the ceiling alone would heal free the instant a villager touched it; undamaged, the two coincide exactly. Crew 1 is the gate's CONTROL — lockstep for a single builder — and the test recomputes the 10% floor from its own literal rather than importing it. Integration corrected two statements in the lane's own text: "57%-built" is 56%, and a test comment read "an hance and a half".
- 2026-09-06 SELECT THE WHOLE ARMY, ORDER IT ACROSS THE MAP, AND NOTHING HAPPENS AT ALL (v0.3.219). Found by the standing loop playing a 93-minute Black Forest match: 58 idle units, `issueAttackMoveCommand` returning `true`, and the army's centre unchanged for 9,200 ticks with no message and no error. TWO findings, and the second nearly hid the first. An independent 4-connected flood — never over the pathfinder under test — established that a normal match SEALS ITS OWN MAP: the human's component falls 368 → 205 cells by tick 20,000 on `black-forest` and enemy units reachable from it 4/4 → 0/26; that is filed as its own OPEN register entry, because an OPEN entry never rolls over. Behind it was a real search bug: the unreachable-target fallback scanned every cell within `max(mapWidth, mapHeight)` = 60 MANHATTAN cells of the target running a full A* per candidate, while the greatest distance on a 60x36 map is 94, so a unit sealed far from the order had NO reachable candidate and `playerCommandsSystem` cleared the order in silence. `moveDestinationSearch.ts` floods out of the UNIT's own cell instead — no radius to run out of — and picks the reachable cell with the smallest `(manhattan-to-target, y, x)`, DELIBERATELY the ring scan's own key, so every order the old scan could already carry out ends in the identical cell; verified by running both arms in ONE process against ONE bridge and comparing destination and path length per call, which also measured 0.89 → 0.02 ms reachable and 162.91 → 1.09 ms sealed. The second half is `describeWalkOrderReach`: an order accepted and dropped in silence is the placement-validator class one layer out, so the player now gets a sentence naming the cell, the barrier and the remedy, once per gesture rather than once per unit — and the barrier is named from the STUCK unit's frontier, because seeded at the target it picked out the player's own town centre on the far side and called that the wall. Two TARGETED mutations, one per half, each reddening exactly its own cases.
- 2026-09-06 A TEST THAT GOES RED THE DAY THE ENGINE GIVES US WHAT WE ARE WAITING FOR. Six civ-engine helpers this repo would use (`applyTickDiff`, `foldTickDiffs`, `emptyTickDiff`, `assertBundleShape`, `validateBundleMarkers`, `cloneTickDiff`) are internal — undefined at runtime on both barrels, absent from both type entry points, unreachable by any path the package permits — so `tests/civEngineInternalHelpers.test.ts` asserts their ABSENCE and goes red the day the engine promotes one; every failure message names the aoe2 workaround that promotion would let us delete, and says that deleting the test throws away the notice for the other five. THE TRAP IT CLOSES: one worker grepped `node_modules` and reported all six present, another imported them and reported all six absent. The internal `.d.ts` files really do ship (`dist` first in `files`, no `.npmignore`) and are text-searchable while unreachable, because `exports` publishes only `.`, `./browser` and `./package.json` — so the test IMPORTS. Its first mutation vector was DISCARDED as a bad instrument: mocking the module made six cases fail rather than one, because the framework throws on names the mock omits, and a mutation racing what it means to disprove has a red indistinguishable from a real one. The second control is the finding worth keeping — aliased against a barrel exporting nothing, ALL SIX absence checks pass VACUOUSLY while only the instrument check and the four public controls go red (5 failed, 6 passed, exit 1), which is "did not run reported as passed" demonstrated rather than argued. No `lessons.md` entry: the gate ships with its lesson. Engine feedback filed on `api-reference.md:6605`, which lists an internal function in a public error-code table beside two reachable call sites — the likeliest origin of the belief that it was callable, and it cost two workers and an investigation to settle.
- 2026-09-06 A RAIDED PLAYER IS WARNED TWICE AS OFTEN, AND A COMMENT CANNOT LIE ABOUT THE TICK RATE (v0.3.218). `ATTACK_WARNING_THROTTLE_TICKS` 400 -> 200: its comment claimed twenty seconds at twenty ticks per second, this build runs at ten, so the real spacing was FORTY seconds and had been since the horn shipped. THE RESEARCH RESULT IS NEGATIVE and had to survive into the docs intact — DE's own spacing could not be established from any authoritative source (one unbadged post on the official forum, HestiaAoE 2023-10-14, citing a video the search could not read; nothing in the wiki or the community databases), and that post describes a rule of a DIFFERENT SHAPE, suppression only when two attacks fall within ten TILES *and* ten seconds, so two raids far apart alarm twice there and once here and THERE IS NO SINGLE NUMBER TO COPY. The value is what the comment always intended, not a measured figure, and the source now says so. 200 was chosen as the largest correction that leaves the existing storm test still measuring what it claims, rather than rewriting an expectation to fit the change; the unimplemented spatial half is an open follow-up, a feature gap rather than a defect. Gate `tests/architecture/tickRateComments.test.ts` reads STRING LITERALS as well as comments, because this defect's twin sat in a test TITLE asserting the wrong tick count under the wrong rate — which is how it found a third instance, a file claiming thirty ticks per second. Twelve stale `v0.3.215` comments across nine files were corrected in the same pass. The register entry filed OPEN earlier the same day is CLOSED.
- 2026-09-06 THE GAME TELLS YOU WHEN YOUR VILLAGERS ARE BEING KILLED (v0.3.217). An enemy militia walked into the base at tick 3084, hit villager #2205 twice (25->21->17 HP), and the screen said nothing. Two defects, and the second had never been hit: the horn already fired — its oscillators counted in the live app — so what was missing was any VISUAL at all; and the horn's own rule compared each swing's target cell against every own building's RENDER ORIGIN within one cell, while a swing targets the VISUAL CENTRE, origin plus half the footprint less one, so every attack on a 4x4 building (Town Center, Market, Castle, Wonder — the brief named three of the four) was silently dropped by the check meant to catch it, and villagers matched only because their 1x1 footprint puts the offset at zero. The root cause ties both: the attack feed is an ANIMATION feed that records a swing at a cell and discards the target id it was handed, so the one consumer needing to know who was hit inferred it from coordinates with the wrong convention. The rule is now exact — another player's unit hitting a villager or building owned by the human — with wildlife and soldier targets argued out rather than assumed, and suppressing the warning when the camera is already there explicitly REJECTED, because the reproduced defect happened inside the player's own base with the camera on it. The mark is raised outside the mute switch and drawn ON the minimap, because nothing may float over it. A second, older defect was found at integration and filed OPEN rather than fixed: the throttle constant is 400 ticks and its comment says "~20s at 20 TPS", but this build is 10 TPS, so a raided player is warned every FORTY seconds and has been since the horn shipped.
- 2026-09-06 A UNIT BEHIND A BUILDING READS AS BEHIND IT, IN ITS OWNER'S COLOUR (v0.3.216). The first thing a player saw on the boot map was a solid, unlit, bright blue humanoid pasted on the Town Centre's wall, units behind houses appeared to stand on the roof, and an enemy rendered vivid magenta, which is nobody's colour. The machinery was correct — `getOccludedUnitStates()` confirmed both were the intended x-ray cue — so no test could see it. The instructive half is WHY the colours were wrong: they had been chosen by maximising RGB distance from the building palette, a reasonable-sounding objective and the wrong one, because maximising distance from the walls also maximises distance from the player colours and the player colour is the entire message; the magenta measured 137 from its nearest building material, better than anything else scored, and still made a red player's unit read as pink. Routing through `ownerTint` was tried and rejected (it preserves luminance, so yellow lands within 70 of the roof tile and grey within 33 of dark stone). The cue moves to the existing translucent `memory` lane — no new instance batch, which the pinned ceiling and a browser spec both forbid — and is drawn TWICE, because at one pass a pixel is half wall: the friendly blue's head over a sunlit roof sampled as a grey-mauve (171,126,135) while the same silhouette's torso over a dark chimney stayed blue, one shape carrying two colours, and the gate reproduces that exact pixel. Six of the eight player slots are still proved by arithmetic only: a skirmish opponent is in fog at boot and no capture reaches one.
- 2026-09-06 THE GAME COULD NOT BE STARTED FROM A WINDOW SHORTER THAN ITS SETUP PANEL (v0.3.215). Below about 600px of viewport height the match setup screen clipped its heading off the top and its Start button off the bottom and nothing scrolled — at 1280x400 the heading sat at y=-70 and Start ran 71px past the fold — and since a bare URL has no other route into a match, a 1366x768 laptop with browser chrome could not start the game at all. It survived twenty versions because 800x600, the browser suite's own default viewport, cleared the 599px panel by ONE pixel. The obvious fix would have shipped half the defect: a flex item centred by `align-items: center` and taller than its line overflows BOTH ends, while scrollable overflow only ever extends past the end edge, so `overflow-y: auto` alone leaves the heading permanently unreachable; automatic margins centre while there is room and resolve to zero when there is not. A first attempt added 24px of vertical padding and pushed 800x600 from fitting to scrolling — the pixel diff caught it at 21.62% on a viewport the change was never meant to touch, and the shipped version diffs 0.00% there. `tests/browser/setup-screen-fits-any-viewport.spec.ts` gates four heights and deliberately never uses Playwright's `locator.click()`, which scrolls an element into view first and reached the button on the BROKEN tree at 1280x500.
- 2026-09-06 THE COORDINATOR'S MODE AND THE IMPLEMENTER CONTRACT, WRITTEN DOWN ONCE. The owner asked for a named operating mode for this repo's coordinator and its implementer subagents, so `docs/policies/local-rules.md` now carries it as a standing contract an assignment inherits by writing "the implementer contract applies" — six clauses on what the coordinator does (never implements; sources work by PLAYING; three or four lanes with disjoint files and ONE gate run per batch; commits as work is accepted; verifies a fact before passing it on; decides rather than blocks) and nine on what every implementer owes (reproduce in the real game before writing a fixture; fix the class; name the instrument before writing a probe; prove the gate red by mutation and check the mutation is not racing what it disproves; state the bound; measure rather than argue and verify the instrument first; never weaken an assertion to get green; never end the turn while a gate runs; report what you did NOT check; correct the coordinator when the brief is wrong). About thirty subagents ran here today and the same eight or nine requirements were retyped into every assignment, which is how a clause goes missing. Every clause carries the incident that paid for it. The entry is also its own last clause working: the worker that wrote it checked each incident the coordinator supplied against the register, the devlog and the gate proofs, CORRECTED TWO and DROPPED FOUR as unsourced rather than write an anecdote it could not stand behind.
- 2026-09-06 THE DEBUGGING RULE NAMED THREE TOOLS AND NONE OF THEM DID WHAT IT SAID. Four read-only audits found every instrument in AGENTS.md's Debugging bullet wrong in a different way: `replay:inspect` is this repo's own `scripts/replay-inspect.mjs` and not an engine capability at all, `createOccupancyDebugProbe` takes an `OccupancyGrid` and this repo holds an `OccupancyBinding` whose grid is `private` with no accessor (so the migration made on the engine's own recommendation is what cost the reach), and `createPathQueueDebugProbe` wants path-queue stats this game cannot produce because it has no queue — movement is synchronous `findGridPath` plus a cache. Worse, the rule AND the enforcing hook both advertised those two as saying "who is blocking whom", and the occupancy payload is eight scalars, five of them counts: that is the best explanation anyone has for a rule skipped twice and asked about twice. The rule's FORCE is unchanged; its list is now true and names what was there and unmentioned — `bundleHotspots` for triage, `snapshotAtTick` for state with no World construction, `selfCheck` for determinism, `WorldHistoryRecorder` + `summarizeWorldHistoryRange` for a live world with no bundle, `createVisibilityDebugProbe` (type-compatible, one export away), `bridge.getDebugSnapshot()` for who is actually blocking whom, and civ-engine's fourteen-tool MCP server. `docs/engine-feedback/current.md` gained seven evidenced asks and one adoption note (`runMetrics` takes `Iterable<SessionBundle>` and is adoptable today; only the directory-shaped `BundleCorpus` and the MCP server are blocked by aoe2's single-file bundles). Two register entries corrected, both this coordinator's to have caused: the "Conquest Victory" entry was headed `owner-reported` when a read-only play session run by this coordinator played that match, and the "browser suite had never run on CI" entry had met its own closure condition — CI run 34059936180 on `06a83e98`, all three jobs green, `ubuntu-latest — browser suite: success` — so it is CLOSED, which took the active register to 13 closed and rolled the v0.3.208 villager/foundation entry to the archive on the gate's own naming.
- 2026-09-06 FIVE STATEMENTS IN THE REPO WERE FALSE, AND ONE LESSON WAS UNWRITTEN. A gate header claimed three checked shapes where `FORBIDDEN` holds four; two files called 4 MiB "three orders of magnitude" under V8's 536,870,888-char string cap when it is 128 times under it; the self-play coverage header's three-way configuration comparison read as a description of this tree when it is a 2026-09-05 reading, and it is now marked as history with the current row stated beside it rather than silently refreshed — refreshing one row would have assembled a three-way A/B out of two different trees; a comment credited `cf774cd4` for a four-month-old Linux-fatal `npm.cmd` line that `002b7280` actually wrote; and a register citation was repointed at the entry that owns it. Every changed line in the five code files is a comment or blank, proved by filtering the `-U0` diff rather than by reading it. The new local rule is the one these three commits are the first prospective use of: a batch split from ONE gated tree gets `npx vitest run tests/architecture` against every intermediate state and a byte-exact reconstruction proof at the end, because `npm run verify` covers the final state and nothing before it.
- 2026-09-06 CANON SYNC: PICK UP FLEET.MD (SHA 36FEF4F3BD4C -> BAFDD66F8299). A peer session's regenerated `FLEET-CANON` block, committed alone and reconstructed from HEAD rather than staged whole, because the same file carried this session's own Debugging-bullet rewrite below `FLEET-CANON:END`. The canon gained one sentence: a harness that sets state directly — assigning the camera pose, calling the render function, writing the store — is not exercising the controls, and is structurally blind to every defect in the input path it skipped while reporting confidently on the path it kept. Qualified before committing by `qualify()` imported read-only from `../fleet/scripts/commit-canon-sync.mjs`, the fleet's own content check.
- 2026-09-06 TWO BROWSER SPECS MEASURED THE HOST INSTEAD OF THE GAME. The `browser` job's first run off the one Windows machine that had ever executed this suite (CI run 34041883943, `ubuntu-latest`) failed 2 of 197, and both failures were tests measuring their host. ONE: `play-opening.spec.ts` read the tick, waited for the HUD clock to leave `00:00`, and read the tick again — but the clock had ALREADY left `00:00`, so that wait resolved on its first poll in 14 ms and waited for nothing, leaving an assertion that one tick fits in 25 ms of the host's wall clock. On two cores with software rendering exactly zero fitted: 20 → 20. It now polls for the world to advance past the tick it read. ITS FALSIFIABILITY PROOF IS THE PART TO KEEP: the first mutation PASSED when it should have failed, because freezing the world at tick 11 rather than zero let the poll succeed for the wrong reason — a mutation that races what it is trying to disprove proves nothing, and its green is indistinguishable from a working gate's. TWO: `game-selection-click.spec.ts` asserted at a click point with **0.30 screen pixels** of room; of the 625 offsets its scan probed, four produced the required ordering, in a ribbon one sample wide that raster order reaches at its TIP, with an edge that moves with the idle animation. The cause was the FIXTURE: co-located bodies take one of four half-cell-apart slots (`UNIT_CELL_SLOT_OFFSETS[entityId % 4]`, and a sheep carries a unit transform so it takes one too) while the militia's body is 0.25 cells across and the sheep's 0.231, so two slots leave a 0.019-cell gap — coincident or never touching, and these never touched. A spawn reorder (militia 1st, sheep 5th, four apart) makes them share a slot; the scan now aims at the MIDDLE of the qualifying run rather than its first raster hit, grows an eight-point compass to MEASURE the margin, and fails the fixture BY NAME below two pixels. 0.30 px → 6.00 px at default zoom, held across five animation phases. No assertion changed; one added. TWO DURABLE FINDINGS: fixture spawns and live play use DIFFERENT lattices — half-cell for fixture spawns, quarter-cell through the occupancy allocator in play — so a fixture pile is spread twice as far as a real one and never touches, which is the real root and needs its own task; and there is NO player-facing defect — three units genuinely on one cell are ordered, separately clickable, and repeated clicks cycle whichever bodies cover the point, which is the spec's own contract and more than the real game does. The OPEN register entry about the browser job never having run is amended rather than closed: its first run was red on a real defect, which is exactly what it existed to make possible, and it stays open until a GREEN run is observed. Open risk recorded: two specs in `game-combat-and-meta-meta.spec.ts` went red from machine load on 2026-09-05 and passed alone, the heavier driving 2,000 single-tick steps against a 30-second budget with the file taking 41.7 s alone, so a slower runner may trip them — and the answer is not a raised timeout. Gated by ONE `npm run verify` over the combined tree of three commits.
- 2026-09-06 A SIMULATION STEP THAT REFUSES TO RUN NOW SAYS WHY. `bridge.step()` had FOUR refusal paths — a manual pause (what the Esc menu applies), `haltState.halted`, a resolved match, and the replay bridge, which never advances from a frame by design — and returned the same nothing for all four. Asking the test seam for 3,000 ticks against a game paused from the real menu gave zero ticks, no error, and `outcome === 'running'` with `engineHalted === null`: the two fields a caller consults, both saying nothing is wrong. THE EXPENSIVE PART WAS NOT THE MISSING TICKS. A play session read that silence and published a hazard in two forms — that `advanceTicks` throws the engine's re-entrancy `EngineError`, and that it "silently under-advances when the simulation is not paused" — which was then repeated in worker assignments as fact; a later session measured both at `d65f84aa` and found both WRONG. Two sessions reasoned from a false model of the engine because a function that declined to do its job said nothing about declining. AND A CALLER WAS ALREADY PAYING: `llmRunner.ts` maps `advanced <= 0` to `stopReason = 'engineHalt'` under a comment asserting that inference is safe ("Reached only when the match is still 'running' ... so no progress here IS a genuine halt"), which is wrong for a paused world and latent only because its host unpauses around each call. Now `step()` returns `{ ticks, refusedBecause }` (`bridge/stepReport.ts`) with `'paused' | 'halted' | 'match-over' | 'replay'`, as shared FROZEN constants so the refusal path allocates nothing — a halted match refuses sixty times a second forever — and `ticks` is reported separately because a `deltaMs` under one tick legitimately runs none. `advanceTicks` returns the tally summed from the steps' own reports instead of reading a tick counter. Throwing was REJECTED (the runner polls a finishing match, and a resolved match is a normal end there); fixing only the test seam was REJECTED, because that leaves the production silence, which was the defect. Gate `tests/app/stepRefusalIsReported.test.ts`: 13 cases over the real bridge and the real replay bridge, the four reasons asserted DISTINCT, a control in the other direction, red 12 of 13 at `bcdc88c3` (the 13th is the backwards-compatibility control and passes on both trees on purpose). Open: `llmRunner.ts` should read the new signal instead of guessing, and `browserTestApi.ts` now sits at EXACTLY 500 lines. Gated by ONE `npm run verify` over the combined tree of three commits.
- 2026-09-06 v0.3.214 A LOST MATCH NO LONGER ANNOUNCES ITSELF AS A VICTORY. Owner played to conquest, LOST, and read "Conquest Victory" — a `hud-footer` at (12, 611), 420x97, in 13px `rgba(226,234,243,0.56)` grey, with no overlay, no winner named and nothing to act on, so a match can end without the player noticing. `postGameSummary`'s `formatWinConditionLabel` switched on `winCondition` ALONE, so a win and a loss printed the identical string while `matchState.outcome` sat correct and unrendered one property away in the same object. Now `src/ui/hud/matchOutcomeText.ts` forms every end-of-match word and EVERY function in it takes the outcome, so the defect is not expressible rather than merely discouraged; `matchResultOverlay.ts` is a dimmed modal on the v0.3.211 halt-notice precedent (one panel, one control, focused on open — a stopped match and a finished one are the same kind of event) and the footer stays as the record behind it. Naming the winner is the interesting half and it is derived HONESTLY: `MatchState` records the outcome from the human's point of view and stores no winner at all, and score cannot stand in — in the conquest fixtures the WINNER HOLDS THE LOWER SCORE, 50 against 10 one way and 10 against 50 the other — so a victory is the player's own, a draw says nobody won, a score defeat names the top scorer because that is that condition's own published rule (`scoreTimerSystem`), a defeat against one opponent names them, and a multi-opponent conquest defeat names the winning SIDE rather than guessing. The missing `winner` field on `MatchState` is recorded as the one real gap. Gates: `tests/browser/match-result-announcement.spec.ts` plays FOUR real matches to resolution and asserts RENDERED TEXT, never `matchState.outcome` (the defect is that the field was right and unrendered), plus legibility — visible, a quarter of the viewport, dimming, headline ≥24px at ≥80% opacity — and dismissal; `tests/ui/matchResultAnnouncement.test.ts` covers every outcome x condition pair over four owner shapes. Red three ways, including the OVER-FIX guard: deleting `.hud-match-result[hidden] { display: none; }` turns the live-match assertion red, which is the author-`display`-beats-UA-`[hidden]` trap that cost `.hud-halt-notice` 99.98% of a frame the day before. Live-game capture diffs 0.00% at both viewports. `createHudController.ts` now sits at 498 of its 500-line cap. Gated by ONE `npm run verify` over the combined tree of this commit and the two after it.
- 2026-09-06 v0.3.213 THE AI FINISHES THE BUILDINGS IT PAYS FOR. Two compounding defects, both exposed when the v0.3.212 placement fix turned the coverage lab red — worth saying plainly, because the lab had been GREEN over both of them and only started reporting once a neighbouring change moved the match onto a different path. (1) `runBuildCrewPhase` built its site list from live build COMMANDS, and `playerCommandsSystem` drops a build command the first tick `findBuildingApproachPlan` returns null, so one blocked tick made a site invisible forever: nothing re-crewed it, while `findOwnedBuilding` still found the foundation, so `missing(type)` in the build order read FALSE for the rest of the match and the AI never ordered that building again. Owner 2's Siege Workshop froze at 125/400 on tick 13,750 and stood there for 31,250 ticks — no ram, no mangonel, neither siege technology; owner 1 held three farm plots at 0/150 and counted them as farms it owned. (2) `pickNextBuildTarget` had no affordability fall-through, so an AI that could not afford its first choice built NOTHING: owner 1 spent the whole Castle Age with wood pinned between 6 and 127 against a 200-wood Siege Workshop while sitting on **2,506 stone**, enough for three Castles, one of which alone qualifies it for Imperial (§7.2). NOT the wood-reserve fix the 2026-09-03 entry measured and rejected (that halved the army on the strongest seed) — it walks the same list and takes the first entry it can pay for, still naming the unaffordable first choice when it can afford none so a broke AI keeps saving. Misses fell from three units and five technologies to ONE and THREE. Gates `aiOrphanedFoundation.test.ts` and `aiBuildOrderAffordable.test.ts`, both contract tests over hand-built inputs — the end-to-end claim is the lab, which is ONE map at ONE configuration. THE FLOOR WAS THEN EDITED for the remaining four and it is a DEFERRAL, measured not argued: the boot map's gold is entirely mined out by tick 30,000 (6,263 in the ground at 2,500, zero at 30,000) and both seats' gold then FREEZES at 13 and 7 for 15,000 ticks, while every removed name is priced in gold — `mangonel` 135, `bracer`/`plate-mail-armor`/`plate-barding` 300/150/200 — and the decisive control is that `blast-furnace`, same Imperial tier at 225 gold, STAYS on the floor and passes because it was bought while gold still existed. FOR THE NEXT SESSION: the AI's market planner (`marketActionForAgeUpShortfall`, from `aiSystemProductionPhase.ts`) trades ONLY to cover an age-up shortfall, which is exactly why gold exhaustion starves technologies — a seat can end the match on 1,262 food, 441 stone and 7 gold beside its own finished Market; when a trade covers a technology or a unit and not only an age-up, put the four names back. ALSO: `src/game/simulation/bridge/registerAllSystems.ts` is at EXACTLY 500 lines again, so the next line added there goes red on `fileSizeBudget.test.ts`. AN ARM WAS BUILT, TESTED, MEASURED AND REVERTED: a third candidate carried its own unit test that went red without it — the whole conventional evidence of a fix — and against the lab produced a verdict byte-identical to the arm without it (same single `mangonel` miss, matching line for line except wall-clock timing, `arm2-coverage.txt` 06:23 against `arm3-coverage.txt` 06:45); the surviving run logs pin the measurement and the revert but NOT which code change it was, and it is recorded as a measurement rather than guessed at. Register entry 2026-09-06, marked OPEN — the lab is green partly by a recorded deferral, and an OPEN entry adds nothing to the closed cap.
- 2026-09-06 v0.3.212 A BUILDING YOU CANNOT REACH IS REFUSED INSTEAD OF EATING YOUR WOOD. Found by play: `isPlacementBlocked` asked only whether the FOOTPRINT was free, so a site in a forest pocket previewed GREEN, the click was accepted, the wood was spent, and `playerCommandsSystem` then got null from `findBuildingApproachPlan` and cleared the build command in the SAME tick — idle villager, foundation at 0/200 forever, no message, no refund. Foundations are solid obstacles, so a player who keeps placing seals their own base in: measured in one real match at **eight sites and 440 wood**, reachable area 1,353 cells down to **106**, with ten such cells on the starting map at tick 600 and four of twelve refusals the game issued naming ground nobody could reach. Fix asks the ACTUAL builders using the MOVER'S OWN passability — `builderReachability.ts` — and the preview, the confirm validator, the construction re-check and the AI's suggestion all read ONE verdict, because a second opinion that drifts from the approach search is worse than the defect; computed as a connected-component labelling rather than A* per hovered cell (exact for a 4-connected uniform-cost grid, so it is A*'s own answer by construction), cached per world and per `worldOccupancy.structuralRevision`, and the pending footprint deliberately NOT carved out first, which the module header proves exact rather than approximate. Cost measured at **0.77 microseconds per hovered cell**, under 0.2% of a live match. Gate `tests/simulation/placementReachability.test.ts`, red 4 of 6 — AND ITS MOST INSTRUCTIVE CASE IS THE ONE THAT STAYED GREEN: asking the game whether it accepts its own suggestion passed WITH the defect reintroduced, both sides being the same code agreeing with itself, so the sweep judges against `staticGridOf`, an independent rebuild of land passability, whose one known divergence (a finished gate is a wall to it, a door to its owner) is guarded by asserting the human owns no complete gate at that tick. Bounds named in the header: ONE map at ONE tick (60x36, 2,160 cells, tick 600), land builders only, footprints 1x1 and 2x2 with only the 1x1 sweep able to detect anything, and the A*/labelling equivalence exact only while `findGridPath`'s 10,000-iteration cap cannot be reached — on the §4 ladder's 116x72 maps it can, which is why the map size is asserted rather than assumed. Spec §8.1 gains the rule; register entry 2026-09-06, and "Six closed threads sat in `docs/threads/current`" rolled to the archive to make room, the oldest closed entry still being pinned unarchivable.
- 2026-09-06 v0.3.211 THE GAME SAYS WHEN IT HAS STOPPED, INSTEAD OF DRAWING A LIVE MATCH OVER A DEAD ONE. Owner-reported as a freeze: the clock and units stopped at tick 29,297 while the HUD kept drawing resources, age, population, minimap and a running match timer, with no banner and no exit but a page reload. ROOT CAUSE IS ONE STATEMENT — `AoeVoxelGameView.frame()` put `window.requestAnimationFrame(this.frame)` LAST with no try/catch (`d65f84aa:src/app/AoeVoxelGameView.ts:382-386`), so a throw anywhere above it (bridge step, camera, renderer) meant the loop was never rescheduled; injecting one throw into the real frame path reproduced every symptom exactly (tick frozen at 15 and still 15 after 1.5s, one uncaught `pageerror`, `engineHalted` **null**, a screenshot indistinguishable from a healthy match). THE REPORTED TRIGGER WAS REFUTED BY MEASUREMENT and the correction is the durable half: `advanceTicks(n)` does NOT throw `EngineError: ... re-entrantly from within a tick` and does NOT under-advance while unpaused — live at `d65f84aa`, running and unpaused 3000 requested → 3000 advanced, paused through the test API 3000 → 3000, and 100/300/1000 all exact — because it is a plain `for` loop of `bridge.step()` from OUTSIDE any tick and cannot reach the engine's guard; what really under-advances silently is `bridge.step()` returning early with NO signal on manual pause, `haltState.halted`, or `matchState.outcome !== 'running'` (drive the real Esc menu, then `advanceTicks(3000)` → **0 ticks, no throw**), which is OPEN in the simulation lane. SECOND SILENT STOP, equally invisible and now covered: `HudState.engineHalted` — set by the bridge when the engine reports a tick failure — was read by `runPlaytest.ts` and by tests and by NO UI CODE AT ALL, so a caught engine failure was as silent as an uncaught one. Fix wraps the frame body with the reschedule OUTSIDE it; first failure halts the simulation for good (a world left part-way through a tick is not safe to step) while camera and renderer keep running so the frozen match stays lookable, second failure stops the loop deliberately; `bootstrap/engineHaltSurface.ts` joins both sources — frame halt pushed, `engineHalted` polled at 500ms — into one modal (`ui/hud/engineHaltNotice.ts`) naming the cause, the tick/phase/code where the engine supplies them, and a Reload button. Gate `tests/app/frameLoopHalt.test.ts`, 10 cases, red 7 of 10 with the defect reintroduced (each with the injected error escaping through `frame` at `AoeVoxelGameView.ts:392`); bound named in its header — jsdom with renderer/camera/pointer/presentation mocked and a hand-driven frame queue, so nothing about real WebGL or real rAF, and the engine-halt case passes even with the frame defect back because it is the OTHER stop. THE PIXEL DIFF CAUGHT WHAT NO TEST WOULD: the notice's first CSS (`position: fixed; inset: 0; display: grid`) beat the UA sheet's `[hidden] { display: none }` on cascade origin and covered every frame of a healthy game — boot capture **99.98% changed, 479,905 of 480,000 px**; the compound `.hud-halt-notice[hidden] { display: none; }` re-assertion takes the re-diff to **0.00%**, and the same trap is already documented in `styles.css` for `.replay-load-dialog__panel` and `.hud-game-menu`. TOOLING TRAP for the next session: importing `/src/app/X.ts` from `page.evaluate` after editing that file yields a SECOND module instance, because Vite's dev server appends `?t=<timestamp>` on invalidation — resolve the real URL out of `performance.getEntriesByType('resource')` instead of typing the path. Spec §14.1 gains the rule (paused, finished and broken are three distinguishable states); the full-screen modal is an explicit integrator decision recorded in `docs/architecture/decisions.md`, not an oversight — the minimap rule governs a LIVE game. Register entry 2026-09-06; "Fourteen loaded carriers freeze head-on on Nomad" rolled to the archive to make room, the oldest closed entry being pinned unarchivable.
- 2026-09-06 THE BROWSER SUITE RUNS ON CI, ON A CONFIG THAT COULD NEVER HAVE RUN THERE. `npm run verify` has six stages and CI ran five: nothing in `.github/workflows/` installed a browser, so all 44 spec files under `tests/browser/` (43 execute — `_captureSelectionPanel.spec.ts` is excluded by `testIgnore`) existed only as something ONE machine had run, the same machine whose unrepresentative line endings let a defect ship the day before. AND IT COULD NEVER HAVE WORKED ANYWAY, which is the substance: `playwright.config.ts` has started its preview with `` `npm.cmd run preview` `` since **2026-04-11**, hardcoded when only Windows ran the suite (`002b7280` "Add browser gameplay test harness" introduced it as `npm.cmd run dev`; `cf774cd4` the same day changed `dev`→`preview` — the new in-file comment credits the hardcoding to the SECOND of those, which is the wrong hash for that claim), and there is no `npm.cmd` on Linux, so all 43 specs would have died together on the 120s `webServer` timeout with NOTHING in the output naming npm — a failure that reads as a broken suite rather than a broken command. Fixed with `process.platform === 'win32' ? 'npm.cmd' : 'npm'`; new `browser` job on `ubuntu-latest`, no `needs:` so it runs beside `gates`, 45-minute timeout, Playwright browser restored from `actions/cache` keyed on the installed Playwright version (full `install --with-deps chromium` on a miss, `install-deps chromium` on a hit because the apt libraries live outside the cached directory), `test-results/` uploaded on failure for 7 days. TWO DELIBERATE OMISSIONS, argued rather than overlooked: no Windows leg (30-40 extra billable minutes at double rate, and Windows is the one platform the suite is NOT unproven on), and `.nvmrc` deliberately kept OUT of both push filters because both workflows pin Node as a LITERAL (`'20'` in ci.yml, `'22'` in playtest.yml) and neither reads that file, so such a run would execute on the wrong version and report green — and for whoever wires `node-version-file:` later, `.nvmrc` today contains **24**, matching neither pin, so that wiring silently moves CI from Node 20 to Node 24. `.gitattributes` WAS added to both filters, because the commit that fixed the Windows line-ending defect (`5274d7e2`) could not trigger its own proof. `ciStatus.test.ts` gained two cases after finding the three `workflow--*.yml` fixtures were copies nobody compared — taken at `ca24709b`, then `52546080` edited two of the originals and the fixtures sat a day behind with nothing red; each fixture's PARSED PUSH TRIGGER must now equal the real workflow's, with controls asserted first (the real file parsed at all, has a readable `name:`) so a match cannot mean "neither parsed", plus a completeness case requiring a fixture for every `.yml` in the directory. Bound: parsed push trigger only — a fixture may differ in a comment, a step or a `permissions:` block and stay green. Register entry 2026-09-06 marked OPEN: the browser job has never run anywhere, so all of this is a claim about the first push rather than an observation, and an OPEN entry adds nothing to the closed cap. No version bump — nothing a player can see.
- 2026-09-06 THE PLAYTEST BUNDLE STREAMS ON EVERY PATH, NOT JUST THE CHEAP ONE. Yesterday's fix claimed it had wired every call site that touches a bundle and had NOT — the claim is now corrected in place in its own register entry rather than quietly dropped. Four more whole-bundle strings survived it. THREE were in `scripts/playtest-llm.mjs`, all on the path that spends model calls for tens of minutes BEFORE it throws: the bundle stringified whole INSIDE the browser page (`window.__AOE2_PLAYTEST_BUNDLE_TEXT__ = JSON.stringify(bundle)`), the 4 MiB chunks rejoined into one Node string (`JSON.parse(parts.join(''))`), and `writeFileSync(..., JSON.stringify(result.bundle))` — the same line, verbatim, that yesterday's fix had just removed from `playtest.mjs`. The chunking READ as a fix for this exact problem and was not one: it bounded the JSON-RPC transport, never the document, and the page-side stringify had already built the whole thing before the first chunk was requested. THE FOURTH, in `scripts/playtest-self-improve.mjs`, is the one that explains the miss: `const bundle = readJson(\`${prefix}.json\`)`, a generic three-line `JSON.parse(readFileSync(...))` helper used four times in that file for artifacts that are all small except this one — nothing at the call site put "bundle" next to a `JSON.parse`, so neither the review nor the first gate could see it, the gate being a source check looking for shapes it had been shown and a wrapper being exactly the shape it had not. That one is a READ ceiling: the bundle wrote fine and was then unreadable. The page now emits STRUCTURAL PIECES — one non-array top-level value, or a batch of individually-stringified array elements — reassembled element by element on the Node side (a spread would push a batch as one argument list and blow the call stack), and the largest string anywhere is now 4 MiB against the 536,870,888 cap. GATE now scans every `scripts/*.mjs` (34 files) for FOUR shapes instead of naming one file — `JSON.stringify(<...>bundle`, `JSON.parse(readFileSync(<...>bundle`, `const bundle = <anything but readBundleFile>(` (the wrapper shape, added for the fourth ceiling) and `JSON.parse(<...>.join(` — proven red against this repository's own pre-fix `playtest-llm.mjs`, naming all three of its ceilings BY LINE NUMBER, with a control case feeding the patterns five verbatim historical lines and five correct shapes so a pattern that stopped matching anything fails there instead of passing everywhere. The 500-line cap was resolved by SPLITTING the script by role — the Playwright host adapter moved to `scripts/playtest-llm-host.mjs`, leaving 425 lines against 531 — which EARNED THE DELETION of a legacy exemption; `LEGACY_VIOLATIONS` is down to ONE entry, `scripts/content-lib.mjs` at 633. TWO WRONG SENTENCES LEFT IN THE SOURCE AND RECORDED IN THE REGISTER RATHER THAN EDITED: the gate's own header says "the three shapes below" while `FORBIDDEN` holds four (the code is right, the sentence undercounts it by exactly the newest entry), and both `playtest-llm-host.mjs` and `src/game/playtest/bundleIo.ts` call 4 MiB "three orders of magnitude under" the cap when it is 128x, about 2.1 — the numbers in both files are correct and only the ratio is overstated. No version bump — nothing a player can see.
- 2026-09-06 THE ROLLOVER GATE NOW NAMES THE ENTRY THAT SHOULD ACTUALLY MOVE, and the two documentation lies beside it are gone. `defectRegisterRollover.test.ts`'s cap-overflow message sorted same-date entries by ASCENDING line number in a file written NEWEST-FIRST, so within a date it named the most recently written entry instead of the oldest — on the live register at cap 10 the old message named lines 215 and 73, the new one marks 215 PINNED and names 179 then 169 — and line 215 is `2026-09-03 — The AI's build order stalls…`, whose `(PROVEN, fix measured and NOT shipped)` marker counts against the cap but the ARCHIVE check refuses, so a reader who obeyed the message turned check (3) red. Found by the integrator doing a REAL rollover the morning before, who ignored the message and used the file's own ordering; the check only COUNTS closed entries, so it was green either way and nothing objected. The message now walks oldest-first (same-date ties by DESCENDING line), marks a refused entry `PINNED, leave it where it is` with the reason and continues to the next archivable one, and when nothing archivable is left says the cap is unreachable rather than returning an empty list that would PASS a check whose cap is still exceeded. What the check ENFORCES and its exit behaviour are unchanged. Six tests -> nine, red two ways: reversing the tie-break names `SYNTHETIC newer of 09-05` where `SYNTHETIC oldest of them all` was expected (1 of 9), removing the archivable filter fails 2 of 9 on `the cap check named an entry that check (3) would then refuse in the archive`. The pure heading logic moved to `tests/architecture/helpers/defectRegisterHeadings.ts` because the test file had 39 lines of headroom under the 500-line cap and the addition needed about 90 — a second TEST file was not an option, since importing one test file from another runs its describe blocks twice. ALSO: `workflowSecretExposure.test.ts` still said js-yaml "is present only as a transitive dependency of eslint" and told the reader to declare it — both true when written and false since `52546080` declared it — corrected in the header comment AND in the sentence `loadYaml()` actually throws, which is the only text a reader ever sees; the loud failure itself is unchanged and still throws BY NAME rather than skipping, because a gate that quietly parses zero workflows reports "did not run" as "passed". AND compacted summary.md 54 -> 47 lines to the convention, folding the oldest entries into dated range lines pointing at their detailed files; nothing from 2026-09-01 on was touched. No version bump and no changelog line: nothing a player can see.
- 2026-09-06 v0.3.210 A GREYED-OUT COMMAND NOW SAYS WHAT IT IS WAITING FOR. Found by the standing loop playing `aoe2-prototype`: the Town Centre's "Research Feudal Age" sat greyed while its own tooltip said the age costs 500 food and the player held **1,295**, with nothing anywhere naming what was missing — measured as tooltip `"Research Feudal Age. Cost: 500 food; takes 130s."`, `food: 1295`, `disabled=true`, `visibleResearchOptions=["feudal-age","loom"]` against `researchOptions=["loom"]` — and a play session sat in the Dark Age for 38 minutes while the AI aged up at 12:52. THE ELIGIBILITY RULE WAS CORRECT AND IS UNCHANGED (spec §7.2 wants two of Barracks/Dock/Lumber Camp/Mill/Mining Camp, and the button enabled the moment a Lumber Camp and a Mill were both done); the class that was actually fixed is that a command the card DRAWS but the player cannot use gives NO REASON — the repo's refusal sentences existed only at the moment of rejection, which leaves two holes: a disabled control can never be clicked so its reason is unreachable from inside the game, and a control that looks usable only explains itself after the click. The reason is now computed in `bridge/commandAvailability.ts` beside the option lists and carried to the HUD in `SelectionState.unavailableCommands` for research, train, build and market, so a tooltip cannot disagree with the validator that would reject the same click, and reason order matches the validators' own so a structural rule outranks a price. Gated by `tests/simulation/commandAvailabilityReasons.test.ts` (6 cases at the bridge) and `tests/browser/command-refusal-reason.spec.ts` (2 cases in the DOM), covering a building prerequisite, a technology prerequisite and a price across all four kinds, asserting the message NAMES the missing thing and that the age-up reason must NOT contain `food` — the misdirection the defect consisted of; red twice (with `unavailableCommands: []` 5 of 6 fail, `expected undefined to be defined`; with the age-up reason degraded to `'Not available.'` 3 of 6 fail, `expected 'Not available.' to contain '2 completed Dark Age buildings'`) and 2 of 2 red in the browser on a rebuilt suppressed tree. THE AUTHOR'S OWN CAUGHT ERROR IS THE INSTRUCTIVE PART: the first cut had the build card assemble a LOCAL fallback sentence — a second copy free to drift from the bridge — and it made the "before" arm of the comparison answer with a sentence the base never had, so that pixel diff read 0.00% and measured NOTHING; it was removed and the bridge is the only source. Captures: resting HUD 0.00% on five views, hover on the locked age-up 7.15% (34,309 px, the tooltip box alone), hover on an unaffordable build card 0.10% (493 px, the diff image spelling the sentence out); `getSelectionState()` costs 0.0215 ms in the worst realistic case, 17 build options all reasoned. BOUND, six commands still refuse silently, ranked: a queue entry blocked by the population cap (OBSERVED — food charged 500→350, three villagers at 0% forever, and `ProductionQueueEntry.isBlocked` is already set and ignored by the HUD), an unfinished building's card drawing its whole set while all three validators reject `under_construction`, tribute naming neither resource nor recipient, keyboard users unable to reach a disabled button's reason at all (`disabled` makes it unfocusable), commands the card does not draw, and no gap found for action/stance/formation on a completed building. TOOLING GAP, not fixed: `scripts/captureMapScreenshot.mjs` has no `HOVER` variable and a disabled button cannot be focused with `HOTKEY`, so this change's only visible surface is unreachable from the maintained capture script and the hover pair came from a scratch driver reimplementing its two trust checks; a `HOVER="<data-command>"` variable is worth adding.
- 2026-09-06 A WORKFLOW THAT CAN SEE A SECRET CAN NO LONGER BE TRIGGERED BY A PULL REQUEST. The repository went public on 2026-09-05, and `.github/workflows/playtest-llm.yml` still triggered on `pull_request: types: [opened, labeled, synchronize]`: it checked out the merge ref and ran `npm ci` plus `npm run playtest:corpus-llm` — the contributor's own install scripts and package script — with `ANTHROPIC_API_KEY` in the step environment. A private repo takes pull requests only from people who can already push; a public one takes them from anyone. THE TRIGGER IS GONE: only `schedule` and `workflow_dispatch` see the secret now, and both run code already on a branch here; the `if:` went with it (it guarded only the PR path and was true for every schedule and dispatch run), as did the unreachable PR-comment step and this workflow's `pull-requests: write`. `ci.yml` gained the `permissions: contents: read` it never declared — the repo default is already read, so that pins the floor rather than closing a live gap — and `pull_request_target` was NOT introduced and must never be. THE FINDING THAT MATTERS is why that path never ran once in 120 runs (119 schedule, 1 push, zero pull_request): THREE things protected the key and NONE was visible in the file as a protection — a GitHub default that withholds secrets from fork PRs, a human applying the `llm-playtest` label, and an `if:` requiring `vars.ANTHROPIC_KEY_AVAILABLE == 'true'` where this repo has NO Actions variables at all, so that half was false for every event that ever reached it. Setting that variable to `true` — the obvious move for anyone wanting LLM playtests on PRs — re-arms it in ONE CLICK with no commit; and the label was never a code gate, because `synchronize` sat in the trigger list, so labelling approved the code once and the key-bearing job re-ran on every commit pushed afterwards. A condition that reads a name nobody has defined is not a gate, and the shape to hunt for is a protection whose evidence lives outside the file. Gated by `tests/architecture/workflowSecretExposure.test.ts` (js-yaml parse, never raw text — `ci.yml`'s comment "uses no secrets." and `playtest-llm.yml`'s "Do NOT add `pull_request_target`" both defeat a raw scan), six in-file fixtures self-testing the classifier, proven red three ways and green once; `js-yaml` is now declared in `devDependencies` at `^4.3.1` (one line in `package.json`, one in the lockfile, verified against what npm itself writes) because it was only a transitive dependency of eslint. CORRECTING A RECOMMENDATION THAT WAS PASSED ON: "turn off sending secrets to workflows from fork pull requests" does not exist for a public repo — that API answers 422, it is a private-repo control — the real and still-unset one is `fork-pr-contributor-approval`, at `first_time_contributors`, which the owner should widen to all external contributors. Left OPEN: CI fetches `civ-engine.tgz` from a rolling mutable release tag, and pinning it needs `civ-engine` to publish a per-commit artifact, which this fleet may not modify; a sha256 pin on the mutable URL was rejected because it would redden all three workflows on every engine publish.
- 2026-09-06 EVERY CHECKOUT OF THIS REPOSITORY NOW GETS LF, WHICH IS WHAT MAIN'S WINDOWS CI LEG WAS RED FOR. The repository went public on 2026-09-05, Actions ran for the first time in weeks, and the first real run found it: on `ca24709b` ubuntu-latest passed all 15 steps and windows-latest failed the Vitest step with ONE red file out of 482 — `tests/scripts/ciStatus.test.ts`, 6 of 8 cases, every one `SyntaxError: Invalid or unexpected token` and nothing else. THE PREMISE TO KILL FIRST WAS "WINDOWS": that file passes on the authoring Windows machine and inside this repo's own Windows `npm run verify`, so the variable is not the OS. It is the WORKING TREE. Git for Windows installs `core.autocrlf=true` in its SYSTEM config and the `windows-latest` image is that same install, so `actions/checkout` writes CRLF while the index stays LF — a fresh clone + `checkout ca24709b` on the authoring machine gives `scripts/ci-status.mjs` **475 carriage returns against 0** in the long-lived tree beside it, and git says so unprompted (`warning: ... LF will be replaced by CRLF the next time Git touches it`). The census is the whole story in one line: index 1,766 LF, WORKING TREE **932 LF, 810 CRLF, 24 mixed** — a long-lived clone is a patchwork, a file written by an agent keeps LF and a file actually checked out holds CRLF, and `ci-status.mjs` was authored the day before. Root cause is one regex in a dependency: `vite@6.4.3` `dist/node/chunks/dep-Dm0c1Wj2.js:25674`, `hashbangRE = /^#!.*\n/`, which `ssrTransform` uses to inject `__vite_ssr_import__` calls AFTER a shebang — a JavaScript `.` does not match `\r`, so under CRLF it misses, the offset falls back to 0 and the imports land in front of `#!/usr/bin/env node`, leaving a bare `#` mid-module. Reproduced exactly by converting the file in place (`6 failed | 2 passed`, same line 131:21), and NARROWED BY A CONTROL that names the cause precisely: same CRLF file, `#!` line replaced by a comment, `8 passed` — so it is shebang+CRLF, not CRLF. Corroboration nobody had to construct: `scripts/content-lib.mjs` is CRLF in the authoring tree and is a static import in four passing content tests; it has no shebang. Fix is `.gitattributes` (`* text=auto eol=lf`, `*.png binary`) and NOTHING ELSE — not the eight cases, not their assertions, not the `node:child_process` seam, and not `scripts/ci-status.mjs`, because deleting its shebang would fix one test and leave 17 other shebang scripts carrying the same defect. No renormalization churn: the index was already uniformly LF. Gate `tests/architecture/checkoutLineEndings.test.ts`, five checks over `git ls-files --eol -z` (git's own attribute resolution, not a re-reading of the file), proved red four ways — no `.gitattributes` 3/5 red, `eol=crlf` 2/5, `scripts/** binary` 1/5 on the enumeration control, a real CRLF blob via `hash-object --no-filters` 1/5 — and it fails on EVERY platform where the failure it stands for was Windows-only. Register entry 2026-09-06; "The AI waits for a full attack group" rolled to the archive to make room, because the oldest closed entry (`PROVEN, fix measured and NOT shipped`) is pinned unarchivable. Bound stated in the gate: it asserts what a CHECKOUT produces and deliberately not what is on this disk, since 810 files here are still CRLF from before and a stale tree cannot reach CI. No version bump and no changelog line: a test fix.
- 2026-09-05 (v0.3.204–v0.3.209, plus five pieces of infrastructure): the gather comparator ranks by the WALK, not by Manhattan distance — misranked wood targets 0-72% per seat -> 0-5% across all nine maps — and the deposit leg descends the same field (v0.3.205, the two boot-map gates re-bound to match resolution and variety moved to a no-attack coverage lab). v0.3.206 restored every unit's gait at 0.75x its OWN base rate (the weight had been speed/2.5, a clock nothing has reached since v0.3.160); v0.3.207 made the command bar one fixed band, which CREATED v0.3.209 — `.hud-panel--selection` had opted eight children back into pointer events BY NAME, so 1,065 of 3,567 painted points at 1280x800 sent clicks straight through to the map; v0.3.208 gave the ground-cell route the build branch the entity route had had since 0.1.17. A claim this session made TWICE was wrong: `trade-route-reach.spec.ts` had not been "passing for two months on an action no player could perform" — it was right until v0.3.207 that same morning, and the swallowing defect then hid the consequence. Infrastructure: `ci:status` answers for a COMMIT rather than for whatever run is newest (`const latest = finished[0];`, and no call in the script mentioned a sha); `bundleIo.ts` streams both directions past V8's 536,870,888-char string cap, which had been discarding finished 30,000-tick playtests; the defect register rolls over behind a gate that refuses to bury a live defect; six closed threads moved to `done/` and closing became a rule with a gate; the harness-first rule became a PreToolUse hook that went red on the first command after install. Also found and repaired: `../civ-engine/.git` had been emptied by this fleet's own `git worktree remove --force` following a copied `node_modules` junction. Detail: `detailed/2026-09-02_2026-09-05.md`, `detailed/2026-09-05_2026-09-05.md`.
- 2026-09-04 (v0.3.200–v0.3.203, and the knowledge-routing pass): two priority-list heads found by MEASURING rather than reasoning — arena o1 had 19.6 villagers assigned to wood and 3.01 CHOPPING, because `paintWoodlines` keeps woodlines at least 14 EUCLIDEAN from every start while `DROP_OFF_ANCHOR_RADIUS` was 12 MANHATTAN, so three maps had zero trees within reach of either start; and neither walled map ENCLOSED A TREE, which makes arena o2's endless Dark Age a death spiral rather than a stall (zero units on the map from t7,000 to t16,000). v0.3.202's critic was right about six things, the first being that the class was not closed — `shouldPush` gates every fallback in the attack phase, so an army one unit under the Castle threshold idled at its own base against an opponent with no units at all. v0.3.201 shipped the thirty-civilization panel (§11.14, spec written that task), which strikes through what the simulation does not implement rather than promising it: 18 complete, 12 partial. The handoff prompt was emptied into the repo on the owner's rule that a prompt holds the immediate task and nothing else — the owner-reported shadow-rotation defect had existed only in prompt text and became an OPEN register entry, and `world.isAlive(id)` became a lint rule over `tests/**`, because the engine recycles ids from a free list and a dead entity's reused id reads back as alive with the same components. Detail: `detailed/2026-09-02_2026-09-05.md`.
- 2026-09-03 (v0.3.199, priority 1): an AI reaching Imperial never trained another unit — `getTrainOptions` offers the RESEARCHED tier, `pickUnitMix` names the top of each line, and the production loop matched by exact name; wanted units now resolve to the offered unit of the same LINE (union-find over `UNIT_LINE_UPGRADES`). Measured with `npm run ai:army-block`: fortress o2 army 0 -> 3, gold-rush victory 41,967 -> 34,704, arena 105 -> 95 (a composition trade). Four hypotheses died first, among them the morning's "root cause" (the fixed per-age villager split); `aiFeudalStone` was measuring a FINISHED match and was repaired, not relaxed.
- 2026-09-03 (v0.3.195, v0.3.197): four of the nine playable maps had NO WOOD — the forest-tree pass lived inline in `defaultMap.ts`; `seedForestTrees` now runs in every generator (arena 346 trees, coastal 198, fortress 348, gold-rush 344) and arena owner 1 goes from never leaving the Dark Age to Castle at 23,750 with peak army 63; gate `mapRoster.test.ts` :: every playable map supplies wood, red on exactly those four. A decided match never ended because `pickAttackTarget` aimed only at Town Centres; a one-surviving-building-per-owner fallback takes gold-rush from running at 60,000 to VICTORY at 41,967 (`aiAttacksAnyEnemy.test.ts`).
- 2026-09-03 (priority 2, proven and REVERTED): `pickNextBuild` returns the first missing entry, so one permanently unaffordable building starves everything behind it — arena owner 1 sits in Castle at 45,000 ticks with no Stable or Market and 1,601 stone it cannot spend, and no seed has ever built a Siege Workshop — because nothing arbitrates wood between military and construction past Feudal. Reserving the wish's wood works completely (arena builds the first Siege Workshop and reaches Imperial) and was reverted: army across slots 136 -> 81 and no seed newly resolves; the lever is wood INCOME.
- 2026-09-03 (v0.3.194, v0.3.196, v0.3.198): shadows take the shape of their caster — `aoeVoxelShadowShape.ts` reads solid parts as a vertical profile and tiles the swept footprint with flat triangles in the sun's frame, and a plinth now RECEIVES its own building's shadow (instances 862 -> 1188, the non-shadow recipe digest byte-identical; a critic then fixed a relax loop that could hang and 9.0% of invented shadow). Painted removed, Moebius only, on owner direction. Stone walls read as walls (roadmap M7): the base fills its tile, the `stone-wall-cap` slab that hid the merlons is deleted, and teeth on the four corners read as a crest at any orientation.
- 2026-09-03 (remote gates red for an infrastructure reason): from 07:44Z every aoe2 Actions run failed in 2-9 s with zero steps executed and `runner_name` empty, across push, rerun and `workflow_dispatch` on both runners — the signature of an exhausted Actions allowance or spending limit, not the code (v0.3.193 passed 3221 unit tests, 184 browser tests, the corpus, typecheck, lint and build locally); the owner checks the account's billing.
- 2026-09-02 (v0.3.192–0.3.193): units walk smoothly and deer run — `displayedPositionSmoother.ts` draws each unit one step cadence behind the sim (villager mid-walk still frames 67.4% -> 0.0%, deer 91.1% -> 0.0%); critics found the fix evaporated below 10 FPS and the whole-cell guard wrong for relics and sheep, so `unit-motion-smoothness.spec.ts` also drives 150 ms frames and bounds drawn-vs-sim lag. Sun-cast ground shadows; "shadows never stack" was FALSE (coincident slabs z-fight), so `resolveShadowLevels` lifts same-cell casters without bound; known limit: shadows bob in crowds (18.9% of caster-frames).
- 2026-09-02 (v0.3.190–0.3.191): the independent critic on v0.3.187/188 found seven real problems behind green gates — nearest-edge ordering restored on every building approach (boot-map Castle 35,250 -> 26,750), 1.47M discarded allocations per 20k ticks in the plan cache, the peak-army bar re-derived to 10. `enemyDefenceRange.ts` keeps gatherers out of enemy static-defence reach: owner 2 villagers lost 50 -> 7 at 45k, gate `villagersAvoidEnemyDefencesSelfPlay.test.ts` red-checked at 43. The army regression (peak 20 -> 11) was measured, both hypotheses died, nothing shipped; dead `lastSeenTick` deleted.
- 2026-09-02 (v0.3.187–0.3.188): DE build times (sixteen wrong rows; House 120 -> 250 ticks), the 3·base/(n+2) multi-builder curve, nearest-edge approaches and AI construction crews, gated by `structureCostsAndBuildTimes.test.ts`; the remote `playtest-corpus` then caught what the local gate could not — a 537 MB bundle over V8's string ceiling (fog memory rewritten every tick; now 181 MB) and nine villagers latched on one cell by nearest-first ordering; corpus horizon 13,000 -> 20,000; two register entries OPEN (military production not retuned for DE build times; `ai-feudal-stone-fixture` freezes at ~15,000).
- 2026-09-02 (v0.3.179, v0.3.186, v0.3.189): three command-bar defects found by playing — the placement status squeezed the build palette to 36 px at 1280x720, the idle-villager badge sat on the SELECTION label at every viewport, and at 800x600 six build cards were off-panel (the build menu now has DE's two pages). Gates `placement-mode-hud-layout.spec.ts`, `hud-floating-controls.spec.ts`, `command-deck-fits.spec.ts` (12 tests, mutation-checked); `PREVIEW_PORT` refuses a preview whose asset hashes differ from this checkout's dist.
- 2026-09-02 (v0.3.177–v0.3.178, v0.3.183): the Lumber Camp candidate SHIPPED on a 30k gate and was REVERTED the next commit — it led only in the 25-36k window and lost the match at 45k (peak army 10 vs baseline 20; thirty villagers killed under the enemy TC); the gate now runs to match resolution (45,000 ticks) and villagers-under-enemy-TC entered the register. v0.3.183 applied the multi-CLI review of the traffic election and corrected the v0.3.175 record: phantom relief admissions 5 -> 0, converted units replan (`convertedUnitGateReplan.test.ts`), six mutants killed.
- 2026-09-02 (canon + tooling): the lessons index was deleted (84 entries, none naming a gate) and returned the same day as an EMPTY queue where every entry names its gate; two standing-loop rules entered FLEET.md; `rosterCoverage.test.ts` derives the roadmap's coverage line (116 versions stale; units 93/93, technologies 137/138, buildings 25/25); `voxel:build` rebuilds the sibling only when stale and under a lock; commit 1b6bae62 swept a peer's file in via `git add -A`, recorded rather than rewritten.
- 2026-09-01 (the horizon was the instrument): "the boot map is walled into Feudal" was a 24,000-tick artifact — at the audit's real 45,000 default 6 of 8 owner-slots reach Castle (boot-map owner 2 at 25,250, peak army 20), one reaches Imperial and matches resolve by conquest; `scripts/selfplay-audit.mjs` now defaults to 45,000 with the reason in the file, and three recorded conclusions were retracted as the same artifact. seed-7 owner 1 closed as a phantom defect: twenty villagers killed by a militia raid, not an economy wall.
- 2026-09-01 (v0.3.176 + performance): `findNearestDropOffBuilding` measured to a building's CORNER, so adjacent cells disagreed on the nearest drop-off and a carrier paced forever — stuck villagers 7 -> 1 across six seeds (`seed-2` regresses by one); `approachPlanCache.ts` memoises the AI's per-tick A* on (unit, cell, target, structuralRevision): +35-58% ticks/s with byte-identical state fingerprints, `npm test` 172 s -> 121 s; tick cost still grows 3.7x over a match without tracking unit count (9.43 ms at 40 units), which is engine-side and filed in engine-feedback.
- 2026-09-01 (villager latch + why the army falls): the narrow-passage arbiter starved high ids (admission 88% -> 0.6% by id; villagers motionless up to 2,750 ticks) — lowest-id stays the rule and yields after 750 ticks without a cell change, stuck villagers 26 -> 7 over six seeds; round 2's candidate was rejected by `aiReachesCastleAge` (peak army 7 < 8) because its army is trained at parity and then DIES (lost 6 vs 1; the message now bound to `PEAK_MILITARY_BAR`), and round 3 proved the latch fix does not rescue it (trained 8, lost 6, peak 7, identical).
- 2026-09-01 (lumber-camp search, rounds 1–3): branch A (anchor the camp on the woodline medoid) was the first of eighteen attempts to move the mechanism — camp usage 39.7% -> 51.9%, Castle slots 4 -> 7 at 24k — and was disqualified by a ten-villager deadlock on one cell (registered); the root cause then moved a layer down: the gather comparator ranks by Manhattan distance while movement is 4-connected, worth 1.49x throughput (wood 2.61x), with camp placement only 8-20% on top; the Feudal wood ledger corrected — farms 63.6%, military under 10%.
- 2026-09-01 (v0.3.171–0.3.174): Sultans (villagers gather gold 10% faster, stacking with Gold Mining), Carrack (ships +1/+1 armour; unique techs 25 of 41) and the Indians' scout line +2 vs buildings — then a RETRACTION, since the Sultans and Carrack cost rows said "wiki" and no wiki was consulted; both are UNVERIFIED and the remaining gap is DATA (`technologies.csv` has no row for any expansion unique tech — 18 techs, 17 units — the wikis are unreachable, aoe2techtree's data.json has costs but no effect magnitudes, and which edition the spec targets is the owner's decision). Also: the behind-building cue's ENEMY colour had never been looked at because the fixture spawned only owner-1 units (coral scored 76 against thatch, magenta 137, gated at a bar of 90 on the property rather than the hex), and boot-map sweeps at four zooms and seven renders found NO defect once three candidate defects of mine dissolved (coordinates derived by eye; a removal-diff settled it), shipping `townCenterFlag.test.ts` — hands-on play on `aoe2-prototype` was sound, and the Browser pane services rAF only when it captures a frame, so it cannot judge pacing.
- 2026-08-30–31 (v0.3.169–0.3.170, and the Feudal-wall search, attempts 4–17, most reverted): the first AI self-play audit ever run RESOLVES the boot map, but neither player leaves Feudal, because the AI cannot pay — `afford=false` 164 times, 525 wood on 21 spearmen — so `agePrerequisiteWoodReserve` (floor 3) made the boot map Castle-eligible at 23,500. The wall was HALF a horizon artifact (Castle slots 4/20 at 24,000 against 10/20 at 34,000) and 73% of the remaining delay is the two prerequisite buildings; every fix that merely moves wood between claimants was retired as a class, and `npm run ai:selfplay-audit` made the acceptance test a per-owner command. The camp the AI never uses was root-caused here: `findNearestDropOffBuilding` is asked from the VILLAGER, so the Town Centre is permanently the reference and 6 of 9 camp-owning slots serve zero villagers. A `const` arrow used above its definition crashed every gather assignment with typecheck and lint GREEN, which is why `@typescript-eslint/no-use-before-define` is enabled (24 live violations when it went on). Detail: `detailed/2026-08-25_2026-08-30.md`, `detailed/2026-08-30_2026-09-02.md`.
- 2026-08-30 (v0.3.166–0.3.168): AI drop-offs anchor at the RESOURCE, not the TC (camp-to-resource median 5 -> 2-3 tiles; worst-owner Feudal 17,200 -> 14,100); tick cost HALVED, 15.13 -> 7.99 -> 5.45 ms/tick at 34 units, by memoising `isCellPassableForSpawn` on `structuralRevision`; wolves retaliate, deer exist, a scout froze on its wander box's far edge (one corner registered OPEN); a Dock counts toward Feudal and a Castle toward Imperial. Translucent silhouette attempted and REVERTED (Moebius quantises 24.1% of it back to opaque; register OPEN).
- 2026-08-29 (v0.3.161–0.3.165, and the end of the CI outage): main's remote gate had been red for 200 consecutive runs since 2026-07-07 behind green local gates — the `.github/voxel-commit` pin sat at 0.1.4 against code needing 1.2.0, GNU tar read `D:` as a host, and a workflow was missing from its own path filter — which is why `npm run ci:status` is a session-start command and the corpus progression budget went 8,000 -> 13,000. Shipped alongside it: gates a player BUILDS open for them (`admitsThroughGate` read completion as "no construction record"), loaded villagers stop idling on a full carry (boot-map owner 1 Feudal 16,000 -> 12,500), ships stop sailing through Fish Traps, `checkEveryOwnerAgeProgression` can see a frozen player, and v0.3.163's shrinking approach budget was corrected to a flat one. Dark-Age tuning 6:3 adopted and WITHDRAWN (800 ticks worse on the boot map); boot-map owner 2 gathering 32% of villager-time against 62% registered OPEN. Detail: `detailed/2026-08-25_2026-08-30.md`.
- 2026-08-26–28 (v0.3.128–0.3.160): the content-differential audit trilogy machine-locked every roster to the CSVs — hosting, the 46-entry attack-bonus harvest plus class armour, base stats, structures 28/28 — and per-civ tech-tree denial (`civDenies`) landed with rows SOURCED from aoe2techtree: ~380 corrections, every civ had errors, plus ~30 more from a civ-bonus source audit, with Tracking and Cartography retired per current DE. Then DE hotkeys (H , F3 +/- Space) and the tech-tree viewer (F1), and a hands-on DE-opening audit that found gather cadences 4.5-10x spec §6.3 and moved default zoom 2.0 -> 1.2; `npm run verify` was created here, after a tail-piped gate hid red lint. Pacing the economy to §6.3 (all 8 gather families ±5%, pinned) and the movement clock to §12.4.2 (0.32 fine/tick) exposed a tick-8300 collapse at 12.3 s/tick — whole-map flood connectivity, cached failing A*s, an iterative BFS in the traffic election. Detail: `detailed/2026-08-25_2026-08-30.md`.
- 2026-08-24–25 (v0.3.56–0.3.127): the rosters CLOSED — every technology (Spies/Atheism v0.3.70), unit (Missionary v0.3.71) and building — alongside land and naval trade, tribute + Coinage + Banking, teams, player colours, 3–8-player skirmishes, the map-size ladder, setup screen, match options and population cap. Coastal, Fortress and Gold Rush maps arrived (the fixture validator caught a Castle anchored on a boar); the civ if-chains became `civBonusTable.ts` and seventeen civs got their identity, superseding the 2026-07-05 five-civ cap in decisions.md; thirteen team-bonus lines run on one predicate (`teamHasCivilization`); `effectiveConstructionCost` reached nine call sites so the build card quotes the owner's price. Then DE controls and feedback — town bell, Delete, control groups, shift-queued waypoints and construction, attack-ground, mechanical repair, allied gate passage, procedural audio, death collapse, per-civ roofs, walls and crowns — and the browser suite caught `garrisonUnit` wiping the human selection. The stray-arrow near-loss and the late-game freeze were measured here (register entry 2026-08-24). Detail: `detailed/2026-08-24_2026-08-25.md`.
- 2026-08-17–23 (v0.3.10–0.3.55): the Moebius art style, animated shoreline surf with decoration variants and terrain colour fields, the dependency audit back to green; then the systems that make it a game — projectiles fly, University/Ballistics, the naval foundation and war fleet, stances/attack-move/patrol/formations, 19 unique units and 16 unique techs declared as data, gates in walls, the Monastery tier, Transport Ships, the camera opening and the command bar — plus the first AI-economy traffic-jam diagnosis. Then the AI learns to LIVE: wood in forests, Castle Age reached, stone income, a build order past farms, the town bell, somebody to attack; Outpost and Fish Trap closed the building roster; Eagle/HC lines, Parthian Tactics, monk faith and Dock techs; and the unreachable-upgrade defect family (v0.3.45-53) seeded the menu↔validator gates. Detail: `detailed/2026-08-17_2026-08-23.md`, `detailed/2026-08-23_2026-08-23.md`.
- 2026-07-14–20 (v0.2.2–0.3.9): the presentation era — attack animations, boar hunting/gore, ungarrison, post-construction mining, building relief, tree forms, terrain materials, concrete unit/building identity, HUD command deck, garrison-on-Alt BREAKING split, melee-reach root cause.
