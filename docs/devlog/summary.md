## 2026-05-08 (spec discipline + visual-non-overlap infrastructure)
- **Spec §12.7 Movement Targets and Slot Reservation:** dual-strategy rule for unit movement to populated cells. Single-unit move to a fully-packed cell uses lazy redirect on arrival (BFS for nearest free slot, target rewrite to prevent oscillation). Group move uses eager pre-reservation (deterministic spiral-outward slot assignment at command issuance). Reservation lifecycle: arrival → occupation; new command / death / garrison / conversion → release; 30s TTL guards against unreachable destinations. Edge cases enumerated: mid-flight target blocker, reserved cell taken first, spawn into full cell, wandering transit, combat engage as ring-around-target group move, gather clusters via lazy-on-arrival, construction adjacency, mass formation truncation at map edge, naval/land domain restriction, save/load/replay determinism. Spec change only this commit; implementation will follow.
- **Visual non-overlap infrastructure (worldOccupancy, partial):** `worldOccupancy.syncUnit` now returns the engine-allocated 16-slot offset; `placeUnitForSpawn` and `getUnitSlotOffset` are added; `pureHelpers.getUnitTargetTransformForCell` and `isUnitTransformAtTarget` accept an optional explicit slot offset. 9 new tests in `worldOccupancyVisualSlots.test.ts` pin the contract. Production wiring (renderer + spawn paths) reverted before commit because threading the slot through `transformOps.syncOccupancyForEntity` / `moveUnitOneSubgridStep` and the train-completion / ungarrison paths broke the unitMovement, fogMemory, aiPlayer, and imperialUpgrades tests. Architectural barrier: a unit mid-move toward a full cell would oscillate as movement re-aims and syncUnit redirects each cell crossing — proper fix lives at the move-command layer (per spec §12.7). Closed pre-existing file-size budget violation (`unitCommandOps.ts` 539→486 LOC via extraction of `monkContextOps.ts`) and per-test timeout flakiness in `tests/simulation/castleUpgrades.test.ts` (15s→30s on six tests). No version bump — game runtime unchanged.
- **Spec persistence rule (process):** AGENTS.md gains "Game spec" top-level section requiring every user-described rule/behavior/mechanic/content/UX/constraint to land in `design/spec-final.md` as part of the same task — the goal is autonomous hand-off iteration without per-change intervention. Spec gains §12.6 "Unit Spacing and Sub-Tile Occupancy" formalizing sub-tile slot packing (4x4 = 16 deterministic slots per tile), neighbor-tile fallback on overflow, garrisoned/contained-unit exemption, and renderer interpolation independence. Spec text emphasizes that **visual** non-overlap is the player-facing contract — the simulation slot system is just the mechanism.

## 2026-05-07 (v0.1.17 multi-villager construction + v0.1.13 thread closure + deferred follow-ups)
- **Multi-villager construction (v0.1.17, user-visible feature):** three intertwined construction defects shipped in one feature — placeConfirm only assigned one builder, mid-build right-click did nothing for joiners, and the HP-bar appeared frozen because side-map mutations don't dirty the building entity for civ-engine's renderAdapter. Fix: `building.placeConfirm` gains optional `additionalBuilderIds: number[]` (backward compat), `routeUnitContextAtEntityCommandDirect` adds an in-progress branch with a `setUnitBuildCommandDirect` helper, `playerCommandsSystem` calls `world.patchComponent('renderable', r => r)` per builder-at-site per tick (dedupes at civ-engine's dirty-set level → one re-projection per site per tick). Iter-1 review (Codex + Claude) caught 3 real bugs — HP-bar test bypassed projector via `getEntityHealth`, mid-build budget too loose, in-progress branch ate fall-through on helper failure — plus 3 LOW/NIT items. All closed in iter-2. Iter-2 Codex unreachable (burned exec budget); Claude verified clean. Full gates pass.

## 2026-05-07 (v0.1.13 thread closure + deferred follow-ups)
- **`replay-deferred-followups` thread closes:** four user-visible items shipped — escapeHtml consolidation (refactor, no bump), cancel-during-await race (v0.1.14), dialog interactivity fix + prior-session e2e (v0.1.15), scrub-workflow e2e (v0.1.16). 7 review iterations across 4 sub-threads (escapehtml: 1; cancel-race: 3; prior-session: 2; scrub: 1) saved under `docs/threads/done/replay-deferred-followups/`. Two user-action items remain open for a future task: v0.1.5 annotation-ui visual baselines and any further visual-snapshot work the user wants to add via `npm run test:browser -- --update-snapshots`.
- **Scrub-workflow Playwright e2e (v0.1.16):** new `tests/browser/replay-scrub.spec.ts` (3 tests) drives the timeline scrubber + step buttons against a real replay session and asserts on `__AOE2_TEST__.replay.getReplayCurrentTick()`. Closes the fourth deferred follow-up from the v0.1.12 thread close. The "drag synthesis" was unnecessary — the timeline thumb is a native `<input type=range>` so value-set + change-event covers the same ground.
- **ReplayLoadDialog interactivity fix + prior-session e2e (v0.1.15, user-visible bug fix):** two pre-existing v0.1.12 CSS bugs surfaced while wiring the deferred prior-session Playwright spec — the author rule `.replay-load-dialog__panel { display: flex }` outranked the UA `[hidden] { display: none }` on cascade origin (so all three tab panels rendered simultaneously), and `#hud-root { pointer-events: none }` cascaded into the dialog (so every click fell through to html). Two new author rules close both bugs; all 5 Playwright tests now pass on chromium (the four pre-existing ones never actually ran end-to-end against a real browser before). New `__AOE2_TEST__.replay.seedPriorSession()` rolls a save+load round-trip so the live recorder closes its current session.
- **Cancel-during-load race fix (v0.1.14, user-visible bug fix):** `ReplayLoadDialog` now uses a generation counter (bumped on `close` event and on `dispose()`) so resumed async handlers bail before mutating controller state or DOM. Closes four races: prior-row click + Cancel before IDB resolve, file-import + Cancel before `File.text()` resolves, dispose-during-pending-handler, tab-click stale-list overwrite of the cache after close+reopen. `loadPriorSessionAsReplay` grows an optional `LoadPriorSessionSignal` parameter and a `'cancelled'` status. Codex iter-1 caught two additional bugs (dispose race + tab-click stale-cache) plus the doc gap; iter-2 closed all three. Full gates pass.
- **escapeHtml consolidation (internal-only refactor):** lifted the 5-char HTML escape (`& < > " '`) to one canonical `src/ui/utils/escapeHtml.ts` from three duplicates; postGameSummary's copy was missing apostrophe coverage (defense-in-depth, text-context no-op). New `tests/ui/escapeHtml.test.ts` (6 tests) locks the contract. Codex + Claude both clean on iter-1 (one NIT on test name, fixed). No version bump.
- **Phase 3E Slice 6 — replay-flow integration + Playwright e2e (v0.1.13):** vitest+jsdom integration suite (6 tests against real `recordCommandReplayFixture` bundles) covering all three load sources + the slice-4 transactional `enterReplay` guarantee at the integration boundary; Playwright e2e (4 tests) drives the actual app via `__AOE2_TEST__.replay.{getReplayMode,getReplayCurrentTick,openReplayLoadDialog}` — open/cancel, deterministic 120-tick live-confirm + Escape exit, file-tab malformed-JSON rejection, programmatic open. No canvas pixel snapshots. Browser test API gains a required `replay` sub-object. **Thread closes** — `replay-load-and-e2e` moves from `current/` to `done/` after Slice 6.

## 2026-05-06 (v0.1.8 + v0.1.9 + v0.1.10 + v0.1.11 + v0.1.12 + thread setup)
- **Phase 3D Slice 5 — ReplayLoadDialog modal (v0.1.12):** unified replay-load entry point — three tabs (Live session / Prior session / From file) under one `<dialog>`. Replaces the slice-2/4 standalone HUD buttons. Default tab depends on availability; HUD button disabled while in replay (re-entry guard). Codex iter-1 caught stale cache + non-idempotent open; Claude iter-1 added missing CSS, asymmetric error toast, live-tab desync, brittle tab dispatch. iter-2 cleared all substantive findings; 3 LOW items (escapeHtml consolidation, generation-token cancel race, visual screenshot proof) deferred to a follow-up cleanup batch. Full gates pass.
- **Phase 3D Slice 4 — Replay file import (v0.1.11):** new "Replay file" HUD button takes a user-uploaded `SessionBundle` JSON file and enters replay mode. New `parseSessionBundleFile` structural validator (10 top-level fields including `schemaVersion`, 7 required arrays, typed metadata fields) + `createReplayFileImport` DOM helper. `ReplayController.enterReplay` restructured to construct BEFORE mutate so any engine-level rejection (`schemaVersion` mismatch, missing `metadata.engineVersion`, range violations) leaves the controller in its pre-call state — closes the partial-apply bug class for replay-while-already-in-replay across all three load sources. Codex iter-1 + iter-2 + iter-3 found three IMPORTANT issues (parser top-level arrays, schemaVersion validation, metadata partial-apply); the third one closed at the controller layer. iter-3 Claude said ship. Full gates pass.
- **Phase 3D Slice 3 — Prior Sessions Replay button (v0.1.10):** `MarkerListPanel` Prior Sessions rows now render a "Replay" button alongside Export/Discard. Click reconstructs the bundle via new `RecordingService.loadPriorSessionBundle(sessionId)` surface, then `loadPriorSessionAsReplay` helper (mirrors `loadCurrentSessionAsReplay`'s contract — rejects `commands.length === 0` before `enterReplay`). Disabled gate: schema-mismatch OR abnormal-empty (`!closedNormally && endTick === startTick`). Codex iter-1 caught the design/impl gate mismatch + stale JSDoc; Codex iter-2 caught the elapsed-but-zero-commands consistency gap with slice 2 (fixed via helper extraction). iter-3 Codex + Claude both said "ship it." Full gates pass.
- **Phase 3D Slice 2 — load current live session as replay (v0.1.9):** new "Replay" HUD button (next to Save/Load) takes the live `RecordingService.bundle()` and hands it to `replayController.enterReplay(bundle)`. Disabled-state tracks three signals (immediate `onModeChange` listener, 500ms poll, click-time re-check). HUD HTML template extracted to `hudTemplate.ts` to keep `createHudController.ts` under the 500-LOC budget. `let stack: AnnotationStack | undefined` forward-declared in `createApp.ts` to avoid the TDZ defect Codex+Claude both caught at iter-1. iter-2 converged with one LOW (changelog wording, fixed inline) and Claude's "ship it." Full gates pass.
- **Phase 3D Slice 1 — replay-mode annotation affordances (v0.1.8):** Alt+M is silent in replay mode; `MarkerListPanel` flips to the replay bundle's markers (read-only, hidden Prior Sessions section, row click → `jumpToMarker`); `Set`-based severity clamp + escaped tick/id/severity render block prototype-pollution and malformed-JsonValue exploits in the upcoming file-import slice. `gateAnnotationHotkeyOnReplayMode` helper makes Alt+M gating explicit and unit-testable. Codex iter-1 + iter-2 found three IMPORTANT issues (version drift, JsonValue escaping, prototype-pollution clamp); all fixed inline. iter-3 Codex + Claude both said "ship it." Full gates pass.
- **`replay-load-and-e2e` thread opened:** new `docs/threads/current/replay-load-and-e2e/` carries Phase 3D + 3E (load sources for current session / IDB Prior Sessions / file import; unifying ReplayLoadDialog modal; browser e2e + integration). DESIGN.md + PLAN.md + design/01-08 sub-files + iter-{1,2,3} REVIEW.md committed alongside Slice 1.
- **Closed replay-scrubber DESIGN.md split:** the 1832-line single-file spec became a 24-line index plus 14 bounded sub-files under `docs/threads/done/replay-scrubber/design/`, ordered to match the original section numbering. Verified lossless by line count and H2/H3 section count. Going forward, design docs will be split into bounded sub-files by default. No code change.

## 2026-05-05 (v0.1.7 replay timeline + v0.1.6 schema-2 save format)
- **Phase 3C replay timeline panel:** `TimelinePanel` now renders the replay-only bottom strip with marker/hotspot pins, coalesced range scrubbing, play/pause, +/-1 step, exit, source/tick readout, inert no-payload controls, and capped-range pins. `ReplayHotkeys` binds replay keys only during replay mode, `createApp` exits replay before save-load bridge replacement, and CSS reserves bottom viewport space for desktop/mobile. Codex reviews found stale-bridge, hotkey, layout, pin, no-payload, transactional load, and docs/version/validation issues; fixes converged after doc cleanup, Claude stayed quota-blocked, and full gates pass.
- **Phase 3B replay controller:** `ReplayController` now owns live/replay bridge swaps, coalesced scrubbing, command-driven playback, failed-tick stops, no-payload replay guards, destructure-safe methods, prior live pause restoration, and transactional exit/scrub rollback. Gemini fallback and Codex follow-up reviews found real issues across playback pacing, selection preservation, rollback, failed ticks, dynamic method binding, pause-state restoration, stale docs, and cached-playback coverage; fixes landed with regression coverage. Final Codex review found no substantive issues, Claude stayed quota-blocked, and full gates pass.
- **Schema-2 save format:** New saves emit `schema: 2` with only `seed + worldSnapshot`; top-level `sideMaps`, `visibility`, and `matchState` are gone. Schema-1 saves still load through the legacy side-map hydration path and re-save as schema 2. `aoe2.pendingCommands` persists AI intentions queued between decision and dispatcher drain. Final Codex review found no substantive issues, Claude stayed quota-blocked, and full gates pass.
- **Phase 2G invariant:** Snapshot equivalence now pins the exact Tier-1 slot inventory, iterates the authoritative `TIER_1_CODECS` registry, covers all Tier-3 slots including `aoe2.pendingCommands`, and adds a bounded bridge snapshot/schema-2 save perf gate. Codex iter-2 found no substantive issues, Claude stayed quota-blocked, and full gates pass.
- **Phase 3A/A.5 replay foundation:** `createReplayWorldOnly` builds replay-mode schema-2 worlds for `SessionReplayer.openAt`, replay context stores bridge internals in a WeakMap, and command round-trip tests now cover initial-snapshot replay, validator/result parity, AI pending-boundary ticks, and pending-snapshot advancement. Codex found and fixes closed two pending-command replay bugs; Claude/Codex final re-review was quota-blocked, Gemini fallback found no open issues after cleanup, and full gates pass.

## 2026-05-04 (v0.1.6 roadmap continuation)
- **AI monk intention split:** `aiSystem` now queues AI monk task decisions as `monk.contextAtEntity` intentions instead of mutating `monkTasks` directly. Queued AI monk commands carry expected owner/task guards, `pendingCommands` persists across save/load with cloned payloads, and the existing monk handler revalidates pickup/deposit/heal on the next tick. Full gates pass.
- **Monk task state migration:** `monkTasks` now lives in `world.state.aoe2.monkTasks` via `monkTasksCodec` + `BridgeStateAccessor`. Active Monk tasks flush into snapshots, schema-1 `sideMaps.monkTasks` is authoritative on load, and no-op Monk-task cleanup avoids empty tick diffs. Full gates pass.
- **Unit command state migration:** `unitCommands` now lives in `world.state.aoe2.unitCommands` via `unitCommandsCodec` + `BridgeStateAccessor`. Active move/build/attack orders flush into snapshots, schema-1 `sideMaps.unitCommands` stays authoritative on load, and `BridgeState` is down to runtime caches/derived maps. Full gates pass.
- **File-size queue:** `unitCommandOps.ts` split into `unitSelectionOps.ts` + `sheepCommandOps.ts`; removed from `fileSizeBudget` legacy exemptions. Codex review found no issues; Claude was quota-blocked until May 5 7pm PT. Targeted file-size/command gates pass, and the final full gates pass after the Vitest runner stabilization below.
- **Vitest gate stabilization:** `vitest.config.ts` now defaults to the `threads` pool with a 30s test timeout, and the long deterministic simulation cases use explicit caps sized for full-suite contention after fork-pool runs showed all assertions passing but exited non-zero from `onTaskUpdate` RPC timeouts. `npm test`, typecheck, lint, and build pass.

## 2026-05-02 (v0.1.6 in flight)
- **/full-review iter-1+2 — correctness batch:** R2-M1 autoAggression race, Gemini MAJOR pruneOrphanEntityKeys value-side leak, R2-C2 saveGame Tier-3 flush, Gemini MEDIUM accessor.flush atomicity, R2-M2/D4 pushIntention rename + dead-fallback fix, Gemini MINOR TC villager-train accumulator. Iter-2 closed atomicity placement, half-dead test gap, exhaustive switch. 725 passed.
- **Phase 2E:** visibility staleness fix — `(playerId, x, y, radius)` fingerprint cache + owner-flip remove+reinsert; closes Codex impl-19 MAJOR.
- **Phase 2D:** `monkTasks` and `unitCommands` now have accessor-backed migration coverage after the AI intention split; no Tier-1 codec remains bridge-owned before schema-2.
- **Phase 2A-C:** bridge-state migration scaffolding (`SlotCodec` registry + `BridgeStateAccessor` + `VisibilityCell`); output-phase tail (`tier3SyncSystem` + `bridgeSnapshotSystem` + `registerOutputTail`); `bootstrapFlush` + tail integration.
- **Phase 1C:** AI intention refactor — aiSystem pushes `pendingCommands` instead of mutating; `pickUnitMix` reorders BEFORE villager training so military wins under tight food.
- **Phase 1B:** all 15 commands commandified (`unit.move/attack/gather/context/contextAtEntity`, `sheep.move`, `monk.contextAtEntity`, `queue.train/research`, `market.action`, `building.placeConfirm/setRallyPoint/action`, `trebuchet.pack/unpack`).
- **Phase 1A:** scaffolding — `GameCommands` type, `dispatcher.drainPendingCommands`, empty `registerCommandHandlers`, `pendingCommands` queue.
- **Multi-CLI review process:** Codex extraction bug fixed (slice from `^codex$` header before awk markers); 5+ retroactive findings recovered post-hoc.

## 2026-04-29 (v0.1.5)
- **In-game annotation UI (Spec 2):** Alt+M for marker capture, Alt+L for list panel, IDB-mirrored sessions, JSON bundle export. Bridge gains `world` getter, `setPaused`, `getSelectedEntityRefs`, `select`. `loadGame` is async. Engine bumped to 0.8.11 for `AgentDriverContext.addMarker`/`attach`.

## 2026-04-26 (full-review V5)
- 15 V5-* fixes shipped: monksByOwner side-map for AI guard O(1), GameScene LOC trimming (regressed since), `findIdleProducer` batch precompute, multiple efficiency fixes.

## 2026-04-25 (full-review V4)
- 25 V4-* fixes: cast-hole closures, trebuchet sig reconciliation, fog-memory delete path, throttle persistence, conquest single-pass, file-size + naming cleanups.

## 2026-04-24 to 2026-04-26
- **createSimulationBridge.ts shrink:** 9519 → 332 LOC across 5 phases. Spawned `bridge/`, `systems/` (18 ECS factories), and ops modules. Side-map ownership lives in `bridgeState.ts`.

## 2026-04-23
- **Major refactors:** `prototypeScenario.ts` god-file split into `fixtures/` (13 categories); `mapGeneration/` for procedural gen; multiple 800+ LOC files split into themed sub-dirs with barrel re-exports.

## 2026-04-19 to 2026-04-22 (FU batches)
- **Batch FU8 (test stability):** EntityRef+ generation, throttled visibility, deterministic player commands; OccupancyGrid migration descoped.
- **Batch FU7 (Persian + bonuses):** Persians (TC +25 HP +25%, faster gather, free Town Watch) + civ-bonus framework.
- **Batch FU6 (Britons):** Britons (faster shepherds + arrow range/anti-cav). 35-tile attack-move "garrison-recall" trigger.
- **Batch FU5 (HUD UX):** queue cancel, rally pin, save/load via blob.
- **Batch FU4 (AI):** AI Castle/Imperial age-up, Monk training + relic collection, Wonder pursuit (wins on `ai-planner-fixture`).
- **Batch FU3 (defensive fire):** Castle garrisoned-archer arrows, real `stone-wall` / palisade.
- **Batch FU2 / FU1:** Militia → Champion, Paladin, Heavy Camel; armor damage + 11 Blacksmith techs.

## 2026-04-17 to 2026-04-18
- **Slices 1-12:** Castle + Longbowman, Imperial Age + 12 units + 14 techs, Wonder/Relic/Score win conditions, Save/Load (29 side maps), HUD UX polish, F2 debug overlay, Black Forest + Arena maps, engine-debt refactors.

## 2026-04-10 to 2026-04-13 (pre-roadmap)
- **Bootstrap + Dark/Feudal/Castle core:** TS + Vite + Phaser 3 + civ-engine; 18 supported civs; full economy loop; Barracks + conquest; Feudal + Castle units; deterministic combat polish.
