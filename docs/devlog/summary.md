## 2026-07-13 (voxel-only world renderer, v0.2.0)
- **One world, one canvas, no fallback:** AoE2 now boots directly into the reusable Three/`voxel` renderer. `AoeVoxelGameView` and renderer-neutral input/camera/presentation controllers replace the Phaser scene, while fog, selection, visible drag marquee, placement, authored-height health, hits, and deaths enter voxel snapshot lanes. Presented recipe silhouettes keep raised and moving entities clickable, and exact-click cycling is limited to current visible semantic targets; startup is visibly fatal and transactional. The entire `src/phaser` tree, painter tests, renderer switch, composite capture, dependency, and separate ~1.48 MB build chunk are gone; DOM HUD/minimap/replay UI remain. Fixed 1280x720 evidence changes 106,615/921,600 pixels (11.57%) at 365 instances, 12 draw calls, and 10,336 triangles. Final gates: engine 80/80; AoE 1,905 unit tests plus two skips, 104 browser tests plus two skips, typecheck, lint, 442-module build, zero-vulnerability audits, clean source/bundle scans, and three-reviewer re-review. Details: `docs/devlog/detailed/2026-07-13_2026-07-13.md`.

## 2026-07-12 (smooth speed-matched voxel gait, v0.1.150)
- **Feet now follow the unit instead of playing a generic walk loop:** AoE advances locomotion phase from the already-interpolated displayed distance, derives its short weight ramp from simulation tick-plus-alpha time, eases path-corner headings, aligns foot flex with X/Z/diagonal travel, and keeps transformed feet above ground. Manual/replay pause freezes the same gait state whether selection forces redraws or not. Static and animated matte/metal lanes are separate, so large scenery no longer inherits the engine's 16,384-slot active-batch ceiling; no shared schema or simulation/save/replay/hit contract changed. The controlled 800x480 proof holds a moving phase and 265 ms sample exactly across a 150 ms wall-time pause, then resumes at 415 ms; five repeated Chromium runs passed with six batches, 1,000 instances, 26 ambient-active slots, ten draw calls, and 16,632 triangles. Final gates: engine 80/80; AoE 2,084 unit tests plus two skips; 102 production-browser tests plus two intentional visual-baseline skips; typecheck, zero-error lint, 456-module build, and three-reviewer re-review green. Details: `docs/devlog/detailed/2026-07-11_2026-07-12.md`.

## 2026-07-12 (animated voxel units, v0.1.149)
- **Live voxel units now breathe and move:** stable identity-phased AoE-owned profiles animate humanoid limbs, cavalry, monks, and siege parts while fog-memory ghosts, shadows, buildings, resources, terrain, simulation state, saves, replays, and hit geometry stay static. The reusable engine owns only bounded injected-time harmonic playback: 8,192 active slots per snapshot, 16,384 total slots per active batch, at most 64 partial upload ranges, conservative affine-safe bounds, and animation metrics. Fixed paused tick-zero 800x600 phase evidence changes 300/480,000 pixels (0.0625%) at 54 animated instances, two animated batches, eight draw calls, and unchanged accepted/presented revision 2. Adversarial re-review closed matrix, upload, bounds, and workload findings; engine `f85f10e` and AoE v0.1.149 were published engine-first. Details: `docs/devlog/detailed/2026-07-11_2026-07-12.md`.
## 2026-07-12 (voxel visual quality, v0.1.148)
- **The experimental voxel battlefield now reads as an RTS settlement:** original procedural Town Centers, houses, farms, military/economic buildings, fortifications, role-specific equipped units, clustered resources, terrain props, neutral materials with faction accents, contact shadows, warm tracked daylight, antialiasing, and smooth sampling replace the generic block look. AoE keeps every game recipe; the reusable engine gains only the configurable daylight rig at published commit `dd9b811`. Fixed tick-zero 800x600 evidence changes 71,616/480,000 pixels (14.92%) and remains bounded at 1,000 instances, 4 batches, 8 draw calls, and 16,632 triangles. Final gates: engine 74/74; AoE 2,062 unit tests plus 2 skips; 100 production-browser tests plus 2 intentional visual-baseline skips; typecheck, zero-warning lint, and 455-module build green. Live-code re-review found no substantive issue. Engine `dd9b811` and AoE v0.1.148 `5a42649` were published engine-first. Details: `docs/devlog/detailed/2026-07-11_2026-07-12.md`.
## 2026-07-12 (isometric voxel renderer opt-in slice, v0.1.147)
- **Real reusable 3-D path, safely reversible:** `?renderer=voxel` now renders lit chunk terrain plus deterministic block units/buildings/resources through the sibling `voxel` package and Three.js, behind Phaser's transparent input/fog/selection/health/feedback overlay. Phaser remains the default because elevation-aware hit geometry and the full voxel parity matrix are still open. World/epoch/revision identity, replay rebuilds, real WebGL context loss/restoration, explicit capture with `preserveDrawingBuffer:false`, one Three runtime, minimap pixels, and a 5/5 focused browser spec are proven. Controlled 800x600/DPR1/tick0 evidence changed 343,580/480,000 pixels (71.58%) while preserving the HUD/minimap. Final gates passed 2,046 unit tests and 100 production-browser tests (two intentional skips in each surface), zero-warning lint/build, and zero-vulnerability audits. Shared engine commit: `7fbae42`; AoE implementation commit: `a22fe8d`; AoE CI pins the full engine hash. Details: `docs/devlog/detailed/2026-07-11_2026-07-12.md`.
## 2026-07-11 (perf — terrain texture bake, v0.1.146)
- **Render ~199ms → ~5ms/frame (~37×)** the terrain was ~24,283 Phaser Graphics fills re-submitted to the GPU EVERY frame (a Graphics re-renders its whole command list per frame even when unchanged, so the v0.1.131 redraw-skip cache didn't help). Now baked to a `RenderTexture` once → 1 textured-quad draw/frame. Measured via a throttle-independent per-render-CPU probe (RAF FPS is headless-throttled). Trade-off: baked terrain samples slightly blocky at max 2.4× zoom (units/buildings crisp; default view identical). `sceneRenderer.ts` only; sim unchanged. Lesson in lessons.md.
## 2026-07-11 (full-review iterations 4–5 — independent verification of the backlog sweep; CONVERGED; no bump)
- **H9 security bypass (both CLIs + driver repros; 3 vectors over 2 iterations)** the `applyAndGate` sensitive-path guard was completely bypassable. iter-4 closed CRLF headers (trailing `\r` defeated every `$`-anchored pattern) + case-insensitive FS (`Vite.config.ts`→real `vite.config.ts`) via case-insensitive patterns + path normalization. iter-5 found a THIRD vector — a manually c-quoted `--- "a/package.json"` that `git apply` unquotes + writes while the `a/`-anchored regex returns `[]` — and hardened it ROBUSTLY (77eda1f) by deriving the touched set from git's OWN parser (`git apply --numstat -z`) unioned with the regex; `parseNumstatPaths` + tests. Proves hand-rolled diff parsing keeps drifting from git; ask git directly. iter-5 also confirmed A/B/C/H6 + all splits correct + green and the deferrals reasonable → CONVERGED. Synthesis: `docs/threads/done/full/2026-07-11/{4,5}/REVIEW.md`.
- **Loop-honesty A/B/C (Codex; verifier-confirmed)** M13/M6 gated the rerun/prove side but left the initial run + tick accounting ungated: (A) `qualifyInitialRun` verify+horizon gate so a degenerate run isn't a clean `no-fix-candidate`; (B) `selectLedgerFixCandidate` now requires `verificationStatus==='verified'` so an unverified finding can't be auto-proven; (C) LLM runner credits the OBSERVED tick delta and detects a completed match (`getMatchOutcome()` → `stopWhen`) instead of mislabeling it `engineHalt`.
- **H6 finding-loss + fileSizeBudget** `withinRunUnionKey` keys conformance findings on per-finding id so two distinct same-`[category,area]` findings both survive the union; and the M14a-induced RED gate (`saveLoad.test.ts` 519>500, committed d692ce5) was fixed by extracting `captureSnapshot`→`saveBlobTestUtils` (476) and the loop fixtures→`selfImprovementLoopTestKit` (387). ARCHITECTURE.md monk-doc corrected (L4). Acknowledged: fog raw-id aliasing + corpusSchema key-permissiveness (bounded/pre-existing). Full synthesis in `docs/threads/done/full/2026-07-11/4/REVIEW.md`.

## 2026-07-11 (full-review deferred backlog batch 1, v0.1.140–141)
- **L5 (v0.1.140)** same-tick Wonder tie now prefers the human (was a strict `<` that handed the human a defeat when the enemy Wonder was inserted first); **M12** reselect a surviving TC on destroy (was left reference-less → AI production/targeting broke); **L1** garrison hydrate validator rejects duplicate unit ids (corrupt-save double-count guard, both paths); **L4** removed dead `assignAiMonkTasks` (KAD-0008 re-wire trap) + corrected the `buildPendingIntentionMaps` empty-queue comment. New fixtures `two-wonder-tie-fixture` / `town-center-reselect-fixture` (in `fixtures/fullReview.ts`).
- **F16 (v0.1.141, render)** building health bars no longer slice through a pitched roof's ridge accent (banner/cross/turret/blades). `computeHealthBarLayout` now clears `pitchedApexPx(w) + 0.6·min(w,100)` above the eave for completed pitched-roof roles (derived from the accent painter's own constants); flat-roof roles + construction scaffolds keep the old margin. Clearance proven conservative vs `drawBuildingRoofAccent`.
- **M1 (debug-only)** the four F-key world overlays (selection-bounds/pathing/fog-state/coarse-vs-fine) now project through `worldToIso` (iso diamonds + projected cell centres) instead of the pre-iso `cell*cellSize` square grid, so the introspection tools paint where the world actually is. New `debugOverlay.test.ts`; no bump (dev tooling).
- **M9 (v0.1.142, replay HUD)** the replay minimap now repaints when you switch fog perspective while paused (redraw key folds in `frame.playerId`, not just tick+camera; pure `minimapContentSignature`/`minimapCameraSignature` helpers in `minimap.ts`). Was showing the prior owner's visibility until a tick/camera change.
- **M2 (teardown)** `createApp`'s destroy handler now calls `hudController.destroy()` (was leaking the render-loop RAF + window listeners on scene destroy — test-isolation/HMR), and the minimap `mousedown`/`mousemove` listeners are now removable. New `hudControllerTeardown.test.ts`; no bump (no user-visible behavior).
- **id-recycle interpolation (no bump)** the per-unit interpolation + facing caches now key by `id:generation` (new `renderIdentityKey`; optional `ProjectedEntityView.generation` set at `visibility.projectEntity`), matching `RenderStore`. Was: a recycled entity id could make a new unit slide/face from the destroyed unit's last position for one frame.
- **M5 (v0.1.143, fog-of-war)** fog-memory cleanup is now generation-aware (`MemoryEntry.generation` + `world.isCurrent` replaces a raw `getComponent(id,'position')` probe), so a destroyed-under-fog building whose id was recycled no longer ghosts forever on your map / through save-load. Additive-optional save field (no schema bump; hydrate self-heals). Spec §12.1. TDD-verified red→green. High-risk save touch → queued for iter-4 multi-CLI.
- **H6 (harness)** `extractImprovementFindingsFromRun` now UNIONs marker + envelope + oracle findings (dedup by `findingIdentityKey`) instead of returning the first non-empty source, so a low-sev marker no longer hides HIGH oracle findings from the ledger + recursive fix-candidate selection. `source` gains `'mixed'`; markdown formatter split to `selfImprovementLedgerFormat.ts` (500-LOC cap). No bump (loop honesty).
- **M3 (v0.1.144, control)** an explicit human command (move/attack/gather/context) now evicts the unit's stale auto-aggression intention (`removePendingUnitCommands`), so a unit ordered to retreat near an enemy no longer loses a tick to an auto-attack that clobbers the move. Determinism-safe (bridge-queue eviction before the drain). Spec §13.3. TDD-verified red→green.
- **M14 + L10 (test guardrails, no bump)** M14a: new bridge save/load OBSERVABLE-equivalence test (`getEconomyState` original-cache-read vs loaded-blob-read) guards the cache-flush-miss class that let H1 through (codec-level snapshotEquivalence is blind to it). M14b: `fileSizeBudget` now covers `scripts/` `.mjs` (2 violators legacy-listed; lint-on-scripts deferred — 300+ pre-existing errors). L10 acknowledged: `applyAndGate`'s tree clobber is already bounded by the `enforceCleanWorktree` precondition.
- **H9 (harness/security, no bump)** `applyAndGate` now refuses a model-authored patch that touches gate-executing / dependency-surface files (package.json, lockfiles, `.npmrc`, CI workflows, git hooks, vite/vitest/eslint/tsconfig* etc.) BEFORE apply — so the recursive loop can't run injected npm lifecycle scripts / altered build config before human review. `allowSensitivePaths` opt-out for supervised callers. New `patchTouchedPaths`/`patchTouchesSensitivePaths` + 5 tests.
- **M10 (v0.1.145, selection)** clicking a building's raised roof/walls now selects the building (front-most on overlap) via its drawn iso silhouette, instead of the empty cell behind it; units under the click still win. Reuses `buildingSilhouettePolygon`/`pointInPolygon`/`depthKey` (new `findFrontmostBuildingAtIsoPoint`); render-layer only. Spec §render.
- **H5 (CONFIRMED, reported — no aoe2 change)** `movePathCache` route divergence at replay snapshot boundaries is real but narrow (transient, same-destination, intermediate-cell-only); root cause is civ-engine's start-dependent A* tie-break. Correct fix = position-stable A* tie-break in civ-engine (forbidden to edit here); aoe2-side mitigations partial+risky. Recorded in `docs/engine-feedback/current.md`.
- **M13/M6 (harness, no bump)** recursive-loop completion honesty: budget death reports as distinct `costBudget` (not laundered into `stopWhen`) so match-completes fires on it (#7/#1); `selectLedgerFixCandidate` excludes non-code-fixable oracles (match-completes/no-perf-regression) from auto-fix (#5); prove-fixed now requires the rerun to be replay-verified AND reach a genuine horizon, not just finding-absence (#6); silent no-op `advanceTicks` → `engineHalt` instead of climbing to maxTicks (#2); conformance finding identity keys on stable category+area not tick-embedded id (#3); dead economy thresholds removed (#4). Full playtest suite green. High-risk agent-loop → queued for iter-4 multi-CLI.
- **LOW-tier (no bump)** L3: AI builder fallback no longer yanks a foundation's sole builder (fallback skips `type:'build'` villagers; new `aiBuilderFallback.test.ts`). L7: `resetForBridgeSwap` deliberately preserves `hasCenteredOnBase` (replay fog-owner swaps must not move the camera) — documented. L9/L2/L6 acknowledged no-fix (benign self-heal / defensive-only / sub-ms perf not worth cache-invalidation risk).
- **M11 (perf, no bump)** `towerCombatSystem` no longer runs full target acquisition every tick while reloading — hoisted the cooldown `continue` above the (side-effect-free) garrison/tech/target-scan work it used to discard. Behaviour-identical. Wildlife scan deferred (safe only with sorted side-map keys + a replay backstop — the loop is order-dependent); villager scan largely refuted (sort not hoistable → constant-factor only, static snapshot would diverge).

## 2026-07-10 (full-codebase review iter-1 — fixes shipping)
- Synthesis + full finding list: `docs/threads/current/full/2026-07-10/1/REVIEW.md` (5 CLI reviewers — Codex `gpt-5.6-sol` ×2 + Claude `opus[1m]` ×3, Fable quota exhausted → Opus fallback — + 4 adversarial verifiers; Codex-1 over-rated ~half its findings, one refuted).
- **H1 (v0.1.134) wildlife save persistence:** wildlife HP/death marked only `combatStatesCodec` dirty, never `wildlifeStatesCodec` → hunted boar reverted to full HP on load, killed corpse boar resurrected. Fix: `markDirty(wildlifeStatesCodec)` at both sites; `boar-hunt-fixture` + `wildlifePersistence.test.ts`. Damage case masked by the boar's own retaliation; kill case unmasked. `snapshotEquivalence` reads live off `world.state` so it can't catch cache-only misses (M14).
- **H2 (v0.1.135) no save during replay:** the HUD saveGame closure resolves the replay bridge, so Save clobbered the live `aoe2-save-v1`. Fix: `saveLoadPanel` refuses in replay (toast) + Save button disabled on mode change.
- **H3 (v0.1.136) IndexedDB flush requeue:** `_flushNow` detached `_pending` before the tx and never restored it on error/abort → dropped recording batch. Fix: `_requeuePending` prepends the failed batch; idempotent retry.
- **H4 (v0.1.137) crash-recovery replay endTick:** a never-closed session's `session_meta` froze at `endTick==startTick`, disabling Replay for the whole recorded session. Fix: `reconstructBundle` recomputes endTick/persistedEndTick/durationTicks from persisted data when `!closed`.
- **M7 (v0.1.138) marker-list click by id:** live-mode row click resolved the marker by positional `data-marker-index` into a re-sorted, re-allocated `recording.markers()`; a marker added since render shifted every index → wrong marker. Now resolves by `row.dataset.markerId` `.find(m=>m.id===id)`, mirroring the replay branch.
- **M8 (v0.1.139) minimap viewport quad:** `getCameraState` returned the cell-space AABB of the 4 view corners (`viewCellMin/Max`); `minimap` re-projected the box → an oversized (~2×) circumscribing diamond. Now exposes the 4 real corners (`viewCorners`, polygon order) and projects them directly. Unit test asserts a skewed quad projects to the true corners, not the AABB.
- **H8 (harness, no bump) validate playtest CLI numbers:** `playtest-llm.mjs` numeric flags were bare `Number(...)`; `Number('nope')`=NaN silently disabled the budget/tick guards. New `requireFinite` exits 2 on non-finite/out-of-range input.

## 2026-07-10 (graphics batch v0.1.129–133)
- **Render/UI-only AoE2 polish, each adversarial-review-vetted:** unit death feedback (fog-gated death feed, v0.1.129); isometric DIAMOND minimap (v0.1.130); terrain render cache (~6× cheaper/frame — draw once, repaint on `computeTerrainSignature` change, v0.1.131); pitched ridged-hip building roofs with ridge-anchored accents (v0.1.132); white occlusion silhouette for units behind buildings (`occlusionSilhouettes.ts`, `depthKey` shared with the draw sort, v0.1.133). All preserve simulation/saves/replays/hit-testing/fog/camera. Spec §14.3/§14.5.

## 2026-07-09/10 (recursive loop + process)
- **pinned-units candidate fixed-proven (v0.1.128):** 16 `no-pinned-or-oscillating-units` canary violations → 0, proven under a strong replay self-check (14 oracle misfires rebuilt as `pinnedUnitsOracle.ts`; 2 real). Adversarial rounds hardened the verdict (gathering-suffix exemption, wide-box/pocket-orbit livelock, `applyWanderKick` patrol). Spec §13.3.
- **oracle canary drill (v0.1.127):** `playtest:canary` seeds one-line bugs on throwaway branches and asserts the declared oracle fires (canary-ok/blind/stale/invalid → passes.jsonl; non-ok exits 1). LLM-free.
- **file-size legacy splits:** GameScene/aiSystem/IndexedDBMirror/wireBridgeOps split into 13 role modules (all ≤450); `LEGACY_VIOLATIONS` is now empty (the oracle test split too — one-way ratchet). No behavior change.
- **Process/tooling:** model policy = most-advanced everywhere (Fable ban lifted, §15.7); multi-CLI review runbook fleet-canonical in `loop-ops/docs/skills/multi-cli-review.md` (single pin-bump site; `-o` codex extraction); AGENTS.md restructured (Session-start reads, real file-size gate). Docs/tooling only.
