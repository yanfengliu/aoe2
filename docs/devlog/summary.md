## 2026-08-25 (combat team bonuses + the attack-step extraction, v0.3.82)
- 2026-09-05 v0.3.207 THE COMMAND BAR IS ONE FIXED BAND. Owner: "the info panel at the bottom should not have different heights for different units". The bar was content-sized and bottom-anchored (72-242.8px at 1280x720, 65-232 at 800x600); now one token, 246px above 1120px / 236px below — the tallest boot card plus 2px, which is the TOWN CENTRE's (two-line name beside its portrait, four chip rows), not the villager's the first pass assumed (185.6; the 208 in the 09-02 header was stale). Found in passing and fixed: the compact multi-select grid never wrapped (auto-fill in an auto column, 269px tall). Gate selection-panel-height.spec.ts, 17 states x 4 viewports, red 4/4 by mutation; queue depth 3 above 1120px, 1 at 1024x768, 0 at 800x600. Diffs confined to the bar plus the two floating controls anchored to it. Open: the bar's WIDTH still follows its content (empty state is a 330x246 slab at 1280x800) — owner's call.
- 2026-09-05 v0.3.206 UNITS SWING THEIR LEGS AGAIN. Owner: "soldiers are not moving their legs". The gait weight was speed / 2.5 tiles/s, the pre-§12.4.2 clock; since v0.3.160 no unit reaches 2.5, so every walker's gait played at 0.2-0.6 of the authored amplitude (villager 0.32, militia 0.36, knight 0.54 at base rate; 0.19-0.51 read live). Now full gait at 0.75 x the unit's own base rate (unitStepCadence.unitBaseTilesPerSecond); pose amplitudes unchanged. Gate aoeVoxelUnitWalkLegs.test.ts over all 93 UnitTypes, red 158 on the old tree. Diffs: default view 0 px static, walkers only elsewhere. Orchestration: the coordinator delegated every step of this task to workers on the owner's directive (canon reworded, fleet 7927fc9 / aoe2 d880fce1).
- 2026-09-05 SIX CLOSED THREADS MOVED TO DONE, AND CLOSING BECAME A RULE WITH A GATE. The owner asked whether the project really had so many open threads; it had two. `animation-feedback-batch`, `auto-mine-after-camp` and `unit-occlusion-outline` shipped on 2026-07-15 (v0.2.5 to v0.2.9) and were never moved — `auto-mine-after-camp` was CREATED by a commit titled "close auto-mine"; `recursive-loop-closure` shipped 2026-07-08 and was dismantled by the 2026-08-01 decision; `moebius-art-style` shipped v0.3.10; `render-realism-2026-09-02` shipped the one improvement it was scoped to (v0.3.193). Each carries a dated closure line now. `lumber-camp-routing` stays open and got the round-6 status its record lacked (v0.3.191, v0.3.204 and v0.3.205 landed against it with nothing written there); `concurrency-review-2026-09-02` stays open on its own status block. Four pointers were stale BEFORE any move (`findingsToMarkers.ts`, `replay-scrub.spec.ts`, two drift-log rows) plus the two the moves created (decisions.md, spec §14.5) and the changelog's seven "see" pointers to long-moved threads — one of the stale ones was written six days AFTER the 2026-06-09 housekeeping review by the very commit that moved its thread. The rule, in `docs/policies/local-rules.md` and the AGENTS.md conventions: a thread closes in the commit that ships or drops it, and every task ends with a look at `current/`. The gate, `tests/architecture/threadHygiene.test.ts`, three checks: every thread path referenced from code, spec, architecture, policy and learning docs must exist with exact casing; no `Closed YYYY-MM-DD:` line may sit under `current/`; every current thread must carry a date within 30 days of HEAD. Run as a matrix over the six: four red on staleness, two inside the 30-day tail, all six red once they carry the closure line (gate-proofs.md). A critic's four blocking findings shaped it — the closure line itself had refreshed the staleness date. Moebius's two never-run follow-ups (Codex retry, GLSL-versus-TS gate) dropped from this repo with reasons: both bodies live in `voxel` since v0.3.196. Register entry 2026-09-05. No version bump.
- 2026-09-05 THE HARNESS-FIRST RULE BECAME A GATE. The owner asked why the engine debug harness was not used for the deposit-leg work; the honest answer was awareness without use — the rule was in a memory loaded every session and was not in AGENTS.md at all (`git log -S` finds it never was). Now it is: AGENTS.md "Debugging" names `replay:inspect`, `diffBundles`/`forkAt` and the path-queue/occupancy probes and requires a `// harness:` line on every `tmp/probes/` file; the debugging template asks the question first; and a PreToolUse hook (`scripts/hooks/probe-harness-check.mjs` via `.claude/settings.json`) refuses to write or run a probe without the line. It went red on the first command after install, against that morning's own probe.
- 2026-09-05 v0.3.205 THE DEPOSIT LEG DESCENDS THE WALK FIELD, merged from the peer session's `deposit-walk-field` branch on the owner's decision, with the two boot-map gates RE-BOUND to the match's resolution and variety moved to a new NO-ATTACK COVERAGE LAB. Measured before merging, because the peer had flagged it unmeasured: owner 2's collapse on the boot map is a raid (twenty villagers dead in 700 ticks at its own base from tick 22,896, lumber camp standing until 25,000, three military against thirteen), not a deposit defect; its `tier 13.4` is a camp-less economy hauling to its Town Centre afterwards. The boot-map instrument reads WORSE for owner 1 after it (`frozen` 0.4 → 0.7/sample, longest 1,430 → 4,201; chop 36% → 34%), recorded open under the head-on class; cost measured for the field's 917 rebuilds only (240 ms, 0.19% of step), the served path untimed. The lab (`disableAiAttacks` + high resources + hard AI, 45,000 ticks) puts both seats in the Imperial Age and reaches 15 building types, 18 unit types and 31 technologies against 11/6 on the conquest match and 13/13 with attacks off alone; `selfPlayContentCoverage.test.ts` holds those names as a floor and prints the gap to the content tables, and the castle gate keeps a named union floor (11/6) for the shipped configuration. `registerAllSystems.ts` and `wireBridgeOps.ts` now sit at exactly 500 lines. Seen, not fixed: the AI researches upgrades for units it never trains, and owner 2 holds three military while twenty villagers die.
- 2026-09-05 v0.3.204 THE GATHER COMPARATOR RANKS BY THE WALK, not by Manhattan distance — the register's 2026-09-01 entry, fixed by replacing the measurement rather than re-weighting it (seventeen re-weightings had failed). One multi-source breadth-first field per owner, kind and domain (`dropOffWalkField.ts`), cached on `structuralRevision` PLUS the drop-off set (a camp completing bumps nothing), supplies every candidate's HAUL; the ranking key and the home range both read it, unreachable is Infinity. Boot map at tick 6,000: owner 1's lumberjacks on a tree at walk 34 (Manhattan 5) with walk-2 and walk-4 trees untouched, 110 wood delivered -> 340. COST MEASURED, not believed: 290 rebuilds in 15,000 ticks at 0.25-0.42 ms = 0.3% of step time; whole-match ticks/s swings -34%..+27% by seed and tracks world size, so the instrument (`npm run ai:gather-walk`, with an independent component-only walk probe) reports ms per thousand unit-ticks beside it. Misranked targets 0-72% per seat -> 0-5% (black-forest o2 15%, a deposit-leg symptom) across all nine playable maps at 45,000 ticks. NOT claimed: per-seat wood delivered is mixed (seats interact, the AI moves villagers off wood), nomad gets worse (the open entry), and a correct ranking eats the near woodline sooner — the one-lumber-camp-ever item now costs more. GATES: `gatherWalkRanking.test.ts` (boot map, red on the pristine tree at haul 34 / 29 beyond), `dropOffWalkField.test.ts` (wall, Infinity, cache keys, open-ground equivalence), three comparator cases with both mutations proved red. Two near-misses in gate-proofs: a unit case green for the home filter's reason, an open-ground fixture that obstructed itself. THE CRITIC FOUND THE DEPOSIT LEG HAS THE SAME DEFECT: on nomad fourteen loaded carriers freeze on two cells for 21,000 ticks because `dropOffStep` aims at the first ring cell an A* reaches in MANHATTAN order — a 27-step loop for the carriers at (14,14), the 10-step route THROUGH (14,14) for those at (15,14) — and the instrument had counted the freeze as walking; it is an OPEN register entry, the instrument counts frozen villagers (a two-cell box, after a second critic found a per-cell clock blind to the shuffle), and the fix (the deposit leg descends the field, `descendFrom`, no per-carrier A*; nomad o1 chop 8% -> 37%, wood 981 -> 1,783, frozen 0.0) is on branch `deposit-walk-field`, NOT merged: it makes the boot map resolve by conquest at 39,812 (o1 Castle 21,250, peak army 15) and trips `aiReachesCastleAge` (winner variety 9/6 against bars 13/9 derived from o2 winning an unresolved match) and `villagersAvoidEnemyDefencesSelfPlay` (demands 40,000 censused ticks; its window ends at 39,191 and was covered with 0 deaths under defences) — re-deriving those contracts is the owner's call, not a bar to move for one's own candidate. LOCAL RULE added (fleet candidate): a feature's cost is its own share of tick time plus interleaved ticks/s on a quiet machine, never whole-match ticks/s — an orphaned headless Chrome burning twelve CPU-minutes per minute made identical baseline work read 366 and 268 ticks/s forty minutes apart.
- 2026-09-05 FOUND WHY `../civ-engine` HAD NO GIT METADATA, and it was this session: `git worktree remove --force` on a scratch worktree (2026-09-03 23:27) followed a COPIED `node_modules/civ-engine` junction — Git Bash `ln -s` copies a directory — and emptied `../civ-engine/.git` down to `refs/`; a 267-character path under `refs/codex/` aborted it, which is all that saved `refs/`. Nothing on `main` is lost (`e418a051` is on origin); the reflog, the local config and the July-10 branch `claude/festive-aryabhata-19b494` are. REPAIRED on the owner's go: `git init` in place, the stale loose refs removed (they made the fetch report `main` up to date and skip it), fetch, `git reset`; `git status` showed the one tracked casualty (`.cursor/hooks/state/continual-learning.json`, restored from HEAD) and the 2026-09-04 canon sync, committed as civ-engine `f123904`; fsck clean. Register entry written, the rule staged for the fleet, a local rule for A/B control builds.
- 2026-09-04 PERSISTED WHAT THE HANDOFF PROMPT HAD BEEN CARRYING, on the owner rule that a prompt holds the immediate task and nothing else. Two gaps found by auditing the prompt line by line against the repo. (1) The owner-reported defect "unit shadows do not rotate with the unit" was NEVER RECORDED — handed over at session start, never opened, and it existed only in prompt text; the canon says a reported defect is recorded whether or not it is fixed, so it is now an OPEN register entry that says plainly that no investigation was done and names the gate the class needs (a rendering test that turns a unit through several headings, because the defect is invisible from any single framing, which is how it survived two versions). (2) The standing definition of DONE — build to completion matching DE, feature-complete meaning rendering + tech + playability + bug-free + performance verified by AI SELF-PLAY, and the acceptance test "can we play this and feel like we are playing DE" — had arrived as prompt text every session and was in no file; now a local rule, positioned as the tiebreak above the others. Everything else the prompt carried was already persisted and was cut: session-start reading and the quota note are in AGENTS.md and the canon, the ai:army-block traps are in that script header, the map-generation control and HOTKEY are local rules, and the five other open items are register entries.
- 2026-09-04 ROUTED THIS SESSION'S LEARNINGS OUT OF THE HANDOFF PROMPT AND INTO THE REPO, on the owner's instruction that a prompt is not where durable knowledge lives and that the generic ones belong fleet-wide. GATED, not queued: `world.isAlive(id)` takes a bare id and the engine recycles ids from a free list, so a dead entity whose id was reused reads back as alive WITH the same components — now a `no-restricted-syntax` lint rule over `tests/**`, red-proved by restoring the exact unsound check a critic found in `aiGarrisonedDefender.test.ts`, and the one pre-existing test use (`rightClickNeverGarrisons.test.ts`, asserting "still on the field" in a way a recycled id satisfies) rewritten to take a ref before the step. Production's 27 call sites are deliberately NOT restricted — they take the id from a live query and ask in the same tick. QUEUED in lessons.md naming its gate: a browser spec within a few seconds of the 30s timeout is already red on somebody's machine (two found today, both passing beforehand — one at 29.8s). LOCAL RULES: a map-generation change is measured against the maps it CANNOT reach, which must come back byte-identical — that control is what made three different woodline orderings comparable to each other; plus `HOTKEY` on the capture script, the only way a HUD panel reaches a maintained capture. FLEET: two candidates STAGED in `../fleet/canon-candidates.md` (new file) rather than written into FLEET.md — a return value is a claim exactly like an exit status (an `indexOf` miss fed to `slice` deleted 319 lines of a tracked spec), and an A/B comparison needs the tree to hold still (source edits between the arms reported a boot-map regression that did not exist). Staged because the drift check covers all 24 repos, so editing the constitution turns fleet's `npm test` red until every one syncs, and three siblings have uncommitted work in them. AGENTS.md now names lessons.md in Session start, which the canon assumes and this repo did not say.
- 2026-09-04 v0.3.203 NEITHER WALLED MAP ENCLOSED A TREE, which is priority 4's root cause. `arena` owner 2 never leaving the Dark Age is a DEATH SPIRAL, not a stall: seven villagers by t4,000, the town bell at t5,000 (owner 1 raiding), and from t7,000 to t16,000 ZERO units on the map with three villagers hiding inside while its wall went 34 buildings to 26. The raid is not the cause either — arena's ring holds a TC, 2 sheep, a boar, 6 berries, gold and stone and NO TREES (fortress: 4 gold, 4 stone, a Castle, NO TREES), so a walled player must leave the wall in the first minutes for the wood every house and every age-up prerequisite costs, and that walk is where the villagers died. `paintWoodlines` keeps patches 14 cells clear of every start so they cannot hole the ring, and nothing put wood back inside. FIX: `paintEnclosedWoodline` paints one inside each wall BEFORE the wall goes up (so no segment lands on a tree) and refuses any cell that would cut the enclosure in two — trees are impassable, checked by flooding rather than by a margin, which caught the first placement stranding two cells. THE ORDER THE CELLS ARE TAKEN IN DECIDES EVERYTHING and my intuition was the worst of three, measured on six seeds: NEAREST the TC (shortest carry) put BOTH arena owners in the Dark Age popCapped 98% at 10/10 holding 1,000 unspendable food — it spends the only ground a walled base can build on; FARTHEST grown from a seed fans a blob inward and fragments what is left (o1 army 35->16, stopped resolving); ROW-MAJOR BAND at one edge is what works — o2 DARK->FEUDAL with 786 food and cap 25 and 82% unblocked, o1 army 35->39 and cap 70->80. Halls in the middle, trees at the edge, which is what a real base looks like. INSTRUMENT CHECK: every map WITHOUT a wall is byte-identical on every column of both owners. Gate `walledMapEconomy.test.ts` (food/wood/gold/stone inside + the enclosure stays one place), red-checked at `trees inside: 0` on all four seats. Still open: fortress does not resolve either way and its two owners merely swapped which is wiped out.
- 2026-09-04 v0.3.202 THE CRITIC WAS RIGHT ABOUT SIX THINGS and the corrections are worth more than the code. (1) THE CLASS WAS NOT CLOSED: `shouldPush` gates EVERY fallback in the attack phase including v0.3.197's last-resort branch, so an army one unit under the Castle threshold idled at its own base against an opponent with zero units and two buildings — the critic reproduced the exact tableau I had blamed on the garrisoned villager, with the villagers NOT garrisoned. Fixed by exempting an enemy with no unit on the map (`ownerHasUnitOnMap`), gated by a second fixture arm that is BROKE on purpose (four attackers, zero resources — with resources they trained back over the threshold, four became eleven, and the case never arose) and measures the MARCH not the win (four Men-at-Arms lose to a Town Centre; closest approach 1 cell with the fix, 21 without, attackers spawned at their OWN base so the question is answerable). (2) THE RENAME DID NOT CLOSE THE CLASS EITHER: `ownedMilitaryUnitIds` (four lines from the fix) and `findAvailableVillagerForBuild` hand-roll the same position-blind query; the military one is SPLIT not filtered because its two callers ask different questions. (3) MY GATE'S "still alive" CHECK WAS UNSOUND: `isAlive` takes a bare id, the engine recycles ids from a free list, and the critic reproduced `alive: false,true` in my own fixture for a killed villager whose id was reused — now an `EntityRef` + `isCurrent`, plus population as an independent reading. (4) THE 151-IDLE-UNITS SYMPTOM I published came from the RADIUS-ONLY arm, not the baseline: the true baseline resolves gold-rush at 34,704 with an army of 17, so that tableau never existed in a shipped build. (5) FORTRESS 1,862->8,829 WOOD is not a carry measurement — that slot ends the baseline at pop 0/40 with zero villagers, so the 4.7x is dominated by it surviving; pulled from the changelog. (6) "finish with zero camps" was wrong: fortress o2 BUILDS one at t7,800 and loses it at t11,600, and at radius 12 that camp stands 2 cells from its TC and survives — so the zero-camp ending is caused by MY change; the Dark-Age gate is why it is never rebuilt. ALSO: my own first attempt at the spec edit TRUNCATED 319 lines off `design/spec-final.md` — a `\n\n` paragraph search against a CRLF file returned -1 and `slice(-1)` kept one character; restored from HEAD and redone CRLF-aware. RE-MEASURED CLEANLY, six seeds, both arms back to back with the source verified unchanged between them: `aoe2-prototype` and `arabia` are BYTE-IDENTICAL across radius 12 and 20 on every column of both owners, which answers §13.2's standing objection that a camp-siting change must not lose on the boot map. Resolution 2 of 6 -> 3 of 6 on the radius alone.
- 2026-09-04 v0.3.201 THE CIVILIZATIONS PANEL the owner asked for (§11.14, spec written this task): all thirty side by side — army type, unique unit, team bonus per row — opened from its own menu button or F4, a row expanding in place for expansion / unique techs / full bonus list, the player's own civ marked, and a search that reads the BONUS TEXT because "how do they differ" is asked as "which of them do X". It never promises what the game cannot deliver: a unique unit or tech the simulation does not implement is struck through and its civ marked PARTIAL (18 civs complete, 12 partial — the runtime tables have no unique units at all for the twelve expansion civs). Model joins two sources by DISPLAY NAME — `civProfiles` (the CSV's design text) and `uniqueUnits`/`uniqueTechnologies` (what the sim has) — which is the only thing they share, so `civCompendiumModel.test.ts` pins the join in BOTH directions: a formatter change would otherwise mark every unit dead and the panel would still render. LOOKED AT IT, five framings via the maintained capture script (1280x720, 1600x900, 800x600, expanded, filtered) and TWO defects came from looking rather than from a test: at 800x600 stacking every cell put FOUR civs on screen, which defeats a panel you open to compare thirty (now folds to two lines, 8-9 visible), and an expanded row's detail floated between its own row and the next with no tint, so which civ it belonged to was a guess. `scripts/captureMapScreenshot.mjs` gained HOTKEY="F4" for this — a HUD panel could not be captured by the maintained script before, and the freshness and port checks are the whole reason a capture can be trusted. `F4,Tab,Tab,Enter` also proved the keyboard path. The file-size gate caught the wiring: `createApp.ts` went to 506 lines, so both look-it-up panels moved to `referencePanels.ts` (491 now).
- 2026-09-04 v0.3.200 TWO DEFECTS, BOTH PRIORITY-LIST HEADS, both found by MEASURING rather than reasoning. (1) WOOD INCOME: the villager split was never the problem — arena o1 has 19.6 villagers assigned to wood and only 3.01 CHOPPING; the other 16.6 walk, because the tree each one picks sits 15-16 cells from the nearest drop-off ALL MATCH and the AI holds one lumber camp. Cause is two constants in different files that cannot both hold: `paintWoodlines` keeps every woodline >= MIN_START_GAP = 14 EUCLIDEAN from every start (so a patch cannot hole Arena's ring) while `DROP_OFF_ANCHOR_RADIUS` was 12 MANHATTAN, where the same gap is up to 20 — so arena/fortress/gold-rush have ZERO trees within reach of either start, the anchor returned null at both, and the camp went up beside the Town Centre. Radius 12->20: chopping 3.01->5.43, wood gained 2,964->4,076 (fortress o1 1,862->8,829), nearest tree to a drop-off 15->4. INSTRUMENT CHECK that makes the attribution safe: coastal (nearest tree 5-7) is BYTE-IDENTICAL across both arms, which is what the reasoning predicts. (2) THE ARMY THAT NEVER ATTACKS: gold-rush o1 holds 151 units, ALL IDLE from tick 30,000, 45 cells from o2's 14 buildings; o2 has no unit on the map. The attack phase prefers the enemy's villager and `findOwnedUnit` returned a GARRISONED one — alive, no position — and `setUnitAttackCommandDirect` returns false without a target position, a SILENT no-op the validator never sees, so all 136 orders were dropped and re-issued every decision tick and the branch continued before the visible-building and Town-Centre fallbacks that would have worked. TWO HYPOTHESES DIED FIRST, both refuted by measurement: the army sealed in by its own 58 buildings (a knight walked 49 cells to distance 2 of that very TC, and all 14 o2 buildings were reachable) and the no-approach-plan clear in `attackCommandStep` (instrumented: ZERO clears). Fix is `findOwnedUnitOnMap` querying ('unit','position'). RESOLUTION across four seeds goes 1-in-4 -> 3-IN-4; the four-arm attribution table is in the register, and it shows the radius change ALONE makes resolution worse (0 of 4) — which seeds resolve is a knife-edge, how many is the number worth reading. Also fixed the suite's one FLAKY spec (trade-route-reach, ~1 in 6 against an identical build): two races, a step needed before the click and after it, neither sufficient alone (1-in-6 and 2-in-8), together 30 of 30. Also repaired a peer's unfinished `civProfiles.ts`, which did not parse and was failing typecheck for the whole repo.
- 2026-09-03 THE BUILD ORDER STALLS ON THE FIRST THING IT CANNOT AFFORD — priority 2's blocker, proven, fix measured and REVERTED. `pickNextBuild` returns the FIRST missing entry, so one permanently-unaffordable entry starves everything behind it: `arena` owner 1 sits in the CASTLE age at 45,000 ticks with NO STABLE and NO MARKET (both in the FEUDAL part of the order), holding 13-69 wood and 1,601 stone, having asked for that 175-wood Stable and failed for the entire match — which is why NO seed has ever built a Siege Workshop, and an AI with no siege cannot break a walled base (`fortress` o2 ends with 63 units unable to finish an opponent with ZERO units behind 27 stone walls). CAUSE: nothing arbitrates wood between military and construction past Feudal — `agePrerequisiteWoodReserve` returns 0 outside `feudal-age` — so military spends it in 25-wood bites as fast as it arrives and a 175 lump never forms. Visible only NOW because v0.3.199 made the AI actually train units, which is what started draining the wood. THE FIX WORKS COMPLETELY (hand the building phase's unaffordable wish to the production phase, reserve its wood): arena builds stable+market+monastery+SIEGE-WORKSHOP+castle, the first siege workshop on any seed, and reaches Imperial. REVERTED anyway: army across slots 136->81 (arena 95->37, coastal 21->14, fortress 3->7, gold-rush 17->23), no seed newly resolves, and the only one that does resolves 11,805 ticks LATER. Tuning cannot rescue it — the floor gates SMALL armies so it is on all match whatever the value, and a partial reserve is drained from above so the lump never forms; the trade is intrinsic to arbitration. Points at wood INCOME instead. ALSO CORRECTS the hoarding entry: arena's 1,601 stone was not misallocated, it was UNSPENDABLE behind three unaffordable build-order entries — with the order unblocked it fell to 251 buying the Castle.
- 2026-09-03 v0.3.199 THE AI REACHES THE IMPERIAL AGE AND NEVER TRAINS ANOTHER UNIT — priority 1's root cause, fixed. `getTrainOptions` returns `latestResearchedInChain` (the tier the owner RESEARCHED); `pickUnitMix` names the TOP of each line; the production loop matched them by EXACT NAME. Every Imperial mix entry is an upgrade-gated top tier, so an AI that aged up without those upgrades matched nothing at ANY building and stopped training permanently — and since reaching an age researches nothing, that is the DEFAULT path into Imperial, not an edge case. Proved by reading the offer rather than reasoning about it: fortress owner 1's barracks offers [long-swordsman, pikeman] against a mix wanting halberdier, and the same NOTHING at its archery range, stable, siege workshop and castle. FOUR HYPOTHESES DIED FIRST, all instrument failures: villager population share (freed 39 pop, army stayed 0 — and the rule was self-defeating, bounding villagers by a share of a cap those villagers produce), the age-up reserve (`ageUpReserveCost` returns {} in Imperial, where the probe's food<1060 column was being read — vacuous for exactly those owners), nothing-to-train-from (hasProducer 100%, idleProducer 100%), and resource starvation (measured, recorded, then refuted here: 1,231 wood and 595 food buys seventeen halberdiers). FIX resolves the wanted unit to the offered unit of the same LINE, derived by union-find over `UNIT_LINE_UPGRADES` rather than restated — that table's ram line already grew a middle tier (Battering->Capped->Siege) a hand-written list would have missed. MEASURED via `SEEDS=arena,fortress,gold-rush,coastal TICKS=60000 SAMPLE=500 npm run ai:army-block` (the probe was PROMOTED to `scripts/aiArmyBlock.mjs` for this: the first write-up cited an env-var invocation no tracked script read, so the headline evidence was not re-runnable — a critic caught it): fortress o2 army 0->3 and it annihilated owner 1; gold-rush victory 41,967->34,704, army 12->17; coastal 20->21; arena 105->95 — CAUSED BY THIS CHANGE, not noise, and my first write-up said the opposite: arena o1 ends with live spearman:24 and man-at-arms:4, which proves it never researched those upgrades, so the spearmen are the fix's own product where the barracks previously trained nothing. There is no `Math.random` in src/game/simulation and reruns are byte-identical, so 'divergence' was never available. It is a composition trade (anti-cavalry it could not train before) costing 10 army on one seed while three slots gain. An independent critic caught it. A SECOND GATE was found wrong and REPAIRED, not relaxed: aiFeudalStone went red at 10 mined vs 30, but nothing regressed — with an army owner 2 now wins at 6,561 instead of 11,442, so the window anchored at the 125-crossing spent 2,535 of its 3,000 ticks measuring a FINISHED match, the exact failure that file already warned about while keeping its liveness assertion AFTER the mining one where it could never fire. INSTRUMENTS: `selectEntityById` fills trainOptions only for the LOCAL player (the second owner's probe rows were stale and discarded), and a HALTED bridge reads exactly like an idle AI — a missing import turned a 2,295ms run into a 73ms run that still failed on 'expected 0', so the new gate asserts engineHalted is falsy.
- 2026-09-03 Priority 1 ("the AI never fields an army") ROOT CAUSE FOUND, unfixed, after THREE refuted hypotheses. The AI hoards what it cannot spend and starves on what gates production: fortress banks 1,478 stone and 1,231 wood with THIRTEEN gold; coastal o1 banks 8,545 food with TEN wood; coastal o2 banks 3,388 wood with 26 food. `UNAFFORDABLE` 53-99% of samples, `noProducer` 1%, military buildings 100% present and 100% IDLE. Cause is `aiEconomyPlan`'s fixed per-age villager split (castle `4/4/3/1`, imperial `4/3/5/1`) which never reads the stockpile, so it cannot move one villager off 8,545 unspent food onto the wood gating every unit. REFUTED ON THE WAY, all three by instrument failures rather than by bad reasoning: (1) villagers eating the army's population — implemented, A/B'd on four seeds, fortress o2 freed 39 population and its army stayed at ZERO, armies smaller on three seeds and never larger, and the rule is self-defeating because it bounds villagers by a share of a cap those villagers produce; reverted. (2) the age-up reserve — `ageUpReserveCost` returns `{}` with no next age and both fortress slots are IMPERIAL, so the `food<1060` column that made it look true is vacuous for exactly the owners it was read against. (3) nothing to train from — `hasProducer` 100%, four producer types built, idle in every sample. INSTRUMENT: `army-block.ts` reports peaks over SAMPLES, and at `SAMPLE=15000` it read arena's cap as 50 against the true 145 and its army as 21 against the true 105 — a collapse big enough to look like a catastrophic regression from the code under test; the control has to be re-run with identical `SAMPLE`/`TICKS`/`SEEDS`, never remembered. Unexplained and worth chasing: coastal o1 has 16 of 22 villagers `to-resource` and 3 `gathering` at tick 45,000, which is priority 3's signature and would make priorities 1, 2 and 3 one defect.
- 2026-09-03 v0.3.198 STONE WALLS READ AS WALLS (roadmap M7). TWO independent causes, both found by LOOKING rather than by reasoning about the code. (1) `wall-base` was 0.92 x 0.50 in a 1x1 tile, so a north-south run left HALF A TILE of bare grass at every joint and a line read as a row of filing cabinets; the base fills its tile now. (2) THE CRENELLATION WAS ALREADY THERE AND INVISIBLE — the detail pass added `stone-wall-cap`, a slab at y=1.03 spanning 0.94 x 0.56, laid straight over the merlons and fusing the crest into a flat lid. Deleted. The interesting constraint is that a wall segment is a per-entity recipe with NO KNOWLEDGE OF ITS NEIGHBOURS, so it cannot know which way its line runs, and the per-set merlon rows all run along X — right for an east-west wall, wrong for a north-south one. FIRST IDEA WAS WRONG: laying the set's row on both axes unions into a nearly solid lid with a grid of holes punched in it (captured, looked at, discarded). The teeth go on the four CORNERS instead, so along a run in either direction the two teeth either side of a shared edge meet flush and read as one tooth centred on the joint, with every tile's middle left open — a tooth-embrasure-tooth crest at any orientation, no neighbour knowledge needed. v0.3.154's per-civ crest identity survives through the tooth's PROPORTIONS rather than its layout. PALISADE: two attempts read WORSE than the original (stakes wide enough to touch, then stakes with pointed heads) — both turned the segment into a wooden crate with black slots where neighbours shaded each other. Stopped at two rather than guessing a third time, and went back to the diagnosis: a palisade SHOULD have gaps between its stakes, so the only real defect was the DEPTH (0.28 in a 1.0 tile). Shipped change is depth plus tint — `timberDark` against a backlit sun read as charcoal and five near-black posts looked burnt. Both failed attempts are recorded in the code comment so the next person does not repeat them. FOOTPRINT INVARIANT caught the first cut: `aoeVoxelBuildingDetail.test.ts` :: "keeps every completed building part inside its footprint" allows 0.02 of overhang and the first version overhung by 0.04, so segments now meet FLUSH at the tile edge rather than overlapping — safe because the two faces that meet point away from each other, so one is backface-culled and the other is buried in its neighbour. Face detail then had to PROTRUDE 0.015: set flush it is coplanar with the wall face and z-fights, set inside it is buried and reads as a dotted line rather than a course (both states were captured before the third was chosen). Split at the 500-LOC ceiling into `aoeVoxelBuildingContext.ts` (the context, `add`, `setDoor`) and `aoeVoxelWallRecipes.ts` (wall and gate — the two buildings that are a LINE rather than a place), which also breaks what would have been an import cycle. Verified on the real map at 2.4, default and 0.7 zoom plus the building showcase; arena's ring now encloses the base as one crenellated wall.
- 2026-09-03 v0.3.197 A DECIDED MATCH NEVER ENDS, because the AI can only aim at a Town Center. `gold-rush` gave the clean case: from tick 48,000 to 60,000, byte-identical at every 4,000-tick sample, owner 2 held pop 0/5, ZERO units, no Town Center (so it could never produce one again) and a blacksmith plus a house — while owner 1 stood at 130 population with an army of 90, building farms. TWO CORRECT RULES DEADLOCKING: `conquestOutcomeSystem` ends a match only when a player has no units AND NO BUILDINGS, which is right; and `pickAttackTarget` chooses the march target from `townCentersByOwner`, a map keyed on owners that still HAVE a Town Center — so razing one drops that owner out of the target list entirely, `targetOwner` goes null, and the survivors are attacked only if they wander into vision. The winner cannot satisfy the condition the loser is kept alive by. Fix: a fallback map of one surviving building per owner, used ONLY when no enemy Town Center exists anywhere; deterministic (owners sorted, ties to lowest entity id) and defaulting to empty so every other caller is untouched. The containment property is the reason to trust it — the fallback cannot engage while any enemy Town Center stands, so a match's course up to the moment the last one dies is unchanged. MEASURED four seeds at 60,000 before and after: `gold-rush` running-at-60,000 -> VICTORY at 41,967. The other three still run and the causes are DIFFERENT defects, which is why this is filed as partly fixed rather than fixed: `arena` and `coastal` owner 2 still HAS a Town Center so the fallback never engages (owner 1 has an army of 105 and will not finish a base inside arena's own stone wall ring — attack EXECUTION, not targeting), and on `fortress` neither side has an army to attack with (2 and 0 in the Imperial age — the villager-target-versus-cap chain). Gate: `aiAttacksAnyEnemy.test.ts` :: `an enemy with no Town Center is still a target`, four cases including that a Town Center still outranks a much closer bare building and that an ALLY without a Town Center is never targeted; red-checked by iterating an empty list instead of the fallback map (`expected null to be 2`). Its bound is named: it is a unit test on the TARGET CHOICE and proves nothing about the army arriving, which arena and coastal are live proof matters.
- 2026-09-03 v0.3.196 PAINTED REMOVED, Moebius only, on owner direction ("I only want the moebius art style. Remove the other style."). Took out the machinery that existed only because there were two: `ART_STYLES`/`ArtStyleId`/`artStyleById`/`nextArtStyleId`, the whole `artStylePreference.ts` module (a stored choice with one option is not a choice), the renderer's `artStyleId` option plus `artStyleId()`/`setArtStyle()`, the game-menu cycle row and its glyph, the HUD-controller and bootstrap wiring, and the capture script's `STYLE` env. `artStyles.ts` SURVIVES as the home of the tuning, because every constant in it records the frame measurement it was chosen against and that provenance is the reason not to inline the numbers at the call site — it now exports one `MOEBIUS_RESOLVE`. Its test was rewritten from "the roster cycles and wraps" to what still has content: the four measured DEPARTURES from `MOEBIUS_RESOLVE_PRESET` (normalEdgeScale 0, depthEdgeScale 1.2, saturation 1.4, brightness 1.12) survive, and the values deliberately INHERITED from the preset (bands, inkStrength) are still inherited rather than restated — so silently falling back to the preset fails, and so does freezing this repo against an upstream improvement. Also corrected a comment in `gameAudioController.ts` that cited the art-style setting as the precedent for "storage failure must never break the game": that setting no longer exists, so the cross-reference is now dated rather than pointing at nothing. The Painted BASELINE MEASUREMENTS stay in the tuning's provenance comment (L=90.1 and the rest) because they are what the Moebius numbers were chosen against — deleting them would leave the constants unexplained.
- 2026-09-03 v0.3.195 FOUR OF THE NINE PLAYABLE MAPS HAD NO WOOD, which is the root of both standing acceptance-test failures on them — "the AI never fields an army" and "matches do not resolve". Went looking for the register's army chain (`villagerCapForAge` 40 against a cap of 50) and could not find it: measured across three seeds, three of six owner-slots never reach Castle, two never reach Feudal, none reaches Imperial, and a player still in the Dark Age at 75 minutes is not bounded by a Castle-Age villager cap. The register's chain was the BEST slot on the BEST seed. Tracing `arena` per 4,000 ticks gave the real shape: wood 0 at EVERY sample from tick 4,000, gold climbing 90 -> 3,184, food 636 unspent, buildings frozen at one TC + one barracks + one house + 31 walls, population pinned 10/10 in 80% of samples. VERIFIED THE INSTRUMENT BEFORE THE CONCLUSION and it is what made the diagnosis right rather than merely plausible: the first probe said `trees alive 0`, and the same filter against controls said `tree x246` on aoe2-prototype and `tree x1718` on black-forest. Root cause: the "every forest cell carries a tree" pass lived INLINE in `defaultMap.ts`, so only maps built from that generator ran it — arena, coastal, fortress and gold-rush had ZERO tree spawns, and with 200 starting wood that buys one barracks and one house and then the economy is dead forever. `createFortressMap` even carried code to fell "a TREE on the wall line" that could never fire. Fix: `seedForestTrees` extracted to its own module and run by every generator, plus `paintWoodlines` for the three grass-world scripts, staying 14 cells clear of every start — measured from the scripts' own ring radii (Arena 7, Fortress 11) so a woodline cannot punch a hole in the wall that IS the script's identity. After: arena 346 trees, coastal 198, fortress 348, gold-rush 344. EFFECT on arena over 45,000 ticks: owner 1 went from never leaving the Dark Age with peak army 4, to Feudal at 10,250, Castle at 23,750, 10 building types, 8 unit types and **peak army 63** with zero villagers lost — against a previous ceiling of 11 across every slot on every seed. Control: aoe2-prototype's audit numbers are byte-identical before and after, so the change is confined to the maps that were broken. GATE: `mapRoster.test.ts` :: `every playable map supplies wood` asserts total tree wood above ten times the starting stock AND more than eight trees within 30 tiles of each start (wood only one player can walk to is the same defect wearing a map shape); red-checked by the defect itself, failing on exactly those four maps and passing on the others. Its bound is named: it counts spawns and straight-line distance, so a woodline walled off by terrain would still pass. WHY NO GATE CAUGHT IT: the roster test boots every map and runs fifty ticks, a horizon that ends long before 200 wood does, so it measured "does this map crash" and was read as "does this map work". ALSO: an independent critic broke the v0.3.194 shadows in three confirmed ways before release — a relax loop that could not terminate (`mergeStrips` will not fold across a genuine discontinuity and no tolerance changes that, so a caster with more irreducible runs than the budget spun forever; reproduced on a plain plinth-plus-two-blocks caster, ~1 in 300 fuzzed plausible recipes, a HARD FREEZE one recipe edit away), a min/max envelope that is not the union and invented shadow where the sun reaches the ground (watch tower 9.0% of its area, two solid wedges down its flanks), and a pad layer drawn at a BAND's top rather than a real part's, floating up to 0.147 world units (~14 px) above the surface it is painted on, on every pad caster, guarded by an assertion comparing against the identical expression production used. All three fixed and re-measured: hang returns 28 pieces in 2 ms, invented shadow 9.0% -> 0.0% on every building, pad gap 0.0000 everywhere. The critic also caught the strip budget silently deleting 24.6% of a watch tower's shadow area (fixed by measurement: 10 strips -> 24, 0.0% missing for four more triangles) and a claim in the module header that the shadow lane is "roughly a quarter" of frame instances when it is 64% on a dense scene. The non-shadow recipe digest is byte-identical across all four recordings of this slice, which is the control that says every one of these moved only the shadow surface.
- 2026-09-03 v0.3.194 SHADOWS TAKE THE SHAPE OF THEIR CASTER. Owner: "they need to roughly match the shape of the thing that created the shadow instead of always being a rectangular", plus the standing requirement that a shape change re-adjusts its shadow in future. The v0.3.193 caster was ONE box per entity, so every shadow was the same rectangle-derived hexagon. `aoeVoxelShadowShape.ts` now reads the solid parts as a vertical PROFILE (<=4 height bands, merged where consecutive slices share a footprint) and tiles the union of their swept footprints with triangles, in the sun's own frame: lower envelope of the near boundaries under the upper envelope of the far ones, sliced at every envelope breakpoint, merged while a straight run stays within 0.03 world units. Nothing in the shadow code names a recipe, part or entity type — that is the standing requirement, and the gate for it changes a caster's height and width and measures the reach. THREE THINGS THE FIRST VERSION GOT WRONG, all found by looking at pixels rather than by tests. (1) The hull turn test had its sign inverted, so `evaluateChain` returned the upper hull for the lower one and a plain box tiled as two slivers — the unit tests caught it only because I had written a coverage test that marches the sun ray independently. (2) The wedge geometry was a PRISM like the slab it replaced; two neighbouring pieces then stand their 0.036-tall side walls back to back along every shared edge, both blend, and the tiling draws itself as a mesh of dark seam lines across every shadow in the scene — visible at default zoom, worse the more pieces a silhouette has. Flat triangle, no walls, seams gone. (3) The overlap test in `resolveShadowLevels` used each piece's enclosing PARALLELOGRAM, which is half again a triangle's area, so casters that never shared a pixel were lifted apart: 0.084 world units of lift on a 6x6 woodline against 0.038 from the triangles. THE BIGGEST FINDING WAS NOT A BUG IN THE CODE: the correct silhouette made buildings look WORSE, because almost every building sits on a plinth wider than its walls and the sun is 52 degrees up, so the whole shadow lands inside the plinth and the ground-only model drew none of it (Town Center: 0.82 tiles reached past a 3.44 tile plinth). A caster's own base is now a shadow RECEIVER when it is a PAD — <=0.45 tall, >=1.15x the plan area of the mass on it, and FLAT, meaning the widest part contributing to it ENDS at the pad's top. Flatness is the load-bearing half: a gold mine's lowest band is also its widest, but its rock keeps going up, so a patch drawn there would hang inside the rock. Cost, measured on the boot map through the live renderer's metrics with the old and new builds served side by side: instances 862 -> 1188 (+38%), batches 9 -> 9, geometry resources 1 -> 2, retained bytes 197,328 -> 214,814; mean shadow instances per caster 3.0 -> 15.7 over thirteen archetypes, bounded structurally by relaxing the merge tolerance until the tiling fits ten strips (plus four for the pad layer), so a recipe cannot grow the lane without bound. Proof the change is render-only and confined to the shadow surface: a digest over every unit, resource and building recipe's NON-shadow parts is byte-identical across it (2326 parts, e2f40e86), while the shadow-only digest moves and the shadow count goes 438 -> 2304 over 146 recipe instances; the `memory` characterization digest (which casts nothing) is the control and did not move. Diffs at 1280x800 vs the pre-change renderer, RE-TAKEN against the shipped build after the flat geometry and the pad receiver landed (the first set was measured mid-slice and is not the shipped picture): 3.90% at default zoom, 15.08% at 2.4, 15.41% at 2.4 Painted, 1.40% at 0.7, each confined to ground toward +x,+z of casters and to each building's own plinth rim, plus the translucent HUD bar the world shows through. WORKED IN A WORKTREE, not the shared checkout: a peer session was mid-write in `github/aoe2` and its `src/game/simulation/civProfiles.ts` does not parse, which fails `npm run typecheck` there for anyone; left untouched per the same-day one-writer-per-checkout rule and reported to the owner.
- 2026-09-03 REMOTE GATES RED FOR AN INFRASTRUCTURE REASON, NOT THE CODE — read this before treating `ci:status` red as the next task. From 07:44Z every aoe2 Actions run fails in 2-9 seconds with **zero steps executed and `runner_name` empty**: the job is created and its matrix name resolves, and then no runner is ever assigned. Reproduced five times across three trigger paths (push, `gh run rerun`, `gh run rerun --failed`, `workflow_dispatch`) on both `ubuntu-latest` and `windows-latest`, for both workflows. Ruled out: the workflow YAML (all three files parse; the merge touched nothing under `.github/`), the matrix (both jobs exist and are named), and a GitHub incident (status.json reports all systems operational). The signature is what an exhausted Actions minute allowance or a reached spending limit looks like — this session pushed ~25 runs, each with a Windows leg that bills at 2x — and confirming it needs the `user` billing scope, which is the owner's to grant, not mine to request. NOTE the trap in the counter-evidence: voxel's runs DO get runners and civ-engine's DO pass, which reads as "runners are available account-wide" until you look at the timestamps — both are from 02:13Z, before the heavy runs, so they say nothing about now. The last aoe2 success was c5385458 at 04:04Z. **The code on main is verified locally**: v0.3.193 (6c990f1a) passed 3221 unit tests, 184 browser tests, the playtest corpus, typecheck, lint and build, all by exit code, before it was pushed. What the owner needs to do: check the account's Actions minutes / spending limit at github.com/settings/billing.
- 2026-09-02 v0.3.193 SHIPS sun-cast ground shadows, merged onto v0.3.192 and then corrected. The gap an audit of the real default view ranked first: the old contact slab sat entirely under a building's plinth, invisible. Each live building/tree/animal/unit now casts from its MASS under the daylight rig's own sun (`aoeVoxelDaylight.ts` is the one source for rig and projection), as three non-overlapping slabs on the existing shadow surface (7 materials / 9 batches / 13 draw calls unchanged), and the shadow no longer turns with a unit's heading. THE CLAIM 'overlapping shadows never stack' WAS FALSE and is fixed here: coincident translucent slabs Z-FIGHT rather than double-darken (proved by forcing the level step to 0 — a boar's shadow came back shredded and ~10k shadowed pixels reverted to lit grass), and the nine levels came from the anchor CELL, which cannot separate two casters in the SAME cell — 12,000-tick AI-vs-AI on the boot map: four villagers on one tile, 13 same-height overlapping slab pairs at once, 5 of 8 sampled frames affected. `resolveShadowLevels` is now a whole-lane pass that steps such a caster up until clear (0 pairs at every sample after). An INDEPENDENT CRITIC then broke that too: the pass capped its search at nine levels and fell back on the emitted height, which is taken by construction — 34 coincident pairs across 7 casters on a 6x6 woodline with 12 villagers in it, an ordinary scene, and every gate and capture I had sat just under the ~11-caster bound. The search is unbounded now (a K-clique needs K heights; a dense cluster rises above the band to 0.058, under 4 px of offset at the closest zoom). The same review found the sweep was never clamped to the ground, so every carcass swept its underground mass — a boar's shadow ran a quarter tile too long — and that four documents claimed "never" where the code had the escape hatch. Both fixes gated and both gates made to go red. A second review pass could not break the unbounded resolver (0 coincident pairs on a 20x20 forest, a 200-pop deathball at 0.35-tile packing, and every scene between) but found five more: my spec merge had left TWO copies of the art-direction bullet (the older still naming the retired contact-shadow material — the "two coexisting rules" the spec invariant forbids, and invisible to every gate), the stated reason the cell seed works is false for large casters (a Town Center and a House three cells away share a level and overlap), and the height-equality test was exact only while `ground` is hard-coded 0. All taken. KNOWN LIMITS recorded rather than fixed: the shadow BOBS as a crowd walks (18.9% of caster-frames, up to 3 px — the flicker moved from the pose channel to the level channel), the lift has no bound in code, and the 0.002 depth step assumes a 24-bit buffer nothing pins. Plain greedy colouring WITHOUT the cell seed was tried and is worse — a 3x3 tree block is a nine-clique and it exhausted all nine on a 6x6 forest. Diffs vs the pre-change renderer at 1280x800, all confined to ground toward +x,+z of casters: 3.51% default zoom, 13.33% at 2.4, 1.27% at 0.7, 6.96% on `arena` — but the DEFAULT style is Moebius, whose tone bands quantise one blend and two into the same step, so those diffs prove shadows appeared and are blind to the one-layer property the scheme exists for. Resolver cost measured at 0.56/1.09/2.70/6.02/17.12 ms per presented frame at 100/300/600/1200/2400 casters (boot map: ~85). Cost: instances 786 -> 862, triangles 14,112 -> 15,936, draw calls unchanged; frame time still UNMEASURED (the earlier attempt ran on a machine sibling agents held at 73-77% CPU). `scripts/captureMapScreenshot.mjs` gained STYLE (moebius|painted — the style is localStorage-only, so a sweep never saw the other shipped look) and GATHER (send every human unit to one cell: the only way a capture reaches the crowd state this defect lived in). The `arena` stone wall is still separate flat-topped boxes (roadmap M7) and cast shadows make it slightly worse, each segment casting its own hexagon across the gaps.
- 2026-09-02 CORRECTION to commit 1b6bae62 (v0.3.192's merge, below): it also carried the fourth fleet-canon sync of the day (AGENTS.md, sha 82e02c73b777 -> 75508464fa63) and its message does not mention it. I used `git add -A` and swept in a file another session had left in the working tree — which is precisely the failure the canon rule I helped write that same day describes: "`git add` fails all-or-nothing while the `commit` after it still succeeds and ships a message that lies about its contents." The content is legitimate and the history is not being rewritten to hide the slip; it is recorded here instead. The habit that prevents it is `git commit -- <pathspec>`, which cannot pick up an unrelated staged or dirty file at all.
- 2026-09-02 v0.3.192 UNITS WALK SMOOTHLY, DEER RUN. The owner saw units "move and briefly stop mid-movement between cells" watching live. Root cause is render-side arithmetic: the sim grants a villager one 0.25-tile fine step only when its carry bank crosses 100 (32 hundredths a tick: ticks 4, 7, 10, 13, ... with every eighth interval a 4) and the presentation blended only between tick-1 and tick, so the drawn root slid for 100 ms and stood for ~200 - a 3.3 Hz pulse the v0.3.160 gait window had hidden from the LEGS but not the BODY; a fleeing deer, moved a whole cell every fourteenth tick, was the worst case. Fix: `src/rendering/displayedPositionSmoother.ts` replays each unit's own sim samples one step cadence behind (`unitStepCadence.ts`: villager 4 ticks, scout 3, knight 2, ram 5, sheep 6, deer 14, charging boar/wolf 1), starts moving the tick the first step lands, never overshoots, snaps on >1.5-tile jumps, skipped ticks, rewinds, swaps, recycled ids, type changes and fog re-entry; `interpolateProjectedEntities` and the store's per-tick `previousPositionFrame` producers are deleted; the sim is untouched. Measured by the new `tests/browser/unit-motion-smoothness.spec.ts` (drawn root after every driven 60 Hz frame): villager on the boot map's open corridor, mid-walk still frames 67.4% -> 0.0%, per-frame step 0.0417/0.0417 (a sixth of a fine step at 2.5 tiles/s, then nothing) -> 0.0104/0.0139 (ratio 1.33, the sim's own 3-tick/4-tick alternation, 0.62-0.83 tiles/s), live rAF 43-53% still -> 0%; deer on the new `deer-flight-fixture` (a scout lures it), still frames inside the fourteen ticks after each hop 91.1% -> 0.0%, per-frame step 0.2357 (a sixth of a diagonal hop, 14 tiles/s) -> 0.0168 (1.01 tiles/s). An independent critic found five must-fixes in the first tree (deer on the old lerp with the exemption written into the spec unmeasured; coalesced syncs glided where the spec says snap, with two deleted tests unreplaced; a live bound one frame could fail; dead per-tick producers; two out-of-brief edits) - all taken, and measuring the deer exposed that the first fixture lost it to fog within a tick and that its "93%" had never been measured. THEN A SECOND CRITIC FOUND THAT THE WHOLE FIX EVAPORATED BELOW 10 FPS, which is where every CI machine and most players live, and the three findings it raised turned out to be ONE root cause. `apply` treated a forward tick gap (`tick - lastProcessedTick >= 2`) as a discontinuity and cleared every track; at 100 ms a frame the sim advances exactly one tick, so at any slower pace EVERY frame is such a gap and every frame threw away the history the smoother exists to keep - the drawn root fell back to the sim position, which IS the pre-smoother picture. Measured through the real bridge with the frame pace as the input: deer still-share inside its hops 0.0% at 16.7 ms and at 100 ms, 14.8% at 105, 78.8% at 125, 92.4% at 143, 93.4% at 167, 100% at 200; villager mid-walk 0.0% at 100 ms then 25.0 / 31.7 / 37.8 / 20.0% at 125 / 150 / 200 / 250, drawn step ratio 25.0 at 125 ms. The rule was wrong, not the code: A DISCONTINUITY IS A JUMP IN SPACE, NOT IN TIME - a two-tick gap is bracketed by two OBSERVED sim positions and is LESS extrapolation than the fourteen-tick deer hop the smoother already interpolates, and the >1.5-tile distance guard catches every real teleport (the fastest unit covers 0.75 tiles in the five ticks a double-speed frame holds). Gap reset deleted, rewind reset kept; spec, ARCHITECTURE and the module header rewritten so snap-vs-glide is stated once. After: 0.0% for deer and villager at every pace 16.7 ms -> 250 ms. Accepted cost, named: ALIASING not stopping - a 150 ms presentation sees a villager 3-tick cadence as alternating 1/2-tick intervals, drawn-speed ripple 1.67 (60 Hz ripples 1.33). THE GATE ITSELF was the second half of the work, because it had driven ONE frame pace, 16.7 ms, where the fix is perfect: `unit-motion-smoothness.spec.ts` now repeats both walks at `SLOW_FRAME_MS = 150` and the live half takes its verdict on TIME (`stillTimeShare`) rather than a frame count - the old `max(1, floor(2% x windowFrames))` over a 51-75 frame window was an allowance of exactly ONE frame against a measurement of zero, which one duplicate rAF timestamp would have spent. RED-CHECKED by restoring the one-line gap reset: the two new slow-pace tests and the rewritten coalesced-sync test fail (deer 98.1% still, 1.3637 tiles drawn per frame; villager 31.7% still, ratio 8.00) while all three 60 Hz tests stay green - the shipped gate blindness reproduced on demand; two smoother unit tests fail with it too. THE INDEPENDENT CRITIC THEN FOUND THE SAME MISTAKE ONE LEVEL DOWN, and it is the more interesting half. Removing the tick-gap reset left the distance guard FLAT at 1.5 tiles - and `wildlifeCombatSystem` moves a CHARGING boar or wolf a whole cell EVERY tick, so a two-tick frame moves one 2.83 tiles and the guard snapped it: measured on `wolf-aggro-fixture`, the drawn root covering 2.0616 tiles in one frame at 150 ms and 3.1623 at 200 ms. A boar lure is one of the most-watched moments of an opening, so that was the reported defect again on a different entity. The guard is now PER TICK of the gap and PER ENTITY, capped at five ticks: 1.5 tiles a tick for a whole-cell mover, 0.75 for everything else - and the second half came with the proof I had said was unavailable, `moveCarryCapHundredths` bounding the bank at `max(100 + perTick, round(perTick*1.5))` so the fastest unit in the game (Demolition Ship, 260% with every movement tech, 83 hundredths/tick) is granted at most TWO fine steps in a tick, half a tile, provably, for every unit. That halves the one hole this change opens - a garrison-and-eject completing inside one coalesced frame now glides at most 1.5 tiles over a two-tick frame instead of 3.0, 3.75 over a five-tick one instead of 7.5 - and it is named in the module header, the spec and the register rather than left implied. THE CRITIC THEN CAUGHT THE WHOLE-CELL LIST WRONG IN BOTH DIRECTIONS, the third time in one change that a rule tuned for one mover broke another: a monk-carried RELIC is a whole-cell mover (`monkBehaviorSystem` puts it on the monk's integer CELL, not its fine transform) so tightening made it snap on every boundary while the monk beside it glided, and a SHEEP is not one (one quarter-tile subgrid step every sixth tick) so it had six times the tolerance it can use. List now enumerated from every `setPositionAndSyncOccupancy` call site and asserted member by member; the mutant that swaps them dies. Smoother test split at the 500-LOC cap into a continuity file and `displayedPositionSmootherDiscontinuities.test.ts`. LOOKING AT THAT CAPTURE then found a third one no test had: across a gap LONGER than the delay the leaving-rest re-timing claimed the departure was `tick - delay`, compressing every tick of travel inside the gap into `delay` ticks of drawing - the wolf drawn back at its start and made to cross three cells inside one tick. It now takes the EARLIER of `tick - delay` and the last sighting, so a really-observed interval is drawn at its own average speed and a genuine rest keeps the cadence departure; wolf max drawn step 2.2361 -> 1.5811 at 200 ms, deer and villager unchanged. THREE more critic findings were about the gate, not the code: nothing in the suite bounded the DRAWN root against the SIM root (a 30-tick deer delay draws it 2.879 tiles behind and passed every bar - `maxLag` now bounds it at 0.5 tiles for a villager and 1.5 for a deer, and that mutant dies on it); the live half's own justification was FALSE above 100 ms a frame, because a still frame cannot be shorter than a frame interval, so it now SKIPS naming the machine's measured median rather than passing on 0.0% it could not have seen; and the aliasing ripple is 2.50 across 110-240 ms and 3.00 at 320 ms rather than the "up to 2:1" first written, so `SLOW_STEP_RATIO_BAR` is stated as a bar for ITS pace and not as a property. Also taken: the changelog no longer narrates an unreleased intermediate, the old register entry carries a dated correction rather than describing a deleted rule as current, and `PLAYWRIGHT_PORT` in it is `PREVIEW_PORT`. Visual: `TICKS=14` before/after on `deer-flight-fixture` differ 0.45% (zoom 2.0) and 0.13% (zoom 1.0), `TICKS=3` on `wolf-aggro-fixture` 0.42% and 0.16% with the diff a single blob on the wolf's two drawn positions; the `aoe2-prototype` framings are byte-identical, which proves only that nothing STATIC moved, because that map has no mover in its first ticks - the critic caught me grouping them under TICKS=14 when they were TICKS=1. The 12-frame 150 ms strips are the real evidence: before, drawn (37,33) x4 then (38,34) x8; after, 37.107, 37.214, 37.321, 37.429, 37.536, 37.643, 37.750 on the same ticks with the same sim positions. Red checks: the gap reset restored fails the two slow-pace tests and the coalesced test while all three 60 Hz tests stay green; the guard flattened fails the two whole-cell-per-tick cases; the deer delay mutated 14 -> 30 fails both deer tests on `maxLag` 2.879 against 1.5; the leaving-rest `min` reverted fails its own case. Spec file split at the 500-LOC cap into `helpers/motionStaging.ts` (what is driven) and the spec (what is asserted).
- 2026-09-02 INVESTIGATED the army regression (peak 11 where the pre-DE-times baseline was 20) and shipped NOTHING, because both hypotheses died to measurement — recorded in the register so the next attempt does not repeat them. Instrumented 45,000 ticks on the boot map sampling every 250: population at cap 11% of samples, gold<60 20%, FOOD<60 50%, WOOD<60 53%. So not a cap problem — except that 11% understates it, since the AI fills headroom instantly and the cap itself only reached 50 against a villager target of 40, which bounds any army at 10. Changed the house rule from "build when already blocked, one at a time" (survivable at 120-tick houses, not at DE's 250) to a headroom rule: the cap DID grow to 55 with eleven spare population, and peak military FELL from 11 to 8 — houses cost 25 wood and the AI is wood-short half the match, so the headroom came out of the army. Reverted. What the numbers actually say: at tick 45,000 the AI holds food 58, wood 27, gold 775, stone 65 with 40 villagers and 4 military — it is not short of population or gold, it is short of FOOD, continuously, while carrying the maximum villagers its age allows at 50 food each. Nothing about food income changed with DE build times; what changed is that a villager now spends far more of its life BUILDING, so the same 40 gather materially less than the tuning assumed. Next thing to measure is named in the register: villager target versus food income per villager, and whether a military food reserve shaped like `ageUpReserveCost` recovers the army without stalling the age-up.
- 2026-09-02 v0.3.191 closes the OPEN register defect (villagers auto-assigned under an enemy Town Centre). `enemyDefenceRange.ts` keeps gather assignment, drop-off choice, the walking/gathering re-check and the AI hunt phase out of enemy static-defence reach (the building's own effective range + 1 cell, shared with `towerCombatSystem` so the two cannot disagree), safe-first across resource kinds before the nothing-else-left exception. MERGED onto main v0.3.190 and re-measured, which overturned session one's conclusion: that session recorded "BEFORE == AFTER to the tick, the baseline never reaches the enemy" — true of the v0.3.183 tree, false of v0.3.190, whose bigger economy (Castle 35,250 -> 26,750) loses 43 villagers under the enemy TC with no candidate involved. 45k boot map, same tree with the rule off vs on: owner 2 villagers lost 50 -> 7, under enemy defences 43 -> 0; owner 1 25 -> 27, 0 -> 0; `seed-2` byte-identical both ways; ages/variety/peak army identical either way, though the critic showed those columns are too coarse to carry the stronger "the 43 were free" claim (two builds nine villagers apart produced byte-identical columns). The first fix still lost NINE (t=38,793-39,191, all `desired=gold`, all to mines 3 cells from the enemy TC): `villagerEconomySystem` has a SECOND idle-assign site — the villager that has just delivered — that called `assignResource` bare and skipped the safe-first kind walk, so every delivery handed the miner the same enemy mine back. Found by a per-death target trace, not by reading. Gate `villagersAvoidEnemyDefencesSelfPlay.test.ts` (45k, bar 3/owner) green at 0/0 in 159 s and RED-CHECKED at 43 with the rule removed; `ai:selfplay-audit` gained `vill lost` / `under def` columns. TWO independent critics, 30 findings, nine acted on in code: the census scored "under defences" against the NEAREST enemy building rather than any covering the cell; a fifth `assignNearestResource` caller (`toResourceStep`'s over-subscription redistribution) was still bare; the first anti-truncation assertion was a TAUTOLOGY (`lastTick >= 40k || outcome !== 'running'` on a loop that only exits those two ways) and three docs had banked it as a fix; "the exact reach is gated separately" was FALSE — nothing referenced `effectiveStaticDefenceRange` or `collectStaticDefences`, now 9 cases pin the arithmetic bonus by bonus; the re-target had no test at all and now has its own file; and the cost figure quoted everywhere was the ISOLATED 159 s against a measured 468-512 s under the full suite (timeout raised to 30 min). Known gaps recorded, not fixed: the rule bounds the NODE, not the ROUTE; for the HUMAN slot the re-target can never fire (`shouldMaintainGatheringOrder` and the re-target's explicit-order bail are complements) so only AI villagers are pulled off a newly-covered node; the AI still SITES buildings without asking where the enemy's arrows fall; a hunt order landing on a carcass sets the explicit-order flag; the reachability probe's 16-candidate cap can buy the exception while unprobed safe nodes remain; and both critics say this gate and `aiReachesCastleAge` should SHARE one 45k run rather than playing the same match twice — not done, recorded in the register.
- 2026-09-02 CLOSES the one critic finding v0.3.190 recorded rather than fixed: `MemoryEntry.lastSeenTick` is DELETED. It was written for every visible building and resource every tick and read nowhere in game logic — only round-tripped through the save — and after the v0.3.188 fix (write an entry only when its APPEARANCE changes) it recorded the first tick the current appearance was seen rather than the last time it was seen, so the name was a lie that persisted into saves. A future "last seen N seconds ago" or an age-out of stale fog memory would have read it and been silently wrong. Deleted rather than renamed, because dead data with a truthful name is still dead: gone from `MemoryEntry`, both `fogMemorySystem` write sites, `vietnameseReveal`, and the hydrate path; `saveSchema` keeps the key OPTIONAL and deprecated so older saves still load and ignore it. `bridgeStateAccessor.test.ts`'s nested-map round-trip used it as its per-entry discriminator and now uses `tint` — the test is about the codec, not that field. No version bump: nothing a player can see changes. Gates: 3158 unit tests, typecheck, lint, build, 178 browser tests, all by exit code.
- 2026-09-02 v0.3.190 APPLIES THE INDEPENDENT CRITIC on v0.3.187/188, which found seven real problems in work whose gates were all green. (1) The nearest-edge scope was TOO NARROW and the reason given for it was never measured: the critic restored the ordering on every BUILDING approach and both instruments stayed green, so the "drop-off path converged a workforce onto one cell" half of the story was asserted, not observed — only the RESOURCE path reproduces (reintroduced: stockpile moves 19 over 6,000 ticks). Re-measured after widening, it is better than green: the boot map's self-play winner castles at 26,750 instead of 35,250, with 14 building types against 9 and 10 unit types against 6. Repair, garrison, drop-off, attack and monk walks had been stripped of it with no evidence against any of them. (2) The peak-army bar's justification was WRONG: "the army arrives later, not smaller" cited owner 1's peak 45 at 90,000, and the gate's winner rule takes the EARLIEST to Castle, whose peak at 90,000 is 11 — the same as at 45,000. Claim withdrawn in the file; bar re-derived to 10 from the new measurement and the army regression (20 -> 11 against the pre-DE-times baseline) recorded as a regression rather than explained away. The file also carried two contradictory measurements of the same run. (3) "Three times under the ceiling" was the 13,000-tick number quoted after the horizon moved to 20,000; CI actually writes 332,020,582 bytes, 1.62x under, with the ceiling returning at ~32,300 ticks. (4) A real PERF regression: footprint cells were built before the plan cache's short-circuit — 1,470,428 discarded allocations per 20,000 ticks against a 92.2% hit rate, in the cache that exists because pathfinding was 23% of tick self time. Moved back below the checks. (5) The crew cap was per-site with no running total, so two crewed sites could exceed the half-the-workforce limit it is named for; now a workforce budget, gated by `aiBuildCrew`'s new arithmetic case. (6) `aiEconomyKeepsMoving`'s second case does NOT gate the deadlock (it passes with the defect present) — the register said it did. (7) The corpus horizon's recorded derivation was stale. `aiFeudalStone` was rewritten to wait on a CONDITION and to assert the match is still running at the end of its window — this fixture now resolves at tick 11,442, before the old fixed 11,000-tick warm-up finished, which is the same trap that produced the retracted defect. Every one of the five new gates was verified by the critic to go RED when its defect is reintroduced.
- 2026-09-02 v0.3.189 FIXES play-test findings F5 and F6 and gives the build menu DE's two pages. F5 measured before, 800x600 with a villager selected: the panel is 204px tall with `overflow-y: hidden`, its content 396px, and the whole BUILD group sits at y 647-767 under a panel clipped at 588 — six cards entirely off-panel, four of the eight stance/formation buttons cut, the stat rows gone; a wheel could not reach any of it because `.hud-panel--selection` is `pointer-events: none` and neither the panel nor `.hud-build-menu` was in the allowlist, and the suite only ever reached the cards because Playwright's `locator.click()` scrolls hidden overflow. After: 0 clipped elements and scrollHeight == clientHeight at 800x600 / 1280x720 / 1920x1080 for a villager (both pages), a scout and a Town Centre. What paid for it: icon tiles on one token pair (`--hud-command-tile` 44px, 34px below 1120px), the summary laid out ACROSS the bar instead of down a 264px column (its 186px `overflow-y: auto` box was hiding Faction/Civ/Inventory OUTSIDE the panel at 1280 and 1920 — the same defect, never reported), the portrait's redundant name caption dropped below 1120px, and a ceiling of min(36vh,268px) / min(46vh,276px). Cost: the 800x600 bar is 227px rather than 204, which is why the Watch Tower fixture's preferred anchors and the play test's Town Centre click both moved (the latter now scrolls the view with the arrow keys first, as a player would). F6: a POINTER focus no longer owns a tooltip (a keyboard focus still does), so a clicked card keeps focus and loses its tooltip — `:focus-visible` was tried first and is WRONG here, because a synthetic `focusin` focuses nothing and it answers "not keyboard" for every unit test. Build pages: `Record<BuildableBuildingType, BuildPageId>` makes a page-less building a typecheck error, the shorter page is padded with empty slots so both pages fill the same rows and the toggle never moves, and the palette follows a placement onto its page. Gates: `command-deck-fits.spec.ts` (12 tests; mutation-checked — the old ceiling fails 4, the old tooltip rule fails 3), `buildPages.test.ts` over the union parsed out of `types.ts` (drops `outpost` -> 3 red), `commandIconButtons.test.ts`, and the F6 rule in `tooltipFocus.test.ts`. THE CRITIC FOUND THE GATE RED ON THE SHIPPING BUILD: `toBeInViewport({ratio:1})` on the last tile failed at 1280 and 1920 (43/44px) because the fit-content panel measured 1px narrower than its content, and the change that caused it landed after the gate's last green run — two of the same file's three containment checks have ±1px slack and hid it. It also found F5 unfixed as a CLASS (16 of 47 fixture selections still clipped at 800x600 — a Blacksmith's research row to x 1724 against a panel ending at 588 — from `.hud-command-group { flex: 0 0 auto }`, a line I never touched, while the spec I wrote asserted it was gone), and F6 incomplete (any keystroke released the pointer's claim, so one ArrowUp put the tooltip back over the world). All three fixed: 2px of deck padding, shrinkable groups whose lists wrap (sideways overflow now zero everywhere; the three cards still taller than an 800x600 bar are wheel-reachable, scrollTop 29/30/152), and a claim held per `data-command` released only when focus reaches another control. Plus: the page toggle was dead during placement and flipped pages on cancel, it moved 113px when placement started (it now leads the heading), and `bombard-tower` shared the Watch Tower's glyph. The full suite then found a THIRD test the bar's height moved — a Dock's new Transport Ship, centred by the camera at y 319, was behind a bar whose top edge a 276px ceiling put at 312 — so the wrapped ceiling is now set by the CAMERA (`min(42vh, 252px)`) and gated: every "go to X" affordance centres what it finds, and a bar that reaches the canvas centre hides it. `placement-mode-hud-layout.spec.ts` now measures the CONTAINER width, not the border box, with the heading's three `@container` steps set from measurement (438 / 379 / 266px for "Placing: Siege Workshop").
- 2026-09-02 v0.3.188 THE REMOTE GATE CAUGHT WHAT THE LOCAL ONE COULD NOT: `playtest-corpus` went red on v0.3.187's first push behind a fully green local gate. Two failures. (1) `RangeError: Invalid string length` on `JSON.stringify(bundle)` — measured 537,183,049 chars against V8's ~536,870,888 ceiling, over by 0.06%; `aoe2.lastSeenStatic` alone was 358 MB of it, 27.5 KB re-serialised into each of 13,001 ticks because the fog system rewrites every visible entity's memory entry every tick and the only field that changes, `lastSeenTick`, is READ NOWHERE outside a round-trip test. Entries are now written only when their APPEARANCE changes and the slot is dirtied only on a real write: that slot 358 MB -> 2.0 MB, the bundle 537 -> 181 MB. (2) The progression gate: THREE diagnoses, the first two convincing from the code and killed by controls. DE build times slowing the opening — reproduced with the OLD times restored, so not the data. The town bell latching on a camped enemy — a shelter cap was built and moved the measurement by exactly nothing (stockpile delta 162 either way; Feudal 14,100/13,800 with the cap and with it disabled), so it was REVERTED. The real cause came from tracing positions: nine villagers stacked on ONE cell (51,28), motionless, all latched in `to-resource`, stockpile byte-identical from tick 8,000 — nearest-first ordering applied to the gather and drop-off walks converges a workforce onto one cell. Narrowed twice: the unit suite caught the widest version (99 failures, arrival slots are an ALLOCATION), and only self-play caught the second over-reach. It is now passed explicitly for a walk to an in-progress construction site and nowhere else. Corpus horizon 13,000 -> 20,000, derived from the measured transitions (boot map 14,100/13,800, default-seed 10,100/10,400). `aiReachesCastleAge`'s peak-army bar LOWERED 19 -> 8 with the reasoning stated in the file: at 45,000 the winner now shows peak 9 where it showed 20, but run to 90,000 owner 1 reaches peak 45 — the army is later, not smaller, and 45,000 stopped being past this map's resolution. Two OPEN register entries: the AI's military production is not retuned for DE build times, and `ai-feudal-stone-fixture` freezes byte-identical at ~15,000 with the day's change both on and off, which a gate whose window ends at 14,000 had never seen.
- 2026-09-02 CANON + LOCAL RULE: this repo's "the lessons index is gone; do not start another list" (owner, this morning, applied as `f8f807ad`) is SUPERSEDED by the owner's later ruling the same day — a lesson may exist as prose first, but it must become a gate, and the sooner the better. `docs/policies/local-rules.md` now says the index returns as a QUEUE, starting EMPTY (the deleted files are not restored: that audit found the large majority of the 84 entries duplicated a rule the constitution or these local rules already state, or concerned code since rewritten, including three about Phaser, which this repo no longer depends on), with every entry naming the gate that will retire it and a gate counting only once it has been made to go red by reintroducing the defect. `npm run verify` stays named in Gates as THE gate. Fleet-side, the two contradictory lessons bullets became one and the delegation bullet's measured token figures were stripped as "evidence about a model", which FLEET.md's own authoring rule forbids inline. A critic caught, before it shipped to 20 repos, a FALSE sentence the author had written into canon from `f8f807ad`'s commit message rather than its diff — "closed the file by gating what it stood for", when the commit is 18 insertions / 1,147 deletions with ZERO files added and the `npm run verify` it "promoted" already existed at `package.json:36`. Second false-provenance catch of the session; the first was a canon line asserting each repo names its entry point in `local-rules.md`, true in 1 of 20. Doc-only push, so the CI path filter skips it by design.
- 2026-09-02 The roadmap's coverage line is DERIVED now, not remembered: `tests/content/rosterCoverage.test.ts` reads the CSVs through the content build's own parser and the rosters from runtime tables the compiler keeps exhaustive, and FAILS when `design/roadmap.md` disagrees. It was 116 versions stale (last derived v0.3.70) and every figure was wrong in a different way — measured now: units 93/93, technologies 137/138 (only Treason open, Regicide-only and out of scope), buildings 25/25, 26 of 30 civilizations with active bonuses (open: Spanish, Burmese, Khmer, Vietnamese), naval implemented. The old line said units 35/104, technologies 133/140, buildings 18/27, five civs, "naval 0". Four parse traps encoded in the test because each had produced a wrong count: `BuildingType` is a type ALIAS so nothing can be scraped from `types.ts`; `-upgrade`/`-unlock` slugs make implemented technologies look missing; both `units.csv` and `structures.csv` repeat names (95 unit rows are 93 names, 60 structure rows are 28), so a row count is not a roster count; and civ bonuses are keyed by name in `civBonusTable.ts`, not in `civBonusEffects.ts`. It refuses to report on an implausible parse, and it checks unit coverage in BOTH directions — "93 of 95 covered" is compatible with one roster entry matching two rows while another matches none, which is exactly what the duplicate Scout Cavalry rows were doing.
- 2026-09-02 CANON APPLIED: the lessons index is gone. `docs/learning/lessons.md` (84 entries, none naming a gate), `lessons-evidence.md` (928 lines), `scripts/check-lessons.mjs` and the `lessons:check` script are DELETED per the owner's 2026-09-02 canon change. Audited all 84 before deleting: the large majority were instances of a constitution rule already in force (at least fifteen restate "verify the instrument before trusting the measurement") or of a local rule already written down (multi-framing capture, DE parity, play-it-like-a-human, the ~1% diff noise floor, the shared stash list); a second group were narrow code-shape notes whose subject has been rewritten since, three of them about Phaser, which this repo no longer depends on; the rest had gates whose headers already carried the same war story, which is the duplication the canon change names. ONE promotion: `npm run verify` is now named in AGENTS.md Gates as THE gate, because its `&&` chain makes the exit code the answer — that converts three separate lessons about piped output into a fixed command. Nothing is lost: both files are in git history, and the defect register (which the canon keeps deliberately) is untouched. Recorded as a local rule, including the note that the FLEET-CANON block still carries the SUPERSEDED older line telling a session to read the deleted file — removing that from `../fleet/FLEET.md` reaches every repo and voxel's local-rules cites it, so it is left for the owner.
- 2026-09-02 v0.3.187 DE build times, the multi-builder curve, nearest-edge approaches, and AI construction crews — the four fixes behind the play test's F1-F4. The build-time table was hand-maintained and SIXTEEN rows were wrong (House 120 ticks against structures.csv's 250, Mill/camps 180 vs 350, Barracks 240 vs 500, Market 200 vs 600, Watch Tower 220 vs 800, Castle 560 vs 2000, Town Center 300 vs 1500, Wonder 1200 vs 35,030); the cost column had three (Watch Tower missing its 25 wood, Wonder charging 1000 food DE does not ask for, Outpost 10 stone against DE's 5) and `structures.csv` itself carried two rows contradicting the aoe2techtree dataset it cites (House 30 wood, Outpost 10 stone). Both columns now differential against the CSV in `structureCostsAndBuildTimes.test.ts`, which also refuses to report on an implausible parse. AoE2's `3 x base / (n + 2)` multi-builder curve was spec text (§7, §16) and never implemented — builders stacked LINEARLY at 5.00x for five villagers, now 2.33x — implemented as a full share for the first builder to work a site in a tick and a third for each of the rest, which telescopes exactly and persists nothing. F2's root cause was one line: `findMovementPathToCandidates` returns the first candidate it can REACH, so the enumeration order was the choice — a villager two tiles west of a House site took the east edge (219 ticks to first progress, now measured at 25 through the real mouse and HUD: walk 2.5 s + build 24.8 s against DE's 25). The same line capped every crew at one working builder (37 AI villagers on a Wonder, site gaining 1.00/tick), which then exposed that the AI had no crew logic at all. Two gate re-anchors, both measured rather than assumed: the boot map's self-play WINNER flipped from owner 2 to owner 1 (Castle 29,500, peak army 40 vs the old 20) so `aiReachesCastleAge` now reads whichever slot wins, and the boot map stopped starving entirely (0 relief admissions where 229 were measured — 0 again with nearest-first DISABLED, which is how the changed match rather than the pathing was established as the cause) so the traffic-relief gate moved to `default-seed` (181 admissions, 0 phantom). Gates: 3137 unit tests, 145 browser.
- 2026-09-02 CANON: two standing-loop rules added to `../fleet/FLEET.md` and propagated to all 20 census repos — a standing loop sources its next task by running the artifact the way its user does (never by reading code for something to improve), and a context compaction is a harness event rather than a task boundary. The first draft FAILED the fleet's own admission test on two counts, both caught by an independent critic and neither by the author: "Each repo names its own entry point in `docs/policies/local-rules.md`" was measurably FALSE for 19 of 20 — 7 repos have that file at all, exactly 1 (aoe2, added by the same change) names an entry point, and `ml`/`news`/`town_3d` have no `docs/` directory — so an agent reading it would have created 13 files to satisfy a canon sentence, the "an unnecessary instruction gets followed" cost fleet/AGENTS.md warns about, and it was already derivable from FLEET.md's own "a rule that applies to one repo goes in that repo's local-rules.md"; and "a compaction is not a stopping point" was the expired "only makes sense as the model keeps doing X" shape FLEET.md explicitly rejects, salvageable only as the environment fact underneath it. Reworked to keep the two non-derivable things and add the trigger the first draft lacked ("a repo with no runnable entry point has no standing loop"), which scopes it away from the pre-code repos (`news`, `3d-maker`) and the headless engine, whose user is a sibling repo rather than a person. Also cut "and sweeping the whole result", which restated the visual-verification rule's whole-result sweep. aoe2's own entry point now named in [local-rules.md](../policies/local-rules.md): `aoe2-prototype`, fog on, default zoom, human slot, sweep the whole frame before choosing what to work on. Doc-only push in every repo, so aoe2's CI path filter (`package.json`/lockfile/workflows only) skips it by design and there is no run to watch; fleet `npm test` 41/41. STILL OPEN, found during the work and not fixed: FLEET.md carries two contradictory lessons bullets — one abolishes the permanent index, the next mandates reading it at session start (`06768ac` added the index rule, `a5852fc` added its replacement without deleting it) — and both propagate to all 20 repos; `badge`, `bot`, `yanfengliu` sit under the fleet root but are not in the census and have never received the canon.
- 2026-09-02 v0.3.186 the idle-villager badge stood ON the command bar — a fixed `bottom:190px` against a bar whose height depends on the selection — so with a villager selected its bottom edge covered the SELECTION label at every viewport: badge [12,366→63,410] over label [27,399→255,412] at 800x600, [12,486→63,530] over [27,515→287,528] at 1280x720, [12,666→63,710] over [27,695→287,708] at 1440x900 (red first in `hud-floating-controls.spec.ts`, 3/3 on v0.3.177 and again on the merged v0.3.179 bar; soft assertions list every overlap in one run, which also showed the badge 29px inside the bottom row's top edge with nothing selected above 1120px, where the minimap panel is the row's taller child). Smallest change: the badge measures its `bottom` from the bar's top edge each frame in the rAF loop it already had, and the speaker toggle stacks above it the same way — after: [12,332→63,376] under bar top 384, [12,448→63,492] under 500, [12,628→63,672] under 680, 8px clear; the spec holds the CLASS (no floating `[data-hud]` control over the bar's heading or another control, three viewports, empty and selected). Diffs of `TICKS=1` pairs are exactly the two buttons' old and new boxes: 5,311 px (0.58%) at 1280x720, 5,311 (0.41%) at 1440x900, 4,888 (1.02%) at 800x600, 0 px elsewhere — boot-frame pairs of two builds also differ on the minimap canvas by ≤4/255 (same-build pairs 0.00%), which is the capture, not the fix, and is now a local rule. INSTRUMENT: `PREVIEW_PORT` selects the preview for the suite and the capture script and both refuse a server whose served asset hashes (JS and CSS) differ from this checkout's dist, because a sibling worktree's preview on 4173 would have been measured as ours; `SELECT=villager` reaches the selected bar from the maintained script; the full suite caught the fix's side effect on `game-hud-and-camera-camera.spec.ts`, whose wheel-zoom probe now walks inward to bare canvas and names what covered the rest. Gates on the merged branch, serially and unpiped: typecheck 0, lint 0, build 0, `npm test` 0 (443 files, 3,114 tests), `npm run test:browser` 0 (144 passed, 2 skipped, 9.6 min on this worktree's own preview).
- 2026-09-02 TOOLING: `voxel:build` now also takes a shared build LOCK (OS temp dir, keyed by the sibling's path). The mtime check alone cut rebuild frequency but not the race: the sibling's own `prebuild` DELETES its dist before `tsc` recreates it, so two concurrent gate runs still deleted each other's output — a rendering agent's build died on 'Could not resolve ./ThreeRenderRuntime.js', and the stale-check's own first run found dist missing because a sibling had just removed it. With the lock, a second caller waits for the in-progress build, re-checks freshness, and skips. A lock older than ten minutes is treated as abandoned.
- 2026-09-02 TOOLING: `voxel:build` now rebuilds the linked sibling `../voxel` only when its sources are newer than its dist (`scripts/voxel-build-if-stale.mjs`; FORCE_VOXEL_BUILD=1 overrides). Every gate's pre-hook ran the full rebuild unconditionally, so concurrent gate runs on one machine - agents in worktrees, a merge on main, a play session on the dev server - rewrote the same unchanged package under each other. Cost today, measured: a live match went black mid-play ('Failed to load url .../voxel/dist/three/index.js'), one merge's `npm test` lost three rendering files to "Cannot find package 'voxel/core'", and a second merge's browser pre-build died in Rollup on an invalid resolved id - three merge attempts of v0.3.179 aborted on environment, none on the change. The sibling's sources change only when someone edits that repo.
- 2026-09-02 v0.3.183 APPLIES THE MULTI-CLI REVIEW of the traffic election and the approach-plan cache (docs/threads/current/concurrency-review-2026-09-02). C1: monk conversion flipped `unit.owner` with no `structuralRevision` bump, so a converted villager replayed a remembered step INTO its old side's gate and stood in the enemy wall - reproduced through the real bridge (new `converted-unit-gate-fixture`: the villager walked to x=20 and sat there 400 ticks in to-resource); fixed by `notePassabilityChange()` at both conversion flips, threaded as a REQUIRED dep. E1: the starvation clock counted ANY standing still, not contested waiting - measured on the boot map in self-play at 30,000 ticks: 245 relief admissions across 18 units, FIVE of them phantom (units whose last attempt was 793-2,354 ticks ago, relieved on their first ask); the clock now restarts when a unit resumes asking after a gap of more than one tick, and the same run after the fix gives 229 admissions across 10 units and ZERO phantom, first relief at tick 22,195 (before: 15,284) - the rule engages on the boot map past the 20k census horizon either way, which the v0.3.175 "preserved field-for-field" claim never saw. E2: the election read each member's clock LIVE against a snapshotted everything-else; clocks now ride the snapshot (`trafficProgressClock` is the one rule for both the stamp and the snapshot), so consult order cannot split a jam. E8: the shipped tests could not tell the fix from a one-line revert; five mutants now die - stamp-every-consult and threshold-1 on `movementTrafficReplenishedJam.test.ts`, the E1 rule removed on `movementTrafficStarvationClock.test.ts`, the range dropped from the building key (C5) on `movementPlanOpsCacheKey.test.ts`, the C1 bump removed on `convertedUnitGateReplan.test.ts`; the tuned 750 is pinned as a LITERAL in both gates because a constant imported into its own boundary test walked a threshold-1 mutant green. E7 CORRECTIONS to the record, because the v0.3.175 line below repeats them and history is not rewritten: the arbiter is consulted ONCE per unit per tick (the "~44" was the whole population's consults per tick at ~44 movers, mislabelled per-unit); the walk speed is 0.32 FINE units per tick = 0.08 tiles/tick, so a cell takes ~12.5 consecutive admissions (not 3.2) and a walking unit's cache entry lives ~12.5 ticks (not "every third tick"); "permission is not progress" still holds, for the entitlement-carry reason - 12 scattered admissions in 40 ticks move under one cell. Also: election extracted to `movementTrafficElection.ts`, the orphaned duplicate JSDoc removed (E10), the `movePathCache` zero-hits claim reworded to the warmed window it came from (C7), the unit-id-recycling boundary stated honestly in `worldOccupancy` (C2), the drift-log row re-joined to its table (C9). Spec section 12 now carries the relief rule and the converted-unit gate rule. A SECOND SESSION the same day finished the application after the first was killed mid-gate: main's voxel:build race fix (781a7f7a) brought into the branch; all six mutants re-run and killed with the source restored bit-for-bit (a plain lowest-id election added as a seventh, five failures); the boot-map gate re-measured, after-fix 229 / 10 units / first 22,195 / 0 phantom and before-fix 245 / 18 / 15,284 / 5 BOTH reproduced exactly - the latter only on the third arm, the shipped v0.3.178 arbiter with the probe matched to the new `starved` semantics (a before is one number per arm and per probe; the header now names both, with the 259 / 6 and 212 / 3 the other two arms give); and the owner-flip audit closed - the only other in-place rewrite of a passability input is the unit-line upgrade sweep (`unit.unitType`, the domain), now gated in `unitDomain.test.ts` (no line crosses a domain); a sheep changing owner is not one.
- 2026-09-02 v0.3.178 REVERTS v0.3.177 on two BLOCKING critic findings, verified by re-running the audit at 45,000 ticks on the candidate: boot map at 45,000 ticks on the shipped HEAD (scripts/selfplay-audit.mjs): owner 2 Castle 24,250, peak army 10, match UNRESOLVED (13 building types, 9 unit types); the baseline on the same script: Castle 25,250, peak army 20, match ended by conquest at 39,890.. The candidate leads ONLY in the 25-36k window - exactly where I had moved the gate - and at match resolution on the PROTECTED map it loses the game the baseline wins: baseline o2 peak army 20 and conquest at 39,890 with zero villager deaths; candidate peak 10, unresolved, villagers 40 -> 12, thirty killed from 35,750 on, every one inside the enemy Town Centre's range, 29 of 30 in `to-resource`, fourteen walking to one boar beside the enemy TC. That is DESIGN.md disqualifier 3 ("one horizon only") on the map it was written to protect, and the 45k run I had cited as evidence was the BASELINE - I never ran the candidate at 45k. Second blocker: the seed-2 "war" attribution was inferred and FALSE - same mechanism, villagers sent into the enemy base, and gather share FELL there (46.0 -> 39.7%) while I measured done-condition 4 only on the two seeds where it rose. Also: "held-out" seeds were in the pre-registered ten; "bars derived before the candidate" is unverifiable from the repo because the timestamped threshold lived in my scratchpad; the test's trained/lost ledger counts garrison flickers as deaths. The critic's method is re-runnable and reproduced my own numbers first. The horizon question is settled the way the critic said: the gate now runs to MATCH RESOLUTION (45,000 ticks) with bars from the 45k baseline, because a horizon that shows a candidate winning is suspect until the resolution horizon agrees. NEW DEFECT registered from the review, present in baseline too: auto-gather and the hunt phase will send villagers to resources inside an enemy Town Centre's range. The thread reopens.
- 2026-09-02 v0.3.179 FIXES the placement-mode HUD defect the owner found by playing: clicking a build card rendered the `Placing: House` status as a THIRD flex sibling between the summary and the command deck, and since the deck is the only sibling allowed to shrink, the build palette paid for it — visible width 261 -> 36px at 1280x720 (14%), 421 -> 196 at 1440x900 (47%), untouched at the 800x600 wrap regime, which is why the suite never saw it. The status is now a 17px pill in the Build group's heading — the count badge's height, so the row is unchanged whether the badge shows or steps aside below 380px of group width (a 20px pill grew the group 180 -> 183px; a 16px one lost 1px once the badge was hidden) — and the palette keeps 261/421/546px and 180/180/120px at all three viewports. The tooltip half was a CLASS, not a consequence: the shared tooltip centred above a right-hand build card overlapped the minimap frame at EVERY viewport before placement mode was entered (1280: x 804..1064 vs frame 1022..; 1440: 958..1218 vs 1182..; 800: 376..636 vs 598..), and the first fix's after-shot showed it covering the very pill the click had revealed. Command-bar tooltips now float above the BAR and slide left of (or above) the minimap frame. Gates: `placement-mode-hud-layout.spec.ts` hovers every card before and during placement at three viewports and holds the palette's VISIBLE width (rect clipped by the scrolling panel — the layout box keeps its min-width and a plain rect check passed on the broken code) at >= 90%; `tooltipAvoidsMinimap.test.ts` pins the arithmetic; `selectionPanelCommands.test.ts` pins the structure. Register entry written. The whole-frame sweep also found the idle-villager badge sitting over the SELECTION label at bottom-left — pre-existing, not touched, named in the register's prediction.
- 2026-09-02 v0.3.177 SHIPS the Lumber Camp candidate after twenty-plus attempts across three sessions, and it ships on a STRICTER gate than the one that rejected it. The peak-army gate's horizon was raised 24,000 -> 30,000 with the owner's approval because 24,000 scored the boot map 1,250 ticks BEFORE its Castle (qualified 23,500, Castle 25,250); every bar was re-derived from the baseline at 30,000 BEFORE the candidate was measured, and every bar moved up: peak army 8 -> 9, unit variety 5 -> 7, plus a NEW assertion that the Castle Age is reached rather than merely qualified for. Branch A's medoid anchor + mining-camp-first (round 2's candidate, unchanged) passes it. Six seeds at 30k: Castle-Age slots 5 -> 7 (three that NEVER reached Castle now do), peak army summed 71 -> 75, building types 99 -> 107; the PROTECTED map improves on every column (qual 23500 -> 16500, Castle 25250 -> 24250, peak 9 -> 10, bld 9 -> 13; owner 1 identical); all four HELD-OUT seeds improve. The one regression is `seed-2`, a MEASURED seed - at 30k it reads as "loses Castle", at 45k it reaches Castle 9,250 ticks late while its villagers collapse 22 -> 6 because the candidate makes owner 1 viable (Castle at 39,750, never before) and owner 1 raids it: a contested match where there was a walkover, the same reading seed-7 got yesterday. Named as a cost. DESIGN.md disqualifiers 1, 3, 4, 5, 6 pass on the data; 7 (haul traded for approach) measured directly: boot map gatherTicks 49,715 -> 58,544 (+17.8%; gather share 46.7% -> 55.2%, walk 51.3% -> 41.4%) and corpus-seed-b 34,666 -> 48,470 (+39.8%; walk 60.9% -> 44.5%). Idle rises 2.0% -> 3.4% and 2.0% -> 4.2%, a small named cost. Villagers walk LESS and extract MORE. The ship threshold was boot-map gatherTicks >= 48,721, fixed and timestamped before the candidate arm reported.. Nomad is unaffected - its TC-less branch returns before pickNextBuildTarget and passes no owner to dropOffAnchorFor, so the work anchor never engages there; spec §281 stands. The horizon change alone would have been worth committing: it retires the "walled into Feudal" claim from the instrument that manufactured it.
- 2026-09-01 PARTIALLY UNBLOCKED the expansion tech gap and found a SPEC QUESTION underneath it. `design/stats/tech-tree.csv`'s own header names the source this repo already uses - "SiegeEngineers/aoe2techtree data.json, fetched 2026-08-27" - which is reachable where the wiki was not (fandom 402, aoe2insights 403). Downloaded it (926 KB, 194 techs with Cost/ResearchTime/internal_name) and found each civ's uniques by set difference over the per-civ Tech id lists. Costs ARE authoritative and available: Italians 499 {Food 250, Gold 250} 60s and 902 {Food 650, Gold 500} 50s; Slavs 455 {Gold 200, Wood 400} 40s and 513 {Food 900, Gold 500} 40s; Khmer 622 {Gold 450, Wood 300} 40s and 623 {Food 700, Gold 400} 40s; Burmese 626 {Food 400, Wood 300} 40s and 627 {Food 300, Gold 300} 40s. TWO THINGS STILL BLOCK, and the second is a decision rather than a lookup. (1) The dataset carries costs and research times but NO EFFECT MAGNITUDES - it is a tech TREE, not an effects table - and the magnitude is the half that matters for `unitEffect`. (2) CURRENT DE HAS REWORKED SEVERAL OF THESE: the dataset names Italians' uniques "Silk Route" and "Pirotecnia" and the Slavs' "Detinets", where our `civilizations.csv` says Pavise/Silk Road and Orthodoxy/Druzhina, and the Khmer and Burmese entries are unnamed placeholders ("Khmer UT"). Our CSV predates the rework, so implementing Pavise would add a technology current DE no longer has. WHICH EDITION THE SPEC TARGETS IS A USER DECISION, not something to infer - and inferring it would be exactly the kind of confident guess that has been wrong twelve times today. Nothing implemented.
- 2026-09-01 THE BOOT MAP IS NOT WALLED INTO FEUDAL, and I had repeated that claim all session. Ran the standing acceptance test (`selfplay-audit`) at its real 45,000-tick default after today's fixes: **6 of 8 owner-slots reach the Castle Age and 1 reaches Imperial**, and `aoe2-prototype` ends early in DEFEAT - the match RESOLVES BY CONQUEST. Boot map owner 2: Feudal 9,250, qualified 23,500, **Castle 25,250**, peak army **20**, 9 building types, 8 unit types. It looks walled only at a 24,000-tick horizon, which cuts off 1,250 ticks BEFORE its Castle. That is exactly the failure the audit script's own header warns about - its old 24,000 default "produced three wrong conclusions that all reached the register before anyone checked the instrument", one of them being that owner-slots are walled in Feudal - and I reproduced it verbatim by quoting a 24k-horizon result as the state of the game. `default-seed` o2 reaches IMPERIAL at 42,750; `corpus-seed-b` both slots reach Castle; `seed-2` o2 Castle at 22,750. Only two slots stay in Feudal, and one of those (`aoe2-prototype` o1) LOST the match. Totals across eight slots: 79 building types, 50 unit types, peak army 74 - against the "four unit types and eight building types" that the `aiReachesCastleAge` header records as the state that motivated this whole workstream. WHAT THIS DOES NOT CHANGE: the camp candidate still fails the peak-army gate at 24k by one untrained unit, and the gate's horizon is its own question. WHAT IT DOES CHANGE: "self-play exercises a fraction of the roster" was a claim about a horizon, not about the game.
- 2026-09-01 TRIED the established path for the expansion tech gap and it is BLOCKED ON SOURCE AVAILABILITY, which turns my claim from an assertion into a tested one. Spec §873 records that the six shipped expansion techs had every value verified against the wiki rather than taken from memory. Attempted exactly that for Pavise (Italians), chosen because it needs NO new seam - `unitEffect` already supports `armor`/`pierceArmor`. Three sources unreachable from here: ageofempires.fandom.com HTTP 402, aoe2insights.com HTTP 403, aoe2techtree.net HTTP 404. The one search that returned values returned CONTRADICTORY ones in a single result block - "+1/+1 to foot archers EXCEPT Skirmishers, and the Condottiero" against "+1 melee / +2 pierce to ALL foot archers INCLUDING Skirmishers". Implementing from that would be exactly the failure §873 names, where taking the value from memory would have made Shatagni +1 range instead of +2, so nothing was implemented. NOT IMPLEMENTED ON PURPOSE. What a future session needs is one authoritative reachable source (or the user supplying the values); the code side is ready - Pavise, Orthodoxy, Howdah, Tusk Swords and Manipur Cavalry each map onto an existing seam and would be a few lines apiece once the magnitudes are known.
- 2026-09-01 CORRECTED my own repeated claim about the TECH gap, which was imprecise in a way that overstated the blockage. I said several times that the remaining expansion content is "blocked on absent data rows". The accurate two-part statement: `design/stats/civilizations.csv` IS complete and NAMES every one (Italians `Pavise;Silk Road`, Slavs `Orthodoxy;Druzhina`, ...) - which is what `expansionCivContentGap.test.ts` means by its assertion name "reads a CSV that is COMPLETE - the gap is in the code, not the data" - while `design/stats/technologies.csv` has NO row for any of them, so cost, build time, applies_to and description are genuinely absent in-repo. The ledger is 18 techs and 17 units, not the 19/17 I kept repeating. WHAT THIS CHANGES: the blocker is not "we do not know what is missing" but "the magnitudes are not in the repo", and spec §15 records the established path for exactly that - the six expansion techs already shipped (Shatagni, Recurve Bow, Farimba, Kasbah, Sultans, Carrack) had "every value verified against the wiki rather than taken from memory (which would have made Shatagni +1 range instead of +2)". Several remaining ones map straight onto seams that already exist: `unitEffect` supports `armor`/`pierceArmor`/`attackDamage`, so Pavise (foot archers +armour), Orthodoxy (monks +armour), Howdah and Tusk Swords (elephant armour/attack) and Manipur Cavalry (+vs buildings, the exact shape shipped for the Indian scout line this session) need no new seam - only sourced magnitudes. Others genuinely do need seams that do not exist (regeneration for Maghrabi Camels, splash for Druzhina and Torsion Engines, trade for Silk Road).
- 2026-09-01 RENDERING SWEEP WIDENED to seven distinct renders - the boot map at zoom 0.7 / 1.2 (default) / 1.7 / 2.4, plus `default-seed`, `seed-7` and `corpus-seed-b` at default zoom, each a different terrain, resource and water layout. Every one is coherent: terrain, water with shoreline, forest, gold, stone, berries, sheep, units, the Town Centre with tower + crown + team flag, the behind-building occlusion silhouette, HUD, selection panel, idle-villager badge, and a fog-correct minimap. NO DEFECT FOUND, and that is the result rather than a failure to look: three candidate defects of mine dissolved under checking earlier the same day, all three from deriving screen coordinates by eye instead of measuring differentially. WHAT THIS DOES NOT COVER, stated so the gap is not mistaken for coverage: these are BOOT states. Selection highlights, building-placement previews, combat and projectiles, damaged-building variants and the tech-tree UI are interactive states a boot screenshot cannot reach - those live in the Playwright browser suite (121 tests, green in CI since 2026-08-24). The rendering gap is BREADTH OF SCENARIOS SAMPLED, not a known-broken area.
- 2026-09-01 CLOSED seed-7 owner 1 as a PHANTOM DEFECT: it is not an economy wall, it is losing a war. A death census counts TWENTY owner-1 villagers killed over 20,000 ticks with `ENEMY2:militia` within five tiles for most, clustered at t=6,800-8,400 - exactly the window where its villager count collapses 7 -> 3. Owner 2 built a Barracks, trained militia and raided. An economy running three villagers cannot bank the 500 food Feudal needs however its gatherers are allocated, so the 5 wood, the missing farm and the ten-villager Dark Age cap are all CONSEQUENCES of the raid. I had been treating this slot as the economy wall in miniature and every allocation-side idea aimed at it (villager mix, drop-off routing, gather targeting) was aimed at a symptom. Both slots run the same AI; one out-aggressed the other, which is Age of Empires working and is evidence FOR the simulation. Register entry closed rather than fixed. UNMEASURED remainder named honestly: four of the twenty deaths show no enemy inside the five-tile window - either a ranged attacker outside it or a second cause.
- 2026-09-01 v0.3.176 FIXES the seed-7 carrier limit cycle, and the cause was a CORRECTNESS bug rather than a stability one: `findNearestDropOffBuilding` measured `manhattanDistance(origin, buildingOrigin)` - to the building's CORNER, not to the nearest cell it occupies. For a 4x4 Town Centre that is out by up to three tiles and out by a DIFFERENT amount from each side, which is what let two adjacent cells disagree about which drop-off was nearest. Traced on `seed-7`: to origins the TC reads 4 and the Lumber Camp 4 from (49,21), then 5 and 3 from (50,21) - so the winner REVERSES across one step, and the carrier paced between the two cells indefinitely holding ten wood with traffic granting `proceed` every tick. To real footprints the TC's nearest cell is (50,24), distance 3 from both, and the reversal is gone. Six seeds at 20k: boot map and `default-seed` BIT-IDENTICAL, seed-7 worst 3500->250 with stuck 5->0 and o2 +5 buildings, corpus-seed-b stuck 2->0, seed-11 o1 +2 buildings; `seed-2` REGRESSES (stuck 0->1, o1 -1 and o2 -2 buildings) and that is a real cost, not rounded away. Stuck villagers across the population 7->1, buildings +5. WHY THIS SHIPPED WHERE THE HYSTERESIS DID NOT: the hysteresis paid a permanent routing cost for stability and regressed the PROTECTED map (o1 villagers 22->19 at both margins tried); this is a bug fix with no routing cost, and the protected map does not move at all. ALSO: my first version of the key test over-asserted (demanded both cells AGREE, when the property that matters is that the winner never REVERSES - a tie is fine); corrected the assertion rather than the helper. And the file-size gate caught me appending the helper to an already-full 500-line `pureHelpers.ts` - split to its own 25-line module and `pureHelpers.ts` is byte-identical to HEAD again.
- 2026-09-01 PERFORMANCE OPTIMISED, not merely measured: the AI ran a full A* to its target EVERY TICK because the only positive path cache covers explicit move commands, which an AI never issues. Instrumented on `default-seed` warmed 33,000 ticks: 32.8 resource + 7.9 building searches per tick at 50 units (about one per unit per tick) and `movePathCache` recorded ZERO hits. The negative cache beside it already spared FAILING searches - its comment records a 12.3 s/tick disaster before it existed - but nobody had done the same for succeeding ones. New `approachPlanCache.ts` memoises on (unit, start CELL, target, structuralRevision), which is behaviour-preserving BY CONSTRUCTION: A* is deterministic, the search reads only the start cell, the target's approach cells and durable topology, and units never block passability, so a stored answer is exactly what a fresh search returns. The CELL in the key is the bound - the entry stops applying the moment the unit steps into a new cell, roughly every third tick at 0.32 tiles/tick. VERIFIED rather than argued: three seeds at 20,000 ticks give BYTE-IDENTICAL state fingerprints (every unit position to two decimals, every building, resources, ages) - aoe2-prototype 73b44244, default-seed a1519947, seed-2 f294b2c0 on both arms - at 196->309, 263->356 and 243->357 ticks/s, i.e. +35% to +58%. The `npm test` suite itself dropped 172s -> 121s. Mutation-checked the new tests: two of three mutants killed, and the third (dropping the `revision === undefined` guard in `get`) is EQUIVALENT - `set` refuses undefined revisions, so a stored entry always has a real one and the comparison already covers it; verified rather than papered over with a test that cannot fail.
- 2026-09-01 PERFORMANCE, the clause with no work all session: measured, and the finding is ENGINE-side. Tick cost is healthy in absolute terms - 2.0-2.8 ms against a 100 ms budget at TPS 10, so 35-50x real-time early - but it grows 3.7x over a match AND DOES NOT TRACK UNIT COUNT: 3.14 ms at 44 units (t=15,000) against 9.43 ms at FORTY units (t=39,000), with buildings up only 1.65x over the same span. So the driver is something monotonic (explored area, buildings, accumulated state), not population. CPU profile at 33k ticks on `default-seed` (55 units, 8.48 ms/tick): pathfinding ~23% (findPath 13.5, neighbors 6.3, findGridPath 1.8, heapPop 1.3) which is inherent to an RTS; then ~10% in `visit @ json.js` + `jsonFingerprint` + GC, and 3.4% in `buildDiff`/`buildSnapshot`. BOTH are engine, not ours. `ComponentStore.set` calls `assertJsonCompatible` on EVERY write - a fresh WeakSet plus a full recursive walk - with NO opt-out anywhere in the dist; and `world-tick.js:131` calls `buildDiff()` unconditionally, so every headless test/corpus/audit tick builds a render diff nobody reads. VERIFIED THE WRITES ARE OURS-BUT-NECESSARY before blaming the engine: instrumenting `setComponent` over 300 late-game ticks gives 43.5 writes/tick (42.2 unitTransform) with only 0.2% redundant, i.e. 0.77 per unit per tick for units that move every tick - there is no local win from writing less. Both filed in `docs/engine-feedback/current.md` per the report-don't-modify-engine policy. Freshness audit of that file done MANUALLY (AGENTS.md asks for a subagent; this session's instructions prohibit them) - re-verified the A* tie-break ask still stands (`pathfinding.js:92` is still bare `newG < prevG`, first-reach-wins).
- 2026-09-01 RENDERING SWEEP: four zoom levels on the real boot view (`aoe2-prototype`, fog on), and the honest result is NO DEFECT - fog, terrain, water, forest, gold/stone, sheep, units, Town Centre with tower+crown+flag, occlusion silhouette, HUD, selection panel and minimap all render correctly. THREE candidate defects of mine dissolved under checking: the blue silhouette reading as a figure in FRONT of the building (max zoom shows a correct behind-building cue); a dark apex block (the column profile shows tower and crown both rendering, the crown's blue-grey matching its team-tinted mix); and a missing Town Centre flag (a removal-diff located it at x 396-414, y 144-173 - my px/unit scale was off by 30px and my colour filters missed it because the LIT colour is rgb(53,68,81), not the 0x3f6fd8 tint). All three came from deriving coordinates by eye and then treating the numbers as facts; the removal-diff needs no coordinates and settled in one shot what three pixel scans could not. SHIPPED from it: `tests/rendering/townCenterFlag.test.ts` - the flag is how a player tells whose base they are looking at and had ZERO coverage. Asserts the CONTRACT (exists, owner's colour, above every other part, on a pole) rather than pixels, so lighting changes cannot shatter it; mutation-checked against three deliberate breaks (drop the flag, bury it under the roof, hardcode the colour), each killed by the right test. Gates: 3096 passed.
- 2026-09-01 ROUND 3 FALSIFIED my own prediction: the latch fix does NOT rescue branch A. Re-ran branch A + mining-camp-first against the boot-map gate on the unstarved sim, predicting the army cost would ease on two mechanisms - the register records branch A CAUSING a latch (ten villagers on one cell, unmoving t=9,000-20,000), and traffic starvation hits military units too, so piecemeal arrival through a contested gap is what a 6-to-1 loss ratio looks like. Result is IDENTICAL to round 2: trained 8, lost 6, peak 7, the same three numbers. Both mechanisms are ELIMINATED rather than unsupported - had either contributed, the loss count would have moved. Branch A's army cost is independent of the traffic election. The open question is now narrower: not why the army falls (it dies) and not whether traffic kills it (no), but where and to what those six units are lost. INCIDENTAL: the gate message I rewrote this morning earned its keep on its first real failure, printing 'trained 8 but LOST 6 - the army died in the field' where the old text would have said 'the age was bought by disbanding the army' and sent the reader after a resource tradeoff that is not there.
- 2026-09-01 FIXED the villager latch, the registered OPEN defect and the sole disqualifier on branch A's economy prize. Root cause: the narrow-passage traffic arbiter elects `Math.min(...cycle)` and admits only that unit, so in a continuously-replenished jam the high ids are never admitted - admission monotonic in id from 88% (2432) down to 0.6% (2460, twelve wins in 2,104 attempts), five villagers motionless up to 2,750 ticks, one holding a full 10-wood load it could not deliver. STARVATION, not deadlock: every unit moved sometimes, so a deadlock-breaker would have been the wrong fix. Geometry: a one-tile choke at (48,30) between trees at (48,29) and the 2x2 lumber camp at (48,31), eleven villagers head-on, five westbound to trees against five eastbound to a drop-off. Shipped fix keeps lowest-id as the ordinary rule and yields only to a unit that has not CHANGED CELL for 750 ticks. Six seeds, stuck villagers 26 -> 7, boot map preserved field-for-field; seed-7 improved 5250/12 -> 3500/5 but NOT cured, so it holds a second cause. TWO REJECTED ATTEMPTS carry the information: (1) a fair epoch rotation cured four seeds but regressed the boot map 0 -> 5 stuck villagers, rejected on the same bar that rejected branch A; (2) yielding on ticks-since-ADMITTED did nothing, because the arbiter is consulted ~44x per unit per tick and a frozen villager still collects sporadic admissions (12 in 40 ticks) that reset the clock - PERMISSION IS NOT PROGRESS, crossing a cell needs ~3.2 consecutive admissions. ALSO MY OWN INSTRUMENT ERROR, caught before publication: the first map I drew used `b.width` where the field is `footprintWidth`, so a 4x4 Town Centre rendered as one cell, and I had a complete chain ready to write from it (not sealed -> pathfinder is wrong -> bug is in A*). Verified by drawing both anchor conventions and counting units inside buildings: TOP-LEFT 0, CENTER 3.
- 2026-09-01 WHY THE ARMY FALLS, measured - and it is none of the three candidates round 2 named. Boot map, owner 2, 24k ticks, baseline vs branch A + mining-camp-first, two independent probes. Unit-identity ledger: baseline trained=9 lost=1 peak=8 feudal@9191; candidate trained=8 lost=6 peak=7 feudal@10751. The army is TRAINED AT PARITY AND THEN DIES - 8 vs 9 is a wash, 6 losses vs 1 is the whole effect - and the ledger reconciles exactly against the sampled probe for both arms (9-1=8 matches mil=8, 8-6=2 matches mil=2), which is what rules out reused unit ids. FALSIFIED: wood diverted to buildings (the candidate holds 985 UNSPENT wood, 98x baseline - it is not starved, it is not spending); villagers idle during relocation (22 villagers both arms, and the candidate leads on buildings 21 to 13); the age bought with food (MY OWN leading hypothesis, and the cleanest kill - NEITHER arm reaches Castle on the boot map, both stop at Feudal, so the 800-food advance never happened and cannot explain food 210 vs 885). Surviving explanation, NOT yet proved: siting the camp on the woodline medoid moves the economy onto exposed ground off the TC, which would explain why all three variants traded army for age rather than three separate tuning accidents. GATE DEFECT FOUND AND FIXED: `aiReachesCastleAge` rejected round 2 correctly and reported a mechanism that never occurred - "the age was bought by disbanding the army" was accurate for the wood-reserve-with-no-defensive-floor it was written for, and a misdescription of the candidate that next tripped it. Threshold UNCHANGED at 8; the message now reports the trained/lost split it measures. My own fix then failed its red-check - message and threshold were two separate literals and printed "below the bar of 8" against a forced bar of 99, the same staleness class - now bound to one PEAK_MILITARY_BAR constant.

- 2026-09-01 ROUND 2 of the lumber-camp search: the closest candidate yet, REJECTED by a gate I wrote earlier the same session. Branch A's work-anchor mechanism plus mining-camp-first ordering (which A proved by isolation removes the ten-villager deadlock) improves Castle-Age slots at both horizons on BOTH populations - measured seeds 4→5 and 10→11, and HELD-OUT seeds 2→3 and 6→8, clearing disqualifier 6 for the first time in the investigation. Boot map o2 qualifies 7,000 ticks earlier and reaches Castle 750 earlier with +4 building types. Rejected because `aiReachesCastleAge.test.ts` fails: "peak military 7 — the age was bought by disbanding the army: expected 7 to be >= 8". That assertion was written before this candidate existed, for exactly this shape; moving it to admit my own candidate is the first item on the playbook's audit list, so the bar stands. WHAT IT ESTABLISHES: the mechanism is real and replicates on populations it was never tuned against, and EVERY variant measured so far trades army for age (A alone 110→101 and 168→158; A+mining-first 110→102 and 168→169 aggregate but 8→7 on the boot map). THE GAP, exactly: nobody has explained WHY the army falls - wood diverted from military, villagers idle during relocation, or displacement residue - and that is a measurement, not another candidate. ALSO MY OWN ERROR, caught before it produced a number: I stashed the working tree while the candidate's held-out run was still reading it, which would have measured baseline under a candidate label - the exact hazard I had documented from branch B forty minutes earlier. Both runs killed and redone serially in one job owning the tree end to end.
- 2026-09-01 branch A (move the camp to the WORK) is the FIRST candidate in eighteen attempts to move the MECHANISM: anchoring the Lumber Camp on the medoid of trees villagers actually target rather than the tree nearest the TC gives camp usage 39.7%→51.9% (delivery ground truth, no tie-breaking), median haul 9→7, total gather time +7%, Castle owner-slots 4→7 at 24k and 10→14 at 34k, building types 156→175 and 176→190. Camps are currently planted a median FOURTEEN tiles from the woodline being cut; the candidate puts them 4.5. Disqualifiers 2,3,4,7 all cleared. DISQUALIFIED by 1, and not by its mechanism: the relocated camp evicts the Mining Camp onto the TC's shoulder and owner 2's economy stops dead at tick ~7,500 - all ten villagers on ONE cell (51,28), in `to-resource`, unmoving from t=9,000 to t=20,000, berries REGROWING because nobody eats them, never leaving the Dark Age. Isolated properly: request the Mining Camp first and the deadlock vanishes, so it is the DISPLACEMENT not the position. (51,28) is NOT sealed - both neighbours are open grass with a gold mine three tiles away. Registered as its own defect. AND I OVER-CLAIMED IT LAST TURN: my first probe counted villagers whose cell did not change, which includes a villager standing on a tree CHOPPING by design, and reported freezes on every seed up to 20,750 ticks. Restricted to `to-resource`/`to-dropoff`, HEAD is `seed-2` 4.2% of walking samples frozen ≥2000 ticks (longest 5,750) and ≤0.3% everywhere else - largely one seed's problem, not the widespread standing defect I implied.
- 2026-09-01 branch B (home range) BLOCKED with the blocker located: its benefit is a function of camp-to-TC SEPARATION and nothing in the design knows that. `corpus-seed-b` (camp 10 tiles out) camp usage 0%→36.6%, wood +72%, o2 from never qualifying to Castle at 22,250; `aoe2-prototype` o2 (camp 5 tiles out, already 75% used) camp usage 75%→69.8%, wood −19%, walking 73.3%→78.8% - splitting one forest between two OVERLAPPING anchors makes villagers walk more. Boot map regresses on five of six columns, so disqualifier 1. The unmeasured variant that should fix it - merge drop-offs within 8 tiles into one site - is named. TWO METHOD ERRORS OF MINE, corrected by the branches: (1) my contention warning was half WRONG - `bridge.step` is a fixed-step accumulator and the sim has no `Math.random`/`Date.now` (verified), and B reproduced the baseline EXACTLY under 106-process load with two byte-identical 34k controls, so contention costs wall-clock not validity; had a branch discarded good numbers on my say-so that would have been my error propagating, and the correct warning was the narrower one about runs not FINISHING; (2) fanning four branches at ONE working tree was a design mistake - B found a sibling editing `src/` underneath it mid-run and moved to private worktrees, with one collision artifact still reaching its patch. Round 2 issues worktrees up front.
- 2026-09-01 THE ROOT CAUSE MOVES ONE LAYER DOWN, superseding 5c31d4e8. The gather comparator ranks by MANHATTAN distance while movement is strictly 4-connected (`movementPlanOps.ts:236` passes no `allowDiagonal`; `civ-engine/src/path-service.ts:202` defaults it false - both verified myself). On open ground those agree exactly, so the metric is right there; around obstacles it is arbitrarily wrong, and a forest is a dense block of impassable cells. Measured on live targets: wood villagers work trees at Manhattan 3.6-10.1 whose TRUE walk is 2.6-45.1 tiles - on `aoe2-prototype` at t=24,000, three villagers were on trees at walk 37 and 35 while trees at walk 0 and 3 holding 100 wood each sat unused, all reading as Manhattan 8-9. THE PRIZE, over 16 owner-snapshots on four seeds (three held out): near-optimal assignment is worth 1.49x total gather throughput, and 93% of the headroom is WOOD (wood 2.61x, stone 1.22x, food 1.05x, gold 1.00x - gold sits in the OPEN and has nothing to gain, the cleanest confirmation that obstacles are the mechanism). Ablated: anchor-only 1.16-7.63x, TRUE-WALK-only 1.62-7.63x, both = metric alone - so the anchor defect I committed as the root cause is the SMALLER half, and all seventeen comparator attempts re-weighted a broken measurement. Corroborated by two shipped games: AoE2's own AI measures resource-to-dropsite and targets a 3-tile wood haul; 0 A.D.'s Petra builds a dropsite when walking/total exceeds 0.15 and ours is 0.66. RETIRED: camp placement is worth only 8-20% on top of good assignment, and the done-condition 'camp usage rises' is WRONG - under a true-path optimum the camp's share FALLS.
- 2026-09-01 CORRECTION, the Feudal wood ledger was wrong by an order of magnitude. Published repeatedly as "715 wood to buildings, 525 to 21 spearmen out of 1,240"; measured properly over nine seeds and eighteen owner-slots it is farms 63.6% (7,500 construction + 4,020 RESEEDS, which the old figure did not count at all) against qualifying halls 20.7% and spearmen under 10%. So military-vs-construction was never the contention that walls the AI. Worse: the AI FARMS WHILE FREE FOOD STANDS NEXT TO IT - `default-seed` o1 held 2,125 food of gatherable natural food within range of its own TC, FLAT from t=18,000, while wood never passed 58 and it built seven farms; a farm gathers 0.333 food/s against boar and deer at 0.408. Capping farm COUNT was built and correctly REJECTED: headline improved (Castle 4→7 and 10→13) while the ledger moved 5.6%, because soil costs 60 wood per 175 food whether built or reseeded - fewer plots, same labour, faster cycling, new-farm wood −23% returning as reseeds +25%. The gain was build-order sequencing, and it failed on held-out seeds (Castle 1→0). Two untested levers named: the ungated RESEED (22% of Feudal wood, fires with 1,600 food banked) and the comparator that prefers a 60-wood farm to free sheep ten tiles away.
- 2026-09-01 v0.3.174 the Indians' scout line gets its +2 attack vs BUILDINGS - a civ bonus `civilizations.csv` has always described and nothing implemented. Notable for how it was sourced: after retracting two unverified wiki claims, this value comes from the repo's OWN stats table, so it needs no external check. It rides `buildingAttack` on `CivBonusEntry`, a seam that already existed and is live at `attackDelivery.ts:131`. Narrowed twice and both stated: the Camel line does not exist in this roster so only the Scout line is covered (widens for free when camels arrive), and AoE2 makes it a TEAM bonus while `civBuildingAttackBonus` reads the attacker's own civilization - identical in a 1v1, narrower in a team game, the same limitation Kasbah carries. Red-checked by zeroing the bonus.
- 2026-09-01 RETRACTION: the Sultans and Carrack cost rows carried "(wiki: ...)" annotations and no wiki was consulted - the numbers came from model knowledge, and the commits said they were verified. The four techs before them WERE checked and two changed as a result (Shatagni is +2 range not +1; Couriers grants armour not speed), which is exactly why the repo has a lesson that a remembered game rule is a hypothesis. Writing a false source note is worse than writing none: it disarms the check for the next reader, because the row looks settled. Both rows now read UNVERIFIED with a sourcing note stating what was and was not checked, and why the repo cannot settle it internally - `technologies.csv` has no row for ANY expansion-civ unique tech. The effects are right in kind and the tests pass on the mechanism; what was wrong was the confidence attached to two numbers.
- 2026-09-01 v0.3.173 CARRACK (Portuguese, Castle, 200w+200g): all ships +1/+1 armour - the sixth expansion-civ unique tech, taking the roster to 25 of 41. Rides `unitEffect` with `applies: isWaterUnit`, the same shape Careening already uses for its pierce-only half, so the naval armour path is shared rather than reinvented. Same three guards fired again and were right again: the content ledger, the spec's unique-tech table and its count sentence. En route, checked whether `careeningPierceArmor` was dead code - it has NO consumer outside its own file - and it is not a defect: Careening is applied inline at `combatStateFactory.ts:206` and `technologyOps.ts:352`, so the exported helper is merely unused with its logic duplicated at both sites. Worth knowing as the two-tables-that-must-agree shape rather than fixing blind.
- 2026-09-01 v0.3.172 SULTANS (Indians, Castle, 400 gold): villagers gather gold 10% faster - the fifth expansion-civ unique tech and the first ECONOMIC one. Its effect lives in `GATHER_RATE_TECH_FACTORS` beside Gold Mining rather than gaining a field on `UniqueTechnology`, so it STACKS with the mining line as AoE2's gold techs do; a bespoke field would have handed one entry different arithmetic. FOUR GATES caught the change and all four were right: the unique-tech roster demands every technology have an effect (extended to accept the gather table as an answer - the question is "does something read it", not "does this record carry a field"), the content ledger had Sultans in its missing list, and the spec's unique-tech table plus its count sentence both had to learn about it. Tests cover the stack (1.1 x 1.15 x 1.15), resource isolation (food/wood/stone untouched - a resource-blind multiplier would read as a far bigger bonus than the wiki describes), and the cost row, because a technology that is offerable and free is how a menu and its validator drift apart.
- 2026-09-01 fixed the INSTRUMENT that produced the three wrong conclusions: `scripts/selfplay-audit.mjs` defaulted to 24,000 ticks, and the ages land at ~9,500 (Feudal), 21,500-30,000 (Castle) and 40,000-41,000 (IMPERIAL) - so any default under ~42,000 cannot see the game's last age and reports its absence as a defect. Default is now 45,000 with the reasoning in the file, and verified: run with defaults, `default-seed` o2 shows Imperial at 40,250 and `seed-3` o2 at 41,250, where the old default showed none. The comment names what the old one cost - a resignation feature designed, built, reviewed and reverted for a symptom that was partly a window artifact - so nobody lowers it back for speed.
- 2026-09-01 CORRECTION - three recorded conclusions were the SAME horizon artifact. The self-play verification at 34,000 ticks reported no slot reaching Imperial and decided matches running unresolved; both went into the record. Run to 60,000 on five of the same seeds: **Imperial IS reached** at ticks 40,250 (`default-seed` o2) and 41,250 (`seed-3` o2), and **two matches RESOLVE by conquest** - `delta` victory, `eta` defeat. Castle arrives 20,500-30,000, so a 34,000 window left as little as 4,000 ticks to bank 1,000 food and 800 gold AND raise two Castle-Age buildings. The game reaches ALL FOUR AGES and resolves matches. This also retro-explains the 2026-08-30 never-resolving-match entry and the resignation feature built and reverted for it: both were responses to 60,000-tick behaviour seen through a 34,000-tick window. `seed-1` o1 qualifies at 53,250 and reaches Castle at 54,750, so late arrivals are real rather than noise. Sharper rule than "use a longer horizon": a horizon is an instrument to be re-justified every time the measured thing changes - 34,000 was chosen to see the CASTLE age and is the wrong instrument for the IMPERIAL one.
- 2026-09-01 AI SELF-PLAY VERIFICATION at the corrected 34,000-tick horizon, ten seeds / twenty owner-slots: **10 reached the Castle Age, 0 reached Imperial**, totals 176 building types / 115 unit types / 168 peak army. Every slot reaches Feudal; Castle arrivals span 20,500 to 30,000. The best match answers whether the systems COMPOSE and it is a yes - `delta` owner 1 goes pop 11 / 1 military at t=8,500, to 26 / 4 at 17,000, to 35 / 8 with pikemen and crossbowmen at 25,500, to **75 population, 41 military and 23 buildings** at 34,000 against an opponent on zero units. Its peak army of 41 looked like an anomaly against a 3-12 norm and is not one: it is what winning looks like uninterrupted. What it does NOT say: no slot reaches Imperial, the ten that never reach Castle are the wall seventeen attempts failed on, and 115 unit types over twenty slots is under 6 per player against a 93-unit roster - the content gap in another form. Also another instance of the unresolved-match shape: `delta` o2 ends on 0 units and ONE building, so conquest correctly does not fire.
- 2026-09-01 HANDS-ON play session on `aoe2-prototype` (the loop's own acceptance test, last done 2026-08-29): boots to a DE-shaped opening - Town Center, villagers, sheep, forest, gold/stone, water, 200/200/100/200 in Dark Age. Verified by playing it: `H` selects and centres the TC; the command card groups Orders / Train / Research with correct costs and tooltips ("Queue a Villager at this building. Cost: 50 food. Needs an open production queue slot."); queuing two villagers shows both in a production queue with progress bars and deducts 100 food AT QUEUE TIME; villager selection offers stances, formations and a build card with costs and READY state; right-click issues an order (Idle to Moving, idle count 3 to 2); `.` selects the next idle villager and centres on it; the behind-building cue renders. THREE things I first mistook for defects and each was the instrument: the game looked FROZEN (tick 59 for five seconds, unchanged by `setPaused(false)`) because the Browser pane services requestAnimationFrame only when it CAPTURES A FRAME - a screenshot advances the clock, so the pane can verify state and interaction but never rate or pacing; `key text:'period'` sends the literal string rather than `.`, which made the idle-villager hotkey look broken; and clicking a command-card button does NOT strand focus - `document.activeElement` is back on the canvas immediately. Lesson landed for the first.
- 2026-09-01 v0.3.171 the behind-building cue's ENEMY colour was the half nobody ever looked at. `occlusion-showcase-fixture` spawned only owner-1 villagers, so every capture through three iterations of the feature was the FRIENDLY blue - and the enemy colour carries the answer to the question the cue exists for. Coral shares a hue family with every roof: scored as the SMALLEST distance to any building material (a cue is only as legible as its worst background) it manages 76 against thatch, where the blue manages 105. Magenta reaches 137 and stays 192 from the blue. Fixed on both sides: the fixture now seats an ENEMY-owned occluded unit beside the friendly one behind the SAME Town Center so both appear in one capture, and a new test pins the PROPERTY not the hex - worst-case distance to any building material plus minimum separation between the two - red-checked against the coral at 76 vs a bar of 90. INSTRUMENT CORRECTION inside the fix: my first probe used a hand-picked palette subset and reported the blue at 97; against the real VOXEL_COLORS it is 67 from `waterGlint`, but a silhouette is only painted over BUILDINGS, so the correct population is building materials and the true figure is 105 - the test names that subset explicitly. Found and fixed a THIRD instance of the load-dependent browser class while shipping it: the monk/relic gold test measured 28 ticks against a 20-25 tolerance band whose own comment named the cause (the view's frame loop advancing between `evaluate()` calls). Pausing makes the count EXACTLY 20 - stronger than the band it replaces and load-independent. Three specs now fixed by the same move, against the register's prediction that only 4 of 34 pause.
- 2026-09-01 the expansion-civ tech gap is a DATA gap, not a code gap - correcting BOTH earlier framings. `civilizations.csv` does name the unique units and techs for all 30 civs, but `technologies.csv` (age, host building, cost, build time, effect) contains NONE of the 20 remaining expansion unique technologies, nor any of the four already shipped. A grep that appeared to find one was a false positive - "Double Bit Axe" matching a `^Double` pattern meant for "Double Crossbow". So closing this is per-technology research sourced and verified outside the repo, not mechanical translation against existing rows - which is why two of the four shipped corrected values memory had wrong. Remaining: 20 techs, 17 units, several additionally blocked on effect vocabulary the engine lacks (regeneration, splash, trade and tribute seams) and five on units that do not exist. INSTRUMENT NOTE, fifth of this class in two days: the probe's first run reported ZERO missing because it searched the header for `unique unit` while the file uses `unique_unit`, so both column indices were -1 and every cell read empty. It now asserts two known-present names resolve true and a fabricated one false before reporting.
- 2026-08-31 closed a CRASH CLASS both gates passed clean: `@typescript-eslint/no-use-before-define` was never configured, so a `const` arrow used above its own definition is a runtime ReferenceError that `tsc` accepts and eslint never checked. It bit for real during attempt sixteen - every gather assignment threw and the AI stopped gathering, with typecheck AND lint green, caught only by a probe reporting zero gatherers. Enabling the rule (functions exempt, since the codebase relies on hoisting for mutually-recursive helpers) immediately reported 24 EXISTING violations across 13 files, so the class was live. Seven genuinely restructured; seventeen marked at the site with a disable naming the register entry - deferred-execution, unaudited individually, and marked rather than quietly excluded. Three touched files crossed the 500-line cap because a per-site suppression costs a line; two were brought back by doing the real restructure instead, which is what the constraint should produce. Verified the rule BITES: a fresh two-line violation is rejected.
- 2026-08-31 seventeenth attempt: weighting the round trip finds NO interior optimum, and the fix stays unshipped. With the routing gate identified (the reference drop-off is resolved from the VILLAGER, so the Town Centre is permanently the reference and no tree near a camp is ever preferred), the direct fix ranks candidates by the haul each implies. Pure haul improves the aggregate at both horizons but costs the boot map 3,000 ticks to Castle. Pricing both legs as approach + w*haul, swept at both horizons over ten seeds: w=1 returns Castle slots to exactly baseline (4 and 10) and w=2 falls BELOW it (3 and 9), with peak army down in every weighted arm - so the approach term does not buy back the boot map, it cancels the benefit. Only the unweighted extreme moves the metric. CONCLUSION: ranking is the wrong lever for this gate. Any ranking that makes camp-adjacent trees attractive enough to use also makes distant trees attractive enough to walk to, and a scalar weight cannot separate them; the fix must distinguish MIGRATING to a camp's neighbourhood from choosing within the current one, which is a home-range or assignment-time question. Two earlier attempts guessed at a home-range filter and changed nothing, so that needs the tracing treatment that found the gate, not a third guess.
- 2026-08-30 THE ROUTING GATE IS IDENTIFIED, by tracing the decision instead of theorising a third time. On corpus-seed-b o2 at tick 16,000 the villagers stand at (50,23); `findNearestDropOffBuilding` is asked from the VILLAGER's position so it returns the Town Centre at (48,24), 3 tiles away, not the Lumber Camp at (57,25), 9 away - and every candidate is then ranked by distance to that Town Centre, best available d6, while the camp sits on trees within 3 of itself. So: the reference drop-off is chosen by proximity to the VILLAGER, villagers cluster round the Town Centre, the Town Centre is therefore permanently the reference, and no tree near a camp is ever preferred. A camp can only serve a villager already standing beside it - which is exactly why building a SECOND one changed nothing. Also caught a flaw in my own probe: `servedByCamp` broke ties by array order, and a tree equidistant from Town Centre and camp is common, so "camp usage barely moved" is not trustworthy as stated (the boot-map regression that caused the revert is an outcome metric and unaffected).
- 2026-08-30 the obvious gate for the TDZ crash class was CONFIGURED, MEASURED and REJECTED. `@typescript-eslint/no-use-before-define` (functions: false) gives 24 violations repo-wide, 11 in `src/game/simulation` - and every one inspected is the SAFE idiom, a function body referencing a `const` declared lower in the file, which runs after module init (PERSIAN_WORK_RATE_BY_AGE, EMBEDDED_TABLE, the UI controllers' `api`/`rafHandle`). The rule cannot separate those from the case that actually crashed, where the helper ran immediately in the same synchronous flow - so adopting it costs 24 refactors of correct code for a signal that is mostly noise and would not specifically gate the hazard. Recorded as a rejected gate WITH its cost so a future session does not re-derive it.
- 2026-08-30 sixteenth attempt: the FIRST whose primary metric improves at BOTH horizons (Castle slots 4-to-6 at 24,000 and 10-to-11 at 34,000, building types up at both), and REVERTED anyway because the boot map pays for it - `aoe2-prototype` o2 takes the Castle Age at 28,250 against a baseline 25,250, three thousand ticks LATER, with unit types 8 down to 5. That is the exact shape of the Dark-Age tuning this repo already adopted and withdrew. More important: the MECHANISM is still not understood. Camp usage barely moved (21/67 wood villagers served by a camp before, 20/73 after) under TWO separate theories of the gate - the ranking comparator, then the home-range filter - each implemented, each changing nothing. So the Castle gain came from somewhere other than what it was aimed at. Also found a latent CRASH CLASS: moving the haul-cost helper put a `const` arrow below the filter that called it, so every gather assignment threw a TDZ ReferenceError and the AI stopped gathering - with typecheck AND lint both GREEN, because `no-use-before-define` is not configured. Only a probe reporting zero gathering villagers caught it; a reviewer had flagged the same hazard earlier the same day on another file.
- 2026-08-30 STRUCTURAL CAUSE FOUND: the AI builds a Lumber Camp and then does not use it. At tick 16,000 across five seeds, SIX OF THE NINE owner-slots owning a camp have ZERO wood villagers served by it, hauling a median 8-11 tiles to the Town Centre instead; where the camp IS used the haul is 4-7. On corpus-seed-b o2 the camp sits at (57,25) with 6 live trees within 3 tiles and 17 within 6, and all four wood villagers work trees 10 tiles from the TC. Cause: `assignNearestResource` ranks candidates by proximity to the drop-off nearest the VILLAGER (its own comment calls this "an accepted one-lookup approximation"), so a villager standing near the Town Centre ranks trees by TC proximity and never migrates to the camp - the camp only serves villagers already beside it. The deposit building IS re-resolved correctly at haul time, which is why this never surfaced as a wrong-destination bug: they walk to the right drop-off, they were sent to the wrong TREE. This is the structural cause the fifteen tuning attempts were working around - it explains the 20% gathering share, the 0.77 wood/s gross, the seventeen minutes to afford 325 wood, and why a SECOND camp changed nothing (a second camp cannot help when the first serves nobody). Fix shape: rank by the round trip a candidate implies - its distance to its OWN nearest drop-off - not to whichever drop-off the villager happens to stand near.
- 2026-08-30 the approach walk is NOT pathological, closing the last cheap hypothesis on this wall. Wood-villager task shares on corpus-seed-b o2 (ticks 12,000-20,000): to-resource 42.3%, to-dropoff 37.7%, GATHERING 20.0% - four fifths of the life is walking. But effective movement while walking is 0.259 fine/tick against a nominal 0.320, i.e. 81% OF THE CLOCK: congestion and re-pathing cost about a fifth and no more. There is no large defect hiding in movement; the shape is that a round trip costs four times the gathering it enables, which is a map-and-clock property and precisely what the honest §12.4.2 speeds bought. Instrument error caught en route, the recorded one AGAIN: the first probe reported ZERO walking ticks because `EconomyState.villagers[]` has no `id` field, so `getComponent(v.id, ...)` asked about undefined for every villager - a query structurally incapable of finding anything. Fixed by reading `units[]` and cross-referencing the gatherer component; printing the object's shape is what caught it.
- 2026-08-30 fifteenth attempt REVERTED, and it retires a whole CLASS. Measuring gross wood separately from spend in Feudal: gross ~0.77/s (~1,100 wood across the age) against spend ~0.90/s, while the two qualifying buildings cost just 325 - so they lose the competition for wood, and v0.3.169 already protects them from MILITARY. Extending that reserve to FARMS, measured at both horizons: 24,000 gives 0 Castle slots against baseline 4; 34,000 gives 5 against 10. Catastrophic, both horizons agreeing on direction (the two-horizon method working as intended). The reason is the correction: starving farms starves FOOD, and the Castle advance costs 800 food against the 325 wood being protected - the qualifying buildings lose the competition to something ALSO required for the same goal, so "this is a misallocation" is false. The allocation is roughly right; the economy is short. RETIRED AS A CLASS: every fix that moves wood between claimants. Military-to-buildings was the only one that helped, farms-to-buildings harms, the weights were already swept. What remains is raising the wood a villager-second produces.
- 2026-08-30 WHERE THE CASTLE-AGE DELAY ACTUALLY GOES, now that the corrected horizon makes arrival times decomposable: for the ten owner-slots that reach the age across ten seeds at 34,000 ticks, Feudal-to-QUALIFIED averages 10,225 ticks (17.0 min) and qualified-to-Castle averages 3,800 (6.3 min) - so 73% of the delay is putting up the two prerequisite buildings, not banking the 800 food and 200 gold. Those two buildings cost 325 wood (Blacksmith 150 + one of Archery Range/Stable/Market at 175), so seventeen minutes is a NET rate near 0.32 wood/sec against a nominal 2.73 for the seven villagers on wood. That is the wood starvation the very first instrument found, now attached to the outcome it causes and priced. The target is Feudal wood INCOME - the gathering efficiency, not the allocation, which already went 4-to-6 in v0.3.169 - and NOT the build order, which is what twelve of the thirteen attempts changed.
- 2026-08-30 THE FEUDAL WALL IS HALF A MEASUREMENT ARTIFACT. With no code change, the same ten seeds give 4 of 20 owner-slots reaching the Castle Age at a 24,000-tick horizon and 10 of 20 at 34,000 (building types 156-to-176, unit types 89-to-115, peak army 110-to-168). Half of all slots reach the age; they arrive after minute 40. "The AI cannot leave the Feudal Age" was never true as stated - it was true of the window I was looking through, and every one of the twelve attempts was judged through it. Also: attempt THIRTEEN was a no-op and falsified its own premise. Review had proposed keeping the Mining Camp in the Dark Age but gating it on a mine being within DROP_OFF_ANCHOR_RADIUS; measured in both arms at both horizons, the results are BYTE-IDENTICAL, because the gate never fires - a mine is always in range when the camp is wanted, so it is never planted away from ore. The Town-Center-fallback hazard carried forward from the second-lumber-camp revert is LATENT on these maps, not live. Nearly corrupted the comparison by stashing the working tree while the first arm was still reading it; caught it, restored, and re-ran both arms inside one job that owns the tree end to end.
- 2026-08-30 twelfth Feudal-wall attempt REVERTED, and it invalidates the instrument the other eleven used. Moving the Mining Camp out of the Dark Age gave Castle slots 4-to-6 over ten seeds with the boot map reaching the age at 22,250 where HEAD never did; six gates green. Review: (1) the insertion sits after a loop demanding ALL FOUR halls, so moved-to-Feudal is in practice NEVER BUILT - camps built 25/34 owner-slots at baseline vs 4/34, all four in the last 17% of the match, and a separately-built never-build arm is byte-identical on 10 of 12 slots: the rule that shipped is the one its own comment called indefensible; (2) the win is a HORIZON ARTIFACT - Castle 7-to-9 at my 24,000 cut-off, 18-to-16 at 34,000, five slots losing an age they otherwise reach; (3) it creates a NEW STALL CLASS, since the insertion is not age-gated and an unplaceable camp now blocks the whole Castle-Age order. My own claims wrong too: the 790-gold/440-stone figure is ONE cherry-picked slot (range 11-1,670 across 34) and the change RAISES the median hoard it was written to stop paying for; and my prose and my test file stated building types moving in OPPOSITE directions. LESSON LANDED: a measurement horizon is part of the instrument - all twelve attempts were judged at 24,000 ticks, close enough to when matches resolve that any change trading later outcomes for earlier ones scores as a pure gain. Next: keep the camp in the Dark Age but gate it on a mine actually being within DROP_OFF_ANCHOR_RADIUS.
- 2026-08-30 tenth Feudal-wall attempt: a win MADE OF ITS OWN BUGS, reverted. A second Lumber Camp anchored away from the first took THREE seeds from 1 Castle-Age slot to 3, reproduced exactly, passed all six gates, no boot-map regression. Review (12 seeds) found two defects: a null anchor fell back to the TOWN CENTER, where a lumber camp shortens no carry at all (`canDropOffAt` already takes wood there) — 100 wood for nothing; and the second lumber camp DISPLACED the mining camp (both 100 wood, lumber asked first, Dark-Age arm the only place either is reachable), mining camps 17→15 and slots with NO gold/stone drop-off 7→9, permanently. FIXING BOTH DESTROYED THE BENEFIT: ten seeds / 20 slots gives Castle 4/4/2 for 1/2/3 camps, with unit types 89→85→83 and peak army 110→99→98. The three-seed win was the bugs. ALSO WRONG, all mine: my approach figures do not reproduce (median 9→10 and p90 12→13, not 5→10 and 9→23 — my p90 only appears in a TICK-WEIGHTED sample, length-biased since a long walk contributes one sample per tick) and I never committed the instrument, so they were unreproducible; "a second camp gained nothing at first" is false at scale (count alone gives 7 vs 5); and the mechanism in my comments is not the one implemented (camps build only in the DARK AGE at ticks 5,900-10,275, and the anchor is bounded to 12 tiles of the TC, so it can never reach the woodline I described). All three tests were theatre — served radius 1 passes, and 1000 passes, which would plant every camp on the TC. THE ACCIDENT POINTS SOMEWHERE REAL: displacing the mining camp helped because it spends 100 wood on WOOD rather than gold and stone, which this AI banks unspent all game (790 gold, 440 stone against 20 wood at match end) — next hypothesis is that the mining camp is a poor buy here, found only because fixing a bug made a result worse.
- 2026-08-30 WHERE THE 25% GOES, 459 samples: villager->target median 6 / **p90 20 / max 61** tiles; target->dropoff median 6 / p90 9. The HAUL is healthy — carry 10 at 0.385/s is ~26s of chopping against a 6-tile haul (~7.5s each way), predicting ~68% efficiency. It is the APPROACH tail: at p90 a villager walks 20 tiles to reach its tree, 25 seconds, as long as chopping the load it came for; worst case 61 tiles. One trip in ten costs more walking than it returns. This also rewrites the failed camp experiments: a villager takes the nearest AVAILABLE tree, so once the near woodline is out the nearest is far, and the AI cannot plant a camp at the new woodline like a player because it owns ONE for the whole game and cannot afford a second — the camp is the instrument for cutting the approach tail, and the wood to build it is the same wall from the other side. Next target is neither an age timing nor a resource weight: cut the p90 approach from 20 tiles.
- 2026-08-30 `corpus-seed-b` measured (the audit's weak seed): owner 1 ends with **food 1,895, gold 790, stone 440 and WOOD 20**; cumulative income over 20k ticks is 1,300 wood, all of it spent as it arrives, never approaching the 150 a Blacksmith needs. Of 7 villagers on wood, **2 gather at any moment** — 3 walking out, 2 back, ~25% of nominal. Found a real divergence from AoE2: the AI builds **exactly ONE lumber camp and one mining camp per game** — `missing()` means 'owns none' AND the camp branch sits in the DARK-AGE arm of the picker, unreachable afterwards, while a DE player builds a camp per woodline. Closing it does NOT help: a second camp changed nothing (cannot afford the 100 wood), camps past Dark Age moved one seed by a single building type, no Castle either way. Both reverted — eight measured negatives on this wall. Every lever moves wood between things that all need it; none creates any. Next candidates are on the GATHER side, and the number to beat is 25%, not an age timing.
- 2026-08-30 `npm run ai:selfplay-audit` makes the standing acceptance test a COMMAND. It had been rebuilt ad hoc a dozen times this session, and one rebuild silently unioned both owners so it could not show whether any single player qualified for an age — I nearly concluded from it. Reports PER OWNER, never merged. Current honest state over three seeds x 24,000 ticks: **1 of 6 owner-slots reaches Castle, 0 reach Imperial**; totals 50 building types, 26 unit types, peak army 37. `corpus-seed-b` is the weak spot (neither owner qualifies) — the regression the Castle-Age work knowingly accepted.
- 2026-08-30 v0.3.170 THREE expansion-civ unique technologies land — Shatagni (Hindustanis, Hand Cannoneers +2 range), Recurve Bow (Magyars, Cavalry Archers +1 range/+1 attack), Farimba (Malians, cavalry +5 attack). The gap was mis-framed by an earlier audit as '~41 unwritten CSV rows': the CSV is COMPLETE for all 30 civs, and the gap is in the CODE — all 19 original civs done, the 12 expansion civs missing 17 unique units and 22 unique techs. Measuring it took four attempts and three LIED: a `types.ts`-only grep called the Jaguar Warrior missing (it is in 20 files), and a whole-tree grep through `subprocess` did the same because Windows STRIPPED THE QUOTES from the pattern, so `'[a-z0-9-]+'` matched bare word fragments like `olves` from "Wolves". Only a Python extraction VALIDATED against two known-present and two known-absent names held up. Values verified against the wiki, not memory — which would have been wrong twice (Shatagni is +2 range, not +1; Couriers now grants armour, not speed). Also found: Slavs' Orthodoxy was REMOVED in Dynasties of India. Five registration points; the exhaustive HUD formatter switch caught the one I missed, and the spec-table architecture test caught the count sentence, whose regex read "Twenty-two" as "two". FOURTH tech added (Kasbah, Berbers — team-scoped in AoE2, owner-scoped here because the train-rate seam reads one owner's set; stated in code). And the remaining gap is now a CHECKED LEDGER (`expansionCivContentGap.test.ts`) asserting the missing set EQUALS the list, so closing one forces a visible edit. It caught its own author immediately: my ledger called Howdah and Pavise missing, and a text scan said they existed — because a howdah is an elephant platform and a pavise is a SHIELD, two voxel render part names colliding with two tech names. It now asks `RESEARCH_COSTS` and `TRAINABLE_UNITS_BY_BUILDING` instead, with both false positives pinned as assertions. Four wrong measurements of one quantity in a single sitting. ALSO root-caused a flake I had earlier written off as contention, validating the fog-memory entry's own prediction the same day: the combat spec aiming at a MOVING enemy failed twice in the suite and passed 4/4 alone because the sim was UNPAUSED — the view's frame loop advances the world in real time, so extra ticks pass between reading the target's position and clicking beside it, as many as load allows; its 4-attempt retry was compensating rather than fixing. `setPaused(true)` removes the wall-clock dependency; suite 135/135. Measured population for the class: only 4 of 34 browser specs pause.
- 2026-08-30 match-resignation fix ATTEMPTED, reproduced-broken by review, REVERTED. Conquest needs every unit AND building gone, so a player holding a Town Center with no units and too little to train anything ran forever. My rule resigned such a player — and review reproduced it declaring a LIVING player dead: `enqueueTraining` debits food at ENQUEUE and creates the unit 250 ticks later, so a player who just correctly bought eight villagers has zero food and zero units, and the match ended "All enemy forces have been eliminated" against a standing Town Center. Against a human that is a false DEFEAT. Three unit-free income routes (market conversion, relic trickle, ally tribute) also falsify "can never act again", and I priced with `trainingCost` where the charge is `effectiveTrainingCost` (a Hindustanis villager is 46). MY grace period rested on MY false claim that the longest fixture horizon is 8,000: `aiReachesCastleAge` runs 24,000 — a test I wrote the same day — plus 14,000/21,600/13,100 elsewhere and 30,000 for `npm run playtest`, and because `step()` no-ops after an outcome the break would have been SILENT. Reverted because proving a position TERMINAL means enumerating every income route and that enumeration rots; a future attempt wants a POSITIVE signal of activity instead of an argument from absence. AND THE SYMPTOM HAD GONE: after v0.3.169, `default-seed` o1 holds 24 units/17 buildings at 30,000 ticks where it used to end with ZERO — the economy fix stopped the loser being wiped out at all, so this was downstream of it. ONE REAL FIX SURVIVED, and it was already live: `scripts/aiTickRate.mjs` and `aiTrajectory.mjs` loop on the tick with no outcome check, and `step` no-ops once a match resolves — so either spins forever the moment one side is wiped out, which conquest does today without any resignation rule. Red-checked on `conquest-victory-fixture` (resolves at tick 117): with the guard, exits 0; without, killed at 40s. `runPlaytest` had the check all along, which is why nobody noticed.
- 2026-08-30 the browser gate is GREEN again — `fog-memory-fixture`'s first assertion root-caused and fixed. The fixture's scout has no wander bounds but does not stand still: it auto-engages the enemy house four tiles away and steps off (10,10), taking that house out of its own radius-4 vision, so a three-tick warm-up asserted the house was in LIVE vision at a moment it had already left. Browser: `isMemory` false at boot, true after ONE extra tick. Node sim: false at 0-3, true at 4-6, false at 12. They disagree because boot advances further in the browser — which is why a fixed warm-up count could not be right in both, and why it passed in a warm suite and failed cold. Warm-up removed; visibility is already computed at boot. Ruled out by measurement first: not the AI change (reverted to HEAD, still failed 3/3), not the stale `vite preview` server squatting on 4173 all session, not the v0.3.167 registration order, not the v0.3.167 wander ceiling. First fix attempt REVERSED the symptom — removing the warm-up passed in isolation and failed in the suite — which is the proof no fixed tick count works. Premise moved to a sim test where boot IS deterministic; the browser spec keeps only what a browser can answer.
- 2026-08-30 v0.3.169 the AI finally gets past Feudal — boot map becomes CASTLE-AGE ELIGIBLE at tick 23,500 having never qualified at all, `default-seed` reaches the age itself at 24,000 with 12 building types vs 7. `agePrerequisiteWoodReserve` holds the next qualifying building's wood back from military, paired with Feudal wood 4→6; each half red-checks independently. REVIEW (40+ matches, 11 seeds) found the first version had no defensive floor — 21% of all peak military lost, and on wood-poor seeds a 100% training ban that never afforded the building either. Its suggested floor (`attackGroupSize`=5) makes the reserve a NO-OP, because `militaryGrowthPausedForAgeUp` already stops growth at 5 and the drain is entirely below it in loss-replacement. Swept 0/2/3/4/5: floor **3** costs nothing — peak military identical to baseline (37), +6 building types, +2 unit types, and no boot-map regression on any metric. Also fixed from review: qualification predicate (Khmer), owner-discounted price (Malians), completed-only counting, the qualifying list now DERIVED not restated, an identity assertion, and three wrong numbers in my own docs.
- 2026-08-30 seventh Feudal-wall experiment, also reverted: pair the wood reserve with a WOOD-FREE unit (militia, 60f+20g, both banked in the hundreds) so the army grows out of surplus while wood accumulates. Measured: peak military HALVED (8→4 on two seeds) and the Castle was LOST — militia's 60 food buys fewer units than the spearman's 35, so trading a wood cost for a food cost bought a smaller army and spent the food the age-up needs. Reserve-alone remains the only config that has ever produced a Castle; its scorecard is one large win (default-seed o2: qualifies 20,000, castle 21,500, bldgs 7→9, units 4→5) against two small losses including one on the BOOT MAP, which this repo's adoption rule refuses. Unadopted; remaining work named: sweep the reserve's SIZE and DURATION, not another single-shot.
- 2026-08-30 FEUDAL WALL ROOT-CAUSED by instrumenting the decision instead of guessing a fifth time: the AI picks a build, has a builder, capacity and a valid anchor, and CANNOT PAY — `afford=false, wood=0..24` against a 60-wood farm, 164 times. Spend tally over 20k ticks: 715 wood on six buildings and **525 on 21 spearmen**; 1,240 total is 0.62 wood/s against a nominal 2.73 (23% efficiency, the rest walking), and military takes 42% of it. Designed in by an expired comment — `aiSystemProductionPhase` says "military is the only pre-age-up food/gold spend (builds spend wood/stone)" and a SPEARMAN COSTS 25 WOOD — so military and construction compete unarbitrated, the 150 for a Blacksmith never accumulates, the AI never qualifies, and `ageUpReserveCost` stays empty so it banks 980-1922 food it cannot spend. Adding the next qualifying building's wood to that reserve put default-seed o2 into the **CASTLE AGE at t=21,500**, the only Castle in this whole investigation. NOT adopted: costs a unit type on the boot map, and gating it to an established economy loses the Castle everywhere — the reserve must bite early. Also: at 60,000 ticks every number is identical to 24,000, and a player with 0 units, an unusable TC and 43 food is still `outcome=running` — a decided match never resolves.
- 2026-08-30 fourth attempt at the Feudal wall, also REVERTED: `ageUpReserveCost` protects the age-up RESEARCH but only once the owner already qualifies, so nothing protects the cost of BECOMING qualified — the AI defends a purchase it cannot make and spends the money for the one it can. Made the farm wait unless the wood also covered the next qualifying building (six tests, red-checked, wired through the caller). `aoe2-prototype` gained a Blacksmith; `default-seed` lost a building; `corpus-seed-b` lost its farms entirely. Reserving for ALL still-needed prerequisites moved default-seed to 10 types and cost the other two their farms. No seed reached Castle either way, and starving food to fund a hall is not a trade worth making. Four hypotheses, four measured failures. Also caught an INSTRUMENT flaw before it became a conclusion: the exercise metric unions both owners, so it can never show that a single owner qualified — the next attempt needs the per-owner metric first.
- 2026-08-30 WHY self-play never leaves Feudal, measured: the winning AI ends a 40-minute `default-seed` match with **food 1571, gold 1140, stone 750 and wood 38, holding NO Feudal buildings** — it has several times the 800f/200g Castle costs and cannot take it, because the advance also needs two Feudal buildings at 150-175 WOOD each and wood never exceeds ~50 on any seed, either side, at any sample. Two obvious fixes tried and BOTH reverted: more wood villagers (7→9) changed nothing on any seed, so wood INCOME is not the constraint; and holding the farm gate to its floor made `aoe2-prototype` worse, because a farm is CONSUMED so the count falls back below any floor and the branch re-fires anyway. What is left is wood SPEND — houses plus endless farm replacement — and `ageUpReserveCost` being empty until the owner already QUALIFIES makes the state stable rather than transient. Registered OPEN with the acceptance metric a fix must use: what a match EXERCISES across three seeds, not Feudal timing, which both failed hypotheses left untouched.
- 2026-08-30 AI SELF-PLAY AUDIT (the goal's own acceptance test, never actually run before): a 40-minute both-seats-AI match on `aoe2-prototype` RESOLVES — owner 2 out-economies owner 1 from ~t=12,000 and destroys it, 15 pop down to 3 against 25, 71 net losses — so combat, production and the age race all work end to end. But NEITHER player leaves Feudal in forty minutes, and the whole match exercises 4 unit types (militia, scout, spearman, villager) and 8 building types. No archery range, stable, blacksmith, market, monastery, university or castle; no archer, knight, siege, monk or ship. Against 137/138 live technologies and 93 trainable units, self-play touches a small fraction of the game — so it does NOT currently verify feature completeness, which is what the goal asks of it. The AI's build order stops at the Dark-Age four. Largest remaining gap between 'the game has these features' and 'playing it exercises them'.
- 2026-08-30 behind-building silhouette: ATTEMPTED, MEASURED, REVERTED. The cue is opaque and reads as a unit standing ON the building; making it translucent looked right in the showcase capture (0.09% diff, confined) and does not survive measurement. Three independent failures, all found by review: under `moebius`, the art style the game actually SHIPS, the tone-band quantiser snaps 24.1% of the cue byte-identical to opaque (every screenshot I judged by was `painted`); a translucent part that overlaps ITSELF blends twice, solved per-pixel as 452px at 0.55 / 68 at 0.7975 / 6 at 0.9089, so 14% of the body drew wrong; and the ENEMY coral shares a hue family with every roof, its background contrast falling 204 to 82 while the friendly blue held — and no fixture spawns an enemy-owned occluded unit, so every capture I judged was the blue one. Engine has the cure for the seams and does not export it: recorded as an ask in engine-feedback. Defect registered OPEN with the measurement method the next attempt must use. Review also found a THIRD stale metrics pin I missed, so the browser suite was red at 1/134 while I read a piped tail as green.
- 2026-08-30 REMOTE GATE BLOCKED, NOT RED-FROM-CODE: every run since ~09:56 fails before it starts with GitHub's "The job was not started because recent account payments have failed or your spending limit needs to be increased". Both jobs die in 2-5 seconds with that annotation and no log. `npm run ci:status` will report red at session start and the canon says a red remote gate is the next task — this one is not actionable from the repo. The last commit whose CI actually EXECUTED is e6c73556, and its one real failure (a Windows timeout) is fixed in b666ff06. b666ff06 is verified locally across all six gates (3,054 sim tests, 135 browser, corpus, typecheck, lint, build) and has never been executed remotely.
- 2026-08-30 v0.3.168 three spec-conformance gaps from a tech-completeness audit, all code-vs-spec rather than content. A DOCK did not count toward Feudal because §7.2 still said "this land-only slice has no Dock" — stale since the naval slice, and §7.3 lists the Dock as a Dark Age unlock in the same document, so the spec contradicted itself and the code followed the stale half; a water opening of Dock+Mill could not age up. §7.2's "2 Castle-Age buildings OR 1 Castle" had only the counting half, so a player who committed to a Castle and nothing else was locked out of Imperial. And the Incas team bonus (farms +50% build speed) was in neither the implemented set nor the deferred list — found by auditing `civilizations.csv` against the code rather than against what I remembered deferring. The Incas fix ships with an end-to-end test that builds a real farm and compares completion ticks, because the audit found this class by looking for table rows whose effect nothing reads, and a constant plus a unit test is exactly that shape (red-checks 200 vs 200). The audit's biggest finding — twelve civs with no unique unit or tech — is a DATASET gap (~41 unwritten CSV rows) and is left as such. REVIEW: the Castle fix was HALF-applied — the predicate learned the rule and the error MESSAGE did not, so a blocked player was still told to build a second building; two of my assertions were direction-only where the exact number was free (the Incas E2E passed with a 1.01 multiplier, and the message test passed with the alternative removed because the candidate list already contains the word "castle"); two controls could not fail; and I evaluated a civilization-map scan for every builder every tick one commit after halving tick cost. Also: WINDOWS CI went red on the previous push — `selectionActivity` timed out at 30s, 8s here, no headroom on a slower machine, tipped over by my new tests competing for cores. Fixed on both sides and my tests re-measured and shortened where the signal allowed.
- 2026-08-30 v0.3.167 wildlife retaliation + a scout that had stopped scouting + the Deer. A completeness audit found two spec-§5.6-required entities unreachable in real matches: deer did not exist in `src/` at all, and wolves were implemented but never spawned. Before placing a wolf where a player would meet one I measured what happens when it bites — the villager died and the wolf finished UNTOUCHED, because auto-aggression queries `('position','unit')` and wildlife are resources, so a wolf is structurally invisible to it. Fixed that first. With retaliation in, scouts killed their wolf and then FROZE: the wander box is declared in cells and enforced in fine units, and the far edge converted as `max * RESOLUTION` — the FIRST fine slot of the last legal cell — so a scout on that edge could not move inside its own final cell (3 cells in 1,200 ticks, for the rest of the match). Reproduces with no wildlife on the map; three corners fixed, the fourth registered OPEN rather than accommodated. Deer ship as an entity (140 food, §6.3 boar rate, voxel recipe, flee system that breaks diagonally because fleeing straight makes a herd a moving wall that corked a scout's box for 900 ticks) but NOT into map generation: deer on generated maps break the canary stranded-scout return, and I eliminated the flee system, proximity, RNG perturbation and terrain painting without finding the cause. I also edited that test three times to accommodate my change and reverted all of it — the test is not the problem. REVIEW then returned 'do not ship as-is' and was right twice over: the retaliation as first written REPLACED the victim's whole command (build refs, trade state, gather progress) and made the spec's own boar lure unplayable, and a deer could not actually be HUNTED — 10 food in five minutes against the 0.41/sec the rate table promised, because gathering happens at adjacency 1 and that is permanently inside the flee radius. It also proved three of my tests were theatre and that the corner gate did not gate (all three corners cleared the bar on the unfixed build). All fixed, each red-checked against the exact measurement, and the register/changelog claims corrected rather than rewritten. Its one efficiency finding moved the headline: `syncResource` bumped the structural revision twice per re-sync, flushing the whole passability memo an extra time on every moving resource, and fixing it took the same measurement point from 7.99 to **5.45 ms/tick** — 15.13 before any of this.
- 2026-08-30 simulation tick cost HALVED, 15.13 -> 7.99 ms/tick at 34 units on `aoe2-prototype`, and the scaling curve itself flattened (44 units now cost less than 34 did before: 8.97 vs 13.11). Profiled with a new `scripts/profile-selfplay.mjs` because tick cost was climbing toward the 100 ms budget at DE-sized populations. `getCellStatus` was 32% of CPU, all of it from `isCellPassableForSpawn` on every pathfinding neighbour. The FIRST fix — making that predicate allocation-free — gave NO win (15.13 -> 15.77): self time says where the process is, not why, and the cost was the call count, not the work inside. Memoising it on `structuralRevision` did it, sound because the predicate reads bounds/terrain/buildings/resources and never units (which crowd without bumping the revision). The critic earned its keep twice: the soundness argument had a WRITE half I never wrote down, and `syncUnit` was violating it (releasing a recycled id's building/resource claim without bumping — a permanently stale `false`), and the equivalence sweep was theatre, proved by mutating the invalidation away and watching all 9 tests stay green. Now `releaseClaims` is the single choke point, non-integer coords are refused rather than silently aliasing another cell, and a mutate-then-requery sweep red-checks against both the never-invalidating mutant (4 fail) and the exact syncUnit regression (1 fail). Also corrected: `getCellStatus` did not leave the profile, it fell to ~5%.
- 2026-08-29 boot-map freeze: narrowing PARTLY RETRACTED. 'All six villagers idle with null targets' was a single-tick sample — over 1,000 ticks owner 2 holds a target 77% of the time vs owner 1's 100%. I hit the one-tick trap one iteration after writing the warning about it. Solid: assignment never takes a no-target branch (200 ticks instrumented), and every `targetResourceId = null` site was tagged — only 3 fired, all normal post-deposit clears. So targets are set and kept, yet gathering is 8% and transit is against far targets while near nodes sit free. Next: measure assignment DISTANCE at the moment of assignment, owner 1 vs owner 2.
- 2026-08-30 v0.3.166 AI drop-offs go to the RESOURCE, not the Town Center — `aiSystemBuildingPhase` anchored every building at the TC (bootstrap lumber camp at the builder's feet), so camps sat a median 5 tiles from what they serve, one mining camp at 16 and a mill 52 from the nearest berry. `dropOffAnchorFor` now anchors them at the nearest harvestable resource of the kind they serve. Camp→resource median 2 (lumber) / 3 (mining); worst-owner Feudal across three seeds 17,200 → 14,100; boot-map owner 2 gather share 51% → 59%. Cost recorded: default-seed owner 1 12,100 → 14,100. Measurement corrected two guesses mid-fix — mills must NOT anchor on sheep/boar (herded to the TC; a wandering sheep won the contest), and camp→nearest-surviving-resource is confounded by depletion.
- 2026-08-30 re-measured the villager time budget AFTER the fixes (the 32%-gathering figure was taken on the broken build): owner 2 now gathers 51% with 1% idle, against 32% gathering / 36% idle before — the idling is gone and what remains is walking. Also verified the reviewer's cross-owner `gatherTargetCounts` note is LATENT: 0 of 360 samples across three seeds had any resource targeted by two owners, so it is registered with its trigger condition rather than fixed speculatively.
- 2026-08-30 verified in the REAL browser build (not just sim tests) that the opponent now plays: on `aoe2-prototype` at tick 12,000 (game clock 20:02) the AI holds Feudal Age, 16 villagers, 8 military, and a town-centre/lumber-camp/mining-camp/mill/barracks/farm/4-house base. Its base renders BLACK from the human seat — fog correctly hides an unscouted enemy — so the census is the evidence and the fog behaviour is itself confirmed. The human seat sits idle at 3 villagers because no orders were given, which is correct.
- 2026-08-29 v0.3.165 CORRECTS v0.3.163 — the distance-based approach budget was recomputed each tick, so it shrank as the villager closed and fired at ~56% of any walk: below 11.5 tiles it gave LESS reach than the flat 80 it replaced (3.9 tiles into a 7-tile walk vs 6.4). It also used Euclidean distance where movement is Manhattan, collapsed to its floor on the destination cell, and could exceed the 600-tick backstop — and its test asserted the Euclidean model, certifying a property the game lacked. Now flat but expressed in TILES (32 → 400): no frozen player on any of three seeds and default-seed owner 1 back to 12,100 from the 15,800 my fix caused. I had flagged the shrinking-budget risk when commissioning the review and shipped without testing it.
- 2026-08-29 v0.3.164 also adds the gate that can SEE a frozen player — `checkEveryOwnerAgeProgression` (every LIVING owner must age up; eliminated exempt), opt-in via `requireAgeForEveryOwner`, enabled on a new `boot-map-both-owners` corpus row for `aoe2-prototype` where both owners now reach Feudal at 9,300/11,900. It could NOT go on the default-seed row: owner 1 needs 15,800 there, and raising that budget to 18,000 crashes the harness (`JSON.stringify(result.bundle)` → `RangeError: Invalid string length`, an undocumented bundle ceiling). Red-checked end to end by requiring castle-age and watching the corpus exit 1.
- 2026-08-29 v0.3.164 ships stop sailing through Fish Traps — the water shortcut returned passable for ANY water cell on a comment claiming water holds no buildings, stale since the Fish Trap shipped. Auditing the class found a SECOND instance in the same file (`isCellPassableForSpawnInDomain`, ships could be placed on a trap); both now consult `isCellBlockedByBuilding` — buildings only, since fish are water resources a fishing ship must still reach. New `fish-trap-blocks-fixture` forces the question with a one-cell channel, and its open-lane control is what stops a fix that merely makes ships refuse water. Both line-neutral: the file sits one line under the 500 cap.
- 2026-08-29 v0.3.162 loaded villagers no longer idle — tracing ONE owner-2 villager (the instrument the register named) caught it standing idle 211 ticks holding a full 10 food: `assignNearestResource` set `idle` at both no-target branches and the idle→assign path re-ran forever, stranding the carry. Now hauls first (`parkOrDeliver`), TDD'd. Owner 1's Feudal on the boot map 16,000 → 12,500 ticks; owner 2 STILL frozen, so the register entry stays open with this cause crossed off. Aggregate task shares said time was missing; one traced round trip said where.
- 2026-08-29 OPEN defect on the boot map — all-AI on `aoe2-prototype`, owner 2 never leaves the Dark Age by tick 24,000 (owner 1 ages at ~16,000). Sharpened by measurement to an INCOME defect: food never exceeds ~50 across 10,000 ticks with 7-10 villagers working, against a 500 age-up. Ruled out: raids (nearest enemy 15-50 tiles, never inside RAID_RADIUS 8), wildlife (damage path instrumented, zero kills logged), boar gather-assignment (component-level zero), auto-aggression (wildlife not in the `unit` query), sheltering (twice in 10,000 ticks). The first version of this entry claimed ten villagers were killed — wrong, and retracted: `getEconomyState().units` comes from `query('position','unit')` and garrisoning removes `position`, so shelterers vanish from the count. Next instrument is the food ledger, not another body count. QUANTIFIED: owner 2 gathers 32% of villager-time vs 62% on a healthy seed, walking 53%, idle 9% — with drop-offs placed BETTER than the winner's (mill at 2, berries 3 away, haul 1, while owner 1 ages up with sheep 41 away), role-flips at 2-4 per 10k ticks (the no-inversion guard holds), and the Dark-Age gold villager confirmed deliberate. Next instrument: trace one villager's complete round trip.
- 2026-08-29 hands-on DE opening on `aoe2-prototype` (fog, default zoom) — mechanics all sound (TC key, queue, tasking, scouting, stance card). Three suspicions, two false on measurement: the scout's 'Idle' was arrival, and the 'clipped' selection title sits 30px inside its panel (it is the panel's fade, low contrast). One real: behind-building silhouettes are solid saturated fills, so three of them across the TC's upper tier read as villagers standing on the roof — position and occlusion are correct, the FILL is the issue, and a third iteration should be an outline (the colour already has a recorded rationale).
- 2026-08-29 Dark-Age tuning adopted and WITHDRAWN — a 16-row sweep on `default-seed` said 6:3 improves both owners (9,400/12,100 vs 10,100/12,200), but on `aoe2-prototype` (DEFAULT_SEED, the map players boot) it is 800 ticks WORSE, and across five seeds it helps two and hurts two; the mechanism claims were seed-local too. Reverted to 4:3. Kept: `DARK_AGE_TUNING` + a pin test, and a harness that now sweeps several seeds, ranks on the WORST owner, fixes an `Infinity - Infinity` NaN comparator, and defaults to 20k ticks so a regression cannot read as a tie.
- 2026-08-29 AI Dark-Age investigation — two hypotheses measured and BOTH falsified: raising the villager cap 10→22 moved Feudal LATER (13,000 vs 10,100; aging up is a race to a threshold, not an economy optimisation), and villagers are not walking the Dark Age away (62% gathering / 22% out / 13% back / 0% idle, camps correctly planted). The 10,100 falls out of ten villagers at a 57% food split under §6.3 rates — arithmetic, not a defect. Closing the DE gap needs cap and resource weights swept TOGETHER, which wants a harness rather than another one-off probe.
- 2026-08-29 REMOTE GATES GREEN — CI on ubuntu AND windows plus playtest-corpus, ending a two-month outage (200 red runs since 2026-07-07). Third cause found while fixing the first two: a workflow was not in its own path filter, so a CI fix could never trigger the CI that would prove it, and `playtest.yml` had the same gap for the corpus config it reads — both closed, with `workflow_dispatch` added. Windows took two attempts because the first fix moved the symptom one line rather than removing it.
- 2026-08-29 first green CI job in two months (ubuntu) — the two months of code behind the stale pin was sound; the outage was the pin. Windows needed the SAME tar fix applied to `-C` (a drive-lettered path is mangled wherever it appears, not just in `-f`). Corpus progression gate: measured owner2 Feudal at tick 10,100 and owner1 at 12,200, so the 8,000-tick budget was hot-economy-era, not a stall — raised to 13,000 with the measurement recorded in `progressionCheck.ts`. Took three probes to get there (an ignored `allAi` flag, then villager counts misread as a stall); the AI's 16m50s Feudal is itself slow against DE's ~11-13m and is the next real piece of work.
- 2026-08-29 CI outage — main's remote gate had been red for 200 consecutive runs since 2026-07-07, every push behind a green local gate (five of them mine, this session). Two causes in the shared voxel-fetch step: the `.github/voxel-commit` pin sat at 0.1.4 while the code needs 1.2.0's stylized-resolve API (ubuntu), and GNU tar read `$RUNNER_TEMP`'s `D:` as host:path (windows). Pin advanced, archive paths made relative in all three workflows, and `npm run ci:status` + a session-start line make reading the remote gate a command instead of an intention.
- 2026-08-29 v0.3.161 gates a player BUILDS finally open for them — `admitsThroughGate` read completion as "has no construction record", true only of scenario-seeded buildings, so every built gate stayed shut to its owner while every fixture gate passed; audit found it the only shape-inferred completion read. Second half: gate completion opens a route without claiming a cell, so it now announces `notePassabilityChange()` or v0.3.160's unreachable-plan cache holds the pre-gate verdict (measured, not theorised). Reviewer caught both files over the 500 cap and a no-op foundation test; the repaired owner-at-its-own-foundation test turns out to be pinned by OCCUPANCY, not by the completion read — proved by hardcoding the read true and watching it stay green.
- 2026-08-28 v0.3.160 movement clock to §12.4.2 (0.32 fine/tick = villager 0.8 tiles/s, per-unit movement_rate percents, everyone on the carry) — honest walks exposed a tick-8300 collapse (12.3 s/tick): AI farm walls sealed its own drop-offs past the margin-boxed connectivity guard (now whole-map flood), sealed carriers re-ran full-map failing A*s per candidate per retry (now cached on occupancy structuralRevision), and the traffic election's per-branch Set copies went combinatorial over a 16-villager door ball (now iterative BFS); dropoff-convoy fixture pins door throughput; stone pin re-calibrated to deposit quanta (teleport-clock math retired).
- 2026-08-27 v0.3.159 economy paced to spec §6.3 (all 8 gather families ±5%, pinned; was 4.5-10x hot) + AI rebalancer no-inversion guard (slow cadences exposed a ping-pong that froze stone at 50 for 12k ticks); triage ledger: 14 tests re-budgeted for DE pacing, unblock-fixture mover respawned so the walk outlasts a 26-tick chop, feudal-stone horizon calibrated (125 stone ≈ tick 4,800; past ~7k ticks AI growth makes ticks several-x dearer); DISCOVERED movement speed ~4-5x hot (next unit); leftover 20k-tick tmp probe in tests/ was the fake suite hang.
- 2026-08-27 v0.3.158 hands-on DE-opening playthrough audit (8 probes, real clicks) + default zoom 2.0→1.2 (base+resource ring framed, DE-like); found: raw gather cadences 4.5-10x spec §6.3 by family (food 2.5/s designed, ~2.4/s measured effective, vs 0.31 target — NEXT major fix); critic caught lint red behind a tail-piped gate (npm run verify now exists), a probable villager death misread as a list anomaly, and an unverified berries-under-HUD claim (softened), Esc-opens-menu vs DE F10, tall-TC voxel column intercepts ground clicks behind it; confirmed working: train/queue, gather loops, stances/formations, boar aggression, edge-scroll, occlusion silhouettes.
- 2026-08-27 v0.3.157 tech-tree viewer (menu button + F1; model from hosting tables + civDenies so the view can't disagree with validators; own uniques in, others' out; denied = dimmed+struck) — F2 collision with debug overlay caught by the existing browser suite; createApp split (dePlayHotkeys.ts); Franks screenshot verified.
- 2026-08-27 v0.3.156 feel-loop iter 2: Space = jump-to-last-home-attack (audio controller records the home-hit cell its horn already detects; replay-mode arbitration mirrors Esc since the registry is first-match) + setup preselects Normal speed.
- 2026-08-27 v0.3.155 feel-loop iteration 1: DE hotkeys H/,/F3/+- (idleMilitaryOps bridge twin; pause via pauseControl toggle; view.adjustSpeed ladder) — 4 real-key browser tests; found arrows/WASD/edge-scroll/minimap-pan already existed; live comma test needed an out-of-sight fixture (auto-aggro empties the idle cycle where soldiers spawn in view).
- 2026-08-27 v0.3.154 per-set wall merlons (pure architectureMerlonBoxes; west identity) — the architecture-set family CLOSES; fortress-map visual sweep on Saracens/Aztecs clean.
- 2026-08-27 v0.3.153 Japanese CA +2 vs ranged (except skirms; civ-side clause in the anti-class thread) — the sourced bonus audit's modelable remainder is now CLOSED; open D-class (Feitoria/Caravanserai/herdables/dynamic monk HP) recorded in the audit trail.
- 2026-08-27 v0.3.152 per-age combat ladders (Malian pierce +1/2/3, Teuton melee +1/2 via factory+sweep; Goth anti-building +1/2/3 at delivery w/ attackerAge threaded, flat table entry retired; Berber villager 1.05 dark->1.1 feudal via movementSpeedPercent age) — breadth test split at the 500 gate (civBonusRounds.test.ts).
- 2026-08-27 v0.3.151 batch 8: Korean wood-component discounts (new unit-cost woodMultiplier), Goth instant Loom + Viet eco x0.5 + Persian TC/dock work-rate-by-age (research AND train through the team-time seam, technologyType threaded), Mayan longevity (swing depletes /1.15, farms excluded) — live twin proves the node outlasts.
- 2026-08-27 v0.3.150 age-aware construction prices (effectiveConstructionCost age param via ownerConstructionCost funnel): Franks castles -15/25 by age, Britons TC wood -50% Castle+; three stale pins migrated (sim x2 + browser HUD wood 425->562).
- 2026-08-27 v0.3.149 Spanish builders x1.3 + +20g per completed tech (BOTH queue completion sites — first landing missed the entry.kind path and the live pin caught it) + Inca villager-armor Castle gate.
- 2026-08-27 v0.3.148 HP ladders sourced (Vikings flat 1.2 Feudal+, Viet flat 1.2, Franks MOUNTED scope Feudal-gated, Mongol scout 1/1/1.2/1.3, Portuguese ships 1/1.1/1.15/1.2) — three flat table entries moved into the age seam so creation + age-up sweep stay one mechanism.
- 2026-08-27 v0.3.147 research-cost seam (effectiveResearchCost at charge/validator/toast/agent-card/AI-budget incl. age-up reserve; hosting-derived scopes): Chinese age ladder, Italians dock+univ+age, Byz Imperial -33%, Turks gunpowder -50%, Spanish smith no-gold, Viet eco no-wood.
- 2026-08-27 v0.3.146 bonus round 3 (Franks foragers, Slav monk speed, Goth full age ladder w/ Dark rung, free-tech catch-up Byz/Teuton/Viet); free Town Patrol broke the 'Byzantines are bonus-free' fixture assumption (mangonel wandered when the enemy TC saw farther) -> neutral-civ pins swept to Saracens; retired GOTHS_INFANTRY_COST_MULTIPLIER.
- 2026-08-27 v0.3.145 bonus round 2: civReloadMultiplier hook (8 civs' attack-speed signatures) + Britons +1/+2 range ladder + Incas food-component discounts, all re-derived on age-up by ratio/delta; enabling fix = fletching live-apply OVERWROTE range from base table (erased civ/team bonuses); reloadTicks joined the economy unit view.
- 2026-08-27 v0.3.144 civ-bonus source audit round 1 (~30 corrections vs current DE across civBonusTable/team files; agent-audited, spot-verified vs dataset help texts; DE-dead entries removed incl. Teuton TC attack/LOS + Saracen CA-siege + free Loom/gold-mining; team constants split per civ; Chinese farms flat+45 -> x1.1); CSV texts + spec 908 re-synced; batch B (reload-seam attack speeds, Britons range ladder, Incas food discounts) queued.
- 2026-08-27 v0.3.143 per-set door forms (pure architectureDoorBoxes table; 5 recipe sites; west identity) — before/after/diff on 3 sets: Britons 0.00% (byte-identical), Saracens 0.36%, Aztecs 0.20%, all confined to door regions; visual sweep clean.
- 2026-08-27 v0.3.142 seats 3+ get real default civs (classic AoK cast; no more hole-less bonus-less 'Player N') + playtest harness --civ flag; two lessons landed (helpers satisfy outcome assertions; no git checkout mid-red-check).
- 2026-08-27 v0.3.141 shift-queued ENTITY orders (per-unit serialized chain via the live context router; generic completion watcher + in-pass villager pop with explicit-flag drop at depletion-when-chain-waits; wipeThen at the recorded-handler layer) — red-check caught two vacuous tests first (auto-aggro killed both victims; fixture spread + outpost sight fixed it).
- 2026-08-27 v0.3.140 Tracking+Cartography retired per current DE (allies share sight from boot; barracks/market cards shrink; infantry LOS = sourced DE values, 19 corrections, scout line kept age-folded); Portuguese team bonus -> research x0.8 via the existing teamResearchTimeMultiplier seam; near-miss: almost double-folded +2 infantry LOS before checking the dataset's militia value.
- 2026-08-27 v0.3.139 denial rows SOURCED: fetched aoe2techtree game data (53-civ current DE), name-matched units (regional-variant ids), diffed all 30 rows (~380 corrections; every civ had errors), 3 recorded roster exceptions (Persians paladin/Savar, Incas eagle+militia/Champi, tracking+cartography global); re-pinned 6 test files (Japanese/Celts/Koreans/Persians/Franks).
- 2026-08-27 v0.3.138 per-civ tech-tree denial (tech-tree.csv 30 rows, one namespace; civDenies filter at the three options choke points so HUD/AI/validators/placement inherit one filter; fails open) + CSV↔embedded identity gate; 12 test files civ-pinned (Huns/Persians/Saracens/Aztecs patterns), browser suite green; critic round fixed 7 (meso stable building, vacuous bracer test, de-fanged menu sweep, unfiltered TC visible list, permanent-hole messages, fails-open fiction, 6 CSV row errors) + DE eagle-scout opener.
- 2026-08-26 v0.3.137 structures gate (28/28 clean on arrival; tower upgrade rows via the multiplier chain) — the content-differential family complete.
- 2026-08-26 v0.3.136 base-stat harvest (militia-line HP −5×3, elite LB range 8, siege-ram atk 4, petard/trade-cog armor un-transposed, scout Feudal +2 via the age-scaled seam hoisted above the civ guard) + the third differential gate; audit trilogy complete — hosting, bonuses both directions, base stats, all machine-locked to the CSVs.
- 2026-08-26 v0.3.135 class armor (UNIT_CLASS_ARMOR, per-class floored subtraction in armorClassBonus; cataphract/turtle/mameluke/hull/ram entries; ram melee −3 in the base table) + Siege Engineers ×1.2 building damage (the CSV ram figures were the TECHED values — v0.3.134's parse over-read them; bases restored 125/150/200, gate grammar taught the row); armor_bonus column now gated too; naval smoke clean (19 cmds, 0 rej).
- 2026-08-26 v0.3.134 the 46-entry attack-bonus harvest (naval fabric WHOLE — galley/fire/cannon-galleon lines had zero bonuses; turtle-ship + cavalry-archer classes; HC's phantom +2 siege was a bonus the CSV never gave; ram building values) + the permanent CSV differential gate with named deferrals and the one bombard exception.
- 2026-08-26 v0.3.133 Chemistry to the University + the hosting DIFFERENTIAL promoted to a permanent gate (tests/content/technologyHosting.test.ts: every CSV row's building vs RESEARCHES_BY_BUILDING, alias + intentional-absence lists both self-checking); free techs follow hosting via the menu automatically.
- 2026-08-26 v0.3.132 Sappers corrected (villagers not infantry, Castle not Blacksmith — the CSV is the spec of record; the combat fixture's militia became a villager); §1143's leftover Siege-Workshop sentence fixed.
- 2026-08-26 v0.3.131 University hosts guard-tower/keep/siege-engineers (two "University does not exist yet" divergence notes and a stale HP-deferred note retired; three fixtures gained universities; edited the WRONG imperial fixture first — registry, then file).
- 2026-08-26 v0.3.130 audit harvest: war-elephant class (cavalry-stacking, +92 halberdier swing), conquistador rides (MOUNTED_UNITS), scout anti-monk ladder live; the convert-vision fixture's scout swapped for militia — the predicted entanglement arrived exactly as the old deferral warned, two years of comments outlived by their roster.
- 2026-08-26 v0.3.129 eagle armor class (the HC-vs-eagle inversion restored; 14 dropped CSV anti-eagle rows live; isInfantryUnit keeps eagles in tech scope — vulnerability ≠ upgrade scope again); found by the off-roster-comment audit that v0.3.128 triggered.
- 2026-08-26 v0.3.128 siege-onager blast 1.5 (the blast table's "off-roster" comment outlived the roster — El Dorado's twin; diagonal-reach proved in the pure blast test).
- 2026-08-25 v0.3.127 monastery finials per set (abstract silhouettes replace the cross outside the default; fn lives in details for the 500 cap).
- 2026-08-25 v0.3.126 shift-queued construction (queuedBuildRefs chain; queue flag rides building.placeConfirm; advance at the finalize site — builderWorkStep's clear was the WRONG hook, the system's post-finalize clear was the real one; constructionStartOps extracted at the 500 gate).
- 2026-08-25 v0.3.125 shift-queued waypoints (queuedTargets on the move command; append rides the recorded unit.move stream — a direct codec write would have desynced replays; first red tests were vacuously green TWICE in one day → mechanism asserts added); trade-route order extracted for the 500 cap.
- 2026-08-25 v0.3.124 ambience (wind bed + LCG birdsong; audio north-star line COMPLETE); Islands naval smoke on v0.3.122 clean (23 cmds, 0 rejections).
- 2026-08-25 v0.3.123 selection chirps (role-family two-note figures; selection-id diff is the observable — no new facade state needed).
- 2026-08-25 v0.3.122 continuous repair charging (repairAccrual codec, hundredths; charge-before-apply so a broke owner stalls; start free; Tier-1 44); Goths stale deferral note retired after teamProductionBonuses audit.
- 2026-08-25 v0.3.121 per-verb work arcs (activeVerb names the work: chopping/mining/foraging; pick double-strikes shallow, forage reaches low).
- 2026-08-25 v0.3.120 TC crowns per set (dome/finial/spire/cap/crest replace the square cap; crown fn lives in details to hold the recipes file under 500); deep playtest on v0.3.117 clean (43 cmds, 1 honest wood rejection).
- 2026-08-25 v0.3.119 death collapse (corpse = unit's own recipe, ease-in topple, hashed heading, gear dropped) + capture-script exactness fix (pause at boot, screenshot paused — transients were structurally unphotographable before; three captures proved three successive diagnoses wrong until the clock was fixed).
- 2026-08-25 v0.3.118 order-ack click (facade gesture counter, same live-only pattern as the bell tally).
- 2026-08-25 v0.3.117 attack-ground (unit.attackGround command; dead-target isArea projectile = pure blast at the cell; G arming; order never self-clears); first bombard test was a FALSE POSITIVE off auto-aggression until the empty-cell test caught the missing executor branch; 500-cap compactions (formation-order helper, view wrappers).
- 2026-08-25 v0.3.116 bell peal (live-facade ring tally as the audio observable — cosmetics never become sim state).
- 2026-08-25 v0.3.115 town bell (ring/back-to-work as building.action; greedy nearest-shelter fill; ungarrisonBuilding gains a villager-only filter so soldiers hold the wall); v0.3.114 Delete key devlog folded here.
- 2026-08-25 v0.3.114 Delete key (entity.delete command; ownership validator; one-per-press; routed through the combat destroy ops); two exhaustiveness guards (pinned-units oracle, pendingCommandQuery) fired on the new variant exactly as designed.
- 2026-08-25 v0.3.113 gathering work animation: activeVerb widened to gathering (gatherer.task source); the stationary-start transition needed an explicit setComponent diff or the projector never re-read the verb; selectionStateOps split (entityReadProbes) for the 500 cap.
- 2026-08-25 v0.3.112 control groups persist (controlGroupsCodec joins Tier-1, inventory pins 42→43); recall's generation prune covers pre-save deaths free.
- 2026-08-25 v0.3.111 per-set roof silhouettes (RoofGeometry heightScale/insetScale/layerDelta in steppedRoof; insets only shrink so footprint invariants hold; accents ride the returned top); four-set capture sweep confirms distinct shapes.
- 2026-08-25 v0.3.110 audio slice 2: research ding (monotonic count diff, pre-seed snapshot) + countdown bell (edge-trigger).
- 2026-08-25 v0.3.109 procedural audio slice 1 (horn/age-up/match stings, WebAudio synth, lazy context on first gesture, mute persists); exposed getRecentUnitAttacks on both bridge facades; extracted selectionRecallHotkeys and caught the stale-bridge capture (pass bridgeRef, not bridge — createApp swaps bridges on load).
- 2026-08-25 v0.3.108 architecture walls: plaster/plasterLight re-key per set centrally in the building part adder (roofs keep their site-level consult); default map 0.33% diff = shimmer only.
- 2026-08-25 v0.3.107 mechanical-unit repair: villagers mend siege+ships (armor-class-derived predicate, building-repair economics, pursuit via findUnitRangePlan, 'repair' command with unit ref); test's 360-HP magic number was the wrong table — parameterize on getEntityHealth.
- 2026-08-25 v0.3.106 allied gate passage (gateAdmits + teams map through cellPassability); gates fixture gained a third idle party after the allied 2-player variant ended by conquest on tick 1 and froze the walk test.
- 2026-08-25 v0.3.105 per-civ architecture pass 1: six roof-material sets keyed off civ (sim projects `architecture`, fog memory + saves carry it, replay resolves real civs), renderer re-keys thatch/tile/ridge before owner blend; diff-proved delta confined to roofs.
- 2026-08-25 v0.3.104: control groups (Ctrl+digit bind / digit recall survivors / double-tap centre; refs-based, selectByRefs prunes the dead; session-transient scope spec-noted; real-key browser spec).
- 2026-08-25 v0.3.103: idle villager bell (count + round-robin '.'-key/button select-and-centre; idleVillagerOps extraction at the 500 gate; pointer-events:auto opt-in — #hud-root is none; browser spec clicks the REAL DOM button per the unwatched-seam lesson).
- 2026-08-25 v0.3.102: browser suite caught the day's one regression — garrisonUnit's unconditional clearSelection wiped the HUMAN selection whenever an AI garrisoned (visible only after v0.3.89's eligibility widening; setter-trap forensics) — fixed as removeSelectedEntity(unit); plus ally right-click=walk / Ctrl=explicit attack, and the pointer->view seam that had silently dropped iso+garrison args; two stale specs re-pinned (fragile house 40/900, Viking imperial berserk 66).
- 2026-08-25 v0.3.101: Heavy Plow farmer-carry clause (+1 base carry on farms, composing with Wheelbarrow like the civ carry bonuses) — the last deferred CSV clause in the farm family.
- 2026-08-25 v0.3.83–100 digest (each line's full story is in git + the detailed devlog): owner-coloured roofs; Teuton/Ethiopian/Goth/Korean/Japanese/Vietnamese/Khmer/Incas civ+team lines with fixtures and red-checks; DE garrison rules + garrison-scaled tower volleys; honest building HP (CSV debut rows) + per-age military-building ladder; damage states (<40% burns) + construction progress fill (HP ramp IS the fraction) + BUILD env for staged captures; Nomad (FOREST_PATCHES fix) + Islands with aiFerryPhase (BFS reachability, ferry pre-empts building spend); AI tribute; Redemption building conversion + monk range ladder; armor-class taxonomy predicates (War Wagon two-class lesson); Heavy Plow carry; ally-click semantics + forceAttack + the pointer-seam repair (Alt/garrison had NEVER worked live); idle villager bell (pointer-events opt-in lesson); quit→setup pin.
- **Ten combat-stat lines, and the 500-line gate forced the right surgery.** LOS bonuses land at BOTH unit-vision sites (trained-unit creation and the upgrade sweep — one site alone would give upgraded knights the wrong eyes); ranges at the combat-state factory (a Korean mangonel is BORN outranging, proved in-match against a plain one); the class-attack extras (Saracen/Indian vs buildings, Persian knights vs archers) are computed by the command loop and threaded into the delivery params. Wiring them pushed `playerCommandsSystem` to 513, and the honest fix was the one that has been due for days: the 230-line ATTACK branch moved verbatim to `attackCommandStep.ts` (one role — advance an attack one tick), taking the system to 281 and the step to 311. Full suite + combat browser specs green on the extraction. Gates: 2798 tests, typecheck, lint, build.

## 2026-08-25 (team bonuses — the column comes alive, v0.3.81)

- **Thirteen team-bonus lines, one membership predicate.** `teamHasCivilization` is true for the owner and its allies, so a side of one still enjoys its own (AoE2's rule — every bonus 1v1-testable). Seams: relic ticks (Aztec +1 per relic every 3rd tick — fractional rates in an integer economy become deterministic cadences), trade payout (Spanish, at load-time pricing), farm capacity incl. reseeds (Chinese), populationProvided (Slavs, at the ONE derivation both raw-supply sites read), production speed at enqueue (Britons/Goths/Huns by building, Turks by gunpowder unit, Malians university research), construction cost (Mayan walls, Viking docks — `ownerConstructionCost` now also feeds the HUD, so the card quotes the true team price), heal cadence (Byzantines, interval 10→7), conversion resistance (Teutons ×0.5 STACKING with Faith to ×0.25, AoE2's composition), shared sight (Portuguese = Cartography on from Dark Age, as the flag it actually is). **The blob-swap trap resurfaced in my own test:** a civ swapped into a SAVE cannot exercise a creation-time bonus — raw supply is persisted history; the test boots fresh with `civilizationsByOwner` and says why. Gates: 2796 tests, typecheck, lint, build.

## 2026-08-24 (building bonuses + houseless Huns, v0.3.80)

- **One seam, nine call sites, and the HUD shows the owner's price.** `effectiveConstructionCost` is the training-cost rule applied to construction — charge, validator, rejection detail, AI afford (3 sites), farm reseed (a Teuton farm reseeds at its build discount), the agent option list, and the build card + tooltip all agree, because the canon says a button never costs more than it says. Persians' 2x TC/Dock HP multiplies the base BEFORE Masonry's ladder (the unit-HP order). The Huns' houseless population is a rawSupply floor at the match cap seeded at boot — deriveCap still clamps, so custom pop caps bind (tested at 75). `getPlayerCivilization` joined the public bridge for the HUD's sake. Gates: 2791 tests, typecheck, lint, build.

## 2026-08-24 (opening bonuses — deltas, carry, housing, v0.3.79)

- **Three more §9.2 families, each guarded against fixture damage.** Starting deltas apply ONLY where `start.startingResources` is unset, so every fixture stays byte-identical (a fixture that pins 500 food keeps 500 food whatever civ it names); extra units (Chinese +3, Mayans +1, the Inca llama-as-sheep) spawn through the same safe-spawn search as the scenario roster; carry bonuses add BEFORE Wheelbarrow's multiplier (AoE2's order); housing bonuses land at BOTH raw-supply sites so the cap never disagrees with itself. The 500-line gate forced `scenarioSeedOps` (510) to shed the whole civ-opening concern into `civOpeningSeed.ts` — the split IS the feature's shape. Gates: 2788 tests, typecheck, lint, build.

## 2026-08-24 (free civilization technologies, v0.3.78)

- **The grant asks the MENU, so the free path can never outrun a player.** `freeTechnologySystem` (every 25 ticks, cheap early-outs) checks each waiting free tech against `getResearchOptions` for the owner's standing completed buildings — the same function the HUD's buttons and the queue validator read — then applies through the same idempotent `applyTechnology` a paid research uses. Age gates, prerequisite chains (Bow Saw needs Double-Bit Axe), and building requirements (Franks farm upgrades wait for a Mill; Slavs Tracking waits for a Barracks — tested) all live ONCE. Twelve civs, 24 grants, zero new rules. Red-check: void the apply → Aztec Loom never lands. Gates: 2784 tests, typecheck, lint, build.

## 2026-08-24 (civ-bonus breadth — the five-civ cap superseded, v0.3.77)

- **The if-chains became a table, and seventeen civs got their identity.** `civBonusTable.ts` (the uniqueTechnologies pattern: entries as data, one loop per seam) now carries every CSV bonus line the six seams express — gather rate, unit HP, anti-building, train time, cost, and a NEW movement-speed seam composing with Squires/Husbandry where a technology would. **This superseded a standing decision:** the 2026-07-05 five-civ cap, recorded properly in decisions.md — the cap was already false in spirit (16 civs of unique units, 19 unique techs) and the concentration it protected has happened. The remainder (free techs, starting deltas, age-scaled HP, carry, building stats) is LISTED in the spec per family, not silently dropped. Two of my own test expectations were wrong before the code ever ran (70×0.75 and a 31.4999 float-round) — the pure-seam tests caught my arithmetic, which is their job too. Gates: 2780 tests, typecheck, lint, build.

## 2026-08-24 (Coastal, Fortress, Gold Rush — the map roster grows, v0.3.76)

- **Six of §5.4's eight, each with a checkable identity.** Coastal: one sea on the southern edge, dry interior (the test walks the whole bottom row and the whole interior). Fortress: an 11-cell wall square per start with a gated centre-facing face and a standing Castle — the FIXTURE VALIDATOR caught the first version's Castle anchored on a boar and its walls eating berry patches; walls now fell trees on their line (`removeSpawnAt`), skip rarer collisions, and let the map edge serve as the wall where a corner start's square is clipped. Gold Rush: nine neutral double-rich gold nodes dead centre. Arabia is the standard generator under its AoE2 name — and the first test claiming byte-identical terrain was wrong on purpose to learn: same generator, own seed, so the SHAPE is the claim, not the noise. Islands and Nomad stay deferred with written reasons (AI can't ferry, AI can't open TC-less). Gates: 2771 tests, typecheck, lint, build, three map captures.

## Older work (compacted 2026-08-27 — every title lives on in git log + docs/devlog/detailed/)

- 2026-08-24 (v0.3.56–0.3.75): rosters CLOSE — every technology (Spies/Atheism v0.3.70), unit (Missionary v0.3.71), and building already done; land+naval trade, tribute+Coinage+Banking, Guilds, Cartography, teams, player colours, 3–8-player skirmishes, map-size ladder, setup screen, match options, population cap; Petard/Hoardings/El Dorado/shore-fishing; the stray-arrow near-loss and the late-game freeze measured.
- 2026-08-23 (v0.3.38–0.3.55): the AI learns to LIVE — wood in forests, Castle Age reached, stone income, build order past farms, town bell, somebody to attack; Outpost + Fish Trap close the building roster; Eagle/HC lines, Parthian Tactics, monk faith, Dock techs; the unreachable-upgrade defect family (v0.3.45-53) that seeded the menu↔validator gates.
- 2026-08-19–20 (v0.3.13–0.3.37): projectiles fly, University/Ballistics, naval foundation + war fleet, stances/attack-move/patrol/formations, 19 unique units + 16 unique techs declared as data, gates in walls, Monastery tier, Transport Ships, camera opening, command bar; the AI-economy traffic jam diagnosis.
- 2026-08-17–18 (v0.3.10–0.3.12 + audit): Moebius art style, animated shoreline surf + decoration variants, terrain colour fields, dependency audit back to green.
- 2026-07-14–20 (v0.2.2–0.3.9): the presentation era — attack animations, boar hunting/gore, ungarrison, post-construction mining, building relief, tree forms, terrain materials, concrete unit/building identity, HUD command deck, garrison-on-Alt BREAKING split, melee-reach root cause.
