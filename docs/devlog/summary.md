## 2026-04-18 (consolidated)
- **Slice 8 (Wonder, Relic, Score win conditions):** three alternative victory paths layered on the existing conquest mechanic. `BuildableBuildingType` gains `'wonder'` — 4x4 / HP 4800 / cost 1000 food + 1000 wood + 1000 gold + 1000 stone, Imperial-Age only, one per owner. Villager build menu exposes `'wonder'` in Imperial Age when the owner has no existing Wonder; Wonder ranks ahead of everything in `buildingTargetPriority` so enemy AI prioritizes it. `MatchState` extends to `{ outcome, summary, winCondition, scores, wonderCountdownTicks, relicCountdownTicks }`. Two new postUpdate systems: `prototypeWonderCountdown` (decrements per-Wonder countdown, triggers Wonder victory at zero, resets on destruction) and `prototypeRelicCountdown` (decrements whenever one owner holds every live + deposited relic — resets on ownership change or Monk mid-carry). Canonical countdowns are 2000 ticks; per-player scenario knobs `wonderCountdownOverrideTicks` / `relicCountdownOverrideTicks` shrink to 10 ticks for test speed. Shared `finalizeMatchEnd(outcome, winCondition, summary)` threads conquest through the same path so scores + winCondition populate uniformly. Per-owner score: `units * 10 + buildings * 25 + resources * 0.05 + relics * 50 + (wonder ? 200 : 0)`, incremented on `addUnitEntity`, construction-complete hook, and every gather drop-off. HUD gains a hidden-by-default Wonder / Relic countdown chip in the top bar and a post-game card with `[data-hud="match-summary-text"]` / `[data-hud="match-summary-win-condition"]` / `[data-hud="match-summary-score"]` rows. Gate: 219/219 vitest (+9 new winConditions cases), Playwright green (+1 new Wonder Victory case, existing conquest tests migrated to the `-text` sub-selector), tsc / vite build clean. Tie-break: Wonder beats simultaneous Relic (system-order deterministic). Follow-ups flagged for Slice 9 Save/Load: `wonderCountdowns`, `relicCountdowns`, `playerScoreCounters`, `wonderCountdownOverrides`, `relicCountdownOverrides`, and `matchState` all need serialization.
- **Castle + Longbowman (Slice 6):** Castle is a 4x4 Castle-Age defensive structure (HP 4800, cost 650 stone, attack 11 pierce at range 8, reload 20 ticks, vision 11, garrison capacity 20). Reuses the existing `prototypeTowerCombat` loop so any building with a combat state auto-fires — no new system. Longbowman is the first civ-unique unit: Britons-gated at the Castle train menu, HP 35 / atk 6 pierce / range 6 / reload 20 ticks / vision 7 / cost 35 food + 40 gold. `isArcherLineUnit` extended so Fletching propagates to Longbowman (before and after spawn). `getPlayerCivilization` helper added for civ-gated production. Garrisoned-archer-extra-arrows deferred to Slice 7.
- **Slice 5 review-fix sweep:** eight correctness fixes landed on `main`. Monk conversion now syncs population across players, reassigns `visionSource.playerId` to the new owner, purges the converted unit's stale orders + any friendly attack command against it, and caps progress at one per tick (multiple Monks no longer stack). `findMonkContextTargetAtCell` rejects fog-hidden enemies and prefers friendly heal over enemy convert on overlapping cells. `issueUnitMoveCommand` clears `monkTasks` so right-click-on-ground is a real cancellation. Destroyed Monasteries drop their stored relics back onto the map at free approach cells. The previously-flaky browser test for Monk relic gold is now stable: samples tick alongside gold at both endpoints and asserts `goldDelta === ticksElapsed` within a 20-25 tick window.
- **Slice 6 review-fix sweep:** three correctness fixes on top of Slices 5 and 6. Monk heal target resolution now skips healthy friendlies so a stacked healthy-friendly + enemy cell routes to convert instead of move-fallback. Monastery destruction's relic-drop loop grows the search radius outward and anchor-stacks any leftover relics when the map is completely cramped — stored relics can no longer vanish into a wall of trees. `findPreferredVisibleEnemyBuilding` now sorts by a new `buildingTargetPriority` (Castle 10, Town Center 9, Watch Tower 2, default 5) before Manhattan distance, so AI / units / defensive buildings won't pick the Castle first just because it's closest. Gate: 164/164 vitest, 65/65 playwright.
- **Slice 7 (Imperial Age + final-tier progression):** shipped in five batches (7A–7E). `AgeType` adds `'imperial-age'`; 12 Imperial units (Arbalest / Halberdier / Hussar / Heavy Cavalry Archer / Cavalier / Champion / Elite Longbowman / Onager / Heavy Scorpion / Siege Ram / Bombard Cannon / Trebuchet) and 14 Imperial techs (1 age-up + 10 unit-line upgrades + 3 Blacksmith) join the content tables with canonical HP / attack / range / reload / vision / cost. Imperial Age research at the Town Center (gated on Castle Age + any two Castle-Age buildings complete via `canAdvanceToImperialAge` / `isCastleAgePrerequisiteBuilding`, starting TC excluded). Unit-line upgrades (Crossbowman → Arbalest, Cavalry Archer → Heavy Cavalry Archer, Militia → Champion, Pikeman → Halberdier, Light Cavalry → Hussar, Knight → Cavalier, Longbowman → Elite Longbowman Britons-gated) reuse the Slice 2 `upgradeOwnedUnits` + `rewriteQueuedPredecessorUnits` pattern; train menus use the new `latestResearchedInChain(owner, chain)` helper so only the newest tier is exposed per producer. Siege upgrades (Mangonel → Onager, Scorpion → Heavy Scorpion, Battering Ram → Siege Ram) land at the Siege Workshop; Siege Ram carries a +250 anti-building bonus. Two Imperial-only siege units with no predecessor: Bombard Cannon at the Siege Workshop (+80 vs buildings, min-range 5) and Trebuchet at any Castle (+200 vs buildings, 16-tile range, pack/unpack simplified to "fires in place" in v1). Halberdier gets a flat +28 anti-cavalry via `isCavalryTarget` (Hussar / Cavalier included); Camel's +9 bonus also carries to Hussar / Cavalier. Imperial Blacksmith tier: Bracer (+1 atk / +1 range to archer line, stacks on Fletching), Blast Furnace (+2 atk to melee units via new `isMeleeUnit`), Plate Mail Armor (+1 armor to infantry via `isInfantryUnit`), Plate Barding Armor (+1 armor to cavalry via `isCavalryUnit`). `CombatState` gains an additive `armor` field; armor is tracked state but does not yet reduce damage (damage-reduction hooks deferred as a small follow-up). Gate: 217/217 vitest, 66/66 Playwright (+1 Imperial Arbalest training case), tsc / vite build clean. Deferred follow-ups: damage-reduction wiring for armor, Trebuchet pack/unpack, Chemistry tech gate, Paladin + intermediate Militia-line tiers, padded/leather/ring armor for archer/skirmisher, Forging / Iron Casting for Feudal / Castle melee attack, Bodkin Arrow for Castle archer attack.

## 2026-04-17 (consolidated)
- **Sheep command & fog memory (Slice 1):** player-commanded claimed sheep move at half villager speed with sticky ownership; drag-box selects herds alongside units. Fog memory renders last-seen static enemy buildings and neutral resource patches at 0.5 opacity once a cell is explored; wildlife is excluded so wandering boars / wolves / fish don't leave ghosts. Review cleanups: fog-hidden targets reject commands, gather cancels stale sheep orders, same-type double-click expands to sheep, footprint visibility uses the full building rectangle.
- **Castle-Age production upgrades (Slice 2):** one-shot Crossbowman / Pikeman / Light Cavalry upgrades. Research at the producer building mutates every owned predecessor in place (HP ratio preserved, entity id stable) and swaps the train menu. Fletching extends to the full archer line; Light Cavalry inherits a 6-tile vision; upgrades remain researchable in Imperial; queued predecessors in flight are rewritten on research completion.
- **Camel & Cavalry Archer (Slice 3):** trainable at Castle Age from Stable / Archery Range. Camel gets +9 vs Scout / Light Cavalry / Knight but is not itself a cavalry target (Spearman / Pikeman bonuses don't fire on it). Cavalry Archer uses the ranged pathway (atk 6, range 4); Skirmisher anti-archer and Fletching both extend to it.
- **Siege Workshop (Slice 4):** 3x3 Castle-Age workshop (cost 200 wood, HP 2000) trains Mangonel (atk 40 pierce, range 7, min range 3, +10 vs infantry), Scorpion (atk 12 pierce, range 7), and Battering Ram (HP 175, atk 2 melee, +75 vs buildings). Siege ranks highest in `targetPriority` so AI and Watch Towers hit it first. Review cleanups: scene double-click allowlist extended to siege; Mangonel AoE still modeled as single-target in v1 with the +10 infantry bonus standing in.
- **Monastery + Monks + relics (Slice 5):** 2x2 Castle-Age Monastery trains Monks. Monks heal (friendly unit), convert (enemy unit flips ownership after 50 progress), pick up and carry relics, and deposit them for a gold income drip per Monastery per tick. Review follow-ups covered in the 2026-04-18 review-fix sweep.
- **UX & rendering:** preserved the 5:3 canvas aspect ratio with letterboxing; minimap click centers the camera exactly on the clicked world position; fixed an edge-pan drift caused by a default `(0, 0)` pointer before cursor entry. Sheep now render with subgrid interpolation.
- **Docs & drafts:** reorganized `docs/` to match AGENTS.md — devlog split into date-ranged detailed files, new `architecture/`, `debugging/`, `learning/` directories. Added a standalone `cavalry-icon-draft.html` exploration page (four SVG concepts) as a second Vite build entry, isolated from the live game.

## 2026-04-12 (consolidated)
- **World / AI:** default map expanded to 60x36 with enemies farther out; baseline AI rush refined; civ-engine 0.3.0 upgrade with explicit system ordering; trapped-spawn prevention.
- **Combat & wildlife:** full wildlife combat for boars and wolves (health, attack, wolf auto-aggro, boar retaliation); exact-entity right-click combat against rendered targets.
- **Movement & rendering:** sub-grid movement with smooth interpolation; sub-cell slots per coarse cell; centralized building footprints; construction-vs-completion visuals; entity health bars.
- **Resources & buildings:** sheep ownership claimed by nearby player units; depleted nodes removed from world; shoreline fish as water food; blocker-aware placement and movement; minimap navigation with viewport rectangle.
- **HUD:** trimmed top status bar to player-facing chips (food / wood / gold / stone / age / pop / time); selection panel finalized (name / icon / health / attack / armor / faction / civ / inventory); Town Center age-up commands stay visible before prerequisites; RTS middle-mouse + edge panning.
- **Docs:** AGENTS.md and CLAUDE.md rewritten as a single shorter instruction set.

## 2026-04-11 (consolidated)
- **Dark Age loop:** TC and villager selection, villager queueing, House placement, population cap from completed construction, explicit gather context orders, Mill / Lumber / Mining drop-off routing.
- **Combat ramp:** Barracks + Militia, melee combat, conquest victory / defeat outcomes with deterministic seeds, baseline AI Barracks rush.
- **Feudal Age:** progression research; Archery Range + Archers; Blacksmith + Fletching; Stable + Scouts; Spearman + Skirmisher counters; Watch Tower; Market with dynamic exchange rates; generic rally points; defensive garrison + arrow fire.
- **Castle Age:** progression research, Knights from Stable, additional Town Centers.
- **UX:** drag-box multi-selection with marquee, double-click same-type selection, stacked-tile cycling, building placement preview, percent-complete queues, hidden running-state footer.
- **Engine:** `EntityRef` semantics for long-lived selection / command targets; generalized building-target combat; Playwright browser tests + repo-wide `verify` command.

## 2026-04-10 (consolidated)
- **Bootstrap:** TypeScript + Vite + Phaser 3 + civ-engine prototype; normalized content pipeline writing `generated/content/content.json`; content validation eliminates the warning budget (18 supported civs, 12 flagged explicitly).
- **Spec & docs:** AoE2 spec finalized (AoC baseline → DE target → single-player AI skirmish); `design/implementation-plan.md` sequencing playable milestones; root `AGENTS.md` mirroring `CLAUDE.md`; README with run / verify commands; `design/stats/*.csv` established as content source of truth.
- **First gameplay loop:** Phase 2 map slice with deterministic resource starts (sheep / boar / berries / gold / stone); fog of war via `VisibilityMap`; minimap from render frame; Dark Age economy loop (villager gather + return + stockpile updates).
