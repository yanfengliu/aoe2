## Prior work
2026-04-10 - Initialized project: created baseline AoE2 spec (`design/aoe2-game-spec.md`), devlog files, research sources. Iterated spec through AoC baseline → DE target → single-player AI skirmish scope. Split sources into `design/sources/` folder. `design/stats/*.csv` established as content source of truth.

## Current
2026-04-17 - Slice 4 Siege Workshop + three siege units: Villagers can place a 3x3 Siege Workshop at Castle Age (cost 200 wood, HP 2000); it trains Mangonel (HP 50, atk 40 pierce, range 7, reload 60t), Scorpion (HP 40, atk 12 pierce, range 7, reload 35t), and Battering Ram (HP 175, atk 2 melee, range 1, reload 50t). New `attackBonusAgainstBuilding(attackerType): number` helper runs in the building-target combat branch; only Battering Ram returns >0 (+75), so a Ram one-shots a 75-HP House in a single swing. Mangonel / Scorpion combat uses the existing ranged-attack pathway with their new stats — no bridge changes required. Pikeman and Camel anti-cavalry bonuses do NOT extend to Ram (pinned by regression tests). Seven new fixtures (siege-workshop-fixture, mangonel-ranged-fixture, scorpion-ranged-fixture, ram-vs-building-fixture, ram-vs-villager-fixture, pikeman-vs-ram-fixture, camel-vs-ram-fixture), 10 new vitest cases in `tests/simulation/siegeWorkshop.test.ts`, and 1 new Playwright case covering Siege Workshop + Mangonel training. HUD icons: SW (Siege Workshop), Mg (Mangonel), Sc (Scorpion), Rm (Battering Ram). Min-range for Mangonel + AoE are deferred to Slice 7 (Onager upgrades). Full gate: `126` Vitest, `63` Playwright, `tsc`, `vite build`.
2026-04-17 - Slice 3 Castle-Age train-menu units: Camel (Stable) and Cavalry Archer (Archery Range) are trainable once the owner reaches Castle Age, additive only — no existing unit stats changed. `UnitType` / `TrainableUnitType` gained the two values in `types.ts`; every exhaustive switch in `createSimulationBridge.ts` (stat tables, tint, size, vision, training cost/time, `targetPriority`) and the HUD label/icon/plural switches were extended. Camel gets a +9 anti-cavalry bonus vs Scout / Light Cavalry / Knight via a new `isCavalryTarget` helper; Camels are NOT counted as cavalry targets so Spearman / Pikeman bonuses do not fire against them. Cavalry Archer picks up the existing ranged-attack pathway with `unitAttackRange = 4` / `unitAttackDamage = 6`. HUD icons `Cm` (Camel) and `CA` (Cavalry Archer) avoid the Crossbowman `CB` clash. Three new fixtures (`camel-vs-cavalry-fixture`, `spearman-vs-camel-fixture`, `cavalry-archer-ranged-fixture`), 9 new vitest cases in `tests/simulation/castleNewUnits.test.ts`, and 1 new Playwright case covering Camel training + HUD label. Full gate: `116` Vitest, `62` Playwright, `tsc`, `vite build`.
2026-04-17 - Slice 2 follow-ups: Codex + Gemini review fixes on the Castle-Age upgrade commits. (1) Fletching now applies to the full archer line — researching Crossbowman no longer silently strips the +1/+1 buff (Codex P1). (2) `upgradeOwnedUnits` and the production-queue spawn consult a new `unitVisionRadius(UnitType)` helper, so Light Cavalry gets its canonical 6-tile line-of-sight instead of the legacy 4 (Codex P2 + Gemini High). (3) Any queued predecessor unit still in a production queue at upgrade-completion time is rewritten to the upgraded type, preserving the "predecessor line is no longer trainable" invariant (Codex P2). (4) A new `isAtLeastAge` helper gates the three upgrades so they stay researchable after a player advances to Imperial (Gemini High). Bundled Fix 5 (Gemini nitpick) into the vision commit by grouping identical `trainingCost` switch arms. Six new vitest cases and an `imperial-upgrades-fixture`. Full gate: `107` Vitest, `61` Playwright, `tsc`, `vite build`.
2026-04-17 - Slice 1 follow-ups: Codex + Gemini review fixes on the fog-memory + sheep-control commits. (1) `issueContextCommandAtEntity` now rejects commands targeting fog-of-war-hidden entities (Codex P1) — owned entities skip the check; multi-tile buildings count as visible if any footprint cell is in vision. (2) `prototypeFogMemory` excludes all wildlife (boar, wolf, sheep, fish) via a new `isStaticMemorableResourceType` predicate so wandering wildlife no longer leaves a frozen ghost when it walks out of sight (Codex P2). (3) `getRenderState` adds a fast path that skips the dedupe Set / memory projection / merge sort entirely when there is no fog memory yet, plus a hoisted comparator (Gemini High perf). (4) Both the projector and the live-vs-memory filter now check the full footprint visibility instead of just the anchor cell, so a 4×4 Town Center with one corner in vision renders as live (Gemini Medium correctness). (5) Cleanup loop drops the `[...humanMemory.entries()]` spread (Gemini Medium perf). (6) `sheepMoveOrders.delete` is now guarded on resource type for clarity (Gemini Minor design). Four new vitest cases (boar exit, footprint visibility, fog-hidden command rejection, plus the existing house/mine fog-memory tests). Full gate: `101` Vitest, `61` Playwright, `tsc`, `vite build`.
2026-04-17 - Slice 2 Castle-Age production-line upgrades: three one-shot upgrades (Crossbowman at Archery Range, Pikeman at Barracks, Light Cavalry at Stable) that research in Castle Age, mutate every existing owned predecessor unit to the upgraded type in place (HP ratio preserved, entity id stable), and swap the train menu so the predecessor can no longer be trained. Unit/tech enums extended in `types.ts`; `createSimulationBridge.ts` gains exhaustive stat-lookup cases (HP/attack/range/reload) plus anti-cavalry bonuses for Pikeman; HUD labels/icons extended (Crossbowman/Pikeman/Light Cavalry names, CB/PK/LC badges); `castle-upgrades-fixture` scenario; 8 new vitest cases in `tests/simulation/castleUpgrades.test.ts` and 1 new Playwright case. Full gate: `98` Vitest, `61` Playwright, `tsc`, `vite build`.
2026-04-17 - Slice 1 post-sheep follow-ups: (A) reordered `prototypeHerdableMovement` before `prototypePlayerCommands`, (B) villager gather cancels any outstanding sheep move order on that sheep, (C) same-type double-click now expands to every owned sheep on screen (`selectOwnedUnitsByTypeInRect` accepts `'sheep'`), (D) fog memory shows last-seen static enemy buildings and neutral resource patches at 0.5 opacity when their cell is explored but not visible (`prototypeFogMemory` system + `ProjectedEntityView.isMemory`). Full gate: `90` Vitest, `60` Playwright, `tsc`, `vite build`.
2026-04-17 - Player can now command claimed sheep. Drag-box selects herds alongside units; one right-click moves the whole group at half villager speed; ownership is sticky once claimed (sheep can't be stolen by enemy units in range). Sheep also render with subgrid interpolation now (the projector previously required `unit && unitTransform`; sheep have only the latter). Full gate: `84` Vitest, `58` Playwright, `tsc`, `vite build`.
2026-04-17 - Preserved the game canvas aspect ratio with 5:3 letterboxing and made minimap clicks center the camera exactly on the clicked world position. Fixed a pre-existing edge-pan drift where `activePointer` defaulting to `(0, 0)` triggered NW scrolling before the cursor had entered the canvas. Full gate: `76` Vitest, `57` Playwright, `tsc`, `vite build`.
2026-04-17 - Reorganized `docs/` to match the current AGENTS.md. Moved the devlog summary to `docs/devlog/summary.md`, split the detailed devlog at the 2026-04-11/2026-04-12 boundary into date-ranged files under `docs/devlog/detailed/`, and created the previously missing `docs/architecture/`, `docs/debugging/`, and `docs/learning/` scaffolding. Doc-only change; no tests were affected.

## 2026-04-12 (consolidated)
- World/AI: expanded default map to `60x36` with enemies farther out for minimap travel range; baseline AI rush refined; civ-engine `0.3.0` upgrade with explicit system ordering; trapped-spawn prevention.
- Combat & wildlife: full wildlife combat for boars and wolves (health, attack, auto-aggro for wolves, retaliation for boars); exact-entity right-click combat against rendered targets.
- Movement & rendering: sub-grid unit movement with smooth interpolation; units can share coarse cells via sub-cell slots; building footprints centralized; construction vs completion visuals; entity health bars.
- Resources & buildings: sheep herdable ownership claimed by nearby player units; depleted resource nodes removed from world; shoreline fish as water food; blocker-aware placement and movement; minimap navigation with viewport rectangle.
- HUD: trimmed top status bar to player-facing chips (`food/wood/gold/stone/age/pop/time`); selection panel finalized with name/icon/health/attack/armor/faction/civ/inventory; Town Center age-up commands stay visible before prerequisites; RTS-style middle-mouse + edge panning.
- Docs: AGENTS.md and CLAUDE.md rewritten as a single shorter instruction set.

## 2026-04-11 (consolidated)
- Player-commanded Dark Age loop: TC and villager selection, villager queueing, House placement, population cap from completed construction, explicit gather context orders, Mill/Lumber/Mining drop-off routing.
- Combat ramp: Barracks + Militia production, melee combat, conquest victory/defeat outcomes with deterministic seeds, baseline AI Barracks rush.
- Feudal Age slice: progression research, Archery Range + Archers, Blacksmith + Fletching, Stable + Scouts, Spearman + Skirmisher counters, Watch Tower static defense, Market with dynamic exchange rates, generic rally points, defensive garrison + arrow fire.
- Castle Age slice: progression research, Knights from Stable, additional Town Centers.
- UX: drag-box multi-selection with marquee, double-click same-type selection, stacked-tile cycling, building placement preview feedback, percent-complete queues, hidden running-state footer.
- Engine integration: `EntityRef` semantics for long-lived selection/command targets, generalized building-target combat, Playwright browser tests + repo-wide `verify` command.

## 2026-04-10 (consolidated)
- Bootstrap: TypeScript + Vite + Phaser 3 + civ-engine prototype, normalized content pipeline writing `generated/content/content.json`, content validation eliminates warning budget (`18` supported civs, `12` unsupported flagged explicitly).
- Content & docs: spec finalized; `design/implementation-plan.md` sequencing playable milestones; root `AGENTS.md` mirroring `CLAUDE.md`; expanded README with run/verify commands.
- First gameplay loop: Phase 2 map slice with deterministic starts (sheep/boar/berries/gold/stone), fog of war via `VisibilityMap`, minimap from render frame; Dark Age economy loop with villager gather + return + stockpile updates.
