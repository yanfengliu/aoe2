## 2026-06-30/07-01 (Opus 4.8; marathon: full-game goal; co-evolve aoe2+civ-engine; engine "throughput" triage)
- **M2 asymmetric armor techs (v0.1.53, thread asymmetric-armor):** split the single armor-tech scalar so AoE2's four asymmetric techs are correct (spec §11.8) — Plate Mail Armor / Plate Barding / Ring Archer Armor / Loom now give +1 melee / **+2 pierce** (the other 6 stay symmetric +1/+1). `armor` keeps its meaning (symmetric, both sides); NEW `pierceArmorBonus` carries the extra pierce-only +1 → effective pierce = base + armor + pierceArmorBonus. New pure `armorTechBonuses.ts` (`EXTRA_PIERCE_ARMOR_TECHS` + `applyArmorTech` + `pierceArmorTechBonus`); the shared helper drives BOTH the combat-state factory (new units) AND technologyOps (existing units on research) + Loom, so they can't drift. Pierce read sites (blastDamage, towerCombat) use `pierceArmorTechBonus`; melee keeps `armor`. Loom restored to +1/+2; full infantry armor line +3 melee / +4 pierce. Save: additive optional field, pre-split saves read symmetric via `?? 0` (no schema bump). TDD: pure `armorTechBonuses.test` + factory-level `asymmetricArmor.test` (each asymmetric tech → melee 1/pierce 2, full line 3/4 — fails on old +1/+1) + updated loomTech/blastDamage/replay literals. 2-CLI reviewed — both INDEPENDENTLY found a HIGH: pre-split SCHEMA-2 saves load with `pierceArmorBonus` undefined (schema-1 hydrate had the `?? 0`, but no schema bump → real pre-split saves are schema-2, loaded via world.deserialize with no defaulting), and researching an asymmetric tech then does `undefined+1 = NaN`. Fixed belt+suspenders (schema-2 normalize in hydrateFromWorldState + defensive applyArmorTech) + 2 regression tests. LOC: technologyOps hit 501 → extracted `applyArmorTechToOwnedUnits` (447).
- **M6 score-timer victory (v0.1.52, thread score-timer-victory):** matches can end on a timer with highest score winning (spec §4.3) — a scenario opts in via a `gameLength` tick count; new `scoreTimerSystem` (registered last in postUpdate, `after: prototypeConquestOutcome`; not registered when `gameLength` unset) fires on the first tick where `world.tick >= gameLength`, comparing every player's `computePlayerScore` in one order-independent pass → victory (human sole top) / draw (tied top) / defeat, the `!isMatchRunning()` guard letting an earlier conquest/wonder/relic that tick win. `WinCondition` union gains `'score'` (threaded through the save schema, additive/back-compatible); post-game card gains "Score Victory". Slice-1 limit: `gameLength` not persisted (load disables it). TDD: 4 fixture tests (victory 180/50, draw 180/180, 3-player defeat 50/180/100 — exact scores confirm the formula) + an EXACT boundary pin (running through step 30, resolves step 31, ground-truth `tsx`-verified) + a HUD-label test. Full suite green (1503). 2-CLI iters 1–2 (both CONFIRMED core; Claude MEDIUM blank-label + Codex MEDIUM weak-coverage + Codex IMPORTANT loose-boundary all fixed → converged; Gemini OAuth-unreachable). **Deterministic-corpus follow-up done (thread score-timer-corpus, no version bump — test-infra only):** a `gameLength` override threads corpus-manifest → `createSimulationBridge`/`createWorld` (stamps the fresh scenario, no-op on save-load), corpus schema gained a validated `gameLength` (positive int `< maxTicks`), and the deterministic smoke (`maxTicks 8100/gameLength 8000`) dropped `matchCompleteRequired: false` — verified end-to-end (`stopWhen`, ticksRun 8001, 0 HIGH); 2-CLI (Codex MEDIUM integer/NaN validation fixed; Claude clean). Still open: LLM corpus (no `gameLength` wiring; LLM stalls in Dark Age).
- **M6 villager repair — buildings (v0.1.51, thread villager-repair):** right-clicking a friendly COMPLETE damaged building with a villager repairs it (spec §8.1) — walk-adjacent + restore HP over time at the build rate, for `ceil(0.5×buildCost×missingHp/maxHp)` per resource, charged up front (`repairCost`, `setUnitRepairCommandDirect`). A DISTINCT `'repair'` UnitCommand (not `'build'`) — fixes a Codex-HIGH free-repair exploit where an over-assigned builder's stale construction command became free repair after completion+damage. Construction-complete block extracted to `finalizeBuildingConstruction.ts` (behaviour-preserving) to keep playerCommandsSystem <500 LOC. Additive save extension (`'repair'` in Serialized/UnitCommand; older saves unaffected). TDD (repairCost pure + damaged-house integration). Full suite green. 2-CLI reviewed (Codex HIGH fixed structurally; Claude no-other-bug + 3 notes addressed; Gemini OAuth-unreachable).
- **M2 blast/splash for the mangonel line (v0.1.50, thread mangonel-blast):** Mangonel/Onager now deal AREA damage (spec §10.7) — a hit splashes every OTHER unit within the attacker's Euclidean `blast_radius` of the impact (enemy AND friendly = friendly fire), each via the same melee/pierce+bonus+armor formula as the primary; restores the anti-infantry removed as a fake bonus in 2b-ii. New pure `bridge/blastDamage.ts` (`computeBlastDamage`/`applyUnitBlast`/`resolveUnitAttackOnUnit`) + `UNIT_BLAST_RADIUS` (mangonel 1, onager 1.25); fires on both unit- and building-target paths; no save change; derived at the damage site. Extracting the unit-hit block kept playerCommandsSystem <500 LOC. TDD: pure test (radius/exclusion/floor/friendly-fire/scoring) + clustered-infantry + building-splash integration fixtures. Debugging: a failing building test's "spearman dies" was the owner-1 TC base-fire confound (ground-truth `tsx` trace), not a blast bug. Full sim suite green. 2-CLI reviewed (Codex MEDIUM building-path gap + LOW scoring guard → both fixed; Claude no-bug; Gemini OAuth-unreachable). (Combat Slice 2b-ii v0.1.49 — AoE2-accurate armor-class bonuses — is in the data-driven-combat entry below.)
- **Engine sim-throughput "regression" = NOT-A-REGRESSION (no version bump):** the standing engine-feedback claim (+50-75% slower, 0.8.24→1.0.1) that had doubled 9 fixture files' timeout caps (`x2 2026-06-12`) was proven a full-suite thread-pool CONTENTION artifact, not engine code. Static audit: every per-tick hot-path file byte-identical across 0.8.23→1.0.2; the sole per-tick change (v1.0.0 strict-by-default) measured within noise on the real ageUp fixture (strict-on 42.71s vs strict-off 43.24s, isolated). Ratcheted the caps back to data-driven values (uniform per file ≈3× worst *contended* per-test: 90s ageUp/blacksmith, 45s mid-weight×6, 30s autoAggression) with truthful annotations; verified green under worst-case parallel contention. Co-evolved civ-engine: added a `commands` benchmark scenario (strict + per-tick validated commands + in-tick events + 14 systems/60 ticks; tier-1 counters exact) closing the gate's aoe2-load blind spot. engine-feedback item moved current→past. Debug doc `2026-06-30-engine-throughput-regression.md`; lesson recorded. Both repos four gates green (engine 1238+1, benchmark gate green).

## 2026-06-13/16 (Opus 4.8; provider-error retry; conformance + AoE2-completeness loops v0.1.23-30; CI repair; M7 graphics; campaign-11 re-ground)
- **campaign-11 re-grounding + gather-unreachable reroute (v0.1.47, thread gather-unreachable-reroute):** the first $15/6000-tick LLM re-ground (24 decisions, stopReason maxTicks, $14.87). Ground-truth replay + the conformance probe INDEPENDENTLY found a [high] bug: 6 villagers stack-deadlocked on a boxed-in OWNED sheep (every approach cell blocked → `findResourceApproachPlan` null), latched in `to-resource`, food frozen at 21 for ~3000t → never reached Feudal, never left Dark Age. Cause: a null approach plan fell to the `to-resource` branch-A idle, then idle→assign re-picked the same tier-0 unreachable sheep every tick (owner-tier sorts BEFORE the saturation fan-out, so it never fell through to the 6 reachable berries; prog stayed 0 so the over-subscription give-up never fired). Fix (no engine/save change): extracted `assignNearestResource`→`bridge/villagerGatherAssignment.ts` (+ optional `requireReachable`/`excludeResourceId`, default-off → byte-identical hot paths), split branch A so an unreachable-but-harvestable target reroutes to the nearest REACHABLE resource (BFS only on the rare reroute path). TDD red-green: 5 mechanism + 1 integration (boxed-farm fixture; FAILS pre-fix food-flat 200, passes post). Four gates green, full suite 1477/2 (+5), clean blast radius. 3-CLI reviewed (thread done/). spec §6.4. Secondary findings: (a) Houses-don't-count-Feudal-prereq INVESTIGATED → **NOT-A-BUG** (thread feudal-prereq-buildings — AoE2 Dark→Feudal counts 2 of {Barracks/Dock/Lumber/Mill/Mining}; Houses/Farms/Walls excluded, so the existing {mill,lumber-camp,mining-camp,barracks} is already correct; conformance-probe misdiagnosis, attempted fix reverted with NO code change); (c) AI never ages up at 510 food → **FIXED v0.1.48** (thread ai-age-up-priority): ground-truth replay confirmed owner 2 reached 510 food + 3 complete prereqs + idle TC at tick 3000 but stayed Dark Age all 6000t, massing Militia — a Militia (60 food) is pushed before the feudal-age research (500 food) in one decision cycle, both affordable alone at 510 but not together, and the FIFO handler spent the Militia first so the age-up silently no-op'd. Fix: the AI now RESERVES its next age-up cost and trains military only from the surplus above it (a full-suppression first attempt regressed the rich-fixture "5+ military" test → reserve lets a rich AI train military AND age up). TDD via `ai-age-up-priority-fixture`. Still open: (b) accepted-vs-executed UX stuck-signal.
- **M1 farm-upgrade Mill techs (v0.1.46, thread farm-upgrade-techs):** the 3 AoE2 Mill farm-food techs — Horse Collar (Feudal, +75→250 food), Heavy Plow (Castle req HC, +125→375), Crop Rotation (Imperial req HP, +175→550) — researchable at the Mill, stacking to 550. DERIVED like the gather/carry techs: a pure `farmFoodCapacity(researchedSet)=175+bonuses` (economyTechEffects) read at farm CREATE (entityCreateOps) + RESEED (farmReseed) via the already-persisted `researchedTechnologiesCodec` → NO save change, NO per-farm state, un-teched farms stay 175 (zero blast radius). Linear prereq+age gating (optionsRules). Data fix: technologies.csv Heavy Plow +75→+125 (was a Horse-Collar dup contradicting 250/375/550). **The subagent breached the 500-LOC budget (optionsRules 521/types 507) + falsely claimed gates-green (died on a socket error)** — lead caught it via the full-suite re-run + fixed it by extracting the camp/mill option blocks into `bridge/economyTechOptions.ts` (behavior-preserving, validated by the pre-existing economyResearchOptions.test through the bridge). TDD 18 tests (helper + gating + cost/time + ground-truth live-sim + no-regression + save-load). Four gates green, full suite 1472/2 (+18). Reviewer comments pending. spec §6.5/§6.6.
- **M7 graphics #8 — combat/gather feedback + selection polish, slice 1 (v0.1.45, thread combat-gather-feedback):** the game's FIRST dynamic (time-based) render feedback (was: static ring + silent instant-HP combat). Two effects, both render-only (no sim/save/replay-determinism impact), derived from EXISTING projected state + a render-side per-frame delta: (1) SELECTION PULSE — the existing ring breathes over ~1.1s (stroke alpha 0.7→1.0 + width 2→3 + 0→2px outward swell from a scene-clock cosine phase), ring center/base radius + hitbox/HP-bar/footprint geometry unchanged; from the `selected` set. (2) HIT FLASH — a brief (~0.26s) bright impact flash (white-hot core→orange rim, inside the unit circle, decaying) on a unit whose `currentHp` dropped vs last tick, tracked via a `HitFlashTracker` (the analogue of `previousUnitProjectedPositions`). New pure `gameScene/feedbackEffects.ts` (`selectionPulse`/`shouldFlashHit`/`createHitFlashTracker`/`drawHitFlash` + a scene-facing orchestrator); `selectionLayers.renderSelection` gained an optional pulse (static default=unchanged ring); `isAnimating` cache-bust keeps the static fast path. Extracted `resourceRenderer.ts` (byte-identical move) to keep pinned GameScene under 1018 (now 1011). Time/random confined to render; no `Math.random`/`Date.now` in sim/bridge. 100% procedural, no assets. **GATHER SPARKS SKIPPED** (gather state NOT on `ProjectedEntityView` → forbidden contract change); death particles + projectiles deferred (projectiles pair with M2). TDD 25 tests (19+4+2); four gates green, full suite 1454/2 (+25). Visual protocol via test-only `feedback-showcase-fixture`: before/after/pixel-diff (`tmp/feedback/` 0.21% px, confined to the selected ring + a combat unit's flash; motion not capturable in a static PNG). 3-CLI converged APPROVE (Codex MEDIUM stale-flash-at-expiry + 4 LOW all fixed; Gemini clean). spec §14.5.
- **M7 graphics #7 — UI icons slice 2 (v0.1.44, thread ui-icons-slice2):** the deferred selection-panel follow-up to slice-1's resource/build icons. The selection badge (the bare "V"/"TC"/"H"/"CB" two-letter code) now sits beside an original procedural glyph: UNITS get a per-RENDER-ROLE glyph (villager/infantry/archer/cavalry/cavalry-archer/siege/monk, mirroring the on-map unitRenderer roles) and BUILDINGS reuse slice-1's per-type `buildingGlyph`. New `src/ui/hud/icons/unitGlyphs.ts` (`unitGlyphRole`/`unitRoleGlyph`/`selectionGlyph` dispatcher; `buildingGlyphWithClass`+`isBuildableBuildingType` added to glyphs.ts to reuse the building art under the selection hook) + a `.hud-selection-unit-glyph` rule in hudIcons.css. AUGMENT not replace: glyph is a SIBLING before the badge (mirroring slice-1's `.hud-value`-sibling), so the badge keeps its exact two-letter code + every `data-selection-(unit|entity)-icon`/`-count`/`-label` hook — every Playwright `toHaveText('V')` badge assertion still passes; resources/wildlife/relic stay text-only (out of scope). Role map is parity-tested against `unitRenderer.unitRole` (drift guard; production stays decoupled from the Phaser layer). DOM/CSS-only — NO `src/phaser/`/sim/save/contract touch. TDD 13 tests; full suite 1429/2 (+13). Visual protocol: villager+TC+multi before/after/pixel-diff in `tmp/ui-icons-2/` (≈4.8% single / ≈8% multi, confined to the selection panel; Phaser canvas pixel-identical). Four gates green. Reviewer comments pending (main agent runs the multi-CLI review). spec §14.5. Still deferred: non-build command-card icons, minimap terrain layer, fog fade.
- **Provider-error retry shipped (thread done/provider-error-retry):** a transient `claude exit 1`/timeout/spawn failure is now a `ProviderCallError` retried in place by a `RetryingProvider` (2 retries/3s base); on exhaustion the runner stops with a distinct `providerError` stopReason (NOT engineHalt), so the winner oracle + screenshot + bundle still run on the healthy game; corpus CI exempts it. 3-CLI iter-1 (2 HIGH "retry at the wrong layer" + a latent secret-leak) → iter-2 RetryingProvider refactor → Codex+Gemini CONVERGED. +16 tests. Lesson: `claude -p --allowedTools` is NOT read-only (memory: claude-p-reviewer-not-readonly).
- **Conformance capture replaces the fun oracle (thread done/conformance-capture) → AoE2-completeness program begins:** user scrapped "looked-fun" as garbage; new decoupled `playtest:findings` pass emits OBJECTIVE signal — deterministic metrics (command usage, accept/reject by reason where a rejected `unknown-kind` = unimplemented feature, stalls, winner) + an AoE2-grounded LLM critique of `{category,area,observed,expected,severity,suggestion}` gaps; re-runs free on any saved bundle. Removed observationOracle/ObservationVerdict/`--observation`. Review iter-1: BOTH CLIs flagged the same HIGH (trace dropped host `unknown-kind` rejections) + 2 Codex MED → fixed w/ tests → iter-2 CONVERGED. Coverage audit (tmp/): complete-but-shallow slice — 35/104 units, 37/140 techs, 17/27 buildings, civ-diff ~0, pop cap 5, no farms; that's the build backlog.
- **AoE2-completeness: roadmap + 3 engine-replay-driven playtest→find→fix loops.** Spec goal raised to full AoE2 + `design/roadmap.md` (gap backlog). New `replay:inspect` (SessionReplayer + getEconomyState) = the "what actually happened" debug tool → AGENTS rule "use engine debug tools FIRST" (it overturned BOTH the LLM findings AND a subagent's synthetic repro — e.g. "wood gather broken"/"enemy attrition" were wrong; the real bug was a villager pile-up; and `unit.attack` already exists). Loops (each TDD + Codex+Gemini CONVERGED): (1) **gather gridlock** v0.1.24 — 14/18 woodcutters jammed on one tree; stuck over-subscribed villagers get a give-up path (surgical, AI untouched). (2) **real opponent** — `--owners 2` left the LLM in the only AI slot (no opponent); default `--owners 1` = LLM-human vs AI-P2. (3) **fan out on idle reassign** v0.1.25 — generous spread cap (cap 2 broke the AI age-up; 4 clears its natural clustering). Plus v0.1.23 **TC/Castle provide population** (spec §6.10 divergence). Note: the claude subscription degrades over a long session (campaigns slow→hang); a confirming playtest for v0.1.25 is deferred.
- **Typed-recording seam removed (thread done/typed-recording-seam):** adopted civ-engine 1.2.0's `TComponents`/`TState` threading — dropped the `toEngineWorld`/`fromEngineWorld` cast from the SessionRecorder/SessionReplayer paths so recorded/replayed worlds are registry-typed by inference (`getComponent` is `Position`, not `unknown`); `toEngineWorld` survives only for WorldDebugger/RenderAdapter (1.2.0 didn't thread those, World's TComponents is invariant). Type-only (no version bump); `ReplayReplayer` pins GameComponents via an `infer`'d TDebug; new compile-time contract test guards regression; 3-CLI review (Codex+Claude+Gemini) converged no-findings. engine-feedback 1.2.0 item → past.md.
- **Economy gather-rate techs = stat-multiplier subsystem v1 (v0.1.26, thread done/economy-gather-techs):** campaign-5 findings HIGH "only age-up ever researched; econ techs data-only" → wired 7 AoE2 Work-Rate techs (Lumber Camp wood ×1.2/1.2/1.1, Mining Camp gold/stone ×1.15) that actually speed gathering. Multiplier DERIVED from the persisted researched-tech set (no new state); AI untouched (only researches age-up). 3-CLI iter-1 found a real HIGH — `round(base/mult)` at tiny base cadences (tree 5, gold/stone 6) made the 2nd-tier techs (Two-Man Saw, Shaft Mining) no-ops — fixed iter-2 with per-tick rate accumulation carrying the remainder (`ticksToGatherCarry` + monotonic no-op-tier guard); iter-2 CONVERGED 3/3 (determinism of fractional gatherProgressTicks cleared). Deferred: carry/farm econ techs, loop↔helper test binding, AoE2 linear prereq chains. spec §6.5.
- **Economy carry techs = stat-multiplier v2 (v0.1.27, thread done/economy-carry-techs):** Wheelbarrow (Feudal +25%) + Hand Cart (Castle +50%) raise villager carry (stack ×1.875), at the Town Center alongside age-up. Same derived-from-researched-set pattern; carry is resource-agnostic so it speeds FOOD too (the Castle-Age bottleneck). Extracted the econ-tech-effect logic into `economyTechEffects.ts` (prototypeEconomyRules hit the 500-LOC budget). 3-CLI CONVERGED 3/3 no defects; full suite caught 4 stale age-up TC-option assertions (TC now offers carry techs too) — fixed. Movement-speed ×1.1 deferred. Next: farms (M1 food backbone) or pop-cap-200.
- **Validation playtest (campaign-7) + Dark-Age Palisade Walls (v0.1.28, thread done/dark-age-palisade):** ran a fresh LLM playtest to re-ground; ground-truth replay overturned the "farms next" plan — the agent is **rushed + wiped out by the AI in the Dark Age** (villagers 5→0, never reaches Feudal, so the econ techs are unexercised). 0 commands rejected (no missing feature; lost the early fight). Reprioritized: early-game survivability before mid-game. First fix: Palisade Walls were wrongly gated to Feudal+barracks; in AoE2 they're a Dark-Age no-prereq wall — moved to the Dark-Age build list (one `getBuildOptions` gate covers surface+placement). Review converged 2/3 clean (Gemini+Claude ship-it; Codex sandbox-inconclusive; Claude/Codex degraded under heavy session load); full suite 1255 green. Roadmap "Playtest findings log" + open follow-ups (wall-line affordance, AI Dark-Age efficiency, TC defensive fire).
- **Re-playtest (campaign-8) → TC base-fire fix LANDED v0.1.29 (thread tc-base-fire):** campaign-8 found (ground-truth) an **empty Town Center fires 0 arrows** (spec §10.8 violation — no economy-phase defense; Castle fires 1 base). A first attempt was reverted (broke ~25 combat tests); a late Claude review claimed the breakage was mostly "flaky (verified in isolation)" — I MEASURED it (baseline 1255-green → 25 fail → all 25 fail **in isolation**, HP off by exactly 5) and disproved it (Claude had Read/Grep only, no test runner → the "verified" claim was fabricated). Sharpened lesson: verify a reviewer's EMPIRICAL claims by re-running. Then did it properly: empty TC fires `1 + min(garrison,4)` like the Castle, and restaged **~25 combat-isolation fixtures (13 files)** so combat is clear of TC range (footprint-Manhattan ≤6 of the 4×4 TC); AI fixtures needed bespoke fixes (ai-planner drops the human TCs so the AI's `humanTownCenterId` is null → military stops churning vs TC fire → ages up; ai-rush/castle/autoAggression moved combat/TCs out of range). Full suite 1255 green; spec §10.8 clarified (base attack fires independent of garrison).
- **campaign-9 + graphics/UI north-star + CI repair (2026-06-15):** campaign-9 (v0.1.29) is the **first run the LLM human SURVIVES the Dark Age** (ground-truth replay: 3→9 villagers, both owners alive at tick 3000, vs campaigns 7-8 wiped to 0) — validates the TC base-fire + Dark-Age walls; remaining gaps: the LLM plays 0-military, and neither side reaches Feudal in 3000t (AI food-starved massing Dark-Age militia). **Graphics/UI north-star added (user directive):** the loop now also pursues render realism + UI polish toward an AoE2 HD clone (original/procedural art only) — roadmap M7 (isometric → sprites/animation → terrain texturing → HUD chrome), after an Explore audit mapped the all-procedural Phaser baseline (units are tinted circles, flat terrain, DOM HUD). **CI repaired (red 1+ day):** aoe2's `file:../civ-engine` (a sibling symlink, never built by aoe2) was absent in CI; now the engine CI publishes a prebuilt `engine-dist` tarball gated on its own tests, and aoe2's 3 workflows curl+untar it (no engine build) — so a green+pushed engine is load-bearing for aoe2 CI (memory project_ci_engine_dist).
- **M7 graphics terrain texturing (#1 v0.1.30 m7-terrain-texture + #6 v0.1.43 terrain-blending):** flat per-kind fills now read as a textured surface. `gameScene/terrainRenderer.ts` adds (a) a deterministic ±7% hue-preserving per-cell brightness jitter (pure `terrainCellTint`) so same-kind cells differ, and (b) kind-to-kind edge BLENDING — each cell feathers every 4-neighbour edge whose kind differs with a dithered stipple band tinted by `blendTint` (the two kinds' base-tint average) on the inner side, so adjacent grass/forest/water/hill interlock into a soft seam instead of a hard rectangular line. Pure per-frame (deterministic from cell coords, no random/time); neighbour kinds read from `entity.entityType` via a WeakMap-memoized intra-frame kind-grid (projection already sets terrain `entityType=kind`); specks stay inside the cell rect; blend sits UNDER overlays. Render-only — NO sim/bridge/save/contract touch (`ProjectedEntityView` unchanged); GameScene net-zero at 1017; terrainRenderer 215<500. **Elevation shading DEFERRED** (not projected → contract change). TDD 18 tests; before/after/pixel-diff (`tmp/terrain/` 1.89% / earlier 35%, confined to terrain). Four gates green, 1416/2.
- **campaign-9 replay deep-dive (2026-06-15, docs only):** replayed the actual bundle (engine debug tools first) to investigate finding (c) "human wood ~0" → RESOLVED as NOT-a-bug (wood gather + fan-out healthy; it's LLM allocation+spend). Real lever found: newly-trained HUMAN villagers default to desired=food but `hasExplicitGatherOrder=false`, and the `shouldMaintainGatheringOrder` gate auto-assigns only AI villagers — so untasked human villagers idle at the TC beside abundant food (AoE2-faithful idle-until-tasked, not a sim bug). Missing AoE2 anti-idle feature = rally-point-to-resource auto-gather (`productionQueueSystem` is move-only); recorded as the NEXT M1 fix. Likely also drives the no-age-up (finding b).
- **Rally-point-to-resource auto-gather (v0.1.31, thread rally-point-gather):** the fix for the above. A villager trained from a building whose rally point sits on a harvestable resource now gets that resource's gather intent (`desiredResource` + `hasExplicitGatherOrder`) and `villagerEconomySystem` routes it to the nearest matching node — so freshly-trained villagers work instead of idling at the TC. Contained to `productionQueueSystem` (fog-agnostic cell-query + 2 gatherer fields; no dep-threading, direct-mutation, determinism-safe); reuses the economy system's assignment + fan-out + reseek. TDD (rally TC onto a tree → new villager works wood); spec §9.3 updated. Open follow-on: surface idle-villager count to the LLM so it sets the rally.
- **Data-driven combat Slice 1 — melee/pierce armor split (v0.1.32, thread data-driven-combat):** every unit had one `armor`; now pierce attackers (archers/skirmishers/siege/tower arrows) are reduced by the target's PIERCE armor (`UNIT_PIERCE_ARMOR` from units.csv), melee attackers by melee armor — so AoE2 counters emerge (skirmishers shrug off arrows, rams near-immune, cavalry carry 2). New `combatDamageAfterArmor`/`unitAttackType`/`unitPierceArmor`; 2 unit-target sites wired. Pierce armor DERIVED at the site (not in persisted CombatState → no save-format change). TDD + bounded fixture re-validation (only pierce-attacker fixtures shift: cav-archer 34→35, mangonel-vs-knight 60→62, tower-vs-mangonel 45→49). Slice 2a (v0.1.33) added base MELEE armor; Slice 2b-i (refactor, no bump) retired the hard-coded attack-bonus if-ladder into data tables (parity-tested). **Slice 2b-ii ✅ (v0.1.49):** full `ArmorClass` model (`prototypeUnitRules/armorClasses.ts`) + cross-class SUMMATION with AoE2-accurate CSV values — spear line flat vs ALL cavalry (+15/+22/+32) + separate `camel` class (+7/+11/+16); camel +10 / heavy-camel +18; skirmisher +3 vs archer + new +3 vs spearman; archer line new +vs-spearman; siege-vs-building to CSV (ram +125/siege-ram +200/bombard +200/treb +250); mangonel's fabricated +10 vs infantry REMOVED (blast → M2). Dead code cleaned (isCavalryTarget + 3 target sets). Deferred: scout anti-monk, off-roster classes. 12 fixture assertions re-validated; full suite 1484/2. 2-CLI reviewed (Codex+Claude converged; Gemini OAuth-unreachable). [Slice 2a v0.1.33: real base MELEE armor from units.csv — `UNIT_MELEE_ARMOR`/`effectiveMeleeArmor`, cavalry 2 / champion·treb·HCA 1 / bombard 2 / rest 0; 4 melee-vs-cav fixtures re-validated.]
- **AI findings → annotation bridge (v0.1.36, thread ai-findings-annotation-bridge):** connects the playtest conformance-findings pass to the annotation/marker system — the first use of the `author:'agent'` marker path built in the 2026-04-29 annotation-ui thread (previously the ONLY place `'agent'` appeared was the schema; nothing created one). `playtest:findings` now overlays each finding onto the run's bundle (`<prefix>.json`) as an annotation marker, so replaying it shows the findings in the existing `MarkerListPanel` list (agent badge + severity icon) + `TimelinePanel` pins for free (no new render code). New pure tested module `findingsToMarkers.ts` (`findingsToMarkers`/`injectAgentMarkers`/`deriveAnchorTick`); `.mjs` is thin glue. Anchor = run's LAST decision tick (findings carry no per-finding tick — coarse by design), no position; category always `ai` (gap-type kept in `data.findingCategory`), severity high→bug/medium→warning/low→info, text `[<gap-type>] <area>: <observed>`. In-place + idempotent (re-run replaces prior agent markers, preserves human; bundle-absent = warn-and-skip). Direct `bundle.markers[]` injection NOT `recorder.addMarker` (post-hoc, no live recorder; the validator actually permits retroactive past-tick markers so the tick isn't the blocker; replay-load is structural-only). TDD 18 tests; full suite 1301 green. Deferred: rich per-finding tick/position, map pins, ai/perf pin colors. spec §15.7.
- **Farms slices 1+2 — villager-built renewable food + auto-reseed (v0.1.34 + v0.1.35, threads farms-m1-slice1 + farm-reseed-m1-slice2):** the M1 food backbone. Slice 1: a villager BUILDS a `farm` (Dark Age, no prereq, 60 wood, 1×1, 480 HP, 15s) via the normal construction flow; on complete it gains a `resource` component (`farm`, 175 food) so `villagerEconomySystem` harvests food at berry-bush cadence. Entity model = building+resource HYBRID single entity (occupancy dispatches on `building` first → no double-claim; villager stands adjacent like a berry bush). Slice 2 (v0.1.35): farm AUTO-RESEED — at food=0, if the owner has ≥60 wood the farm refills to 175 in place for 60 wood (SAME entity, instant, no re-path, gathering continues); only a broke owner triggers the slice-1 removal (which clears the building side-maps, no orphan/cycle). Reseed reuses the construction charge trio (`canAfford`+`spendResources`+markDirty) + `constructionCost('farm')` + `FARM_FOOD_AMOUNT`; charged to the farm's `baseOwner`. Extracted `farmReseed.ts` to keep `villagerEconomySystem` <500. No save-format change. TDD (build→gather→deplete + two-cycle reseed-then-removed on the live bridge). Deferred: Mill batch-reseed queue + manual toggle, Mill anchor, farm sprite, AI farms (farm-upgrade techs ✅ v0.1.46).
- **Standard 200 population cap (v0.1.37, thread pop-cap-200):** the M1 ceiling, done correctly. `PopulationState` gains an honest unclamped `rawSupply` sum; `cap = deriveCap(rawSupply)=min(200,max(0,rawSupply))` is re-derived at all FOUR cap sites (entityCreateOps + playerCommandsSystem build-complete; two entityDestroyOps paths) + seed + the save codec/hydrate — one shared `deriveCap` in bridgeConstants, no inline 200s. Over-housing past 200 adds raw supply but not cap; losing housing while raw≥200 keeps cap 200; only raw<200 drops it (fixes the Codex iter-1 HIGH that a stored-clamp would drop 200→195 on a House loss at raw 245). Removed the `Math.max(current,…)` don't-evict FLOOR: losing housing can leave you over cap (current>cap) with NO eviction (already reachable via monk conversion; HUD shows e.g. 60/55), training blocked until current<cap. Training gate (`productionQueueSystem` current>=cap) unchanged. Save migration additive (bespoke `populationCodec` + schema-1 hydrate default `rawSupply=cap`, re-clamp; no schema bump). TDD `populationCap.test.ts` (13) incl. the lose-House-at-245→cap-stays-200 regression, no-eviction, reachability, round-trip + legacy-load. Deferred: configurable/"no-limit" cap option (deriver already parameterized), civ cap modifiers, AI stop-housing-at-200 (wastes wood at the ceiling; unreachable at current scale).
- **M7 graphics #2 — HUD chrome (v0.1.38, thread hud-chrome):** the flat dark-teal HUD panels get AoE2's framed wood/stone look via 100% PROCEDURAL CSS — no assets. New `src/hudChrome.css` (imported after `styles.css`) repaints only the panel CHASSIS: wood-plank gradient grain on `.hud-bar`/`.hud-panel`/`.hud-footer`, a stone-fleck radial-gradient on the `.hud-panel--map` minimap frame, a beveled raised edge (light TL + dark BR inset box-shadows) + an inner recess on all, and a sunken inset on the minimap canvas. Palette derived from existing gold-on-dark-teal `:root` tokens (no recolour); layout/buttons/chips/icons/`src/phaser/` untouched. CSS-only → no code/HTML change, contract-safe (controller selects by `data-hud`). Verified by the visual protocol: before/after/pixel-diff (`tmp/hud-chrome/`, 29.34% px changed — all on panel chassis; the Phaser canvas + panel text/buttons pixel-identical). Four gates green, full test 1314/2 unchanged. Deferred (separate `[ui]` items): UI icons, minimap terrain layer, fog fade.
- **M7 graphics #3 — UI icons slice 1 (v0.1.39, thread ui-icons):** the text-only resource chips + build buttons get small ORIGINAL procedural glyph icons (augment, don't replace the labels). New `src/ui/hud/icons/glyphs.ts` = hand-authored inline-SVG `resourceGlyph` (food sheaf / wood logs / gold coin / stone rock + pop house / age banner / time clock) + `buildingGlyph` (all 18 buildable types: house/windmill/axe/pickaxe/swords+shield/tower/anvil/stall/catapult/chapel/keep/dome/wall/stakes/field…), `stroke=currentColor`, ZERO `url()`/`<image>`/font/asset ref; `src/hudIcons.css` (imported after `hudChrome.css`) sizes/tones only — palette stays in CSS via the gold tokens. Contract kept: chip glyph is a SIBLING of `.hud-value` (value holds only the number → `toHaveText('200')` passes); build glyph goes inside the button before "Build X", `data-command` + click handler intact (extracted/exported `renderBuildButtons`). Nothing in `src/phaser/`, sim, or save-format. TDD 9 tests (self-contained SVG, no external refs, every building covered, hooks+text preserved). Visual protocol: before/after/pixel-diff (`tmp/ui-icons/`, 21.72% px — confined to the resource bar + Build-button panel; Phaser canvas pixel-identical). Four gates green, full test 1323/2 (+9). Deferred (later `[ui]`): unit-roster/selection icons (still "V"/"TC" text badges), non-build command-card icons, minimap terrain, fog fade.
- **Loom (v0.1.40, thread loom-tech):** the verified data-only Dark-Age TC tech from campaign-10, now wired. Researchable at the Town Center (Dark Age onward, no prereq, 50 gold, 250t); on complete every owned villager — existing AND future — gains +15 max HP (25→40, flat current+max bump) + +1 armor. Pure copy of the Wheelbarrow/Hand-Cart + blacksmith-tech patterns: `'loom'` in the type union + `RESEARCHES_BY_BUILDING` TC row + the two compile-enforced cost/time tables + both TC option branches (appended after age-up/carry); +HP/+armor applied imperatively to existing villagers in a new `applyTechnology` `case 'loom':` (FLAT, not via the ratio-preserving `upgradeOwnedUnits`; the already-researched guard makes it race-safe) and to new ones in `combatStateFactory`; `formatTechnologyName` `loom→'Loom'`. Per the lead's call, the +1 armor uses the SINGLE `CombatState.armor` scalar (+1 melee/+1 pierce, reducing both at the damage site) — NO save change, NO `CombatState` split; AoE2's +2 pierce (vs +1) DEFERRED to the melee/pierce-armor-split slice (spec §11.8 + roadmap M2 follow-up). TDD 17 tests; updated TC research-option assertions (5+6) since the TC now offers Loom. Four gates green.
- **M7 graphics #5 — building visuals, slice 1 (v0.1.42, thread building-visuals-slice1):** the building analogue of the units slice. Buildings were GENERIC (all types drew the same body+roof, differing only by footprint+tint — the renderer never read `entityType`); now each COMPLETED building draws a distinct per-ROLE procedural silhouette (13 roles over 18 types, grouped like `unitRole`): TC hall, crenellated castle, domed wonder, pitched-roof house, windmill-blade mill, furrow-row farm, lean-to+stockpile drop-site, banner-flag military hall, anvil+chimney blacksmith, striped-awning market, cross-finial monastery, battlement tower, low-merlon wall. New `gameScene/buildingRole.ts` (exhaustive `satisfies Record<BuildingType,BuildingRole>`) + `buildingSilhouettes.ts`; `buildingRenderer.ts` slimmed to an orchestrator (construction + memory paths byte-identical, only the completed body+roof varies). Body fill = the existing `buildingTint`; all primitives within the footprint rect so HP-bar/selection geometry is unchanged; visual-state FLAG contract preserved (completed→body/roof/completion; construction→foundation/scaffold). Render-only, 100% procedural (no assets), pure per-frame (no random/time); no sim/save/contract touch; GameScene untouched at 1017. TDD 27 tests (role map + exhaustive + per-role distinct primitives + footprint containment for all 18 + variant flags). Visual protocol via a test-only `building-showcase-fixture`: before/after/pixel-diff in `tmp/buildings/` — 10.72% px, confined to building footprints (HUD/terrain/health-bars/selection/minimap pixel-identical). Four gates green, full test 1404/2 (+27). Reviewer comments pending (main agent runs the multi-CLI review). spec §14.5.
- **M7 graphics #4 — units beyond colored circles, slice 1 (v0.1.41, thread units-beyond-circles):** units render as READABLE PER-ROLE procedural silhouettes with FACING instead of flat tinted circles. New `gameScene/unitRenderer.ts` (extracted like terrainRenderer/buildingRenderer so legacy `GameScene` stays net-zero, 1018→1017): `unitRole` (pure, exhaustive `satisfies Record<UnitType,UnitRole>` — 7 roles cover all 34 types: villager/infantry/archer/cavalry/cavalry-archer/siege/monk; `skirmisher→archer` is render-owned, not the sim's ARCHER_LINE), `unitFacingRadians(prev,current)` (pure heading from the per-tick projected-position delta the interpolation already tracks → NO sim/bridge/contract change; idle→rest orientation, no fabricated heading), and `drawUnit` (2-4 Phaser primitives per role, body=owner tint, all within the unit's bounding circle so health-bar/selection geometry is unchanged). Facing genuinely available render-side; animation + per-UNIT silhouettes deferred. Render-only, 100% procedural (no assets); no sim/save/contract touch. TDD 35 tests (role map + exhaustive + skirmisher, facing angles/idle-null, per-role primitive sets, bounding-circle containment). Visual protocol via a new test-only `unit-showcase-fixture`: before/after/pixel-diff in `tmp/units/` — 0.29% px changed, confined to the 7 unit positions (HUD/TC/terrain/health-bars/minimap/fog pixel-identical). Four gates green, full test 1375/2 (+35). Reviewer comments pending (main agent runs the multi-CLI review). spec §14.5.

## 2026-06-12 (replay fog-owner toggle v0.1.22 + engine 1.0.x absorb + README replay instructions)
- **Fog-owner toggle shipped (thread done/replay-fog-owner):** timeline-panel `Fog: P<n>` button + Alt+F cycles the rendered fog perspective — campaign replays watchable from the agent's seat. makeReplayBridge `fogOwner` option; ReplayController fog API (bridge-rebuild over same world, transactional, P1 reset per entry, candidates cached); ghosts P1-only; HUD stays P1. 3-CLI converged iter-2 on the adapter-leak (outgoing RenderAdapter never disconnected on same-world rebuilds) → explicit `disposeReplayRenderAdapter` lifecycle. 1215 tests; spec §15.3; README documents watching replays. **civ-engine 1.0.x absorbed:** strict-by-default → saveGame flush wrapped in `runMaintenance`; 1.0.1 factory contract → test stubs use real createReplayWorldOnly; a "+50-75% sim-throughput regression" was filed + caps doubled in 9 files — **later (2026-06-30) proven NOT-a-regression (full-suite contention); caps ratcheted back, engine-feedback resolved (top entry)**; vitest CRITICAL advisory fixed via `npm audit fix`.

## 2026-06-11 (campaign-2 FEUDAL REACHED + agent affordances v0.1.21 + visual baselines removed)
- **Campaign-2 (17 decisions, 4250 ticks, $15.72) on v0.1.21: Feudal completed ~tick 3100** (campaign-1 only QUEUED it at 3500). Rejections 27→1 (the 1 was an anticipated wood race confirmed by the new need-vs-have message); blind placements 19→0 (every building on a NAMED placementHints anchor, both footprint sizes); prereq rule known at t250 from buildingOptions (was ~7 burned decisions). Cost/decision +6%. Run ended early at 4250/6500: transient `claude exit 1` misclassified as engineHalt — new backlog: distinct providerError stopReason + retry (HIGH, harness); military units don't auto-attack buildings (gameplay); `--out` is a prefix (operator note).
- **Actionable rejections + agent affordances shipped (thread done/agent-affordances):** cannot_research/cannot_train/insufficient_resources/placement_blocked messages now state the actual unmet rule (shared reason engine `researchAvailability.ts`; placement causes named via getCellStatus claims + nearest open owner-visible anchor); HUD research/placement toasts pass the message through. New `SimulationBridge.getAgentBuildingOptions` + `findOpenPlacementAnchorsNear` → snapshot `buildingOptions` + `placementHints` blocks → tactical prompt. Validator decisions byte-identical (message-only). Review: 3-CLI iter-1 (zero runtime defects; Codex quota recovered) + iter-2 fix-verify CONVERGED; 2 LOW hardenings folded (gates consume AGE_ADVANCE_REQUIRED_COUNT; non-creating in-flight probe). 1198 tests; spec §15.2/§15.7; campaign-2 queued as the validation gate. Engine v0.8.20-0.8.23 absorbed (zero exposure). Also v0.1.21: visual baselines removed (option C) — a non-deterministic player has no "correct" reference image; checkpoint screenshots stay dashboard-only.

## 2026-06-10 (playtest-fixes shipped + engine v0.8.15 absorb)
- **Campaign-1 (5000 ticks) — first full-length LLM playtest:** plumbing flawless (20/20 decisions, exact ticks, all 5 checkpoints, 60.9MB bundle, $18.19); grounded oracle verdict looked-fun with accurate agent-state notes. Agent reached pop 20/25 + mill/barracks and queued Feudal at tick 3500 (completes ~5300, just past cutoff) after reverse-engineering the two-Dark-Age-buildings rule from 8 bare cannot_research rejections. Ranked backlog mined into the devlog: 1) actionable rejection messages, 2) research-options in snapshot, 3) placement-validity hints (19 blind placement failures), 4) OPERATOR: baseline policy — checkpoints 3000-5000 now diff 9.86% > the 5% corpus HIGH gate (baselines predate a playing agent); recommend advisory-only visual oracle for LLM rows, 5) late-game cost ~$0.83/call.
- **LLM agent can actually play now (playtest-fixes A-F):** snapshot + tactical prompt gain own units/buildings/nearby resources with real entityIds (+"never invent ids"); dispatch verdicts feed back into the next prompt with a REJECTED warning; sim pauses while the agent thinks (atomic unpause→stepN→repause advance = exact tick semantics, checkpoints align); bundle export rewritten to chunked page.evaluate (blob: was dead under Playwright — engineHalted every real run); diagnostics honest; ALL playtest LLM calls now `claude-fable-5` ($10/$50 in the cost table; stale Opus row corrected). Verification: **37/40 commands accepted** (was 0/42), exact 250-tick advances, checkpoint 1000 captured+diffed, 6.1 MB bundle shipped, maxTicks clean exit. Test ratchet: llmRunner.test.ts 692-pin DELETED via 3-way split; agentSnapshot tests split too. Spec §15.7 + memory updated.
- **civ-engine v0.8.15/16 absorb + oracle grounding (finding G) + ownership (threads done/oracle-grounding + done/playtest-fixes):** v0.8.15's invariant-TComponents refactor → 17 files' `CivWorld` aliases unified onto `GameWorld` + single `toEngineWorld`/`fromEngineWorld` cast seam at the recorder/replayer/debugger/adapter boundaries (96 type errors → 0). Observation oracle now reads the AGENT's per-player state (had graded off the human HUD); corpus smoke fog-filtered. Codex HIGH: agent commands weren't owner-scoped → `checkAgentActorOwnership` + `dispatchAgentCommand({expectedOwner})` + `'not-owned'`, fail-closed, 9 tests; snapshot clock tps 50→10. Final gates: 1159 passed.

## 2026-04 → 2026-05-09 (foundation + deterministic/LLM playtest infrastructure — all shipped + stable; full detail in the detailed devlogs)
- **Foundation (2026-04, engine 0.8.11):** TS+Vite+Phaser3+civ-engine bootstrap; 18 civs; economy loop; deterministic combat; Slices 1-12 (Castle/Imperial, 12 units, 14 techs, Wonder/Relic/Score wins, save/load, Black Forest+Arena, F2 debug); FU 1-8 (blacksmith/upgrade lines, AI Castle/Imperial+Monks+Wonder, defensive fire, Britons/Persians); `createSimulationBridge` 9519→332 LOC split; full-review V4+V5; annotation UI.
- **Command-queue + schema-2 (v0.1.6, 05-02..04):** all 15 commands commandified through `GameCommands`+dispatcher (AI emits intentions, no longer mutates); tier-1 state → `world.state.aoe2.*` via a 35-codec SlotCodec registry; schema-2 saves (schema-1 still loads); visibility fingerprint cache.
- **Replay stack + deterministic/LLM playtest loops (v0.1.7-0.1.20, 05-05..09):** `ReplayController` (live/replay swap, coalesced scrubbing, transactional enterReplay) + `TimelinePanel`/hotkeys + `ReplayLoadDialog` + Playwright e2e + multi-villager construction (`additionalBuilderIds`, mid-build join, v0.1.17); the deterministic recording runner + pure oracles (match-completes/no-tick-failures/no-perf-regression/no-pinned-units) + position replay + fix-bot + corpus + CI + group-move spiral-BFS reservation; the LLM agent core (LlmProvider, two-tier prompts, codegen'd 15-command tool schemas, cost budget), Playwright Blob-URL runner, pixelmatch oracle, per-owner visibility-gating, winner oracle, auto-apply fix loop, and `ClaudeCodeProvider` (subscription, no API key, via `claude -p`).
