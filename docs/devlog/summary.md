## 2026-06-12 (README replay instructions)
- **README gains "Watching replays (incl. LLM playtest campaigns)":** bundle locations (`--out` prefix semantics), HUD Replay… → From file import flow, hotkeys, player-1 fog caveat. Claims verified against live tree; doc-only, no bump. Fog-owner toggle queued next (user-approved).

## 2026-06-11 (campaign-2 — FEUDAL REACHED, affordances validated)
- **Campaign-2 (17 decisions, 4250 ticks, $15.72) on v0.1.21: Feudal completed ~tick 3100** (campaign-1 only QUEUED it at 3500). Rejections 27→1 (the 1 was an anticipated wood race confirmed by the new need-vs-have message); blind placements 19→0 (every building on a NAMED placementHints anchor, both footprint sizes); prereq rule known at t250 from buildingOptions (was ~7 burned decisions). Cost/decision +6%. Run ended early at 4250/6500: transient `claude exit 1` misclassified as engineHalt — new backlog: distinct providerError stopReason + retry (HIGH, harness); military units don't auto-attack buildings (gameplay); `--out` is a prefix (operator note).

## 2026-06-11 (agent affordances v0.1.21 — campaign-1 backlog #1-#3)
- **Actionable rejections + agent affordances shipped (thread done/agent-affordances):** cannot_research/cannot_train/insufficient_resources/placement_blocked messages now state the actual unmet rule (shared reason engine `researchAvailability.ts`; placement causes named via getCellStatus claims + nearest open owner-visible anchor); HUD research/placement toasts pass the message through. New `SimulationBridge.getAgentBuildingOptions` + `findOpenPlacementAnchorsNear` → snapshot `buildingOptions` + `placementHints` blocks → tactical prompt. Validator decisions byte-identical (message-only). Review: 3-CLI iter-1 (zero runtime defects; Codex quota recovered) + iter-2 fix-verify CONVERGED; 2 LOW hardenings folded (gates consume AGE_ADVANCE_REQUIRED_COUNT; non-creating in-flight probe). 1198 tests; spec §15.2/§15.7; campaign-2 queued as the validation gate. Engine v0.8.20-0.8.23 absorbed (zero exposure).

## 2026-06-11 (visual baselines removed from LLM harness — option C)
- **Baseline comparison deleted, not de-gated:** the committed PNGs dated from the no-op-agent era; with a playing agent the diff measured expected divergence, not regressions. Removed runVisualOracle wiring + envelope.visualOracle + corpus HIGH gate + auto-fix visual trigger + baselines/ + capture-baselines script; checkpoint screenshots stay (--screenshot-every, default 1000) as dashboard-only thumbnails; visualOracle.ts retained unwired for a future deterministic corpus row. Coverage decomposition in spec §15.7 (determinism → replay suites; render → Playwright browser tests; LLM health → engineHalt + oracles + grounded observation).

## 2026-06-10 (playtest-fixes shipped + engine v0.8.15 absorb)
- **Campaign-1 (5000 ticks) — first full-length LLM playtest:** plumbing flawless (20/20 decisions, exact ticks, all 5 checkpoints, 60.9MB bundle, $18.19); grounded oracle verdict looked-fun with accurate agent-state notes. Agent reached pop 20/25 + mill/barracks and queued Feudal at tick 3500 (completes ~5300, just past cutoff) after reverse-engineering the two-Dark-Age-buildings rule from 8 bare cannot_research rejections. Ranked backlog mined into the devlog: 1) actionable rejection messages, 2) research-options in snapshot, 3) placement-validity hints (19 blind placement failures), 4) OPERATOR: baseline policy — checkpoints 3000-5000 now diff 9.86% > the 5% corpus HIGH gate (baselines predate a playing agent); recommend advisory-only visual oracle for LLM rows, 5) late-game cost ~$0.83/call.
- **LLM agent can actually play now (playtest-fixes A-F):** snapshot + tactical prompt gain own units/buildings/nearby resources with real entityIds (+"never invent ids"); dispatch verdicts feed back into the next prompt with a REJECTED warning; sim pauses while the agent thinks (atomic unpause→stepN→repause advance = exact tick semantics, checkpoints align); bundle export rewritten to chunked page.evaluate (blob: was dead under Playwright — engineHalted every real run); diagnostics honest; ALL playtest LLM calls now `claude-fable-5` ($10/$50 in the cost table; stale Opus row corrected). Verification: **37/40 commands accepted** (was 0/42), exact 250-tick advances, checkpoint 1000 captured+diffed, 6.1 MB bundle shipped, maxTicks clean exit. Test ratchet: llmRunner.test.ts 692-pin DELETED via 3-way split; agentSnapshot tests split too. Spec §15.7 + memory updated.
- **civ-engine v0.8.15 absorb (96 type errors → 0):** the engine's layer-chain refactor made World's TComponents effectively invariant; unified 17 files' local `CivWorld` aliases onto `GameWorld` (codemod) + single sanctioned cast seam (`toEngineWorld`/`fromEngineWorld` in pureHelpers) at the SessionRecorder/SessionReplayer/WorldDebugger/RenderAdapter boundaries. Engine ask (thread TComponents through recorder/replayer) recorded in engine-feedback.
- **Oracle grounding (finding G) + engine v0.8.16 check:** observation oracle now receives the AGENT's final per-player state and is told the screenshot HUD belongs to the passive human observer (it had graded a competent run "looked-broken" off the wrong player's panel); cost guidance re-derived ($0.55/call observed → ~8 decisions on the default $5; spec updated); corpus smoke row now runs fog-filtered (omniscient dropped). Engine v0.8.16 (session/replay hardening, findNearest rewrite) checked: zero aoe2 exposure, suite green unchanged. Review converged iter-1 (doc-accuracy items only; thread done/oracle-grounding). Queued for operator decision: committed baselines predate the playing agent (re-capture policy).
- **Multi-CLI review converged at iter-2 (thread done/playtest-fixes):** iter-1 Codex HIGH — agent commands were not owner-scoped (engine validators carry no actor identity; the LLM could command enemy entities) → `checkAgentActorOwnership` + `dispatchAgentCommand({expectedOwner})` + `'not-owned'` reason, fail-closed default arm, 9 tests; dispatch-observer log scoped via runtime-only `agentIssued` tag (never persists). Iter-2 (both reviewers independently): pre-queue rejections now merge into the agent's feedback loop; wolves/relics filtered from gather targets; snapshot clock fixed (hardcoded tps 50 → real TPS 10; agent's clock ran 5× fast). Ownership work split `browserTestAgentApi.ts` out of browserTestApi (314+251). Ownership smoke: 14/14 accepted, exact ticks. Final gates: 1159 passed.

## 2026-06-09 (thread housekeeping + LLM playtest verification + review-model bump)
- **Review model bumped to `claude-fable-5[1m]` across all 9 repos' AGENTS.md** (aoe2, civ-engine, civ-sim-web, idle-life, investing, news, photo, pixel_lab, town) per the keep-model-IDs-current rule; smoke-tested (`echo | claude -p --model "claude-fable-5[1m]" --effort max` → ok) before committing. `investing/README.md`'s `claude-opus-4-7` mention intentionally untouched — it documents that app's backend default, not review config.
- **LLM playtest verified live on ClaudeCodeProvider (no API key):** harness plumbing passes end-to-end (auto-detect, 8 Sonnet tactical + Opus strategy + observation calls via `claude -p`, $1.43 tracked, trace/envelope written, winner omitted on engineHalt per contract; observation verdict `looked-broken` — accurate). Five findings, A gating: tactical prompt never renders own units/buildings with entityIds (`llmPromptBuilder.ts:55-62`) → model fabricates ids → ALL 42 commands rejected (`not_a_building`/`not_a_unit`) — agent has zero agency. B: game self-ticks during ~60-100s claude latency (bridge tick 5413 vs maxTicks 2000) → baseline checkpoints never align, visual oracle inert. C: 62.5 MB bundle blob export fails (`blob:` unsupported by Playwright apiRequestContext.fetch) → engineHalt. D: misleading skip diagnostic. E: dispatch rejections not fed back into next-decision context. Details in detailed devlog; fix thread queued.
- **Converged threads relocated + doc accuracy + red-gate pin:** 8 thread folders that converged 2026-05-09 (`full/2026-05-01`, `llm-agent-claude-code-provider`, `llm-agent-phase-6a`–`6e`) moved `current/` → `done/` via `git mv`; `docs/threads/current/` is now empty. ARCHITECTURE.md's stale `threads/current/playtest-loop` pointer fixed to `done/`. Summary compacted from ~117 lines to budget. Discovered fileSizeBudget red at HEAD — `llmRunner.test.ts` (692 LOC) grew past 500 in f2df6ce without a legacy entry because Phase-6 sessions iterated affected-tests-only; pinned at 692 per the oracles.test.ts precedent, split queued alongside GameScene.ts (1018). Lesson added. AI age-up test cap 180s→300s (contention flake: 187s under load, 139.7s isolated). Doc + test-hygiene only, no bump.

## 2026-05-09 (LLM-agent playtest thread closed + Phase 6 complete + ClaudeCodeProvider)
- **Thread closed after 3 review iterations; 17 commits.** Server-lifecycle hardening (`taskkill /F /T` / process-group kill so vite preview can't squat port 5174), pixelmatch `diffMask: true`, retention pruning rewrite. Baseline PNGs captured + committed for `aoe2-prototype` seed at ticks 1000-5000 — visual oracle has its source of truth.
- **`ClaudeCodeProvider`:** playtest harness runs on Claude Code subscription, no API key — shells to `claude -p` with stream-json + `--json-schema`, parses `structured_output`, synthesizes legacy content shape. Runner auto-selects claude-code → api → fail-loud; `--provider` overrides. Tool-surface lockdown (`--permission-mode plan`, `--strict-mcp-config`, cwd:tmpDir). ~$0.10-0.30/call due to subscription's ~15K cache prelude. `llmProviders.ts` split into 4 sub-500-LOC modules.
- **Phase-6.A quick wins:** ESLint `no-restricted-imports` guard against `@anthropic-ai/sdk` bundle leak; `assertEconomyShape` fail-loud schema-drift guard in `buildAgentSnapshot`; timestamped corpus output dirs.
- **Phase-6.B:** per-owner visibility-gating for agent snapshot — `enemies` filtered through `VisibilityProbe` by default (building footprint walk + unit single-cell); `omniscient: true` opt-in preserves cheat-mode; `SimulationBridge.isCellVisibleForOwner` added.
- **Phase-6.C.1 + 6.C.2:** visual oracle wired into runner (checkpoint screenshots at exact baseline ticks, HIGH ≥5% gates corpus, HTML drift dashboard with thumbnails via `playtest:corpus-dashboard`); post-hoc observation oracle (`--observation`) emits advisory `looked-fun | looked-broken | inconclusive` verdict into the envelope with cost rolled up.
- **Phase-6.D winner oracle:** `envelope.winner` populated unconditionally on clean exits (`winner | in-progress | tie`) from per-owner unit/building counts; omitted on engineHalt; count-probe failures escalate to engineHalt.
- **Phase-6.E auto-apply fix loop:** `applyAndGate.ts` validates patch (`git apply --check`), branches, applies, runs gates, commits; `playtest-llm-auto-fix.mjs` orchestrates playtest → LLM `submit_fix_diff` on regression → N=3 counterfactual validation (any halt rejects); never auto-pushes; HEAD-ref ownership detection protects operator branches. 196 LLM-thread tests; bundle 554.71 kB.

## 2026-05-08 (LLM-agent playtest phases 1-5 + playtest loop closed + spec §12.7)
- **LLM-agent phases 1-5 shipped:** browser test API (`__AOE2_TEST__.agent`, `?disableAi=` gating); LLM agent core (LlmProvider + Mock/Anthropic providers via dynamic import, two-tier strategy/tactical prompts, codegen'd tool schemas for all 15 GameCommands, cost-budget enforcement with abort); Playwright runner (`runLlmPlaytest` pure loop over `RunnerHost`, `playtest-llm.mjs` orchestrator, vite preview, Blob-URL bundle round-trip, crash-surviving trace JSONL); visual-regression oracle (pngjs + pixelmatch, medium ≥0.5% / high ≥5%, ignoreRects); baseline capture script; CI (`playtest-llm.yml`: nightly cron, dispatch, PR label opt-in gated on `vars.ANTHROPIC_KEY_AVAILABLE`, skip-clean without key).
- **Deterministic playtest loop closed (phases 1-5):** bridge-driven recording runner (`runPlaytest.ts` + `scripts/playtest.mjs`, stop-reason envelope); pure oracle layer (match-completes, no-tick-failures, no-perf-regression, no-pinned-or-oscillating-units) + position replay; fix-bot (`propose-fix.mjs`, propose-only via `git apply --check`); corpus runner + `playtest-corpus.json` + SUMMARY.md; CI workflow on PR + main with artifact upload and PR comment. Windows CVE-2024-27980 spawn mitigation; garrison/destroy false-positive fixes.
- **Spec §12.7 movement targets + slot reservation (v0.1.18-0.1.20):** single-unit lazy redirect on arrival to packed cells; group right-click pre-reserves one cell per unit via deterministic spiral BFS (`allocateGroupMoveTargets`); snap-on-stop renders stationary shared-cell units at engine 16-slot offsets; spawn-time snap deferred (undiagnosed fogMemory/selectionActivity dependency); reservation lifecycle registry deferred. Spec §12.6/§12.7 written first per the new AGENTS.md spec-persistence rule.

## 2026-05-07 (multi-villager construction)
- **v0.1.17:** placeConfirm gains `additionalBuilderIds`, mid-build right-click joins builders, per-tick `patchComponent('renderable')` dirties the site so the HP bar moves. 2 review iterations; 3 real bugs caught and fixed.

## 2026-05-05 to 05-06 (replay system, v0.1.7-0.1.16)
- **Replay stack shipped end-to-end:** `ReplayController` (live/replay bridge swap, coalesced scrubbing, transactional enterReplay), `TimelinePanel` + replay hotkeys, `ReplayLoadDialog` modal unifying three load sources (live session / IDB prior sessions / file import with structural validation), cancel-race generation counters, escapeHtml consolidation, Playwright e2e (prior-session, scrub workflow, dialog interactivity CSS fixes). Threads `replay-scrubber`, `replay-load-and-e2e`, `replay-deferred-followups` all done.

## 2026-05-02 to 05-04 (v0.1.6 command-queue + schema-2 arc)
- **All 15 gameplay commands commandified** through `GameCommands` + dispatcher drain; AI emits intentions instead of mutating. Tier-1 state migrated to `world.state.aoe2.*` via SlotCodec registry + accessor (35 codecs); schema-2 saves (`seed + worldSnapshot` only, schema-1 still loads); `aoe2.pendingCommands` persists queued AI intentions; visibility fingerprint cache; vitest pool stabilization. /full-review iter-1+2 correctness batch.

## 2026-04 (foundation)
- **04-23 to 04-29 (v0.1.5):** `createSimulationBridge.ts` 9519 → 332 LOC (bridge/ + 18 system factories + ops modules); fixtures/ + mapGeneration/ splits; full-review V4+V5 (40 fixes); annotation UI (Alt+M/Alt+L, IDB sessions, JSON export); engine 0.8.11.
- **04-17 to 04-22:** Slices 1-12 (Castle/Imperial ages, 12 units, 14 techs, Wonder/Relic/Score wins, save/load, Black Forest + Arena, F2 debug) + FU batches 1-8 (blacksmith techs, upgrade lines, AI Castle/Imperial + Monks + Wonder, defensive fire, Britons, Persians, HUD UX, test stability).
- **04-10 to 04-13:** bootstrap — TS + Vite + Phaser 3 + civ-engine; 18 civs; economy loop; Dark→Feudal→Castle core; deterministic combat.
