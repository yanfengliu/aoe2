## 2026-04-26
- **Building HP ramps from low to full during construction (canonical AoE2):** foundations spawn at `max(1, floor(maxHp * 0.1))` and gain `(maxHp − startHp) / totalBuildTicks` per tick (additive). Auto-aggression now pursues enemy foundations. HUD floors fractional HP at display site. Save/load round-trips mid-construction HP. Version 0.1.3 → 0.1.4.
- **AGENTS.md mandatory code-review rule:** added Core-rules bullet making multi-CLI review mandatory for every behavior change before declaring done; renamed Code review section header to `(mandatory; not optional)`. Synced across 8 repos via `/sync-instructions`.
- **Engine fail-fast handler:** `world.step()` wrapped via `bridge/tickHaltGuard.ts` (`createTickHaltState` + `tryTick`). Catches `WorldTickFailureError`, captures `{tick, phase, code, systemName, message}` into `haltState.halted`, halts further ticks. New `HudState.engineHalted` field. Version 0.1.2 → 0.1.3.
- **Iter-2 multi-AI review batch-1:** V5-1 `monksByOwner` side map; V5-3 extracted `ALL_UNIT_TYPES` to `gameScene/unitTypeMap.ts` (GameScene 1034 → 994 LOC); V5-4 saveLoadPanel localStorage fallback; V5-8 batched `findIdleProducer` via `ownerBuildingsByType`.
- **`createSimulationBridge.ts` shrink — 7606 → 319 lines (96%):** Phase 4+5: 18 ECS systems → `bridge/systems/*`, 17 ops modules → `bridge/`, side-map ownership consolidated in `bridge/bridgeState.ts`, plus follow-up sweep extracting `createWorld`/`wireBridgeOps`/`wirePostSeedOps`/`assembleBridgeApi`/`registerAllSystems`/`registerBridgeSystems`/`scenarioSeedOps`/`hydrateFromSavedGame`/`monkAiSearchHelpers`+`monkTaskAppliers`/`renderStateOps`/`selectionFinders`. Determinism preserved via explicit `before`/`after` deps.
- **Iter-4 multi-AI review batch-1:** dropped type-erasing casts in `registerBridgeSystems`; V4-3 fog-memory delete uses `isFootprintVisible`; V4-7 `gathererDropOffStuckSinceTick` in `SaveBlob`; V4-8 lazy scenario gen on save-load; V4-9/V4-10 inline-filter perf wins; V4-12 AI Monk-task skip when AI owns no Monks; V4-14 monk convert guard switched to `Map<targetId, tick>`; V4-19 `bridge/sharedTypes.ts` extracted.
- **Vitest parallelism (3.3× speedup):** `pool: 'forks'` + `maxForks: 4`. Serial 18.8 min → 4-fork 5.7 min.
- **GameScene `buildingRenderer.ts` extraction:** scene 1176 → 1034 lines.

## 2026-04-25
- **Iter-3 review batch + footprint-visibility sweep (22+ fixes):** V3-1+V3-2 footprint visibility for fog memory + click selection; V3-3 FU2 units in double-click whitelist; V3-9 Wonder display name; V3-12 conquest mutual-annihilation = `'draw'`; V3-13 default map landmarks footprint-aware; V3-14 `seedToNumber` uses full code points; V3-15 AI Watch Tower no longer requires Blacksmith; V3-22 `?seed=` empty value warns; V3-7 Monk conversion vision/LOS gate; V3-20/V3-21 build hardening; V3-11/V3-17 HUD `destroy()` + `getPlacementPreviewAt` parity; V3-5 H2-2 retry throttle (per-gatherer stuck-since tick + 30-tick window); V3-6 H2-1 `inFlightTechByOwner` Set; V3-24 AI `findIdleProducer` load-balanced; V3-8 save-load `pruneOrphanEntityKeys` walks all entity-id-keyed side maps; V3-18/V3-19 `renderFog` memo + `getRenderState()` per-tick cache.
- **Iter-2 review batch-1:** V2-1 `getHudState()` returns saved-game seed; H2-1 `applyTechnology` idempotency guard + cost dedupe; H2-2 gather preserves carry on null drop-off path; H2-3 Black Forest `POCKET_RADIUS` 6 → 7; M2-1 building target finding uses `isFootprintVisible`.
- **`civ-engine` 0.5.3 migration:** in-place renderable mutations need explicit refresh. Fixed construction-complete `visualVariant` and `bridge/technologyOps.ts:upgradeOwnedUnits` (tech-upgrade mutates `unit.unitType` + `renderable.tint`/`size` + `visionSource.radius`).
- **Iter-1 multi-AI review batch-1:** C-1 Monk conversion flip-flop (per-tick guard moved above reset); H-1 save-load projector seed; H-3 garrison side-map cross-reference invariant; M-16 ARCHITECTURE.md `bridge/` topology refresh.

## 2026-04-24
- **Auto-aggression — idle military pursues, idle villagers defend:** new `prototypeAutoAggression` system. Military uses live `visionSource.radius`; villagers use radius 1 (Defensive Stance, gather orders honored). New `disableAi` on `PlayerStartSpec` for fixture-passive enemies.
- **Sheep show up in selection panel icon grid for multi-selections:** added `id` to `EconomyState.resources`; `renderSelectionIcons` in `selectionPanel.ts` builds `sheepById` map and emits chips via entity-helpers.
- **Default-map narrow-path fix:** `cellBlockedByScenarioEntity` now reserves four guaranteed-clear cardinal exit corridors (2-row horizontal + 2-column vertical, extending 13 cells) so no ring arc can seal starting villagers in a pocket.

## 2026-04-23
- **`prototypeScenario.ts` god-file split (9968 → 869 lines):** every fixture factory + map-gen helper extracted into `src/game/simulation/fixtures/` (13 category modules + barrel) and `src/game/simulation/mapGeneration/` (6 modules). 800+-line fixture files later split into themed sub-directories with barrel re-exports.
- **`createSimulationBridge.ts` Phase 1-3 `bridge/` extractions:** factored pure helpers, visibility/projector, trebuchet pack/unpack, fog-memory ops, monk task subsystem, technology, match-end, AI decision helpers, placement, save serializer, target-finding into `src/game/simulation/bridge/`. 9519 → 7214 lines.
- **`GameScene.ts` `gameScene/` extraction:** factored `debugOverlay`, `worldLayers`, `selectionLayers`, `cameraController` into `src/phaser/scenes/gameScene/`. 1737 → 1161 lines.
- **Selection info panel shows activity:** new bridge module `src/game/simulation/selectionActivity.ts` emits `{ verb, target }`; HUD's `formatActivityLabel` in `selectionPanel.ts` composes via `displayNames.ts`.
- **Resource cell exclusivity + procedural default map:** new `mapGeneration/spawnList.ts` (first-write-wins) + `mapGeneration/defaultMap.ts`. Bridge-boot resource-on-resource validator added.
- **Selection panel compact-icon grid + `civ-engine` `OccupancyBinding` integration + bundled IBM Plex Sans typography + fullscreen-only edge-pan safeguard.**

## 2026-04-22 and earlier
- **Precise unit selection + browser spec split:** exact rendered-body geometry for click + drag-box; live marquee preview matches final selection.
- **Prototype rule-table extraction:** `prototypeEconomyRules.ts`, `prototypeBuildingRules.ts`, `prototypeUnitRules.ts` extracted from bridge.
- **Sheep vision from ownership; move-order path caching (`movePathCache` side map); FU8 vitest worker config; FU7 Trebuchet pack/unpack + Wonder/Relic tie-break + score tuning; FU6 HUD controller split (2173 → 433); FU5 Save/Load HUD + Playwright; FU4 AI improvements (Castle/Imperial/Monastery/Wonder); FU3 defensive-fire polish + real walls; FU2 Militia line + Paladin + Heavy Camel; FU1 armor + full Blacksmith progression; Slice 12 fixture validator + F2 overlay; Slice 11 maps; Slice 9 Save/Load (`SaveBlob` + 29 side maps); Slice 8 Wonder/Relic/Score; Slice 7 Imperial; Slice 6 Castle + Longbowman; Slice 1-5 sheep + fog memory + Castle Age + Camel + Siege Workshop + Monastery.**
- **Bootstrap (2026-04-10..13):** TypeScript + Vite + Phaser 3 + `civ-engine`; AoE2 spec finalized (18 supported civs); full Dark/Feudal/Castle core; 60x36 default map; sub-grid movement.
