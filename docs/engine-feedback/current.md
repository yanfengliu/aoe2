# civ-engine Feedback

Live engine feedback belongs here. Historical observations and slice-by-slice notes live in [past.md](./past.md).

## Current verdict

- Status: `civ-engine` remains viable as the authoritative simulation core for this project.
- Coverage so far: the repo has now shipped through Slice 12 on top of the current engine boundary.
- What still fits cleanly behind the bridge: upgrades, age gating, combat bonuses, conversion, relics, win conditions, save/load, AI, maps, and debug overlays.
- The earlier occupancy revisit is now closed in both repos: `civ-engine` shipped `OccupancyBinding`, and `aoe2` now uses it for bridge-side occupancy queries.

## Current engine asks

- **Every component write deep-walks the component to assert JSON compatibility, with no way to turn it off — measured at ~10% of late-game tick time (surfaced 2026-09-01, engine 2.4.1).** `ComponentStore.set` calls `assertJsonCompatible(component)` on EVERY write in the default `strict` diffMode (and `jsonFingerprint`, another full walk, in `semantic`). `assertJsonCompatible` allocates a fresh `WeakSet` per call and recursively visits every field of the component. There is no opt-out: no `NODE_ENV` gate, no constructor option, no sampling — `grep -rln "skipValidation|validateComponents|NODE_ENV" dist/*.js` returns nothing. **Measured**, CPU profile of `default-seed` self-play warmed 33,000 ticks (55 units, 50 buildings, 8.48 ms/tick): `visit @ json.js` **8.2% self time**, plus `jsonFingerprint` 1.5% and GC 1.8%. The writes driving it are legitimate, not aoe2 waste — instrumenting `world.setComponent` over 300 late-game ticks counted **43.5 writes/tick (42.2 of them `unitTransform`) of which only 0.2% were redundant** (value identical to what was stored), i.e. 0.77 writes per unit per tick for units that move every tick. So aoe2 cannot buy this back by writing less. **Ask:** a way to disable or sample the JSON-compatibility assertion once a build is past development — a constructor option, an env gate, or validating only on first write of a component TYPE rather than every write. A correctness assertion of this value is worth paying during development and is pure overhead in a shipped match. Recorded per the report-don't-modify-engine policy.
- **`buildDiff()` runs unconditionally inside `world-tick`, so headless runs pay for a render diff nobody reads (surfaced 2026-09-01, engine 2.4.1).** `world-tick.js:131` calls `this.buildDiff()` in every tick with no consumer check; it then iterates every component store's dirty set plus tag and meta diffs. Profiled at **2.2% `buildDiff` + 1.2% `buildSnapshot`** in the same headless self-play run above. aoe2's whole test suite, playtest corpus and self-play audits are headless and never read a render diff — 3.4% of every tick in those runs is spent building one. **Ask (lower priority than the above):** skip or lazily build the diff when no `RenderAdapter` is attached, or expose a `collectRenderDiff: false` world option. Recorded per the report-don't-modify-engine policy.
- **No public way to persist or drain in-flight human commands, so a save in the ≤1-tick window before an early return drops them (surfaced 2026-07-11, full-review M4).** aoe2 human orders go through `world.submitWithResult(type, data)`, which validates AND enqueues to the engine's TRANSIENT `commandQueue` (a plain array on `World`, not part of `world.state`, so it is not serialized). The AI path avoids this by staging into aoe2's own Tier-3-persisted `pendingCommands`, but the human facades cannot: they need the SYNCHRONOUS accept/reject that `submitWithResult` returns (to drive the HUD's rejection toasts), and there is no public validate-without-enqueue — `world.validateCommand` is PRIVATE. So a human order issued in the tick before the game-menu auto-pause (which returns from `bridge.step` before `world.step()`/`processCommands` runs) is lost when Save serializes a world whose engine queue is omitted. Both clean fixes need an engine API aoe2 lacks and may not add itself: **(a)** a PUBLIC synchronous command-validate (so aoe2 can validate for the toast, then stage into its own persisted queue), OR **(b)** a PUBLIC `processCommands`/queue-drain (both are `protected` on `World`; the only public drain is a full `world.step()`, which would advance a tick at save time). **Ask:** expose either a public `validateCommand`-style precheck OR a public queue-peek/drain, so in-flight human commands can be persisted or flushed at save time without a tick advance. Narrow, low-consequence today (one dropped command in a rare ≤1-tick + menu-open window; no crash/corruption), so deferred; recorded here per the report-don't-modify-engine policy.
- **A* has no position-stable tie-break, so an equal-cost route recomputed from a different start cell can pick different intermediate cells (surfaced 2026-07-11, full-review H5).** `civ-engine/src/pathfinding.ts` orders the open set on `f` only (no secondary key on `h`/`g`/node hash) and admits a predecessor on strict `newG < prevG` (first-reach-wins), so among equal-cost 4-connected shortest paths the chosen route depends on the START node's expansion order. aoe2's bridge-side `movePathCache` is transient (not serialized), so on save/load and on `SessionReplayer.openAt` (which re-sims from the nearest 1000-tick snapshot with an EMPTY cache) a unit mid-non-straight-move recomputes a possibly-different equal-cost route than the live run committed — a transient, same-destination, intermediate-cell-only divergence that `selfCheck` structurally misses (it compares serialized state at boundaries, which excludes the cache, and routes that re-converge before the next boundary leave identical state). Proven by explicit trace: ordered (0,0)→(2,2), standing on (0,1), the committed route's next cell is (1,1) but a fresh recompute yields (0,2). **Ask:** add a position-stable secondary tie-break (e.g. break equal-`f` by `h`, then by a deterministic node hash) so a recompute from any node on a path reproduces that path's suffix — making the cache a provably pure memoization and closing the divergence for BOTH replay and save/load with no save-format change. aoe2 cannot fix this cleanly on its own (a cache codec is a save-format change; a snapshot-boundary cache reset is replay-only and couples the sim to the recorder cadence), and per policy does not modify `civ-engine` directly.
- **Terrain surfaces are per-cell voxel columns, so coastlines and kind seams cannot curve at the geometry level (observed 2026-08-17, terrain-colour/surf work).** The terrain chunk contract (`DensePaletteChunk` + `meshVisibleFaces`) gives each cell one palette entry and axis-aligned cube faces; there is no sub-cell surface tint gradient or non-cube shore meshing (marching-squares/dual-contour style). aoe2 approximates the user-requested soft boundaries render-side in v0.3.12 — per-cell colour fields (patch noise, shallow-water band, cross-kind blends) plus a connected instanced-box surf curve — which reads well but keeps the underlying colour seam square at cell resolution. **Ask (low priority, no blocker):** a sub-cell terrain surface contract (per-vertex/corner palette blending on the chunk top face, or a shore-profile mesh hook) would let waterlines and terrain transitions curve in geometry rather than by overlay. Recorded per the report-don't-modify-engine policy; the current render-side approximation is shippable.
- Engine 1.5.0/1.6.0 (visual runner hardening + improvement-loop contract completion) adopted cleanly on 2026-07-08: `minimalImprovementFindingSchemaVersion` keeps aoe2's v1-vocabulary payloads stamping schema version 1, `createImprovementRunManifest`/`assertImprovementRunManifest` back the new `playtest:recursive` pass manifests, and the strict `requireVerificationEvidence` mode guards the verified-by-metric upgrade. Two adoption notes, no asks: the widened `ImprovementNextAction` union broke a local exhaustiveness `never` guard (expected consequence of input-vocabulary widening — handled), and the constant bump flipped two local schema-version pins until minimal stamping was adopted (the engine's changelog migration note called this exactly).
- The earlier `SessionRecorder` / `SessionReplayer` `TComponents`/`TState` generics ask is RESOLVED (engine 1.2.0) **and now adopted here** — the `toEngineWorld`/`fromEngineWorld` recorder/replayer cast seam was removed and a component-typed `GameWorld` flows through recording and replay by inference (history in [past.md](./past.md)). `toEngineWorld` survives only at the `WorldDebugger` / `RenderAdapter` boundary, which 1.2.0 did not thread (those classes still take a default-generic `World<TEventMap, TCommandMap>`, and `World`'s `TComponents` is invariant).
- The previous occupancy/crowding concerns have moved to [past.md](./past.md).
- Why they moved: `civ-engine` now ships a higher-level `OccupancyBinding`, measurable occupancy scan counters, and blocker metadata that closed the earlier FU8 revisit conditions.

- **A voxel chunk's vertical step is one whole tile, so raised terrain can only be a mesa (measured 2026-08-23, raised-terrain presentation).** `VoxelChunkV1` carries `origin: Int3V1` and `size: Int3V1` with an implicit unit-size voxel and no per-chunk scale or transform, so a terrain chunk's Y resolution is exactly its X/Z resolution: one elevation level is one tile tall. AoE2's elevation step is a fraction of a tile — a hill is a slope you read at a glance and shoot down from, not a cliff. aoe2's data path for elevation is already proved end to end (the projector stamps `terrain.elevation` on every entity, the chunk builder fills the column from y=0, and units and trees already stand ON a raised tile rather than inside it), so the only thing between the current flat map and raised terrain is the vertical granularity of the chunk lane. The instance-batch lane does take float positions and sizes, but covering a whole map's raised surface with instanced boxes trades a dense-meshed chunk for thousands of parts, which is the wrong shape for terrain. **Ask:** either a per-chunk scale or non-uniform voxel extent on `VoxelChunkV1` (a `voxelExtent: Vec3V1`, defaulting to 1,1,1, would do it and keeps `origin` integral in chunk space), or a fractional-Y origin. Recorded per the report-don't-modify-engine policy; aoe2 conveys elevation by shading in the meantime, which reads as high ground but has no geometry.

- **Single-layer transparency is implemented in the engine and unreachable from `voxel/three`'s public surface (measured 2026-08-30, behind-building silhouette).** A translucent instanced part that overlaps ITSELF blends twice. The behind-building silhouette mirrors a unit's own posed parts, and a villager overlaps itself in several places — belt inside tunic, apron coplanar with the tunic face, hair over the head's crown, arms rolled into shoulders. Solved per-pixel from three captures (background, opaque, translucent) at 0.55 declared opacity: 452 pixels at 0.55, **68 at 0.7975 (= 1−0.45², two blends)** and **6 at 0.9089 (= 1−0.45³, three)** — an exact match for multi-layer compositing, and about 14% of the body area drawing at the wrong opacity, visible as seams down one arm, streaks on the torso and bands across the boots. Two units behind one building are the same batch and hit it the same way. The cause is `transparent: true` with three's default `depthWrite: true`, plus instance ordering by key STRING rather than by depth. **The engine already has the cure** — `markSingleLayerTransparencyInternal` / `createSingleLayerDepthPrepassInternal` in `voxel/src/three/singleLayerTransparencyInternal.ts`, whose own header describes this exact artefact — but it is reachable only through `THREE_MATERIAL_DECORATOR_INTERNAL`, which `voxel/three`'s index does not export. **Ask:** export the single-layer transparency decorator (or a `singleLayer: true` flag on `MaterialResourceV1`) from the public `voxel/three` surface. Until then a translucent overlay that mirrors a multi-part body cannot be drawn correctly here, and the silhouette stays opaque. Recorded per the report-don't-modify-engine policy.

- **`createOccupancyDebugProbe` cannot be attached to `OccupancyBinding`, the class the engine recommended (surfaced 2026-09-06, engine 2.4.1).** `createOccupancyDebugProbe(key, occupancy: OccupancyGrid)` (`src/world-debug-probes.ts:17`) takes the raw grid. `OccupancyBinding` holds it as `private readonly occupancy: OccupancyGrid` (`src/occupancy-binding.ts:38`), and `dist/occupancy-binding.d.ts` declares the same `private readonly occupancy` — no accessor, no overload, nothing else on the public surface hands the grid back. `getMetrics()` (`occupancy-binding.ts:380`) does forward `occupancy: this.occupancy.getMetrics()`, but those are scan counters, not the grid. So this repo, which moved to `OccupancyBinding` on the engine's own recommendation (`src/game/simulation/worldOccupancy.ts:98`; the "earlier occupancy revisit is now closed" line above is that migration), lost reach to the engine's occupancy probe by taking the engine's advice. **Ask:** a public accessor for the wrapped grid, or a `createOccupancyDebugProbe` overload that accepts an `OccupancyBinding`. Recorded per the report-don't-modify-engine policy.
- **The occupancy probe is eight scalars and cannot answer "who is blocking whom", which is how downstream docs describe it (surfaced 2026-09-06, engine 2.4.1).** Its `capture()` returns exactly `width`, `height`, `version`, `blockedCells`, `occupiedEntities`, `occupiedCells`, `reservedEntities`, `reservedCells` (`src/world-debug-probes.ts:23-34`) — a width, a height, a version and five counts. There is no per-cell and no per-entity claim in the payload. This repo's own AGENTS.md and its probe-enforcing hook both advertised the probe (with `createPathQueueDebugProbe`) as saying "who is blocking whom, which is what a freeze is" until 2026-09-06; the real freeze investigation needed three villagers' fine positions on a named cell and got them from aoe2's own `bridge.getDebugSnapshot()`. The failure mode is quiet: an agent that reaches for the advertised tool once, finds five counters, and never reaches again is behaving correctly, and the rule that sent it there gets blamed instead of the tool. **Ask:** an optional per-cell/per-entity detail mode (a bounded listing of blocked cells and their occupants would do it), or a doc line saying plainly that the probe is a counter. Recorded per the report-don't-modify-engine policy.
- **No bundle read path survives a bundle past V8's maximum string length (surfaced 2026-09-06, engine 2.4.1).** `FileSink` writes a line at a time (`appendFileSync`, `src/session-file-sink.ts:235`) but reads whole: `_readJsonlLines` does `readFileSync(path, 'utf-8')` then `raw.split('\n')` (`:244-267`), materializing both the entire file as one string and the entire parsed array before anything iterates. `BundleCorpus` inherits it (`src/bundle-corpus.ts:226` — `loadBundle: () => new FileSink(dir).toBundle()`), and the MCP server inherits it through `BundleCorpus` (`mcp/src/state.ts:18`). Measured on this side: `ticks[]` is **95.9%** of a bundle at a mean 15.7 KB per entry, a 20,001-tick bundle is 313 MB, and the 30,000-tick default of `npm run playtest` lands past V8's 536,870,888-char cap (`src/game/playtest/bundleIo.ts` header, measured 2026-09-03). So splitting the read per stream buys about **4%** and the ticks file alone still crosses. aoe2 wrote its own streaming reader and writer to get past this; the engine, corpus and MCP server have no such path. **Ask:** a line-streaming JSONL reader under `FileSink` (a generator over a read stream, not over an array read whole), so the corpus and the MCP server inherit it. Recorded per the report-don't-modify-engine policy.
- **`docs/api-reference.md` overstates that sink, and the sentence is exactly what would stop someone looking (surfaced 2026-09-06, engine 2.4.1).** Line 5180 describes `ticks()`, `commands()`, `executions()`, `failures()`, `markers()` as "lazy generators streaming the JSONL files". The generators are lazy; the read underneath is whole-file (previous item). A reader who hits the string-length wall and checks the docs is told the streaming problem is already solved. **Ask:** reword to say the generators iterate an eagerly-read array — or make the sentence true by shipping the streaming read. Recorded per the report-don't-modify-engine policy.
- **No multi-source distance field, so "distance from each of N cells to the nearest of M sinks" costs N×M searches (surfaced 2026-09-06, engine 2.4.1).** `findGridPath` (`src/path-service.ts:193`) is single-source A* over `PathConfig` (`src/pathfinding.ts`): one `start`, one `goal`. `grep -rn gridDistanceField src dist docs` in the engine returns nothing. The question is common in an RTS — how far is every resource node from its nearest drop-off, how far is every cell from the nearest of a player's buildings — and one multi-source BFS answers it in a single pass over the grid. This repo's `src/game/playtest/walkDistanceProbe.ts` does exactly that and had to write it. **Ask:** a `gridDistanceField(sources, options)` alongside `findGridPath`, over the same neighbour/cost/passability inputs, returning per-cell distance to the nearest source. Recorded per the report-don't-modify-engine policy.
- **Neither playtest runner can drive a game that wraps `world.step()` (surfaced 2026-09-06, engine 2.4.1).** `runAgentPlaytest` (`src/ai-playtester.ts:132`) and `runSynthPlaytest` (`src/synthetic-playtest.ts:155`) each own the tick loop and call `world.step()` directly (`ai-playtester.ts:226`, `synthetic-playtest.ts:267`). Between them the configs take `world`, `agent`/`policies`, `maxTicks`, `sink`, `sourceLabel`, `snapshotInterval`, `stopWhen` and `policySeed` — `stopWhen` is a predicate evaluated around the step, not a way to supply the step, and there is no other hook. aoe2's per-tick work (AI, economy, movement resolution) lives in `bridge.step(deltaMs)` around `world.step()`, so neither runner can advance an aoe2 match; `src/game/playtest/runPlaytest.ts:39-41` reimplements the loop and both engine runners have **zero call sites here**. **Ask:** an optional `advance(world): void` on both configs, defaulting to `world.step()`, so a game that wraps the tick keeps the runners' recording, stop-reason and error handling instead of copying them. Recorded per the report-don't-modify-engine policy.
- **Recorded bundles arrive with `metadata.endTick = 0` and are refused by the replayer; the attribution for it is not established (surfaced 2026-09-06, engine 2.4.1).** `scripts/replay-inspect.mjs:36-50` carries a standing repair: campaign bundles export `metadata.endTick = 0` and `durationTicks 0` while `ticks`, `executions` and `snapshots` all reach `persistedEndTick`, and `SessionReplayer.openAt` clamps to `endTick`, so without the repair the replayer refuses any tick above 0. The comment credits "an LLM-harness recording bug" — **that attribution has not been traced to a writer by anyone here**, and the stamping boundary is engine-side (`SessionRecorder`/sink), so it is as likely engine as harness. Reporting it rather than asserting a cause. **Ask:** the engine team's eyes on where `endTick` is stamped and under what termination path it can be left at 0 with a fully-recorded bundle — either a fix, or a statement that a writer must set it and the replayer should say so in its error. Recorded per the report-don't-modify-engine policy.
- **Adoption note, not an ask: the bundle corpus and the behavioural metrics family have been available since 2026-04-27 (`runMetrics`, engine 0.8.2; `BundleCorpus`, 0.8.3) and are unused here — for two different reasons, one of which is a consequence rather than an oversight.** `BundleCorpus` is directory-shaped (`new BundleCorpus(rootDir)`, each entry a `FileSink` directory), and aoe2 writes single-file JSON bundles, so the corpus has never had an input here — and adopting `FileSink` directories today would trade aoe2's own streaming reader for the sink's whole-file one, which its bundle sizes do not survive (the read-path item above). The 14-tool MCP server reads through `BundleCorpus` and is out of reach for the same reason. `runMetrics` is the one with no such excuse: it takes `Iterable<SessionBundle>` (`src/behavioral-metrics.ts:32-38`), not directories, so aoe2's own bundles could feed it today and simply never have. Fixing the streaming read would open the corpus and the MCP server together; `runMetrics` is available now.

## Current recommendation

- Continue building on top of `civ-engine`.
- The engine is not the blocker today.
- Revisit engine work only if a new integration cost appears that cannot be kept cleanly behind the current bridge.
